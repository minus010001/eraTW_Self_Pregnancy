[SKIPSTART]
;ここからまるまる廃止したい（875行目まで）
@Show_CharaData
#DIM 角色登録番号
#DIM 列表番号
#DIM 角色列表, 人物数量上限
#DIM 最大列表角色番号

;初期化
列表番号 = 0
VARSET ソート項目 , 0

;表示する角色列表を作成
VARSET 角色列表, -1
FOR 角色登録番号, 0, CHARANUM
	;出禁角色は列表入りしない(角色の実体は存在する)
	IF CFLAG:角色登録番号:出禁 == 1 || TALENT:角色登録番号:路人
		CONTINUE
	ELSE
		角色列表:列表番号 = 角色登録番号
		列表番号++
	ENDIF
NEXT
;どこまで入ってるかを明示する
最大列表角色番号 = 列表番号 - 1
DEBUGPRINTFORML {最大列表角色番号}
CALL CheckCharaData(角色列表, 最大列表角色番号)
RETURN


;この辺の入れ子を解消した方がいいんだろうか
@CheckCharaData(角色列表, 最大列表角色番号)
#DIM REF 角色列表
#DIM 列表页番号
#DIM 最大列表页番号
#DIM 最大列表角色番号

;初期化
列表页番号 = 0
最大列表页番号 = 最大列表角色番号 / 列表表示行数

;WHILEループを別関数に分離
CALL CheckCharaDataProc(角色列表, 列表页番号, 最大列表页番号, 最大列表角色番号)
RETURN


@CheckCharaDataProc(角色列表, 列表页番号, 最大列表页番号, 最大列表角色番号)
#DIM REF 角色列表
#DIM REF 列表页番号
#DIM REF 最大列表页番号
#DIM REF 最大列表角色番号
#DIM 入力値
#DIM 列表番号
#DIM 全員表示フラグ

WHILE 1
	;どちらも詳細を知りたい角色の列表番号を帰す
	;CSVで定義された番号や角色生成の時に付与される番号ではない
	IF 全員表示フラグ
		CALL DrawAllChara(角色列表, 最大列表角色番号)
	ELSE
		;一覧を表示
		CALL DrawCharaList(角色列表, 列表页番号, 最大列表页番号, 最大列表角色番号)
	ENDIF
	;RESULTに置きっぱ無にしたくないというわがまま
	入力値 = RESULT

	;列表番号以外の特殊な入力の処理
	IF 入力値 == 返回
		RETURN
	;全員表示の切り替え
	ELSEIF 入力値 == 9000
		;ビット反転し、再描画
		全員表示フラグ = !全員表示フラグ
		CONTINUE
	ENDIF
	列表番号 = 入力値
	;能力を表示
	CALL DrawCharaData(角色列表, 最大列表角色番号, 列表番号)
	
	;詳細表示から一覧に戻った際にさっきまで能力を見てた奴の页に合わせる
	列表页番号 = 列表番号 / 列表表示行数
WEND


@DrawCharaList(角色列表, 列表页番号, 最大列表页番号, 最大列表角色番号)
#DIM REF 角色列表
#DIM REF 列表页番号
#DIM REF 最大列表页番号
#DIM 页番号
#DIM 最大页数的番号
#DIM REF 最大列表角色番号
#DIM 列表番号
#DIM 入力値
#DIM CONST バー長さ = 10

;页番号の初期化はしない方がいいと言われたので削除、誤作動は知らん
	;页番号 = 0

;.ERH
最大页数的番号 = 簡易能力表示最大页数的番号

WHILE 1
	DRAWLINE
	CALL ShowStatusHeader(页番号, 最大页数的番号, 最大列表角色番号)
	DRAWLINE
	CALL ShowStatusBody(角色列表, 列表页番号, 页番号, 列表番号)
	DRAWLINE
	CALL ShowListFooter(列表页番号, 最大列表页番号)
	
	PRINTL [9000] 全員表示
	

	CALL InputListDataNum(页番号, 最大页数的番号, 列表页番号, 最大列表页番号)
	入力値 = RESULT
	SELECTCASE 入力値
		CASE 0 TO 最大列表角色番号, 9000
			RETURN 入力値
		CASE 再描画
			CONTINUE
		CASE 返回
			RETURN 返回
		CASE 2001 TO 2000 + (最大页数的番号 + 1) * 10
			CALL SORT_能力(角色列表, 页番号, 入力値, 最大列表角色番号)
			CONTINUE
		CASEELSE
			PRINTW 入力値が異常です
	ENDSELECT
WEND

THROW ループに不備があるようです


@DrawCharaData(角色列表, 最大列表角色番号, 列表番号)
#DIM REF 角色列表
#DIM REF 最大列表角色番号
#DIM REF 列表番号
#DIM 页番号
#DIM 最大页数的番号
#DIM 入力値
#DIM 素質番号
#DIM 項目数

;初期化
页番号 = 0
;.ERH
最大页数的番号 = 詳細能力表示最大页数的番号

WHILE 1
	;能力表示
	CALL PRINT_STATE(角色列表:列表番号, 页番号)

	DRAWLINE
	CALL ShowListFooter(列表番号, 最大列表角色番号)

	CALL InputListDataNum(页番号, 最大页数的番号, 列表番号, 最大列表角色番号)
	入力値 = RESULT
	
	SELECTCASE 入力値
		CASE 再描画
			CONTINUE
		CASE 返回
			RETURN 返回
		CASE 画像切换
			FLAG:画像表示 = !FLAG:画像表示
		CASE 160 TO 167
			TFLAG:入力値 = !TFLAG:入力値
		CASEELSE
	ENDSELECT
WEND

THROW ループに不備があるようです


@InputListDataNum(页番号, 最大页数的番号, 列表页番号, 最大列表页番号)
#DIM REF 页番号
#DIM REF 最大页数的番号
#DIM REF 列表页番号
#DIM REF 最大列表页番号
#DIMS 入力文字列
#DIM 入力数値

WHILE 1
	INPUTS
	入力文字列 = %RESULTS%
	PRINTL
	SELECTCASE 入力文字列
		;処理の共通部分を抜き出したいなあ
		CASE 察看前一頁 , "1100"
			页番号--
			IF 页番号 < 0
				页番号 = 最大页数的番号
			ENDIF
			RETURN 再描画
		CASE 察看下一頁 , "1200"
			页番号++
			IF 页番号 > 最大页数的番号
				页番号 = 0
			ENDIF
			RETURN 再描画
		CASE 前一位角色 , "1001"
			列表页番号--
			IF 列表页番号 < 0
				列表页番号 = 最大列表页番号
			ENDIF
			RETURN 再描画
		CASE 下一位角色 , "1002"
			列表页番号++
			IF 列表页番号 > 最大列表页番号
				列表页番号 = 0
			ENDIF
			RETURN 再描画
		CASE 放弃 , "1000"
			;帰れ
			RETURN 返回
		CASEELSE
			入力数値 = TOINT(入力文字列)
			RETURN 入力数値
	ENDSELECT
WEND


@ShowStatusHeader(页番号, 最大页数的番号, 最大列表角色番号)
#DIM 页番号
#DIM 最大页数的番号
#DIM REF 最大列表角色番号

PRINTFORM Page {页番号, 2}/{最大页数的番号, 2}    
SELECTCASE 页番号
	;体力气力
	CASE 0
		PRINT                   
		CALL SORT_能力_表示("体力")
		PRINT                   
		CALL SORT_能力_表示("気力")
	;能力(簡易)
	CASE 1
		PRINT 能力   
		CALL SORT_能力_表示("C感")
		PRINT |
		CALL SORT_能力_表示("V感")
		PRINT |
		CALL SORT_能力_表示("A感")
		PRINT |
		CALL SORT_能力_表示("B感")
		PRINT |
		CALL SORT_能力_表示("M感")
		PRINT |
		CALL SORT_能力_表示("親密")
		PRINT |
		CALL SORT_能力_表示("従順")
		PRINT |
		CALL SORT_能力_表示("欲望")
		PRINT |
		CALL SORT_能力_表示("技巧")
		PRINT |
	;性技
	CASE 2
		PRINT 性技   
		CALL SORT_能力_表示("指")
		PRINT |
		CALL SORT_能力_表示("舌")
		PRINT |
		CALL SORT_能力_表示("胸")
		PRINT |
		CALL SORT_能力_表示("腰")
		PRINT |
		CALL SORT_能力_表示("膣")
		PRINT |
		CALL SORT_能力_表示("A")
		PRINT |
	;刻印
	CASE 3
		PRINT 刻印   
		CALL SORT_能力_表示("苦痛")
		PRINT |
		CALL SORT_能力_表示("快楽")
		PRINT |
		CALL SORT_能力_表示("不埒")
		PRINT |
		CALL SORT_能力_表示("反発")
		PRINT |
		CALL SORT_能力_表示("時姦")
		PRINT |
	;好感度他
	CASE 4
		PRINT        
		CALL SORT_能力_表示("好感")
		PRINT |
		CALL SORT_能力_表示("信頼")
		PRINT |
	CASEELSE
		PRINTFORM 変数が異常値です"页番号 = {页番号}"
ENDSELECT
PRINTL


@ShowStatusBody(角色列表, 列表页番号, 页番号, 列表番号)
#DIM REF 角色列表
#DIM REF 列表页番号
#DIM 页番号
#DIM 列表番号
#DIM CONST バー長さ = 10
#DIM 表示ステータス番号
#DIM 角色登録番号
#DIMS 表示文

FOR 列表番号, 列表页番号 * 列表表示行数, (列表页番号 + 1) * 列表表示行数

	;配列の同じ要素を何度も書くのが見苦しかったのでまとめる
	角色登録番号 = 角色列表:列表番号

	IF 角色登録番号 == -1
		PRINTL [   ]
		CONTINUE
	ENDIF
	PRINTFORM [{列表番号, 3}] %CALLNAME:角色登録番号, 名字显示长度, LEFT% 
	SELECTCASE 页番号
		;体力气力
		;空きスペースに二つ名とか入れると楽しいけどCSVいじるのめんどいのでパス
		CASE 0
			CALL PRINT_COLORBAR, BASE:角色登録番号:体力, MAXBASE:角色登録番号:体力, バー長さ, UNICODE(0x2585), UNICODE(0x2585), BARCOLORSET("緑"), RESULT:1
			PRINTFORM  ({BASE:角色登録番号:体力,5}/{MAXBASE:角色登録番号:体力,5}) 
			CALL PRINT_COLORBAR, BASE:角色登録番号:気力, MAXBASE:角色登録番号:気力, バー長さ, UNICODE(0x2585), UNICODE(0x2585), BARCOLORSET("青"), RESULT:1
			PRINTFORM  ({BASE:角色登録番号:気力,5}/{MAXBASE:角色登録番号:気力,5})
		;能力(簡易)
		;各感覚と親密従順欲望技巧のみ
		CASE 1
			;超随意つき修正の余地あり
			FOR 表示ステータス番号, 0, 13
				IF ABLNAME:表示ステータス番号 != ""
					CALL PRINT_字母表顺序評価(角色登録番号, 4, 表示ステータス番号)
					PRINTFORM {ABL:角色登録番号:表示ステータス番号, 4}|
				ENDIF
			NEXT
		;性技
		CASE 2
			;超随意つき修正の余地あり
			FOR 表示ステータス番号, 50, 56
				IF ABLNAME:表示ステータス番号 != ""
					CALL PRINT_字母表顺序評価(角色登録番号, 3, 表示ステータス番号)
					PRINTFORM {ABL:角色登録番号:表示ステータス番号, 3}|
				ENDIF
			NEXT
		;刻印
		CASE 3
			;超随意つき修正の余地あり
			FOR 表示ステータス番号, 0, 6
				IF MARKNAME:表示ステータス番号 == ""
					CONTINUE
				ENDIF
				;反発刻印履歴は表示不会うに
				SIF 表示ステータス番号 == 4
					CONTINUE
				IF 表示ステータス番号 == 3
					SETCOLOR C_RED
					PRINTFORM %"×" * (MARK:角色登録番号:表示ステータス番号), 6%|
				ELSE
					SETCOLOR 255, 255, 0
					PRINTFORM %"★" * (MARK:角色登録番号:表示ステータス番号), 6%|
				ENDIF
				RESETCOLOR
			NEXT
		;好感度他
		;スペースの取り方は超適当
		CASE 4
			;好感度
			CALL PRINT_字母表顺序評価(角色登録番号, 1, )
			PRINTFORM {CFLAG:角色登録番号:好感度, 6}|
			
			;信赖度
			CALL PRINT_字母表顺序評価(角色登録番号, 2, )
			PRINTFORM {CFLAG:角色登録番号:信赖度, 6}|
			
			;恋慕
			IF TALENT:角色登録番号:恋人
				SETCOLOR C_RED
				表示文 = 恋人
			ELSEIF TALENT:角色登録番号:恋慕
				SETCOLOR 0xEE60EE
				表示文 = 恋慕
			ELSEIF TALENT:角色登録番号:思慕 == 1
				表示文 = 思慕
			ELSE
				表示文 = 
			ENDIF
			PRINTFORM %表示文, 4, LEFT%|
			RESETCOLOR
			
			;既成事實
			SELECTCASE CFLAG:(角色登録番号):允许无套
				CASE 2
					表示文 = 危険日生Ｈ解禁
				CASE 1
					表示文 = 生Ｈ解禁
				CASE 0
					IF GETBIT(CFLAG:(角色登録番号):既成事實,1)
						表示文 = 合意取得
					ELSE
						表示文 = 
					ENDIF
			ENDSELECT
			PRINTFORM %表示文, 14, LEFT%|
			
			;生理周期可視化
			SIF FLAG:1007
				CALL PRINT_CYCLE_翌日(角色登録番号)

			SIF CFLAG:(角色登録番号):出産経過日 > 0
				PRINTFORM 育児中 
			RESETCOLOR
			SIF GETBIT(CFLAG:(角色登録番号):既成事實,2)
				PRINTFORM 恋人同士 
			;把柄
			IF CFLAG:(角色登録番号):掌握把柄 && CFLAG:(角色登録番号):被掌握把柄
				PRINTFORM %CALLNAME:MASTER%抓住了彼此的把柄 
			ELSEIF CFLAG:(角色登録番号):掌握把柄
				PRINTFORM 抓住了%CALLNAME:MASTER%的把柄 
			;SIF GETBIT(CFLAG:(角色登録番号):把柄,0)
			;	PRINTFORM ぱんつを没収された 
			;SIF GETBIT(CFLAG:(角色登録番号):把柄,1)
			;	PRINTFORM 情事を見られた 
			ELSEIF CFLAG:(角色登録番号):被掌握把柄
				PRINTFORM 被%CALLNAME:MASTER%目撃过自慰现场
			ENDIF
	ENDSELECT
	PRINTL
NEXT


@ShowListFooter(列表页番号, 最大列表页番号)
#DIM 列表页番号
#DIM 最大列表页番号

IF FLAG:能力表記
	PRINTBUTTONLC @"[1100] - 察看前一頁", 察看前一頁
	PRINTBUTTONLC @"[1000] - 返回", 放弃
	PRINTBUTTONLC @"[1200] - 察看下一頁", 察看下一頁
	PRINTL
	PRINTBUTTONLC @"[1001] - 前一位角色", 前一位角色
	PRINTFORMLC 列表 {列表页番号}/{最大列表页番号}
	PRINTBUTTONLC @"[1002] - 下一位角色", 下一位角色

ELSE
	PRINTBUTTONLC @"[%察看前一頁%] - 察看前一頁", 察看前一頁
	PRINTBUTTONLC @"[%放弃%] - 放弃", 放弃
	PRINTBUTTONLC @"[%察看下一頁%] - 察看下一頁", 察看下一頁
	PRINTL
	PRINTBUTTONLC @"[%前一位角色%] - 前一位角色", 前一位角色
	PRINTFORMLC 列表 {列表页番号}/{最大列表页番号}
	PRINTBUTTONLC @"[%下一位角色%] - 下一位角色", 下一位角色
ENDIF


@DrawAllChara(角色列表, 最大列表角色番号)
#DIM REF 角色列表
#DIM REF 最大列表角色番号
#DIM 角色列表番号
#DIM 入力値

WHILE 1
	;FOR文が条件を満たした時点で終了するので+1
	;一番最後一位角色が表示されないことへの対応
	FOR 角色列表番号, 0, 最大列表角色番号 + 1
		PRINTFORMLC [{角色列表番号}] %CALLNAME:(角色列表:角色列表番号),15,LEFT%
	NEXT
	PRINTL
	DRAWLINE
	PRINTL [9000] 回到列表
	PRINTL [1000] - 中止

	INPUT
	入力値 = RESULT
	SELECTCASE 入力値
		CASE 1000
			RETURN 返回
		CASE 0 TO 最大列表角色番号, 9000
			RETURN 入力値
	ENDSELECT
WEND


;実際に能力のソートを行う
@SORT_能力(角色列表, 页番号, 入力値, 最大列表角色番号)
#DIM REF 角色列表
#DIM REF 页番号
#DIM REF 最大列表角色番号
#DIM 入力値
#DIM 参照する能力
#DIM 角色登録番号
#DIM 列表番号


入力値 -= 2000
参照する能力 = 入力値

;降序にする場合
IF ソート項目:入力値 == 0
	SELECTCASE 参照する能力
		;体力气力
		CASE 1 TO 2
			参照する能力 --
		;Ｃ感覚からＭ感覚まで
		CASE 11 TO 15
			参照する能力 -= 11
		;親密従順欲望技巧
		CASE 16 TO 19
			参照する能力 -= 7
		;性技
		CASE 21 TO 26
			参照する能力 += 29
		;刻印(時姦刻印以外)
		CASE 31 TO 34
			参照する能力 -= 31
		;刻印(時姦刻印)
		CASE 35
			参照する能力 = 5
		;好感度
		CASE 41
			参照する能力 = 2
		;信赖度
		CASE 42
			参照する能力 = 4
		CASEELSE
			PRINTW 変数値が異常です(能力表示.ERB @SORT_能力)
	ENDSELECT
	
	;見る場所がBASEだったりABLだったりMARKだったりするので页で分けて分かりやすく
	;0页目の場合(参照部分BASE)
	IF 页番号 == 0
		FOR LOCAL:0,1,最大列表角色番号
			FOR LOCAL:1,LOCAL:0 +1,最大列表角色番号+1
				IF BASE:(角色列表:(LOCAL:0)):(参照する能力) < BASE:(角色列表:(LOCAL:1)):(参照する能力)
					LOCAL:2 = 角色列表:(LOCAL:0)
					角色列表:(LOCAL:0) = 角色列表:(LOCAL:1)
					角色列表:(LOCAL:1) = LOCAL:2
				ENDIF
			NEXT
		NEXT
	;1页目の場合(参照部分ABL)
	ELSEIF 页番号 == 1
		FOR LOCAL:0,1,最大列表角色番号
			FOR LOCAL:1,LOCAL:0 +1,最大列表角色番号+1
				IF ABL:(角色列表:(LOCAL:0)):(参照する能力) < ABL:(角色列表:(LOCAL:1)):(参照する能力)
					LOCAL:2 = 角色列表:(LOCAL:0)
					角色列表:(LOCAL:0) = 角色列表:(LOCAL:1)
					角色列表:(LOCAL:1) = LOCAL:2
				ENDIF
			NEXT
		NEXT
	;2页目の場合(参照部分ABL)
	ELSEIF 页番号 == 2
		FOR LOCAL:0,1,最大列表角色番号
			FOR LOCAL:1,LOCAL:0 +1,最大列表角色番号+1
				IF ABL:(角色列表:(LOCAL:0)):(参照する能力) < ABL:(角色列表:(LOCAL:1)):(参照する能力)
					LOCAL:2 = 角色列表:(LOCAL:0)
					角色列表:(LOCAL:0) = 角色列表:(LOCAL:1)
					角色列表:(LOCAL:1) = LOCAL:2
				ENDIF
			NEXT
		NEXT
	;3页目の場合(参照部分MARK)
	ELSEIF 页番号 == 3
		FOR LOCAL:0,1,最大列表角色番号
			FOR LOCAL:1,LOCAL:0 +1,最大列表角色番号+1
				IF MARK:(角色列表:(LOCAL:0)):(参照する能力) < MARK:(角色列表:(LOCAL:1)):(参照する能力)
					LOCAL:2 = 角色列表:(LOCAL:0)
					角色列表:(LOCAL:0) = 角色列表:(LOCAL:1)
					角色列表:(LOCAL:1) = LOCAL:2
				ENDIF
			NEXT
		NEXT
	;4页目の場合(参照部分CFLAG)
	ELSEIF 页番号 == 4
		FOR LOCAL:0,1,最大列表角色番号
			FOR LOCAL:1,LOCAL:0 +1,最大列表角色番号+1
				IF CFLAG:(角色列表:(LOCAL:0)):(参照する能力) < CFLAG:(角色列表:(LOCAL:1)):(参照する能力)
					LOCAL:2 = 角色列表:(LOCAL:0)
					角色列表:(LOCAL:0) = 角色列表:(LOCAL:1)
					角色列表:(LOCAL:1) = LOCAL:2
				ENDIF
			NEXT
		NEXT
	ENDIF
;降序から升序に切り替える場合
ELSEIF ソート項目:入力値 == 1
	FOR LOCAL:0,1,最大列表角色番号 / 2 + 1
		LOCAL:1 = 角色列表:(LOCAL:0)
		角色列表:(LOCAL:0) = 角色列表:(最大列表角色番号 - LOCAL:0 + 1)
		角色列表:(最大列表角色番号 - LOCAL:0 + 1) = LOCAL:1
	NEXT
ELSE
	列表番号 = 0
	FOR 角色登録番号, 0, CHARANUM
	;出禁角色は列表入りしない(角色の実体は存在する)
	IF CFLAG:角色登録番号:出禁 == 1
		CONTINUE
	ELSE
		角色列表:列表番号 = 角色登録番号
		列表番号++
	ENDIF
NEXT
ENDIF

A = ソート項目:入力値

VARSET ソート項目 , 0

ソート項目:入力値 = A + 1
SIF ソート項目:入力値 == 3
	ソート項目:入力値 = 0


;ShowStatusHeader内でどのように表示させるか
@SORT_能力_表示(能力)

#DIMS 能力
#DIM 順番

CALL SHOW_SORT_昇降序(能力)
順番 = RESULT

SELECTCASE 能力
	CASE "体力"
		IF 順番 == 1
			PRINTBUTTON @"▼体力", 2001
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲体力", 2001
		ELSE
			PRINTBUTTON @"  体力", 2001
		ENDIF
	CASE "気力"
		IF 順番 == 1
			PRINTBUTTON @"▼気力", 2002
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲気力", 2002
		ELSE
			PRINTBUTTON @"  気力", 2002
		ENDIF
	CASE "C感"
		IF 順番 == 1
			PRINTBUTTON @" ▼C感", 2011
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲C感", 2011
		ELSE
			PRINTBUTTON @"   C感", 2011
		ENDIF
	CASE "V感"
		IF 順番 == 1
			PRINTBUTTON @" ▼V感", 2012
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲V感", 2012
		ELSE
			PRINTBUTTON @"   V感", 2012
		ENDIF
	CASE "A感"
		IF 順番 == 1
			PRINTBUTTON @" ▼A感", 2013
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲A感", 2013
		ELSE
			PRINTBUTTON @"   A感", 2013
		ENDIF
	CASE "B感"
		IF 順番 == 1
			PRINTBUTTON @" ▼B感", 2014
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲B感", 2014
		ELSE
			PRINTBUTTON @"   B感", 2014
		ENDIF
	CASE "M感"
		IF 順番 == 1
			PRINTBUTTON @" ▼M感", 2015
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲M感", 2015
		ELSE
			PRINTBUTTON @"   M感", 2015
		ENDIF
	CASE "親密"
		IF 順番 == 1
			PRINTBUTTON @"▼親密", 2016
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲親密", 2016
		ELSE
			PRINTBUTTON @"  親密", 2016
		ENDIF
	CASE "従順"
		IF 順番 == 1
			PRINTBUTTON @"▼従順", 2017
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲従順", 2017
		ELSE
			PRINTBUTTON @"  従順", 2017
		ENDIF
	CASE "欲望"
		IF 順番 == 1
			PRINTBUTTON @"▼欲望", 2018
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲欲望", 2018
		ELSE
			PRINTBUTTON @"  欲望", 2018
		ENDIF
	CASE "技巧"
		IF 順番 == 1
			PRINTBUTTON @"▼技巧", 2019
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲技巧", 2019
		ELSE
			PRINTBUTTON @"  技巧", 2019
		ENDIF
	CASE "指"
		IF 順番 == 1
			PRINTBUTTON @" ▼指", 2021
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲指", 2021
		ELSE
			PRINTBUTTON @"   指", 2021
		ENDIF
	CASE "舌"
		IF 順番 == 1
			PRINTBUTTON @" ▼舌", 2022
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲舌", 2022
		ELSE
			PRINTBUTTON @"   舌", 2022
		ENDIF
	CASE "胸"
		IF 順番 == 1
			PRINTBUTTON @" ▼胸", 2023
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲胸", 2023
		ELSE
			PRINTBUTTON @"   胸", 2023
		ENDIF
	CASE "腰"
		IF 順番 == 1
			PRINTBUTTON @" ▼腰", 2024
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲腰", 2024
		ELSE
			PRINTBUTTON @"   腰", 2024
		ENDIF
	CASE "膣"
		IF 順番 == 1
			PRINTBUTTON @" ▼膣", 2025
		ELSEIF 順番 == 2
			PRINTBUTTON @" ▲膣", 2025
		ELSE
			PRINTBUTTON @"   膣", 2025
		ENDIF
	CASE "A"
		IF 順番 == 1
			PRINTBUTTON @"  ▼A", 2026
		ELSEIF 順番 == 2
			PRINTBUTTON @"  ▲A", 2026
		ELSE
			PRINTBUTTON @"    A", 2026
		ENDIF
	CASE "苦痛"
		IF 順番 == 1
			PRINTBUTTON @"▼苦痛", 2031
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲苦痛", 2031
		ELSE
			PRINTBUTTON @"  苦痛", 2031
		ENDIF
	CASE "快楽"
		IF 順番 == 1
			PRINTBUTTON @"▼快楽", 2032
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲快楽", 2032
		ELSE
			PRINTBUTTON @"  快楽", 2032
		ENDIF
	CASE "不埒"
		IF 順番 == 1
			PRINTBUTTON @"▼不埒", 2033
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲不埒", 2033
		ELSE
			PRINTBUTTON @"  不埒", 2033
		ENDIF
	CASE "反発"
		IF 順番 == 1
			PRINTBUTTON @"▼反発", 2034
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲反発", 2034
		ELSE
			PRINTBUTTON @"  反発", 2034
		ENDIF
	CASE "時姦"
		IF 順番 == 1
			PRINTBUTTON @"▼時姦", 2035
		ELSEIF 順番 == 2
			PRINTBUTTON @"▲時姦", 2035
		ELSE
			PRINTBUTTON @"  時姦", 2035
		ENDIF
	CASE "好感"
		IF 順番 == 1
			PRINTBUTTON @"  ▼好感", 2041
		ELSEIF 順番 == 2
			PRINTBUTTON @"  ▲好感", 2041
		ELSE
			PRINTBUTTON @"    好感", 2041
		ENDIF
	CASE "信頼"
		IF 順番 == 1
			PRINTBUTTON @"  ▼信頼", 2042
		ELSEIF 順番 == 2
			PRINTBUTTON @"  ▲信頼", 2042
		ELSE
			PRINTBUTTON @"    信頼", 2042
		ENDIF
ENDSELECT

@SHOW_SORT_昇降序(能力)
#DIMS 能力
#DIM 表示能力

SELECTCASE 能力
	CASE "体力"
		表示能力 = 1
	CASE "気力"
		表示能力 = 2
	CASE "C感"
		表示能力 = 11
	CASE "V感"
		表示能力 = 12
	CASE "A感"
		表示能力 = 13
	CASE "B感"
		表示能力 = 14
	CASE "M感"
		表示能力 = 15
	CASE "親密"
		表示能力 = 16
	CASE "従順"
		表示能力 = 17
	CASE "欲望"
		表示能力 = 18
	CASE "技巧"
		表示能力 = 19
	CASE "指"
		表示能力 = 21
	CASE "舌"
		表示能力 = 22
	CASE "胸"
		表示能力 = 23
	CASE "腰"
		表示能力 = 24
	CASE "膣"
		表示能力 = 25
	CASE "A"
		表示能力 = 26
	CASE "苦痛"
		表示能力 = 31
	CASE "快楽"
		表示能力 = 32
	CASE "不埒"
		表示能力 = 33
	CASE "反発"
		表示能力 = 34
	CASE "時姦"
		表示能力 = 35
	CASE "好感"
		表示能力 = 41
	CASE "信頼"
		表示能力 = 42
	CASEELSE
		PRINTW 異常な値を参照しています(能力表示.ERB @SHOW_SORT_昇降序)
ENDSELECT
RETURN ソート項目:表示能力
[SKIPEND]


[SKIPSTART]
角色列表表示関数
列表上の角色を一覧で表示し列表の切替も可能

OPはSTRCOUNT使用で各種の機能を切り替える
・「情報表示」
　このOPを適用する場合、番号クリックで角色情報が表示される
　適用しない場合、戻り値としてクリックした角色の登録番号を返す
・「情報固定」
　このOPを適用する場合、DISP_TYPE（一行表示での表示内容）が変更できない
・「条件変更」
　このOPを適用する場合、関数内で列表の種類を切り替えることができる
・「最終確認」
　「情報表示」を適用しないと意味が無い
　情報表示画面で表示中の角色を候補として返すことができるようになる
　決定/中止が可能

CHARA_LISTはプライベートで保持しているため
この関数からCALL/JUMPした関数にREFで渡すことは可能だが
この関数の呼び出し元の関数に対して渡すことは不可能である

一行表示の表示項目一覧、わかりやすいように数字を併記しておく
	 0"体力",  1"気力", 
	 2"Ｃ感",  3"Ｖ感",  4"Ａ感",  5"Ｂ感",  6"Ｍ感", 
	 7"親密",  8"従順",  9"欲望", 10"技巧", 
	11"指技", 12"舌技", 13"胸技", 14"腰技", 15"膣技", 16"Ａ技", 
	17"苦痛", 18"快楽", 19"不埒", 20"反発", 21"時姦", 
	22"好感", 23"信頼", 24"欲求"
[SKIPEND]
@SHOW_CHARA_LIST(LIST_TYPE = "现有的角色列表", DISP_TYPE_STR = "体力气力", OP)
#DIMS DYNAMIC LIST_TYPE
#DIMS DYNAMIC DISP_TYPE_STR
#DIM  DYNAMIC DISP_TYPE
#DIMS DYNAMIC OP
#DIM  DYNAMIC GET_LIST = 1
#DIM  DYNAMIC L_ID
#DIM  DYNAMIC L_CLASS_ID
#DIM  DYNAMIC CHARA_LIST , 人物数量上限
#DIM  DYNAMIC CHARA_CNT
#DIMS DYNAMIC INPUT_STR
#DIM  DYNAMIC INPUT_INT
#DIM  DYNAMIC NOW_GROUP = 1
#DIM  DYNAMIC LAST_GROUP
#DIM  DYNAMIC SORT_TYPE
#DIM  DYNAMIC CREDRAW
#DIM  DYNAMIC FIRST_LINE
#DIMS DYNAMIC DISP_NAME = "体力气力", "敏感度", "性技", "刻印", "技能", "好感", "胜利等"

IF 高级列表功能
	CALL SHOW_CHARA_LIST_DT(LIST_TYPE, DISP_TYPE_STR, OP)
	RETURN RESULT
ENDIF

;情報表示用
DISP_TYPE = FINDELEMENT(DISP_NAME, DISP_TYPE_STR)
SIF DISP_TYPE == -1
	DISP_TYPE = 0

FIRST_LINE = LINECOUNT
$REPRINT
CLEARLINE LINECOUNT - FIRST_LINE
CREDRAW = CURRENTREDRAW()
REDRAW 0

IF GET_LIST || ResetList
	CALL MAKE_CHARA_LIST(LIST_TYPE, L_CLASS_ID, CHARA_LIST, CHARA_CNT)
	GET_LIST = 0
	SORT_TYPE = -1
	ResetList = 0
ENDIF
DRAWLINE

;項目なし
IF CHARA_CNT <= 0
	PRINTFORML ＜无符合角色＞　%LIST_TYPE%
	PRINTFORML 
	DRAWLINE
	FOR LOCAL, 0, 40
		PRINTL
	NEXT
;項目あり
ELSE
;現在のタイプの総グループ数を更新
	LAST_GROUP = 1 + (CHARA_CNT - 1) / 40
	PRINTFORML 　%LIST_TYPE%　（符合条件的角色：{CHARA_CNT}名　{NOW_GROUP}/{LAST_GROUP}）　名字有颜色的角色是有剧本的
	PRINTFORM 
	CALL QOL_CHARA_BUTTON(-1, , DISP_TYPE, SORT_TYPE)
	DRAWLINE
	FOR L_ID, (NOW_GROUP - 1) * 40, NOW_GROUP * 40
		IF INRANGE(L_ID, 0, CHARA_CNT - 1)
			CALL QOL_CHARA_BUTTON(L_ID, CHARA_LIST:L_ID, DISP_TYPE, SORT_TYPE)
		ELSE
			PRINTL
		ENDIF
	NEXT
ENDIF
DRAWLINE


IF STRCOUNT(OP, "「情報固定」")
	PRINTFORM %" "*20%
ELSE
	PRINTBUTTON @"← %DISP_NAME:((DISP_TYPE - 1 + VARSIZE("DISP_NAME")) % VARSIZE("DISP_NAME")), 20, LEFT%", 444
ENDIF

IF LAST_GROUP == 1
	SETCOLOR 0x606060
	PRINTFORM %"上一页",15,LEFT%
	RESETCOLOR 
ELSE
	PRINTBUTTON @"%"上一页",15,LEFT%", 888
ENDIF
PRINTFORM 
IF LAST_GROUP == 1
	SETCOLOR 0x606060
	PRINTFORM %"下一页",15,LEFT%
	RESETCOLOR 
ELSE
	PRINTBUTTON @"%"下一页",15,LEFT%", 222
ENDIF
IF STRCOUNT(OP, "「情報固定」")
	PRINTFORM 　　　　　　　　　　　
ELSE
	PRINTBUTTON @"%DISP_NAME:((DISP_TYPE + 1 + VARSIZE("DISP_NAME")) % VARSIZE("DISP_NAME")), 20, LEFT% →", 666
ENDIF
PRINTFORML 

PRINTFORML 
PRINTBUTTON "[999] 返回　", 999
PRINTFORM                
SIF STRCOUNT(OP, "「条件変更」")
	PRINTBUTTON "[555] 切换筛选条件", 555
IF FLAG:画像表示
	PRINTFORM %" "*15%
	PRINTBUTTON "[777] 查看花名册", 777
ENDIF
PRINTFORML 


INPUT
SELECTCASE RESULT
	;先にシステム系を処理、222なら余裕あるっしょ（Kでは使えんわな）
	;modded to have infinite scroll + tweaked goto's to prevent crashing when entering incorrect values
	CASE 222, 888
		SIF LAST_GROUP == 1
			GOTO REPRINT
		NOW_GROUP = PAGE_HANDLE(RESULT, 888, NOW_GROUP, 1, LAST_GROUP)
		GOTO REPRINT
	CASE 444
		SIF STRCOUNT(OP, "「情報固定」")
			GOTO RETRY
		DISP_TYPE = (DISP_TYPE - 1 + VARSIZE("DISP_NAME")) % VARSIZE("DISP_NAME")
		GOTO REPRINT
	CASE 555
		SIF !STRCOUNT(OP, "「条件変更」")
			GOTO RETRY
		CALL CHANGE_LIST(LIST_TYPE, OP)
		GET_LIST = 1
		NOW_GROUP = 1
		GOTO REPRINT
	CASE 666
		SIF STRCOUNT(OP, "「情報固定」")
			GOTO RETRY
		DISP_TYPE = (DISP_TYPE + 1 + VARSIZE("DISP_NAME")) % VARSIZE("DISP_NAME")
		GOTO REPRINT
	CASE 999
		;REFで配列作れば多キャラ選択も行けそう
		;受け渡しはともかくとして表示まわりがめんどいか
		GET_LIST = 1
		NOW_GROUP = 1
		RETURN 999
	CASE SORT_BTN_BASE TO (SORT_BTN_BASE + SORT_BTN_DESC_OFFSET + VARSIZE("DISP_MEMO"))
		SORT_TYPE = RESULT
		CALL QOL_SORT_LIST(RESULT, CHARA_LIST, CHARA_CNT)
		GOTO REPRINT
	CASE 777
		SIF FIRSTTIME("花名册警告",,0)
			CALL 花名册警告
		SIF !FLAG:画像表示
			GOTO REPRINT
		CALL QOL_FULL_ROSTER
		GOTO REPRINT
	CASEELSE
	;キャラが選択された時
		IF !INRANGE(RESULT, MAX(0, (NOW_GROUP - 1) * 40), MIN(CHARA_CNT - 1, NOW_GROUP * 40))
			PRINTFORMW {RESULT}超出范围!
			GOTO REPRINT
		ELSEIF L_CLASS_ID == -1 && !CHECK_CHARA(CHARA_LIST:RESULT, LIST_TYPE)
			PRINTFORMW {RESULT}不是有效的列表!
			GOTO REPRINT
		ELSEIF L_CLASS_ID > 0 && !GETMETH_INT(CHARA_LIST:INPUT_INT, "角色列表", L_CLASS_ID, "判定")
			PRINTFORMW {RESULT}不在列表中!
			GOTO REPRINT
		ENDIF
;ここだけDO-LOOPぬけます
ENDSELECT
$RETRY

IF STRCOUNT(OP, "「情報表示」")
	CALL LIST_CHARA_STATE(CHARA_LIST, RESULT, CHARA_CNT, OP)
	SIF !RESULT
		GOTO REPRINT
	NOW_GROUP = 1
	GET_LIST = 1
;一応RESULTが正になるのは「最終確認」のみだが念のために判定
	SIF STRCOUNT(OP, "「最終確認」")
		RETURN RESULT
ELSE
	NOW_GROUP = 1
	GET_LIST = 1
	RETURN CHARA_LIST:RESULT
ENDIF



;使用列表の切替関数
;てぬき
;現状では不要だけどOPを渡してあるので"SHOP表示"以外への切替も容易です
@CHANGE_LIST(LIST_TYPE, OP)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS REF LIST_TYPE;現在使用中の表示内容
#DIMS OP           ;実行オプション
PRINTFORML 更改筛选条件（现在：%LIST_TYPE%）
PRINTFORML 
CALL CLASS_LIST("角色列表", "SHOP表示")
PRINTFORML 
PRINTFORML [999] - 返回
DO
	INPUT
	SELECTCASE RESULT
	CASE 999
		RETURN 0
	CASEELSE
		IF !STRLENS(GETMETH_STR(0, "角色列表", RESULT, "名前")) 
		ELSEIF GETMETH_INT(0, "角色列表", RESULT, "SHOP表示")
			IF LIST_TYPE == GETMETH_STR(0, "角色列表", RESULT, "名前")
				;もし現在の設定値と新しい設定値が同じなら変更は無かったと見なす
				RETURN 0
			ELSE
				LIST_TYPE '= GETMETH_STR(0, "角色列表", RESULT, "名前")
				RETURN 1
			ENDIF
		ENDIF
		REUSELASTLINE 无效输入
		CONTINUE
	ENDSELECT
LOOP 1



;単角色情報表示関数
;主に行動中のMASTERの情報表示用
@SINGLE_CHARA_STATE(C_ID, OP)
#DIM  C_ID ;登録番号
#DIMS OP   ;実行オプション
#DIM  PAGE ;表示中の页
#DIMS CONST DISP_NAME = "服装&能力", "经验&宝珠", "个人情报", "个人好恶", "身体情报", "陷落状态"
#DIM CREDRAW
#DIM DYNAMIC FIRST_LINE
FIRST_LINE = LINECOUNT
$REPRINT
CLEARLINE LINECOUNT - FIRST_LINE
CREDRAW = CURRENTREDRAW()
REDRAW 0
CALL CHARA_STATE(C_ID, PAGE, OP)
DRAWLINE
PRINTFORM 　　　　　
FOR LOCAL, 0, VARSIZE("DISP_NAME")
	SIF PAGE == LOCAL
		SETCOLOR 0x00FFFF
	;exception for player character
	PRINTBUTTON C_ID == MASTER && LOCAL == 5 ? "[技能习得]" # "["+DISP_NAME:LOCAL+"]", LOCAL + 2
	RESETCOLOR
	PRINTFORM 　　
NEXT
PRINTFORML 
IF STRCOUNT(OP, "「最終確認」")
	PRINTBUTTON "[1] 选择此角色", 1
	PRINTFORM 　　　　　　　　　
	PRINTBUTTON "[0] 返回", 0
	PRINTFORML 
ELSE
	PRINTBUTTON "[0] 返回", 0
ENDIF
DO
	INPUT
	SELECTCASE RESULT
	CASE 0
		RETURN 0
	CASE 1
		SIF STRCOUNT(OP, "「最終確認」")
			RETURN C_ID
		GOTO RETRY
	CASE 2 TO 7
		PAGE = RESULT-2
	CASE 98
		CFLAG:C_ID:差し替え適用 ++
		SIF CFLAG:C_ID:別顔有 < CFLAG:C_ID:差し替え適用
			CFLAG:C_ID:差し替え適用 = 0
		;qol custom code, combine face and standing images
		; IF FLAG:立ち絵種類
		; 	SIF CFLAG:C_ID:別顔有 < CFLAG:C_ID:差し替え適用
		; 		CFLAG:C_ID:差し替え適用 = 0
		; ELSE
		; 	SIF CFLAG:C_ID:別立ち有 < CFLAG:C_ID:差し替え適用
		; 		CFLAG:C_ID:差し替え適用 = 0
		; ENDIF
	;表示・非表示の切替
	CASE 160 TO 169, 900, 904, 909, 914, 915 ;custom code
		TFLAG:RESULT = !TFLAG:RESULT
	;qol custom code, disable here
	; CASE 画像切り替え
	; 	IF !FLAG:画像表示
	; 		FLAG:画像表示 = !FLAG:画像表示
	; 		SIF FLAG:画像表示 && nStoreImageCount > 0 ;qol custom code, restore previous value if enabled
	; 			FLAG:画像表示 = nStoreImageCount
			
	; 		FLAG:立ち絵種類 = 0
	; 		CFLAG:C_ID:差し替え適用 = 0 ;custom code
	; 	ELSEIF !FLAG:立ち絵種類
	; 		FLAG:立ち絵種類 = !FLAG:立ち絵種類
	; 	ELSEIF FLAG:立ち絵種類
	; 		SIF FLAG:画像表示 ;qol custom code, store previous value if it's going to be disabled
	; 			nStoreImageCount = FLAG:画像表示
				
	; 		FLAG:画像表示 = !FLAG:画像表示
	; 	ENDIF
	CASE 9997 ;custom code
		CALL Qol_Info_ChildList(C_ID, 1)
		WAIT
	CASE 9998 ;custom code
		CALL Qol_MC_CHILD_LIST
		;switch to a mother's page when clicked
		IF INRANGE(RESULT, 10000, 11000)
			RESULT -= 10000
			IF EXISTCSV(NO:RESULT)
				PAGE = 0
				C_ID = RESULT
			ENDIF
		ENDIF
		BREAK
	CASE 9999 ;custom code
		qol_Relation_AscendingOrder = !qol_Relation_AscendingOrder
	CASE 3500 TO 3505 ;Falling Condition Tab
		SFSR_PAGE = RESULT-3500
	; BBAS ALTER statusDisplay
	CASE 89999
		ALTER_statusDisplay = !ALTER_statusDisplay
	CASE 90000 TO 90005
		CALL CHECK_BODYGRAPH_RECORD("set",RESULT-90000)
	CASEELSE
		$RETRY
		REUSELASTLINE 无效输入
		CONTINUE
	ENDSELECT
	BREAK
LOOP 1
GOTO REPRINT



;調教中ターゲット用の列表表示関数
;ふつうにTARGETを渡すとTARGET:0が重複してしまうのでそのままでは使えなかった
;列表にMASTERを含めるように改良
@TARGET_LIST_STATE
#DIM TARGET_LIST, 人物数量上限
#DIM TARGET_NUM
#DIM VIEW
VARSET TARGET_LIST
TARGET_NUM = FINDELEMENT(TARGET, 0) - 1
VIEW = TARGET
SIF TARGET_NUM <= 0
	RETURN
ARRAYCOPY OPR_目标, "TARGET_LIST"
TARGET_LIST:0 = MASTER
VIEW = FINDELEMENT(TARGET_LIST, TARGET)
SIF VIEW == -1
	VIEW = 0
CALL LIST_CHARA_STATE(TARGET_LIST, VIEW, TARGET_NUM + 1, "「調教中」")



;列表がある時専用の情報表示関数
;CHARA_LISTにTARGETを渡せばTARGET順番で表示してくれる
@LIST_CHARA_STATE(CHARA_LIST, C_VIEW, CHARA_CNT, OP)
#DIM REF CHARA_LIST ;リスト本体、登録番号にて記録
#DIM     C_VIEW     ;表示中キャラのリスト上の位置
#DIM     CHARA_CNT  ;リストに該当したキャラの数
#DIMS    OP         ;実行オプション
#DIM     C_ID       ;登録番号
#DIM     PAGE       ;表示中のページ
#DIMS CONST DISP_NAME = "服装&能力", "经验&宝珠", "个人情报", "个人好恶", "身体情报", "陷落状态", "剧本设置"
#DIM CREDRAW
#DIM DYNAMIC FIRST_LINE
#DIM nSettingsEnable
FIRST_LINE = LINECOUNT
$REPRINT

;conditions for the setting page
RESULT = 0
LOCAL = 0
;CALL KOJO_MULTIPLE(CHARA_LIST:C_VIEW, 1)
;SIF RESULT > 1
;	SETBIT LOCAL, 0
;RESULT = 0
CALL KOJO_OPTION_EXIST(CHARA_LIST:C_VIEW)
SIF RESULT
	SETBIT LOCAL, 1
;disable the button if no extra dialogues or settings exist
nSettingsEnable = (GETBIT(LOCAL, 0) || GETBIT(LOCAL, 1)) && OP != "「調教中」" && CHARA_LIST:C_VIEW != MASTER
;SIF PAGE == 6 && !nSettingsEnable ;revert to default page if character settings aren't accessible
;	PAGE = 0
;actually keep the page even if it's empty
;reset the page if outside of sleep menu/looking at yourself (pending: might add special settings under PC?)
SIF PAGE == 6 && (OP == "「調教中」" || CHARA_LIST:C_VIEW == MASTER)
	PAGE = 0
;--------------------------------

CLEARLINE LINECOUNT - FIRST_LINE
CREDRAW = CURRENTREDRAW()
REDRAW 0
CALL CHARA_STATE(CHARA_LIST:C_VIEW, PAGE, OP)
DRAWLINE
FOR LOCAL, 0, VARSIZE("DISP_NAME")
	;extra cond for settings
	SIF DISP_NAME:LOCAL == "剧本设置" && !nSettingsEnable
		BREAK
	
	SIF PAGE == LOCAL
		SETCOLOR 0x00FFFF
	;exception for player character
	PRINTBUTTON CHARA_LIST:C_VIEW == MASTER && LOCAL == 5 ? "[技能习得]" # "["+DISP_NAME:LOCAL+"]", LOCAL + 2
	RESETCOLOR
	PRINTFORM    
NEXT
PRINTFORML 
PRINTBUTTON "[77] 前一位角色", 77
PRINTFORM   
PRINTBUTTON "[88] 下一位角色", 88
PRINTFORML 

IF STRCOUNT(OP, "「最終確認」")
	PRINTBUTTON "[1] 选择此角色", 1
	PRINTFORM 　　　　　　　　　
	PRINTBUTTON "[0] 返回", 0
	PRINTFORML 
ELSE
	PRINTBUTTON "[0] 返回", 0
ENDIF
DO
	INPUT
	SELECTCASE RESULT
	CASE 0
		RETURN 0
	CASE 1
		SIF STRCOUNT(OP, "「最終確認」")
			RETURN CHARA_LIST:C_VIEW
		GOTO RETRY
	CASE 2 TO 7
		PAGE = RESULT-2
	CASE 8
		SIF !nSettingsEnable
			GOTO RETRY
		PAGE = RESULT-2 
	CASE 77
		C_VIEW = (C_VIEW - 1 + CHARA_CNT) % MAX(1,CHARA_CNT)
	CASE 88
		C_VIEW = (C_VIEW + 1 + CHARA_CNT) % MAX(1,CHARA_CNT)
	CASE 98
		CFLAG:(CHARA_LIST:C_VIEW):差し替え適用 ++
		;qol custom code, combine face and standing images
		SIF CFLAG:(CHARA_LIST:C_VIEW):別顔有 < CFLAG:(CHARA_LIST:C_VIEW):差し替え適用
			CFLAG:(CHARA_LIST:C_VIEW):差し替え適用 = 0
		; IF FLAG:立ち絵種類
		; 	SIF CFLAG:(CHARA_LIST:C_VIEW):別顔有 < CFLAG:(CHARA_LIST:C_VIEW):差し替え適用
		; 		CFLAG:(CHARA_LIST:C_VIEW):差し替え適用 = 0
		; ELSE
		; 	SIF CFLAG:(CHARA_LIST:C_VIEW):別立ち有 < CFLAG:(CHARA_LIST:C_VIEW):差し替え適用
		; 		CFLAG:(CHARA_LIST:C_VIEW):差し替え適用 = 0
		; ENDIF
	;表示・非表示の切替
	CASE 160 TO 169, 900, 904, 909, 914, 915 ;custom code
		TFLAG:RESULT = !TFLAG:RESULT
	;qol custom code, disable here
	; CASE 画像切り替え
	; 	IF !FLAG:画像表示
	; 		FLAG:画像表示 = !FLAG:画像表示
	; 		SIF FLAG:画像表示 && nStoreImageCount > 0 ;qol custom code, restore previous value if enabled
	; 			FLAG:画像表示 = nStoreImageCount
			
	; 		FLAG:立ち絵種類 = 0
	; 		CFLAG:(CHARA_LIST:C_VIEW):差し替え適用 = 0 ;custom code
	; 	ELSEIF !FLAG:立ち絵種類
	; 		FLAG:立ち絵種類 = !FLAG:立ち絵種類
	; 	ELSEIF FLAG:立ち絵種類
	; 		SIF FLAG:画像表示 ;qol custom code, store previous value if it's going to be disabled
	; 			nStoreImageCount = FLAG:画像表示
			
	; 		FLAG:画像表示 = !FLAG:画像表示
	; 	ENDIF
	CASE 7000 TO 7050
		CALL KOJO_OPTION_RESULT(CHARA_LIST:C_VIEW, RESULT)
		SIF RESULT == -1
			GOTO RETRY
		SIF RESULT
			ResetList = 1
	CASE 9996
		FLAG:胖次収集状況タイプB = !FLAG:胖次収集状況タイプB
	CASE 9997 ;custom code
		CALL Qol_Info_ChildList(CHARA_LIST:C_VIEW, 1)
		WAIT
	CASE 9998 ;custom code
		CALL Qol_MC_CHILD_LIST
		;switch to a mother's page when clicked
		IF INRANGE(RESULT, 10000, 11000)
			RESULT -= 10000
			IF RESULT < CHARANUM && EXISTCSV(NO:RESULT)
				PAGE = 0
				C_VIEW = RESULT
			ENDIF
		ENDIF
		BREAK
	CASE 9999 ;custom code
		qol_Relation_AscendingOrder = !qol_Relation_AscendingOrder
	CASE 3500 TO 3505 ;Falling Condition Tab
		SFSR_PAGE = RESULT-3500
	; BBAS ALTER statusDisplay
	CASE 89999
		ALTER_statusDisplay = !ALTER_statusDisplay
	CASE 90000 TO 90005
		CALL CHECK_BODYGRAPH_RECORD("set",RESULT-90000)
    CASE -999 ;custom code, do nothing
	CASEELSE
		$RETRY
		REUSELASTLINE 无效输入
		CONTINUE
	ENDSELECT
	BREAK
LOOP 1
GOTO REPRINT



;基礎情報表示関数
;@CHARA_STATEから共通要素を抜き出しただけ（長ったらしいので）
;BASE系と劣情を基礎情報に含ませる
@BASIC_STATE(C_ID, OP)
#DIM C_ID
#DIMS OP
DRAWLINEFORM ━
PRINTFORM ■%NAME:C_ID%
;MASTERならば所持金を記載
IF C_ID == MASTER
	PRINTFORM (所持金:%金額表示(MONEY)%)
	PRINTFORM  (所持筹码:%三桁区切(MONEY:2)%CM)
	PRINTFORM  (所持守札:{ITEM:131}枚)
	SIF MAGIC_DLC && (TALENT:MASTER:神性 >= 10 || TALENT:MASTER:昨日重现 >= 5)
		PRINTFORM  (所持魔力结晶:{M_FLAG:魔力结晶数}枚)
	SIF KOMAFUDA:MASTER
		PRINTFORM  (所持驹: {KOMAFUDA:MASTER})
ELSE
	PRINTFORM (好感度:
	CALL PRINT_RANK(CFLAG:C_ID:好感度, "MAX_50000")
	PRINTFORM  {CFLAG:C_ID:好感度} 信赖度:
	CALL PRINT_RANK(CFLAG:C_ID:信赖度, "MAX_20000")
	PRINTFORM  {CFLAG:C_ID:信赖度}　#ID:{C_ID}) 
ENDIF
;IF FLAG:生理周期可視化 && TALENT:C_ID:性別 != 2
IF NOTICE_CYCLE(C_ID) ;bugfix
	IF STRCOUNT(OP, "「調教中」")
		CALL CYCLE_PRINT(C_ID)
;路人物色中判定
	ELSEIF SELECTCOM == 444
		CALL CYCLE_PRINT(C_ID)
	ELSE
		CALL CYCLE_PRINT翌日(C_ID)
	ENDIF
ENDIF
IF C_ID != MASTER
	CALL INFO_理论受孕概率(C_ID)
ENDIF
SIF C_ID != MASTER && NOTICE_CYCLE(C_ID)
	SETCOLOR C_AQUA
IF CFLAG:C_ID:允许无套 == 2
	PRINT 危险日生Ｈ解禁
ELSEIF CFLAG:C_ID:允许无套 == 1
	PRINT 生Ｈ解禁
ELSEIF GETBIT(CFLAG:C_ID:既成事實, 1)
	PRINT 合意取得
ENDIF
RESETCOLOR
;ここだけBASE基本表示で独立させてもいいかもって感じに使い勝手良さそう
PRINTL 　
IF !MAGIC_DLC
	CALL BASE_BAR(C_ID, "体力", 28, "緑")
	PRINTFORM 　　
	CALL BASE_BAR(C_ID, "気力", 28, "青", "「改行」")
	SIF BASE:C_ID:酒気
		CALL BASE_BAR(C_ID, "酒気", 28, "赤", "「改行」")

	IF C_ID == MASTER
		CALL BASE_BAR(MASTER, "精力", 28, "黄")
		PRINTFORM 　　
		CALL BASE_BAR(C_ID, "TSP", 28, "水色", "「改行」")
	ELSE
		GOTO 其他角色信息表示
	ENDIF
ELSE
	CALL BASE_BAR(C_ID, "体力", 28, "緑")
	PRINTFORM 　　
	CALL BASE_BAR(C_ID, "気力", 28, "青", "「改行」")
	IF HAS_PENIS(C_ID)
		CALL BASE_BAR(C_ID, "精力", 28, "黄")
		PRINTFORM 　　
	ENDIF
	CALL BASE_BAR(C_ID, "酒気", 28, "赤", "「改行」")
	IF MAXBASE:C_ID:法力 > 0
		CALL BASE_BAR(C_ID, "法力", 28, "橙")
		PRINTFORM 　　
	ENDIF
	IF MAXBASE:C_ID:TSP > 0
		CALL BASE_BAR(C_ID, "TSP", 28, "水色", "「改行」")
	ELSE
		PRINTFORML
	ENDIF
	SIF C_ID != MASTER
		GOTO 其他角色信息表示
ENDIF
GOTO 其他角色信息表示结束
$其他角色信息表示
IF NOTICE_LUST(C_ID)
	;記述が整理できるという理由で微妙に閾値変更
	SELECTCASE CFLAG:C_ID:积攒度
	CASE IS >= 665
		SETCOLOR C_RED
	CASE IS >= 332
		SETCOLOR C_YELLOW
	ENDSELECT
	PRINTFORMD 欲求不满度：
	PRINTFORM {CFLAG:C_ID:积攒度 / 10}
	PRINTFORMD ％
	RESETCOLOR
ENDIF
PRINTFORM 　　
SIF CFLAG:(C_ID):弾幕勝負勝利 == 1
	PRINTFORM ★已赢得弹幕对决胜利　
SIF CFLAG:(C_ID):斗虫直接勝利 == 1
	PRINTFORM ★已赢得直接斗虫胜利　
SIF CFLAG:(C_ID):料理勝負勝利 == 1
	PRINTFORM ★已赢得料理对决胜利　
IF MAGIC_DLC
	SIF GETBIT(M_CFLAG:C_ID:魔法奖励获得, 1)
		PRINTFORM ★已获得清扫魔法奖励　
	SIF GETBIT(M_CFLAG:C_ID:魔法奖励获得, 2)
		PRINTFORM ★已获得博识魔法奖励　
	SIF GETBIT(M_CFLAG:C_ID:魔法奖励获得, 3)
		PRINTFORM ★已获得解酒魔法奖励　
ENDIF
PRINTFORML
IF MAGIC_DLC_好感亲密重做
	PRINTFORM 当前好意珠：{JUEL:C_ID:20}/{JUEL_DEMAND_9(0,20,C_ID)}|
	PRINTFORM 当前欲情珠：{JUEL:C_ID:21}/{JUEL_DEMAND_11(1,21,C_ID)}|
	PRINTFORML
ENDIF
;qol custom code, show currently used dialogue
;first see if we have something on the current line to determine if we want to add extra spacing
SIF CFLAG:(C_ID):弾幕勝負勝利 || CFLAG:(C_ID):斗虫直接勝利 == 1 || CFLAG:(C_ID):料理勝負勝利 == 1
	PRINTFORM 　　　　
CALL QoL_DetailedInfo(Qol_Info_Dialogue, C_ID)
PRINTFORML
$其他角色信息表示结束
;角色情報表示関数
;登録番号と页番号、オプションを指定して情報を表示する
@CHARA_STATE(C_ID, PAGE, OP)
#DIM  C_ID
#DIM  PAGE
#DIMS OP
;#DIM STARTLINE

;STARTLINE = LINECOUNT

CALL BASIC_STATE(C_ID, OP)
SELECTCASE PAGE
CASE 0
	CALL SHOW_SIZE(C_ID)
	CALL PRINT_STATE_IMAGE(C_ID)
	PRINTL 
	CALL PRINT_STATE_CLOTH(C_ID)
	CALL PRINT_STATE_TALENT(C_ID)
	PRINTL
	CALL SHOW_ABILITY(C_ID)
CASE 1
	PRINTL
	CALL PRINT_STATE_EXP(C_ID)
	PRINTL
	;現状マスターは妊娠しないはずだが将来の機能拡張を考えるとこれでいいのか？
	SIF TALENT:C_ID:妊娠 || CFLAG:C_ID:兒童人数 > 0
		CALL 妊娠関連(C_ID)
	SIF (C_ID == MASTER) && (CFLAG:MASTER:子供人数 > 0)	;custom code
		CALL Qol_MC_CHILD
	IF STRCOUNT(OP, "「調教中」")
		CALL PRINT_STATE_EX(C_ID)
	ELSEIF !(C_ID == MASTER)
		CALL QOL_PRINT_STATE_JUEL(C_ID)
	ENDIF
	PRINTL
	IF C_ID == MASTER
		CALL 予定
	ELSE
		CALL 胖次収集状況(C_ID)
	ENDIF
	SIF C_ID != MASTER ;custom code
		CALL QOL_CHARA_SCORES(C_ID)
CASE 2
	CALL PRINT_STATE_PERSONAL(C_ID)
	;CALL SHOW_RELATION(C_ID)
	CALL Qol_SHOW_RELATION(C_ID)
CASE 3
	CALL PRINT_STATE_PREFERENCE(C_ID)
	CALL PRINT_STATE_GIFTDATA(C_ID, 15)
CASE 4
	CALL 肉体情報(C_ID)
CASE 5
	IF C_ID == MASTER
		CALL SHOW_ALLITEM_INFO
	ELSE
		CALL 陥落条件表示_QOL(C_ID)
		CALL 浮気把握状況(C_ID)
	ENDIF
CASE 6
	CALL KOJO_OPTION_PRINT(C_ID)
CASEELSE
	THROW 異常なPAGE {PAGE}
ENDSELECT

;WHILE (680/GETCONFIG("一行の高さ"))-(LINECOUNT-STARTLINE) > 0
;	PRINTL
;WEND


;刻印と能力を一括して表示する
;縦幅を圧縮したい
@SHOW_ABILITY(C_ID)
#DIM  C_ID
{
#DIMS CONST DISP_NAME = 
	"苦痛",	"亲密",		"教养",		"Ｃ感觉",	"指技术",	"受虐属性",	"自慰中毒",
	"快乐",	"顺从",		"话术技能",	"Ｖ感觉",	"膣技术",	"施虐属性",	"精液中毒",
	"屈服",	"欲望",		"战斗能力",	"Ａ感觉",	"Ａ技术",	"百合属性",	"百合中毒",
	"反感",	"技巧",		"清扫技能",	"Ｂ感觉",	"胸技术",	"断袖属性",	"断袖中毒",
	"履历",	"侍奉精神",	"料理技能",	"Ｍ感觉",	"舌技术",	"",			"膣射中毒",
	"时奸",	"露出癖",	"音乐技能",	"",			"腰技术",	"",			"肛射中毒",
	"",		"",			"",			"",			"足技术",	"",			"",
}
CALL PRINTBUTTON_EX("能力", 162, TFLAG:162, , , 1)
SIF TFLAG:能力表示
	RETURN

FOR LOCAL, 0, VARSIZE("DISP_NAME")
	CALL PRINT_ABILITY(C_ID, DISP_NAME:LOCAL, LOCAL)
	SIF LOCAL % 7 == 6
		PRINTFORML 
NEXT



;↑専用の表示関数
@PRINT_ABILITY(C_ID, DISP_NAME, VIEW)
#DIM  C_ID
#DIMS DISP_NAME
#DIM  VIEW
{
#DIM CONST DISP_ID = 
	0,	9,	43,	0,	50,	15,	30,
	1,	10,	41,	1,	54,	16,	31,
	2,	11,	42,	2,	55,	17,	32,
	3,	12,	40,	3,	52,	18,	33,
	4,	13,	44,	4,	51,	0,	34,
	5,	14,	45,	0,	53,	0,	35,
	0,	0,	0,	0,	56,	0,	0,
}
PRINTFORM 　
SELECTCASE DISP_NAME
CASE ""
	$SHOW_BLANK
	PRINTFORM 　　　　　　　　
CASE "苦痛", "快乐", "屈服", "反感", "履历", "时奸"
	IF C_ID == MASTER
		PRINTFORM %"　" * 8%
	ELSEIF GROUPMATCH(DISP_NAME, "反感", "履历")
		PRINTFORM %DISP_NAME, 8%：
		CALL COLOR_STAMP(MARK:C_ID:(DISP_ID:VIEW), 3, "×", C_RED)
	ELSE
		PRINTFORM %DISP_NAME, 8%：
		CALL COLOR_STAMP(MARK:C_ID:(DISP_ID:VIEW), 3, "★", 0xFFFF00)
	ENDIF
{
CASE 
	"指技术", "膣技术", "Ａ技术", "胸技术", "舌技术", "腰技术", "足技术",
	"清扫技能", "话术技能", "战斗能力",     "教养", "料理技能", "音乐技能"
}
	PRINTFORM %DISP_NAME, 8%：
	CALL PRINT_RANK(ABL:C_ID:(DISP_ID:VIEW), "MAX_6")
	PRINTFORM  {ABL:C_ID:(DISP_ID:VIEW), 3}
CASEELSE
	SIF GROUPMATCH(DISP_NAME, "百合属性", "百合中毒") && !GETBIT(TALENT:C_ID:2, 0)
		GOTO SHOW_BLANK
	SIF GROUPMATCH(DISP_NAME, "断袖属性", "断袖中毒") && GETBIT(TALENT:C_ID:2, 0)
		GOTO SHOW_BLANK
	PRINTFORM %DISP_NAME, 8%：
	CALL PRINT_RANK(ABL:C_ID:(DISP_ID:VIEW), "MAX_99")
	PRINTFORM  {ABL:C_ID:(DISP_ID:VIEW), 3}
ENDSELECT


[SKIPSTART]
;ソート用ボタン作成関数
;無造作に扱えるが
;同一列表内のB_NAMEは横幅を揃えたほうがみばえがよろしい
@MAKE_SORT_BUTTON(SORT_TYPE, BUTTON)
#DIM SORT_TYPE ;現在のソートタイプ
#DIM BUTTON    ;基本のボタン名
;auto sort
SortListSize = VARSIZE("DISP_MEMO")
FOR LOCAL, 1000, 9999
	IF LOCAL%SortListSize == 0
		StartingValue = LOCAL
		BREAK
	ENDIF
NEXT

IF SORT_TYPE%SortListSize != BUTTON
	;LOCAL = 1032+BUTTON+24	;PRINT
	;BUTTON @"{LOCAL}", LOCAL
	PRINTBUTTON @"　%DISP_MEMO:BUTTON%", StartingValue+BUTTON
ELSEIF SORT_TYPE < StartingValue+SortListSize ;this is the sum of <button value> + total amount of items in DISP_MEMO
	PRINTBUTTON @"▼%DISP_MEMO:BUTTON%", StartingValue+BUTTON+SortListSize
ELSEIF SORT_TYPE >= StartingValue+SortListSize
	PRINTBUTTON @"▲%DISP_MEMO:BUTTON%", StartingValue+BUTTON
ENDIF
;IF SORT_TYPE == @"%B_NAME%降序"
;	PRINTBUTTON @"▼%B_NAME%", @"%B_NAME%升序"
;ELSEIF SORT_TYPE == @"%B_NAME%升序"
;	PRINTBUTTON @"▲%B_NAME%", @"%B_NAME%降序"
;ELSE
;	PRINTBUTTON @"　%B_NAME%", @"%B_NAME%降序"
;ENDIF


;一行情報表示関数
;角色の情報を一行で表示しつつボタンとして扱う
;元々は@ShowStatusBodyと@ShowStatusHeader中奖了
;ラベル（ボタンとしての番号）と登録番号を別々に指定する必要がある
@CHARA_BUTTON(LABEL, C_ID, DISP_TYPE = 0, SORT_TYPE)
#DIM  LABEL
#DIM  C_ID
#DIM DISP_TYPE
#DIM SORT_TYPE
#DIMS 表示文
#DIM  表示ステータス番号
#DIM  CONST バー長さ = 10
;LABELが負数はありえないので表示内容のアナウンスに流用してます
{
#DIMS CONST DISP_NAME = 
	"体力气力", "敏感度", "性技", "刻印", "技能", "好感", "胜利等"
}

IF LABEL >= 0
	;Temporary code, stopgap since the original flag used isn't set correctly
	TRYCALLFORM M_KOJO_K{NO:C_ID}
	SIF RESULT
		SETCOLOR 103,228,126
	SIF C_ID == MASTER
		SETCOLOR C_AQUA
	PRINTFORM [{LABEL, 3}] %CALLNAME:C_ID, 14, LEFT%
	RESETCOLOR
ENDIF

SELECTCASE DISP_TYPE
CASE 0 ;STA and ENE
	IF LABEL < 0
		PRINTFORM %"　" * 9%
		CALL MAKE_SORT_BUTTON(SORT_TYPE, 0)
		PRINTFORM %"　" * 9%
		CALL MAKE_SORT_BUTTON(SORT_TYPE, 1)
	ELSE
		CALL BASE_BAR(C_ID, "体力", 10, "緑", "「種類表記無し」")
		CALL BASE_BAR(C_ID, "気力", 10, "青", "「種類表記無し」")
	ENDIF
	PRINTFORML 
CASE 1 ;Sense
	IF LABEL < 0
		PRINTFORM %"　" * 4%敏感度%"　" * 3%
		FOR LOCAL, 2, 13
		CALL MAKE_SORT_BUTTON(SORT_TYPE, LOCAL)
		PRINT |
		NEXT
	ELSE
		FOR LOCAL, 0, 15
			SIF ABLNAME:LOCAL == ""
				CONTINUE
			CALL PRINT_RANK(ABL:C_ID:LOCAL, "MAX_99")
			PRINTFORM {ABL:C_ID:LOCAL, 4}|
			SIF LOCAL != 10
				PRINTFORM 
			SIF LOCAL == 4
				PRINTFORM 
			SIF LOCAL == 9
				PRINTFORM 
			SIF LOCAL == 10
				PRINTFORM 
			SIF LOCAL == 11
				PRINTFORM 
			SIF LOCAL == 12
				PRINTFORM 　　
			SIF LOCAL == 13
				PRINTFORM 　
		NEXT
	ENDIF
	PRINTFORML 

CASE 2 ;Technique
	IF LABEL < 0
		PRINTFORM %"　" * 4%性技%"　" * 4%
		FOR LOCAL, 13, 22
			CALL MAKE_SORT_BUTTON(SORT_TYPE, LOCAL)
			PRINT |
		NEXT
	ELSE
		;超適当につき修正の余地あり
		FOR LOCAL, 50, 56
			SIF ABLNAME:LOCAL == ""
				CONTINUE
			CALL PRINT_RANK(ABL:C_ID:LOCAL, "MAX_6")
			PRINTFORM {ABL:C_ID:LOCAL, 4}|
			SIF LOCAL != 54
				PRINTFORM 
			SIF LOCAL == 50
				PRINTFORM 
			SIF LOCAL == 53
				PRINTFORM 
		NEXT
		PRINTFORM 
		CALL PRINT_RANK(ABL:C_ID:15, "MAX_99")
		PRINTFORM {ABL:C_ID:15, 4}|
		PRINTFORM 
		CALL PRINT_RANK(ABL:C_ID:16, "MAX_99")
		PRINTFORM {ABL:C_ID:16, 4}|
		PRINTFORM 　　
		CALL PRINT_RANK(ABL:C_ID:31, "MAX_99")
		PRINTFORM {ABL:C_ID:31, 4}|
	ENDIF
	PRINTFORML 


CASE 3 ;Marks
	IF LABEL < 0
		PRINTFORM %"　" * 4%刻印%"　" * 4%
		FOR LOCAL, 22, 27
			CALL MAKE_SORT_BUTTON(SORT_TYPE, LOCAL)
			PRINT |
		NEXT
	ELSE
		;超適当につき修正の余地あり
		FOR 表示ステータス番号, 0, 6
			IF MARKNAME:表示ステータス番号 == ""
				CONTINUE
			ENDIF
			;反発刻印履歴は表示しないように
			SIF 表示ステータス番号 == 4
				CONTINUE
			IF 表示ステータス番号 == 3
				CALL COLOR_STAMP(MARK:C_ID:表示ステータス番号, 3, "×", C_RED)
			ELSE
				CALL COLOR_STAMP(MARK:C_ID:表示ステータス番号, 3, "★", 0xFFFF00)
			ENDIF
			PRINTFORM |
		NEXT
	ENDIF
	PRINTFORML 

CASE 4 ;skills
	IF LABEL < 0
		PRINTFORM %"　" * 4%技能%"　" * 4%
		FOR LOCAL, 27, 37
			CALL MAKE_SORT_BUTTON(SORT_TYPE, LOCAL)
			PRINT |
		NEXT
	ELSE
		COUNT = 43, 41, 42, 40, 44, 45
		;超適当につき修正の余地あり
		FOR LOCAL, 0, 6
			SIF ABLNAME:(COUNT:LOCAL) == ""
				CONTINUE
			;SIF LOCAL == 0
			;	PRINTFORM    
			CALL PRINT_RANK(ABL:C_ID:(COUNT:LOCAL), "MAX_6")
			PRINTFORM {ABL:C_ID:(COUNT:LOCAL), 4}|
			;SELECTCASE LOCAL
			;	CASE 0, 1
			;		PRINTFORM   
			;	CASE 2
			;		PRINTFORM     
			;	CASE 3, 5
			;		PRINTFORM    
			;	CASE 4
			;		PRINTFORM  
			;ENDSELECT
		NEXT
		FOR LOCAL, 46, 50
			SIF TALENTNAME:LOCAL == ""
				CONTINUE  
			CALL PRINT_RANK(TALENT:C_ID:LOCAL, "MAX_6")
			PRINTFORM {TALENT:C_ID:LOCAL, 4}|
			;SELECTCASE LOCAL
			;	CASE 46
			;		PRINTFORM    
			;	CASE 47
			;		PRINTFORM     
			;	CASE 48
			;		PRINTFORM   
			;ENDSELECT
		NEXT
	ENDIF
	PRINTFORML 

CASE 5 ;Favor
	IF LABEL < 0
		PRINTFORM %"　" * 4%陷落状态%"　" * 3% 
		FOR LOCAL, 37, 39
			CALL MAKE_SORT_BUTTON(SORT_TYPE, LOCAL)
			PRINT |
			SIF LOCAL == 37
				PRINTFORM     
		NEXT
		IF TALENT:MASTER:劣情察知
			CALL MAKE_SORT_BUTTON(SORT_TYPE, 39)
			PRINT |
		ENDIF
		;CALL MAKE_SORT_BUTTON(SORT_TYPE, 40)
		;PRINT |
		PRINTFORML
	ELSE
		;好感度
		CALL PRINT_RANK(CFLAG:C_ID:好感度, "MAX_50000")
		PRINTFORM {CFLAG:C_ID:好感度, 7}| 
		
		;信頼度
		CALL PRINT_RANK(CFLAG:C_ID:信頼度, "MAX_20000")
		PRINTFORM {CFLAG:C_ID:信頼度, 7}|
		
		;欲求
		IF TALENT:MASTER:劣情察知
			SELECTCASE CFLAG:C_ID:溜まってる度
			CASE IS >= 665
				SETCOLOR C_RED
			CASE IS >= 332
				SETCOLOR C_YELLOW
			ENDSELECT
			PRINTFORM {CFLAG:C_ID:溜まってる度 / 10, 4}
			RESETCOLOR
			PRINT ％|
		ENDIF
		;IF DIALOGSTATUS:C_ID:0 == -1 || C_ID == MASTER
		;	PRINTFORM %" ",10%|
		;ELSE
		;	PRINTFORM {DIALOGSTATUS:C_ID:(CFLAG:C_ID:口上セレクタ), 8}％|
		;ENDIF
		PRINTFORM 　　
		;恋慕
		IF TALENT:C_ID:妻
			SETCOLOR C_RED
			表示文 '= PRINT_MALE("丈夫", C_ID)
		ELSEIF TALENT:C_ID:妾
			SETCOLOR C_RED
			表示文 = 侧室
		ELSEIF TALENT:C_ID:恋人
			SETCOLOR C_RED
			表示文 = 恋人
		ELSEIF TALENT:C_ID:恋慕 > 1
			SETCOLOR 0xEE60EE
			表示文 = 爱恋
		ELSEIF TALENT:C_ID:恋慕 == 1
			SETCOLOR 0xEE60EE
			表示文 = 恋慕
		ELSEIF TALENT:C_ID:愛欲 == 1 
			SETCOLOR 0x2E8B57 
			表示文 = 爱欲
		ELSEIF TALENT:C_ID:セフレ == 1 ;bugfix custom code
			SETCOLOR 0x3D85C6
			表示文 = 炮友
		ELSEIF TALENT:C_ID:セフレ == 2 
			SETCOLOR 0xFF6347 
			表示文 = 情人
		ELSEIF TALENT:C_ID:思慕 == 1
			表示文 = 思慕
		ELSE
			表示文 = 
		ENDIF
		PRINTFORM %表示文, 9, LEFT%
		RESETCOLOR
		PRINT |
		
		;既成事実
		SELECTCASE CFLAG:(C_ID):生解禁
			CASE 2
				表示文 = 危险日生Ｈ解禁
			CASE 1
				表示文 = 生Ｈ解禁
			CASE 0
				IF GETBIT(CFLAG:(C_ID):既成事実,1)
					表示文 = 合意取得
				ELSE
					表示文 = 
				ENDIF
		ENDSELECT
		PRINTFORM %表示文, 14, LEFT%|
		
		;生理周期可視化
		SIF FLAG:1007
			CALL PRINT_CYCLE_翌日(C_ID)
		SIF CFLAG:(C_ID):出産経過日 > 0
			PRINTFORM 育儿中 
		
		RESETCOLOR
		SIF GETBIT(CFLAG:(C_ID):既成事実,2)
			;Custom code, makes it seem they have a lover other than you, Lover status is printed earlier anyway
			;PRINTFORM Has Lover 
			PRINTFORM 
		;弱み
		IF CFLAG:(C_ID):掌握把柄 && CFLAG:(C_ID):被掌握把柄
			PRINTFORM %CALLNAME:MASTER%抓住了彼此的把柄 
		ELSEIF CFLAG:(C_ID):掌握把柄
			PRINTFORM 抓住了%CALLNAME:MASTER%的把柄 
		;SIF GETBIT(CFLAG:(C_ID):把柄,0)
		;	PRINTFORM ぱんつを没収された 
		;SIF GETBIT(CFLAG:(C_ID):把柄,1)
		;	PRINTFORM 情事を見られた 
		ELSEIF CFLAG:(C_ID):被掌握把柄
			PRINTFORM 被%CALLNAME:MASTER%目击过自慰现场
		ENDIF

		;;show translation note, do a little hacking
		;IF CHARANUM < 145 ;failsafe, the function below ree's, so ignore if exception [Remember to update this value if new characters are added!)
		;	PRINTFORML
		;ELSE
		;	;consult the list again just to be safe
		;	RESULTS =
		;	CALL CHARACTER_DIALOG_STATUS(C_ID)
		;	IF STRLENS(RESULTS) ;activate if only there's a note
		;		LOCALS '= HTML_POPPRINTINGSTR()
		;		;insert tooltip into button
		;		LOCALS '= replace(locals, "button value", @"button title= '%HTML_ESCAPE("Translated By " + RESULTS)%' value")
		;		HTML_PRINT LOCALS
		;	ELSE ;default line break
				PRINTFORML
		;	ENDIF
		;ENDIF
	ENDIF
	
CASE 6 ;victories&etc
	IF LABEL < 0
		PRINTFORM %"　" * 8%
		
		;CALL MAKE_SORT_BUTTON(SORT_TYPE, 41)
		CALL MAKE_SORT_BUTTON(SORT_TYPE, 40)
		PRINT |
		;PRINTFORM     
		;CALL MAKE_SORT_BUTTON(SORT_TYPE, 42)
		CALL MAKE_SORT_BUTTON(SORT_TYPE, 41)
		PRINT |
		;PRINT 　Requests
		CALL MAKE_SORT_BUTTON(SORT_TYPE, 42)
		PRINT |
		PRINTFORML
	ELSE
		PRINTFORM 　
		SELECTCASE CFLAG:(C_ID):弾幕勝負勝利
			CASE IS >= 1
				表示文 = 已取胜
				CALL COLORMESSAGE, "★", C_YELLOW
		CASEELSE
			表示文 = 
			PRINTFORM 　
		ENDSELECT
		PRINTFORM %表示文, 6, LEFT%|
		
		PRINTFORM       
		SELECTCASE CFLAG:(C_ID):斗虫直接勝利
			CASE IS >= 1
				表示文 = 已取胜
				CALL COLORMESSAGE, "★", C_YELLOW
		CASEELSE
			表示文 = 
			PRINTFORM 　
		ENDSELECT
		PRINTFORM %表示文, 6, LEFT%|
		PRINTFORM       
		SELECTCASE CFLAG:(C_ID):料理勝負勝利
			CASE IS >= 1
				表示文 = 已取胜
				CALL COLORMESSAGE, "★", C_YELLOW
		CASEELSE
			表示文 = 
			PRINTFORM 　
		ENDSELECT
		PRINTFORM %表示文, 6, LEFT%|
		
		PRINTFORML
	ENDIF
CASEELSE
	THROW 異常なDISP_TYPE　{DISP_TYPE}
ENDSELECT
[SKIPEND]

@陥落条件表示(ARG, EXPECTED_WIDTH) ;qol custom code, added EXPECTED_WIDTH
#DIM 自覚絶頂
#DIM EXPECTED_WIDTH
自覚絶頂 = EXP:ARG:絶頂経験 - EXP:ARG:無自覚絶頂経験
;PRINTL
;qol custom code, collapsible
CALL PRINTBUTTON_EX("陷落状态",914,TFLAG:HideFallingStates, , , 1, EXPECTED_WIDTH)
SIF TFLAG:HideFallingStates
	RETURN
PRINTL
;custom code - show current value
IF TALENT:ARG:恋慕
	CALL COLORMESSAGE(@"□爱恋达成条件",C_AQUA,1)
	PRINTFORML 
	IF TALENT:ARG:恋慕 > 1
		PRINT 　
		CALL COLORMESSAGE(@"-　已达成",C_YELLOW,1)

		CALL CONCUBINAGE_FALL_STATE(ARG, EXPECTED_WIDTH) ;marriage custom code
	;ELSEIF (MARK:ARG:時姦刻印 && !CFLAG:ARG:時間停止バレ) || EXP:ARG:睡眠姦経験
	;	PRINT 　
	;	CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
	ELSE
		CALL KOJO_CHECK(ARG, "FALL_STATE_OVERRIDE", "DEEP LOVE") ;addition custom code
		IF RESULT
			IF RESULT < 0 ;no conditions should be set in "FALL_STATE" check
				PRINT 　
				CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
			ENDIF
			GOTO CUSTOM_DEEP_LOVE
		ENDIF
		PRINT 　
		CALL COND_COLORMESSAGE(CFLAG:ARG:2 >= REQUIRED_FAVOR_TRUE_LOVE,@"好感度{REQUIRED_FAVOR_TRUE_LOVE}以上 (现在：{CFLAG:ARG:2})")
		PRINT 　
		CALL COND_COLORMESSAGE(CFLAG:ARG:4 >= REQUIRED_TRUST_TRUE_LOVE,@"信赖度{REQUIRED_TRUST_TRUE_LOVE}以上 (现在：{CFLAG:ARG:4})")
		PRINT 　
		CALL COND_COLORMESSAGE(ABL:ARG:親密 >= REQUIRED_AFFECTION_TRUE_LOVE,@"亲密{REQUIRED_AFFECTION_TRUE_LOVE}以上 (现在：{ABL:ARG:親密})")
		PRINT 　
		CALL COND_COLORMESSAGE(EXP:ARG:デート経験 >= REQUIRED_DATE_XP_TRUE_LOVE,@"约会经验大于 {REQUIRED_DATE_XP_TRUE_LOVE} (现在：{EXP:ARG:デート経験})")
		PRINT 　
		CALL COND_COLORMESSAGE(EXP:ARG:愛情経験 >= REQUIRED_LOVE_XP_TRUE_LOVE,@"爱情经验大于 {REQUIRED_LOVE_XP_TRUE_LOVE} (现在：{EXP:ARG:愛情経験})")
		PRINT 　
		CALL COND_COLORMESSAGE(EXP:ARG:デート経験+EXP:ARG:愛情経験 >= REQUIRED_DATE_AND_LOVE_XP_TRUE_LOVE,@"约会+爱情经验大于 {REQUIRED_DATE_AND_LOVE_XP_TRUE_LOVE} (现在：{EXP:ARG:デート経験+EXP:ARG:愛情経験})")
		PRINT 　
		CALL COND_COLORMESSAGE(!MARK:ARG:反発刻印,@"没有反感刻印 (现在：{MARK:ARG:反発刻印})")
		;CALL COND_COLORMESSAGE(!((MARK:ARG:時姦刻印 && !CFLAG:ARG:時間停止バレ) || EXP:ARG:睡眠姦経験),@"No rape marks or exp")
		$CUSTOM_DEEP_LOVE
		CALL ADD_PRINT_FALL_STATES("DEEP LOVE", ARG) ;addition custom code
	ENDIF
	PRINTFORML %"-"*(EXPECTED_WIDTH)%
ENDIF
CALL COLORMESSAGE(@"□恋慕达成条件",C_AQUA,1)
PRINTFORML 
IF TALENT:ARG:恋慕
	PRINT 　
	CALL COLORMESSAGE(@"-　已达成",C_YELLOW,1)
ELSEIF TALENT:ARG:愛欲 && !FLAG:陥落路線変化 ;qol custom code
	PRINT 　
	CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
ELSE
	CALL KOJO_CHECK(ARG, "FALL_STATE_OVERRIDE", "LOVE") ;addition custom code
	IF RESULT
		IF RESULT < 0 ;no conditions should be set in "FALL_STATE" check
			PRINT 　
			CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
		ENDIF
		GOTO CUSTOM_LOVE
	ENDIF
	PRINT 　
	CALL COND_COLORMESSAGE(CFLAG:ARG:2 >= REQUIRED_FAVOR_恋慕,@"好感度{REQUIRED_FAVOR_恋慕}以上 (现在：{CFLAG:ARG:2})")
	PRINT 　
	CALL COND_COLORMESSAGE(CFLAG:ARG:4 >= REQUIRED_TRUST_恋慕,@"信赖度{REQUIRED_TRUST_恋慕}以上 (现在：{CFLAG:ARG:4})")
	PRINT 　
	CALL COND_COLORMESSAGE(ABL:ARG:親密 >= REQUIRED_AFFECTION_恋慕,@"亲密{REQUIRED_AFFECTION_恋慕}以上 (现在：{ABL:ARG:親密})")
	PRINT 　
	CALL COND_COLORMESSAGE(!MARK:ARG:反発刻印,@"没有反感刻印 (现在：{MARK:ARG:反発刻印})")
	$CUSTOM_LOVE
	CALL ADD_PRINT_FALL_STATES("LOVE", ARG) ;addition custom code
ENDIF
PRINTFORML %"-"*(EXPECTED_WIDTH)%
CALL COLORMESSAGE(@"□思慕达成条件",C_AQUA,1)
PRINTFORML 
IF TALENT:ARG:恋慕 || TALENT:ARG:思慕
	PRINT 　
	CALL COLORMESSAGE(@"-　已达成",C_YELLOW,1)
ELSEIF TALENT:ARG:セフレ && !FLAG:陥落路線変化 ;qol custom code
	PRINT 　
	CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
ELSE
	CALL KOJO_CHECK(ARG, "FALL_STATE_OVERRIDE", "YEARNING") ;addition custom code
	IF RESULT
		IF RESULT < 0 ;no conditions should be set in "FALL_STATE" check
			PRINT 　
			CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
		ENDIF
		GOTO CUSTOM_YEARNING
	ENDIF
	PRINT 　
	CALL COND_COLORMESSAGE(CFLAG:ARG:2 >= REQUIRED_FAVOR_思慕,@"好感度{REQUIRED_FAVOR_思慕}以上 (现在：{CFLAG:ARG:2})")
	PRINT 　
	CALL COND_COLORMESSAGE(CFLAG:ARG:4 >= REQUIRED_TRUST_思慕,@"信赖度{REQUIRED_TRUST_思慕}以上 (现在：{CFLAG:ARG:4})")
	PRINT 　
	CALL COND_COLORMESSAGE(ABL:ARG:親密 >= REQUIRED_AFFECTION_思慕,@"亲密{REQUIRED_AFFECTION_思慕}以上 (现在：{ABL:ARG:親密})")
	$CUSTOM_YEARNING
	CALL ADD_PRINT_FALL_STATES("YEARNING", ARG) ;addition custom code
ENDIF
PRINTFORML %"-"*(EXPECTED_WIDTH)%
CALL COLORMESSAGE(@"□爱欲达成条件",C_AQUA,1)
PRINTFORML 
IF TALENT:ARG:恋慕
	PRINT 　
	CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
ELSEIF TALENT:ARG:愛欲
	PRINT 　
	CALL COLORMESSAGE(@"-　已达成",C_YELLOW,1)

	;CALL CONCUBINAGE_FALL_STATE(ARG) ;marriage custom code
ELSE
	CALL KOJO_CHECK(ARG, "FALL_STATE_OVERRIDE", "LUST")
	IF RESULT
		IF RESULT < 0 ;no conditions should be set in "FALL_STATE" check
			PRINT 　
			CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
		ENDIF
		GOTO CUSTOM_LUST
	ENDIF
	PRINT 　
	CALL COND_COLORMESSAGE(CFLAG:ARG:2 >= REQUIRED_FAVOR_愛欲,@"好感度{REQUIRED_FAVOR_愛欲}以上 (现在：{CFLAG:ARG:2})")
	PRINT 　
	CALL COND_COLORMESSAGE(ABL:ARG:欲望 >= REQUIRED_DESIRE_愛欲,@"欲望{REQUIRED_DESIRE_愛欲}以上 (现在：{ABL:ARG:欲望})")
	PRINT 　
	CALL COND_COLORMESSAGE(自覚絶頂 >= REQUIRED_ECSTACY_愛欲,@"在有意识的状态下绝顶经验在{REQUIRED_ECSTACY_愛欲}次以上 (现在：{自覚絶頂})")
	PRINT 　
	;custom code
	CALL COND_COLORMESSAGE((ABL:ARG:欲望 - ABL:ARG:親密) >= 1,@"欲望比亲密高 (现在：{ABL:ARG:欲望}vs{ABL:ARG:親密})")
	$CUSTOM_LUST
	CALL ADD_PRINT_FALL_STATES("LUST", ARG) ;addition custom code
ENDIF
PRINTFORML %"-"*(EXPECTED_WIDTH)%
CALL COLORMESSAGE(@"□炮友达成条件",C_AQUA,1)
PRINTFORML 
;Custom code, lover blocks sex friend as well
IF TALENT:ARG:恋慕 || TALENT:ARG:恋人
	PRINT 　
	CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
ELSEIF TALENT:ARG:セフレ
	PRINT 　
	CALL COLORMESSAGE(@"-　已达成",C_YELLOW,1)
ELSE
	CALL KOJO_CHECK(ARG, "FALL_STATE_OVERRIDE", "SEX FRIEND") ;addition custom code
	IF RESULT
		IF RESULT < 0 ;no conditions should be set in "FALL_STATE" check
			PRINT 　
			CALL COLORMESSAGE(@"-　无法达成",C_RED,1)
		ENDIF
		GOTO CUSTOM_SEX_FRIEND
	ENDIF
	PRINT 　
	;Custom code, wrong conditions, technically printing-related so not a proper bugfix
	CALL COND_COLORMESSAGE(CFLAG:ARG:2 >= REQUIRED_FAVOR_炮友,@"好感度{REQUIRED_FAVOR_炮友}以上 (现在：{CFLAG:ARG:2})")
	PRINT 　
	CALL COND_COLORMESSAGE(ABL:ARG:欲望 >= REQUIRED_DESIRE_炮友,@"欲望{REQUIRED_DESIRE_炮友}以上 (现在：{ABL:ARG:欲望})")
	PRINT 　
	CALL COND_COLORMESSAGE(自覚絶頂 >= REQUIRED_ECSTACY_炮友,@"在有意识的状态下绝顶经验在{REQUIRED_ECSTACY_炮友}次以上 (现在：{自覚絶頂})")
	PRINT 　
	;custom code
	CALL COND_COLORMESSAGE((ABL:ARG:欲望 - ABL:ARG:親密) >= 0,@"欲望不小于亲密 (现在：{ABL:ARG:欲望}vs{ABL:ARG:親密})")
	$CUSTOM_SEX_FRIEND
	CALL ADD_PRINT_FALL_STATES("SEX FRIEND", ARG) ;addition custom code
ENDIF
;PRINTFORML 
