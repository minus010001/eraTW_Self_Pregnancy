@LOOK_AT
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC 装备部位
#DIM DYNAMIC 着衣状況
#DIMS DYNAMIC 插入状况
#DIMS DYNAMIC 插入状况2
#DIMS DYNAMIC 表示内容
#DIM DYNAMIC 肛门
#DIM DYNAMIC 小穴
CALL PRINTBUTTON_EX(TFLAG:LOOK非表示 ? "▽[＋][Look]----------" # "▼[－][Look]----------", 3003, TFLAG:LOOK非表示)
PRINTBUTTON "[显示立绘]", 816
SIF FLAG:画像表示設定
	SETCOLOR C_ORANGE
PRINTBUTTON "[显示设定]", 3100
RESETCOLOR
IF CFLAG:诶嘿嘿
	IF FLAG:挿絵表示
		PRINTBUTTON "[显示动画-ON-]", 819
	ELSE
		SETCOLOR C_L_GRAY
		PRINTBUTTON "[显示动画-OFF-]", 819
		RESETCOLOR
	ENDIF
ENDIF
IF 自机立绘开关
	SIF !自机立绘显示
		SETCOLOR C_L_GRAY
	PRINTBUTTON "[显示自机立绘]", 3099
	RESETCOLOR
ENDIF
PRINTL 

SIF TALENT:MASTER:154 && TALENT:154 != TARGET
	CALL MESSAGE_TALK(14)

IF !TFLAG:LOOK非表示
	;TARGETが大勢居るときはヒマを持て余す
	CALL ANOTHERTARGET_TALK
	CALL 情景描写
ENDIF
IF CFLAG:MASTER:現在位置 == CFLAG:MASTER:初期位置 && 在家召唤告白女神 == 1
	SETCOLOR C_PINK
	PRINTBUTTON "房间里放置着告白之神的信物", 11501
	PRINTL
	RESETCOLOR
ENDIF
IF CFLAG:MASTER:現在位置 == CFLAG:MASTER:初期位置 && 摄影系统开关 && !射影机
	PRINTBUTTON "一个表面有一层薄灰的木盒子静静地摆在桌子上", 11502
	PRINTL
ENDIF
FIGURE_STARTLINE = LINECOUNT
IF TARGET || 自机立绘显示
	IF !TFLAG:LOOK非表示
		PRINT 【上半身】
		IF TEQUIP:1 == 0
			PRINT 赤裸的 
			SIF PALAM:欲情 >= PALAMLV:3
				PRINTFORM %NIPPLE_DESCRIPTION(TARGET)%勃起着
			SIF 湿身度:TARGET > 10 && !TEQUIP:TARGET:SWIMMING
				PRINTFORM 上面满是雨水
		ELSE
			;外側の服から表示
			FOR 装备部位, 17, 6, -1
				SIF GROUPMATCH(装备部位, 15, 12, 11)
					CONTINUE
				表示内容 '= LOOK_AT_CLOTHNAME(TARGET, 装备部位)
				SIF !STRLENS(表示内容)
					CONTINUE
				PRINTFORM %表示内容% 
				IF !TEQUIP:TARGET:SWIMMING
					SELECTCASE 湿身度:TARGET
					CASE 10 TO 29
						PRINTFORM 雨水浸染下有什么东西露了出来 
					CASE 30 TO 80
						PRINTFORM 打湿的衣物间
						IF EQUIP:TARGET:7
							PRINTFORM %BRANAME(EQUIP:TARGET:7, TARGET)%
						ELSEIF EQUIP:TARGET:8
							PRINTFORM %BRANAME(EQUIP:TARGET:8, TARGET)%
						ELSE
							PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%
						ENDIF
						PRINTFORM 若隐若现 
					CASE 81 TO 100
						PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%的轮廓已经遮掩不住了 
					ENDSELECT			
				ENDIF
			BREAK
			NEXT
			IF TEQUIP:上半身裸露状態 > 0
				IF TALENT:胸围 == 2
					PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%呼之欲出
					;PRINTFORM はち切れんばかりの胸がこぼれ出ている
						SIF 湿身度:TARGET > 10 && !TEQUIP:TARGET:SWIMMING
						PRINTFORM 雨水刚落在乳根处便滑进了深邃的乳沟里 
				ELSEIF TALENT:胸围 == 1
					PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%快要蹦出来了
					;PRINTFORM 丰满的胸がこぼれ出ている
					SIF 湿身度:TARGET > 10 && !TEQUIP:TARGET:SWIMMING
						PRINTFORM 乳肉上的点点雨水衬得整对玉乳愈发鲜嫩可口 
				ELSEIF TALENT:胸围 == 0
					PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%快要露出来了
					;PRINTFORM 外形良好的胸が露わになっている
					SIF 湿身度:TARGET > 10 && !TEQUIP:TARGET:SWIMMING
						PRINTFORM 位置恰到好处的水滴和姣好双乳相得益彰 
				ELSEIF TALENT:胸围 == -1
					PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%若隐若现
					;PRINTFORM 小巧的胸が露わになっている
					SIF 湿身度:TARGET > 10 && !TEQUIP:TARGET:SWIMMING
						PRINTFORM 雨水留下的道道痕迹似能勾勒出乳房的轮廓 
				ELSEIF TALENT:胸围 == -2
					PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%若隐若现
					;PRINTFORM 可爱的乳头が露わになっている
					SIF 湿身度:TARGET > 10 && !TEQUIP:TARGET:SWIMMING
						PRINTFORM 雨水丝滑地沿着浅显的轮廓滑了下去 
				ENDIF
				SIF EQUIP:TARGET:ボディースーツ == 0 && PALAM:欲情 >= PALAMLV:5
					PRINTFORM  %NIPPLE_DESCRIPTION(TARGET)%勃起着
			ELSE
				SIF EQUIP:TARGET:ボディースーツ == 0 && PALAM:欲情 >= PALAMLV:MIN((4 + 2 * (GETMETH_INT(TARGET, "上半身内衣_はだけ不可", EQUIP:TARGET:上半身内衣１, "布料厚度"))),15)
					PRINTFORM 即使隔着衣服也能看出%NIPPLE_DESCRIPTION(TARGET)%已经勃起了
			ENDIF
		ENDIF
		IF TEQUIP:乳頭夾 && TEQUIP:揉胸
			PRINTFORM 乳头夹吸附在%NIPPLE_DESCRIPTION(TARGET)%上%OPPAI_DESCRIPTION(TARGET)%也被%CALLNAME:MASTER%揉搓着
		ELSEIF TEQUIP:吸乳头 && TEQUIP:揉胸
			PRINTFORM %CALLNAME:MASTER%一边揉着%OPPAI_DESCRIPTION(TARGET)%一边\@ ABL:MASTER:舌 > 2? 用舌头舔弄# 吸吮\@%NIPPLE_DESCRIPTION(TARGET)%
		ELSEIF TEQUIP:乳頭夾
			PRINTFORM 乳头夹吸附在%NIPPLE_DESCRIPTION(TARGET)%上
		ELSEIF TEQUIP:搾乳機
			PRINT 搾乳机正在搾乳母乳
		ELSEIF TEQUIP:吸乳头
			PRINTFORM %CALLNAME:MASTER%\@ ABL:MASTER:舌 > 2 ? 舔舐着 # 吮吸着 \@%NIPPLE_DESCRIPTION(TARGET)%
		ELSEIF TEQUIP:揉胸
			PRINTFORM %CALLNAME:MASTER%揉搓着%OPPAI_DESCRIPTION(TARGET)%
		ENDIF
		SIF CFLAG:口内精液 == 1
			PRINT 精液垂挂在嘴角上
		SIF CFLAG:脸上精液 == 1
			PRINT 精液浇在了脸上
		SIF CFLAG:手上精液 == 1
			PRINT 手上沾满了精液
		PRINTL
		PRINT 【下半身】
		;TEQUIP:下半身着衣状況 下半身着衣状况(位0=裙子 1=可拉扯 2=不可拉扯 3=不可拉扯的下半身外衣)
		;	(不希望使用位的人用 0=无 1=无内裤裙子 2=只有内裤 3=裙子+内裤 
		;	4=紧身裤(内衣)或无内裤裤子 5=裙子+紧身裤 6=裤子+内裤 7=全装备(此变体无法实现)）
		;CFLAG:パンツ 内裤标志
		;0 穿着内裤
		;1 没穿内裤

		;没穿下半身衣物，或者没穿内裤
		IF TEQUIP:下半身着衣状況 == 0 || CFLAG:没穿内裤 > 0
			;穿着下半身衣物，但是没穿内裤
			IF CFLAG:没穿内裤 && TEQUIP:下半身着衣状況 != 0
				着衣状況 = 0
				FOR 装备部位, 15, 4, -1
					SIF GROUPMATCH(装备部位, 8, 7)
						CONTINUE
					表示内容 '= LOOK_AT_CLOTHNAME(TARGET, 装备部位)
					SIF !STRLENS(表示内容)
						CONTINUE
					PRINTFORM %表示内容% 
					BREAK
				NEXT
				;胖次は是ていないが見えないので表示しない
				;PRINT （胖次沒穿）
				IF PREVCOM == 330
					IF PALAM:潤滑 >= PALAMLV:7
						PRINT  下体洪水泛滥 爱液拉出淫丝❤
					ELSEIF PALAM:潤滑 >= PALAMLV:6
						PRINT  腿上流的全是水
					ELSEIF PALAM:潤滑 >= PALAMLV:4
						PRINT  爱液沿腿流下
					ELSEIF PALAM:潤滑 >= PALAMLV:2
						PRINT  羞人的部分湿气满满
					ELSEIF PALAM:潤滑 >= PALAMLV:1
						PRINT  羞人的部分濡湿了
					ENDIF
					
					IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150
						PRINTFORM  可以听到精液喷出的噗嘟啪哒声❤
					ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100
						PRINTFORM  挤出的精液拉着不透明的白色淫丝下落
					ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70
						PRINTFORM  溢出的精液顺腿流下
					ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40
						PRINTFORM  大腿上留有精液的痕迹
					ELSEIF 充填率(TARGET,1) >= 10 || 充填率(TARGET,2) >= 10
						PRINTFORM  羞人的部分挂着精液
					ENDIF
					
				ELSEIF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150
					PRINTFORM  周围空气里弥漫着\@TALENT:MASTER:濃厚精液 ? 浓厚且独属于%CALLNAME:MASTER%白浊的气味 # 厚重的精液气味\@ ❤
				ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100
					PRINTFORM  喷出的\@TALENT:MASTER:濃厚精液 ? 浓厚白浊在脚边堆成了山 # 精液在脚边形成一滩水渍\@
				ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70
					PRINTFORM  溢出的\@TALENT:MASTER:濃厚精液 ? 粘稠精液在大腿上拉出淫丝 # 精液垂落拉丝\@
				ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40
					PRINTFORM  大腿上\@TALENT:MASTER:濃厚精液 ? 满是粘稠精液的痕迹 # 满是精液的痕迹\@
				ELSEIF 充填率(TARGET,1) >= 10 || 充填率(TARGET,2) >= 10
					PRINTFORM  羞人的部分\@TALENT:MASTER:濃厚精液 ? 残留着粘稠精液的痕迹 # 残留着精液的痕迹\@
				ENDIF

			ELSEIF EQUIP:下半身内衣１ || EQUIP:下半身内衣２
				;2025年7月10日修改 创口贴、兜裆布、贞操带在两类内裤中重复，可以复用加笔的分枝
				IF EQUIP:下半身内衣２
					表示内容 '= PANTSNAME(EQUIP:TARGET:下半身内衣２, TARGET)
				ELSE
					表示内容 '= GETMETH_STR(TARGET, "下半身内衣_ずらし不可", EQUIP:TARGET:下半身内衣１, "名前")
				ENDIF
				SELECTCASE 表示内容
					CASE "兜襠布", "安全褲"
						IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150 || PALAM:潤滑 >= PALAMLV:7
							PRINTFORM  %表示内容%吸满了溢出的体液不断下坠、羞人的部分被看光光了❤
						ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100 || PALAM:潤滑 >= PALAMLV:6
							PRINTFORM  %表示内容%沉甸甸的不能再吸收体液了、羞人的部分要露出来了❤
						ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70 || PALAM:潤滑 >= PALAMLV:4
							PRINTFORM  %表示内容%明显完全湿透了、真诱人❤
						ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40 || PALAM:潤滑 >= PALAMLV:2
							PRINTFORM  %表示内容%上面染满了水渍
						ELSEIF CFLAG:TARGET:膣内射精 == 1 || CFLAG:TARGET:肛門射精 == 1 || PALAM:潤滑 >= PALAMLV:1
							PRINTFORM  %表示内容%有被体液濡湿的痕迹
						ENDIF

					CASE "Ｃ字袴", "創口貼","御札"
						IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150 || PALAM:潤滑 >= PALAMLV:7
							PRINTFORM  %表示内容%已经剥落了下来和黏液一起垂落、露出了羞人的部分❤
						ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100 || PALAM:潤滑 >= PALAMLV:6
							PRINTFORM  %表示内容%勉强在上面挂着、羞人的部分能看到一半了❤
						ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70 || PALAM:潤滑 >= PALAMLV:4
							PRINTFORM  %表示内容%开始剥落了、羞人的部分都要露出来了❤
						ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40 || PALAM:潤滑 >= PALAMLV:2
							PRINTFORM  %表示内容%完全湿透了
						ELSEIF CFLAG:TARGET:膣内射精 == 1 || CFLAG:TARGET:肛門射精 == 1 || PALAM:潤滑 >= PALAMLV:1
							PRINTFORM  %表示内容%有被体液濡湿的痕迹
						ENDIF

					CASE "貞操帯", "陰唇環", "陰蒂環"
						IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150 || PALAM:潤滑 >= PALAMLV:7
							IF 表示内容 == "貞操帯"
								PRINTFORM  %表示内容%的缝隙不断喷出体液❤
							ELSE
								PRINTFORM  %表示内容%被溢出的体液彻底淹没❤
							ENDIF
						ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100 || PALAM:潤滑 >= PALAMLV:6
							IF 表示内容 == "貞操帯"
								PRINTFORM  %表示内容%完全无法阻止溢出的精液❤
							ELSE
								PRINTFORM  %表示内容%被溢出的体液搞到完全看不清❤
							ENDIF
						ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70 || PALAM:潤滑 >= PALAMLV:4
							PRINTFORM  %表示内容%里面悬满了体液
						ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40 || PALAM:潤滑 >= PALAMLV:2
							PRINTFORM  %表示内容%完全湿透了
						ELSEIF CFLAG:TARGET:膣内射精 == 1 || CFLAG:TARGET:肛門射精 == 1 || PALAM:潤滑 >= PALAMLV:1
							PRINTFORM  %表示内容%被体液打湿
						ENDIF
					CASEELSE
						IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150 || PALAM:潤滑 >= PALAMLV:7
							PRINTFORM  %表示内容%吸满了溢出的体液不断下坠、羞人的部分被看光光了❤
						ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100 || PALAM:潤滑 >= PALAMLV:6
							PRINTFORM  %表示内容%沉甸甸的不能再吸收体液了、羞人的部分要露出来了❤
						ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70 || PALAM:潤滑 >= PALAMLV:4
							PRINTFORM  %表示内容%明显完全湿透了、真诱人❤
						ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40 || PALAM:潤滑 >= PALAMLV:2
							PRINTFORM  %表示内容%上面染满了水渍
						ELSEIF CFLAG:TARGET:膣内射精 == 1 || CFLAG:TARGET:肛門射精 == 1 || PALAM:潤滑 >= PALAMLV:1
							PRINTFORM  %表示内容%有被体液濡湿的痕迹
						ENDIF
				ENDSELECT
			;没穿下半身衣物
			ELSE
				PRINT 赤裸着 
				SIF TEQUIP:陰蒂夾
					PRINT 阴蒂夹吸在阴蒂上震动
				SIF TEQUIP:飛機杯
					PRINT 飞机杯套在阴茎上震动
				IF !CFLAG:诶嘿嘿
					IF CFLAG:TARGET:膣内射精
						IF CFLAG:TARGET:肛門射精 == 1
							IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150
								PRINT 精液从两穴涌泉般喷泻而出
							ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100
								PRINT 精液从两穴中如关不紧的水龙头一样喷出❤
							ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70
								PRINT 精液从两穴中不断溢出 
							ELSEIF 充填率(TARGET,1) >= 30 || 充填率(TARGET,2) >= 30
								PRINT 精液从两穴中溢出
							ELSE
								PRINT 精液从私处和肛门里缓缓流出 
							ENDIF
						ELSEIF EX:処女喪失记号
							PRINT 混合着破瓜之血的精液从私处流了出来
						ELSEIF PALAM:潤滑 >= PALAMLV:2
							PRINT 精液和爱液的混合物从私处流了出来
						ELSE
							PRINT 精液从蜜唇中流了出来 
						ENDIF
					ELSEIF CFLAG:TARGET:肛門射精 == 1
						PRINT 精液从肛门里流了出来
					ELSEIF PALAM:潤滑 >= PALAMLV:2
						IF PALAM:潤滑 >= PALAMLV:6
							PRINTFORM \@ EX:処女喪失记号? 混合着破瓜之血的# \@爱液从私处滴落下来
						ELSEIF EX:処女喪失记号
							PRINT 破瓜之血从私处流了出来 
						ELSEIF PALAM:潤滑 >= PALAMLV:4
							PRINT 爱液流到了大腿上
						ELSEIF PALAM:潤滑 >= PALAMLV:2
							PRINT 私处被爱液打湿了
						ENDIF
					ENDIF
				ELSEIF PALAM:潤滑 >= PALAMLV:2 && !(CFLAG:TARGET:膣内射精 == 1 || TEQUIP:Ｖ性交 == PLAYER || TEQUIP:振動棒)
					IF PALAM:潤滑 >= PALAMLV:4
						PRINTFORM \@ EX:処女喪失记号? 混合着破瓜之血的# \@爱液从私处滴落下来
					ELSEIF EX:処女喪失记号
						PRINT 破瓜之血从私处流了出来
					ELSEIF PALAM:潤滑 >= PALAMLV:3
						PRINTFORM 爱液流到了大腿上 
					ELSEIF PALAM:潤滑 >= PALAMLV:2
						PRINTFORM 私处被爱液打湿了 
					ELSEIF TALENT:処女 == 1
						PRINTFORM 露出了未经人事紧紧闭合的蜜缝 
					ENDIF
				ENDIF
			ENDIF
		;穿着下半身衣物，并且也穿着内衣
		ELSE
			着衣状況 = 0
			;如果穿着上下连体的外套，就显示它
			;如果没有，就显示裙子或裤子
			;因为没有特别复杂的顺序，所以合并了循环
			FOR 装备部位, 15, 4, -1
				SIF GROUPMATCH(装备部位, 8, 7)
					CONTINUE
				表示内容 '= LOOK_AT_CLOTHNAME(TARGET, 装备部位)
				SIF !STRLENS(表示内容)
					CONTINUE
				PRINTFORM %表示内容% 
				IF !TEQUIP:TARGET:SWIMMING
					SELECTCASE 湿身度:TARGET
					CASE 10 TO 29
						PRINTFORM 布料因为飞来的雨水似乎起了皱 
					CASE 30 TO 80
						PRINTFORM 布料吸满了水贴在大腿上显得腿型更加优美 
					CASE 80 TO 100
						PRINTFORM 湿透的布料衬托得%HIP(TARGET)%愈发诱人 
					ENDSELECT
				ENDIF
				BREAK
			NEXT
			IF !CFLAG:诶嘿嘿
				;なんか分岐入ってたけどTEQUIP:下半身着衣状況 == 0は↑で处理されるので死に分岐
				;IF TEQUIP:下半身着衣状況 != 0
				;ENDIF
				IF PALAM:潤滑 >= PALAMLV:7
					PRINT  简直是洪水泛滥❤
				ELSEIF PALAM:潤滑 >= PALAMLV:6
					PRINT  腿上流的全是水
				ELSEIF PALAM:潤滑 >= PALAMLV:4
					PRINT  连腿都被弄得湿答答的
				ELSEIF PALAM:潤滑 >= PALAMLV:2
					PRINT  羞人的部分湿气满满
				ELSEIF PALAM:潤滑 >= PALAMLV:1
					PRINT  羞人的部分染上了一滴滴的污渍
				ENDIF
				;小人
				IF TALENT:TARGET:100 == -5
					LOCAL = 200
				;幼儿体型、矮小体型
				ELSEIF TALENT:TARGET:100 < 0
					LOCAL = 300
				;长身以上
				ELSEIF TALENT:TARGET:100 >= 1
					LOCAL = 500
				;普通体型
				ELSE
					LOCAL = 400
				ENDIF
				SIF EX:TARGET:精液残量 > LOCAL || EX:TARGET:アナル精液残量 > LOCAL
					PRINTFORM  溢出的\@TALENT:MASTER:濃厚精液 ? 粘稠精液在大腿上拉出淫丝 # 精液垂落拉丝\@
			;ここからは诶嘿嘿中
			ELSEIF TEQUIP:下半身着衣状況 & 4
				IF PALAM:潤滑 >= PALAMLV:7
					PRINT  简直是洪水泛滥❤
				ELSEIF PALAM:潤滑 >= PALAMLV:6
					PRINT  腿上流的全是水
				ELSEIF PALAM:潤滑 >= PALAMLV:4
					PRINT  连腿都被弄得湿答答的
				ELSEIF PALAM:潤滑 >= PALAMLV:2
					PRINT  羞人的部分湿气满满
				ELSEIF PALAM:潤滑 >= PALAMLV:1
					PRINT  羞人的部分染上了一滴滴的污渍
				ENDIF
			ELSE
				LOCALS = %HIP(TARGET)%
				PRINTFORM 被拨开后露出%LOCALS%
				IF PALAM:潤滑 >= PALAMLV:1 || CFLAG:TARGET:肛門射精 == 1 || CFLAG:TARGET:膣内射精 == 1 && !((CFLAG:TARGET:肛門射精 == 1 && CFLAG:TARGET:膣内射精 == 1) || TEQUIP:Ｖ性交 == PLAYER || TEQUIP:振動棒 || TEQUIP:Ａ性交 == PLAYER || TEQUIP:肛用振動棒 || TEQUIP:後庭拉珠 || TEQUIP:浣腸 || TEQUIP:拡張氣球 || TEQUIP:肛門電極)
					;2025年7月10日修改 创口贴、兜裆布、贞操带在两类内裤中重复，可以复用加笔的分枝
					IF EQUIP:下半身内衣２
						表示内容 '= PANTSNAME(EQUIP:TARGET:下半身内衣２, TARGET)
					ELSE
						表示内容 '= GETMETH_STR(TARGET, "下半身内衣_ずらし不可", EQUIP:TARGET:下半身内衣１, "名前")
					ENDIF
					SELECTCASE 表示内容
						CASE "兜裆布", "安全裤"
							IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150 || PALAM:潤滑 >= PALAMLV:7
								PRINTFORM  %表示内容%吸满了溢出的体液不断下坠、羞人的部分被看光光了❤
							ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100 || PALAM:潤滑 >= PALAMLV:6
								PRINTFORM  %表示内容%沉甸甸的不能再吸收体液了、羞人的部分要露出来了❤
							ELSEIF STRCOUNT(LOCALS,表示内容)
								;这以下的与HIP描述重复
							ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70 || PALAM:潤滑 >= PALAMLV:4
								PRINTFORM  %表示内容%明显完全湿透了、真诱人❤
							ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40 || PALAM:潤滑 >= PALAMLV:2
								PRINTFORM  %表示内容%上面染满了水渍
							ELSEIF CFLAG:TARGET:膣内射精 == 1 || CFLAG:TARGET:肛門射精 == 1 || PALAM:潤滑 >= PALAMLV:1
								PRINTFORM  %表示内容%有被体液濡湿的痕迹
							ENDIF

						CASE "Ｃ字裤", "创口贴","御札"
							IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150 || PALAM:潤滑 >= PALAMLV:7
								PRINTFORM  %表示内容%已经剥落了下来和黏液一起垂落、露出了羞人的部分❤
							ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100 || PALAM:潤滑 >= PALAMLV:6
								PRINTFORM  %表示内容%勉强在上面挂着、羞人的部分能看到一半了❤
							ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70 || PALAM:潤滑 >= PALAMLV:4
								PRINTFORM  %表示内容%开始剥落了、羞人的部分都要露出来了❤
							ELSEIF STRCOUNT(LOCALS,表示内容)
								;这以下的与HIP描述重复
							ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40 || PALAM:潤滑 >= PALAMLV:2
								PRINTFORM  %表示内容%完全湿透了
							ELSEIF CFLAG:TARGET:膣内射精 == 1 || CFLAG:TARGET:肛門射精 == 1 || PALAM:潤滑 >= PALAMLV:1
								PRINTFORM  %表示内容%有被体液濡湿的痕迹
							ENDIF

						CASE "贞操带", "阴唇环", "阴蒂环"
							IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150 || PALAM:潤滑 >= PALAMLV:7
								IF 表示内容 == "贞操带"
									PRINTFORM  %表示内容%的缝隙不断喷出体液❤
								ELSE
									PRINTFORM  %表示内容%被溢出的体液彻底淹没❤
								ENDIF
							ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100 || PALAM:潤滑 >= PALAMLV:6
								IF 表示内容 == "贞操带"
									PRINTFORM  %表示内容%完全无法阻止溢出的精液❤
								ELSE
									PRINTFORM  %表示内容%被溢出的体液搞到完全看不清❤
								ENDIF
							ELSEIF STRCOUNT(LOCALS,表示内容)
								;这以下的与HIP描述重复
							ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70 || PALAM:潤滑 >= PALAMLV:4
								PRINTFORM  %表示内容%里面悬满了体液
							ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40 || PALAM:潤滑 >= PALAMLV:2
								PRINTFORM  %表示内容%完全湿透了
							ELSEIF CFLAG:TARGET:膣内射精 == 1 || CFLAG:TARGET:肛門射精 == 1 || PALAM:潤滑 >= PALAMLV:1
								PRINTFORM  %表示内容%被体液打湿
							ENDIF
						CASEELSE
							IF 充填率(TARGET,1) >= 150 || 充填率(TARGET,2) >= 150 || PALAM:潤滑 >= PALAMLV:7
								PRINTFORM  %表示内容%吸满了溢出的体液不断下坠、羞人的部分被看光光了❤
							ELSEIF 充填率(TARGET,1) >= 100 || 充填率(TARGET,2) >= 100 || PALAM:潤滑 >= PALAMLV:6
								PRINTFORM  %表示内容%沉甸甸的不能再吸收体液了、羞人的部分要露出来了❤
							ELSEIF STRCOUNT(LOCALS,表示内容)
								;这以下的与HIP描述重复
							ELSEIF 充填率(TARGET,1) >= 70 || 充填率(TARGET,2) >= 70 || PALAM:潤滑 >= PALAMLV:4
								PRINTFORM  %表示内容%明显完全湿透了、真诱人❤
							ELSEIF 充填率(TARGET,1) >= 40 || 充填率(TARGET,2) >= 40 || PALAM:潤滑 >= PALAMLV:2
								PRINTFORM  %表示内容%上面染满了水渍
							ELSEIF CFLAG:TARGET:膣内射精 == 1 || CFLAG:TARGET:肛門射精 == 1 || PALAM:潤滑 >= PALAMLV:1
								PRINTFORM  %表示内容%有被体液濡湿的痕迹
							ENDIF
					ENDSELECT
				ENDIF
				IF !EQUIP:下半身内衣２
				ELSEIF TEQUIP:Ｖ性交 == PLAYER || TEQUIP:振動棒 || TEQUIP:Ａ性交 == PLAYER || TEQUIP:肛用振動棒 || TEQUIP:後庭拉珠 || TEQUIP:浣腸 || TEQUIP:拡張氣球 || TEQUIP:肛門電極
					PRINTFORM  %PANTSNAME(EQUIP:TARGET:下半身内衣２, TARGET)%为了爱爱而被错开
				ELSEIF CFLAG:TARGET:肛門射精 == 1 && CFLAG:TARGET:膣内射精 == 1 && !(TEQUIP:Ｖ性交 == PLAYER || TEQUIP:振動棒 || TEQUIP:Ａ性交 == PLAYER || TEQUIP:肛用振動棒 || TEQUIP:後庭拉珠 || TEQUIP:浣腸 || TEQUIP:拡張氣球 || TEQUIP:肛門電極)
					PRINTFORM  %PANTSNAME(EQUIP:TARGET:下半身内衣２, TARGET)%湿了一大片
				ENDIF
				IF CFLAG:TARGET:膣内射精 == 1 && PALAM:潤滑 >= PALAMLV:1 && !(TEQUIP:Ｖ性交 == PLAYER || TEQUIP:振動棒) && (TEQUIP:Ａ性交 == PLAYER || TEQUIP:肛用振動棒 || TEQUIP:後庭拉珠 || TEQUIP:浣腸 || TEQUIP:拡張氣球 || TEQUIP:肛門電極)
					PRINT  爱液混合着白浊流了出来 
				ELSEIF CFLAG:TARGET:膣内射精 == 1 && !(TEQUIP:Ｖ性交 == PLAYER || TEQUIP:振動棒 || PALAM:潤滑 >= PALAMLV:1) && (TEQUIP:Ａ性交 == PLAYER || TEQUIP:肛用振動棒 || TEQUIP:後庭拉珠 || TEQUIP:浣腸 || TEQUIP:拡張氣球 || TEQUIP:肛門電極)
					PRINT  精液从蜜唇中流了出来 
				ELSEIF CFLAG:TARGET:肛門射精 == 1 && !(TEQUIP:Ａ性交 == PLAYER || TEQUIP:肛用振動棒 || TEQUIP:後庭拉珠 || TEQUIP:浣腸 || TEQUIP:拡張氣球 || TEQUIP:肛門電極) && (TEQUIP:Ｖ性交 == PLAYER || TEQUIP:振動棒)
					PRINT  精液从肛门里流了出来 
				ENDIF
			ENDIF
		ENDIF
		;================================
		SELECTCASE TEQUIP:TARGET:体位
			CASE 1
				插入状况 = \@ TCVAR:TARGET:强行?以正常位压制# 以正常位\@
			CASE 2 
				插入状况 = \@ TCVAR:TARGET:强行?以后入式压着# 以后入式\@
			CASE 3 
				插入状况 = \@ TCVAR:TARGET:强行?以强迫骑乘位# 以骑乘位\@
			CASE 4 
				插入状况 = \@ TCVAR:TARGET:强行?紧紧搂着#以面对面坐位\@
			CASE 5 
				插入状况 = \@ TCVAR:TARGET:强行?从身后挟住腰# 以背对坐位\@
			CASE 6
				插入状况 = 像对待飞机杯一样
		ENDSELECT

		IF TEQUIP:TARGET:Ｖ性交 == PLAYER || TEQUIP:振動棒
			PRINTL
			CALL HPH_PRINT, "【HPH膣内HPH】"
			IF TEQUIP:TARGET:Ｖ性交 == PLAYER
				IF TCVAR:V被中出FLAG
					IF CFLAG:TARGET:生理周期 == 7
						SETCOLOR C_PINK
						PRINTFORM 被%CALLNAME:MASTER%%插入状况%\@ EX:処女喪失记号 == 1 ? 在危险日的处女嫩穴里# 在危险日的蜜穴里\@灌注了{NOWEX:TARGET:膣内精液}mL\@TALENT:MASTER:濃厚精液 ?浓厚#\@孕汁 
						RESETCOLOR
					ELSE
						SETCOLOR C_PINK
						PRINTFORM 被%CALLNAME:MASTER%%插入状况%\@ EX:処女喪失记号 == 1 ? 在处女嫩穴里# 在蜜穴里\@注入了{NOWEX:TARGET:膣内精液}mL\@TALENT:MASTER:濃厚精液 ?浓厚的 # \@精液 
						RESETCOLOR
					ENDIF
				ELSEIF SHIRAHU(TARGET) && TALENT:TARGET:恋慕
					IF EX:処女喪失记号 == 1
						SETCOLOR C_PINK
						PRINTFORM 把处女献给了%插入状况%插入进来的%CALLNAME:MASTER% 
						RESETCOLOR
					ELSE
						SETCOLOR C_PINK
						PRINTFORM 与%插入状况%插入的%CALLNAME:MASTER%深情交合❤ 
						RESETCOLOR
					ENDIF
				ELSEIF CFLAG:诶嘿嘿 == 2
					PRINTFORM 被%CALLNAME:MASTER%%插入状况%插入着\@ EX:処女喪失记号 == 1? 处子穴#\@ 
				ELSE
					PRINTFORM 被%CALLNAME:MASTER%%插入状况%插入着\@ EX:処女喪失记号 == 1? 处子穴#\@ 
				ENDIF
			ELSEIF TEQUIP:振動棒
				PRINT  被震动棒插入着 
			ENDIF
			SELECTCASE TEQUIP:子宮
				CASE 1 
	 				SETCOLOR C_PINK
	 					PRINT 下降着的子宫颤抖着❤子宫口在兴奋地吮吸
	 				RESETCOLOR
	 				;リセットしないとAct_COM行と[V]まで粉色になったため追加
				CASE 2 
					FONTBOLD
	 				SETCOLOR C_PINK
	 					PRINT 下降着的子宫颤抖着❤等待着精液的浇灌
	 				RESETCOLOR
	 				FONTREGULAR
			ENDSELECT
		ENDIF
		IF TEQUIP:TARGET:Ａ性交 == PLAYER || TEQUIP:肛用振動棒 || TEQUIP:後庭拉珠
			PRINTL
			CALL HPH_PRINT, "【HPH肛门HPH】"
			IF TCVAR:TARGET:A挿入管理FLAG == PLAYER
				IF TCVAR:A被中出FLAG
					PRINTFORM 被注入了{NOWEX:TARGET:肛門内精液}mL\@TALENT:MASTER:濃厚精液 ?浓厚的 # \@精液 
				ELSEIF (!FLAG:70 || CFLAG:TARGET:344 == 1) && !CFLAG:睡眠 && TALENT:TARGET:恋慕
					PRINTFORM 被%CALLNAME:MASTER%%插入状况%插入着 
				ELSEIF CFLAG:诶嘿嘿 == 2
					PRINTFORM 被%CALLNAME:MASTER%%插入状况%插入着 
				ELSE
					PRINTFORM 被%CALLNAME:MASTER%%插入状况%插入着 
				ENDIF
			ELSEIF TEQUIP:肛用振動棒
				PRINT 被肛用震动棒插入着 
			ELSEIF TEQUIP:後庭拉珠
				PRINT 被后庭拉珠插入着 
			; ELSEIF TEQUIP:浣腸
			; 	PRINT 被肛塞塞着　 
			; ELSEIF TEQUIP:拡張氣球
			; 	PRINT 被扩张气球扩张着　 
			; ELSEIF TEQUIP:肛門電極
			; 	PRINT 被电极插入着　
			ENDIF
		ENDIF
		IF TEQUIP:PLAYER:Ｖ性交 == TARGET || TEQUIP:PLAYER:Ａ性交 == TARGET
			SELECTCASE TEQUIP:MASTER:体位
				CASE 1 
					插入状况2 = 正常位
				CASE 2 
					插入状况2 = 后背位
				CASE 3 
					插入状况2 = 骑乘位
				CASE 4 
					插入状况2 = 对面座位
				CASE 5 
					插入状况2 = 背面座位
				CASE 6
					插入状况2 = 对待飞机杯一样
				CASEELSE
					插入状况2 = 
			ENDSELECT
			PRINTL 
			SETCOLOR C_PINK
			PRINT 【阴茎】
			RESETCOLOR
			;就算女MASTER没有妊娠能力至少涩涩还是有的
			;现在有了
			IF TEQUIP:PLAYER:Ｖ性交 == TARGET
				SETCOLOR C_PINK
				PRINTFORM 以%插入状况2%插入%CALLNAME:PLAYER%穴内❤
				RESETCOLOR
				SELECTCASE TEQUIP:MASTER:子宮
					CASE 1 
						SETCOLOR C_PINK
						PRINT 下降着的子宫颤抖着❤子宫口在兴奋地吮吸
						RESETCOLOR
						;リセットしないとAct_COM行と[V]まで粉色になったため追加
					CASE 2 
						FONTBOLD
						SETCOLOR C_PINK
						PRINT 下降着的子宫颤抖着❤等待着精液的浇灌
						RESETCOLOR
						FONTREGULAR
				ENDSELECT
			ELSEIF TEQUIP:PLAYER:Ａ性交 == TARGET
				SETCOLOR C_ORANGE
				PRINTFORM 以%插入状况2%插入%CALLNAME:PLAYER%的肛内❤
				RESETCOLOR
			ENDIF
			SIF NOWEX:TARGET:射精量
				PRINTFORM ，射入了{NOWEX:TARGET:射精量}mL
			PRINTL
		ENDIF
		CALL LOOK_BELLY(TARGET)
		CALL LOOK_RING(TARGET) ;marriage custom code
		CALL LOOK_GIFT(TARGET)
	ENDIF
	IF FLAG:画像表示
		SIF !TFLAG:LOOK非表示
			PRINTL 　
		CALL PRINT_TARGET_IMAGE(TARGET)
	ENDIF
ELSE
	IF !TFLAG:LOOK非表示
		;視点が大きくずれるのを防ぐためにTARGETがいないときの余白,パワプロみたいに小さい背景画でも那个ばいいんだけど
		FOR LOCAL, 1, 16
			PRINTL 
		NEXT
	ENDIF
ENDIF
[SKIPSTART]
PRINTL

IF CFLAG:诶嘿嘿 && TALENT:MASTER:性別 & 1
	IF TEQUIP:MASTER:Ａ性交 > 0
		肛门 = TEQUIP:MASTER:Ａ性交
		TARGET = 肛门
		TFLAG:現在的TARGET = TARGET
		PRINTFORM 【女主肛内】  %CALLNAME:肛門%    
	ENDIF
	IF TEQUIP:MASTER:Ｖ性交 > 0
		小穴 = TEQUIP:MASTER:Ｖ性交
		TARGET = 小穴
		TFLAG:現在的TARGET = TARGET
		PRINTFORM 【女主膣内】  %CALLNAME:小穴%    
	ENDIF
	PRINTL
	IF EX:Master:肛門内精液 > 0
		PRINTFORM  肛内精液量:{EX:Master:肛門内精液,11}  -  来自:
	FOR LOCAL,1,CHARANUM
		SIF TCVAR:LOCAL:603
			PRINTFORM %CALLNAME:LOCAL%❤{TCVAR:LOCAL:603}  
	NEXT
		PRINTL
	ENDIF
	IF EX:Master:膣内精液 > 0
		PRINTFORM  膣内精液量:{EX:Master:膣内精液,11}  -  来自:
	FOR LOCAL,1,CHARANUM
		SIF TCVAR:LOCAL:601
			PRINTFORM %CALLNAME:LOCAL%❤{TCVAR:LOCAL:601}  
	NEXT
		PRINTL
	ENDIF

	IF EX:Master:子宮内精液 > 0
		SETCOLOR C_CREAM
		PRINTFORM  子宮精液量:{EX:Master:子宮内精液,4}❤{充填率(Master,4),3}％
		RESETCOLOR
		PRINTFORM   -  来自:
	FOR LOCAL,1,CHARANUM
		SIF TCVAR:LOCAL:602
			PRINTFORM %CALLNAME:LOCAL%❤{TCVAR:LOCAL:602}  
	NEXT
		PRINTL
	ENDIF
	PRINTL
ENDIF
[SKIPEND]

@LOOK_AT_CLOTHNAME(C_ID, E_ID)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM C_ID;对象角色
#DIM E_ID;对象となるEQUIPの部位
SIF !EQUIP:C_ID:E_ID
	RETURNF
IF 0;CSTR:C_ID:(50 + E_ID) != "" && !CFLAG:C_ID:睡衣 已弃用
	RETURNF CSTR:C_ID:(50 + E_ID)
ELSEIF EQUIPNAME:E_ID == "上半身内衣１"
	RETURNF BRANAME(EQUIP:C_ID:E_ID, C_ID)
ELSEIF EQUIPNAME:E_ID == "下半身内衣２"
	RETURNF PANTSNAME(EQUIP:C_ID:E_ID, C_ID)
ELSE
	RETURNF CLOTHNAME(E_ID, EQUIP:C_ID:E_ID, C_ID)
ENDIF



;身体部位 1=V, 2=A, 3=VA 4= 子宮 5=妊娠判定用 6=演算给定的填充率
@充填率(キャラ登録番号 = -1, 身体部位, GET_ACCUMULATIVE = 0, 精液量)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM キャラ登録番号
#DIM 身体部位
#DIM DYNAMIC 精液量
#DIM 身体容量
#DIM GET_ACCUMULATIVE
SIF キャラ登録番号 == -1
	キャラ登録番号 = TARGET
;該当部位の精液量の取得
SELECTCASE 身体部位
	CASE 1
		IF GET_ACCUMULATIVE
			精液量 = EX:キャラ登録番号:膣内精液
		ELSE
			精液量 = EX:キャラ登録番号:精液残量
		ENDIF
	CASE 2
		IF GET_ACCUMULATIVE
			精液量 = EX:キャラ登録番号:アナル内精液
		ELSE
			精液量 = EX:キャラ登録番号:アナル精液残量
		ENDIF
	CASE 3
		IF GET_ACCUMULATIVE
			精液量 = EX:キャラ登録番号:膣内精液 + EX:キャラ登録番号:アナル内精液
		ELSE
			精液量 = EX:キャラ登録番号:精液残量 + EX:キャラ登録番号:アナル精液残量
		ENDIF
	CASE 4
		精液量 = EX:キャラ登録番号:子宮内精液
	CASE 5
		精液量 = EX:キャラ登録番号:妊娠判定用精液量
ENDSELECT

;該当部位の容量を取得
SELECTCASE TALENT:キャラ登録番号:体型
	CASE -5
		身体容量 = 300
	CASE -4 TO -2
		身体容量 = 600
	CASE -1
		身体容量 = 1000
	CASE 0
		身体容量 = 1200
	CASE IS > 0
		身体容量 = 1500
ENDSELECT

;子宮ならば容量半減
IF GROUPMATCH(身体部位, 4, 5) 
	身体容量 /= 2
;因为充填率新设计了上限，3=VA不再按两个充填率之和计算
ELSEIF 身体部位 == 3
	身体容量 *= 2
ENDIF
;RETURNF 100 * 精液量 / 身体容量
;线性收益的有理化修正，100%以内的充填率仍符合拟合，超过100%开始削减增幅
RETURNF 3 * 100 * 精液量 / (2 * 身体容量 + 精液量)

;腹描写
@LOOK_BELLY(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 0
IF TCVAR:TARGET:V挿入管理FLAG == PLAYER || TCVAR:TARGET:A挿入管理FLAG == PLAYER
	PRINTL
	CALL HPH_PRINT, @"【HPH\@CFLAG:ARG:妊娠経過日数 >= 20?孕肚#小腹\@HPH】"
ELSEIF 充填率(ARG,3) >= 15
	PRINTL
	PRINTFORM 【\@CFLAG:ARG:妊娠経過日数 >= 20?孕肚#小腹\@】
ENDIF
IF 充填率(ARG,3) >= 15
	CALL LOOK_BELLY_2(ARG)
ELSEIF CFLAG:ARG:妊娠経過日数 >= 80
	PRINT 临近分娩的肚子仿佛要撑破了
ELSEIF CFLAG:ARG:妊娠経過日数 >= 60
	PRINT 孕育着生命的肚子高高的鼓起
ELSEIF CFLAG:ARG:妊娠経過日数 >= 40
	PRINT 肚子隆起变得明显了
ELSEIF CFLAG:ARG:妊娠経過日数 >= 30
	PRINT 肚子开始隆起
ENDIF
;处理中文语法-主语
IF 充填率(ARG,3) >= 15 || CFLAG:ARG:妊娠経過日数 >= 30
	LOCAL = 1
	PRINT  
ENDIF
IF TCVAR:TARGET:V挿入管理FLAG == PLAYER || TCVAR:TARGET:A挿入管理FLAG == PLAYER
	SELECTCASE 插入不可(ARG)
		;CASE 3
			;这是小人飞机杯专属，特殊描述征集中
		;CASE 2
			;这是插入不可2，因为本来就少所以干脆先合并，差分描述征集中
			;PRINT  肉棒的轮廓清晰浮现在小腹上
		CASE 1,2,3
			PRINTDATA
				DATAFORM \@LOCAL?#小腹\@被大肉棒顶出明显的凸起
				DATAFORM \@LOCAL?#小腹上\@清晰地浮现出肉棒的轮廓
				DATAFORM \@LOCAL?#小腹\@随着抽插被顶得一起一伏
			ENDDATA
		CASE 0
			;假鸡吧进入这一分枝
			PRINTDATA
				DATAFORM \@LOCAL?里面#肚子里\@被插入的\@TALENT:PLAYER:2 & 2?肉棒#假阴茎\@撑得满满当当
				DATAFORM \@LOCAL?#小腹\@随着抽插被顶得一起一伏
			ENDDATA
		CASE -1
			;被大车碾了
			PRINTFORM 腹中清晰感受到小鸡鸡的火热
		CASE -2
			;牙签搅水缸，可我不想做出不利于撸管的描述
			PRINTFORM 腹中清晰感受到小鸡鸡的火热
	ENDSELECT	
	IF TALENT:PLAYER:形状 == 5
		PRINT 、勾住肉褶的凸起使得从腹中抽出时刺激非常强…
	ELSEIF TALENT:PLAYER:形状 == 4
		CALL HPH_PRINT,@"、肉棒的光芒\@LOCAL?#在小腹上\@透视出了肉腔的轮廓…好色哦HPH",
	ELSE
		PRINT …
	ENDIF
ENDIF

@LOOK_BELLY_2(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
IF 充填率(ARG,3) >= 250
	;数值上等于原先的1000%充填率
	IF TALENT:ARG:妊娠
		PRINT 肚子涨得仿佛要临盆了，精液像开了水龙头一样从股间喷涌而出
	ELSE
		PRINT 肚子仿佛要撑破了，精液像开了水龙头一样从股间喷涌而出
	ENDIF
ELSEIF 充填率(ARG,3) >= 125
	IF TALENT:ARG:妊娠
		PRINT 肚子涨得仿佛要临盆了，装不下的精液顺着大腿汩汩流下
	ELSE
		PRINT 肚子像孕妇一样隆起，装不下的精液顺着大腿汩汩流下
	ENDIF
ELSEIF 充填率(ARG,3) >= 75
	IF TALENT:ARG:妊娠
		PRINT 肚子涨得仿佛要临盆了
	ELSE
		PRINT 肚子像孕妇一样隆起
	ENDIF
ELSEIF 充填率(ARG,3) >= 50
	IF TALENT:ARG:妊娠
		PRINT 明显鼓胀起来的腹部与孕期完全对不上了
	ELSE
		PRINT 肚子胀鼓鼓的满满都是精液
	ENDIF
ELSEIF 充填率(ARG,3) >= 35
	IF TALENT:ARG:妊娠
		PRINT 大量的精液让腹部的尺寸肉眼可见的变大了一些
	ELSE
		PRINT 肚子因存储大量精液而鼓起
	ENDIF
ELSEIF 充填率(ARG,3) >= 15
	IF TALENT:ARG:妊娠
		PRINT 相当容量的精液将已经因妊娠而鼓起的小腹稍微抬起了一点
	ELSE
		PRINT 小腹因灌注的精液微微隆起
	ENDIF
ENDIF
