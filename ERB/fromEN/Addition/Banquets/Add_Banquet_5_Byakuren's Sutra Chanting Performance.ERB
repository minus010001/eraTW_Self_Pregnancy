@FADD_BANQUET5(ARG, O_DATA)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIMS DYNAMIC O_DATA

SELECTCASE O_DATA
	CASE "NAME"
		RETURNF "白莲读经Live"
	CASE "ACTIVITIES"
		RETURNF "听经"
	CASE "TERM"
		RETURNF "演唱会"
	CASE "CATEGORY"
		RETURNF "演唱会"
	CASE "PRIORITY"
		RETURNF "1"
	CASE "NOTIFY_PLAYER"
		RETURNF "1"
	CASE "HOST"
		RETURNF @"{[[白蓮]]}"
	CASE "HOST_LIST"
		RETURNF @"{[[白蓮]]}/{[[一輪]]}/{[[星]]}/{[[響子]]}"
	CASE "GUEST_LIST"
		RETURNF ""
	CASE "BANNED_LIST"
		RETURNF ""
	CASE "CONDITIONS"
		SIF DAY:3 >= 8 && DAY:3 <= 10
			RETURNF "1"
		RETURNF "0"
	CASE "WEATHER_DEPENDENT"
		RETURNF "0"
	CASE "KEEP_AWAKE"
		RETURNF "1"
	CASE "LISTEN_TO_MUSIC"
		RETURNF "1"
	CASE "PERFORMERS"
		RETURNF @"{[[白蓮]]}"
ENDSELECT
RETURNF ""

@ADD_BANQUET_PLANNING5
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC LOCATION
#DIM DYNAMIC SCALE
#DIM DYNAMIC START_TIME

LOCATION = P104講堂

;especially popular in summer amongst youkai
SCALE = 1000 + RAND:2000
SIF DAY:2 == 2
    SCALE += 1000
START_TIME = CLOCK_TO_TIME(23, 0)

CALL ADD_BANQUET_CREATE(5, LOCATION, START_TIME, DAY + (10 - DAY:3), 360, SCALE)
LOCAL = RESULT ;banquet_index

SIF LOCAL < 0 ;couldn't create
    RETURN

ADD_BANQUETS_INVITED:LOCAL = ADD_BANQUET_INVITE_INVITED
PRINTL  
PRINTFORMW 白莲好像又要举办通宵读经Live了。
PRINTFORML 

PRINTFORMW %ADD_BANQUET_TIME_AND_PLACE_DESCRIPTION(LOCAL)%

@ADD_BANQUET_START5
#LOCALSIZE 1
#LOCALSSIZE 1
CALL ADD_BANQUET_DRESS_UP(5)

@ADD_BANQUET_FINISH5
#LOCALSIZE 1
#LOCALSSIZE 1
CALL ADD_BANQUET_DRESS_DOWN(5)

@ADD_BANQUET_VISIT5(PREVIOUS_STATE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC PREVIOUS_STATE
#DIM DYNAMIC BANQUET_INDEX
#DIM DYNAMIC CURRENT_STATE
#DIM DYNAMIC PARTICIPANTS
BANQUET_INDEX = ADD_BANQUET_INDEX(5)
PARTICIPANTS = ADD_BANQUET_PARTICIPANT_COUNT(BANQUET_INDEX)
CURRENT_STATE = ADD_BANQUETS_STATUS:BANQUET_INDEX

IF CURRENT_STATE == ADD_BANQUET_STATE_IN_PROGRESS && PREVIOUS_STATE == ADD_BANQUET_STATE_PREPARING ;player was present during start
    PRINTFORML 
    IF PARTICIPANTS >= 10
        PRINTW 白莲开始了读经，众多听众热切地聆听着她的歌声。
    ELSE
        PRINTW 白莲开始了读经，少数听众认真地聆听着她的歌声。
    ENDIF
    PRINTFORML 
ELSEIF CURRENT_STATE == ADD_BANQUET_STATE_IN_PROGRESS ;joins later
    PRINTL  
    PRINTW 白莲的读经已经开始了…
    PRINTL  
ENDIF

@ADD_BANQUET_PROGRESS5(BANQUET_INDEX)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC BANQUET_INDEX
#DIM DYNAMIC BANQUET_ID
#DIM DYNAMIC PRESENT
#DIM DYNAMIC LAPSE
BANQUET_ID = ADD_BANQUETS_UPCOMING:BANQUET_INDEX
;if master at location
PRESENT = CFLAG:MASTER:現在位置 == GET_MAP_REPLACEMENT(ADD_BANQUETS_LOCATION:BANQUET_INDEX)
LAPSE = TIME_PROGRESS(TIME:1)

SELECTCASE ADD_BANQUETS_STATUS:BANQUET_INDEX
    CASE ADD_BANQUET_STATE_PREPARING
        IF PRESENT
            FOR LOCAL, 1, CHARANUM
                IF CFLAG:LOCAL:BANQUET == BANQUET_ID && CFLAG:LOCAL:BANQUET_ROLE > 1
                    IF TALENT:LOCAL:恋慕
                        PRINTFORML %CALLNAME:LOCAL%一边牵挂着%CALLNAME:MASTER%一边准备着读经Live。
                    ELSE
                        PRINTFORML %CALLNAME:LOCAL%正忙碌地准备着读经Live。
                    ENDIF
                ENDIF
            NEXT
        ENDIF
    CASE ADD_BANQUET_STATE_IN_PROGRESS
        SIF PRESENT
            PRINTFORMDL 白莲保持着完美的节奏，继续诵读着经文。
        FOR LOCAL, 1, CHARANUM
            IF CFLAG:LOCAL:BANQUET == BANQUET_ID
                IF CFLAG:LOCAL:行動 == 5
                    BASE:LOCAL:仕事量 = MAX(BASE:LOCAL:仕事量 - LAPSE * 10, 0)
                    SIF CFLAG:LOCAL:BANQUET_ROLE < ADD_BANQUET_ROLE_HOST && (BASE:LOCAL:仕事量 == 0 || CFLAG:LOCAL:睡眠)
                        CALL ADD_BANQUET_END_PARTICIPATION(LOCAL)
                    IF PRESENT && CFLAG:LOCAL:職種 == 49
                        SIF LOCAL == [[白蓮]]
                            CONTINUE

                        IF TALENT:LOCAL:妖怪
                            IF TALENT:LOCAL:恋慕 && !RAND:5
                                PRINTFORMDL %CALLNAME:LOCAL%一边颤抖着一边紧紧依偎着你。
                            ELSE
                                PRINTFORMDL %CALLNAME:LOCAL%听着读经，浑身颤抖。
                            ENDIF
                        ELSE
                            IF CFLAG:LOCAL:職種 == 49 && TALENT:LOCAL:恋慕 && !RAND:5
                                PRINTFORML %CALLNAME:LOCAL%靠在你身上打着瞌睡。
                            ELSE
                                PRINTFORML %CALLNAME:LOCAL%看起来快要睡着了。
                            ENDIF
                        ENDIF
                    ENDIF
                ENDIF
            ENDIF
        NEXT
    CASE ADD_BANQUET_STATE_FINISHED
ENDSELECT

@ADD_BANQUET_PREPARATION_TEXT5(CHARA, LOCATION)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC CHARA
#DIM DYNAMIC LOCATION
IF CFLAG:CHARA:現在位置 == CFLAG:MASTER:現在位置 && CFLAG:MASTER:現在位置 == LOCATION
    PRINTFORML %CALLNAME:CHARA%开始准备读经Live。
ELSEIF CFLAG:MASTER:現在位置 == LOCATION
    PRINTFORML %CALLNAME:CHARA%来到了%NAME_FROM_PLACE(LOCATION)%开始准备读经Live。
ELSEIF CFLAG:CHARA:現在位置 == CFLAG:MASTER:現在位置
    PRINTFORML %CALLNAME:CHARA%前往%NAME_FROM_PLACE(LOCATION)%开始准备读经Live。
ENDIF

@ADD_BANQUET_JOIN_TEXT5(CHARA, LOCATION)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC CHARA
#DIM DYNAMIC LOCATION
IF CFLAG:CHARA:現在位置 == CFLAG:MASTER:現在位置 && CFLAG:MASTER:現在位置 == LOCATION
    PRINTFORML %CALLNAME:CHARA%坐下来开始聆听读经Live。
ELSEIF CFLAG:MASTER:現在位置 == LOCATION
    PRINTFORML %CALLNAME:CHARA%来到了%NAME_FROM_PLACE(LOCATION)%聆听白莲的读经Live。
ELSEIF CFLAG:CHARA:現在位置 == CFLAG:MASTER:現在位置
    PRINTFORML %CALLNAME:CHARA%前往%NAME_FROM_PLACE(LOCATION)%参加读经Live。
ENDIF

@ADD_BANQUET_SET_PARTICIPANTS5
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC BANQUET_INDEX
#DIM DYNAMIC BANQUET_SCALE
BANQUET_INDEX = ADD_BANQUET_INDEX(5)
BANQUET_SCALE = ADD_BANQUETS_SCALE:BANQUET_INDEX

CALL ADD_BANQUET_FILL_PARTICIPANTS(BANQUET_INDEX, 0, 8)

@ADD_BANQUET_WORKLOAD_PREPARATION5(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC SCALE
#DIM DYNAMIC BANQUET_INDEX
#DIM DYNAMIC WORKLOAD
BANQUET_INDEX = ADD_BANQUET_INDEX(5)
SCALE = ADD_BANQUETS_SCALE:BANQUET_INDEX
WORKLOAD = ADD_BANQUET_TIME_UNTIL(BANQUET_INDEX) * 10

IF CFLAG:ARG:職種 != JOB_宴会準備
	MAXBASE:ARG:仕事量 = MIN(WORKLOAD, 2400)
	BASE:ARG:仕事量 = MAXBASE:ARG:仕事量
	CFLAG:ARG:職種 = JOB_宴会準備
    TCVAR:ARG:手伝えない = 2
	CFLAG:ARG:行動 = 5
ENDIF

@ADD_BANQUET_WORKLOAD_PARTICIPATION5(ARG, SCALE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC SCALE

IF CFLAG:ARG:職種 != JOB_宴会参加
    IF CFLAG:ARG:BANQUET_ROLE > 1
        MAXBASE:ARG:仕事量 = 3600 ;hosts remain the full duration
    ELSE
    	MAXBASE:ARG:仕事量 = LIMIT(RAND:4000, 600, 3600) ;stay as long as they can stand it
    ENDIF

	BASE:ARG:仕事量 = MAXBASE:ARG:仕事量
	CFLAG:ARG:職種 = JOB_宴会参加
	CFLAG:ARG:行動 = 5
    TCVAR:ARG:手伝えない = 1
ENDIF

@ADD_BANQUET_MESSAGE_TALK5
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC BANQUET_INDEX
BANQUET_INDEX = ADD_BANQUET_INDEX(5)

PRINTFORMDL 白莲一边敲着木鱼一边唱着单调的经文。

@ADD_BANQUET_LISTEN_TEXT5
#LOCALSIZE 1
#LOCALSSIZE 1
PRINTFORMDW 你聆听着白莲以单调而富有节奏的方式诵读经文。

@ADD_BANQUET_LISTEN_TOGETHER_TEXT5
#LOCALSIZE 1
#LOCALSSIZE 1
PRINTFORML 你和%CALLNAME:TARGET%一起聆听白莲的读经Live。
IF TALENT:妖怪
    PRINTFORMDW 你看到%CALLNAME:TARGET%专注地听着经文，额头上渗出了汗珠。
ELSE
    PRINTFORMDW %CALLNAME:TARGET%似乎被读经催眠得快要睡着了。
ENDIF
