@SCOM12_CHECK(ARG)
#FUNCTION
RETURNF (ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_B, 0) && ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_M, 1)) || (ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_B, 1) && ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_M, 0))

@SCOM1_CHECK(ARG)
#FUNCTION
{
RETURNF (ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_M) || ADD_CHARA_POSE(PLAYER, ARG, SET_C, SET_M)) 
    && (ADD_CHARA_POSE(PLAYER, ARG, SET_M, SET_P) || ADD_CHARA_POSE(PLAYER, ARG, SET_M, SET_C))
    && (SELECTCOM == 1 || SELECTCOM == 81) && !FLAG:70
}

@SCOM5_CHECK(ARG)
#FUNCTION
{
RETURNF (ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_YUBI) && ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_M, 1)) 
    || (ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_YUBI, 1) && ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_M))
}

@SCOM8_CHECK(ARG)
#FUNCTION
{
RETURNF ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_B)
    && (ADD_CHARA_POSE(PLAYER, ARG, SET_M, SET_P) || ADD_CHARA_POSE(PLAYER, ARG, SET_M, SET_C))
    && (SELECTCOM == 1 || SELECTCOM == 82)
}

@SCOM9_CHECK(ARG)
#FUNCTION
{
RETURNF ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_M)
    && (ADD_MASTER_POSE_TR(PLAYER, SET_M,SET_P,1) && ADD_MASTER_POSE_TR(PLAYER, SET_M,SET_P,1) != ARG )
}

@SCOM10_CHECK(ARG)
#FUNCTION
{
RETURNF ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_C)
    && (ADD_MASTER_POSE_TR(PLAYER, SET_C,SET_P,1) && ADD_MASTER_POSE_TR(PLAYER, SET_C,SET_P,1) != ARG )
}

@SCOM11_CHECK(ARG)
#FUNCTION
{
RETURNF ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_B)
    && (ADD_MASTER_POSE_TR(PLAYER, SET_B,SET_P,1) && ADD_MASTER_POSE_TR(PLAYER, SET_B,SET_P,1) != ARG )
}

@SCOM19_CHECK(ARG)
#FUNCTION
{
RETURNF (SELECTCOM == 80 && TFLAG:103 == ARG && TEQUIP:ARG:乳首吸い) 
    || (ADD_CHARA_POSE(PLAYER, ARG, SET_P, SET_YUBI) && TFLAG:103 == ARG  && SELECTCOM == 11 && PREVCOM != 11)
}
