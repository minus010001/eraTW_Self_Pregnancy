;打印玩家可前往的地点，可以是一个，也可以是多个
;qol 现在ARG ARG:1将不再是最后两位，而是直接使用地点ID
;如果未指定ARG:1，则仅打印一个
;如果指定了ARG:1，它将打印从ARG到ARG:1的所有内容
@PRINT_DESTINATION(ARG, ARG:1 = -1)
SIF ARG:1 < 0
	ARG:1 = ARG

FOR LOCAL, ARG, ARG:1+1
	;SIF CFLAG:MASTER:現在位置 == 100*MAIN_MAP+LOCAL ;highlight current location
	SIF CFLAG:MASTER:現在位置 == LOCAL
		SETCOLOR C_AQUA
	IF STRLENS(NAME_FROM_PLACE(LOCAL)) > 12
		PRINTBUTTON @"[{LOCAL % 100, 2}] - %NAME_FROM_PLACE(LOCAL),32,LEFT% ", LOCAL
	ELSEIF STRLENS(NAME_FROM_PLACE(LOCAL)) > 0
		PRINTBUTTON @"[{LOCAL % 100, 2}] - %NAME_FROM_PLACE(LOCAL),12,LEFT% ", LOCAL
	ENDIF
	RESETCOLOR
NEXT
