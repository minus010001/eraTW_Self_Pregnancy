@ADD_CUSTOM_COM2
#LOCALSIZE 1
#LOCALSSIZE 1
IF !ADD_SWIMMING_CONSTITUTION(TARGET,1)
	RETURN 0
ELSEIF MARK:反発刻印 || CFLAG:ブチギレ
	TFLAG:194 = 2
	RETURN 0
ELSEIF ADD_SWIMMING_DAMAGE(TARGET) && ABL:(TARGET):従順 < 5
	TFLAG:194 = 6
	RETURN 0
ELSEIF CFLAG:(TARGET):態度 < 1
	TFLAG:194 = 6
	RETURN 0
ELSEIF CFLAG:(TARGET):同行 || CFLAG:(TARGET):態度 > 0
	CALL ADD_START_SWIMMING(TARGET)
	TFLAG:194 = 0
	;不再使用游泳趴
	;CALL ADD_BYSTANDERS_JOIN_SWIMMING
	TIME += 5
	RETURN 1
ENDIF

@ADD_CUSTOM_COM_ABLE2
#LOCALSIZE 1
#LOCALSSIZE 1
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF TFLAG:102 != 1
	RETURN 0
SIF CFLAG:うふふ
	RETURN 0
SIF !TEQUIP:MASTER:SWIMMING
	RETURN 0
SIF TEQUIP:SWIMMING
	RETURN 0
;SIF MARK:反発刻印 || CFLAG:ブチギレ
;	RETURN 0
SIF !ADD_SWIMMING_AREA(CFLAG:MASTER:現在位置)
	RETURN 0
;NAS: Non 0 Master
SIF TARGET == MASTER
	RETURN 0
;SIF BASE:気力 <= 0 ;is tired
;	RETURN 0
SIF CFLAG:行動
	RETURN 0
;SIF !ADD_SWIMMING_CONSTITUTION(TARGET)
;	RETURN 0
;停止中は不可
SIF FLAG:70
	RETURN 0
SIF TFLAG:デート道中
	RETURN 0
RETURN 1

@ADD_CUSTOM_MESSAGE_COM2
#LOCALSIZE 1
#LOCALSSIZE 1
SIF TFLAG:194
	PRINTFORM +++本想邀请%CALLNAME:TARGET%来游泳，
SELECTCASE TFLAG:194
	CASE 0
		;PRINTFORML %CALLNAME:TARGET%接受了邀请
	CASE 1
		PRINTFORML %CALLNAME:TARGET%表示自己很疲惫，只想好好休息会儿
	CASE 2
		PRINTFORML %CALLNAME:TARGET%不悦地拒绝了
	CASE 3
		PRINTFORML %CALLNAME:TARGET%谢绝了，表示流水会伤害到她
	CASE 4
		PRINTFORML 但%CALLNAME:TARGET%的体格还承受不了这么冷的水温
	CASE 5
		PRINTFORML %CALLNAME:TARGET%想到自己可能湿漉漉的样子断然拒绝了
	CASE 6
		SELECTCASE CFLAG:TARGET:性格傾向
		;气弱系
		CASE 1
			PRINTFORML %CALLNAME:TARGET%连忙摆手拒绝了…
		;强气系
		CASE 2
			PRINTFORML %CALLNAME:TARGET%摆摆手回绝了邀请…
		;坦率系
		CASE 3
			PRINTFORML %CALLNAME:TARGET%表示并不想游泳…
		;真面目系
		CASE 4
			PRINTFORML %CALLNAME:TARGET%委婉地回绝了…
		;其他
		CASEELSE
			PRINTFORML %CALLNAME:TARGET%没有接受邀请…
		ENDSELECT
ENDSELECT
