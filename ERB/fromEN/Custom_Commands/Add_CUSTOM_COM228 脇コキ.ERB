;-------------------------------------------------
;armpitjob/sex, based off TW's footjob with text from K's armpitjob command mixed in
;脇扱き
;奉仕系コマンド、レベル4
;-------------------------------------------------
@ADD_CUSTOM_COM228
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;行為者
TCVAR:116 = TARGET
;奉仕快楽フラグ
TCVAR:108 = 1

; [EJACU_MIGRATION] Removed: CSTR:PLAYER:10 += @"{TARGET}/"
;Armpit
;強度
TCVAR:PLAYER:射精快感強度 = 500
CALL DECLARE_EJACULATION(PLAYER, 0, 射精箇所_脇コキ, TARGET, CHARA_NONE, TARGET, CHARA_NONE, 500)

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力
IF (FLAG:70 == 1 && CFLAG:TARGET:344 == 0)
	BASE:MASTER:TSP -= 70
	IF TALENT:197
		SOURCE:PLAYER:快Ｃ = 300
	ELSE
		SOURCE:PLAYER:快Ｃ = 150
	ENDIF

ELSE

	DOWNBASE:体力 += 10
	DOWNBASE:気力 += 100
	IF TALENT:197
		SOURCE:PLAYER:快Ｃ = 300
	ELSE
		SOURCE:PLAYER:快Ｃ = 150
	ENDIF

	SOURCE:加虐 = 1000
	SOURCE:征服 = 700
	SOURCE:性行動 = 800
	SOURCE:達成 = 700
	SOURCE:屈従 = 700
	SOURCE:逸脱 = 500
	SOURCE:反感 = 500

	;上のほうで計算した汚れデータ
	SOURCE:不潔 = Y*10 + 80

	;-------------------------------------------------
	;特殊ソース処理
	;-------------------------------------------------
	;抑圧、抵抗持ちには、抑鬱ソース追加
	SIF TALENT:自己愛 < 0 || TALENT:抵抗
		SOURCE:鬱屈 += 800

	;Need to do this here because the custom command framework doesn't handle it
	TIME += 5
ENDIF
;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;同性愛　レズなら戻り値1ゲイなら戻り値2
IF FLAG:70 == 1 || CFLAG:イタズラ
	IF HETEROSEX(TARGET,PLAYER)
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 4
	ENDIF
ELSE
	IF HETEROSEX(TARGET,PLAYER)
		EXP:(50 + HETEROSEX(TARGET,PLAYER)) += 4
		EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 4
	ENDIF

	;サドは嗜虐快楽
	SIF TALENT:サド || ABL:サドっ気
		EXP:嗜虐快楽経験 ++
ENDIF
PRINTFORMDL 
RETURN 1

@ADD_CUSTOM_CAN_COM228(ARG)
SKIPDISP ARG
PRINTL 腋交
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------
IF FLAG:70 == 1 || CFLAG:イタズラ

ELSE

	A = 0
	S = 0

;すべての命令に共通の要素を考慮
;(従順が高いと命令に従いやすいなど)
	CALL COM_ORDER

;ABL:欲望
	IF ABL:欲望
		SIF S
			PRINT  + 
		A += ABL:欲望 * 1
		PRINT 欲望
		PRINTV 'LV,ABL:欲望,'(,ABL:欲望 * 1,')
		S = 1
	ENDIF
;ABL:奉仕精神
	IF ABL:奉仕精神
		SIF S
			PRINT  + 
		A += ABL:奉仕精神 * 4
		PRINT 侍奉精神
		PRINTV 'LV,ABL:奉仕精神,'(,ABL:奉仕精神 * 4,')
		S = 1
	ENDIF
;ABL:精液中毒
	IF ABL:精液中毒
		SIF S
			PRINT  + 
		A += ABL:精液中毒 * 1
		PRINT 精液中毒
		PRINTV 'LV,ABL:精液中毒,'(,ABL:精液中毒 * 1,')
		S = 1
	ENDIF

	IF MARK:快楽刻印
		SIF S
			PRINT  + 
		A += MARK:快楽刻印 * 1
		PRINT 快乐刻印
		PRINTV 'LV,MARK:快楽刻印,'(,MARK:快楽刻印 * 1,')
		S = 1
	ENDIF
;PALAM:欲情
	IF GETPALAMLV(PALAM:欲情,5)
		SIF S
			PRINT  + 
		A += GETPALAMLV(PALAM:欲情,5) * 1
		PRINT 情欲
		PRINTV 'LV,GETPALAMLV(PALAM:欲情,5),'(,GETPALAMLV(PALAM:欲情,5) * 1,')
		S = 1
	ENDIF
;恥じらい
	IF TALENT:羞恥心 > 0
		PRINT  - 
		A -= 5
		PRINT 害羞
		PRINTV '(,5,')
		S = 1
	ENDIF
;汚臭鈍感
	IF TALENT:汚臭耐性 > 0
		SIF S
			PRINT  + 
		A += 1
		PRINT 污臭钝感
		PRINTV '(,1,')
		S = 1
	ENDIF
;汚臭敏感
	IF TALENT:汚臭耐性 < 0
		PRINT  - 
		A -= 5
		PRINT 污臭敏感
		PRINTV '(,5,')
		S = 1
	ENDIF
;献身的
	IF TALENT:献身的
		SIF S
			PRINT  + 
		A += 6
		PRINT 献身的
		PRINTV '(,6,')
		S = 1
	ENDIF
;快感の否定（淫乱、娼婦で無効）
	IF TALENT:快感応答 < 0 && !TALENT:淫乱
		PRINT  - 
		A -= 3
		PRINT 否定快感
		PRINTV '(,3,')
		S = 1
	ENDIF
;男嫌い（恋慕、親愛で無効）
	IF OTOKOGIRAI(TARGET)
		PRINT  - 
		A -= 10
		PRINT 讨厌男人
		PRINTV '(,10,')
		S = 8
	ENDIF
;恋慕
	IF TALENT:恋慕 && ASSIPLAY == 0
		SIF S
			PRINT  + 
		A += 5
		PRINT 恋慕
		PRINTV '(,5,')
		S = 5
	ENDIF
;親愛
	IF TALENT:恋慕 > 1 && ASSIPLAY == 0
		SIF S
			PRINT  + 
		A += 5
		PRINT 爱恋
		PRINTV '(,5,')
		S = 5
	ENDIF

;倒錯的
	IF TALENT:倒錯的
		SIF S
			PRINT  + 
		A += 6
		PRINT 倒错的
		PRINTV '(,6,')
		S = 6
	ENDIF
;マゾ
	IF TALENT:マゾ
		SIF S
			PRINT  + 
		A += 4
		PRINT M
		PRINTV '(,4,')
		S = 4
	ENDIF
;小悪魔
	IF TALENT:小悪魔
		SIF S
			PRINT  + 
		A += 4
		PRINT 小恶魔
		PRINTV '(,4,')
		S = 4
	ENDIF
;調教者がふたなり
	IF (TALENT:PLAYER:2 & 3) == 3
		SIF S
			PRINT  + 
		A += 8
		PRINT 扶她
		PRINTV '(,8,')
		S = 1
	ENDIF

;巫女
;I apologize for nothing
	IF TALENT:197 == 1
		SIF S
			PRINT  + 
		A += 4
		PRINT 巫女
		PRINTV '(,4,')
		S = 4
	ENDIF

	Y = 0
;愛液の汚れ
	SIF STAIN:PLAYER:2 & 1
		Y += 1
;精液の汚れ
	SIF STAIN:PLAYER:2 & 4
		Y += 3
;アナルの汚れ
	SIF STAIN:PLAYER:2 & 8
		Y += 7
	SIF STAIN:PLAYER:2 & 16
		Y += 15
	Y = Y * (2 - TALENT:汚臭耐性) / 2

;汚れあり
	IF Y
		PRINT  - 
		A -= Y
		PRINT 有污垢
	;汚臭鈍感
		IF TALENT:汚臭耐性 > 0
			PRINT 、污臭钝感
		ELSEIF TALENT:汚臭耐性 < 0
			PRINT 、污臭敏感
		ENDIF
		PRINTV '(,Y,')
		S = 1
	ENDIF


;合計を表示(22以上で実行)
	PRINT  = 
	PRINTV A

	V = 22
	SIF A < V
		PRINT  < 
	SIF A == V
		PRINT  = 
	SIF A > V
		PRINT  > 
	PRINT 实行值 
	PRINTV V

	WAIT
	SKIPDISP 0
;実行できない
	SIF A < V
		RETURN 0
ENDIF
RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@ADD_CUSTOM_COM_ABLE228
;脇扱き実行判定
SIF !TFLAG:100
	RETURN 0
SIF TARGET == MASTER
	RETURN 0
SIF CFLAG:イタズラ == 1 && CFLAG:MASTER:イタズラ == 1 && CFLAG:睡眠 ;sleep rape
	RETURN 1
SIF CFLAG:うふふ != 1
	RETURN 0
;一括管理
;調教者がオトコか半人半妖かふたなりじゃないとダメ
SIF !(HAS_PENIS(MASTER))
	RETURN 0
RETURN 1

@ADD_CUSTOM_MESSAGE_COM228
IF (FLAG:70 == 1 && CFLAG:TARGET:344 == 0)
	PRINTFORML 在静止的时间里，+++用%TINKO(PLAYER)%摩擦着%CALLNAME:TARGET%的腋下……
ELSEIF CFLAG:MASTER:イタズラ
	PRINTFORML +++用%TINKO(PLAYER)%摩擦着熟睡的%CALLNAME:TARGET%的腋下……
ELSE
	;Based off K's footjob text
	IF TALENT:サド
		PRINTFORML %CALLNAME:TARGET%带着虐待狂的笑容，将+++的%TINKO(PLAYER)%夹在胳膊和身体之间，猛烈地摩擦着……
	ELSE
		PRINTFORML %CALLNAME:TARGET%用腋下\@TALENT:恋慕?充满爱意地#\@摩擦着+++的%TINKO(PLAYER)%……
	ENDIF

ENDIF