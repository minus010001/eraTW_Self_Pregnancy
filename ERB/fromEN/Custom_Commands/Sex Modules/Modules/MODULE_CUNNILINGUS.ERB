;cunnilingus
@ADD_SEX_ACTION_4_2(ARG, MULTIPLIER)
#DIM MULTIPLIER
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;行為者
TCVAR:ARG:116 = PLAYER
;奉仕快楽
TCVAR:PLAYER:108 = 1

; [EJACU_MIGRATION] Removed: CSTR:ARG:10 += @"{PLAYER}/"
;強度
TCVAR:ARG:射精快感強度 = MULTIPLY(500, MULTIPLIER)
CALL DECLARE_EJACULATION(ARG, 0, 射精箇所_口, PLAYER, CHARA_NONE, PLAYER, CHARA_NONE, MULTIPLY(500, MULTIPLIER))

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力
IF FLAG:70 == 1
	BASE:MASTER:TSP -= 20
	SOURCE:ARG:快Ｃ = MULTIPLY(80, MULTIPLIER)
	SOURCE:PLAYER:快Ｍ += MULTIPLY(50, MULTIPLIER)
	SOURCE:ARG:液体 = MULTIPLY(1000, MULTIPLIER)
	;性技補正
	IF ABL:PLAYER:舌 >= 4
		SOURCE:ARG:快Ｃ += MULTIPLY(100, MULTIPLIER)
		SOURCE:ARG:液体 += MULTIPLY(400, MULTIPLIER)
	ELSE
		SOURCE:ARG:快Ｃ += MULTIPLY(ABL:PLAYER:舌 * 20, MULTIPLIER)
		SOURCE:ARG:液体 += MULTIPLY(ABL:PLAYER:舌 * 80, MULTIPLIER)
	ENDIF

	;口淫経験
	EXP:PLAYER:口淫経験 ++
;時間停止なし
ELSE
	DOWNBASE:ARG:体力 += 5
	DOWNBASE:ARG:気力 += 50
	SOURCE:PLAYER:快Ｍ += MULTIPLY(50, MULTIPLIER)
	SOURCE:ARG:快Ｃ = MULTIPLY(80, MULTIPLIER)
	SOURCE:ARG:液体 = MULTIPLY(1000, MULTIPLIER)
	SOURCE:ARG:露出 = MULTIPLY(10, MULTIPLIER)
	SOURCE:ARG:逸脱 = 15
	SOURCE:ARG:反感 = 15

	;-------------------------------------------------
	;特殊ソース処理
	;-------------------------------------------------
	;抑圧、抵抗持ちには、抑鬱ソース追加
	SIF TALENT:ARG:自己愛 < 0 || TALENT:ARG:抵抗
		SOURCE:ARG:鬱屈 += 20

	;性技補正
	IF ABL:PLAYER:舌 >= 4
		SOURCE:ARG:快Ｃ += MULTIPLY(100, MULTIPLIER)
		SOURCE:ARG:液体 += MULTIPLY(400, MULTIPLIER)
	ELSE
		SOURCE:ARG:快Ｃ += MULTIPLY(ABL:PLAYER:舌 * 20, MULTIPLIER)
		SOURCE:ARG:液体 += MULTIPLY(ABL:PLAYER:舌 * 80, MULTIPLIER)
	ENDIF

	;口淫経験
	EXP:PLAYER:口淫経験 ++
ENDIF
Qol_Info_Count_Array:ARG:Qol_Count_MSexCunni += EXP_UP(GETNUM(EXP, "口淫経験"), PLAYER) ;qol custom code
;-------------------------------------------------
;時間停止分岐終わり、共通する素質補正や汚れの処理
;-------------------------------------------------
;舌を使う調教では、調教者の舌使いによって、快感のソース増加
IF TALENT:PLAYER:舌使い
	TIMES SOURCE:ARG:快Ｃ , 1.50
	TIMES SOURCE:ARG:液体 , 1.50
ENDIF
;猫舌では減少、調教者に嗜虐快楽経験を追加
IF TALENT:PLAYER:猫舌
	TIMES SOURCE:ARG:快Ｃ , 0.75
	EXP:PLAYER:嗜虐快楽経験 += 1
ENDIF

;奴隷のＶ⇔調教者の口の汚れが移動
STAIN:ARG:Ｖ |= STAIN:PLAYER:口
STAIN:PLAYER:口 |= STAIN:ARG:Ｖ

RETURN 1

@ADD_SEX_ABLE_4_2(ARG)
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(1)
	RETURN RESULT
;性器が汚れているとPLAYERの場合クンニはできない（調教者が汚れ無視か愛液、母乳、チョコのよごれならＯＫ）
SIF STAIN:ARG:3 >= 2 && (STAIN:ARG:3 != 16 && STAIN:ARG:3 != 17 && STAIN:ARG:3 != 128 && STAIN:ARG:3 != 129 && STAIN:ARG:3 != 144 && STAIN:ARG:3 != 145) && ASSIPLAY == 0 && TALENT:PLAYER:汚臭耐性 < 2
	RETURN 0
;性器が汚れており、汚臭敏感で従順3以下のASSIの場合クンニはできない（調教者が汚れ無視か愛液、母乳、チョコのよごれならＯＫ）
IF STAIN:ARG:3 >= 2 && (STAIN:ARG:3 != 16 && STAIN:ARG:3 != 17 && STAIN:ARG:3 != 128 && STAIN:ARG:3 != 129 && STAIN:ARG:3 != 144 && STAIN:ARG:3 != 145) && ASSIPLAY
	SIF ABL:ASSI:従順 <= 3 && TALENT:ASSI:汚臭耐性 < 0
		RETURN 0
ENDIF
;下半身着衣
SIF TEQUIP:ARG:0 & 4
	RETURN 0
RETURN 1

@ADD_SEX_CAN_4_2()
;no checks
