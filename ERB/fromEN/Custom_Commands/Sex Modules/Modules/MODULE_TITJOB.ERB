;titjob
@ADD_SEX_ACTION_1_5(ARG, MULTIPLIER)
#DIM MULTIPLIER
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;行為者
TCVAR:ARG:116 = ARG
;奉仕快楽
TCVAR:ARG:108 = 1

; [EJACU_MIGRATION] Removed: CSTR:PLAYER:10 += @"{ARG}/"
CALL DECLARE_EJACULATION(PLAYER, 0, 射精箇所_胸, ARG, CHARA_NONE, ARG, CHARA_NONE, MULTIPLY(1200, MULTIPLIER))

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力
IF FLAG:70 == 1
	BASE:MASTER:TSP -= 60
	SOURCE:ARG:快Ｂ = MULTIPLY(150, MULTIPLIER)
	SOURCE:PLAYER:快Ｃ += MULTIPLY(110, MULTIPLIER)
ELSE

	DOWNBASE:体力 += 10
	DOWNBASE:気力 += 100
	SOURCE:PLAYER:快Ｃ += MULTIPLY(110, MULTIPLIER)
	;性技補正
	SOURCE:PLAYER:快Ｃ += MULTIPLY(MIN(ABL:胸 * 25,100), MULTIPLIER)

	SOURCE:ARG:快Ｂ = MULTIPLY(50, MULTIPLIER)
	SOURCE:ARG:情愛 = MULTIPLY(150, MULTIPLIER)
	SOURCE:ARG:性行動 = MULTIPLY(400, MULTIPLIER)
	SOURCE:ARG:達成 = MULTIPLY(200, MULTIPLIER)
	SOURCE:ARG:屈従 = MULTIPLY(1000, MULTIPLIER)
	SOURCE:ARG:逸脱 = 500
	SOURCE:ARG:反感 = 500

	;上のほうで計算した汚れデータ
	SOURCE:ARG:不潔 = Y*10 + 60

	;-------------------------------------------------
	;特殊ソース処理
	;-------------------------------------------------
	;抑圧、抵抗持ちには、抑鬱ソース追加
	SIF TALENT:自己愛 < 0 || TALENT:抵抗
		SOURCE:ARG:鬱屈 += 600
ENDIF
;-------------------------------------------------
;汚れの処理
;-------------------------------------------------
;奴隷のＢ⇔調教者のＰの汚れが移動
STAIN:ARG:5 |= STAIN:PLAYER:2
STAIN:PLAYER:2 |= STAIN:ARG:5
;-------------------------------------------------
;経験の処理
;-------------------------------------------------

;同性愛　レズなら戻り値1ゲイなら戻り値2
IF FLAG:70 == 1 || CFLAG:ARG:イタズラ
ELSE
	EXP:ARG:パイズリ経験 += 2
	;愛情経験
	SIF TALENT:ARG:恋慕 == 1 && ASSIPLAY == 0
		EXP:ARG:愛情経験 ++
ENDIF
RETURN 1

@ADD_SEX_ABLE_1_5(ARG)
;パイズリ実行判定
SIF !TFLAG:100
	RETURN 0
SIF GLOBAL_COMABLE(82)
	RETURN RESULT
;オトコだとダメ
SIF !(TALENT:ARG:2 & 1)
	RETURN 0
;時間停止中は許可
IF FLAG:70 != 1
	;[絶壁]だと不可 だと思ったら大間違いだ！ 愛があればできる！
	;SIF TALENT:バストサイズ == -2
	;	RETURN 0
	;[爆乳]なら技巧LV1以上、[巨乳]でなければ技巧3以上、[貧乳]だと技巧4以上、[絶壁]だと技巧5以上必要
	SIF TALENT:ARG:バストサイズ + ABL:ARG:技巧 < 3
		RETURN 0
	;縄使用中は不可
	SIF TEQUIP:ARG:縄
		RETURN 0
ENDIF
;調教者がオトコか半人半妖かふたなりじゃないとダメ
SIF !(TALENT:PLAYER:2 & 2)
	RETURN 0
SIF TEQUIP:ARG:継続キス
	RETURN 0
;上半身着衣
SIF TEQUIP:ARG:1 > 1 && TEQUIP:ARG:3 == 0
	RETURN 0
RETURN 1

@ADD_SEX_CAN_1_5()
ADD_SEX_MODULE_DIFFICULTY_TEMP += 30
ADD_SEX_MODULE_COM_ORDER_TEMP |= QOL_COM_ORDER_SERVICE_CUM_FULL | QOL_COM_ORDER_DIRT_SOURCE_PENIS
