;fingering
@ADD_SEX_ACTION_3_6(ARG, MULTIPLIER)
#DIM MULTIPLIER
;行為者
TCVAR:ARG:116 = PLAYER
;奉仕快楽
TCVAR:PLAYER:108 = 1
;破瓜抑制
TFLAG:2 = 1

;破瓜抑制
TFLAG:2 = 1
IF FLAG:70
	IF TALENT:ARG:処女 || TALENT:ARG:処女 == -1
		SOURCE:ARG:快Ｖ = MULTIPLY(120, MULTIPLIER)
		SOURCE:ARG:苦痛 = 10
	ELSE
		SOURCE:ARG:快Ｖ = MULTIPLY(60, MULTIPLIER)
		SOURCE:ARG:苦痛 = 5
	ENDIF
ELSE
	DOWNBASE:ARG:体力 += 30
	DOWNBASE:ARG:気力 += 80

	IF TALENT:ARG:処女 || TALENT:ARG:処女 == -1
		SOURCE:ARG:快Ｖ = MULTIPLY(120, MULTIPLIER)
		SOURCE:ARG:苦痛 = 15
		SOURCE:ARG:露出 = MULTIPLY(10, MULTIPLIER)
		SOURCE:ARG:逸脱 = 20
		SOURCE:ARG:反感 = 20
	;V経験が0の時はV経験を伴わない物に派生
	;Vの珠は入らないが、快Vが高いと慣らしてあるため処女喪失時に影響
	ELSE
		SOURCE:ARG:快Ｖ = MULTIPLY(60, MULTIPLIER)
		SOURCE:ARG:苦痛 = 30
		SOURCE:ARG:露出 = MULTIPLY(30, MULTIPLIER)
		SOURCE:ARG:逸脱 = 30
		SOURCE:ARG:反感 = 30
	ENDIF
ENDIF

SIF TALENT:PLAYER:器用な指
	SOURCE:ARG:快Ｖ += MULTIPLY(20, MULTIPLIER)
SOURCE:ARG:快Ｖ += MULTIPLY(ABL:PLAYER:指 * 5, MULTIPLIER)
;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;抑圧、抵抗持ちには、抑鬱ソース追加
IF TALENT:ARG:自己愛 < 0 || TALENT:ARG:抵抗
	SOURCE:ARG:鬱屈 += MULTIPLY(20, MULTIPLIER)
	;処女
	SIF TALENT:ARG:処女
		SOURCE:ARG:鬱屈 += MULTIPLY(10, MULTIPLIER)
ENDIF
;Vへの苦痛
CALL PAIN_CHECK_V(ARG)

STAIN:ARG:6 |= STAIN:PLAYER:1
STAIN:PLAYER:1 |= STAIN:ARG:6
EXP:ARG:Ｖ経験 ++

@ADD_SEX_ABLE_3_6(ARG)
;指挿入れ実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(3)
	RETURN RESULT
;毒人形だと性器がついてないのでダメ
SIF TALENT:ARG:196 == 99
	RETURN 0
;オトコだとダメ
SIF !(TALENT:ARG:2 & 1)
	RETURN 0
;時間停止中は許可
IF FLAG:70 != 1
	;潤滑不足で、従順3以下・レズっ気3以下のASSIの場合アナル愛撫はできない（サドならＯＫ）
	IF PALAM:ARG:潤滑 < PALAMLV:2 && ASSIPLAY
		SIF (ABL:ASSI:従順 <= 3 || ABL:ASSI:レズっ気 <= 3) && !TALENT:ASSI:サド
			RETURN 0
	ENDIF
ENDIF

;下半身着衣
SIF TEQUIP:ARG:0 & 4
	RETURN 0
RETURN 1

@ADD_SEX_CAN_3_6()
;nothing for now
