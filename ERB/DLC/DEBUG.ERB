@DEBUG_MODE
DEBUG_HISTORY = 1
DRAWLINE
PRINTL 请选择要修改人物，或进入FLAG/自定义修正
DRAWLINE

LOCAL:1 = 0
FOR LOCAL,0,CHARANUM
	PRINTFORM [{LOCAL,3,RIGHT}] %CALLNAME:LOCAL,12,LEFT%
	LOCAL:1 += 1
	SIF !(LOCAL:1 % 8)
		PRINTL
NEXT
SIF LOCAL:1 % 8 
	PRINTL
DRAWLINE
PRINTLC [500] - 全局参数
PRINTLC [501] - 道具修改
PRINTLC [600] - 杂项修改
PRINTLC [700] - 全员回复
PRINTLC [900] - 重置素质
SIF MAGIC_DLC
	PRINTLC [901] - 魔法参数
PRINTL [998] - 更改当前人物
;PRINTFORML [803] - 特殊模式：\@ TFLAG:301 > 0 ? [使用中] # [关闭中] \@
DRAWLINE
PRINTFORML [999] - 离开

WHILE 1
INPUT
IF RESULT == 999
	RETURN
ELSEIF RESULT == 900
	PRINTFORML 这是为升级存档时由于各种未知原因导致素质丢失准备的简易修复功能
	PRINTFORML 角色的素质会回到初始状态，任何按机制获得的素质会在结束一日后自动获得
	PRINTFORML 输入你要重置素质的角色编号即可
	WHILE 1
	LOCAL = LINECOUNT
	PRINTLC [900] 重置全部角色
	PRINTLC [999] 返回
		INPUT
		IF INRANGE(RESULT,1,CHARANUM)
			CALL 重置素质(RESULT)
			PRINTFORMW %CALLNAME:RESULT%的素质已重置
		ELSEIF RESULT == 900
			PRINTFORML 请确定
			CALL ASK_YN
			SIF RESULT
				CONTINUE
			FOR LOCAL:1, 1, CHARANUM
				CALL 重置素质(LOCAL:1)
			NEXT
			PRINTFORMW 所有角色的素质已重置
		ELSEIF RESULT == 999
			BREAK
		ENDIF
		CLEARLINE LINECOUNT - LOCAL
	WEND
ELSEIF RESULT == 700
	FOR LOCAL,0,CHARANUM
		BASE:LOCAL:0=MAXBASE:LOCAL:0
		BASE:LOCAL:1=MAXBASE:LOCAL:1
		SIF TALENT:LOCAL:2==1;性别
			CONTINUE
		BASE:LOCAL:6=MAXBASE:LOCAL:6
	NEXT
	BASE:0:8=MAXBASE:0:8
	PRINTFORML 全员体力、气力、精力已回复
	PRINTFORMW TSP已回复
	CLEARLINE 3
ELSEIF RESULT == 600
	CALL SHOW_OTHER_DEBUG
ELSEIF RESULT == 500
	CALL SHOW_FLAG_DEBUG
ELSEIF RESULT == 501
	CALL ITEM_DEBUG
;ELSEIF RESULT == 803
;	CALL ESTRUS_DEBUG
ELSEIF RESULT == 998
	CALL TARGET_EDIT
ELSEIF RESULT == 901 && MAGIC_DLC
	CALL MAGIC_DEBUG
ELSEIF RESULT == 902
	FOR LOCAL, 0, CHARANUM
		SIF TALENT:LOCAL:处女
			CALL SET_VIRGIN_LIGHT(LOCAL)
	NEXT
ELSEIF !INRANGE(RESULT,0,CHARANUM)|| NAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
;ELSEIF RESULT < 14
;	CALL SHOW_COUNTRY_DEBUG,RESULT
;	RESTART
ELSE
	CALL SHOW_SLAVE_DEBUG,RESULT
ENDIF
RESTART
WEND
@SHOW_SLAVE_DEBUG, ARG
DRAWLINE
PRINTL 请选择要修改的数据类型
DRAWLINE
PRINTL [ 1] - 基本属性　　　　　[ 2] - 扩展属性　　　　　[ 3] - 调教状态　　　　　[ 4] - 经验修改
PRINTL [ 5] - 能力修改　　　　　[ 6] - 素质修改　　　　　[ 7] - 刻印修改　　　　　[ 8] - TCVAR修改
PRINT [ 9] - CFLAG修改 　　　　[10] - 污渍修改　　　　　
IF MAGIC_DLC
	PRINTL [11] - 魔法CFLAG修改
ELSE
	PRINTL
ENDIF
DRAWLINE
PRINTFORML [99] 离开

WHILE 1

INPUT

IF RESULT == 99
	RETURN 0
ELSEIF RESULT == 1
	CALL BASE_DEBUG,ARG
ELSEIF RESULT == 2
	CALL EX_DEBUG,ARG
ELSEIF RESULT == 3
	CALL PALAM_DEBUG,ARG
ELSEIF RESULT == 4
	CALL EXP_DEBUG,ARG
ELSEIF RESULT == 5
	CALL ABL_DEBUG,ARG
ELSEIF RESULT == 6
	CALL TALENT_DEBUG,ARG
ELSEIF RESULT == 7
	CALL MARK_DEBUG,ARG
ELSEIF RESULT == 8
	CALL TCVAR_DEBUG,ARG
ELSEIF RESULT == 9
	CALL CFLAG_DEBUG,ARG
ELSEIF RESULT == 10
	CALL STAIN_DEBUG,ARG
ELSEIF RESULT == 11 && MAGIC_DLC
	CALL M_CFLAG_DEBUG,ARG
ELSE
	CLEARLINE 1
	CONTINUE
ENDIF
RESTART
WEND
@SHOW_OTHER_DEBUG
DRAWLINE
PRINTL 请选择要修改的数据类型
DRAWLINE
PRINTL [1] - 所持金钱　　　　　[2] - 所持筹码　　　　　[3] - 更改日期　　　　　[4] - 更改时间
PRINTL [5] - 更改TFLAG 　　　　[6] - 更改天气　　　　　[7] - 弾幕特能　　　　　
;PRINTL [ 8 ] - 一键演奏　　　　　
PRINTL [9] - 更改清洁度　　　　　
DRAWLINE
PRINTFORML [999] 离开

WHILE 1

INPUT

IF RESULT == 999
	RETURN 0
ELSEIF RESULT == 1
	CALL DEBUG_CHANGE_MONEY()
ELSEIF RESULT == 2
	CALL DEBUG_CHANGE_MONEY(1)
ELSEIF RESULT == 3
	CALL DEBUG_CHANGE_TIME(1)
ELSEIF RESULT == 4
	CALL DEBUG_CHANGE_TIME
ELSEIF RESULT == 5
	CALL SHOW_TFLAG_DEBUG
ELSEIF RESULT == 6
	CALL CHANGE_WEATHER_DEBUG
ELSEIF RESULT == 7
	CALL DANMAKU_FEATURE
;ELSEIF RESULT == 8
;	CALL ONE_KEY_MUSIC
;	CONTINUE
ELSEIF RESULT == 9
	CALL DEBUG_CHANGE_YOGORE
ELSE
	CLEARLINE 1
	CONTINUE
ENDIF
RESTART
WEND
@ITEM_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,1000
	SIF ITEMNAME:LOCAL == ""
		CONTINUE
	LOCALS = %ITEMNAME_TR(LOCAL),18,LEFT%：{ITEM:LOCAL}
	PRINTFORMLC [{LOCAL,4,RIGHT}] %LOCALS,25,LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT

IF RESULT == 9999
	RETURN 
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"ITEM")
	RESTART	
ELSEIF !INRANGE(RESULT,0,1000)
	CLEARLINE 1
ELSEIF ITEMNAME:RESULT == ""
	CLEARLINE 1
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %ITEMNAME_TR((LOCAL:2))%当前数值为：{ITEM:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	ITEM:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%ITEMNAME_TR((LOCAL:2))%当前数值为：{ITEM:(LOCAL:2)}
	RESTART
ENDIF
WEND
@BASE_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,30
	SIF BASENAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %BASENAME:LOCAL%：{BASE:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTL 以下为最大值（MAXBASE）
FOR LOCAL,0,30
	SIF BASENAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL+30,4,RIGHT}] %BASENAME:LOCAL%：{MAXBASE:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTL 请选择要修改的数据
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"BASE")
	CALL SEARCHFOR(1,"MAXBASE")
	RESTART
ELSEIF !INRANGE(RESULT,0,60)
	CLEARLINE 1
	CONTINUE	
ELSEIF RESULT <30 && BASENAME:RESULT == "" 
	CLEARLINE 1
	CONTINUE
ELSEIF RESULT >29 && BASENAME:(RESULT-30) == "" 
	CLEARLINE 1
	CONTINUE
ELSEIF RESULT >29
	LOCAL:2 = (RESULT-30)
	PRINTFORML %BASENAME:(LOCAL:2)%当前数值为：{MAXBASE:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	MAXBASE:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%BASENAME:(LOCAL:2)%当前数值为：{MAXBASE:ARG:(LOCAL:2)}
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %BASENAME:(LOCAL:2)%当前数值为：{BASE:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	BASE:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%BASENAME:(LOCAL:2)%当前数值为：{BASE:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@EX_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,100
	SIF EXNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %EXNAME:LOCAL%：{EX:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"EX")
	RESTART
ELSEIF !INRANGE(RESULT,0,100)||EXNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %EXNAME:(LOCAL:2)%当前数值为：{EX:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	EX:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%EXNAME:(LOCAL:2)%当前数值为：{EX:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@PALAM_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,200
	SIF PALAMNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %PALAM_TR(PALAMNAME:LOCAL)%：{PALAM:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"PALAM")
	RESTART
ELSEIF !INRANGE(RESULT,0,200)||PALAMNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %PALAMNAME:(LOCAL:2)%当前数值为：{PALAM:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	PALAM:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%PALAMNAME:(LOCAL:2)%当前数值为：{PALAM:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND
@EXP_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,200
	SIF EXPNAME:LOCAL == ""
		CONTINUE
	LOCALS = [{LOCAL,3}] %EXP_TR(LOCAL)%
	LOCALS:2 = ：
	LOCALS:1 = %TOSTR(EXP:ARG:LOCAL)%
	LOCALS += LOCALS:2
	LOCALS += LOCALS:1
	PRINTFORM %LOCALS, 30, LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINT [9997] 重置为处女　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"EXP")
	RESTART
ELSEIF RESULT == 9997
	CALL SET_VIRGIN_LIGHT(ARG)
	RESTART
ELSEIF !INRANGE(RESULT,0,150)||EXPNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %EXP_TR(LOCAL:2)%当前数值为：{EXP:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	EXP:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%EXP_TR(LOCAL:2)%当前数值为：{EXP:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@ABL_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,100
	SIF ABLNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,2,RIGHT}] %ABL_TR(LOCAL)%：{ABL:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"ABL")
	RESTART
ELSEIF !INRANGE(RESULT,0,100)||ABLNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %ABL_TR(LOCAL:2)%当前数值为：{ABL:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	ABL:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%ABL_TR(LOCAL:2)%当前数值为：{ABL:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@TALENT_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,300
	SIF TALENTNAME:LOCAL == ""
		CONTINUE
	LOCALS = [{LOCAL,3}] %TALENT_TR(LOCAL)%
	LOCALS:2 = ：
	LOCALS:1 = 【%GET_TALENTNAME(LOCAL, TALENT:ARG:LOCAL)%】
	SIF LOCALS:1 == "【】"
		LOCALS:1 = 【－】
	LOCALS += LOCALS:2
	LOCALS += LOCALS:1
	PRINTFORM %LOCALS, 40, LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTFORML [9999] 离开
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF !INRANGE(RESULT,0,300)||TALENTNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
LOCAL:2 = RESULT
PRINTFORML %TALENT_TR(LOCAL:2)%当前数值为：{TALENT:ARG:(LOCAL:2)}  【 %GET_TALENTNAME(LOCAL:2, TALENT:ARG:(LOCAL:2))% 】
PRINTL 请输入新的数据
INPUT
TALENT:ARG:(LOCAL:2) = RESULT
PRINTFORMW 修改完毕，%TALENT_TR(LOCAL:2)%当前数值为：{TALENT:ARG:(LOCAL:2)}   【 %GET_TALENTNAME(LOCAL:2, TALENT:ARG:(LOCAL:2))% 】
ENDIF
RESTART
WEND

@MARK_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,10
	SIF MARKNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %MARKNAME:LOCAL%：{MARK:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTFORML [9999] 离开
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF !INRANGE(RESULT,0,10)||MARKNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %MARKNAME:(LOCAL:2)%当前数值为：{MARK:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	MARK:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%MARKNAME:(LOCAL:2)%当前数值为：{MARK:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@CFLAG_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,1000
	SIF CFLAGNAME:LOCAL == ""
		CONTINUE
	SIF GROUPMATCH(LOCAL,387,388,389)
		CONTINUE
	LOCALS = [{LOCAL,3,RIGHT}] %CFLAGNAME_TR(CFLAGNAME:LOCAL), 25, LEFT%：{CFLAG:ARG:LOCAL}
	PRINTFORMLC %LOCALS, 40, LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
PRINTL
FOR LOCAL,6300,6327
	SIF CFLAGNAME:LOCAL == ""
		CONTINUE
	LOCALS = [{LOCAL,3,RIGHT}]%CFLAGNAME_TR(CFLAGNAME:LOCAL), 25, LEFT%：{CFLAG:ARG:LOCAL}
	PRINTFORMLC %LOCALS, 40, LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
FOR LOCAL,387,390
	LOCALS = [{LOCAL,3,RIGHT}] %CFLAGNAME_TR(CFLAGNAME:LOCAL), 25, LEFT%：{CFLAG:ARG:LOCAL}
	PRINTFORML %LOCALS, 40, LEFT%
NEXT
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"CFLAG")
	RESTART
ELSEIF !INRANGE(RESULT,0,9999)
	CLEARLINE 1
	CONTINUE	
;ELSEIF CFLAGNAME:RESULT == ""
;	CLEARLINE 1
;	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %CFLAGNAME_TR(CFLAGNAME:(LOCAL:2))%当前数值为：{CFLAG:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	CFLAG:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%CFLAGNAME_TR(CFLAGNAME:(LOCAL:2))%当前数值为：{CFLAG:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@TCVAR_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,1000
	SIF TCVARNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,3,RIGHT}] %@"%TCVARNAME:LOCAL%：{TCVAR:ARG:LOCAL}",30,LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"TCVAR")
	RESTART
ELSEIF RESULT < 0 || RESULT > 999
	CLEARLINE 1
	CONTINUE	
;ELSEIF TCVARNAME:RESULT == ""
;	CLEARLINE 1
;	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %TCVARNAME:(LOCAL:2)%当前数值为：{TCVAR:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	TCVAR:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%TCVARNAME:(LOCAL:2)%当前数值为：{TCVAR:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@STAIN_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,100
	SIF STAINNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %STAINNAME:LOCAL%：{STAIN:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"STAIN")
	RESTART
ELSEIF RESULT < 0 || RESULT > 99
	CLEARLINE 1
	CONTINUE	
;ELSEIF STAINNAME:RESULT == ""
;	CLEARLINE 1
;	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %STAINNAME:(LOCAL:2)%当前数值为：{STAIN:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	STAIN:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%STAINNAME:(LOCAL:2)%当前数值为：{STAIN:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@M_CFLAG_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,6
	SIF ERDNAME(M_CFLAG,LOCAL) == ""
		CONTINUE
	LOCALS = [{LOCAL,3,RIGHT}] %ERDNAME(M_CFLAG,LOCAL), 25, LEFT%：{M_CFLAG:ARG:LOCAL}
	PRINTFORMLC %LOCALS, 40, LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF !INRANGE(RESULT,0,9999)
	CLEARLINE 1
	CONTINUE	
;ELSEIF CFLAGNAME:RESULT == ""
;	CLEARLINE 1
;	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %ERDNAME(M_CFLAG,LOCAL:2)%当前数值为：{M_CFLAG:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	M_CFLAG:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%ERDNAME(M_CFLAG,LOCAL:2)%当前数值为：{M_CFLAG:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

[SKIPSTART]
@SHOW_COUNTRY_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,1000
	SIF CFLAGNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %CFLAGNAME:LOCAL%：{CFLAG:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTFORML [9999] 离开

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF !INRANGE(RESULT,0,999)||CFLAGNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %CFLAGNAME:(LOCAL:2)%当前数值为：{CFLAG:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	CFLAG:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%CFLAGNAME:(LOCAL:2)%当前数值为：{CFLAG:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND
[SKIPEND]
@MAGIC_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,18
	SIF ERDNAME(M_FLAG,LOCAL) == ""
		CONTINUE
	LOCALS = %ERDNAME(M_FLAG,LOCAL), 18, LEFT%：{M_FLAG:LOCAL}
	PRINTFORMLC [{LOCAL,4,RIGHT}] %LOCALS,25,LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
FOR LOCAL,0,17
	SIF ERDNAME(M_TFLAG,LOCAL) == ""
		CONTINUE
	LOCALS = %ERDNAME(M_TFLAG,LOCAL), 18, LEFT%：{M_TFLAG:LOCAL}
	PRINTFORMLC [{LOCAL+100,4,RIGHT}] %LOCALS,30,LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
WHILE 1
INPUT

IF RESULT == 9999
	RETURN 
ELSEIF !INRANGE(RESULT,0,1000)
	CLEARLINE 1
ELSEIF ITEMNAME:RESULT == ""
	CLEARLINE 1
ELSEIF INRANGE(RESULT,0,99)
	LOCAL:2 = RESULT
	PRINTFORML %ERDNAME(M_FLAG,LOCAL:2)%当前数值为：{M_FLAG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	M_FLAG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%ERDNAME(M_FLAG,LOCAL:2)%当前数值为：{M_FLAG:(LOCAL:2)}
	RESTART
ELSEIF INRANGE(RESULT,100,199)
	LOCAL:2 = RESULT - 100
	PRINTFORML %ERDNAME(M_TFLAG,LOCAL:2)%当前数值为：{M_TFLAG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	M_TFLAG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%ERDNAME(M_TFLAG,LOCAL:2)%当前数值为：{M_TFLAG:(LOCAL:2)}
	RESTART
ENDIF
WEND

@SHOW_FLAG_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,10000
	SIF FLAGNAME:LOCAL == ""
		CONTINUE
	LOCALS = [{LOCAL,4}] %FLAGNAME:LOCAL%
	LOCALS:2 = ：
	LOCALS:1 = %TOSTR(FLAG:LOCAL)%
	LOCALS += LOCALS:2
	LOCALS += LOCALS:1
	PRINTFORM %LOCALS, 45, LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"FLAG")
	RESTART
ELSEIF !INRANGE(RESULT,0,9999)||FLAGNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %FLAGNAME:(LOCAL:2)%当前数值为：{FLAG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	FLAG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%FLAGNAME:(LOCAL:2)%当前数值为：{FLAG:(LOCAL:2)}
ENDIF
RESTART
WEND

@DEBUG_CHANGE_MONEY(ARG)
PRINTFORML 当前所持\@!ARG?金#筹码\@：{MONEY}
PRINTFORML 所持\@!ARG?金#筹码\@改为多少？
PRINTFORML 输入负数则不改动
INPUT
IF RESULT >= 0
	MONEY:(ARG*2) = RESULT
	PRINTFORMW 现在的所持金:%金額表示(MONEY:(ARG*2))%
ELSE
	PRINTFORM 未作改动
	PRINTL
	WAIT
	RETURN 0
ENDIF
[SKIPSTART]
@DEBUG_CHANGE_MONEY2
PRINTFORML 当前所持筹码：{MONEY:2}
PRINTL 所持筹码改为多少？
PRINTFORML 输入负数则不改动
INPUT

IF RESULT >= 0
	MONEY:2 = RESULT
	PRINTFORMW 现在的所持筹码:%金額表示(MONEY:2)%
ELSE
	PRINTFORM 未作改动
	PRINTL
	WAIT
	RETURN 0
ENDIF
[SKIPEND]
[SKIPSTART]
@ESTRUS_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
PRINTL ★★★当日有效　当日有效　当日有效　次日如需使用起床后请重新开启★★★
PRINTL 
PRINTL 开启恶堕模式后，当日任何接近主角的人
PRINTL 情绪欲情潤滑ＭＡＸ，丧失理性（理性＝０）
PRINTL 忘記对你的壞印象(取消憤怒狀態)，无条件进入发情状态
PRINTL 总之就是湿得一塌糊涂咯！
PRINTL 
PRINTL 后宫模式須先開啟惡墮模式
PRINTL 開啟后宫模式後的效果如下
PRINTL 告白数量无限制
PRINTL 無視生理期，無視體質，無視種族，無視宮填充度，無視宮狀態，無視精質量，一發入魂，必定受孕
DRAWLINE
PRINTFORM [1] - 恶堕模式：\@ TFLAG:301 > 0 ? [已经开启] # [尚未开启] \@　
PRINTFORML [2] - 后宫模式：\@ TFLAG:301 == 2 ? [已经开启] # [尚未开启] \@　
DRAWLINE
PRINTFORML [9999] 离开

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 1
	IF TFLAG:301 == 0
		TFLAG:301 = 1
		PRINTL 恶堕模式已开启
		wait
	ELSE
		TFLAG:301 = 0
		PRINTL 恶堕模式已關閉
		wait
	ENDIF
	CONTINUE
ELSEIF RESULT == 2
	IF TFLAG:301 == 1
		TFLAG:301 = 2
		PRINTL 受孕模式已开启
		wait
	ELSEIF TFLAG:301 == 2
		TFLAG:301 = 1
		PRINTL 受孕模式已關閉
		wait
	ELSE
		PRINTL 請先開啟恶堕模式
		wait
	ENDIF
	CONTINUE
ENDIF
RESTART
WEND
[SKIPEND]

@SHOW_TFLAG_DEBUG
LOCAL = 0
VARSIZE TFLAG
LOCAL:9 = RESULT

;$PRINT_TFLAG_LIST

PRINTFORMLC (MAX = {LOCAL:9-1})
PRINTL
DRAWLINE
PRINTFORML 現在目前的TFLAG列表
PRINTFORML 请选择需要修改的TFLAG
DRAWLINE
REPEAT LOCAL:9
	SIF STRLENS(TFLAGNAME:COUNT) == 0
		CONTINUE
	LOCALS = [{COUNT, 3}] \@ (STRLENS(TFLAGNAME:COUNT) == 0) ? TFLAG:{COUNT, 3} # %TFLAGNAME:COUNT%\@ = {TFLAG:COUNT}
	IF COUNT != 194 && COUNT != 152
		PRINTFORMLC %LOCALS, 40, LEFT%
	ELSE
		PRINTFORMLC %LOCALS, 80, LEFT%
	ENDIF
REND
PRINTL 
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"TFLAG")
	;GOTO PRINT_TFLAG_LIST
ELSEIF !INRANGE(RESULT,0,LOCAL:9)
	CONTINUE
ENDIF
LOCAL:1 = RESULT
DRAWLINE
PRINTFORML \@ (STRLENS(TFLAGNAME:RESULT) == 0) ? TFLAG:{RESULT} # %TFLAGNAME:RESULT%\@ 修改為(現在{TFLAG:RESULT})
PRINTFORML 請輸入数値
PRINTLC [99999] - 返回
INPUT
SIF RESULT == 99999
	RESTART
TFLAG:(LOCAL:1) = RESULT
PRINTFORMW 已修改%TFLAGNAME:(LOCAL:1)%为{RESULT}
;GOTO PRINT_TFLAG_LIST
RESTART
WEND

@CHANGE_WEATHER_DEBUG
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = LINECOUNT
WHILE 1
	CLEARLINE LINECOUNT - LOCAL
	PRINTL 请选择要修改的天气类型
	PRINTL
	PRINTL [0] - 标准　　　　　[1] - 异常　　　　　[2] - 特殊　　　　　
	PRINTL
	PRINTL [9] - 返回
	INPUT
	IF RESULT == 9
		RETURN
	ELSEIF INRANGE(RESULT,0,2)
		CALL CHANGE_WEATHER_DEBUG_Menu(RESULT)
	ENDIF
WEND

@CHANGE_WEATHER_DEBUG_Menu(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
IF !ARG
	PRINTL 天气改为哪一种？
	
	PRINTLC [ 0] - 晴天
	PRINTLC [ 1] - 晴朗
	PRINTLC [ 2] - 多云
	PRINTL [ 3] - 阴天
	PRINTLC [ 4] - 雨
	PRINTLC [ 5] - 大雨
	PRINTLC [ 6] - 雾雨
	PRINTL [ 7] - 雾
	PRINTLC [ 8] - 雪
	PRINTLC [ 9] - 暴风雪
	PRINTLC [10] - 小雪
	PRINTL [11] - 雾雪
	PRINTLC [12] - 雨雪交加
	PRINTL [13] - 冰雹
	PRINTL
	PRINTL [99] - 返回
	LOCAL = LINECOUNT
	WHILE 1
		CLEARLINE LINECOUNT - LOCAL
		INPUT
		IF RESULT == 99
			RETURN 0
		ELSEIF INRANGE(RESULT,0,13)
			TIME:5 = RESULT
			;天气重修，强行设定对应天气系统
			CALL qol_FORCESET_REGION_WEATHER_SYSTEM
			PRINTFORMW 已修改
			RETURN 0
		ENDIF
	WEND
ELSEIF ARG==1
	;@CHANGE_WEATHER_FLAG_89_DEBUG
	PRINTL 异常气象改为哪一种？
	
	PRINTLC [ 1] - Ｃ敏感
	PRINTLC [ 2] - Ｖ敏感
	PRINTLC [ 3] - Ａ敏感
	PRINTL [ 4] - Ｂ敏感
	PRINTLC [ 5] - Ｍ敏感
	PRINTLC [ 6] - 裙子
	PRINTLC [ 7] - 内裤
	PRINTL [ 8] - 桃色之风
	PRINTLC [ 9] - 台风
	PRINTL [10] - 猛烈暴风雪
	PRINTL
	PRINTL [ 0] - 无
	PRINTL
	PRINTL [99] - 返回
	
	LOCAL = LINECOUNT
	WHILE 1
		CLEARLINE LINECOUNT - LOCAL
		INPUT
		IF RESULT == 99
			RETURN 0
		ELSEIF INRANGE(RESULT,0,10)
			FLAG:89 = RESULT
			PRINTFORMW 已修改
			RETURN 0
		ENDIF
	WEND
ELSEIF ARG==2
	;@CHANGE_WEATHER_7_DEBUG
	PRINTL 特殊气象改为哪一种？
	
	PRINTLC [1] - 彩虹
	PRINTLC [2] - 钻石尘
	PRINTLC [3] - 流星
	PRINTLC [4] - 流星雨
	PRINTL
	PRINTLC [0] - 无
	PRINTL
	PRINTL [9] - 返回
	
	LOCAL = LINECOUNT
	WHILE 1
		CLEARLINE LINECOUNT - LOCAL
		INPUT
		IF RESULT == 9
			RETURN 0
		ELSEIF INRANGE(RESULT,0,4)
			TIME:7 = RESULT
			;天气重修，强行设定对应天气系统
			CALL qol_FORCESET_REGION_RAINBOW_SYSTEM
			PRINTFORMW 已修改
			RETURN 0
		ENDIF
	WEND
ENDIF
@TARGET_EDIT
IF TARGET == 0
	PRINTL 当前没有正在对话的人物
	WAIT
	RETURN 0
ELSE
	PRINTFORML  当前人物是%CALLNAME:TARGET%,序号{TARGET}
	CALL SHOW_SLAVE_DEBUG,TARGET
	RETURN 0
ENDIF

;检索函数___by.LJ
@SEARCHFOR(resultFLAG,SEARCHFORname)
#DIM resultFLAG
#DIM SEARCHFORlog
#DIMS SEARCHFORname
#DIMS ESCname,3
ESCname:0 = 返回
ESCname:1 = ESC
ESCname:2 = esc
PRINTL
WHILE 1
;$LOOP_SEARCH
DRAWLINEFORM =
PRINTFORML 输入需要检索的%SEARCHFORname%的值\@(resultFLAG == 1) ?（輸入-999999則返回） # （離開請輸入返回/ESC）\@
PRINTFORM %" "*10%
IF resultFLAG == 1
    PRINTBUTTON @" [返回] ",-999999
    INPUT
ELSEIF resultFLAG == 2
    PRINTBUTTON @" [返回] ","返回"
    INPUTS
ENDIF
SEARCHFORlog = 0
IF resultFLAG == 1
    IF RESULT == -999999
        PRINTW 返回
        DRAWLINE
        RETURN
    ELSE
        PRINTFORML %SEARCHFORname%的值為【{RESULT}】的搜尋結果：
        SELECTCASE SEARCHFORname
            CASE "FLAG"
                REPEAT VARSIZE("FLAG")
                    IF FLAG:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%FLAGNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "CFLAG"
                REPEAT VARSIZE("CFLAG")
                    IF CFLAG:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%CFLAGNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TFLAG"
                REPEAT VARSIZE("TFLAG")
                    IF TFLAG:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%TFLAGNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "BASE"
                REPEAT VARSIZE("BASE")
                    IF BASE:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%BASENAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "MAXBASE"
                REPEAT VARSIZE("MAXBASE")
                    IF MAXBASE:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%BASENAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "EXP"
                REPEAT VARSIZE("EXP")
                    IF EXP:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%EXP_TR(COUNT)%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "ABL"
                REPEAT VARSIZE("ABL")
                    IF ABL:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%ABLNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TALENT"
                REPEAT VARSIZE("TALENT")
                    IF TALENT:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%TALENT_TR(COUNT)%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "PALAM"
                REPEAT VARSIZE("PALAM")
                    IF PALAM:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%PALAMNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "JUEL"
                REPEAT VARSIZE("JUEL")
                    IF JUEL:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%PALAMNAME:COUNT%の珠】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "EX"
                REPEAT VARSIZE("EX")
                    IF EX:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%EXNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "SOURCE"
                REPEAT VARSIZE("SOURCE")
                    IF SOURCE:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%SOURCENAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "EQUIP"
                REPEAT VARSIZE("EQUIP")
                    IF EQUIP:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%EQUIPNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TEQUIP"
                REPEAT VARSIZE("TEQUIP")
                    IF TEQUIP:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%TEQUIPNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "ITEM"
                REPEAT VARSIZE("ITEM")
                    IF ITEM:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%ITEMNAME_TR(COUNT)%】
                        SEARCHFORlog ++
                    ENDIF
                REND
                CASE "MARK"
                REPEAT VARSIZE("MARK")
                    IF MARK:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%MARKNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "RELATION"
                REPEAT VARSIZE("RELATION")
                    IF RELATION:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{RELATION:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "STAIN"
                REPEAT VARSIZE("STAIN")
                    IF STAIN:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{STAIN:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "GOTJUEL"
                REPEAT VARSIZE("GOTJUEL")
                    IF GOTJUEL:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{GOTJUEL:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "NOWEX"
                REPEAT VARSIZE("NOWEX")
                    IF NOWEX:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{NOWEX:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TCVAR"
                REPEAT VARSIZE("TCVAR")
                    IF TCVAR:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{TCVAR:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASEELSE
                RETURN 0
        ENDSELECT
        PRINTL
        PRINTFORML 合計【{SEARCHFORlog}】筆
    ENDIF
ELSEIF resultFLAG == 2
    IF RESULTS == ESCname:0 || RESULTS == ESCname:1 || RESULTS == ESCname:2
        PRINTW 返回
        DRAWLINE
        RETURN
    ELSE
        PRINTFORML %SEARCHFORname%的值為【%RESULTS%】的搜尋結果
        SELECTCASE SEARCHFORname
            CASE "STR"
                REPEAT VARSIZE("STR")
                    IF STRFIND(STR:COUNT,RESULTS,-1) != -1
                        PRINTFORMLC [{COUNT}]:【%STR:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "CSTR"
                REPEAT VARSIZE("CSTR")
                    IF STRFIND(CSTR:TARGET:COUNT,RESULTS,-1) != -1
                        PRINTFORMLC [{COUNT}]:【%CSTR:TARGET:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TSTR"
                REPEAT VARSIZE("TSTR")
                    IF STRFIND(TSTR:COUNT,RESULTS,-1) != -1
                        PRINTFORMLC [{COUNT}]:【%TSTR:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TRAIN"
                REPEAT VARSIZE("TRAINNAME")
                    IF STRFIND(TRAINNAME:COUNT,RESULTS,-1) != -1
                        PRINTFORMLC [{COUNT}]:【%TRAINNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASEELSE
                RETURN 0
        ENDSELECT
        PRINTL
        PRINTFORML 合計【{SEARCHFORlog}】筆
    ENDIF
ENDIF
PRINTL
WEND

@DANMAKU_FEATURE
DRAWLINE
PRINTFORML %CALLNAME:MASTER%的弾幕特能情况如下
CALL DANMAKU_OPPONENT_STATUS(0, 1)
DRAWLINE
PRINTL 请选择需要添加的弾幕特能
DRAWLINE
PRINT [ 0 ] - 言笑自若　　　
PRINT [ 1 ] - 温柔敦厚　　　
PRINT [ 2 ] - 勇気凛々　　　
PRINTL [ 3 ] - 豪放磊落　　　
PRINT [ 4 ] - 怪力乱神　　　
PRINT [ 5 ] - 洽覧深識　　　
PRINT [ 6 ] - 残忍酷薄　　　
PRINTL [ 7 ] - 乾坤一擲　　　
PRINT [ 8 ] - 狷介孤高　　　
PRINTL [ 9 ] - 幽愁暗恨　　　   
DRAWLINE
PRINTL [ 98 ] - 重置技能　　　
PRINT [ 99 ] - 返回上级　　　
WHILE 1
INPUT
IF RESULT >= 0 && RESULT <= 10
	SETBIT CFLAG:0:弾幕特能, RESULT
ELSEIF RESULT == 98
	CFLAG:0:弾幕特能 = 0
ELSEIF RESULT == 99
	RETURN 0
ELSE
	CLEARLINE 1
ENDIF
RESTART
WEND
@DEBUG_CHANGE_TIME(ARG)
#DIMS MM,4="春","夏","秋","冬"
IF !ARG
	PRINTFORML 现在时间是：{TIME/60}时{TIME%60}分
	;CALL INFO_CLOCK_IMAGE(TIME)
	PRINTFORML 请用hhmm格式输入时间（输入范围hh=0 ~ 23,mm=0 ~ 59，对象外的值输入取消）
	PRINTFORML [-1] - 中止
	INPUT
	IF RESULT==-1
		RETURN
	ELSEIF INRANGE(RESULT/100,0,23)&&INRANGE(RESULT%100,0,59)
		TIME = RESULT/100 * 60 + RESULT%100
		TIME:1 = TIME + 1440 * DAY
		CALL COLORMESSAGE(@"%時刻表示(TIME)%", C_YELLOW)
		PRINTFORMW 了。
	ENDIF
ELSEIF ARG
	;@DEBUG_CHANGE_DAY
	PRINTFORML 请输入天数。今天是：第{DAY}天
	PRINTBUTTON @"[更改日期] ",-1
	PRINTBUTTON @"[返回菜单] ",-2
	WHILE 1
	INPUT
	IF RESULT==-1
		PRINTFORML 请输入月份(春夏秋冬1~4) 现在%MM:(DAY:2-1)%之月
		WHILE 1
			INPUT
			IF !INRANGE(RESULT,1,4)
				CLEARLINE 1
			ELSE
				DAY:2=RESULT
				BREAK
			ENDIF
		WEND
		PRINTFORML 请输入日期(1~31)	现在{DAY:3}日
		WHILE 1
			INPUT
			IF !INRANGE(RESULT,1,31)
				CLEARLINE 1
			ELSE
				DAY:3=RESULT
				BREAK
			ENDIF
		WEND
		PRINTFORMW 今天的日期已改为：%MM:(DAY:2-1)%之月 {DAY:3}日
	ELSEIF RESULT >= 0
		DAY = RESULT
		PRINTFORMW 今天的天数已改为：第{DAY}天
	ELSE
		SIF RESULT!=-2
			PRINTFORMW 未作改动
		RETURN 0
	ENDIF
	RESTART
	WEND
ENDIF
[SKIPSTART]
PRINTL 请选择或输入要改变的时间长度
PRINTL
PRINTL [-60] -60分　[-30] -30分　[-20] -20分　[ 20] +20分　[ 30] +30分　[ 60] +60分　
PRINTL
PRINTL [-10] -10分　[ -5] -5 分　[ -1] -1 分　[  1] +1 分　[  5] +5 分　[ 10] +10分　
PRINTL
PRINTL [  0] 归零　 [999] 返回上级
WHILE 1
INPUT

IF RESULT == 999
	RETURN 0
ELSEIF !RESULT
	TIME=0
ELSEIF TIME + RESULT < 0 || TIME + RESULT > 1439
	PRINTL 数值超出允许范围
	PRINTW
ELSE
	TIME += RESULT
ENDIF
RESTART
WEND
[SKIPEND]

@DEBUG_CHANGE_YOGORE
PRINTFORML 请输入地点编号，当前玩家位置为{CFLAG:MASTER:現在位置}
PRINTBUTTON @"[一键干净] ",-1
PRINTBUTTON @"[一键垃圾] ",-2
PRINTBUTTON @"[返回菜单] ",-3
WHILE 1
	INPUT
	IF RESULT == -1
		FOR LOCAL, 0, 2000
			YOGORE:LOCAL = 0
		NEXT
		PRINTFORMW 全地图垃圾量已变为0
		RESTART
	ELSEIF RESULT == -2
		FOR LOCAL, 0, 2000
			YOGORE:LOCAL = 6000
		NEXT
		PRINTFORMW 全地图垃圾量已变为6000
		RESTART
	ELSEIF RESULT == -3
		BREAK
	ELSEIF RESULT >= 1 && RESULT <= 2000
		LOCAL = RESULT
		PRINTFORML 当前编号为{LOCAL}，位置名称为%NAME_FROM_PLACE(LOCAL)%，请输入垃圾量
		INPUT
		IF RESULT >= 0
			YOGORE:LOCAL = RESULT
		ELSE
			PRINTFORMW 垃圾量非法，请重新输入
			RESTART
		ENDIF
	ELSE
		PRINTFORMW 地点ID非法，请重新输入
		RESTART
	ENDIF
WEND

@重置素质(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
FOR LOCAL,0,352
	TALENT:ARG:LOCAL = CSVTALENT(ARG,LOCAL)
NEXT
RETURN RESULT
