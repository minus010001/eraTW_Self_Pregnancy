@DLCOPTIONPRINT
	PRINTFORML 
	PRINTFORML 
	PRINTFORML 设定内置补丁的启动与否
	PRINTFORML 点击选项可以查看补丁详细说明
	PRINTFORML 如果有BUG可以找迷子反馈，但不一定会修复，因为我没有脑子
	SETCOLOR C_RED
	PRINTFORML 部分补丁可能会极大的降低游戏难度，请自己酌情启用！！！
	RESETCOLOR
	PRINTFORML 补丁适用游戏版本：4.842（自用测试版，有bug也不奇怪）
	DRAWLINEFORM =
	PRINTFORML 现在的设定：

	PRINT 天之声补丁：
	IF SKYSOUND == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [ 1] 天之声补丁设置", 20%


	PRINT 防袭击补丁：
	IF ENCOUNTDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [ 2] 防袭击补丁设置", 20%

	PRINT 丰富的天气：
	IF WEATHERDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [ 3] 丰富的天气设置", 20%

	PRINT 幻想乡之主：
	IF DEBUGGERR == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [ 4] 幻想乡之主设置", 20%
	
	PRINT 重选欧金金：
	IF OUJINJIN == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [ 5] 重选欧金金设置", 20%

	PRINT 简单化实绩：
	IF BONUSX == 0 || BONUSX == 100
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [ 6] 简单化实绩设置", 20%

	PRINT 道歉免时间：
	IF !APLOGIZEDLC
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [ 7] 道歉免时间设置", 20%

	PRINT 无条件访问：
	IF !HOMEWEL
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [8] 无条件访问设置", 20%

	PRINT 工作可推倒：
	IF !JOBFUCKING
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [9] 工作可推倒设置", 20%

	PRINT 药物简易化：
	IF !DRUG
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [10] 药物简易化设置", 20%

	PRINT 属性简易化：
	IF ABLDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [11] 属性简易化设置", 20%

	PRINT 弹幕战强化：
	IF BATTLEDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [12] 弹幕战强化设置", 20%

	PRINT 新妻泛用化：
	IF NEWWIFE == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [13] 新妻泛用化设置", 20%

	PRINT 胖次弹幕战：
	IF PANGCI == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [14] 胖次弹幕战设置", 20%

	PRINT 生动的记忆：
	IF MEETCHECKDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [15] 生动的记忆设置", 20%
	
	PRINT 自己开宴会：
	IF EATDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [16] 自己开宴会设置", 20%

	PRINT 同居与陪睡：
	IF SLEEPTOGETHER == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [17] 同居与陪睡设置", 20%

	PRINT 无限化入住：
	IF HOUGONG == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [18] 无限化入住设置", 20%

	PRINT 易书与隙间：
	IF HANDBOOK == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [19] 易书与隙间设置", 20%

	PRINT 自动化浇水：
	IF AUTOWATER == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [20] 自动化浇水设置", 20%
	
	PRINT 高强度怀孕：
	IF BABYDLC == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [21] 高强度怀孕设置", 20%
	
	PRINT 彩色动态图：
	IF RECOLOREDMAPS == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSEIF ANIMATERECOLOREDMAPS == 0
		SETCOLOR 0x00FF00
		PRINTFORM 无动态
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 动态图
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [22] 彩色动态图设置", 20%

	PRINT 射精后属性增长：
	IF DEVELOPMENT == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [23] 射精后属性上限增长设置", 20%

	PRINT 射精倍率：
	IF SHOOTINGSTAR == 0
		SETCOLOR 0x994C00
		PRINTFORM 未加倍
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已加倍
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [24] 射精倍率设置", 20%

	PRINT 一键扫除：
	IF SWEEPGOD == 0
		SETCOLOR 0x994C00
		PRINTFORM 未开启
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已开启
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [25] 一键扫除设置", 20%
		
	PRINT 催眠大师系统：
	IF HYGOD == 0
		SETCOLOR 0x994C00
		PRINTFORM 未启动
	ELSE
		SETCOLOR 0x00FF00
		PRINTFORM 已启动
	ENDIF
	RESETCOLOR
	PRINTFORML %"      [26] 催眠大师设置", 20%
