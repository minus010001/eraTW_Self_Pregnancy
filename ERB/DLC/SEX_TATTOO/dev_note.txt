目前已经实装的烙印类别：

1. 快感系列淫纹
   实现代码在 `ERB/ステータス計算関連/SOURCE/SOURCE_CBVA.ERB` 的最后（在函数 `@SOURCE_CVAB_EXTRA(ARG)` 的最后），直接对 CUP:ARG:快Ｃ 等上升量进行乘算。

2. 欲情纹、屈服纹（只有辅纹）
   实现代码在 `ERB/ステータス計算関連/SOURCE/SOURCE_EXTRA.ERB` 中，函数 `@SOURCE_EXTRA(ARG)` 的末尾。与上面类似，直接对 CUP:ARG 数组中对应项乘算。

TODO:

1. 实现其他几类淫纹（魅魔、同化、封印）
   （看上去精壶之纹（扩大子宫大小）在 TW 中没有意义，可以删除？）
2. 调整平衡性，以 TW 的方式衡量目标角色的接受淫纹意愿，例如爱欲、炮友、思慕的判定。目前意愿普遍偏低，因为 eraYM 中的若干个正向判定在 TW 中没有对应的概念。
   以及，TW 中角色较多，2 万元的价格是否应该下调？