;-------------------------------------------------
;被放入Ａ珠
;-------------------------------------------------
@COM127
;-------------------------------------------------
;COM別FLAGの設定
;-------------------------------------------------
;行為者
TCVAR:116 = TARGET
;奉仕快楽フラグ
TCVAR:108 = 1

CALL DECLARE_EJACULATION(PLAYER, 射精場合_Ａ責め, 0, TARGET, CHARA_NONE, TARGET, CHARA_NONE, 300)
;強度

;-------------------------------------------------
;ソース追加処理
;-------------------------------------------------
;消費体力、気力
DOWNBASE:体力 += 100
DOWNBASE:気力 += 150

SOURCE:PLAYER:快Ａ = 200
SOURCE:性行動 = 300
SOURCE:達成 = 200
SOURCE:征服 = 800
SOURCE:不潔 = 200
SOURCE:反感 = 100

;-------------------------------------------------
;特殊ソース処理
;-------------------------------------------------
;圧抑、抵抗持ちには、抑鬱ソース追加
SIF TALENT:自己愛 < 0 || TALENT:抵抗
	SOURCE:鬱屈 += 400

;-------------------------------------------------
;汚れの処理
;-------------------------------------------------
;対象の手⇔調教者のＡ的污垢が移動
STAIN:1 |= STAIN:PLAYER:4
STAIN:PLAYER:4 |= STAIN:1

;-------------------------------------------------
;経験の処理
;-------------------------------------------------
;同性愛　レズなら戻り値1ゲイなら戻り値2
IF HETEROSEX(TARGET,PLAYER)
	EXP:(50 + HETEROSEX(TARGET,PLAYER)) += 7
	EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 7
ENDIF

STR:0 = 被放入Ａ珠

CALL PEARL_INSERT(TARGET, 5)

PRINTFORML 让%CALLNAME:TARGET%往自己肛门里放入拉珠
PRINTFORMW 尝试在{TFLAG:パール期限-TIME}分的时间里忍住

CALL ENDUFUFU_ALL

RETURN 1

@CAN_COM127(ARG)
SKIPDISP ARG
PRINTL 被放入Ａ珠
;-------------------------------------------------
;実行判定処理
;-------------------------------------------------

RETURN 1
;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE127
#LOCALSIZE 1
#LOCALSSIZE 1
;被放入Ａ珠実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(127)
	RETURN RESULT
;下半身着衣（調教者）
SIF TEQUIP:PLAYER:0 & 4
	RETURN 0
;睡眠姦中
SIF CFLAG:恶作剧 > 1
	RETURN 0
;停止中は不可
SIF (FLAG:70 == 1 && CFLAG:TARGET:344 == 0)
	RETURN 0
;にゅーぷらん実行中
SIF TFLAG:泡風呂
	RETURN 0
;SIF ABL:施虐属性 < 2
;	RETURN 0
;SIF EXP:PLAYER:Ａ経験 < 10
;	RETURN 0

RETURN 1

