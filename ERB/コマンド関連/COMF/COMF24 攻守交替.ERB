;-------------------------------------------------
;攻守交替
;-------------------------------------------------
@COM24
VARSET LOCAL
CFLAG:MASTER:诶嘿嘿 = 2
TFLAG:COMABLE管理 = 3
FOR LOCAL,1,CHARANUM
	IF CFLAG:MASTER:現在位置 == CFLAG:LOCAL:現在位置
		;跳过本来不在诶嘿嘿的角色，防止潜伏时将在场所有人拉入啪啪啪
		SIF CFLAG:LOCAL:诶嘿嘿 == 0
			CONTINUE
		CFLAG:LOCAL:诶嘿嘿 = 2
		;2025年7月7日 新增性互动，平等地交换攻守
		SETBIT TCVAR:LOCAL:随便,1
		LOCAL:1 ++
	ENDIF
NEXT
PRINTFORML 把主导权交给了%CALLNAME:TARGET%\@ LOCAL:1 > 1 ? 她们 #  \@
TIME += 5

RETURN 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE24
#LOCALSIZE 1
#LOCALSSIZE 1
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(150)
	RETURN RESULT
;相手の欲情
SIF GETPALAMLV(PALAM:欲情,10) < 5
	RETURN 0
SIF TCVAR:强行
	RETURN 0
;時間停止中も駄目
SIF !SHIRAHU(TARGET)
	RETURN 0
;延迟中不行，因为对方无法发动指令
SIF CFLAG:延迟
	RETURN 0
;なんか装着してるとダメ
FOR LOCAL,11,25
	SIF TEQUIP:LOCAL
		RETURN 0
NEXT
;插入中让对方开始主导，有何不可呢？
;SIF TEQUIP:50 != -1
;	RETURN 0
;SIF TEQUIP:51 != -1
;	RETURN 0
;淋浴中はダメ
SIF TEQUIP:32
	RETURN 0
;吸乳头中はダメ
SIF TEQUIP:41
	RETURN 0
;乳揉み中はダメ
SIF TEQUIP:42
	RETURN 0
;継続接吻
SIF TEQUIP:継続接吻
	RETURN 0

RETURN 1
