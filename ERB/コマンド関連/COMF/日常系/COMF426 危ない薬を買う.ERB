;-------------------------------------------------
;危ない買薬
;-------------------------------------------------
@COM426

$LOOP
PRINTFORML 请选择要购买的道具 所持金:%金额表示(MONEY)%
PRINTL
VARSET LOCALS
LOCALS = [41] 媚药
LOCALS '= @"%LOCALS,24,LEFT%%金额表示(3000), 8, RIGHT%"
PRINTFORM %LOCALS,48,LEFT%
PRINTL 
LOCALS = [42] 利尿剂
LOCALS '= @"%LOCALS,24,LEFT%%金额表示(3000), 8, RIGHT%"
PRINTFORM %LOCALS,48,LEFT%
PRINTL 
LOCALS = [250] 催情药
LOCALS '= @"%LOCALS,24,LEFT%%金额表示(5000), 8, RIGHT%"
PRINTFORM %LOCALS,48,LEFT%
PRINTL 更容易催生好感

;LOCALS = [2] 丰胸剂　￥2000
LOCALS = [2] 牛乳
LOCALS '= @"%LOCALS,24,LEFT%%金额表示(2000), 8, RIGHT%"
PRINTFORM %LOCALS,48,LEFT%
PRINTL 持续饮用这个说不定胸部会变大呢
LOCALS = [501] 缩胸剂
LOCALS '= @"%LOCALS,24,LEFT%%金额表示(2000), 8, RIGHT%"
PRINTFORM %LOCALS,48,LEFT%
PRINTL 持续饮用这个说不定胸部会变小呢
LOCALS = [502] ＡＰＴＸ4869
LOCALS '= @"%LOCALS,24,LEFT%%金额表示(10000), 8, RIGHT%"
PRINTFORM %LOCALS,48,LEFT%
PRINTL 让新一倒霉的药
LOCALS = [503] 大力士072
LOCALS '= @"%LOCALS,24,LEFT%%金额表示(10000), 8, RIGHT%"
PRINTFORM %LOCALS,48,LEFT%
PRINTL 为了迅速练出巨大的肉体而研制出的训练药物
LOCALS = [42] 快乐饮料
LOCALS '= @"%LOCALS,24,LEFT%%金额表示(3000), 8, RIGHT%"
PRINTFORM %LOCALS,48,LEFT%
PRINTL 能暂时获得幸福感、最终会使感情枯竭
DRAWLINE
PRINTL [100] 中止
INPUT
SELECTCASE RESULT
	CASE 0
		IF MONEY < ITEMPRICE:RESULT
			PRINTW 金钱不足
			GOTO LOOP
		ELSEIF ITEM:RESULT > 50
			PRINTW 已经够多了
			GOTO LOOP
		ELSE
			PRINTFORMW 购买了%ITEMNAME_TR(RESULT)%
			ITEM:RESULT += 5
			MONEY -= ITEMPRICE:RESULT
			GOTO LOOP
		ENDIF
	CASE 100
		RETURN -1
	CASEELSE
		GOTO LOOP
ENDSELECT

;買薬
@COM_ABLE426
#LOCALSIZE 1
#LOCALSSIZE 1
;ひとまず停止
RETURN 0
;停止中は不可
SIF FLAG:70 == 1
	RETURN 0
;移動実行判定
SIF !TFLAG:100
	RETURN 0

SIF CFLAG:MASTER:現在位置 != CFLAG:[[鈴仙]]:現在位置 && CFLAG:MASTER:現在位置 != CFLAG:[[帝]]:現在位置
	RETURN 0
SIF CFLAG:诶嘿嘿 == 2
	RETURN 0
RETURN 1
