;-------------------------------------------------
;素材購入
;日常系指令　レベル1
;-------------------------------------------------
@COM365
#DIM PRICE
;とりあえずデフォは\3000にしてみる
PRICE = 3000

PRINTFORML 要从%CALLNAME:TARGET%以%金额表示(PRICE)%购买素材吗？
PRINTFORML （购买的素材与采集等级和住宅所在位置相关、并且追加采集经验）
CALL ASK_M("买",MONEY >= PRICE, "不买",1)
SELECTCASE RESULT
	CASE 0
		CALL BUY_MATERIAL(TARGET, CFLAG:素材販売, PRICE)
		TIME += 5
		RETURN 1
	CASE 1
		RETURN -1
ENDSELECT

@BUY_MATERIAL(C_ID, SPOT, PRICE)
#DIM C_ID		;角色
#DIM SPOT		;対応採集位置
#DIM PRICE		;価格
#DIM 採集パワー	;
#DIM QTY, 3
#DIM COLLECTED, 3

SIF TCVAR:C_ID:素材購入済み
	RETURN
SIF !SPOT
	RETURN
SIF MONEY < PRICE
	RETURN

TCVAR:C_ID:素材購入済み = 1
採集パワー = 20 + TALENT:C_ID:採集Lv * 20 + TFLAG:幸運補正 * 15
SOURCE:C_ID:歓楽 += 500
SOURCE:C_ID:達成 += 150
SIF TALENT:C_ID:思慕
	採集パワー += 5
SIF TALENT:C_ID:恋慕
	採集パワー += 15

FOR LOCAL,0,3
	CALL Gathering_Drawing(SPOT, 採集パワー)
	COLLECTED:LOCAL = RESULT:0
	QTY:LOCAL = RESULT:1
	EXP:C_ID:採集経験 ++
	;なんも見つからないと経験にBONUS
	IF COLLECTED:LOCAL == 0
		EXP:C_ID:採集経験 += 2
	ELSE
		LOCALS = %ITEMNAME:(COLLECTED:LOCAL)%
		CALL GET_ITEM(LOCALS, QTY:LOCAL, 2)
	ENDIF
NEXT

IF !COLLECTED:0 && !COLLECTED:1 && !COLLECTED:2
	PRINTFORMW 今天似乎什么都没找到
	EXP:C_ID:採集経験 += 1
	TFLAG:193 = -1
ELSE
	MONEY -= PRICE
	PRINTFORMW 以%金額表示(PRICE)%收购了素材
ENDIF

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE365
#LOCALSIZE 1
#LOCALSSIZE 1
;実行判定
SIF !TFLAG:100
	RETURN 0
;一括管理
SIF GLOBAL_COMABLE(365)
	RETURN RESULT
;睡眠中
SIF !SHIRAHU(TARGET)
	RETURN 0
SIF !CFLAG:素材販売
	RETURN 0
;don't show if already performed today
SIF TCVAR:TARGET:素材購入済み
	RETURN 0
RETURN 1
