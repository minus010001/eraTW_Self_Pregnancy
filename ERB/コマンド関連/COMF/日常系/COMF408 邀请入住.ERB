;-------------------------------------------------
;願掛け
;日常系コマンド、レベル1
;-------------------------------------------------
@COM408
#DIM C_ID
PRINTFORM 当前住在
SETCOLOR 219,90,107
SIF CFLAG:MASTER:現在位置 > P60大客厅
	PRINTFORM {CFLAG:MASTER:現在位置-60}号房-
PRINTFORM %NAME_FROM_PLACE(CFLAG:MASTER:現在位置)%
RESETCOLOR
PRINTFORM 的角色,
SIF CFLAG:MASTER:初期位置 == CFLAG:MASTER:現在位置
	PRINTFORM %CALLNAME:MASTER%也住在这里,
PRINTFORML 点击可以\@CFLAG:MASTER:現在位置==P59大厨房 ? 释放她们 #让她们搬出去\@...

FOR LOCAL,1,CHARANUM
	SIF CFLAG:LOCAL:神社在住 == CFLAG:MASTER:現在位置
		PRINTFORMLC [{LOCAL,3}] %CALLNAME:LOCAL,12,LEFT%
NEXT
PRINTL
DRAWLINEFORM －
;SIF TALENT:MASTER:魔王
;	PRINTFORML 尊敬的魔王大人,邀请谁呢...
FOR LOCAL,1,CHARANUM
	SIF !CFLAG:LOCAL:出禁 && !TALENT:LOCAL:路人 && !(CFLAG:LOCAL:神社在住 == CFLAG:MASTER:現在位置)
		PRINTFORMLC [{LOCAL,3}] %CALLNAME:LOCAL,12,LEFT%
NEXT
PRINTL 
DRAWLINEFORM － 
SIF CFLAG:MASTER:初期位置 != CFLAG:MASTER:現在位置
	PRINTFORMLC [200] - 我也要住在这里
PRINTFORMLC [233] - 还是算了
INPUT
SETCOLOR 219,90,10
IF RESULT == 200
	CFLAG:MASTER:初期位置 = CFLAG:MASTER:現在位置
	PRINTFORML 把家搬到了{CFLAG:MASTER:現在位置}%NAME_FROM_PLACE(CFLAG:MASTER:現在位置)%...
	SIF CFLAG:MASTER:初期位置 == P60大客厅
		PRINTFORML 是大卧室的床不够软吗?居然要睡大厅!!!
ENDIF

IF !INRANGE(RESULT,1,CHARANUM)
	RESETCOLOR
	RETURN -1
ELSE
	C_ID = RESULT
	IF CFLAG:C_ID:神社在住 == CFLAG:MASTER:現在位置
		IF CFLAG:MASTER:現在位置 == SUMIKOMI_ROOM
			CALL HOME_DATA_INITIALIZE(C_ID)
			Setting_CFLAG:C_ID:后宫居住标记 = 0
			PRINTFORML %CALLNAME:C_ID,10,RIGHT%离开了{CFLAG:MASTER:現在位置}%NAME_FROM_PLACE(CFLAG:MASTER:現在位置)%...
		ELSE
			CALL HOME_DATA_INITIALIZE(C_ID, 1)
			PRINTFORML %CALLNAME:C_ID,10,RIGHT%离开了{CFLAG:MASTER:現在位置}%NAME_FROM_PLACE(CFLAG:MASTER:現在位置)%...
		ENDIF
	ELSE

			IF !CFLAG:C_ID:睡眠
				CFLAG:C_ID:現在位置 = CFLAG:MASTER:現在位置
				PRINTFORML 受到了邀请,%CALLNAME:C_ID%很高兴就赶来了...
			ENDIF
			CFLAG:C_ID:神社在住 = CFLAG:MASTER:現在位置
			CFLAG:C_ID:初期位置 = CFLAG:MASTER:現在位置
			PRINTFORML %CALLNAME:C_ID,10,RIGHT%把家搬到了{CFLAG:MASTER:現在位置}%NAME_FROM_PLACE(CFLAG:MASTER:現在位置)%...
			SIF CFLAG:MASTER:現在位置 == P60大客厅
				PRINTFORML 居然忍心让%CALLNAME:C_ID%睡大厅???你这个人渣!!!

	ENDIF
ENDIF
RESETCOLOR
RESTART


@COM_ABLE408
#LOCALSIZE 1
#LOCALSSIZE 1
SIF !TFLAG:100
	RETURN 0
SIF !AT_HOME(MASTER)
	RETURN 0
;SIF !INRANGE(CFLAG:MASTER:現在位置, 58, 72)
;	RETURN 0
SIF !GROUPMATCH(CFLAG:MASTER:現在位置,P58大主卧,P60大客厅,P61地の間,P62水の間,P63火の間,P64风の間,P65光の間,P66暗の間) && !(HOUGONG && FLAG:住宿人物 && CFLAG:MASTER:現在位置 == SUMIKOMI_ROOM)
	RETURN 0
SIF CFLAG:MASTER:诶嘿嘿
	RETURN 0
RETURN 1
