@特殊ソース処理
;圧抑、抵抗持ちには、抑鬱ソース追加
SIF TALENT:自己愛 < 0 || TALENT:抵抗
	SOURCE:鬱屈 += 1000

;贞操重视持ちで恋慕、親愛持ち以外には常に反発のソースを追加する（性交系のみ）
SIF TALENT:貞操 > 0 && !TALENT:恋慕
	SOURCE:反感 += 1000

;処女だった場合は、苦痛のソースと反発のソースを追加する
IF TALENT:処女 == 1
	SOURCE:苦痛 += 2000
	SOURCE:反感 += 3000 / ((TCVAR:TARGET:Counter行動 == 41 || TCVAR:TARGET:Counter行動 == 43) ? 10 # 1)
ENDIF

;再生処女だった場合は、苦痛のソースと反発のソースを追加する
IF TALENT:処女 == 2
	SOURCE:苦痛 += 500
	SOURCE:反感 += 1000 / ((TCVAR:TARGET:Counter行動 == 41 || TCVAR:TARGET:Counter行動 == 43) ? 10 # 1)
ENDIF