@DAILY_CONDITION_EV2
;21/06/25,有意義な射し方した時夢精イベント起こらないように変更
IF !RAND:10 && TALENT:MASTER:濃厚精液 && HAS_PENIS(MASTER) && BASE:MASTER:精力 == MAXBASE:MASTER:精力;←ここに発生条件を入れよう　例として30分の1にしてるけど随意変えてね
	CALL DAILY_EV2
	RETURN 1
ELSE
	RETURN -1
ENDIF

@DAILY_EV2
#DIM CHARA
PRINTFORML 半夜、%CALLNAME:MASTER%在幸福的感覺中醒了
PRINTFORMW 总感觉胯下温热的……
PRINTFORML ！！！！　糟了！是夢遺！
PRINTFORMW 如果能更有意義的射出去就好了…　%CALLNAME:MASTER%深感後悔
PRINTL 
FLAG:71 = 0
TALENT:MASTER:120 = 0
BASE:MASTER:精力 -= 500
EXP:MASTER:射精経験 ++
IF RAND:10 == 0 && ITEM:奇跡之石 == 0
	PRINTFORML 气温、风向、湿度、大气压、曜日、月龄、行星排列等各种条件奇迹般组合的结果
	FONTBOLD
	PRINTFORM %CALLNAME:MASTER%射出的精液结晶化了
	CALL COLORMESSAGE("独占爱欲的奇迹之石", C_D_PINK, 0, 1)
	PRINTFORMW 生成了！
	FONTREGULAR
	CALL GET_ITEM("奇跡之石", 1, 1)
	RETURN 1
ENDIF
CALL FISHER_YATES_SHAFFLE(CHARANUM)
VARSET LOCAL
FOR LOCAL, 1, CHARANUM
	CHARA = SHAFFLE_ARRAY:LOCAL
	;対象が你
	SIF CHARA == MASTER
		CONTINUE
	;でてこない
	SIF CFLAG:CHARA:出禁
		CONTINUE
	;拠点角色ではない
	SIF !CFLAG:CHARA:神社在住
		CONTINUE
	;同じエリア内に住んでいない
	SIF MAP_AREASELECT(MAIN_MAP, CFLAG:MASTER:初期位置) != MAP_AREASELECT(MAIN_MAP, CFLAG:CHARA:初期位置)
		CONTINUE
	LOCAL:1 = CHARA
	BREAK
NEXT
SIF LOCAL:1 <= 0
	RETURN

CALL KOJO_MESSAGE_SEND("DAILY", 2, LOCAL:1)

@DAILY_MESSAGE_2(ARG, ARG:1, ARG:2, ARG:3, ARGS:1, ARGS:2, ARGS:3)
PRINTFORMDL %CALLNAME:MASTER%洗着被夢遺弄髒的衣服時%CALLNAME:ARG%飛過來了！
PRINTFORMDW %CALLNAME:ARG%抗议说不要把這種東西和其他要洗的衣服一起洗、從%CALLNAME:MASTER%的手中搶走了內衣…
JUEL:ARG:11 += 1000
PRINTFORMDW %CALLNAME:ARG%　欲情之珠＋1000
PRINTL 
RETURN 1

