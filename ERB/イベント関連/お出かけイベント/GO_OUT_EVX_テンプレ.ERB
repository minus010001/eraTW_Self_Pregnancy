;イベント発生判定。EVXのXを一括置換で空いてる数字に変えてください。
@GO_OUT_CONDITION_EVX
IF !RAND:10
	CALL GO_OUT_EVX
ELSE
	;イベント不成立ならRETURN 0
	RETURN 0
ENDIF

;イベント本体。最後はRETURN 1でお願いします。
@GO_OUT_EVX
RETURN 1

[SKIPSTART]
※補足事項
・発生タイミングは時止め無しの拠点帰還時、一日一回
・CFLAG:現在位置は移動直前のまま
・FLAG:约会的对象に约会的对象の角色番号、0なら你一人
・角色変数を使用場合は角色番号の指定が必要
・お出かけイベントの成否に係わらず、この次に约会後イベントも発生機会あります

経験変動処理の後に運動ののでEXP加算が次ターンに持ち越されます
この関数内で例えばMASTERの射精経験を3足す時は
EXP:MASTER:射精経験 +=3
TCVAR:MASTER:射精経験 += 3
PRINTFORML 射精経験 +3(%CALLNAME:MASTER%)
こうするとスマートに見えます
[SKIPEND]
