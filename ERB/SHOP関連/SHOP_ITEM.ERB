;----------------------------------------------------------------------
;SHOPの通信販売
;----------------------------------------------------------------------
@ITEM_BUY(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC TYPE
#DIM DYNAMIC SELECT_ITEM
#DIM DYNAMIC INPUT_LOOP_LINE
;購入条件設定
FOR LOCAL, 0, 1000
	CALL ITEMDATA(LOCAL, "SALES")
	ITEMSALES:LOCAL = RESULT
NEXT
DRAWLINE
;CALL LIST_ITEM("全て")
;DRAWLINE
;とりあえず無条件に全部表示にしたがTYPE指定も可能
PRINTFORML 请选择要购买的道具 所持金:%金額表示(MONEY)%
DRAWLINE
INPUT_LOOP_LINE = LINECOUNT
$INPUT_LOOP
CLEARLINE LINECOUNT - INPUT_LOOP_LINE
CALL SHOW_SHOPPING("通信販売", TYPE)
DRAWLINE
IF MAGIC_DLC
	PRINTL [990] - 全部显示　[991] - 調教道具　[992] - 消耗品　[993] - 日用品　[994] - 食材　[995] - 酒　[996] - 素質　[997] - 魔法
ELSE
	PRINTL [990] - 全部显示　[991] - 调教道具　[992] - 消耗品　[993] - 日用品　[994] - 食材　[995] - 酒　[996] - 素质
ENDIF
PRINT [999] - 返回
INPUT

;返回
IF RESULT == 999
	RETURN
ELSEIF INRANGE(RESULT, 990, 996+MAGIC_DLC)
	SELECTCASE RESULT
	CASE 990
		TYPE = 
	CASE 991
		TYPE = 調教
	CASE 992
		TYPE = 消耗品
	CASE 993
		TYPE = 日用品
	CASE 994
		TYPE = 食材
	CASE 995
		TYPE = 酒
	CASE 996
		TYPE = 素質
	CASE 997
		TYPE = 魔法
	ENDSELECT
	GOTO INPUT_LOOP
ELSEIF INRANGE(RESULT, 0, 1000)
	SELECT_ITEM = RESULT
	SELECTCASE ITEMSTOCK(SELECT_ITEM)
	CASE 1, 4
		PRINTW 无效输入
	CASE 2
		PRINTW 卖完了
	CASE 3
		PRINTW 所持金不足
	CASE 5
		PRINTW 已经有足够多了
	CASEELSE
		CALL ITEMDATA(SELECT_ITEM, "購入")
		FOR LOCAL, 0, 1000
			CALL ITEMDATA(LOCAL, "SALES")
			ITEMSALES:LOCAL = RESULT
		NEXT
	ENDSELECT
	GOTO INPUT_LOOP
ELSE
	GOTO INPUT_LOOP
ENDIF

;----------------------------------------------------------------------
;基本のお店メニュー
;----------------------------------------------------------------------
@SHOW_SHOPPING(販売店, TYPE, 割引)
#DIMS 販売店
#DIMS TYPE
#DIM 割引
#DIM 表示数
#DIM 表示行
#DIM 値段
表示数 = 0
表示行 = 0
FOR LOCAL, 0 , 1000
	CALL ITEMDATA(LOCAL, "SHOP:" + 販売店)
	IF RESULT
		IF TYPE != ""
			CALL ITEMDATA(LOCAL, "TYPE:" + TYPE)
		ELSE
			RESULT = 1
		ENDIF
		IF RESULT
			値段 = ITEMPRICE:LOCAL * (100 - 割引) / 100
			SELECTCASE ITEMSTOCK(LOCAL, 値段)
			CASE 1
				LOCALS:1 = 不存在
				CONTINUE
			CASE 2
				LOCALS:1 = 卖完
				SETCOLOR C_GRAY
			CASE 3
				LOCALS:1 = %金額表示(値段)%
				SETCOLOR C_GRAY
			CASE 4
				LOCALS:1 = 封印
				CONTINUE
			CASE 5
				LOCALS:1 = 已满
				SETCOLOR C_GRAY
			CASEELSE
				LOCALS:1 = %金額表示(値段)%
			ENDSELECT
			LOCALS = [{LOCAL,3}] %ITEMNAME_TR(LOCAL)%
			PRINTFORM %LOCALS, 24, LEFT% %LOCALS:1, 12% 
			RESETCOLOR
			表示数 ++
			IF 表示数 % 3 == 0
				PRINTL 
				表示行 ++
			ENDIF
		ENDIF
	ENDIF
NEXT
IF 表示数 % 3 != 0
	PRINTL 
	表示行 ++
ENDIF
RETURN 表示行

;---------------------------------------------------------------------
;ITEM所持数列表表示関数
;---------------------------------------------------------------------
@LIST_ITEM(TYPE = "全て")
#DIMS TYPE
#DIM 表示数
;custom code
#DIM PADDING = 32 ;tweak if necessary in translated, make sure to merge logging branch afterwards

SELECTCASE TYPE
CASE "全て"
	CALL LIST_ITEM("調教")
	CALL LIST_ITEM("日用品")
	CALL LIST_ITEM("素質")
	CALL LIST_ITEM("体質変化")
	CALL LIST_ITEM("調合物")
	CALL LIST_ITEM("弾幕")
	CALL LIST_ITEM("消耗品")
	CALL LIST_ITEM("酒")
	CALL LIST_ITEM("食材")
	CALL LIST_ITEM("野菜")
	CALL LIST_ITEM("お肉")
	CALL LIST_ITEM("巧克力")
	CALL LIST_ITEM("回復")
	CALL LIST_ITEM("園芸用品")
	CALL LIST_ITEM("素材")
	CALL LIST_ITEM("衣装")
	CALL LIST_ITEM("書物")
	CALL LIST_ITEM("特殊")
	CALL LIST_ITEM(OPR_其他)
	SIF MAGIC_DLC
		CALL LIST_ITEM("魔法")
	
	;logging custom code, show current stock of lumber
	CALL LIST_ITEM("製材")
;logging custom code
CASE "製材"
	;logging custom code, show current stock of lumber
	表示数 = 0
	FOR LOCAL, 1, 50
		IF LOG_SALES:LOCAL
			PRINTFORM %@"%WOOD_NAME(LOCAL)%({LOG_SALES:LOCAL})", PADDING, LEFT%
			表示数 ++
			SIF 表示数 % 4 == 0
				PRINTL 
		ENDIF
	NEXT
	SIF 表示数 % 4 != 0
		PRINTL 
CASEELSE
	CALL QOL_LIST_ITEM_HTML(TYPE) ;qol custom code
	RETURN
	;全部回す
	表示数 = 0
	FOR LOCAL, 0, 1000
		CALL ITEMDATA(LOCAL, "TYPE:" + TYPE)
		IF RESULT && ITEM:LOCAL
			;custom code, formatting printing
			LOCALS = %ITEMNAME_TR(LOCAL)%({ITEM:LOCAL})
			IF STRLENS(LOCALS) > PADDING
				IF 表示数 % 3 == 0 && 表示数 % 4 != 0
					表示数 ++
					PRINTL
				ENDIF
				PRINTFORM %LOCALS, PADDING*2, LEFT%
				;SIF 表示数 % 4 != 0
					表示数 ++
			ELSE
				PRINTFORM %LOCALS, PADDING, LEFT%
			ENDIF
			;-----------
			表示数 ++
			SIF 表示数 % 4 == 0
				PRINTL 
		ENDIF
	NEXT
	SIF 表示数 % 4 != 0
		PRINTL 
ENDSELECT

;----------------------------------------------------------------------
;買う前に最終チェックをするもの
;ITEMDATAから呼ばれる
;購入後どうなるかは道具次第（主に素質）
;----------------------------------------------------------------------
@SHOP_ASK(ARG, 割引)
#DIM 割引
#DIM price
;Qol Custom code, prints descriptions of items
CALL PRINT_ITEM_DESC(ARG)
PRINTFORML 要购入%ITEMNAME_TR(ARG)%吗？
CALL ASK_YN
IF !RESULT
	IF ITEMPRICE:ARG * (100 - 割引) / 100 <= MONEY
		price = ITEMPRICE:ARG * (100 - 割引) / 100
		MONEY -= price
		PRINTFORMW 花费%金額表示(price)%购入了%ITEMNAME_TR(ARG)%
	ELSE
		PRINTFORMW 金钱不足…
		RETURN 2
	ENDIF
ENDIF
RETURN RESULT

;--------------------------------------------------------------
;道具まとめ買い関数
;任意の个数で購入できるものはこれ
;乌冬麺ちゃんからまとめ買いしたかったので独立関数化
;必ず先に@ITEMSTOCKで判定して購入可能な道具のみで呼び出すこと
;単体購入専用だろうがまとめ買い可能だろうが購入可不可の判定は必要なので先に共通で行って下さい
;ARG：購入対象道具
;買ったら値段を返す
;--------------------------------------------------------------
@ITEM_MATOMEGAI(ARG, 割引)
#DIM 割引
#DIM 値段
;Qol Custom code, prints descriptions of items
CALL PRINT_ITEM_DESC(ARG)
PRINTFORM 要购买多少%ITEMNAME_TR(ARG)%？(在库：{ITEMSALES:ARG}　所持数：{ITEM:ARG})
PRINTL 
値段 = ITEMPRICE:ARG * (100 - 割引) / 100
;金欠、道具数の超過はあとで合わせる
SIF ITEMSALES:ARG
	PRINTL [ 1] １　　
SIF MIN(ITEMSALES:ARG, 所持数制限 - ITEM:ARG) >= 5
	PRINTL [ 5] ５　　
SIF MIN(ITEMSALES:ARG, 所持数制限 - ITEM:ARG) >= 10
	PRINTL [10] １０　
SIF MIN(ITEMSALES:ARG, 所持数制限 - ITEM:ARG) >= 20
	PRINTL [20] ２０　
SIF MIN(ITEMSALES:ARG, 所持数制限 - ITEM:ARG) >= 50
	PRINTL [50] ５０　
PRINTFORML [{MIN(ITEMSALES:ARG, 所持数制限 - ITEM:ARG), 2}] 全部
PRINTL [ 0] 放弃
INPUT
IF INRANGE(RESULT, 1, ITEMSALES:ARG)
	IF 値段 * RESULT > MONEY
		PRINTFORMW 金钱不足…
		RETURN 0
	ELSEIF ITEM:ARG + RESULT > 所持数制限
		PRINTFORMW 即使买下了也拿不下！
		RETURN 0
	ENDIF
	MONEY -= 値段 * RESULT
	ITEMSALES:ARG -= RESULT
	ITEM:ARG += RESULT
	PRINTFORMW 花费了%金額表示(値段 * RESULT)%、购入了{RESULT}个%ITEMNAME_TR(ARG)%
	RETURN 値段
ENDIF
RETURN 0

;----------------------------------------------------------------------
;道具をカートンで買う関数
;購入数が固定のものはこれ,
;1个でも最終チェックを飛ばして買うのに使用
;ARG：購入対象道具
;カートン商品は所持数制限を超えた場合でも余剰分を省いて購入できる
;品物を購入すると値段を返す、なんやかんやで買ってないと0
;----------------------------------------------------------------------
@ITEM_CARTON(ARG, 値段, 个数, 割引)
#DIM 个数
#DIM 値段
#DIM 割引
IF !个数
	个数 = 1
ELSEIF ITEM:ARG + 个数 > 所持数制限
	个数 = 所持数制限 - ITEM:ARG
ENDIF
SIF !値段
	値段 = ITEMPRICE:ARG * 个数
値段 = 値段 * (100 - 割引) / 100

IF 値段 > MONEY
	PRINTW 金钱不足…
ELSEIF ITEMSALES:ARG == -1
	PRINTW 卖完了
ELSEIF ITEM:ARG >= 所持数制限
	PRINTW 已经有很多了
ELSE
	MONEY -= 値段
	ITEMSALES:ARG -= 个数
	ITEM:ARG += 个数
	PRINTFORMW 花费%金額表示(値段)%购入%ITEMNAME_TR(ARG)%{个数}个
	RETURN 値段
ENDIF
RETURN 0

;----------------------------------------------------------------------
;説明文付きの道具陳列
;----------------------------------------------------------------------
@ITEM_SHOPPING(ARG, 説明文, 割引)
#DIMS 説明文
#DIM 割引
#DIM 値段
値段 = ITEMPRICE:ARG * (100 - 割引) / 100
;在庫確認
CALL ITEMDATA(ARG, "SALES")
ITEMSALES:ARG = RESULT
LOCALS = %金額表示(値段)%
IF RESULT <= 0
	SETCOLOR C_GRAY
	LOCALS = 售罄
ENDIF
SIF 値段 > MONEY
	SETCOLOR C_GRAY
IF ITEM:ARG >= 所持数制限
	SETCOLOR C_GRAY
	LOCALS = 拿不下了
ENDIF
LOCALS = [{ARG, 3}] %ITEMNAME_TR(ARG), 22, LEFT% %金額表示(値段)%

;LOCALS = [{ARG, 3}] %ITEMNAME_TR(ARG), 22, LEFT% %LOCALS%

PRINTFORML %LOCALS, 48, LEFT%%説明文%
RESETCOLOR

;----------------------------------------------------------------------
;カートン商品等の特殊な道具陳列
;----------------------------------------------------------------------
@EX_SHOPPING(ARG, 商品名, 説明文, 値段, 个数, 表示番号)
#DIMS 商品名
#DIMS 説明文
#DIM 割引
#DIM 値段
#DIM 个数
#DIM 表示番号
SIF !値段
	値段 = ITEMPRICE:ARG * 个数
SIF !表示番号
	表示番号 = ARG
LOCALS = [{表示番号, 3}] %商品名, 22, LEFT% %金額表示(値段)%
CALL ITEMDATA(ARG, "SALES")
ITEMSALES:ARG = RESULT
SIF RESULT <= 0
	SETCOLOR C_GRAY
SIF 値段 > MONEY
	SETCOLOR C_GRAY
SIF ITEM:ARG >= 所持数制限
	SETCOLOR C_GRAY
PRINTFORML %LOCALS,48,LEFT%%説明文%
RESETCOLOR

;------------------------------------------------------------------------
;一日の終わりの在庫補充
;	平价食材は+10、お店には最大で20が保管されます
;	优质食材は+7、お店の最大在庫は15です
;	高级食材は+5、お店の最大在庫は10です
;----------------------------------------------------------------------
@SHOP_EVENTTURNEND
IF !MAGIC_DLC	
	CALL お店に入荷(GETNUM(ITEM, "平价食材"), 10, 20)
	CALL お店に入荷(GETNUM(ITEM, "优质食材"), 7, 15)
	CALL お店に入荷(GETNUM(ITEM, "高级食材"), 5, 10)
	CALL お店に入荷(GETNUM(ITEM, "甜味料"), 5, 10)
	CALL お店に入荷(GETNUM(ITEM, "獣肉"), 5, 10)
	CALL お店に入荷(GETNUM(ITEM, "鳥肉"), 5, 10)
	CALL お店に入荷(GETNUM(ITEM, "卵"), 5, 10)
	CALL お店に入荷(GETNUM(ITEM, "肥料"), 1, 3)
ELSE
	CALL お店に入荷(GETNUM(ITEM, "平价食材"), RANDSF(10,10), 200)
	CALL お店に入荷(GETNUM(ITEM, "优质食材"), RANDSF(7,10), 150)
	CALL お店に入荷(GETNUM(ITEM, "高级食材"), RANDSF(5,10), 100)
	CALL お店に入荷(GETNUM(ITEM, "甜味料"), RANDSF(5,10), 100)
	CALL お店に入荷(GETNUM(ITEM, "獣肉"), RANDSF(5,10), 100)
	CALL お店に入荷(GETNUM(ITEM, "鳥肉"), RANDSF(5,10), 100)
	CALL お店に入荷(GETNUM(ITEM, "卵"), RANDSF(5,10), 100)
	CALL お店に入荷(GETNUM(ITEM, "肥料"), RANDSF(1,10), 30)
ENDIF
;酒屋は独自に入荷
CALL 酒屋の在庫

; 苗は1種1日1つ入荷、最大在庫も3です
;	苗を粗末にするとゆうかりんに怒られますよ？
FOR LOCAL, 0, 1000
	IF STRCOUNT(ITEMNAME:LOCAL, "苗")
		IF !MAGIC_DLC
			CALL お店に入荷(LOCAL, 1, 3)
		ELSE
			CALL お店に入荷(LOCAL, RANDSF(2,5), 20)
		ENDIF
	ELSE
		;ついでに八百屋,入荷しないこともある
		CALL ITEMDATA(LOCAL, "SHOP:八百屋")
		IF RESULT && !MAGIC_DLC
			CALL お店に入荷(LOCAL, RAND:2, 5)
		ELSEIF RESULT && MAGIC_DLC
			CALL お店に入荷(LOCAL, RANDSF(4,5), 50)
		ENDIF
	ENDIF
NEXT


; RETURN 1 して是けません

;----------------------------------------------------------------------
; お店に入荷 関数
;	EVENTTURNENDでの入荷と 酒屋での売却の時に使用
;----------------------------------------------------------------------
@お店に入荷(ARG, 入荷量, 最大在庫)
#DIM 入荷量
#DIM 最大在庫
SELECTCASE ITEMSALES:ARG
CASE IS >= 最大在庫
	; 過剰在庫のときは在庫入荷しない
CASE -1, 0
	ITEMSALES:ARG = 入荷量
CASEELSE
	ITEMSALES:ARG = MIN(ITEMSALES:ARG + 入荷量, 最大在庫)
ENDSELECT
