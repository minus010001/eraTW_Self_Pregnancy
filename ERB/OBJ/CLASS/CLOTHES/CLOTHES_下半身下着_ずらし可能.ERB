;要素名→ID変換用関数の存否確認用関数
;変換可能（IDと要素名の関係が常に変化しない）なら設置
;状況によって名前が変わるようなのは不可能
;WEAPONIDは変換可能
@EXIST_下半身内衣_ずらし可能_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;要素名→ID変換用関数
;必要なら各オブジェクトで同様に作成のこと
@下半身内衣_ずらし可能_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する変数名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GETMETH_STR(, "下半身内衣_ずらし可能", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



;存否関数
;F関数はTRYCALL系が使えないので通常関数で存否配列を作っちゃう
@EXIST_下半身内衣_ずらし可能0
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@下半身内衣_ずらし可能0(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	;STR型の一時変数を定義する命令
	CALLF MAKE_STR(V_NAME, "没穿内裤")
CASE "能穿"
;没穿内裤を履くとは
	SIF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || TALENT:ARG:炮友 || ABL:ARG:露出癖 >= 8) && !RAND:5
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



@EXIST_下半身内衣_ずらし可能1
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "四角裤")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "男式")
CASE "半透明"
	CALLF MAKE_INT(V_NAME, 1)
CASE "換装可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_Ｔ恤衫]])
ENDSELECT



@EXIST_下半身内衣_ずらし可能2

@下半身内衣_ずらし可能2(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "灯笼裤")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "构造宽松的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF !RAND:(1 + (2 * 勝負内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能3
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "小熊胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "印着吉祥物的可爱")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:無知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:無知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能4
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "波点胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "孩子气的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能5
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能5(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "條紋胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "凸显臀部曲线的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF !RAND:(1 + (2 * 勝負内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能6
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "褶边胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "装饰着可爱褶边的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能7
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "純色胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "毫不做作的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF !RAND:(1 + (2 * 勝負内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能10
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "低腰胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "设计简洁的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !RAND:(1 + (2 * 勝負内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能11
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能11(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "低腰緊身胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "仿佛能看到股沟的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF !RAND:(2 - MIN(TALENT:ARG:恋慕,1)) ;marriage custom code
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能12
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能12(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "高開叉胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "剪裁大胆的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF !RAND:(2 - MIN(TALENT:ARG:恋慕,1)) ;marriage custom code
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_背带裙内衣]])
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_下半身内衣_ずらし可能13
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能13(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "白色蕾丝胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "清纯却又隐约透明的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !(TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	SELECTCASE (RAND:2 + 勝負内衣判定(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_背带裙内衣]])
		CASE 1
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_白色蕾丝胸罩]])
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
ENDSELECT



@EXIST_下半身内衣_ずらし可能14
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能14(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	;STR型の一時変数を定義する命令
	CALLF MAKE_STR(V_NAME, "黑色蕾丝胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "充满成熟韵味的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !(TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	SELECTCASE (RAND:2 + 勝負内衣判定(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_背带裙内衣]])
		CASE 1
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_黑色蕾丝胸罩]])
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
ENDSELECT



@EXIST_下半身内衣_ずらし可能15
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能15(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "Ｔ字褲")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "露出大片屁屁的大胆")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	SIF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_背带裙内衣]])
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	SIF TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
ENDSELECT



@EXIST_下半身内衣_ずらし可能16
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能16(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "繩系胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "系在腰上的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && !RAND:(2 - MIN(TALENT:ARG:恋慕,1)) ;marriage custom code
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_背带裙内衣]])
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	SIF TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
	CALLF MAKE_STR(V_NAME, "解禁：恋慕")
ENDSELECT



@EXIST_下半身内衣_ずらし可能17
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能17(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "皮革胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "缝着拉链的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	SIF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && ABL:ARG:施虐属性 > 4 && !RAND:3
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	IF !RAND:2
		CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_皮带]])
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF

CASE "收藏表示可能"
	SIF TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
	IF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && ABL:ARG:施虐属性 <= 4
		CALLF MAKE_STR(V_NAME, "解禁：施虐属性5")
	ELSE
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能18
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能18(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, @"%UNIQUE_UNDERWARE_BOTTOM(ARG)%")
CASE "描写"
	CALLF MAKE_STR(V_NAME, @"%UNIQUE_UNDERWARE_ADJ(ARG)%")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_TRANSPARENCY_FRONT(ARG))
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_TRANSPARENCY_BACK(ARG))
CASE "能穿"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_ABLE(ARG))
CASE "関連内衣"
	;固有ブラ
	IF GETMETH_INT(ARG, "キャラデータ", ARG, "固有上半身内衣")
		CALLF MAKE_INT(V_NAME, 13)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
;とりあえず今回は無条件で投げるけど本来はキャラデータ関数に渡して戻り値を見るべき
;そうすると着用条件の有無まで設定できるが、再び全角色分の設定が必要になる
;そこまでするの大変でしょ
CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
ENDSELECT



@EXIST_下半身内衣_ずらし可能19
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能19(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "三角胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "充满少女味的可爱")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
ENDSELECT



@EXIST_下半身内衣_ずらし可能20
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能20(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "超短胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "布料超少的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	SIF FLAG:セクシー胖次追加 && !RAND:(2 - MIN(TALENT:ARG:恋慕,1)) ;marriage custom code
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_背带裙内衣]])
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	SIF FLAG:セクシー胖次追加
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
ENDSELECT



@EXIST_下半身内衣_ずらし可能21
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能21(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "兜襠布")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "紧紧包住屁股的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	SIF FLAG:セクシー胖次追加 && !RAND:(3 + 勝負内衣判定(ARG))
		CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_裹胸]])

CASE "收藏表示可能"
	SIF FLAG:セクシー胖次追加
		CALLF MAKE_INT(V_NAME, 1)
CASE "收藏解禁条件"
ENDSELECT



@EXIST_下半身内衣_ずらし可能22
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能22(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "丁字褲")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "简直就是绳子的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_背带裙内衣]])
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	IF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知) && ABL:ARG:露出癖 >= 4
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	ELSEIF ABL:ARG:露出癖 < 4
		CALLF MAKE_STR(V_NAME, "解禁：露出癖4")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能23
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能23(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "透明胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "重要部位完全暴露的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	SELECTCASE (RAND:2 + 勝負内衣判定(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_背带裙内衣]])
		CASE 1
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_透明胸罩]])
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	IF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知) && ABL:ARG:露出癖 >= 4
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	ELSEIF ABL:ARG:露出癖 < 4
		CALLF MAKE_STR(V_NAME, "解禁：露出癖4")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能24
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能24(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "開放式胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "清楚暴露出耻丘的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(4 - (TALENT:ARG:恋慕 > 0) - (2 * 勝負内衣判定(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "関連内衣"
	SELECTCASE (RAND:2 + 勝負内衣判定(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_背带裙内衣]])
		CASE 1
			CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_露两点胸罩]])
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "收藏表示可能"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "收藏解禁条件"
	IF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知) && ABL:ARG:露出癖 >= 4
		CALLF MAKE_STR(V_NAME, "解禁：恋慕")
	ELSEIF ABL:ARG:露出癖 < 4
		CALLF MAKE_STR(V_NAME, "解禁：露出癖4")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能25
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能25(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "創口貼")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "沿着蜜缝轻轻贴上的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "能穿"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知) && !RAND:3
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_創口貼]])

CASE "收藏表示可能"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "收藏解禁条件"
	IF ABL:ARG:露出癖 < 4
		CALLF MAKE_STR(V_NAME, "解禁：露出癖4")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能26
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能26(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "拉链（屁股处可開）")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "屁股处可拉开的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "能穿"
	IF FLAG:セクシー胖次追加 && (TALENT:ARG:炮友 && ABL:ARG:露出癖 >= 5 || TALENT:ARG:無知) && !RAND:4
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)

CASE "收藏表示可能"
	IF !FLAG:セクシー胖次追加
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:炮友 && ABL:ARG:露出癖 >= 5 || TALENT:ARG:無知)
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "收藏解禁条件"
	IF TALENT:ARG:無知
	ELSEIF TALENT:ARG:炮友 && ABL:ARG:露出癖 >= 5
	ELSEIF TALENT:ARG:炮友
		CALLF MAKE_STR(V_NAME, "解禁：露出癖5")
	ELSEIF ABL:ARG:露出癖 >= 5
		CALLF MAKE_STR(V_NAME, "解禁：炮友")
	ENDIF
ENDSELECT



@EXIST_下半身内衣_ずらし可能27
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能27(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "貞操帯")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "挂着锁的金属")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_贞操带胸罩]])
ENDSELECT



@EXIST_下半身内衣_ずらし可能28
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能28(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "触手胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "内侧有什么在蠢动着的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_触手胸罩]])
ENDSELECT



@EXIST_下半身内衣_ずらし可能29
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能29(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "振動棒胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "装着柱状怪东西的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT

@EXIST_下半身内衣_ずらし可能30
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能30(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "二穴振動棒胖次")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "两根棒子在胯下嗡嗡作响的")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT

@EXIST_下半身内衣_ずらし可能31
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能31(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "比基尼内裤")
;下半身内衣２の20
CASE "換装可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_比基尼内衣]])
ENDSELECT

@EXIST_下半身内衣_ずらし可能32
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能32(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "迷你比基尼内裤")
;下半身内衣２の21
CASE "換装可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, [[上半身内衣_はだけ不可_迷你比基尼内衣]])
ENDSELECT

;来自TOKI觉的剧本
@EXIST_下半身内衣_ずらし可能33
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能33(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "御札")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "紧密贴合饱满耻丘的博丽")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "関連内衣"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT

;-------------
;游泳补丁
;-------------
@EXIST_下半身内衣_ずらし可能34
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能34(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "泳裤")
CASE "描写"
	CALLF MAKE_STR(V_NAME, "男性")
CASE "換装可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_下半身内衣_ずらし可能35
#LOCALSIZE 1
#LOCALSSIZE 1

@下半身内衣_ずらし可能35(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, @"%UNIQUE_UNDERWARE_SWIMMING_BOTTOM(ARG)%")
CASE "描写"
	CALLF MAKE_STR(V_NAME, @"%UNIQUE_UNDERWARE_SWIMMING_ADJ(ARG)%")
CASE "半透明(前)"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_SWIMMING_TRANSPARENCY_FRONT(ARG))
CASE "半透明(後)"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_SWIMMING_TRANSPARENCY_BACK(ARG))
CASE "関連内衣"
	IF GETMETH_INT(ARG, "キャラデータ", ARG, "固有上半身泳衣")
		CALLF MAKE_INT(V_NAME, CLOTHES_ID("上半身内衣１", @"%UNIQUE_UNDERWARE_SWIMMING_TOP(ARG)%"))
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
ENDSELECT


@勝負内衣判定(ARG)
#FUNCTION
IF !TALENT:ARG:無知
	SIF (CFLAG:ARG:既成事實 & 合意_诶嘿嘿 || (TALENT:ARG:恋慕 > 0)) && CFLAG:ARG:翌日来訪
		RETURNF 1
	IF DAY:2 == 1
		SELECTCASE DAY:3
			CASE 14
				SIF (TALENT:ARG:恋慕 > 0)
					RETURNF 1
		ENDSELECT
	ELSEIF DAY:2 == 4
		SELECTCASE DAY:3
			CASE 14, 24, 25
				SIF (TALENT:ARG:恋慕 > 0)
					RETURNF 1
		ENDSELECT
	ENDIF
ENDIF
RETURNF 0

