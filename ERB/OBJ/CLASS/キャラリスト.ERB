;角色列表
;要素名→ID変換用関数の存否確認用関数
;変換可能（IDと要素名の関係が常に変化しない）なら設置
;状況によって名前が変わるようなのは不可能
@EXIST_角色列表_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;要素名→ID変換用関数
;必要なら各オブジェクトで同様に作成のこと
@角色列表_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する変数名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "角色列表", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



@EXIST_角色列表1
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@角色列表1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "現有的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "存在")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_角色列表2
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "主人公以外的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER || !CHECK_CHARA(ARG, "存在")
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT


@EXIST_角色列表3
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "陷落的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "陥落")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_角色列表4
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "纯洁交往的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "清い交際")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT





@EXIST_角色列表6
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "掌握着主角的把柄的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "掌握把柄")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_角色列表7
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "主角掌握着把柄的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "被掌握把柄")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_角色列表8
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表8(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "有反発刻印的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "反発刻印")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_角色列表9
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表9(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "有時姦刻印的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "時姦刻印")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_角色列表10
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "住处的角色列表")
CASE "判定"
	SIF CHECK_CHARA(ARG, "同室")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT


@EXIST_角色列表11
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表11(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "有口上的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "口上実装状況")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_角色列表12
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表12(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "怀孕中的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "妊娠")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_角色列表13
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表13(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "育儿中的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "育児中")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_角色列表14
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表14(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "有孩子的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "子持ち")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

;種族
@EXIST_角色列表22
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表22(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "巫女角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "巫女")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_角色列表23
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表23(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "妖精角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "妖精")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_角色列表24
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表24(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "神霊角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "神霊")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_角色列表25
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表25(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "付喪神角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "付喪神")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_角色列表26
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表26(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "鬼角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "鬼")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_角色列表27
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表27(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "天狗角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "天狗")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_角色列表28
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表28(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "蓬莱人角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "蓬莱人")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_角色列表29
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表29(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "妖怪角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "妖怪")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_角色列表30
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表30(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "魔法使角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "魔法使")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

;複合条件系


@EXIST_角色列表31
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@角色列表31(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "可换装的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
;換装ることができる
	IF ARG == MASTER
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF !CHECK_CHARA(ARG, "存在") || !CHECK_CHARA(ARG, "同室")
	ELSEIF FLAG:時間停止 == 1
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF 睡眠時間(ARG)
	ELSEIF CHECK_CHARA(ARG, "陥落")
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF CHECK_CHARA(ARG, "掌握把柄")
	ELSEIF CHECK_CHARA(ARG, "被掌握把柄")
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
	ENDIF
ENDSELECT



@EXIST_角色列表32
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表32(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "可以穿同款衣服的角色列表")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "掌握把柄")
	ELSEIF CHECK_CHARA(ARG, "陥落") || CHECK_CHARA(ARG, "被掌握把柄")
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
	ENDIF
ENDSELECT



@EXIST_角色列表33
#LOCALSIZE 1
#LOCALSSIZE 1

@角色列表33(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "TARGET列表")
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在") || !CHECK_CHARA(ARG, "同室")
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT
