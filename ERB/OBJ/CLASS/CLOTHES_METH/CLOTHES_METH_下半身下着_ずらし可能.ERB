
;オブジェクト本体
@F下半身内衣_ずらし可能0(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	;STR型の一時変数を定義する命令
	RETURNF "没穿内裤"
CASE "能穿"
;没穿内裤を履くとは
	SIF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || TALENT:ARG:炮友 || ABL:ARG:露出癖 >= 8) && !RAND:5
		RETURNF "1"
CASE "関連内衣"
	RETURNF "0"
ENDSELECT

@F下半身内衣_ずらし可能1(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "四角裤"
CASE "描写"
	RETURNF "男式"
CASE "半透明"
	RETURNF "1"
CASE "換装可能"
	RETURNF "1"
CASE "関連内衣"
	RETURNF @"{[[上半身内衣_はだけ不可_Ｔ恤衫]]}"
ENDSELECT


@F下半身内衣_ずらし可能2(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "灯笼裤"
CASE "描写"
	RETURNF "构造宽松的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	SIF !RAND:(1 + (2 * 勝負内衣判定(ARG)))
		RETURNF "1"
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	RETURNF "1"
ENDSELECT

@F下半身内衣_ずらし可能3(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "小熊胖次"
CASE "描写"
	RETURNF "印着吉祥物的可爱"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:無知
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:無知
		RETURNF "1"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能4(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "波点胖次"
CASE "描写"
	RETURNF "孩子气的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知
		RETURNF "1"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能5(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "條紋胖次"
CASE "描写"
	RETURNF "凸显臀部曲线的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	SIF !RAND:(1 + (2 * 勝負内衣判定(ARG)))
		RETURNF "1"
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	RETURNF "1"
ENDSELECT

@F下半身内衣_ずらし可能6(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "褶边胖次"
CASE "描写"
	RETURNF "装饰着可爱褶边的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知
		RETURNF "1"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能7(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "純色胖次"
CASE "描写"
	RETURNF "毫不做作的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSEIF !RAND:(1 + (2 * 勝負内衣判定(ARG)))
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	IF TALENT:ARG:小悪魔 || TALENT:ARG:炮友
	ELSE
		RETURNF "1"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能10(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "低腰胖次"
CASE "描写"
	RETURNF "设计简洁的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF !RAND:(1 + (2 * 勝負内衣判定(ARG)))
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	RETURNF "1"
ENDSELECT

@F下半身内衣_ずらし可能11(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "低腰緊身胖次"
CASE "描写"
	RETURNF "仿佛能看到股沟的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	SIF !RAND:(2 - MIN(TALENT:ARG:恋慕,1)) ;marriage custom code
		RETURNF "1"
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	RETURNF "1"
ENDSELECT

@F下半身内衣_ずらし可能12(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "高開叉胖次"
CASE "描写"
	RETURNF "剪裁大胆的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	SIF !RAND:(2 - MIN(TALENT:ARG:恋慕,1)) ;marriage custom code
		RETURNF "1"
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			RETURNF @"{[[上半身内衣_はだけ不可_背带裙内衣]]}"
		CASEELSE
			RETURNF "0"
	ENDSELECT

CASE "收藏表示可能"
	RETURNF "1"
ENDSELECT

@F下半身内衣_ずらし可能13(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "白色蕾丝胖次"
CASE "描写"
	RETURNF "清纯却又隐约透明的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF !(TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		RETURNF "0"
	ELSEIF !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	SELECTCASE (RAND:2 + 勝負内衣判定(ARG))
		CASE 2
			RETURNF @"{[[上半身内衣_はだけ不可_背带裙内衣]]}"
		CASE 1
			RETURNF @"{[[上半身内衣_はだけ不可_白色蕾丝胸罩]]}"
		CASEELSE
			RETURNF "0"
	ENDSELECT

CASE "收藏表示可能"
	IF !(TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
	ELSE
		RETURNF "1"
	ENDIF
CASE "收藏解禁条件"
	RETURNF "解禁：恋慕"
ENDSELECT

@F下半身内衣_ずらし可能14(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	;STR型の一時変数を定義する命令
	RETURNF "黑色蕾丝胖次"
CASE "描写"
	RETURNF "充满成熟韵味的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF !(TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		RETURNF "0"
	ELSEIF !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	SELECTCASE (RAND:2 + 勝負内衣判定(ARG))
		CASE 2
			RETURNF @"{[[上半身内衣_はだけ不可_背带裙内衣]]}"
		CASE 1
			RETURNF @"{[[上半身内衣_はだけ不可_黑色蕾丝胸罩]]}"
		CASEELSE
			RETURNF "0"
	ENDSELECT

CASE "收藏表示可能"
	IF !(TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
	ELSE
		RETURNF "1"
	ENDIF
CASE "收藏解禁条件"
	RETURNF "解禁：恋慕"
ENDSELECT

@F下半身内衣_ずらし可能15(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "Ｔ字褲"
CASE "描写"
	RETURNF "露出大片屁屁的大胆"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "0"
CASE "能穿"
	SIF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		RETURNF "1"
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			RETURNF @"{[[上半身内衣_はだけ不可_背带裙内衣]]}"
		CASEELSE
			RETURNF "0"
	ENDSELECT

CASE "收藏表示可能"
	SIF TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)
		RETURNF "1"
CASE "收藏解禁条件"
	RETURNF "解禁：恋慕"
ENDSELECT

@F下半身内衣_ずらし可能16(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "繩系胖次"
CASE "描写"
	RETURNF "系在腰上的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	SIF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)) && !RAND:(2 - MIN(TALENT:ARG:恋慕,1)) ;marriage custom code
		RETURNF "1"
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			RETURNF @"{[[上半身内衣_はだけ不可_背带裙内衣]]}"
		CASEELSE
			RETURNF "0"
	ENDSELECT

CASE "收藏表示可能"
	SIF TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0)
		RETURNF "1"
CASE "收藏解禁条件"
	RETURNF "解禁：恋慕"
ENDSELECT

@F下半身内衣_ずらし可能17(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "皮革胖次"
CASE "描写"
	RETURNF "缝着拉链的"
CASE "半透明(前)"
	RETURNF "0"
CASE "半透明(後)"
	RETURNF "0"
CASE "能穿"
	SIF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && ABL:ARG:施虐属性 > 4 && !RAND:3
		RETURNF "1"
CASE "関連内衣"
	IF !RAND:2
		RETURNF @"{[[上半身内衣_はだけ不可_皮带]]}"
	ELSE
		RETURNF "0"
	ENDIF

CASE "收藏表示可能"
	SIF TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友
		RETURNF "1"
CASE "收藏解禁条件"
	IF (TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && ABL:ARG:施虐属性 <= 4
		RETURNF "解禁：施虐属性5"
	ELSE
		RETURNF "解禁：恋慕"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能18(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF UNIQUE_UNDERWARE_BOTTOM(ARG)
CASE "描写"
	RETURNF UNIQUE_UNDERWARE_ADJ(ARG)
CASE "半透明(前)"
	RETURNF @"{UNIQUE_UNDERWARE_TRANSPARENCY_FRONT(ARG)}"
CASE "半透明(後)"
	RETURNF @"{UNIQUE_UNDERWARE_TRANSPARENCY_BACK(ARG)}"
CASE "能穿"
	RETURNF @"{UNIQUE_UNDERWARE_ABLE(ARG)}"
CASE "関連内衣"
	;固有ブラ
	IF GETMETH_INT(ARG, "キャラデータ", ARG, "固有上半身内衣")
		RETURNF "13"
	ELSE
		RETURNF "0"
	ENDIF
;とりあえず今回は無条件で投げるけど本来はキャラデータ関数に渡して戻り値を見るべき
;そうすると着用条件の有無まで設定できるが、再び全角色分の設定が必要になる
;そこまでするの大変でしょ
CASE "收藏表示可能"
	RETURNF "1"
CASE "收藏解禁条件"
ENDSELECT

@F下半身内衣_ずらし可能19(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "三角胖次"
CASE "描写"
	RETURNF "充满少女味的可爱"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	RETURNF "1"
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	RETURNF "1"
CASE "收藏解禁条件"
ENDSELECT

@F下半身内衣_ずらし可能20(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "超短胖次"
CASE "描写"
	RETURNF "布料超少的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	SIF FLAG:セクシー胖次追加 && !RAND:(2 - MIN(TALENT:ARG:恋慕,1)) ;marriage custom code
		RETURNF "1"
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			RETURNF @"{[[上半身内衣_はだけ不可_背带裙内衣]]}"
		CASEELSE
			RETURNF "0"
	ENDSELECT

CASE "收藏表示可能"
	SIF FLAG:セクシー胖次追加
		RETURNF "1"
CASE "收藏解禁条件"
ENDSELECT

@F下半身内衣_ずらし可能21(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "兜襠布"
CASE "描写"
	RETURNF "紧紧包住屁股的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "0"
CASE "能穿"
	SIF FLAG:セクシー胖次追加 && !RAND:(3 + 勝負内衣判定(ARG))
		RETURNF "1"
CASE "関連内衣"
	RETURNF @"{[[上半身内衣_はだけ不可_裹胸]]}"

CASE "收藏表示可能"
	SIF FLAG:セクシー胖次追加
		RETURNF "1"
CASE "收藏解禁条件"
ENDSELECT

@F下半身内衣_ずらし可能22(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "丁字褲"
CASE "描写"
	RETURNF "简直就是绳子的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "0"
CASE "能穿"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		RETURNF "0"
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		RETURNF "0"
	ELSEIF !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	SELECTCASE RAND:(1 + 勝負内衣判定(ARG))
		CASE 1
			RETURNF @"{[[上半身内衣_はだけ不可_背带裙内衣]]}"
		CASEELSE
			RETURNF "0"
	ENDSELECT

CASE "收藏表示可能"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		RETURNF "0"
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		RETURNF "0"
	ELSE
		RETURNF "1"
	ENDIF
CASE "收藏解禁条件"
	IF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知) && ABL:ARG:露出癖 >= 4
		RETURNF "解禁：恋慕"
	ELSEIF ABL:ARG:露出癖 < 4
		RETURNF "解禁：露出癖4"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能23(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "透明胖次"
CASE "描写"
	RETURNF "重要部位完全暴露的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		RETURNF "0"
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		RETURNF "0"
	ELSEIF !RAND:(MAX(3 - TALENT:ARG:恋慕 - 勝負内衣判定(ARG),1)) ;marriage custom code
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	SELECTCASE (RAND:2 + 勝負内衣判定(ARG))
		CASE 2
			RETURNF @"{[[上半身内衣_はだけ不可_背带裙内衣]]}"
		CASE 1
			RETURNF @"{[[上半身内衣_はだけ不可_透明胸罩]]}"
		CASEELSE
			RETURNF "0"
	ENDSELECT

CASE "收藏表示可能"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		RETURNF "0"
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		RETURNF "0"
	ELSE
		RETURNF "1"
	ENDIF
CASE "收藏解禁条件"
	IF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知) && ABL:ARG:露出癖 >= 4
		RETURNF "解禁：恋慕"
	ELSEIF ABL:ARG:露出癖 < 4
		RETURNF "解禁：露出癖4"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能24(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "開放式胖次"
CASE "描写"
	RETURNF "清楚暴露出耻丘的"
CASE "半透明(前)"
	RETURNF "0"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		RETURNF "0"
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		RETURNF "0"
	ELSEIF !RAND:(MAX(4 - TALENT:ARG:恋慕 - (2 * 勝負内衣判定(ARG)),1)) ;marriage custom code
		RETURNF "1"
	ENDIF
CASE "関連内衣"
	SELECTCASE (RAND:2 + 勝負内衣判定(ARG))
		CASE 2
			RETURNF @"{[[上半身内衣_はだけ不可_背带裙内衣]]}"
		CASE 1
			RETURNF @"{[[上半身内衣_はだけ不可_露两点胸罩]]}"
		CASEELSE
			RETURNF "0"
	ENDSELECT

CASE "收藏表示可能"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		RETURNF "0"
	ELSEIF !(TALENT:ARG:風騷 || TALENT:ARG:小悪魔 || (TALENT:ARG:恋慕 > 0) || TALENT:ARG:炮友) && (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		RETURNF "0"
	ELSE
		RETURNF "1"
	ENDIF
CASE "收藏解禁条件"
	IF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知) && ABL:ARG:露出癖 >= 4
		RETURNF "解禁：恋慕"
	ELSEIF ABL:ARG:露出癖 < 4
		RETURNF "解禁：露出癖4"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能25(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "創口貼"
CASE "描写"
	RETURNF "沿着蜜缝轻轻贴上的"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "0"
CASE "能穿"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		RETURNF "0"
	ELSEIF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知) && !RAND:3
		RETURNF "1"
	ELSE
		RETURNF "0"
	ENDIF
CASE "関連内衣"
	RETURNF @"{[[上半身内衣_はだけ不可_創口貼]]}"

CASE "收藏表示可能"
	IF !(FLAG:セクシー胖次追加 && ABL:ARG:露出癖 >= 4)
		RETURNF "0"
	ELSEIF (TALENT:ARG:幼稚 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:体型 < 0 || TALENT:ARG:無知)
		RETURNF "1"
	ELSE
		RETURNF "0"
	ENDIF
CASE "收藏解禁条件"
	IF ABL:ARG:露出癖 < 4
		RETURNF "解禁：露出癖4"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能26(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "拉链（屁股处可開）"
CASE "描写"
	RETURNF "屁股处可拉开的"
CASE "半透明(前)"
	RETURNF "0"
CASE "半透明(後)"
	RETURNF "1"
CASE "能穿"
	IF FLAG:セクシー胖次追加 && (TALENT:ARG:炮友 && ABL:ARG:露出癖 >= 5 || TALENT:ARG:無知) && !RAND:4
		RETURNF "1"
	ELSE
		RETURNF "0"
	ENDIF
CASE "関連内衣"
	RETURNF "0"

CASE "收藏表示可能"
	IF !FLAG:セクシー胖次追加
		RETURNF "0"
	ELSEIF (TALENT:ARG:炮友 && ABL:ARG:露出癖 >= 5 || TALENT:ARG:無知)
		RETURNF "1"
	ELSE
		RETURNF "0"
	ENDIF
CASE "收藏解禁条件"
	IF TALENT:ARG:無知
	ELSEIF TALENT:ARG:炮友 && ABL:ARG:露出癖 >= 5
	ELSEIF TALENT:ARG:炮友
		RETURNF "解禁：露出癖5"
	ELSEIF ABL:ARG:露出癖 >= 5
		RETURNF "解禁：炮友"
	ENDIF
ENDSELECT

@F下半身内衣_ずらし可能27(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "貞操帯"
CASE "描写"
	RETURNF "挂着锁的金属"
CASE "半透明(前)"
	RETURNF "0"
CASE "半透明(後)"
	RETURNF "0"
CASE "関連内衣"
	RETURNF @"{[[上半身内衣_はだけ不可_贞操带胸罩]]}"
ENDSELECT

@F下半身内衣_ずらし可能28(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "触手胖次"
CASE "描写"
	RETURNF "内侧有什么在蠢动着的"
CASE "半透明(前)"
	RETURNF "0"
CASE "半透明(後)"
	RETURNF "0"
CASE "関連内衣"
	RETURNF @"{[[上半身内衣_はだけ不可_触手胸罩]]}"
ENDSELECT

@F下半身内衣_ずらし可能29(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "振動棒胖次"
CASE "描写"
	RETURNF "装着柱状怪东西的"
CASE "半透明(前)"
	RETURNF "0"
CASE "半透明(後)"
	RETURNF "1"
CASE "関連内衣"
	RETURNF "0"
ENDSELECT

@F下半身内衣_ずらし可能30(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "二穴振動棒胖次"
CASE "描写"
	RETURNF "两根棒子在胯下嗡嗡作响的"
CASE "半透明(前)"
	RETURNF "0"
CASE "半透明(後)"
	RETURNF "1"
CASE "関連内衣"
	RETURNF "0"
ENDSELECT

@F下半身内衣_ずらし可能31(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "比基尼内裤"
;下半身内衣２の20
CASE "換装可能"
	RETURNF "1"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "関連内衣"
	RETURNF @"{[[上半身内衣_はだけ不可_比基尼内衣]]}"
ENDSELECT

@F下半身内衣_ずらし可能32(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "迷你比基尼内裤"
;下半身内衣２の21
CASE "換装可能"
	RETURNF "1"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "関連内衣"
	RETURNF @"{[[上半身内衣_はだけ不可_迷你比基尼内衣]]}"
ENDSELECT

;来自TOKI觉的剧本
@F下半身内衣_ずらし可能33(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "御札"
CASE "描写"
	RETURNF "紧密贴合饱满耻丘的博丽"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
CASE "関連内衣"
	RETURNF "0"
ENDSELECT

;-------------
;游泳补丁
;-------------
@F下半身内衣_ずらし可能34(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF "泳裤"
CASE "描写"
	RETURNF "男性"
CASE "換装可能"
	RETURNF "1"
CASE "半透明(前)"
	RETURNF "1"
CASE "半透明(後)"
	RETURNF "1"
ENDSELECT

@F下半身内衣_ずらし可能35(ARG, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
SELECTCASE O_DATA
CASE "名前"
	RETURNF UNIQUE_UNDERWARE_SWIMMING_BOTTOM(ARG)
CASE "描写"
	RETURNF UNIQUE_UNDERWARE_SWIMMING_ADJ(ARG)
CASE "半透明(前)"
	RETURNF @"{UNIQUE_UNDERWARE_SWIMMING_TRANSPARENCY_FRONT(ARG)}"
CASE "半透明(後)"
	RETURNF @"{UNIQUE_UNDERWARE_SWIMMING_TRANSPARENCY_BACK(ARG)}"
CASE "関連内衣"
	IF GETMETH_INT(ARG, "キャラデータ", ARG, "固有上半身泳衣")
		RETURNF @"{CLOTHES_ID("上半身内衣１", @"%UNIQUE_UNDERWARE_SWIMMING_TOP(ARG)%")}"
	ELSE
		RETURNF "0"
	ENDIF
ENDSELECT
