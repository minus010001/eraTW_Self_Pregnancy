;関数置き場
;-------------------------------------------------
;道具販売状況@ITEMSTOCK
;戻り値1 道具が存在しない
;戻り値2 卖完
;戻り値3 お金が足りない
;戻り値4 条件を満たしていない
;戻り値5 すでに99个所持している
;-------------------------------------------------
@ITEMSTOCK(ARG, 値段)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 値段
SIF !STRLENS(ITEMNAME:ARG)
	RETURNF 1
SIF ITEMSALES:ARG == -2
	RETURNF 4
SIF ITEMSALES:ARG == -1
	RETURNF 2
SIF MONEY < 値段
	RETURNF 3
SIF ITEM:ARG >= 999
	RETURNF 5
RETURNF 0

;-------------------------------------------------
;関数名:CHOICE
;概　要:２～４択関数
;引　数:ARGS:0…質問内容
;      :ARGS:1～4…選択肢の文字列(3,4は省略可)
;戻り値:ユーザ入力結果(0～3)
;選択条件の無い、シンプル的選択肢作成関数
;口上とかでも使えるかもしれない
;とりあえず4択まで。増やしてもいいけど
;-------------------------------------------------
@CHOICE(ARGS:0, ARGS:1, ARGS:2, ARGS:3, ARGS:4, ARGS:5, ARGS:6, ARGS:7, ARGS:8, ARGS:9, ARGS:10)
#LOCALSIZE 1
#LOCALSSIZE 1
PRINTSL ARGS:0
FOR LOCAL, 0, 10
	SIF !STRLENS(ARGS:(1 + LOCAL))
		CONTINUE
	PRINTFORML [{LOCAL}] %ARGS:(1 + LOCAL)%
NEXT
$INPUT_LOOP
INPUT
SIF INRANGE(RESULT, 0, 9) && STRLENS(ARGS:(1 + RESULT))
	RETURN RESULT
;这种想看特殊剧情的人当然得给点特殊剧情照顾下
CALL SIMATTYAUOJISAN()
CLEARLINE 1
GOTO INPUT_LOOP

;-------------------------------------------------
;選択肢を表示し入力結果を返す関数 条件を満たさない選択肢をグレー表示にして無効にすることが可能
;引数にセリフと条件(0で無効、非0で有効)を指定。最大7個まで
;戻り値は選択した選択肢の番号
;set any ARG to -1 to hide option entirely
;-------------------------------------------------
@ASK_M(ARGS:0, ARG:0, ARGS:1, ARG:1, ARGS:2, ARG:2, ARGS:3, ARG:3, ARGS:4, ARG:4, ARGS:5, ARG:5, ARGS:6, ARG:6, ARGS:7, ARG:7, ARGS:8, ARG:8, ARGS:9, ARG:9)
#LOCALSIZE 3
#LOCALSSIZE 1
#DIM CONST 選択肢上限 = 9
#DIM PREV_REDRAW
PREV_REDRAW = CURRENTREDRAW()
LOCAL:2 = GETCOLOR()
REDRAW 0
RESETCOLOR
FOR LOCAL:0, 0, (選択肢上限 + 1)
	IF ARGS:(LOCAL:0) != "" && ARG:(LOCAL:0) >= 0
		IF ARG:(LOCAL:0)
			;PRINTBUTTON @"[{LOCAL:0, 2, RIGHT}] %ARGS:(LOCAL:0)%", LOCAL:0
			PRINTBUTTON @"[{LOCAL:0}] %ARGS:(LOCAL:0)%", LOCAL:0
		ELSE
			SETCOLOR 0x606060
			PRINTPLAINFORM [{LOCAL:0}] %ARGS:(LOCAL:0)%
			RESETCOLOR
		ENDIF
		PRINTL
	ENDIF
NEXT
$INPUT_LOOP
INPUT
IF RESULT < 0 || RESULT > 選択肢上限 || ARGS:RESULT == "" || ARG:RESULT <= 0
	CLEARLINE 1
	GOTO INPUT_LOOP
ENDIF
REDRAW PREV_REDRAW
SETCOLOR LOCAL:2
RETURN RESULT
;-------------------------------------------------
;現金と筹码どちらでも払える選択肢のための関数
;-------------------------------------------------
@PAY_YEN_OR_CM(ARG,ARG:1)
#LOCALSIZE 3
#LOCALSSIZE 1
LOCAL:2 = GETCOLOR()
RESETCOLOR
PRINTFORML [0] 放弃
PRINTFORML [1] 支付%金額表示(ARG)%　　（现在的所持金：%金額表示(MONEY)%）
PRINTFORML [2] 支付CM%三桁区切(ARG:1)%　（现在的所持筹码：CM%三桁区切(MONEY:2)%）
$INPUT_LOOP
INPUT
SELECTCASE RESULT
	CASE 0
	CASE 1
		IF MONEY < ARG
			PRINTFORMW 金钱不足
			GOTO INPUT_LOOP
		ENDIF
		MONEY -= ARG
	CASE 2
		IF MONEY:2 < ARG:1
			PRINTFORMW 筹码不足
			GOTO INPUT_LOOP
		ENDIF
		MONEY:2 -= ARG:1
	CASEELSE
		GOTO INPUT_LOOP
ENDSELECT
SETCOLOR LOCAL:2
RETURN RESULT
;-------------------------------------------------
;函数名:GET_REVISION
;概要:通用修正值获取函数
;参数:ARG:0…要参照的数值
;返回值:修正后的值
;备注:式内函数
;当需要将某个数值转换为一个渐近上限值的数值时
;如果上升率小，则很快达到上限；反之，如果上升率大，则难以接近上限
;-------------------------------------------------
@GET_REVISION(ARG:0, 上限, 上昇率)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 上限
#DIM 上昇率
RETURNF 上限 - 上昇率 * 上限 / (上昇率 + ARG:0)

;eg 10000,2000,20000だと2000-2000*20000/30000

@GET_REVISION2(ARG:0, 上限, 上昇率)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 上限
#DIM 上昇率
RETURNF 上限 * (ARG:0 + 1) / (上昇率 + ARG:0 + 1)

;-------------------------------------------------
;関数名:GET_REVISION3
;概　要:汎用補正値取得関数
;引　数:num…参照する数値　TH:0~2…しきい値1~3　
;-------------------------------------------------
@GET_REVISION3(num, TH:0, TH:1, TH:2)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM TH, 3
#DIM num

FOR LOCAL,0,3
	SIF num <= TH:LOCAL || !TH:LOCAL
		RETURNF num
	;引数がTHを超えてたら超過分をSQRT*2
	num = TH:LOCAL + SQRT(num - TH:LOCAL) * 2
NEXT

RETURNF num

;10000, 50, 100, 200なら124


[SKIPSTART]
;-------------------------------------------------
;関数名:PRINT_BASE
;概　要:汎用補正値取得関数
;引　数:ARGS バーの名前
;		ARG:0…参照する角色番号
;       ARG:1…ベースの番号
;       ARG:2…バーの長さ
;BARを表示させたいときに
;-------------------------------------------------
@PRINT_BASE(ARGS,ARG:0,ARG:1,ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
PRINTFORM %ARGS,%
BAR BASE:ARG:(ARG:1),MAXBASE:ARG:(ARG:1),ARG:2
PRINTFORML ({BASE:ARG:(ARG:1)}/{MAXBASE:ARG:(ARG:1)})

;FileName_COMMON.ERB ------------------------------- Rev1.00
;文字入りライン表示関数
;CALL		USER
;ARG		ARGS:表示ライン名, ARG:0 line_to_"-", ARG:1 ライン長
;RETURN		VOID
;COMMENT
;-----------------------------------------------------------
@DRAW_INFORMATIONLINE(ARGS,ARG,ARG:1)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS EQUAL
#DIMS MINUS
EQUAL = =
MINUS = -
LOCAL = ARG:1

IF ARGS =="Info"
	SIF TFLAG:Info表示中
		RETURN
	TFLAG:Info表示中 = 1
ENDIF
SIF !ARG:1
	LOCAL = MAXWIDTH()
IF ARG == 1
	PRINTSINGLEFORMS "-- %ARGS% " + "%(MINUS * MAX((LOCAL - STRLENS(ARGS)),0))%"
ELSE
	PRINTSINGLEFORMS "== %ARGS% " + "%(EQUAL * LOCAL)%"
ENDIF
[SKIPEND]
 ;-------------------------------------------------
;絶頂時など、ＡとＢとＤと…みたいな感じで「と」でつないで列挙するための関数
;引数に表示する文字列と条件(0で無効、非0で有効)を指定。最大5个まで
;-------------------------------------------------
@PRINT_EMUNERATE(表示:1,条件:1,表示:2,条件:2,表示:3,条件:3,表示:4,条件:4,表示:5,条件:5)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS 表示,6
#DIM 条件,6

SIF 条件:1
	PRINTFORM %表示:1%
SIF 条件:1 && (条件:2 || 条件:3 || 条件:4 || 条件:5)
	PRINT 和
SIF 条件:2
	PRINTFORM %表示:2%
SIF 条件:2 && (条件:3 || 条件:4 || 条件:5)
	PRINT 和
SIF 条件:3
	PRINTFORM %表示:3%
SIF 条件:3 && (条件:4 || 条件:5)
	PRINT 和
SIF 条件:4
	PRINTFORM %表示:4%
SIF 条件:4 && 条件:5
	PRINT 和
SIF 条件:5
	PRINTFORM %表示:5%
;-------------------------------------------------
;悪魔で女僕から流用させていただきました。多謝。
;単語1/単語2/…/単語n の書式で書かれたARGS:0～の単語のどれかを等確率で返す
;(ARGSが複数あるのは関数などが突っ込まれることを想定しているため)
;＿は何も出力しない選択とする
;選択肢の数を100まで拡張、RESULT退避処理追加
;抽選処理が等確率ならFOR-NEXT使わなくてよいと思ったのでRANDで済ませる
;-------------------------------------------------
@TEXTR(ARGS, ARGS:1, ARGS:2, ARGS:3, ARGS:4, ARGS:5, ARGS:6)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM  L_RESULT               ;RESULT退避用
#DIMS DYNAMIC CHOICES        ;結果
#DIMS DYNAMIC STR_ARRAY, 300 ;選択肢格納用の一時変数
;まず一つにまとめる
ARGS = %ARGS%\/%ARGS:1%\/%ARGS:2%\/%ARGS:3%\/%ARGS:4%\/%ARGS:5%\/%ARGS:6%
;不要な"/"を削る作業
;空のARGSがあると//がたくさん出来て判定の邪魔になるので
;1.//は置換で削る
;2.最末尾が/なら削る（SPLITとRANDをそのまま使用都合上）
ARGS '= REPLACE(ARGS, "\/+", "/")
ARGS '= REPLACE(ARGS, "(^/|/$)", "")
SPLIT ARGS, "/", STR_ARRAY, L_RESULT
CHOICES '= STR_ARRAY:(RAND:L_RESULT)
SIF CHOICES == "＿"
	CHOICES =
RETURNF CHOICES
;-------------------------------------------------
;未読判定関数@ONCE
;式中で使用する関数です
;ARGSはイベント番号、ARGは真だと初回判定を更新しない、ARG:1は角色登録番号（省略するとTARGET,-1ならMASTER）
;{イベント番号}/　という文字列をCSTR:1にぶっこんでいきます
;-------------------------------------------------
@ONCE(ARGS,ARG, ARG:1)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
;ARG:2の変換
ARG:1 = ARG:1 ? ARG:1 # TARGET
ARG:1 = ARG:1 == -1 ? MASTER # ARG:1
;新方法跳转
RETURNF !SET_UPDATECHECK(ARGS, "CSTR:ONCE", ARG:1, !ARG)

SIF CSTR:(ARG:1):1 == ""
	CSTR:(ARG:1):1 = /

LOCALS = /%ARGS%/
IF STRCOUNT(CSTR:(ARG:1):1,LOCALS)
	RETURNF 0
ELSEIF ARG
	RETURNF 1
ENDIF
;初回判定更新
CSTR:(ARG:1):1 = %CSTR:(ARG:1):1%%ARGS%/
RETURNF 1

;-------------------------------------------------
;未読判定関数@FIRSTTIME
;式中で使用する関数です。
;ARGSはイベント番号、ARGは真だと初回判定を更新しない、ARG:1は角色登録番号（省略するとTARGET,-1ならMASTER）
;{イベント番号}/　という文字列をCSTR:0にぶっこんでいきます
;-------------------------------------------------
@FIRSTTIME(ARGS,ARG, ARG:1)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
;ARG:1の変換
ARG:1 = ARG:1 ? ARG:1 # TARGET
ARG:1 = ARG:1 == -1 ? MASTER # ARG:1
;新方法跳转
RETURNF !SET_UPDATECHECK(ARGS, "CSTR:FIRSTTIME", ARG:1, !ARG)

SIF CSTR:(ARG:1):0 == ""
	CSTR:(ARG:1):0 = /

LOCALS = /%ARGS%/
IF STRCOUNT(CSTR:(ARG:1):0,LOCALS)
	RETURNF 0
ELSEIF ARG
	RETURNF 1
ENDIF
;初回判定更新
CSTR:(ARG:1):0 = %CSTR:(ARG:1):0%%ARGS%/
RETURNF 1

;-------------------------------------------------
;関数名:EXP_UP
;概　要:経験値変動チェック
;引　数:ARG:0…参照するEXP
;       ARG:1…参照する角色番号
;-------------------------------------------------
@EXP_UP(ARG,ARG:1)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF EXP:(ARG:1):ARG - TCVAR:(ARG:1):(400 + ARG)

;-------------------------------------------------
;関数名:INVAGINA
;概　要:Vに何が入っているのかを返す
;引　数:ARG:0…参照する角色番号
;戻り値:0無
;戻り値:1 阴茎
;戻り値:2 振動棒
;-------------------------------------------------
@INVAGINA(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
IF TEQUIP:ARG:50
	IF TALENT:PLAYER:2 & 2
		RETURNF 1
	ELSE
		RETURNF 3
	ENDIF
ELSEIF TEQUIP:ARG:振動棒
	RETURNF 2
ENDIF
;-------------------------------------------------
;関数名:INANAL
;概　要:Aに何が入っているのかを返す
;引　数:ARG:0…参照する角色番号
;戻り値:0無
;戻り値:1 阴茎
;戻り値:2 肛用振動棒
;戻り値:3 後庭拉珠
;-------------------------------------------------
@INANAL(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
IF TEQUIP:ARG:51
	RETURNF 1
ELSEIF TEQUIP:ARG:肛用振動棒
	RETURNF 2
ELSEIF TEQUIP:ARG:後庭拉珠
	RETURNF 3
ENDIF
;-------------------------------------------------
;関数名:HETEROSEX
;ARGとARG:1の性別が同じかどうかを返すほんとはHOMOSEXだけど語感が悪いので
;女性器を持つものは女、持たないものを男とする
;戻り値0=異性
;戻り値1=同性(レズ)
;戻り値2=同性(ゲイ)
;-------------------------------------------------
@HETEROSEX(ARG,ARG:1)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
IF GETBIT(TALENT:ARG:2,0) == GETBIT(TALENT:(ARG:1):2,0)
	IF GETBIT(TALENT:ARG:2,0)
		RETURNF 1
	ELSE
		RETURNF 2
	ENDIF
ELSE
	RETURNF 0
ENDIF

;-------------------------------------------------
;関数名:NameToID
;引数にCALLNAMEを入れると角色番号を返す
;-------------------------------------------------
@NameToID(ARGS)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = FINDCHARA(CALLNAME, @"%ARGS%" , 1)
IF LOCAL < 0
	PRINTFORML 没有找到%ARGS%，将返回+++，请保存日志报错
	RETURNF 0
ENDIF
RETURNF LOCAL

;-------------------------------------------------
;関数名:GET_TARGETNUM
;同室中のTARGETの数を返す
;引数廃止してFOR-NEXTを排除して高速化
;-------------------------------------------------
@GET_TARGETNUM()
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF MAX(0, FINDELEMENT(TARGET, 0) - 1)
;-------------------------------------------------
;関数名:IN_ROOM_MEMBER
;部屋の中で特定要素での最低・最高を持つ角色を取得
;ARGS MAXかMINかを選択
;ARG 部屋の番号
;ARGS:1 対象となる変数の名前
;ARG:1 変数の番号
;-------------------------------------------------
@IN_ROOM_MEMBER, ARGS, ARG, ARGS:1, ARG:1
#FUNCTION
#LOCALSIZE 4
#LOCALSSIZE 1
VARSET LOCAL, 0
SELECTCASE ARGS:1
	CASE "CFLAG"
		FOR LOCAL, 1, CHARANUM
			SIF CFLAG:LOCAL:現在位置 != ARG
				CONTINUE
			IF LOCAL:3++ == 0
				LOCAL:1 = CFLAG:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF ARGS == "MAX" && LOCAL:1 < CFLAG:LOCAL:(ARG:1)
				LOCAL:1 = CFLAG:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF ARGS == "MIN" && LOCAL:1 > CFLAG:LOCAL:(ARG:1)
				LOCAL:1 = CFLAG:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF LOCAL:1 == CFLAG:LOCAL:(ARG:1) && RAND:2
				LOCAL:1 = CFLAG:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ENDIF
		NEXT
		RETURNF LOCAL:2
	CASE "TCVAR"
		FOR LOCAL, 1, CHARANUM
			SIF CFLAG:LOCAL:現在位置 != ARG
				CONTINUE
			IF LOCAL:3++ == 0
				LOCAL:1 = TCVAR:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF ARGS == "MAX" && LOCAL:1 < TCVAR:LOCAL:(ARG:1)
				LOCAL:1 = TCVAR:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF ARGS == "MIN" && LOCAL:1 > TCVAR:LOCAL:(ARG:1)
				LOCAL:1 = TCVAR:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF LOCAL:1 == TCVAR:LOCAL:(ARG:1) && RAND:2
				LOCAL:1 = TCVAR:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ENDIF
		NEXT
		RETURNF LOCAL:2
	CASE "ABL"
		FOR LOCAL, 1, CHARANUM
			SIF CFLAG:LOCAL:現在位置 != ARG
				CONTINUE
			IF LOCAL:3++ == 0
				LOCAL:1 = ABL:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF ARGS == "MAX" && LOCAL:1 < ABL:LOCAL:(ARG:1)
				LOCAL:1 = ABL:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF ARGS == "MIN" && LOCAL:1 > ABL:LOCAL:(ARG:1)
				LOCAL:1 = ABL:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF LOCAL:1 == ABL:LOCAL:(ARG:1) && RAND:2
				LOCAL:1 = ABL:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ENDIF
		NEXT
		RETURNF LOCAL:2
	CASE "TALENT"
		FOR LOCAL, 1, CHARANUM
			SIF CFLAG:LOCAL:現在位置 != ARG
				CONTINUE
			IF LOCAL:3++ == 0
				LOCAL:1 = TALENT:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF ARGS == "MAX" && LOCAL:1 < TALENT:LOCAL:(ARG:1)
				LOCAL:1 = TALENT:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF ARGS == "MIN" && LOCAL:1 > TALENT:LOCAL:(ARG:1)
				LOCAL:1 = TALENT:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ELSEIF LOCAL:1 == TALENT:LOCAL:(ARG:1) && RAND:2
				LOCAL:1 = TALENT:LOCAL:(ARG:1)
				LOCAL:2 = LOCAL
			ENDIF
		NEXT
		RETURNF LOCAL:2
	CASE "RAND"
		LOCALS =
		FOR LOCAL, 1, CHARANUM
			SIF CFLAG:LOCAL:現在位置 != ARG
				CONTINUE
			LOCALS += TOSTR(LOCAL) + ":"
		NEXT
		RETURNF TOINT(SPLIT_G(REPLACE(LOCALS, ":$", "")))
ENDSELECT
;FileName_COMMON.ERB ------------------------------- Rev1.00
;TARGET再設定関数
;CALL		USER
;ARG		VOID
;RETURN		VOID
;COMMENT
;-----------------------------------------------------------
;
;列表函数とソート函数ができたのでかなり简易化できたかな
@TARGETSET_CHECK(OP)
#LOCALSIZE 2
#LOCALSSIZE 1
#DIMS OP         ;用于跳过SHOW_STATUS，因为如果包含同室状态刷新会出问题（已弃用）
#DIM  TARGET_CNT ;对象数目
#DIM  L_ID       ;REF对策、临时用
#DIM  SWAP_ID    ;现在的TARGET指定处理用临时变量（已转正）
;在“诶嘿嘿”状态下，暂时保存TARGET（以免因情事败露等情况导致逻辑错乱）
SIF !TFLAG:現在的TARGET
	TFLAG:現在的TARGET = TARGET

CALL MAKE_CHARA_LIST("TARGET列表", L_ID, TARGET, TARGET_CNT)
SIF !TARGET_CNT
	RETURN
CALL QOL_SORT_LIST(FINDELEMENT(DISP_MEMO, "好感") + SORT_BTN_BASE + SORT_BTN_DESC_OFFSET, TARGET, TARGET_CNT)

LOCAL:1 = TARGET
IF TFLAG:現在的TARGET > 0
	LOCAL:1 = FINDELEMENT(TARGET, TFLAG:現在的TARGET)
	IF LOCAL:1 == -1
		LOCAL:1 = 0
		TFLAG:現在的TARGET = 0
	ENDIF
	LOCAL:1 = TARGET:(LOCAL:1)
ENDIF
ARRAYSHIFT TARGET, 1, LOCAL:1

@TARGETSET_CHACK(ARGS)
#LOCALSIZE 1
#LOCALSSIZE 1
CALL TARGETSET_CHECK(ARGS)

;====================================================
@性技補正(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF 1 + (ABL:PLAYER:ARG - 2) / 10
;====================================================

;-------------------------------------------------
;関数名:RECOVER
;概　要:BASE回復処理
;		表示文字列が空の場合かつ回復量が100未満非表示
;		表示文字列がある場合は表示
;引　数:ARG:0	参照する角色番号
;		回復量	ARGSに対応するBASEに加算する値（負数の場合は減らす）
;       ARGS	ベース名
;		ARGS:1	表示文字列
;-------------------------------------------------
@RECOVER(ARG,回復量,ARGS,ARGS:1)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM BASE_ID
#DIM 回復量
SIF 回復量 == 0 || !CHECK_BASE(ARG, ARGS)
	RETURN
;回復処理
IF MAGIC_DLC 
	IF (ARGS == "TSP" || ARGS == "法力") && 回復量 < 0 
		SIF ARGS == "TSP" || (ARGS == "法力" && M_TFLAG:时之蠹)
			MONEY:1 -= 回復量
		SIF TALENT:ARG:大魔法师
			回復量 = 回復量 * 7 / 10
		SIF TALENT:ARG:昨日重现
			回復量 = 回復量 * (20 - TALENT:ARG:昨日重现) / 20
		SIF 无限TSP && ARGS == "TSP"
			回復量 = 0
		SIF 无限法力 && ARGS == "法力"
			回復量 = 0
	ENDIF
ENDIF
BASE:ARG:(ARGS) = LIMIT(BASE:ARG:(ARGS) + 回復量, 0, MAXBASE:ARG:(ARGS))
;表示処理
SIF 回復量 > 0 && 回復量 < 100 && ARGS:1 == ""
	RETURN
IF CFLAG:ARG:現在位置 == CFLAG:MASTER:現在位置
	BASE_ID = GETNUM(BASE, ARGS)
	LOCALS = \@ ARGS:1 != "" ? （%ARGS:1%） # \@
	IF 回復量 > 0
		CALL COLORMESSAGE(@"%BASE_TR(BASENAME:BASE_ID)%＋{回復量} （%CALLNAME:ARG%）%LOCALS%",C_GREEN,1)
	ELSEIF 回復量 < 0
		CALL COLORMESSAGE(@"%BASE_TR(BASENAME:BASE_ID)%－{ABS(回復量)} （%CALLNAME:ARG%）%LOCALS%",C_RED,1)
	ENDIF
ENDIF

;-------------------------------------------------
;関数名:RECOVER_PERMIL
;概　要:BASE割合回復処理
;引　数:ARG:0		参照する角色番号
;		回復割合	MAXBASE対して算出する値（負数の場合は減らす）
;       ARGS		ベース名
;       回復速度の影響
;		ARGS:1		表示文字列
;回復割合は0.1%単位
;-------------------------------------------------
@RECOVER_PERMIL(ARG,回復割合,ARGS,回復速度の影響,ARGS:1="")
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 回復割合
#DIM 回復速度の影響
#DIM 回復量
SIF !CHECK_BASE(ARG, ARGS)
	RETURN
回復量 = MAXBASE:ARG:(ARGS) * 回復割合 / 1000
SIF 回復速度の影響
	回復量 = 回復量 * (TALENT:ARG:回復速度 + 4) / 4
CALL RECOVER(ARG, 回復量, ARGS, ARGS:1)

;-------------------------------------------------
;関数名:RECOVER_PERMIL_BOTH
;概　要:RECOVER_PERMILを你と対象角色両方やる
;-------------------------------------------------
@RECOVER_PERMIL_BOTH(ARG,回復割合,ARGS,回復速度の影響,ARGS:1="")
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 回復割合
#DIM 回復速度の影響
CALL RECOVER_PERMIL(ARG,回復割合,ARGS,回復速度の影響,ARGS:1)
SIF ARG != MASTER
	CALL RECOVER_PERMIL(MASTER,回復割合,ARGS,回復速度の影響,ARGS:1)

;-------------------------------------------------
;関数名:CHECK_BASE
;概　要:ARGが使えるBASEか判定
;戻り値:0：使用不可
;		1：使用可能
;-------------------------------------------------
@CHECK_BASE(ARG, ARGS)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC L_ID
L_ID = GETNUM(BASE, ARGS)
IF L_ID < 0
	PRINTFORML 没有找到%ARGS%，请保存日志报错
	RETURNF 0
ENDIF
SELECTCASE L_ID
	CASE BASE_体力, BASE_气力
	;竿無ならスキップ
	CASE BASE_精力, BASE_勃起, BASE_射精
		SIF !HAS_PENIS(ARG)
			RETURNF 0
	;你以外TSPスキップ
	CASE BASE_TSP
		SIF ARG != MASTER
			RETURNF 0
	CASE BASE_母乳
		SIF !TALENT:ARG:母乳体質
			RETURNF 0
ENDSELECT
RETURNF 1
;-------------------------------------------------
;関数名:CHANGE_MAXBASE(魔法DLC)
;概要:特定BASE的永久变化
;-------------------------------------------------
@CHANGE_MAXBASE(ARG,强化能力,变动值,info = 0)
#DIM 变动值
#DIM 强化能力
#DIM info
SIF !CHECK_BASE(ARG, BASENAME:强化能力)
	RETURN
IF MAGIC_DLC
	SIF 变动值 < 0 && ABS(变动值) > MAXBASE:ARG:强化能力
		变动值 = (MAXBASE:ARG:强化能力 -1) * -1
	SIF (MAXBASE:ARG:强化能力 + 变动值) > 9999999
		变动值 = 9999999 - MAXBASE:ARG:强化能力
	MAXBASE:ARG:强化能力 += 变动值
	SIF (info == 1 || (info == 2 && ARG == MASTER))&& 变动值 != 0
		CALL COLORMESSAGE(@"%CALLNAME:ARG%的%BASENAME:强化能力%永久\@ 变动值 > 0? 上升了# 下降了\@{ABS(变动值)}",C_YELLOW,1)
ELSE
	MAXBASE:ARG:强化能力 += 变动值
ENDIF

;-------------------------------------------------
;関数名:BUFF_BASE
;概　要:特定BASEを一時的に強化
;-------------------------------------------------
@BUFF_BASE(ARG,強化能力,変動値,メッセージ)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 変動値
#DIM 強化能力
#DIM メッセージ
IF !MAGIC_DLC
	SIF MAXBASE:ARG:強化能力 <= 0
		RETURN
	SIF 変動値 < 0 && ABS(変動値) > MAXBASE:ARG:強化能力
		変動値 = (MAXBASE:ARG:強化能力 -1) * -1

	MAXBASE:ARG:強化能力 += 変動値
	BASE:ARG:強化能力 += 変動値
	BUFF:ARG:強化能力 += 変動値
	;failsafe
	MAXBASE:ARG:強化能力 = MAX(0, MAXBASE:ARG:強化能力)
	BASE:ARG:強化能力 = MAX(0, BASE:ARG:強化能力)
	SIF メッセージ
		CALL COLORMESSAGE(@"%CALLNAME:ARG%的%BASE_TR(BASENAME:強化能力)%暂时\@ 変動値 > 0? 上升了# 下降了\@{ABS(変動値)}",C_YELLOW,1)
ELSE
	SIF 強化能力 == BASE_TSP && 无限TSP
		RETURN
	SIF 強化能力 == BASE_法力 && 无限法力
		RETURN
	SIF MAXBASE:ARG:強化能力 <= 0 && ARG != MASTER
		RETURN
	SIF (強化能力 == BASE_TSP || 強化能力 == BASE_法力) && TALENT:MASTER:高难开局 && !M_TFLAG:BUFF标记 && !TALENT:MASTER:神性
		RETURN
	SIF 変動値 < 0 && ABS(変動値) > MAXBASE:ARG:強化能力
		変動値 = (MAXBASE:ARG:強化能力 -1) * -1
	SIF (MAXBASE:ARG:強化能力 + 変動値) > 9999999
		変動値 = 9999999 - MAXBASE:ARG:強化能力
	MAXBASE:ARG:強化能力 += 変動値
	BASE:ARG:強化能力 += 変動値
	BUFF:ARG:強化能力 += 変動値
	SIF (メッセージ == 1 || (メッセージ == 2 && ARG == MASTER))&& 変動値 != 0
		CALL COLORMESSAGE(@"%CALLNAME:ARG%的%BASENAME:強化能力%暂时的\@ 変動値 > 0? 上升了# 下降了\@{ABS(変動値)}",C_YELLOW,1)
ENDIF
;-------------------------------------------------
;関数名:BUFF_RESET
;概　要:Buffを完全にリセット
;-------------------------------------------------
@BUFF_RESET
#LOCALSIZE 2
#LOCALSSIZE 1
FOR LOCAL,0,CHARANUM
	FOR LOCAL:1, 0, 16
		SIF BUFF:LOCAL:(LOCAL:1) == 0
			CONTINUE
		MAXBASE:LOCAL:(LOCAL:1) = MAX(0, MAXBASE:LOCAL:(LOCAL:1) - BUFF:LOCAL:(LOCAL:1))
		BASE:LOCAL:(LOCAL:1) = MAX(0, MAXBASE:LOCAL:(LOCAL:1) - BUFF:LOCAL:(LOCAL:1))
		BUFF:LOCAL:(LOCAL:1) = 0
	NEXT
NEXT

;-------------------------------------------------
;関数名:CHANGE_CFLAG
;概　要:メッセージ付きCFLAG変動
;减少にはならないようになってるので注意
;-------------------------------------------------
@CHANGE_CFLAG(変動フラグ,対象,変動数,DISPLAY_TYPE = 2, DESCRIPTION = "") ;custom code, added DISPLAY_TYPE, DESCRIPTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 変動フラグ
#DIM 対象
#DIM 変動数
#DIM DISPLAY_TYPE ;custom code
#DIMS DESCRIPTION ;custom code
IF 変動フラグ < 0
	PRINTFORMW 没有找到CFLAG:{変動フラグ}，请保存日志报错
	RETURN
ENDIF

CFLAG:対象:変動フラグ = MAX(CFLAG:対象:変動フラグ + 変動数, 0)
IF 変動数 > 0
	;Custom code, translate Favor/trust
	CALL COLORMESSAGE(@"%CFLAGNAME_TR(CFLAGNAME:変動フラグ)%＋{変動数}（%CALLNAME:対象%）%COND_STR(@"（%DESCRIPTION%）", DESCRIPTION != "")%",C_YELLOW,DISPLAY_TYPE,1)
ELSEIF 変動数 < 0 ;don't show if zero
	;Custom code, translate Favor/trust
	CALL COLORMESSAGE(@"%CFLAGNAME_TR(CFLAGNAME:変動フラグ)%−{-変動数}（%CALLNAME:対象%）%COND_STR(@"（%DESCRIPTION%）", DESCRIPTION != "")%",C_RED,DISPLAY_TYPE,1)
ENDIF

;;-------------------------------------------------
;;関数名:AddEXP 已重构
;;概　要:メッセージ付きEXP加算
;;マイナスにはならないようになってるので注意
;;-------------------------------------------------
;@AddEXP(VAR_NAME,C_ID,変動数)
;#DIMS VAR_NAME
;#DIM VAR_ID
;#DIM C_ID
;#DIM 変動数
;
;SIF 変動数 == 0
;	RETURN
;
;VAR_ID = FINDELEMENT(EXPNAME, VAR_NAME)
;EXP:C_ID:VAR_ID += 変動数
;CALL COLORMESSAGE(@"%VAR_NAME%＋{変動数}（%CALLNAME:C_ID%）",C_YELLOW,2,1)

;-------------------------------------------------
;関数名:SHIRAHU
;概　要:普通に意識がある状態か　2だとウフフ中
;-------------------------------------------------
@SHIRAHU(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
IF FLAG:時間停止 && CFLAG:ARG:時間停止行動 == 0
	RETURNF 0
ELSEIF !CFLAG:ARG:睡眠 && !CFLAG:ARG:陪睡中 && !TCVAR:ARG:烂醉 && !CFLAG:ARG:衰弱
	IF TFLAG:102 > 1
		RETURNF 2
	ELSE
		RETURNF 1
	ENDIF
ELSE
	RETURNF 0
ENDIF

;反感时返回真
@EXPOSURE_CHARA_IS_SHY(ARG = -1)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC CHECK_SCORE
SIF ARG == -1
	ARG = TARGET

CHECK_SCORE = ABL:ARG:露出癖 - TALENT:ARG:羞恥心

SIF CHECK_SCORE < 3
	RETURNF 1

RETURNF 0

;-------------------------------------------------
;関数名:BETWEENTIME
;概　要:TIMEがARGからARG:1までの間なのか返す関数
;-------------------------------------------------
@BETWEENTIME(ARG,ARG:1)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
IF ARG < ARG:1
	IF TIME >= ARG && TIME < ARG:1
		RETURNF 1
	ELSE
		RETURNF 0
	ENDIF
ELSE
	IF TIME >= ARG || TIME < ARG:1
		RETURNF 1
	ELSE
		RETURNF 0
	ENDIF
ENDIF
;-------------------------------------------------
;関数名:VISIT
;概　要:その角色の活動時間帯かどうかを返す関数    判断该角色是否处于活动时间段的函数
;-------------------------------------------------
@VISIT(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
;幻想乡大地图 如果是据点地图模式，且玩家所在地图位于其初期地图，则起床-来访和归宅-就寝时间区间也视为活动时间
SIF GMF_GETBIT("幻想乡大地图") && !GMF_IS_ODEKAKE(-1) && GET_MAPID(CFLAG:MASTER:現在位置) == GET_MAPID(CFLAG:ARG:初期位置) && (BETWEENTIME(CFLAG:ARG:起床時間,CFLAG:ARG:来訪時間) || BETWEENTIME(CFLAG:ARG:帰宅時間,CFLAG:ARG:就寝時間))
	RETURNF 1

;if they're on the main map before visit time (e.g. slept over), they don't go home    如果他们在访问时间之前已经在主地图上（例如留宿），那么他们就不会回家
SIF (BETWEENTIME(CFLAG:ARG:起床時間,CFLAG:ARG:来訪時間) || CFLAG:ARG:睡眠) && CFLAG:MASTER:初期位置 == CFLAG:ARG:現在位置 && CFLAG:ARG:拠点来訪
	RETURNF 1

RETURNF BETWEENTIME(CFLAG:ARG:来訪時間,CFLAG:ARG:帰宅時間)
;-------------------------------------------------
;関数名:睡眠時間
;概　要:住宿人物が寝てるかどうかを返す関数だったのを全員使えるように    将原本只对住宿人员是否在睡觉进行反馈的函数改为全员可用
;寝てると1を返す    睡觉时返回1
;-------------------------------------------------
@睡眠時間(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
SIF ARG == MASTER
	RETURNF CFLAG:MASTER:睡眠
IF QOL_STAY_AWAKE(ARG)
	RETURNF 0
ELSEIF CFLAG:ARG:衰弱
	RETURNF 1
ELSE
	;睡眠時間をチェックする    检查睡眠时间
	CALLF 睡眠時間２(ARG)
	IF !BETWEENTIME(CFLAG:ARG:起床時間, CFLAG:ARG:就寝時間 + CFLAG:ARG:清醒劑)
		RETURNF 1
	ELSE
		RETURNF 0
	ENDIF
ENDIF
;-------------------------------------------------
;関数名:睡眠時間２
;概　要:角色ARGが寝てから起きるまでの時間の計測    测量角色ARG从睡下到醒来的时间
;逸脱していた場合は自動で修正する    如有偏离则自动修正
;-------------------------------------------------
@睡眠時間２(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM SLEEPTIME
#DIM WAKEUPTIME
;清醒劑の強度分延長する    清醒剂延长持续时间
WAKEUPTIME = CFLAG:ARG:起床時間
SLEEPTIME = CFLAG:ARG:就寝時間 + CFLAG:ARG:清醒劑
;就寝時間が24時を超えている場合    若就寝时间超过24点的情况
WHILE SLEEPTIME >= 1440
	SLEEPTIME -= 1440
WEND
;前日の夜に寝て翌日の朝に起きる場合    前一天晚上睡下第二天早上起的情况
SIF SLEEPTIME > WAKEUPTIME
	WAKEUPTIME += 1440
;睡眠時間が2～14時間を逸している場合はチェック    若睡眠时间偏离2～14小时进行检查
IF !INRANGE(WAKEUPTIME - SLEEPTIME, 120, 840)
	;清醒劑が原因
	IF CFLAG:ARG:清醒劑
		;30分ずつ是正していく
		CFLAG:ARG:清醒劑 -= 30
		RESTART
	;設定がおかしい
	ELSE
		[IF_DEBUG]
			;チェックのためTHROWしておく,路人子は許可,一時的に守矢神簽対策,あとで修正,THROWはアレなのでデバッグ用に
			;SIF ARG != [[モブ子]] && CFLAG:ARG:起床時間 % 1440 != CFLAG:ARG:就寝時間 % 1440 && !CFLAG:ARG:出禁
				;THROW
		[ENDIF]
	ENDIF
ENDIF
RETURNF WAKEUPTIME - SLEEPTIME

;-------------------------------------------------
;角色の生活時間を設定する関数
;一日の始まりに設定する
;-------------------------------------------------
@生活時間設定(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
;新增剧本侧接口，调用剧本侧设定的角色移动数值
CALL KOJO_CHECK(CHARA, "CHARADATA", "MOVE", 3)
IF RESULT == -1
	;剧本侧禁用该机制
	;对于生活时间设定，禁用等于什么都不会发生
ELSEIF RESULT
	;采用剧本设定的值
ELSE 
;剧本没有采用CHECK函数或者剧本需要是结果为0，从而采用原版移动数值
TRYCALLFORM CHARAMOVE_DATA_{NO:CHARA}(3)
ENDIF 
CFLAG:CHARA:起床時間 = 時刻計算(CSVCFLAG(CHARA, GETNUM(CFLAG, "起床時間")) + CFLAG:CHARA:生活時間補正)
CFLAG:CHARA:来訪時間 = 時刻計算(CSVCFLAG(CHARA, GETNUM(CFLAG, "来訪時間")) + CFLAG:CHARA:生活時間補正)
CFLAG:CHARA:帰宅時間 = 時刻計算(CSVCFLAG(CHARA, GETNUM(CFLAG, "帰宅時間")) + CFLAG:CHARA:生活時間補正)
CFLAG:CHARA:就寝時間 = 時刻計算(CSVCFLAG(CHARA, GETNUM(CFLAG, "就寝時間")) + CFLAG:CHARA:生活時間補正)
;大晦日はみんな夜ふかし    跨年夜大家都熬夜
IF FESTIVAL() == "大晦日"
	IF INRANGE(CFLAG:CHARA:帰宅時間, 900 , 1440) || INRANGE(CFLAG:CHARA:帰宅時間, 0 , 59) || INRANGE(CFLAG:CHARA:就寝時間, 900 , 1440) || INRANGE(CFLAG:CHARA:就寝時間, 0 , 119)
		CFLAG:CHARA:帰宅時間 = 60
		CFLAG:CHARA:就寝時間 = 120
	ENDIF
ENDIF
;-------------------------------------------------
;時刻の計算
;時刻X+Yを時刻で返す
;-------------------------------------------------
@時刻計算(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = ARG
WHILE LOCAL > 1440
	LOCAL -= 1440
WEND
WHILE LOCAL < 0
	LOCAL += 1440
WEND
RETURNF LOCAL

;-------------------------------------------------
;関数名:兒童の睡眠時間
;概　要:角色ARGの兒童が寝てるかどうかを返す関数
;寝てると1を返す
;-------------------------------------------------
@兒童の睡眠時間(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 兒童の起床時間
#DIM 兒童の就寝時間
SIF !TALENT:ARG:育児中
	RETURNF 0
兒童の起床時間 = CFLAG:ARG:起床時間
兒童の就寝時間 = CFLAG:ARG:就寝時間 - 120 + (CFLAG:ARG:就寝時間 - 120 < 0 ? 1440 # 0 )
IF !BETWEENTIME(兒童の起床時間, 兒童の就寝時間)
	RETURNF 1
ELSE
	RETURNF 0
ENDIF
;-------------------------------------------------
;関数名:WORKING
;概　要:その角色の仕事時間帯かどうかを返す関数
;-------------------------------------------------
@WORKING(ARG, ARG:1) ;custom code
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
IF !SHIRAHU(ARG) && !ARG:1 ;custom code
	RETURNF 0
ELSEIF !BETWEENTIME(TCVAR:ARG:工作開始,TCVAR:ARG:工作結束)
	RETURNF 0
ELSEIF BASE:ARG:工作量 <= 0
	RETURNF 0
ELSEIF CHARA_HOLIDAY(ARG)
	RETURNF 0
ELSE
	RETURNF 1
ENDIF
;-------------------------------------------------
;関数名:連続実行
;概　要:同一指令連続実行した際ペナルティが発生するか
;戻り値:0だとペナが発生しない
;-------------------------------------------------
@連続実行()
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
SIF TARGET != TFLAG:103
	RETURNF 0
SELECTCASE PREVCOM
	CASE 1,60,61,65,67,68,300,304,368,410,442,445,446,604,621
		RETURNF 0
	CASEELSE
		RETURNF 1
ENDSELECT

;-------------------------------------------------
;関数名:Counter不発指令
;戻り値:0でないとCounter不発
;-------------------------------------------------
@Counter不発指令(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
SELECTCASE ARG
	;(天候パッチ)「眺望天空」はCounter不发
	CASE 495
		RETURNF 1
	;立即行为：告白&炮友契约 
	CASE 352,357
		RETURNF 1
	;养鸡补丁
	CASE 499
		RETURNF 1
	;换装补丁
	CASE 429
		RETURNF 1
	;可能的立即行为：特殊地点移动
	CASE 464
		SIF SPECIAL_AREAMOVE_TIME() <= 1
			RETURNF 1
	;读日记
	CASE 406
		RETURNF 1
	;自定义指令补丁
	CASE ADD_CUSTOM_COM
		RETURNF Counter不发_ADD_CUSTOM(ADD_SELECTCUSTOMCOM)
	CASE 697 TO 699
		RETURNF 1
	;外出、推倒、挣扎
	CASE 405, 350, 850
		RETURNF 1
	CASEELSE
		RETURNF 0
ENDSELECT
;-------------------------------------------------
;関数名:ENDUFUFU
;概　要:诶嘿嘿終了時リセット処理
;-------------------------------------------------
@ENDUFUFU(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
[IF DEBUG]
	PRINTFORML 诶嘿嘿終了処理
[ENDIF]
IF CFLAG:ARG:诶嘿嘿
	CALLF SET_UPDATECHECK(TOSTR(ARG), "WITHDRAWN_LIST", , 1)
ENDIF
;時間停止中は服装セットしない
;睡眠中分岐などは全部リセット処理側にあります
SIF !FLAG:時間停止
	CALL CHANGE_CLOTHES(ARG, "复原衣服")
;服装をTEQUIPへ
CALL CLOTHES_SETTING_TRAIN(ARG)

;MASTERとの接触位置をリセット
CALL TOUCH_SET(0, 0, 0, 1)

;ウフフ時系装備リセット
;TEQUIP:ARG:10-TEQUIP:ARG:149を初期化する
VARSET TEQUIP:ARG:0, 0, 10, 150

;魔法DLC的魅惑发情解除
IF TCVAR:ARG:魅惑发情 && MAGIC_DLC
	TCVAR:ARG:発情 = 0
	TCVAR:ARG:魅惑发情 = 0
ENDIF
;射精関係リセット
IF TFLAG:102 < 2
	BASE:ARG:射精 = 0
	TCVAR:ARG:要去了 = 0
ENDIF
CFLAG:ARG:诶嘿嘿 = 0
TEQUIP:ARG:50 = -1
TEQUIP:ARG:51 = -1
TEQUIP:ARG:上半身裸露状態 = 0
;TEQUIP:ARG:下半身挪開状態 = 0
TEQUIP:ARG:上衣脱衣完毕 = 0
TEQUIP:ARG:縄 = 0
TEQUIP:ARG:拘束 = 0
TCVAR:ARG:强行 = 0
TCVAR:ARG:随便 = 0
TCVAR:ARG:次回Counter = 0
;一応MASTERも初期化（しとかないと、終了後も○○装着中が表示される可能性があるので
IF ARG == 0 || (CFLAG:MASTER:うふふ && CMATCH(CFLAG:うふふ, 0, 1, CHARANUM) >= CHARANUM - 1) ;qol custom code
	CFLAG:MASTER:うふふ = 0
	CALL CHANGE_CLOTHES(MASTER, "复原衣服")
	CALL CLOTHES_SETTING_TRAIN(MASTER)
	VARSET TEQUIP:MASTER:0, 0, 10, 150
	TEQUIP:MASTER:50 = -1
	TEQUIP:MASTER:51 = -1
	TEQUIP:MASTER:上半身裸露状態 = 0
	;TEQUIP:MASTER:下半身挪開状態 = 0
	CALL ADD_TOUCH_CLEAR() ;addition custom code
ENDIF

IF CFLAG:睡眠
	IF CFLAG:ARG:口内精液
		IF TALENT:ARG:精愛味覚 == 1
			SETCOLOR C_PINK
			PRINTFORML ❤～
			PRINTFORML 梦中的%CALLNAME:ARG%自然的饮下了%CALLNAME:MASTER%赠与的白浊，随后就像触发了什么开关一样缓缓将手移到身下，满面红潮
			TCVAR:ARG:発情 = 1
			IF MAGIC_DLC
				CFLAG:ARG:积攒度 += LIMIT((10 + RAND:20 + RAND:20 + RAND:20) * ABL:ARG:欲望,200,积攒度上限(ARG))
			ELSE
				CFLAG:ARG:积攒度 += LIMIT((10 + RAND:20 + RAND:20 + RAND:20) * ABL:ARG:欲望,200,MAX_积攒度)
			ENDIF
		ELSEIF ABL:ARG:精液中毒 > 4
			SETCOLOR C_YELLOW
			PRINTFORML 嗯～
			PRINTFORML %CALLNAME:ARG%在美梦中熟练的吞下了%CALLNAME:MASTER%留在口中的罪恶，但似乎没能满足似的舔舐着口中残存的味道
			IF MAGIC_DLC
				CFLAG:ARG:积攒度 += LIMIT((10 + RAND:20 + RAND:20 + RAND:20) * ABL:ARG:欲望,200,积攒度上限(ARG))
			ELSE
				CFLAG:ARG:积攒度 += LIMIT((10 + RAND:20 + RAND:20 + RAND:20) * ABL:ARG:欲望,200,MAX_积攒度)
			ENDIF
		ELSEIF ABL:ARG:精液中毒 > 2
			SETCOLOR C_AQUA
			PRINTFORML 唔...
			PRINTFORML %CALLNAME:ARG%不自觉的咽下%CALLNAME:MASTER%留在口中的证据，然后美美的微笑着发出一声梦呓
		ELSE
			SETCOLOR C_RED
			PRINTFORML 咳...咳...
			PRINTFORML %CALLNAME:MASTER%处理掉了%CALLNAME:ARG%口中残留的精液，%CALLNAME:ARG%在睡梦中轻声咳了一声
		ENDIF
			CFLAG:ARG:口内精液 = 0
			RESETCOLOR
	ENDIF
	IF CFLAG:ARG:脸上精液 || CFLAG:ARG:手上精液
		PRINTFORM %CALLNAME:MASTER%把粘在%CALLNAME:ARG%
		IF CFLAG:ARG:脸上精液 && !CFLAG:ARG:手上精液
			PRINTFORM 脸上的
		ELSEIF !CFLAG:ARG:脸上精液 && CFLAG:ARG:手上精液
			PRINTFORM 手上的
		ELSE
			PRINTFORM 脸上和手上的
		ENDIF
		PRINTFORML 精液擦掉了
		CFLAG:ARG:脸上精液 = 0
		CFLAG:ARG:手上精液 = 0
	ENDIF
ENDIF
IF !FLAG:時間停止
	TFLAG:運搬 = 0
	IF CFLAG:ARG:口内精液
		PRINTFORML %CALLNAME:ARG%把嘴里残留着的精液\@ ABL:ARG:精液中毒 > 2 ? 喝下去了 # 吐出来了 \@
		CFLAG:ARG:口内精液 = 0
	ENDIF
	IF CFLAG:ARG:脸上精液 || CFLAG:ARG:手上精液
		PRINTFORM %CALLNAME:ARG%把
		IF CFLAG:ARG:脸上精液 && !CFLAG:ARG:手上精液
			PRINTFORM 粘在脸上的
		ELSEIF !CFLAG:ARG:脸上精液 && CFLAG:ARG:手上精液
			PRINTFORM 粘在手上的
		ELSE
			PRINTFORM 粘在脸上和手上的
		ENDIF
		PRINTFORML 精液\@ ABL:ARG:精液中毒 > 2 ? 含进嘴里 # 擦掉了 \@
		CFLAG:ARG:脸上精液 = 0
		CFLAG:ARG:手上精液 = 0
	ENDIF
ENDIF

CALL ADD_TOUCH_CLEAR_CHARA(ARG) ;addition custom code
;潜伏モード解除
SIF GETBIT(CFLAG:ARG:潜伏状態,0)
	CLEARBIT CFLAG:ARG:潜伏状態,0

;-------------------------------------------------
;関数名:ENDUFUFU_ALL
;概　要:全員にENDUFUFU
;人数を返す
;-------------------------------------------------
@ENDUFUFU_ALL
#LOCALSIZE 2
#LOCALSSIZE 1
VARSET LOCAL
;诶嘿嘿结束时统一管理
CALL RECORD_UFUFU_LOG
FOR LOCAL, 0, CHARANUM
	IF CFLAG:LOCAL:诶嘿嘿
		CALL ENDUFUFU(LOCAL)
		LOCAL:1 ++
	ENDIF
NEXT
;潜伏モード解除
SIF GETBIT(FLAG:潜伏,0)
	CALL 潜伏モード終了処理
;----------推倒补丁测试----------
;时停合意模式解除
SIF GETBIT(FLAG:潜伏,3)
	FLAG:潜伏 = 0
;----------推倒补丁测试----------
UFUFU_START_TIME = 0
UFUFU_START_DAY = 0
EXTRA_UFUFU_TIME = 0
UFUFU_START_TYPE = 0
WITHDRAWN_LIST = /
TFLAG:MASTERのＰ挿入中 = 0
TFLAG:102 = 1
RETURN LOCAL:1

;-------------------------------------------------------------
;ARGと你が同性だった場合、両者にレズ（ゲイ）経験を追加する関数
;-------------------------------------------------------------
@ADD_EXP_LESNIAN(UP_EXP,ARG,ARGS)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM UP_EXP
#DIM SAMESEX

;第二引数を省略するとTARGET扱い
SIF !ARG
	ARG = TARGET
IF HETEROSEX(ARG,MASTER)
	SAMESEX = 50 + HETEROSEX(ARG,MASTER)
	EXP:ARG:SAMESEX += UP_EXP
	EXP:PLAYER:SAMESEX += UP_EXP
	IF ARGS != ""
		CALL COLORMESSAGE(@"%EXP_TR(SAMESEX)%＋{UP_EXP}（%CALLNAME:ARG%）",C_YELLOW,2,1)
		CALL COLORMESSAGE(@"%EXP_TR(SAMESEX)%＋{UP_EXP}（%CALLNAME:MASTER%）",C_YELLOW,2,1)
	ENDIF
ENDIF

;----------------------------------------------------
;性別判定用
;----------------------------------------------------
@IS_MALE(ARG:0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF TALENT:ARG:性別 == 2

@IS_FEMALE(ARG:0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF !IS_MALE(ARG:0)

@IS_FUTA(ARG:0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF TALENT:ARG:性別 == 3

;ARG:0番のキャラとARG:1番のキャラが同性かどうかを返す関数
@IS_SAMESEX(ARG:0, ARG:1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF IS_MALE(ARG:0) == IS_MALE(ARG:1)

@HAS_PENIS(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
SIF GETBIT(TALENT:ARG:性別, 1)
	RETURNF 1
RETURNF 0

@HAS_VAGINA(ARG:0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF GETBIT(TALENT:ARG:性別, 0)

@IS_VIRGIN(ARG:0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF TALENT:(ARG:0):処女

@IS_DOUTEI(ARG:0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF !TALENT:ARG:非童貞 && HAS_PENIS(ARG)

;Determines if the player has all their applicable non-anal virginities (vaginal if female, penile if male, vaginal
;and penile if futa)
@MASTER_IS_VIRGIN()
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF (IS_FEMALE(MASTER) && IS_VIRGIN(MASTER)) || (IS_MALE(MASTER) && IS_DOUTEI(MASTER)) || (IS_FUTA(MASTER) && IS_DOUTEI(MASTER) && IS_VIRGIN(MASTER))

;Returns 1 if the character can impregnate others and/or get pregnant
@CAN_REPRODUCE(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
IF !IS_CHILD(ARG)
	RETURNF 1
ELSE
	IF HAS_VAGINA(ARG) && TALENT:ARG:初潮 ;First Menstruation
		RETURNF 1
	ELSEIF HAS_PENIS(ARG) && IS_SPERMARCHE(ARG)
		RETURNF 1
	ELSE
		RETURNF 0
	ENDIF
ENDIF

;-------------------------------------------------
;イベントなどでキレた場合の処理
;-------------------------------------------------
@GET_ANGRY(ARG, 刻印付与)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 刻印付与

CFLAG:ARG:心情不快 = 1
CFLAG:ARG:ブチギレ = 1
BASE:ARG:憤怒 = MAXBASE:ARG:憤怒
TALENT:ARG:心情 = -1
MARK:ARG:反発刻印 = MIN(MARK:ARG:反発刻印 + 刻印付与, 3)
MARK:ARG:反発取得履歴 = MAX(MARK:ARG:反発取得履歴,MARK:ARG:反発刻印)

;FileName_COMMON.ERB ------------------------------- Rev1.00
;酒酔い状態取得
;CALL		USER
;ARG		角色No
;RETURN		酒酔い状態_toSTR
;COMMENT
;-----------------------------------------------------------
@ALCOHOL_FACE(ARG)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
SELECTCASE IS_DRUNK(ARG)
	CASE 酒気_素面
		RETURNF "清醒"
	CASE 酒気_ほろ酔い
		RETURNF "微醺"
	CASE 酒気_酔いどれ
		RETURNF "醉酒"
	CASE 酒気_ぐでぐで
		RETURNF "不省人事"
ENDSELECT

;Returns 0 if the character is sober, 1 if the character is Tipsy, 2 if the character is Drunk, and 3 if the character is Wasted
@IS_DRUNK(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM CHARA
SIF BASE:CHARA:酒気 <= 2
	RETURNF 酒気_素面
SELECTCASE BASE:CHARA:酒気
	CASE IS > (MAXBASE:CHARA:酒気 / 10) * 8
		RETURNF 酒気_ぐでぐで
	;	CASE IS > (MAXBASE:ARG:酒気 / 10) * 7
	;		RETURNF 酒気_へべれけ
	CASE IS > (MAXBASE:CHARA:酒気 / 10) * 5
		RETURNF 酒気_酔いどれ
	CASE IS <= (MAXBASE:CHARA:酒気 / 10) * 5
		RETURNF 酒気_ほろ酔い
ENDSELECT

;-------------------------------------------------
;是／否 の選択肢を表示し入力結果を返す関数
;引数 0=肯定選択肢の文字列、1=否定選択肢の文字列
;戻り値 0=是、1=否
;-------------------------------------------------
@ASK_YN(ARGS:0 = "是", ARGS:1 = "否")
#LOCALSIZE 2
#LOCALSSIZE 1
#DIM PREV_REDRAW
PREV_REDRAW = CURRENTREDRAW()
LOCAL:1 = GETCOLOR()
RESETCOLOR
REDRAW 0
PRINTBUTTON @"[0] %ARGS:0%", 0
PRINTL
PRINTBUTTON @"[1] %ARGS:1%", 1
PRINTL
$INPUT_LOOP
INPUT
IF RESULT != 0 && RESULT != 1
	CLEARLINE 1
	GOTO INPUT_LOOP
ENDIF
REDRAW PREV_REDRAW
SETCOLOR LOCAL:1
RETURN RESULT

[SKIPSTART]
;-------------------------------------------------
;特殊な機能を持つボタンを作成する関数
;ARGは入力される数字、ARG:1に1を設定すると灰色に、
;ARG:2に1を設定するとボタンが黄色、ARG:3に1を設定するとボタンが無効(灰色)
;-------------------------------------------------
@PRINTBUTTON_EX(ARGS:0, ARG,ARG:1,ARG:2,ARG:3, ARG:4, ARG:5 = -1) ;qol custom code ARG:5, override maxwidth
#LOCALSIZE 1
#LOCALSSIZE 1
IF ARG:3
	SETCOLOR C_L_GRAY
	PRINTFORM %ARGS:0%
ELSE
	SIF ARG:2
		SETCOLOR C_YELLOW
	SIF ARG:1
		SETCOLOR C_L_GRAY
	IF ARG:4
		;qol custom code
		SIF ARG:5 < 0
			ARG:5 = MAXWIDTH()

		LOCAL = STRLENS(ARGS)
		PRINT □ 
		PRINTBUTTON @"%ARGS:0%", ARG
		PRINTFORML  □%"-"*((ARG:5-6)-LOCAL)%
	ELSE
		PRINTBUTTON @"%ARGS:0%", ARG
	ENDIF
ENDIF
RESETCOLOR
;-------------------------------------------------
;特殊な機能を持つボタンを作成する関数
;ARGは入力される数字、ARG:1に1を設定すると灰色に、
;ARG:2に1を設定するとボタンが黄色、ARG:3に1を設定するとボタンが無効(灰色)
;-------------------------------------------------
@PRINTBUTTON_EXC(ARGS:0, ARG,ARG:1,ARG:2,ARG:3)
#LOCALSIZE 1
#LOCALSSIZE 1
IF ARG:3
	SETCOLOR C_L_GRAY
	PRINTFORMC %ARGS:0%
ELSE
	SIF ARG:2
		SETCOLOR C_YELLOW
	SIF ARG:1
		SETCOLOR C_L_GRAY
	PRINTBUTTONC @"%ARGS:0%", ARG
ENDIF
RESETCOLOR
[SKIPEND]
;-------------------------------------------------
;Fisher-Yatesアルゴリズムによるシャッフル配列の作成
;専用配列SHAFFLE_ARRAYを使用
;引数 0:配列の長さ（0～ARG:0-1までのシャッフル）
;-------------------------------------------------
@FISHER_YATES_SHAFFLE(ARG:0)
#LOCALSIZE 2
#LOCALSSIZE 1
;シャッフル配列の初期化
SIF !INRANGE(ARG:0 , 1 , 999)
	THROW 引数({ARG:0})が異常です。1～999の範囲で指定してください。
VARSET SHAFFLE_ARRAY , 0
;SHAFFLE_ARRAYの初期化
FOR LOCAL:0 , 0 , ARG:0
	SHAFFLE_ARRAY:(LOCAL:0) = LOCAL:0
NEXT

FOR LOCAL:0 , 0 , ARG:0 - 1
	LOCAL:1 = RAND(LOCAL:0 , ARG:0)
	SWAP SHAFFLE_ARRAY:(LOCAL:0), SHAFFLE_ARRAY:(LOCAL:1)
NEXT
;終端用に-1をセット
SHAFFLE_ARRAY:(ARG:0) = -1
RETURN 0

@NEMUKE()
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL =  TIME + 1440 * DAY - TFLAG:你起床 - TFLAG:198 - CFLAG:MASTER:清醒劑
RETURNF LOCAL

;-------------------------------------------------
;時刻表示(ARG)
;与えられたTIMEをH時MM分表示に変換
;-------------------------------------------------
@时刻表示(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
RETURNF 時刻表示(ARG)
@時刻表示(ARG)
#FUNCTIONS
#DIM HOUR
#DIM MINUTE
#DIMS 時刻
ARG = ABS(ARG) ;custom code
HOUR   = ARG / 60
MINUTE = ARG % 60
SIF HOUR > 24
	HOUR = HOUR - 24
時刻 = %TOSTR(HOUR)%时%TOSTR(MINUTE,"00")%分
RETURNF 時刻

;-------------------------------------------------
;時刻変換(HOUR, MINUTE)
;与えられたHOUR時MINUTE分をTIME用に変換
;念のためにHOURが25だったりMINUTEが61だったりした場合にも対応した
;例）CLOCK_TO_TIME(10, 0)をすると返る数値は600
;eraTWの一日は00:01～24:00の範囲、詳しくは@INFO_GO_NEXTDAYを見よ
;-------------------------------------------------
@CLOCK_TO_TIME(HOUR, MINUTE)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM HOUR
#DIM MINUTE
SIF HOUR == 24 && MINUTE == 0
	RETURNF 24 * 60
HOUR   = (HOUR + MINUTE / 60) % 24
MINUTE = MINUTE % 60
RETURNF HOUR * 60 + MINUTE



;-------------------------------------------------
;TIMESTOP(ARG)
;その角色の時間停止状態を返す
;0=時間停止無（意識あり）　1=時間停止世界入り（意識あり）　2=完全に止められてる（意識がない状態）
;-------------------------------------------------
@TIMESTOP(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
;引数を略すとTARGET
SIF !ARG
	ARG = TARGET
IF !FLAG:70
	LOCAL = 0
ELSEIF CFLAG:ARG:344 == 1
	LOCAL = 1
ELSE
	LOCAL = 2
ENDIF
RETURNF LOCAL

@KIGEN_CHANGE(ARG,確率,傾向)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM 確率
#DIM 傾向
;傾向を1にすると+のみ、-1にすると-のみ
SIF TALENT:ARG:感情缺乏
	確率 /= 2
IF RAND:100 < 確率
	IF !傾向
		IF !RAND:2
			TALENT:ARG:心情 ++
		ELSE
			TALENT:ARG:心情 --
		ENDIF
	ELSE
		TALENT:ARG:心情 += 傾向
	ENDIF
ENDIF
TALENT:ARG:心情 = LIMIT(TALENT:ARG:心情,-1,1)

;-------------------------------------------------
;ARGが討厭男人で你が男なら1
;ARGが討厭女人で你が女なら2を返す
;恋慕愛欲兩面通吃で無効
;-------------------------------------------------
@OTOKOGIRAI(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
;兩面通吃
SIF TALENT:ARG:性別嗜好 == -1
	RETURNF 0
;恋慕
SIF TALENT:ARG:恋慕
	RETURNF 0
SIF TALENT:ARG:愛欲
	RETURNF 0
;討厭男人
IF TALENT:ARG:性別嗜好 & 1 && !(TALENT:PLAYER:2 & 1)
	RETURNF 1
;討厭女人
ELSEIF TALENT:ARG:性別嗜好 & 2 && !(TALENT:PLAYER:2 & 2)
	RETURNF 2
ENDIF
RETURNF 0



;-------------------------------------------------
;劣情察知の个別判定F関数
;-------------------------------------------------
;実績解除しなくても察知できるように
;指令としての劣情察知は使用不可能
@NOTICE_LUST(C_ID)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM C_ID
SIF C_ID == MASTER
	RETURNF 0
SIF TALENT:MASTER:劣情察知
	RETURNF 1
SIF EXP:C_ID:約會経験 + EXP:C_ID:愛情経験 >= 100
	RETURNF 1
RETURNF 0



;-------------------------------------------------
;生理周期可視化の个別判定F関数
;-------------------------------------------------
;実績解除しなくても可視化できるように
@NOTICE_CYCLE(C_ID)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM C_ID
SIF !GETBIT(TALENT:C_ID:性別, 0)
	RETURNF 0
;自分のは把握しとるじゃろ
SIF C_ID == MASTER
	RETURNF 1
SIF FLAG:生理周期可視化
	RETURNF 1
SIF EXP:C_ID:約會経験 + EXP:C_ID:愛情経験 >= 200
	RETURNF 1
RETURNF 0



;-------------------------------------------------
;角色チェックF関数
;-------------------------------------------------
;角色が特定条件に合致しているかどうかを返す
;条件はTYPEごとに指定
;複合した条件に名前をつける時は角色列表クラス関数を増設することにした
;-------------------------------------------------
@CHECK_CHARA(C_ID, TYPE = "存在")
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC TYPE ;判定タイプ、DYNAMICしたので多重呼び出し可能
#DIM C_ID          ;登録番号
SELECTCASE TYPE
;基本条件
CASE "存在"
	RETURNF !CFLAG:C_ID:出禁
CASE "陥落"
	RETURNF (TALENT:C_ID:思慕 || TALENT:C_ID:恋慕 || TALENT:C_ID:愛欲 || TALENT:C_ID:恋人 || TALENT:C_ID:炮友)
CASE "人类"
	RETURNF TALENT:C_ID:人类
CASE "仙人"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "人类")) == "仙人"
CASE "天人"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "人类")) == "天人"
CASE "月人"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "人类")) == "月人"
CASE "魔界人"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "魔界人")) == "魔界人"
CASE "外界人"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "人类")) == "外界人"
CASE "妖怪"
	RETURNF TALENT:C_ID:妖怪
CASE "鬼"
	RETURNF GROUPMATCH(GET_TRIBENAME(C_ID, GETNUM(TALENT, "妖怪")), "鬼", "吸血鬼")
CASE "吸血鬼"
	RETURNF IS_VAMPIRE(C_ID)
CASE "河童"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "妖怪")) == "河童"
CASE "天狗"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "妖怪")) == "天狗"
CASE "妖兽"
	RETURNF GROUPMATCH(GET_TRIBENAME(C_ID, GETNUM(TALENT, "妖怪")), "妖兽", "妖鸟", "妖虫")
CASE "妖精"
	RETURNF TALENT:C_ID:妖精
CASE "神霊"
	RETURNF TALENT:C_ID:神霊
CASE "幽霊"
	RETURNF TALENT:C_ID:幽霊
CASE "付喪神"
	RETURNF TALENT:C_ID:付喪神
CASE "人形"
	RETURNF TALENT:C_ID:人形
CASE "巫女"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "追加種族")) == "巫女"
CASE "魔法使"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "追加種族")) == "魔法使"
CASE "女仆"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "追加種族")) == "女仆"
CASE "蓬莱人"
	RETURNF GET_TRIBENAME(C_ID, GETNUM(TALENT, "追加種族")) == "蓬莱人"
CASE "半人类"
	RETURNF TALENT:C_ID:人类 && (TALENT:C_ID:妖怪 || TALENT:C_ID:妖精 || TALENT:C_ID:神霊 || TALENT:C_ID:幽霊 || TALENT:C_ID:付喪神 || TALENT:C_ID:人形)
CASE "掌握把柄"
;C_IDがMASTERの把柄を握っている
	RETURNF CFLAG:C_ID:掌握把柄
CASE "被掌握把柄"
;C_IDがMASTERに把柄を握られている
	RETURNF CFLAG:C_ID:被掌握把柄
CASE "反発刻印"
	RETURNF (MARK:C_ID:反発刻印 >= 1)
CASE "時姦刻印"
	RETURNF (MARK:C_ID:時姦刻印 >= 1)
CASE "時姦経験"
	RETURNF (EXP:C_ID:時姦経験 >= 1)
CASE "清い交際"
	RETURNF (CFLAG:C_ID:清い交際 >= 1)
CASE "口上実装状況"
	RETURNF (CFLAG:C_ID:口上実装状況 >= 1)
CASE "種付可"
	RETURNF TALENT:C_ID:性別 == 1 && CFLAG:C_ID:妊娠経過日数 == 0 && !INRANGE(CFLAG:C_ID:出産経過日, 1, 59)
CASE "子持ち"
	RETURNF (CFLAG:C_ID:兒童人数 >= 1)
CASE "同室"
	RETURNF (CFLAG:C_ID:現在位置 == CFLAG:MASTER:現在位置)
CASE "全裸"
	;simplified, include all clothing parts, including socks/shoes
	RETURNF SUMARRAY(EQUIP:C_ID:0, 1, 22) == 0
CASE "女性"
	RETURNF TALENT:C_ID:性別 == 1
CASE "妊娠"
	RETURNF TALENT:C_ID:妊娠
CASE "育児中"
	RETURNF TALENT:C_ID:育児中
CASE "扶她"
	RETURNF TALENT:C_ID:性別 == 3
CASE "ケモミミ","動物耳","兽耳"
	RETURNF TALENT:C_ID:動物耳 != 0
CASE "人类耳"
	RETURNF TALENT:C_ID:動物耳 == 0
CASE "非処女"
	SIF TALENT:C_ID:性別 == 2
		RETURNF 0
	RETURNF GROUPMATCH(TALENT:C_ID:処女, 0, -1)
CASEELSE
	THROW 不明なTYPE%TYPE%
ENDSELECT


[SKIPSTART]
;-------------------------------------------------
;角色比較関数（已禁用）
;-------------------------------------------------
;全引数必须
;スマートとは程遠いコピペゴリ押しである
;-------------------------------------------------
@CHARA_COMPARE(VAR_NAME, VAR_ID, CHARA_A, TYPE, CHARA_B)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS VAR_NAME ;比較対象角色変数
#DIM  VAR_ID   ;比較対象角色変数のID
#DIM  CHARA_A  ;比較対象角色Aの登録番号
#DIMS TYPE     ;比較内容
#DIM  CHARA_B  ;比較対象角色Bの登録番号
SELECTCASE VAR_NAME
CASE "ABL"
	SELECTCASE TYPE
	CASE "=="
		RETURNF ABL:CHARA_A:VAR_ID == ABL:CHARA_B:VAR_ID
	CASE ">"
		RETURNF ABL:CHARA_A:VAR_ID > ABL:CHARA_B:VAR_ID
	CASE ">="
		RETURNF ABL:CHARA_A:VAR_ID >= ABL:CHARA_B:VAR_ID
	CASE "<"
		RETURNF ABL:CHARA_A:VAR_ID < ABL:CHARA_B:VAR_ID
	CASE "<="
		RETURNF ABL:CHARA_A:VAR_ID <= ABL:CHARA_B:VAR_ID
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT

CASE "BASE"
	SELECTCASE TYPE
	CASE "=="
		RETURNF BASE:CHARA_A:VAR_ID == BASE:CHARA_B:VAR_ID
	CASE ">"
		RETURNF BASE:CHARA_A:VAR_ID > BASE:CHARA_B:VAR_ID
	CASE ">="
		RETURNF BASE:CHARA_A:VAR_ID >= BASE:CHARA_B:VAR_ID
	CASE "<"
		RETURNF BASE:CHARA_A:VAR_ID < BASE:CHARA_B:VAR_ID
	CASE "<="
		RETURNF BASE:CHARA_A:VAR_ID <= BASE:CHARA_B:VAR_ID
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT

CASE "MAXBASE"
	SELECTCASE TYPE
	CASE "=="
		RETURNF MAXBASE:CHARA_A:VAR_ID == MAXBASE:CHARA_B:VAR_ID
	CASE ">"
		RETURNF MAXBASE:CHARA_A:VAR_ID > MAXBASE:CHARA_B:VAR_ID
	CASE ">="
		RETURNF MAXBASE:CHARA_A:VAR_ID >= MAXBASE:CHARA_B:VAR_ID
	CASE "<"
		RETURNF MAXBASE:CHARA_A:VAR_ID < MAXBASE:CHARA_B:VAR_ID
	CASE "<="
		RETURNF MAXBASE:CHARA_A:VAR_ID <= MAXBASE:CHARA_B:VAR_ID
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT

CASE "EXP"
	SELECTCASE TYPE
	CASE "=="
		RETURNF EXP:CHARA_A:VAR_ID == EXP:CHARA_B:VAR_ID
	CASE ">"
		RETURNF EXP:CHARA_A:VAR_ID > EXP:CHARA_B:VAR_ID
	CASE ">="
		RETURNF EXP:CHARA_A:VAR_ID >= EXP:CHARA_B:VAR_ID
	CASE "<"
		RETURNF EXP:CHARA_A:VAR_ID < EXP:CHARA_B:VAR_ID
	CASE "<="
		RETURNF EXP:CHARA_A:VAR_ID <= EXP:CHARA_B:VAR_ID
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT

CASE "CFLAG"
	SELECTCASE TYPE
	CASE "=="
		RETURNF CFLAG:CHARA_A:VAR_ID == CFLAG:CHARA_B:VAR_ID
	CASE ">"
		RETURNF CFLAG:CHARA_A:VAR_ID > CFLAG:CHARA_B:VAR_ID
	CASE ">="
		RETURNF CFLAG:CHARA_A:VAR_ID >= CFLAG:CHARA_B:VAR_ID
	CASE "<"
		RETURNF CFLAG:CHARA_A:VAR_ID < CFLAG:CHARA_B:VAR_ID
	CASE "<="
		RETURNF CFLAG:CHARA_A:VAR_ID <= CFLAG:CHARA_B:VAR_ID
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT

CASE "PALAM"
	SELECTCASE TYPE
	CASE "=="
		RETURNF PALAM:CHARA_A:VAR_ID == PALAM:CHARA_B:VAR_ID
	CASE ">"
		RETURNF PALAM:CHARA_A:VAR_ID > PALAM:CHARA_B:VAR_ID
	CASE ">="
		RETURNF PALAM:CHARA_A:VAR_ID >= PALAM:CHARA_B:VAR_ID
	CASE "<"
		RETURNF PALAM:CHARA_A:VAR_ID < PALAM:CHARA_B:VAR_ID
	CASE "<="
		RETURNF PALAM:CHARA_A:VAR_ID <= PALAM:CHARA_B:VAR_ID
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT

CASE "TALENT"
	SELECTCASE TYPE
	CASE "=="
		RETURNF TALENT:CHARA_A:VAR_ID == TALENT:CHARA_B:VAR_ID
	CASE ">"
		RETURNF TALENT:CHARA_A:VAR_ID > TALENT:CHARA_B:VAR_ID
	CASE ">="
		RETURNF TALENT:CHARA_A:VAR_ID >= TALENT:CHARA_B:VAR_ID
	CASE "<"
		RETURNF TALENT:CHARA_A:VAR_ID < TALENT:CHARA_B:VAR_ID
	CASE "<="
		RETURNF TALENT:CHARA_A:VAR_ID <= TALENT:CHARA_B:VAR_ID
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT

CASE "TCVAR"
	SELECTCASE TYPE
	CASE "=="
		RETURNF TCVAR:CHARA_A:VAR_ID == TCVAR:CHARA_B:VAR_ID
	CASE ">"
		RETURNF TCVAR:CHARA_A:VAR_ID > TCVAR:CHARA_B:VAR_ID
	CASE ">="
		RETURNF TCVAR:CHARA_A:VAR_ID >= TCVAR:CHARA_B:VAR_ID
	CASE "<"
		RETURNF TCVAR:CHARA_A:VAR_ID < TCVAR:CHARA_B:VAR_ID
	CASE "<="
		RETURNF TCVAR:CHARA_A:VAR_ID <= TCVAR:CHARA_B:VAR_ID
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT

CASE "MARK"
	SELECTCASE TYPE
	CASE "=="
		RETURNF MARK:CHARA_A:VAR_ID == MARK:CHARA_B:VAR_ID
	CASE ">"
		RETURNF MARK:CHARA_A:VAR_ID > MARK:CHARA_B:VAR_ID
	CASE ">="
		RETURNF MARK:CHARA_A:VAR_ID >= MARK:CHARA_B:VAR_ID
	CASE "<"
		RETURNF MARK:CHARA_A:VAR_ID < MARK:CHARA_B:VAR_ID
	CASE "<="
		RETURNF MARK:CHARA_A:VAR_ID <= MARK:CHARA_B:VAR_ID
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT
CASE "DIALOG" ;custom code
	SELECTCASE TYPE
	CASE "=="
		RETURNF DIALOGSTATUS:CHARA_A:0 == DIALOGSTATUS:CHARA_B:0
	CASE ">"
		RETURNF DIALOGSTATUS:CHARA_A:0 > DIALOGSTATUS:CHARA_B:0
	CASE ">="
		RETURNF DIALOGSTATUS:CHARA_A:0 >= DIALOGSTATUS:CHARA_B:0
	CASE "<"
		RETURNF DIALOGSTATUS:CHARA_A:0 < DIALOGSTATUS:CHARA_B:0
	CASE "<="
		RETURNF DIALOGSTATUS:CHARA_A:0 <= DIALOGSTATUS:CHARA_B:0
	CASEELSE
		THROW 不明なTYPE　%TYPE%
	ENDSELECT

CASEELSE
	THROW 不明なVAR_NAME　%VAR_NAME%
ENDSELECT

;コレをコピペして置換すれば項目をふやせる
;CASE "CVAR"
;	SELECTCASE TYPE
;	CASE "=="
;		RETURNF CVAR:CHARA_A:VAR_ID == CVAR:CHARA_B:VAR_ID
;	CASE ">"
;		RETURNF CVAR:CHARA_A:VAR_ID > CVAR:CHARA_B:VAR_ID
;	CASE ">="
;		RETURNF CVAR:CHARA_A:VAR_ID >= CVAR:CHARA_B:VAR_ID
;	CASE "<"
;		RETURNF CVAR:CHARA_A:VAR_ID < CVAR:CHARA_B:VAR_ID
;	CASE "<="
;		RETURNF CVAR:CHARA_A:VAR_ID <= CVAR:CHARA_B:VAR_ID
;	CASEELSE
;		THROW 不明なTYPE　%TYPE%
;	ENDSELECT



;-------------------------------------------------
;ステータス評価表示関数
;-------------------------------------------------
;元は@PRINT_字母表顺序評価
;引数を評価対象数値にすることで汎用化し
;RANK_SCALEの名称をわかりやすくした
;-------------------------------------------------
@PRINT_RANK(VAR_VALUE, RANK_SCALE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM  VAR_VALUE  ;評価対象数値
#DIMS RANK_SCALE ;評価に使用する尺度の種類
#DIM  RANK       ;評価ランク、数が小さいほど優秀
#DIM  RANK_NUM   ;評価尺度に含まれる評価の種数
CALL CALC_RANK(VAR_VALUE, RANK_SCALE, RANK, RANK_NUM)
CALL PRINT_ALPHABET(RANK, RANK_NUM)
[SKIPEND]


;-------------------------------------------------
;ステータス評価算出関数
;-------------------------------------------------
;RANK_SCALEにもとづいてVAR_VALUEの評価を行う
;REFによりRANK/RANK_NUMとして渡された引数の数値を直接書き換える
;-------------------------------------------------
@CALC_RANK(VAR_VALUE, RANK_SCALE, RANK, RANK_NUM)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM      VAR_VALUE  ;評価対象数値
#DIMS     RANK_SCALE ;評価に使用する尺度の種類
#DIM  REF RANK       ;評価ランク、数が小さいほど優秀
#DIM  REF RANK_NUM   ;評価尺度に含まれる評価の種数
SELECTCASE RANK_SCALE
CASE "MAX_50000"
;好感度用
	RANK_NUM = 10
	SELECTCASE VAR_VALUE
	CASE IS >= 50000
		RANK = 0
	CASE IS >= 30000
		RANK = 1
	CASE IS >= 10000
		RANK = 2
	CASE IS >=  5000
		RANK = 3
	CASE IS >=  3000
		RANK = 4
	CASE IS >=  1500
		RANK = 5
	CASE IS >=  1000
		RANK = 6
	CASE IS >=   500
		RANK = 7
	CASE IS >=   100
		RANK = 8
	CASEELSE
		RANK = 9
	ENDSELECT
CASE "MAX_20000"
;信赖度用
	RANK_NUM = 10
	IF !MAGIC_DLC_好感亲密重做
		SELECTCASE VAR_VALUE
		CASE IS >= 20000
			RANK = 0
		CASE IS >= 10000
			RANK = 1
		CASE IS >=  3000
			RANK = 2
		CASE IS >=  1000
			RANK = 3
		CASE IS >=   500
			RANK = 4
		CASE IS >=   150
			RANK = 5
		CASE IS >=   100
			RANK = 6
		CASE IS >=    50
			RANK = 7
		CASE IS >=    10
			RANK = 8
		CASEELSE
			RANK = 9
		ENDSELECT
	ELSE
		SELECTCASE VAR_VALUE
			CASE IS >= 20000
				RANK = 0
			CASE IS >= 8000
				RANK = 1
			CASE IS >=  3000
				RANK = 2
			CASE IS >=  1500
				RANK = 3
			CASE IS >=   700
				RANK = 4
			CASE IS >=   250
				RANK = 5
			CASE IS >=   100
				RANK = 6
			CASE IS >=    50
				RANK = 7
			CASE IS >=    10
				RANK = 8
			CASEELSE
				RANK = 9
		ENDSELECT
	ENDIF
CASE "MAX_99"
;技術系以外のABL
	;数字が縦に並ぶと非常に見にくかったんで意図的にインデントずらしてます
	RANK_NUM = 10
	SELECTCASE VAR_VALUE
	CASE IS >= 99
			RANK = 0
	CASE IS >= 75
			RANK = 1
	CASE IS >= 50
			RANK = 2
	CASE IS >= 20
			RANK = 3
	CASE IS >= 10
			RANK = 4
	CASE IS >=  5
			RANK = 5
	CASE IS >=  3
			RANK = 6
	CASE IS >=  2
			RANK = 7
	CASE IS >=  1
			RANK = 8
	CASEELSE
			RANK = 9
	ENDSELECT
CASE "MAX_6"
;技能系ABL
	RANK_NUM = 7
	RANK = MAX(6 - VAR_VALUE, 0)
CASEELSE
	THROW 不明なRANK_SCALE %RANK_SCALE%
ENDSELECT
RETURNF RANK

[SKIPSTART]
;-------------------------------------------------
;ランク評価表示関数
;-------------------------------------------------
;ランク評価(数字が小さいほど優秀)の内容を字母表顺序表記してカラー表示する
;形式さえあってりゃ別にPRINT_RANKでなくとも使えるぞ
;ARG=1で大文字
;-------------------------------------------------
@PRINT_ALPHABET(RANK, RANK_NUM, ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM RANK
#DIM RANK_NUM
#DIMS CONST STR_RANK10, 10 = "Ex", "SS", " S", " A", " B", " C", " D", " E", " F", " G"
#DIMS CONST STR_RANK7,   7 = "Ex", " S", " A", " B", " C", " D", " E"
#DIMS CONST STR_RANK10_F, 10 = "ＳＳＳ", "ＳＳ", "Ｓ", "Ａ", "Ｂ", "Ｃ", "Ｄ", "Ｅ", "Ｆ", "Ｇ"
#DIMS CONST STR_RANK7_F,   7 = "ＥＸ", "Ｓ", "Ａ", "Ｂ", "Ｃ", "Ｄ", "Ｅ"
{
#DIMS  CONST C_RANK10, 10 =
	"C_P_PURPLE", "C_WHITE", "C_WHITE", "C_PINK", "C_RED",
	"C_ORANGE", "C_YELLOW", "C_GREEN", "C_AQUA", "C_GRAY"
}
#DIMS  CONST C_RANK7,   7 = "C_P_PURPLE", "C_WHITE", "C_PINK", "C_RED", "C_ORANGE", "C_YELLOW", "C_GRAY"
IF ARG
	SELECTCASE RANK_NUM
	CASE 10
		CALL COLOR_PRINT(STR_RANK10_F:RANK, C_RANK10:RANK, 2)
	CASE  7
		CALL COLOR_PRINT(STR_RANK7_F:RANK, C_RANK7:RANK, 2)
	CASEELSE
		THROW 错误的RANK_NUM {RANK_NUM}
	ENDSELECT
ELSE
	SELECTCASE RANK_NUM
	CASE 10
		CALL COLOR_PRINT(STR_RANK10:RANK, C_RANK10:RANK, 2)
	CASE  7
		CALL COLOR_PRINT(STR_RANK7:RANK, C_RANK7:RANK, 2)
	CASEELSE
		THROW 错误的RANK_NUM {RANK_NUM}
	ENDSELECT
ENDIF
RESETCOLOR



;-------------------------------------------------
;色付きPRINT関数
;-------------------------------------------------
;渡した文字を渡した色でPRINTしてRESETCOLOR
;広域変数でDIMってある色名のみ可能（追加は容易）
;-------------------------------------------------
@COLOR_PRINT(ARGS, C_NAME, STR_NUM = 2)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS C_NAME
#DIM  STR_NUM
SIF !STRLENS(ARGS)
	RETURN
SELECTCASE C_NAME
CASE "C_P_PURPLE"
	SETCOLOR C_P_PURPLE
CASE "C_WHITE"
	SETCOLOR C_WHITE
CASE "C_PINK"
	SETCOLOR C_PINK
CASE "C_RED"
	SETCOLOR C_RED
CASE "C_ORANGE"
	SETCOLOR C_ORANGE
CASE "C_YELLOW"
	SETCOLOR C_YELLOW
CASE "C_GREEN"
	SETCOLOR C_GREEN
CASE "C_AQUA"
	SETCOLOR C_AQUA
CASE "C_GRAY"
	SETCOLOR C_GRAY
CASEELSE
	THROW 错误的C_NAME %C_NAME%
ENDSELECT
PRINTFORM %ARGS, STR_NUM%
RESETCOLOR


;-------------------------------------------------
;BASEカラーバー関数
;-------------------------------------------------
;体力などのカラーバー
;-------------------------------------------------
@BASE_BAR(C_ID, BASE_NAME, BAR_LENGTH, COLOR_NAME, OP)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM  C_ID
#DIMS BASE_NAME
#DIM  BAR_LENGTH
#DIMS COLOR_NAME
#DIMS OP
#DIM  BASE_ID
BASE_ID = GETNUM(BASE, BASE_NAME)
SIF BASE_ID < 0
	THROW 存在しないBASE_NAME %BASE_NAME%
IF GROUPMATCH(BASE_ID, 0, 1, 6, 8)
	SIF GETPERCENT(BASE:C_ID:BASE_ID - (BASE_ID == 0 ? 500 # 0), MAXBASE:C_ID:BASE_ID) < 50
		SETCOLOR C_YELLOW
	SIF GETPERCENT(BASE:C_ID:BASE_ID - (BASE_ID == 0 ? 500 # 0), MAXBASE:C_ID:BASE_ID) < 25
		SETCOLOR C_RED
ENDIF
SIF !STRCOUNT(OP, "「種類表記無し」")
	PRINTFORM %BASE_TR(BASE_NAME), 4%
RESETCOLOR
CALL PRINT_COLORBAR(BASE:C_ID:BASE_ID, MAXBASE:C_ID:BASE_ID, BAR_LENGTH, UNICODE(0x2585), UNICODE(0x2585), BARCOLORSET(COLOR_NAME), RESULT:1)
IF !STRCOUNT(OP, "「数字表記無し」")
	IF BUFF:C_ID:BASE_ID > 0
		SETCOLOR C_AQUA
	ELSEIF BUFF:C_ID:BASE_ID < 0
		SETCOLOR C_YELLOW
	ENDIF
	IF !MAGIC_DLC
		PRINTFORM ({BASE:C_ID:BASE_ID,5}/{MAXBASE:C_ID:BASE_ID,5})
	ELSE
		PRINTFORM  ({BASE:C_ID:BASE_ID,7}/{MAXBASE:C_ID:BASE_ID,7})
	ENDIF
	RESETCOLOR
ENDIF
SIF STRCOUNT(OP, "「改行」")
	PRINTFORML

;-------------------------------------------------
;INFO用の改良型BASEカラーバー関数
;-------------------------------------------------
@BASE_BAR_2(C_ID, BASE_NAME, BASE_NAME_SHORT, BAR_LENGTH, COLOR_NAME)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM  C_ID
#DIMS BASE_NAME
#DIMS BASE_NAME_SHORT
#DIM  BAR_LENGTH
#DIMS COLOR_NAME
#DIM  BASE_ID
BASE_ID = GETNUM(BASE, BASE_NAME)
SIF BASE_ID < 0
	THROW 存在しないBASE_NAME %BASE_NAME%
IF BASE_ID == GETNUM(BASE, "精力") && TALENT:C_ID:濃厚精液
	FONTBOLD
	SETCOLOR C_CREAM
	PRINTFORM %BASE_NAME_SHORT%
	FONTREGULAR
	RESETCOLOR
ELSEIF C_ID == MASTER && BASE_ID == GETNUM(BASE, "酒気") && TFLAG:酔い止め
	FONTBOLD
	SETCOLOR C_D_RED
	PRINTFORM %BASE_NAME_SHORT%
	FONTREGULAR
	RESETCOLOR
ELSE
	IF GROUPMATCH(BASE_ID, 0, 1, 6, 8)
		SIF GETPERCENT(BASE:C_ID:BASE_ID - (BASE_ID == 0 ? 500 # 0), MAXBASE:C_ID:BASE_ID) < 50
			SETCOLOR C_YELLOW
		SIF GETPERCENT(BASE:C_ID:BASE_ID - (BASE_ID == 0 ? 500 # 0), MAXBASE:C_ID:BASE_ID) < 25
			SETCOLOR C_RED
	ENDIF
	PRINTFORM %BASE_NAME_SHORT%
	RESETCOLOR
ENDIF
CALL PRINT_COLORBAR(BASE:C_ID:BASE_ID, MAXBASE:C_ID:BASE_ID, BAR_LENGTH, UNICODE(0x2585), UNICODE(0x2585), BARCOLORSET(COLOR_NAME), RESULT:1)
IF BUFF:C_ID:BASE_ID > 0
	SETCOLOR C_AQUA
ELSEIF BUFF:C_ID:BASE_ID < 0
	SETCOLOR C_YELLOW
ENDIF
IF !MAGIC_DLC
	PRINTFORM ({BASE:C_ID:BASE_ID, 5})
ELSE
	IF BASE:C_ID:BASE_ID > 100000
		LOCALS = {BASE:C_ID:BASE_ID/1000}k
	ELSE
		LOCALS = {BASE:C_ID:BASE_ID}
	ENDIF
	PRINTFORM (%LOCALS, 5%)
ENDIF
RESETCOLOR

;-------------------------------------------------
;カラースタンプ関数
;-------------------------------------------------
;特定の記号を特定の色で並べる
;MARKの表示用だけどなんか其他にも使えるでしょう
;ARG=1なら左寄せ
;-------------------------------------------------
@COLOR_STAMP(STAMP_NUM, MAX_NUM, STAMP_STR, STAMP_COLOR, ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM  STAMP_NUM   ;並べるスタンプの数
#DIM  MAX_NUM     ;スタンプの最大数
#DIMS STAMP_STR   ;スタンプに使用する文字列
#DIM  STAMP_COLOR ;スタンプの色
#DIM  STAMP_LENGTH;スタンプ文字列の長さ
STAMP_LENGTH = STRLENS(STAMP_STR)
SIF !STAMP_LENGTH
	THROW STAMP_STRが指定されていません
SIF STAMP_COLOR
	SETCOLOR STAMP_COLOR
SIF !ARG
	PRINTFORM %" " * MAX((MAX_NUM - STAMP_NUM),0) * STAMP_LENGTH%
PRINTFORM %STAMP_STR * STAMP_NUM%
RESETCOLOR
[SKIPEND]

;-------------------------------------------------
;角色抽選関数
;-------------------------------------------------
;角色列表から随机で1名を選出する
;戻り値は登録番号、失敗したら-1
;-------------------------------------------------
@RAND_CHARASELECT(L_TYPE = "主人公以外的角色列表")
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS L_TYPE                       ;抽選対象タイプ
#DIM  DYNAMIC L_ID                         ;列表のクラスID
#DIM  DYNAMIC CHARA_LIST, 人物数量上限 ;作成する列表
#DIM  DYNAMIC CHARA_CNT                    ;角色カウンタ
CALL MAKE_CHARA_LIST("主人公以外的角色列表", L_ID, CHARA_LIST, CHARA_CNT)
SIF !CHARA_CNT
	RETURN -1
RETURN CHARA_LIST:(RAND:CHARA_CNT)



;----------------------------------------------------------------------------------------------------
;角色の登場判定
;とりあえず你は弾くようにした
;----------------------------------------------------------------------------------------------------
@CAN_MEET(CHARA)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
;if it's passerby but passerby hasn't been generated, it'll break. Just ignore the case completely
SIF !CHARA || CHARA == RANDOM_CHARANUM || CHARA >= CHARANUM
	RETURNF 0
SIF !EXISTCSV(CHARA)
	RETURNF 0
SIF CFLAG:CHARA:出禁
	RETURNF 0
SIF CHARA == FLAG:扮演
	RETURNF 0
RETURNF 1

;----------------------------------------------------------------------------------------------------
;現在地点から移動可能な場所へ移動    从当前位置移动到可到达的位置
;部屋から追い出されるときなど    比如被赶出房间的时候
;----------------------------------------------------------------------------------------------------
@GETOUT(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC MAP_ID
TCVAR:ARG:追い出され時刻 = TIME
TCVAR:ARG:追い出され地点 = CFLAG:ARG:現在位置
MAP_ID = GET_CURRENT_MAP(ARG)
FOR LOCAL, MINROOM(MAP_ID), MAXROOM(MAP_ID)
	;全くもってスマートでないが、番号の関係上幽香私室からは露台に追い出されてしまうため个別処理する    虽然一点也不聪明，但因为编号关系，我被幽香私室赶到了露台，所以要单独处理
	IF CFLAG:ARG:現在位置 == P463幽香私室
		CFLAG:ARG:現在位置 = P465大堂
		BREAK
	;bugfix: when the Tsukumo Sisters and Raiko kick you out of their room, move the player to Residential Area    当雷鼓弁弁八桥将你从她们的房间赶出去时，将玩家移动到居住区
	ELSEIF GROUPMATCH(CFLAG:ARG:現在位置, P208雷鼓の部屋, P209八橋の部屋, P210弁々の部屋)
		CFLAG:ARG:現在位置 = P214長屋前
		BREAK
	ELSEIF CAN_MOVE(CFLAG:ARG:現在位置, LOCAL) & 1
		CFLAG:ARG:現在位置 = LOCAL
		BREAK
	ENDIF
NEXT


;----------------------------------------------------------------------------------------------------
;角色の随机選抜関数
;手抜き処理でできるだけ簡素な抽選関数をと思った（RAND_CHARASELECTはちと凝り過ぎ）
;渡した角色は選ばれない
;出禁角色が多すぎると無限ループのおそれがあるのが難点
;難点と是うが全角色対象の抽選では
;極端なプレイングをしなければ問題にはならない、はず
;----------------------------------------------------------------------------------------------------
@RAND_PERSON(CHARA)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM HIT_CHARA
DO
	HIT_CHARA = RAND(1, 人物数量上限)
	SIF !CAN_MEET(HIT_CHARA) || HIT_CHARA == CHARA || TALENT:CHARA:路人
		CONTINUE
	BREAK
LOOP 1
RETURNF HIT_CHARA



;----------------------------------------------------------------------------------------------------
;自宅位置取得関数
;----------------------------------------------------------------------------------------------------
@GET_CHARAHOME(CHARA)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
;MASTERは初期位置決まってる
SIF CHARA == MASTER
	RETURNF CFLAG:CHARA:初期位置
;住み込むフラグ立ってるなら決まってる
SIF CHARA == FLAG:住宿人物
	RETURNF CFLAG:CHARA:初期位置
;marriage custom code
SIF CFLAG:CHARA:LIVE_IN_WIFE
	RETURNF CFLAG:CHARA:初期位置
;住所不定の角色は先に
SIF GROUPMATCH(CSVCFLAG(CHARA, GETNUM(CFLAG, "初期位置")), 0, 99)
	RETURNF -100
RETURNF CSVCFLAG(CHARA, GETNUM(CFLAG, "初期位置"))


;----------------------------------------------------------------------------------------------------
;場所名取得関数
;MAIN_MAP次第で挙動が変わってしまって个人的に気持ち悪い
;問題点：GET_MAP_REPLACEMENTを使用済みのPLACE_IDを受け取ると誤動作≒散步Mapの場所IDは受け取れません
;问题点：当接收到已使用 GET_MAP_REPLACEMENT 函数的 PLACE_ID 时会发生错误操作，这意味着不能传入散步地图的地点ID
;修复：ARG为真时，PID已经已使用 GET_MAP_REPLACEMENT 函数跳转
;----------------------------------------------------------------------------------------------------
@GET_PLACENAME(PLACE_ID, ARG)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM PLACE_ID
#DIM DYNAMIC LOCAL_MAP
;GMF：用局部变量替代轻量级上下文切换
LOCAL_MAP = GET_CURRENT_MAP()
SIF PLACE_ID == SUKIMA(LOCAL_MAP)
	RETURNF "隙间空间"
SIF PLACE_ID == OMANEKIBEYA(LOCAL_MAP)
	RETURNF OMANEKI_PLACE()
IF GET_MAPID(PLACE_ID) == LOCAL_MAP
	RETURNF NAME_FROM_PLACE(PLACE_ID)
ELSE
	RETURNF STR_TR(6000 + (ARG ? PLACE_ID # GET_MAP_REPLACEMENT(PLACE_ID)) / 10)
ENDIF


;----------------------------------------------------------------------------------------------------
;配列抽選F関数
;----------------------------------------------------------------------------------------------------
@ARRAY_HIT(HIT_LIST, LIST_SIZE)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM REF HIT_LIST ;確率分布列表
#DIM     LIST_SIZE;確率分布列表の尺寸
#DIM     CHK_ID   ;判定対象となる要素
#DIM     HIT_RAND ;抽選乱数処理
LOCAL = MAX(SUMARRAY(HIT_LIST, 0, LIST_SIZE),1)
HIT_RAND = RAND(0, LOCAL)
;DEBUGPRINTFORML LIST_SIZE＝{LIST_SIZE}　HIT_RAND{HIT_RAND}
FOR CHK_ID, 0, LIST_SIZE
	SIF HIT_RAND < SUMARRAY(HIT_LIST, 0, CHK_ID + 1)
		RETURNF CHK_ID
NEXT
;THROW 抽選失敗
;Counter抽選失敗によるTHROW落ちが発生してるのでｰ1を返すことにする
RETURNF -1

;----------------------------------------------------------------------------------------------------
;配列RAND化関数
;重複制限無なので適宜FISHER_YATES_SHAFFLEと使い分けて下さい
;----------------------------------------------------------------------------------------------------
@ARRAY_RAND(RAND_ARRAY, ARRAY_SIZE, RAND_MIN, RAND_MAX)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM REF RAND_ARRAY; 対象配列
#DIM     ARRAY_SIZE; 配列尺寸（1以上）
#DIM     RAND_MIN  ; RANDの下限（0以上）
#DIM     RAND_MAX  ; RANDの上限-1、この数自体は抽選に選ばれない（RAND_MIN+1以上）
#DIM     ID_ARRAY  ; ループ用
FOR ID_ARRAY, 0, ARRAY_SIZE
	RAND_ARRAY:ID_ARRAY = RAND(RAND_MIN, RAND_MAX)
NEXT


;----------------------------------------------------------------------------------------------------
;FISHER_YATES_SHAFFLE（F関数版）
;ソースはwikipedia
;CALLF FUNC_FISHER_YATES_SHAFFLE(LOCAL, 10)
;これでLOCAL:0～9に0～9が重複無で代入される
;----------------------------------------------------------------------------------------------------
@FUNC_FISHER_YATES_SHAFFLE(ARRAY, ARRAY_SIZE)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM REF ARRAY   ;対象配列
#DIM ARRAY_SIZE  ;配列の長さ（2以上の数値にして下さい）
#DIM ELE_SHAFFLE ;シャッフル対象要素
VARSET ARRAY, -1
ARRAY:0 = 0
;要素0に対する↓の処理は必ず0になるんだから1以降でおｋ
FOR LOCAL:0, 1, ARRAY_SIZE
	ELE_SHAFFLE = RAND(0, LOCAL:0 + 1)
	;挙動変わらないようならif設けるより素通しの方が処理数少なくなったりせんか？
	;SIF ELE_SHAFFLE != LOCAL:0
	ARRAY:(LOCAL:0) = ARRAY:ELE_SHAFFLE
	ARRAY:ELE_SHAFFLE = LOCAL:0
NEXT


;----------------------------------------------------------------------------------------------------
;複数行自動表示関数
;一種の超簡易アニメーション
;1秒に1行追記していくよ関数だよ
;渡された配列の要素を順番に表示する
;本格的に動かしたいならぱにめーしょん使え
;使用箇所は納茲琳の灵摆查找指令
;----------------------------------------------------------------------------------------------------
@PRINT_GROUP(COMMENT, NUM_COMMENT, TIME_REDRAW = 1000)
#LOCALSIZE 2
#LOCALSSIZE 1
#DIMS REF COMMENT         ;表示する文字列の配列
#DIM  NUM_COMMENT         ;配列の要素数
#DIM  PREV_REDRAW         ;一時退避用
#DIM  TIME_REDRAW         ;行追加の間隔、標準で1秒
#DIM  DYNAMIC NOW_COMMENT ;表示隠蔽制御用
PREV_REDRAW = CURRENTREDRAW()
REDRAW 0
FOR LOCAL:0, 0, NUM_COMMENT
	FOR LOCAL:1, 0, NUM_COMMENT
		IF LOCAL:1 <= NOW_COMMENT
			PRINTFORML %COMMENT:(LOCAL:1)%
		ELSE
			PRINTFORML
		ENDIF
	NEXT
	TWAIT TIME_REDRAW, 0
	NOW_COMMENT ++
	SIF LOCAL:0 < NUM_COMMENT -1
		CLEARLINE NUM_COMMENT
NEXT
REDRAW PREV_REDRAW
WAIT


;----------------------------------------------------------------------------------------------------
;ARG(DAY)を年月日に変換する関数
;----------------------------------------------------------------------------------------------------
@DAY_TO_DATE(ARG)
#FUNCTION
#LOCALSIZE 6
#LOCALSSIZE 1
VARSET LOCAL
;冬之月31日は例外
SIF ARG % 124 == 0
	LOCAL:5 = 1
;年目
LOCAL:1 = ARG / 124
SIF !LOCAL:5
	LOCAL:1 += 1
;月
IF LOCAL:5
	LOCAL:2 = 4
ELSEIF ARG % 124 % 31 == 0
	LOCAL:2 = ARG % 124 / 31
ELSE
	LOCAL:2 = ARG % 124 / 31 + 1
ENDIF
;日
IF LOCAL:5 || ARG % 124 % 31 == 0
	LOCAL:3 = 31
ELSE
	LOCAL:3 = ARG % 124 % 31
ENDIF
LOCAL = LOCAL:1 * 1000 + LOCAL:2 * 100 + LOCAL:3
RETURNF LOCAL

;----------------------------------------------------------------------------------------------------
;ARG(年月日)をDAYに変換する関数
;----------------------------------------------------------------------------------------------------
@DATE_TO_DAY(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
VARSET LOCAL
LOCAL += (ARG / 1000 - 1) * 124
LOCAL += (ARG % 1000 / 100 - 1) * 31
LOCAL += ARG % 1000 % 100
RETURNF LOCAL

;----------------------------------------------------------------------------------------------------
;ARG(DAY)を年月日（曜日）で表示する関数
;ARGが年月日であるならARG:1を真にすること
;----------------------------------------------------------------------------------------------------
@PRINT_DATE(ARG, ARG:1)
#LOCALSIZE 4
#LOCALSSIZE 1
;ARGをDAYと判定
IF !ARG:1
	LOCAL:1 = DAY_TO_DATE(ARG)
	LOCAL:2 = ARG
;ARGを年月日と判定
ELSE
	LOCAL:1 = ARG
	LOCAL:2 = DATE_TO_DAY(ARG)
ENDIF
LOCAL:3 = LOCAL:1 % 1000 / 100
PRINTFORM 第{LOCAL:1 / 1000}年 %GET_MONTH(LOCAL:3)% {LOCAL:1 % 1000 % 100}日（%GET_DAY_CN(LOCAL:2)%）

;向后兼容，新增第二参数为真时输出介词短语
@PRINT_DATE_F(ARG,ARG:1 = 0)
#LOCALSIZE 4
#LOCALSSIZE 1
#FUNCTIONS
LOCAL = DAY_TO_DATE(ARG)
LOCAL:1 = LOCAL / 1000
LOCAL:2 = LOCAL % 1000 / 100
LOCAL:3 = LOCAL % 1000 % 100
SELECTCASE ARG:1
	CASE 0
		LOCALS = 第{LOCAL:1}年 %GET_MONTH(LOCAL:2)%{LOCAL:3}日
	CASEELSE
		LOCALS = 在第{LOCAL:1}年%GET_MONTH(LOCAL:2)%{LOCAL:3}日
ENDSELECT
RETURNF LOCALS

;----------------------------------------------------------------------------------------------------
;複数文字列カウントF関数
;----------------------------------------------------------------------------------------------------
;TARGET_STRに含まれる複数の文字列の数を検索する
;TARGET_STR、KEY_STRともに"/"を仕切りとして複数の単語をカウントする
;TARGET_STR、KEY_STRで一度に渡す単語の種類はそれぞれ100種類未満、語群は1000个未満にすること
;
;動作例）
;TARGET_STR："うさぎ/うさぎ/うさぎ/きつね/ねこ/ねこ/"
;例１）
;　KEY_STR："うさぎ/ねこ/"
;　"うさぎ"が3、"ねこ"が2で合計値の5を返す
;例２）
;　KEY_STR："きつね/ねこ/"
;　"きつね"が1、"ねこ"が2で合計値の3を返す
;例３）
;　KEY_STR："うさぎ/うさぎ/"
;　"うさぎ"が3、の処理を2回分やって6を返す
;----------------------------------------------------------------------------------------------------
@STR_MULTI_COUNT(TARGET_STR, KEY_STR)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS TARGET_STR             ;検索対象文字列
#DIMS KEY_STR                ;検索したい語群、語群の種類の上限は100種類
#DIMS DYNAMIC KEY_LIST, 100  ;分割後の単語列表
#DIM  DYNAMIC KEY_CNT,  100  ;各単語の出現回数
#DIM  DYNAMIC KEY_NUM        ;単語の総数
#DIM          KEY_ID         ;ループ処理用
#DIM  DYNAMIC STR_CNT        ;カウント个数
#DIMS DYNAMIC TEMP_STR, 1000 ;SPLIT用一次配列
#DIM  DYNAMIC TEMP_NUM       ;一次配列で使用した要素数
;念のため
TARGET_STR '= REPLACE(TARGET_STR, "\/+", "/")
CALLF CNT_SPLIT(KEY_STR, KEY_LIST, KEY_CNT, KEY_NUM)

SPLIT TARGET_STR, "/", TEMP_STR, TEMP_NUM
FOR KEY_ID, 0, KEY_NUM
	STR_CNT += MATCH(TEMP_STR, KEY_LIST:KEY_ID, 0, TEMP_NUM) * KEY_CNT:KEY_ID
NEXT
RETURNF STR_CNT


;----------------------------------------------------------------------------------------------------
;带计数功能的 SPLIT 函数
;----------------------------------------------------------------------------------------------------
;・特性：不会污染 RESULT 变量，可以在 #FUNCTION (式中函数) 中安全使用
;・用法：必须使用 CALLF 调用，通过 REF (引用传递) 直接修改传入的数组变量
;・注意：参数是必须的，请注意传入数组(单语列表/出现个数列表)的大小上限！
;・分隔符：固定为 "/"
;・数据结构：除了用于存储字符串的数组外，还需要一个额外的数值数组来存储各单词的出现次数
;
;・返回值1 (REF参数) STR_LIST：被 "/" 分割并去重后的单词列表
;・返回值2 (REF参数) CNT_LIST：各单词对应的出现回数列表
;・返回值3 (REF参数) STR_NUM ：不重复的单词种类总数
;
;・限制：TARGET_STR 一次传递的单词种类建议 <100 种，单词总数建议 <1000 个 (受限于函数内临时数组大小)
;
; 动作示例)
; TARGET_STR："灵梦/灵梦/灵梦/灵梦/魔理沙/魔理沙/早苗/"
;   STR_LIST:0 == "灵梦"
;   STR_LIST:1 == "魔理沙"
;   STR_LIST:2 == "早苗"
;   CNT_LIST:0 == 4   ;"灵梦"出现了4次
;   CNT_LIST:1 == 2   ;"魔理沙"出现了2次
;   CNT_LIST:2 == 1   ;"早苗"出现了1次
;   STR_NUM    == 3   ;"灵梦"、"魔理沙"、"早苗" 合计3个种类
;----------------------------------------------------------------------------------------------------
@CNT_SPLIT(TARGET_STR, STR_LIST, CNT_LIST, STR_NUM)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS         TARGET_STR     ;分割対象STR
#DIMS         NEW_STR        ;更新用一時STR
#DIMS REF     STR_LIST       ;単語列表
#DIM  REF     CNT_LIST       ;各単語の出現个数列表
#DIM  REF     STR_NUM        ;単語の種類の総数
#DIMS DYNAMIC TEMP_STR, 1000 ;SPLIT用一次配列
#DIM  DYNAMIC TEMP_NUM       ;一次配列で使用した要素数
#DIM          TEMP_ID        ;ループ用
VARSET STR_LIST
VARSET CNT_LIST
STR_NUM = 0

SPLIT TARGET_STR, "/", TEMP_STR, TEMP_NUM
FOR TEMP_ID, 0, TEMP_NUM
	;文字長判定
	SIF !STRLENS(TEMP_STR:TEMP_ID)
		CONTINUE
	;重複判定
	SIF FINDELEMENT(STR_LIST, TEMP_STR:TEMP_ID, 0, STR_NUM, 1) != -1
		CONTINUE
	STR_LIST:STR_NUM '= TEMP_STR:TEMP_ID
	CNT_LIST:STR_NUM  = MATCH(TEMP_STR, TEMP_STR:TEMP_ID)
	STR_NUM ++
NEXT

;----------------------------------------------------------------------------------------------------
;同行中の人数を返す函数
;----------------------------------------------------------------------------------------------------
@同行人数()
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF (CHARANUM - 1) - CMATCH(CFLAG:同行 , 0, 1, CHARANUM)
;MASTERが数に入らないのでCHARANUM - 1です

;----------------------------------------------------------------------------------------------------
;DAILY_EV0から切り出し
;主にMAPのYOGORE関連判定
;懸念事項：名無し地点のYOGOREの扱いってどうなってんだろ？
;----------------------------------------------------------------------------------------------------
;拠点Map的污垢の合計値を返す
@SUM_ALL_YOGORE()
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF SUMARRAY(YOGORE, MINROOM(), MAXROOM)

;拠点Map的污垢の平均値を返す
@AVERAGE_YOGORE()
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF SUM_ALL_YOGORE() / ROOM_NUM()

;拠点Mapに存在する地点の数を返す
;名無し地点を考慮してない点に注意
@ROOM_NUM()
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF MAXROOM % 100


;----------------------------------------------------------------------------------------------------
;メッセージ付きでアイテム/円/カリスマを入手したり支払ったりする関数
;とりあえず正の数だけ
;----------------------------------------------------------------------------------------------------
@GET_ITEM(ITEM_NAME, ITEM_NUM, PRINT_TYPE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC ITEM_NAME
#DIM DYNAMIC ITEM_ID
#DIM DYNAMIC ITEM_NUM
#DIM DYNAMIC PRINT_TYPE;表示形式　0=通常　1=L　2=W 3=Silent
IF ISNUMERIC(ITEM_NAME)
	ITEM_ID = TOINT(ITEM_NAME)
ELSE
	ITEM_ID = GETNUM(ITEM, ITEM_NAME)
ENDIF
IF !INRANGE(ITEM_ID, 0, 999)
	PRINTFORMW [错误]GET_ITEM: "%ITEM_NAME%" 不是合法的物品名称
	RETURN 0
ENDIF
CALL QOL_SHOW_SET_ITEM(ITEM_ID, ITEM_NUM, PRINT_TYPE)
RETURN RESULT

;SIF ITEM_NUM <= 0
;	RETURN
;
;ITEM_ID = GETNUM(ITEM, ITEM_NAME)
;IF ITEM_ID == -1
;	PRINTFORML %ITEM_NAME%不存在
;ENDIF
;DEBUGPRINTFORML %ITEM_NAME%取得、ID:{ITEM_ID}
;IF PRINT_TYPE != 3
;	IF ITEM:ITEM_ID >= 99
;		CALL COLORMESSAGE(@"%ITEMNAME:ITEM_ID%没法再多拿了", C_YELLOW, PRINT_TYPE)
;	ELSEIF ITEM:ITEM_ID + ITEM_NUM >= 100
;		CALL COLORMESSAGE(@"%ITEMNAME:ITEM_ID%只能拿满为止", C_YELLOW, PRINT_TYPE)
;	ELSE
;		CALL COLORMESSAGE(@"得到了{ITEM_NUM}个%ITEMNAME:ITEM_ID%", C_YELLOW, PRINT_TYPE)
;	ENDIF
;ENDIF
;ITEM:ITEM_ID += ITEM_NUM
;ITEM:ITEM_ID = LIMIT(ITEM:ITEM_ID + ITEM_NUM, 0, 99)

@LOST_ITEM(ITEM_NAME, ITEM_NUM, PRINT_TYPE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC ITEM_NAME
#DIM DYNAMIC ITEM_ID
#DIM DYNAMIC ITEM_NUM
#DIM DYNAMIC PRINT_TYPE;表示形式　0=通常　1=L　2=W
IF ISNUMERIC(ITEM_NAME)
	ITEM_ID = TOINT(ITEM_NAME)
ELSE
	ITEM_ID = GETNUM(ITEM, ITEM_NAME)
ENDIF
IF !INRANGE(ITEM_ID, 0, 999)
	PRINTFORMW [错误]LOST_ITEM: "%ITEM_NAME%" 不是合法的物品名称
	RETURN 0
ENDIF
CALL QOL_SHOW_SET_ITEM(ITEM_ID, -ITEM_NUM, PRINT_TYPE)
RETURN RESULT

;ITEM_ID = GETNUM(ITEM, ITEM_NAME)
;IF ITEM_ID < 0
;	PRINTFORMW %ITEM_NAME%不存在，请保存日志报错
;	RETURN
;ENDIF
;SIF ITEM:ITEM_ID <= 0
;	RETURN
;SIF PRINT_TYPE != 3
;	CALL COLORMESSAGE(@"失去了{ITEM_NUM}个%ITEMNAME_TR(ITEM_ID)%", C_YELLOW, PRINT_TYPE)
;ITEM:ITEM_ID = MAX(ITEM:ITEM_ID - ITEM_NUM, 0)

@GET_MONEY_YEN(VALUE, PRINT_TYPE)
#DIM VALUE
#DIM PRINT_TYPE;表示形式　0=通常　1=L　2=W
SIF VALUE <= 0
	RETURN
CALL COLORMESSAGE(@"得到了%金額表示(VALUE)%", C_YELLOW, PRINT_TYPE)
IF TALENT:MASTER:黄金律 && MAGIC_DLC_魔法成就 && VALUE > 4
	CALL COLORMESSAGE(@"黄金律使你获得的金钱凭空增加了%金額表示(VALUE * 3 / 10)%!", C_YELLOW, PRINT_TYPE)
	MONEY += VALUE * 3 / 10
ENDIF
MONEY += VALUE

@GET_SPELL_CARD_BONUS_YEN(VALUE, PRINT_TYPE)
#DIM VALUE
#DIM PRINT_TYPE;表示形式　0=通常　1=L　2=W
SIF VALUE <= 0
	RETURN
CALL COLORMESSAGE(@"GET ", 0xFFFFFF, 0)
CALL COLORMESSAGE(@"SpEll ", 0xC8FFFF, 0)
CALL COLORMESSAGE(@"CARd ", 0x64FFDC, 0)
CALL COLORMESSAGE(@"BONUS!!", 0x00FFB4, 1)
CALL COLORMESSAGE(@"        %金額表示(VALUE)%", C_YELLOW, PRINT_TYPE)
IF TALENT:MASTER:黄金律 && MAGIC_DLC_魔法成就 && VALUE > 4
	CALL COLORMESSAGE(@"黄金律使你获得的金钱凭空增加了%金額表示(VALUE * 3 / 10)%!", C_YELLOW, PRINT_TYPE)
	MONEY += VALUE * 3 / 10
ENDIF
MONEY += VALUE

@GET_MONEY_CM(VALUE, PRINT_TYPE)
#DIM VALUE
#DIM PRINT_TYPE;表示形式　0=通常　1=L　2=W
SIF VALUE <= 0
	RETURN
CALL COLORMESSAGE(@"得到了{VALUE}个筹码", C_YELLOW, PRINT_TYPE)
MONEY:2 += VALUE

@LOST_MONEY_YEN(VALUE, PRINT_TYPE)
#DIM VALUE
#DIM PRINT_TYPE;表示形式　0=通常　1=L　2=W
SIF VALUE <= 0
	RETURN
CALL COLORMESSAGE(@"失去了%金額表示(VALUE)%", C_YELLOW, PRINT_TYPE)
MONEY = MAX(0, MONEY - VALUE)

@LOST_MONEY_CM(VALUE, PRINT_TYPE)
#DIM VALUE
#DIM PRINT_TYPE;表示形式　0=通常　1=L　2=W
SIF VALUE <= 0
	RETURN
CALL COLORMESSAGE(@"失去了{VALUE}筹码", C_YELLOW, PRINT_TYPE)
MONEY:2 = MAX(0, MONEY:2 - VALUE)

@PAY_MONEY_YEN(VALUE, PRINT_TYPE)
#DIM VALUE
#DIM PRINT_TYPE;表示形式　0=通常　1=L　2=W
SIF VALUE <= 0
	RETURN
CALL COLORMESSAGE(@"支付了%金額表示(VALUE)%", C_YELLOW, PRINT_TYPE)
MONEY = MAX(0, MONEY - VALUE)

@PAY_MONEY_CM(VALUE, PRINT_TYPE)
#DIM VALUE
#DIM PRINT_TYPE;表示形式　0=通常　1=L　2=W
SIF VALUE <= 0
	RETURN
CALL COLORMESSAGE(@"支付了{VALUE}筹码", C_YELLOW, PRINT_TYPE)
MONEY:2 = MAX(0, MONEY:2 - VALUE)

;角色番号C_IDのABLをMOD_NUM変化させる
@ChangeABL(C_ID, ENUM, MOD_NUM)
#DIM C_ID
#DIMS VARNAME
#DIMS ENUM
#DIMS LABEL
#DIM MOD_NUM
VARNAME = ABL

SIF MOD_NUM == 0 || !INRANGE(C_ID,0,CHARANUM)
	RETURN

LABEL '= QOL_GETENUMNAME(VARNAME,ENUM)
IF LABEL == "NULL"
	PRINTFORMW [错误]参数1 "%ENUM%" 不是合法的[%VARNAME%]类型名称或ID，请保存日志报错
	RETURN
ENDIF
LOCAL = QOL_GETVAR(VARNAME,ENUM,C_ID)
IF LOCAL == 安全失败值
	DEBUGPRINTFORML [警告]尝试读取 %VARNAME%:%ENUM% 失败。
	RETURN 0
ENDIF

IF QOL_SETVAR(VARNAME,ENUM,MAX(LOCAL + MOD_NUM, 0),C_ID)
	CALL COLORMESSAGE(@"%CALLNAME:C_ID%的%LABEL%\@ MOD_NUM > 0 ?上升 # 下降 \@至了{QOL_GETVAR(VARNAME,ENUM,C_ID)}",C_YELLOW,2)
	RETURN 1
ELSE 
	DEBUGPRINTFORML %VARNAME%:%ENUM%赋值失败
ENDIF

;----------------------------------------------------------------------------------------------
; Author: えにゅあら
; Licensed by: CC0
; L_SEEDの値が同じなら同じ結果を返す疑似乱数（ノイズ関数）
; 0～L_SCALEの数値を返す（L_SCALEは含まない）
; L_SCALEの最大値は多分8589934592　計算の都合上これ以上を指定しても切り捨てられるはず
;----------------------------------------------------------------------------------------------
@NOISE(L_SEED, L_SCALE)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM L_SEED
#DIM L_VAL
#DIM L_SCALE
;此函数利用整数溢出高位阶段进行哈希处理
;专门引入了非检查运算函数，以下运算不会触发溢出检查，保持与原溢出行为一致的哈希效果
L_SEED = L_SEED ^ 0x145579EC
L_VAL = (L_SEED >> 13) ^ L_SEED
L_VAL = UNCHECKED_ADD(UNCHECKED_MUL(L_VAL, UNCHECKED_ADD(UNCHECKED_MUL(UNCHECKED_MUL(L_VAL, L_VAL), 60493), 19990303)), 1376312589) & 0x7fffffff
RETURNF UNCHECKED_MUL(L_VAL, L_SCALE) / 0x80000000


; ノイズを二つ加算したものを返す。平均値付近の値が出やすい。出現分布はきれいな三角形。
@NOISE2(L_SEED, L_SCALE)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM L_SCALE
#DIM L_SCALE2
#DIM L_SEED
L_SCALE2 = L_SCALE / 2
L_SCALE = L_SCALE - L_SCALE2
RETURNF NOISE(L_SEED, L_SCALE) + NOISE(L_SEED ^ 0x1D47E2A3, L_SCALE2)


; ノイズを三つ加算したものを返す。平均値付近の値がとても出やすい。出現分布は山なりの曲線(正規分布)。
@NOISE3(L_SEED, L_SCALE)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM L_SCALE
#DIM L_SCALE2
#DIM L_SEED
L_SCALE2 = L_SCALE / 3
L_SCALE = L_SCALE - L_SCALE2*2
RETURNF NOISE(L_SEED, L_SCALE) + NOISE(L_SEED ^ 0xFA7B7E26, L_SCALE2) + NOISE(L_SEED ^ 0x1D47E2A3, L_SCALE2)

;４つ以上加算しても３つ版と分布の形状は変わらず、単に平均値を中心に縮小されたものになるので、省く



; 使えそうか試しに表示してみる
@TEST_PRINTNOISE()
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
PRINTL NOISE1
FOR LOCAL, 0, 100
	PRINTFORML {NOISE(LOCAL, 100)}
NEXT
PRINTL NOISE2
FOR LOCAL, 0, 100
	PRINTFORML {NOISE2(LOCAL, 100)}
NEXT
PRINTL NOISE3
FOR LOCAL, 0, 100
	PRINTFORML {NOISE3(LOCAL, 100)}
NEXT

;お金の单位付きで3桁ごとにカンマ表示（函数名は仮）
@金额表示(ARG)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF MONEYSTR(ARG, "#,##0")

@金額表示(ARG)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF MONEYSTR(ARG, "#,##0")

@三桁区切(ARG)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
RETURNF TOSTR(ARG, "#,##0")

@数え方(ITEM_NO, ITEM_NUM = 1)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM ITEM_NO
#DIM ITEM_NUM
SELECTCASE ITEM_NO
	CASE 627
		RETURNF "瓶"
	CASE 706
		RETURNF "只"
	CASE 707, 708
		RETURNF "条"
	CASEELSE
		RETURNF \@ ITEM_NUM < 10 ? 个 # 个 \@
ENDSELECT


@CHARASORT_EX
#LOCALSIZE 1
#LOCALSSIZE 1
;reminder to update this list whenever new characters are added!!!
;first lists games in numerical order (including characters who debuted in print works), then print works in release order, and finally PC-98 games in order
VARSET RESULT, -1 ;set to -1 to include player character in the list
{
RETURN MASTER,
[[霊夢]], [[魔理沙]],
[[ルーミア]], [[大妖精]], [[チルノ]], [[美鈴]], [[小悪魔]], [[パチュリー]], [[咲夜]], [[レミリア]], [[フラン]],
[[レティ]], [[橙]], [[アリス]], [[リリーＷ]], [[リリーＢ]], [[リリカ]], [[メルラン]], [[ルナサ]], [[妖夢]], [[幽々子]], [[藍]], [[紫]],
[[萃香]],
[[リグル]], [[ミスティア]], [[慧音]], [[てゐ]], [[鈴仙]], [[永琳]], [[輝夜]], [[妹紅]],
[[メディスン]], [[幽香]], [[文]], [[小町]], [[映姫]],
[[静葉]], [[穣子]], [[雛]], [[にとり]], [[椛]], [[早苗]], [[神奈子]], [[諏訪子]],
[[衣玖]], [[天子]],
[[キスメ]], [[ヤマメ]], [[パルスィ]], [[勇儀]], [[さとり]], [[お燐]], [[お空]], [[こいし]],
[[ナズーリン]], [[小傘]], [[一輪]], [[ムラサ]], [[星]], [[白蓮]], [[ぬえ]],
[[はたて]],
[[サニー]], [[ルナ]], [[スター]],
[[響子]], [[芳香]], [[青娥]], [[屠自古]], [[布都]], [[神子]], [[マミゾウ]],
[[こころ]],
[[わかさぎ姫]], [[蛮奇]], [[影狼]], [[弁々]], [[八橋]], [[正邪]], [[針妙丸]], [[雷鼓]],
[[華扇]], [[菫子]],
[[清蘭]], [[鈴瑚]], [[ドレミー]], [[サグメ]], [[クラウンピース]], [[純狐]], [[ヘカーティア]],
[[ラルバ]], [[ネムノ]], [[あうん]], [[成美]], [[舞]], [[里乃]], [[隠岐奈]],
[[女苑]], [[紫苑]],
[[瓔花]], [[潤美]], [[久侘歌]], [[八千慧]], [[磨弓]], [[袿姫]], [[早鬼]],
[[ミケ]], [[たかね]], [[駒草]], [[魅須丸]], [[典]], [[龍]], [[千亦]], [[百々世]],
[[尤魔]],
[[美天]], [[慧ノ子]], [[ちやり]], [[日狭美]], [[残無]],
[[姥芽]], [[魑魅]], [[驯子]], [[维缦]], [[阿梨夜]], [[贝子]], 

[[蓮子]], [[メリー]],
[[朱鷺子]],
[[阿求]],
[[豊姫]], [[依姫]], [[レイセン]],
[[影華扇]],
[[小鈴]],
[[美宵]],
[[瑞霊]],

[[ユウゲンマガン]], [[エリス]], [[サリエル]], [[魅魔]], [[キクリ]], [[コンガラ]],
[[里香]], [[明羅]],
[[エレン]], [[小兎姫]], [[カナ]], [[理香子]], [[ちゆり]], [[夢美]], [[る～こと]],
[[オレンジ]], [[くるみ]], [[エリー]], [[夢月]], [[幻月]],
[[サラ]], [[ルイズ]], [[ユキ]], [[マイ]], [[夢子]], [[神綺]],
[[先代]], [[麟]],
[[モブ子]]
}



;/**************************************************************************************
;Character manipulation functions
;/**************************************************************************************
;-----------------------------------------------------------
;初期化 能力和经验的重置被拆分出去
;-----------------------------------------------------------
@RESET_CHARA(ARG,ARG:1)
#LOCALSIZE 1
#LOCALSSIZE 1

SIF TALENT:MASTER:恋人 == ARG
	TALENT:MASTER:恋人 = 0

FOR LOCAL, 0, VARSIZE("TALENTNAME")
	TALENT:ARG:LOCAL = CSVTALENT(ARG, LOCAL)
NEXT
IF !ARG:1
	FOR LOCAL, 0, VARSIZE("EXPNAME")
		EXP:ARG:LOCAL = CSVEXP(ARG, LOCAL)
	NEXT
	FOR LOCAL, 0, VARSIZE("ABLNAME")
		ABL:ARG:LOCAL = CSVABL(ARG, LOCAL)
	NEXT
	FOR LOCAL, 0, VARSIZE("EXNAME")
		EX:ARG:LOCAL = 0
	NEXT
	FOR LOCAL, 0, VARSIZE("BASENAME")
		BASE:ARG:LOCAL = CSVBASE(ARG, LOCAL)
		MAXBASE:ARG:LOCAL = BASE:ARG:LOCAL
	NEXT
ENDIF

BASE:ARG:射精 = 0
BASE:ARG:母乳 = 0
BASE:ARG:尿意 = 0
BASE:ARG:勃起 = 0
BASE:ARG:ムード = 0
BASE:ARG:怒り = 0
BASE:ARG:酒気 = 0

IF !ARG:1
	CFLAG:ARG:好感度 = 0
	CFLAG:ARG:信赖度 = 0
ENDIF
CFLAG:ARG:掌握把柄 = 0
CFLAG:ARG:被掌握把柄 = 0
CFLAG:ARG:既成事實 = 0
CFLAG:ARG:允许无套 = 0
CFLAG:ARG:面識 = 0
CFLAG:ARG:ダメ出し = 0
CFLAG:ARG:オナバレ = 0
CFLAG:ARG:眠姦発覚 = 0
CFLAG:ARG:睡眠姦 = 0
CFLAG:ARG:烂酔奸 = 0
CFLAG:ARG:バレンタイン = 0
CFLAG:ARG:最後に会った日 = 0
CFLAG:ARG:無自覚妊娠 = 0
CFLAG:ARG:由于妊娠而追加尺寸 = 0
CFLAG:ARG:清い交際 = 0
CFLAG:ARG:出産経過日 = 0
CFLAG:ARG:育児人数 = 0
CFLAG:ARG:身に覚え_母 = 0
CFLAG:ARG:身に覚え_父 = 0
CFLAG:ARG:妊娠自覚状態 = 0
CFLAG:ARG:不會喝酒 = 0
CFLAG:ARG:强行内射 = 0
CFLAG:ARG:妊娠 = 0
CFLAG:ARG:妊娠経過日数 = 0
CFLAG:ARG:兒童人数 = 0
CFLAG:ARG:子宝補正 = 0
CFLAG:ARG:産休中 = 0
CFLAG:ARG:累計膣内精液 = 0
CFLAG:ARG:累計肛門精液 = 0
CFLAG:ARG:累計精飲 = 0
CFLAG:ARG:累計精浴 = 0
CFLAG:ARG:弾幕勝負勝利 = 0 ;reset danmaku win
CFLAG:ARG:斗虫直接勝利 = 0 ;reset bug battle win
CFLAG:ARG:料理勝負勝利 = 0 ;reset cooking competition win
CFLAG:ARG:肛門開発許可 = 0 ;com custom code, reset anal permission
CFLAG:ARG:体目当て = 0 ;reset mirada stone
CFLAG:ARG:デート後イベントフラグ = 0 ;flag that determines if post date events can be triggered
CFLAG:ARG:ヤラせちゃった = 0
CFLAG:ARG:喜欢的体位 = 0

IF CFLAG:ARG:回復速度降低
	;MAXBASE:ARG:体力 += 500
	;MAXBASE:ARG:気力 += 500
	;TALENT:ARG:回復速度 = CFLAG:ARG:回復速度降低 - 2
	CFLAG:ARG:回復速度降低 = 0
ENDIF
IF CFLAG:ARG:罩杯提升 ;&& TALENT:ARG:胸围 > -2
	;TALENT:ARG:胸围 -= CFLAG:ARG:罩杯提升
	CFLAG:ARG:罩杯提升 = 0
ENDIF

;reset custom dialogue CFLAG:1000～1999 TCVAR:350～400
VARSET CFLAG:ARG:0, 0, 1000, 2000
VARSET TCVAR:ARG:0, 0, 350, 400

IF !ARG:1
	MARK:ARG:苦痛刻印 = 0
	MARK:ARG:快楽刻印 = 0
	MARK:ARG:不埒刻印 = 0
	MARK:ARG:反発刻印 = 0
	MARK:ARG:反発取得履歴 = 0
	MARK:ARG:時姦刻印 = 0
ENDIF

;経験
;EX:ARG:噴乳 = 0
;EX:ARG:射精 = 0
;EX:ARG:放尿 = 0
;EX:ARG:射精量 = 0
;EX:ARG:膣内精液 = 0
;EX:ARG:肛門内精液 = 0
;EX:ARG:子宮内精液 = 0
;EX:ARG:精液残量 = 0
;EX:ARG:アナル精液残量 = 0

MASTERNAME:ARG =

UWAKI_KNOWN:ARG:0 =
UWAKI_ACCEPTED:ARG:0 =

;reset FIRSTTIME and ONCE
FOR LOCAL, 0, 4
	CSTR:ARG:LOCAL =
NEXT
CSTR:ARG:処女喪失履歴 =
CFLAG:ARG:処女喪失記念日 = 0
CSTR:ARG:尻穴処女喪失履歴 =
CFLAG:ARG:尻穴処女喪失記念日 = 0
CSTR:ARG:初吻喪失履歴 =
CFLAG:ARG:初吻喪失記念日 = 0
CSTR:ARG:童貞喪失履歴 =
CFLAG:ARG:童貞喪失記念日 = 0
SIF !EXP:ARG:接吻経験
	TALENT:ARG:キス未経験 = 1

CALLF CLEAR_CEVENT(ARG)

CALL PREGNANCY_VAR_RESET(ARG)

IF IS_FUTA(ARG)
	PRINTFORML 是否也将%CALLNAME:ARG%的扶他状态初始化，恢复其原本的性别？
	CALL ASK_YN
	SIF !RESULT
		;TALENT:ARG:性別 = CSVTALENT(NO:ARG, GETNUM(TALENT, "性別"))
		TALENT:ARG:性別 = CSVTALENT(NO:ARG, 2)
ENDIF


VARSET JUEL:ARG:0, 0
VARSET CHILD_BIRTHDAY:ARG:0, 0
VARSET CHILD_WEATHER:ARG:0, ""
VARSET CHILD_SEX:ARG:0, 0
VARSET CHILDNAME:ARG:0, ""

;カスタム素質
VARSET CUSTOM_TALENT:ARG:0, 0
VARSET CUSTOM_TALENT_NAME:ARG:0, ""
VARSET CUSTOM_TALENT_COLOR:ARG:0, 0
VARSET CUSTOM_TALENT_TYPE:ARG:0, 0

;validate and set exp
CALL CHARA_VALIDATE(ARG)
CALL UPDATE_MAIN_UPDATE_ABL(ARG)
CALL UPDATE_MAIN_UPDATE_SEX_EXP(ARG)

;TALENT:ARG:PREGNANCY_TYPE = PREGNANCY_CHARA_DEFAULT(ARG)

SIF 全員ロリ化
	CALL SET_LOLI(ARG)
SIF 全員小人化
	TALENT:ARG:体型 = -5
SIF 全員動物耳化 && !TALENT:ARG:動物耳
	TALENT:ARG:動物耳 = 1
SIF 全員扶她化
	CALL SET_FUTANARI(ARG)
IF 初期貞操:ARG:0 > 0
	CALL VIRGIN_SET_APPLY_PART(ARG, 初期貞操:ARG:0)
ELSEIF 全員の貞操 > 0
	CALL VIRGIN_SET_APPLY_PART(ARG, 全員の貞操)
ENDIF

[SKIPSTART]
;= custom code ======

TRYCALLFORM ADD_EXTENDED_TRAITS_SET_{NO:ARG}(ARG)
CALL CustomTraits(ARG)

VARSET CUSTOM_TALENT_HTML_DESC:ARG:0, ""
VARSET CUSTOM_JOB_DESC:ARG:0, ""
VARSET CUSTOM_CSTR:0, ""
VARSET CUSTOM_PANTY_EXISTS:0, 0
VARSET CUSTOM_PANTY_NAME:0, ""
VARSET CUSTOM_PANTY_DESCRIPTION:0, ""
VARSET CUSTOM_PANTY_DESCRIPTION_ORDER:0, 0
VARSET CUSTOM_PANTY_OPTIONS:0, 0
VARSET CUSTOM_PANTY_DAILY_JUDGMENT:0, 0
VARSET CUSTOM_PANTY_MATCHING_BRA:0, 0
VARSET CUSTOM_PANTY_COLLECTION_DISPLAY:0, 0
VARSET CUSTOM_PANTY_COLLECTION_UNLOCK_TEXT:0, ""
VARSET CUSTOM_BRA_EXISTS:0, 0
VARSET CUSTOM_BRA_NAME:0, ""
VARSET CUSTOM_BRA_DAILY_JUDGMENT:0, 0
VARSET CUSTOM_BRA_FABRIC_THICKNESS:0, 0
;====================
[SKIPEND]

@RESET_PARTS(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
FOR LOCAL, 0, VARSIZE("EXPNAME")
	EXP:ARG:LOCAL = CSVEXP(ARG, LOCAL)
NEXT
FOR LOCAL, 0, VARSIZE("ABLNAME")
	ABL:ARG:LOCAL = CSVABL(ARG, LOCAL)
NEXT
FOR LOCAL, 0, VARSIZE("EXPNAME")
	EXP:ARG:LOCAL = CSVEXP(ARG, LOCAL)
NEXT
TALENT:ARG:ヤリマン = CSVTALENT(ARG,142)
TALENT:ARG:貞操 = CSVTALENT(ARG,30)
TALENT:ARG:性別嗜好 = CSVTALENT(ARG,81)
TALENT:ARG:無知 = CSVTALENT(ARG,27)

;-----------------------------------------------------------
;ふたなり
;-----------------------------------------------------------
@SET_FUTANARI(ARG = -1, NEW_形状)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC NEW_形状

SIF ARG == -1
	ARG = MASTER
SIF !NEW_形状
	NEW_形状 = IS_CHILD(ARG) ? 1 # 3

TALENT:ARG:性別 = 3
TALENT:ARG:形状 = NEW_形状
MAXBASE:ARG:精力 = 1000
SIF BASE:ARG:精力 < 1000
	BASE:ARG:精力 = 1000
MAXBASE:ARG:射精 = 10000
MAXBASE:ARG:勃起 = 1500

@UNSET_FUTANARI(ARG = -1)
#LOCALSIZE 1
#LOCALSSIZE 1
SIF !IS_FUTA(ARG)
	RETURN -1

TALENT:ARG:濃厚精液 = 0
TALENT:ARG:形状 = 0
TALENT:ARG:性別 = 1
BASE:ARG:精力 = 0
MAXBASE:ARG:精力 = 0
BASE:ARG:勃起 = 0
MAXBASE:ARG:勃起 = 0

;-----------------------------------------------------------
;ロリ
;-----------------------------------------------------------
@SET_LOLI(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
TALENT:ARG:バストサイズ = MAX(TALENT:ARG:バストサイズ - 2, -2)
TALENT:ARG:ヒップサイズ = MAX(TALENT:ARG:ヒップサイズ - 2, -2)
TALENT:ARG:体型 = -2

;-----------------------------------------------------------
;処女化
;-----------------------------------------------------------
@SET_VIRGIN(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
TALENT:ARG:処女 = 1
EXP:ARG:Ｃ経験 = 0
EXP:ARG:Ｂ経験 = 0
EXP:ARG:Ｖ経験 = 0
EXP:ARG:Ａ経験 = 0
ABL:ARG:Ｖ感覚 = 0
ABL:ARG:Ａ感覚 = 0
EXP:ARG:Ｖ性交経験 = 0
EXP:ARG:Ｖ拡張経験 = 0
EXP:ARG:子宮口開発経験 = 0
EXP:ARG:Ａ性交経験 = 0
EXP:ARG:Ａ拡張経験 = 0
EXP:ARG:手淫経験 = 0
EXP:ARG:口淫経験 = 0
EXP:ARG:パイズリ経験 = 0
EXP:ARG:キス経験 = 0
EXP:ARG:奉仕快楽経験 = 0
EXP:ARG:愛情経験 = 0
EXP:ARG:苦痛快楽経験 = 0
EXP:ARG:嗜虐快楽経験 = 0
EXP:ARG:露出快楽経験 = 0
EXP:ARG:緊縛経験 = 0
EXP:ARG:絶頂経験 = 0
EXP:ARG:射精経験 = 0
EXP:ARG:噴乳経験 = 0
EXP:ARG:放尿経験 = 0
EXP:ARG:精液経験 = 0
EXP:ARG:精飲経験 = 0
EXP:ARG:出産経験 = 0
TALENT:ARG:キス未経験 = 1
TALENT:ARG:ヤリマン = 0
TALENT:ARG:性別嗜好 = CSVTALENT(ARG,81)
TALENT:ARG:貞操 = CSVTALENT(ARG,30)
ABL:ARG:腰 = CSVABL(ARG, GETNUM(ABL, "腰"))
ABL:ARG:欲望 = CSVABL(ARG, GETNUM(ABL, "欲望"))
;-----------------------------------------------------------
;処女化轻量
;-----------------------------------------------------------
@SET_VIRGIN_LIGHT(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
TALENT:ARG:処女 = 1
EXP:ARG:Ｖ性交経験 = 0
EXP:ARG:Ｖ拡張経験 = 0
EXP:ARG:子宮口開発経験 = 0
EXP:ARG:Ａ性交経験 = 0
EXP:ARG:Ａ拡張経験 = 0
EXP:ARG:精液経験 = 0
EXP:ARG:精飲経験 = 0
EXP:ARG:出産経験 = 0
CSTR:ARG:処女喪失履歴 = 
CSTR:ARG:尻穴処女喪失履歴 = 
;-----------------------------------------------------------
;経験豊富
;-----------------------------------------------------------
@SET_WELL_EXP(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
TALENT:ARG:処女 = 0
SIF TALENT:ARG:性別嗜好 == 1
	TALENT:ARG:性別嗜好 = 0
EXP:ARG:Ｖ経験 = MAX(EXP:ARG:Ｖ経験, 50)
ABL:ARG:Ｖ感覚 = MAX(ABL:ARG:Ｖ感覚, 2)
ABL:ARG:腰 = MAX(ABL:ARG:腰, 2)
EXP:ARG:Ｖ性交経験 = MAX(EXP:ARG:Ｖ経験, 50)
EXP:ARG:Ｖ拡張経験 = MAX(EXP:ARG:Ｖ拡張経験, 3)
EXP:ARG:精液経験 = MAX(EXP:ARG:精液経験, 10)

;-----------------------------------------------------------
;ヤリマン
;-----------------------------------------------------------
@SET_YARIMAN(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
TALENT:ARG:処女 = 0
SIF TALENT:ARG:性別嗜好 == 1
	TALENT:ARG:性別嗜好 = 0
EXP:ARG:Ｖ経験 = MAX(EXP:ARG:Ｖ経験, 100)
ABL:ARG:Ｖ感覚 = MAX(ABL:ARG:Ｖ感覚, 3)
ABL:ARG:腰 = MAX(ABL:ARG:腰, 3)
ABL:ARG:欲望 = 3
EXP:ARG:Ｖ性交経験 = MAX(EXP:ARG:Ｖ経験, 100)
EXP:ARG:Ｖ拡張経験 = MAX(EXP:ARG:Ｖ拡張経験, 3)
EXP:ARG:精液経験 = MAX(EXP:ARG:精液経験, 50)
TALENT:ARG:ヤリマン = 1
TALENT:ARG:貞操 = -1

;/**************************************************************************************
;Game related functions
;/**************************************************************************************
;返回MASTER的恋人是否在场的表达式中函数
;=-1 参数ARG是恋人 =0 原本没有恋人 =1 恋人不在场 =2 恋人同席
;借用了「映姫様口上」中「M_KOJO_K30_関数・お説教ライブラリ」的内容
@FIND_LOVER(ARG = - 1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF ARG == -1
	ARG = TARGET
SIF TALENT:MASTER:恋人 == 0
	RETURNF 0
SIF TALENT:ARG:恋人
	RETURNF -1
SIF GET_TARGETNUM() < 2
	RETURNF 1
FOR LOCAL, 1, GET_TARGETNUM() + 1
	SIF TALENT:(TARGET:LOCAL):恋人
		RETURNF 2
NEXT
RETURNF 1

@IS_LOVER(ARG)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
SIF TALENT:ARG:恋慕
	RETURNF 1
SIF TALENT:ARG:愛欲
	RETURNF 1
RETURNF 0

@MUTUAL_KISS(ARG = -1, ARG:1 = 1, ARG:2 = 1)
#LOCALSIZE 1
#LOCALSSIZE 1
SIF ARG == -1
	ARG = TARGET
CALL ADD_KISS(ARG, ARG:1, ARG:2)

;性交中判定
@IS_SEXING(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF !ARG
	ARG = TARGET
SIF TEQUIP:ARG:Ｖ性交 == MASTER || TEQUIP:ARG:Ａ性交 == MASTER || TEQUIP:MASTER:Ｖ性交 == ARG || TEQUIP:MASTER:Ａ性交 == ARG
	RETURNF 1
RETURNF 0

;==================================================================
;Return 1 if the target is under the effect of an aphrodisiac or in heat
;Borrowed from Kaguya's dialogue
;==================================================================
@IN_HEAT(ARG = -1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF ARG == -1
	ARG = TARGET
RETURNF (TCVAR:TARGET:媚薬 || TCVAR:TARGET:発情)

;==========================================================================
;Returns 1 if the character has any of Yearning, Love, Sex Friend, or Lust
;==========================================================================
@IS_FALLEN(ARG = -1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF ARG == -1
	ARG = TARGET
RETURNF TALENT:ARG:思慕 || TALENT:ARG:恋慕 || TALENT:ARG:セフレ || TALENT:ARG:愛欲

;==========================================================================
;Returns 1 if the character has a hate mark or is angry
;==========================================================================
@IS_ANGRY(ARG = -1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF ARG == -1
	ARG = TARGET
RETURNF MARK:ARG:反発刻印 || CFLAG:ARG:ブチギレ

;==========================================================================
;返回勃起状态的数字的表达式中函数
;=0 普通 =1 微微勃起 =2 非常勃起 =3 Erection!!
;Checks if character is erect
;Returns 0 if character has no penis or is completely flaccid
;Returns 1 if character has no penis or is soft
;Returns 2 if character has penis and is at least Hard
;Returns 3 if character has penis and is ERECT!!
;==========================================================================
@IS_ERECT(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF !HAS_PENIS(ARG) ;Doesn't have a penis
	RETURNF 0
IF BASE:ARG:勃起 >= MAXBASE:ARG:勃起
	RETURNF 3
ELSEIF BASE:ARG:勃起 >= 1000 ;Erection value is equal or bigger than 1000 (Hard)
	RETURNF 2
ELSEIF BASE:ARG:勃起 >= 500 ;(Soft)
	RETURNF 1
ELSE ;(Flaccid)
	RETURNF 0
ENDIF

;returns true if all passed characters are in the same location as MASTER
@IS_HERE(ARG, ARG:1=-1, ARG:2=-1, ARG:3=-1, ARG:4=-1, ARG:5=-1, ARG:6=-1, ARG:7=-1, ARG:8=-1, ARG:9=-1)
#LOCALSIZE 2
#LOCALSSIZE 1
#FUNCTION
LOCAL:1 = FINDELEMENT(ARG, -1)
SIF LOCAL:1 == -1
    LOCAL:1 = 10
FOR LOCAL, 0, LOCAL:1
    SIF CFLAG:MASTER:現在位置 != CFLAG:(ARG:LOCAL):現在位置
        RETURNF 0
NEXT
RETURNF 1

;returns true if all passed characters have been met
@IS_MET(ARG, ARG:1=-1, ARG:2=-1, ARG:3=-1, ARG:4=-1, ARG:5=-1, ARG:6=-1, ARG:7=-1, ARG:8=-1, ARG:9=-1)
#LOCALSIZE 2
#LOCALSSIZE 1
#FUNCTION
LOCAL:1 = FINDELEMENT(ARG, -1)
SIF LOCAL:1 == -1
    LOCAL:1 = 10
FOR LOCAL, 0, LOCAL:1
    SIF !CFLAG:(ARG:LOCAL):面識
        RETURNF 0
NEXT
RETURNF 1

;returns true if all passed characters are not disabled
@IS_ENABLED(ARG, ARG:1=-1, ARG:2=-1, ARG:3=-1, ARG:4=-1, ARG:5=-1, ARG:6=-1, ARG:7=-1, ARG:8=-1, ARG:9=-1)
#LOCALSIZE 2
#LOCALSSIZE 1
#FUNCTION
LOCAL:1 = FINDELEMENT(ARG, -1)
SIF LOCAL:1 == -1
    LOCAL:1 = 10
FOR LOCAL, 0, LOCAL:1
    SIF CFLAG:(ARG:LOCAL):出禁
        RETURNF 0
NEXT
RETURNF 1

;returns true if all passed characters have been met and are not disabled
@IS_MET_ENABLED(ARG, ARG:1=-1, ARG:2=-1, ARG:3=-1, ARG:4=-1, ARG:5=-1, ARG:6=-1, ARG:7=-1, ARG:8=-1, ARG:9=-1)
#LOCALSIZE 2
#LOCALSSIZE 1
#FUNCTION
LOCAL:1 = FINDELEMENT(ARG, -1)
SIF LOCAL:1 == -1
    LOCAL:1 = 10
FOR LOCAL, 0, LOCAL:1
    SIF !CFLAG:(ARG:LOCAL):面識 || CFLAG:(ARG:LOCAL):出禁
        RETURNF 0
NEXT
RETURNF 1

;returns true if MASTER is any of the passed characters
@IS_MASTER(ARG, ARG:1=-1, ARG:2=-1, ARG:3=-1, ARG:4=-1, ARG:5=-1, ARG:6=-1, ARG:7=-1, ARG:8=-1, ARG:9=-1)
#LOCALSIZE 2
#LOCALSSIZE 1
#FUNCTION
LOCAL:1 = FINDELEMENT(ARG, -1)
SIF LOCAL:1 == -1
    LOCAL:1 = 10
FOR LOCAL, 0, LOCAL:1
    SIF FLAG:なりきり == ARG:LOCAL || MASTER == ARG:LOCAL
        RETURNF 1
NEXT
RETURNF 0

;returns true if the weather is bad for being outside
@IS_BAD_WEATHER
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF GROUPMATCH(TIME:5,4,5,6,9,12,13,14,15) || GROUPMATCH(FLAG:異常気象,9,10)
	RETURNF 1
RETURNF 0

;returns true if there's a storm happening
@IS_STORM
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF GROUPMATCH(TIME:5,9,13,14,15) || GROUPMATCH(FLAG:異常気象,9,10)
	RETURNF 1
RETURNF 0

;Determines if a character can visit your home map
;Made this a function to avoid code reduplication
@CAN_VISIT(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
IF FLAG:交流来訪モード == 1 && CFLAG:ARG:好感度 <= 50
	RETURNF 0
ELSEIF FLAG:交流来訪モード == 2 && !TALENT:ARG:思慕 && !IS_LOVER(ARG) && !TALENT:ARG:恋人 && !TALENT:ARG:セフレ
	RETURNF 0
ELSEIF FLAG:交流来訪モード == 3
	RETURNF 0
ELSE
	RETURNF 1
ENDIF

;==================================================
;Counts the number of lovers the player has
;==================================================
@LOVER_COUNT()
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM DYNAMIC LOVERCOUNT
FOR LOCAL,1,CHARANUM
	IF TALENT:LOCAL:恋人
		LOVERCOUNT ++
	ENDIF
NEXT
RETURNF LOVERCOUNT

;种族相关的硬编码函数，已全部移至ERB\ステータス表示関連\PRINT_TALENTNAME.ERB
;但一改动编码，请检查所有判定函数

;==================================================
;Determines if a character believes they still have all their applicable non-anal virginities
;==================================================
@THINKS_IS_VIRGIN(ARG = -1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF ARG == -1
	ARG = TARGET
SIF HAS_VAGINA(ARG)
	RETURNF TALENT:ARG:処女 != 0
RETURNF 0

;@PENILE_VIRGINITY_LOSS_WAS_UNAWARE(ARG = -1)
;#FUNCTION
;SIF ARG == -1
;	ARG = TARGET
;RETURNF STRCOUNT( TOLOWER(CSTR:ARG:童貞喪失履歴), "stolen")

;-------------------------------------------------
;キャラARG:0のバストサイズを数値に変換する
;-2:絶壁～2:爆乳
;-------------------------------------------------
@GET_BUSTSIZE(ARG:0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF IS_MALE(ARG:0)
	RETURNF -2

RETURNF TALENT:ARG:胸围

;==================================================
;number of people in location
;does not include player
;From /egg/ Sakuya's dialogue
;==================================================
@NUMBER_OF_PEOPLE(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM iNumOfPeople

iNumOfPeople = 0
FOR LOCAL,1,CHARANUM
	SIF CFLAG:LOCAL:同室
		iNumOfPeople ++
NEXT
RETURNF iNumOfPeople

;/**************************************************************************************
;Misc helper functions
;/**************************************************************************************
@IS_DEBUG
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
[IF_DEBUG]
	RETURNF 1
[ENDIF]
RETURNF 0

[SKIPSTART]
;-------------------------------------------------
;最大幅の定義関数
;-------------------------------------------------
@MAXWIDTH
#FUNCTION
RETURNF STRLENS(DRAWLINESTR)

;-------------------------------------------------
;Make a start of a section (non-button)
;-------------------------------------------------
@PRINTLINE, ARGS, ARGS:1 = "-"
LOCALS '= @"□ %ARGS:0% □"
PRINTFORM %LOCALS%
IF GETBIT(GETSTYLE(), 0) ;assume bold font allows for ~10.5% less characters on a line (currenly only used by Scouter)
	PRINTFORML %ARGS:1*((MAXWIDTH()-STRLENS(LOCALS)) * 895 / 1000)%
ELSE
	PRINTFORML %ARGS:1*(MAXWIDTH()-STRLENS(LOCALS))%
ENDIF
[SKIPEND]

@PALAM_NUM(ARG, ARG:1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
;999999を超えたらkMGTP
;Emueraではペタ程度（64ビット）まで扱えるとのこと
VARSET LOCALS
FOR LOCAL, 0, 5
	SIF ARG < 1000000
		BREAK
	SELECTCASE LOCAL
	CASE 0
		LOCALS = k
	CASE 1
		LOCALS = M
	CASE 2
		LOCALS = G
	CASE 3
		LOCALS = T
	CASE 4
		LOCALS = P
	ENDSELECT
	ARG /= 1000
NEXT
;数値の桁数に応じて間隔を調整
IF STRLENS(LOCALS)
	LOCALS = {ARG, ARG:1 ? 0 # 5}%LOCALS%
ELSE
	LOCALS = {ARG, ARG:1 ? 0 # 6}
ENDIF

RETURNF LOCALS

;-------------------------------------------------
;Shortcut for usual way of handling paging with infinite scroll
;-------------------------------------------------
@PAGE_HANDLE(nCode, nBackPageCode, nCurPage, nFirstPage, nLastPage)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM nCode
#DIM nBackPageCode
#DIM nCurPage
#DIM nFirstPage
#DIM nLastPage

RETURNF nCode == nBackPageCode ? ROUND_DECREMENT(nCurPage, nFirstPage, nLastPage) # ROUND_INCREMENT(nCurPage, nFirstPage, nLastPage)

;-------------------------------------------------
;指定範囲内で数値をインクリメントさせる関数
;-------------------------------------------------
@ROUND_INCREMENT(OPERAND, MINVAL, MAXVAL)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM OPERAND
#DIM MINVAL
#DIM MAXVAL
OPERAND ++
SIF OPERAND > MAXVAL
	OPERAND = MINVAL

RETURNF OPERAND

;-------------------------------------------------
;指定範囲内で数値をインクリメントさせる関数
;-------------------------------------------------
@ROUND_DECREMENT(OPERAND, MINVAL, MAXVAL)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM OPERAND
#DIM MINVAL
#DIM MAXVAL
OPERAND --
SIF OPERAND < MINVAL
	OPERAND = MAXVAL

RETURNF OPERAND

;---------------------------------------------------------
;10* larger than usable to simulate decimal
;---------------------------------------------------------
;This is to compensate for the engine's tendency to round numbers toward zero.
@ROUND_NEAREST(ARG:0 = 0, ARG:1 = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
IF ARG:0 == 0
	RETURNF ARG:0
ELSEIF ARG:1 == 1 || ARG:1 == 0
	RETURNF ARG:0
ELSEIF ARG:0 > 0
	RETURNF (ARG:0 + (ARG:1 / 2)) / ARG:1
ELSE
	RETURNF (ARG:0 - (ARG:1 / 2)) / ARG:1
ENDIF

;----------------------------------------------------------
;ARG行になるまでラインを追加
;----------------------------------------------------------
@EQUATE_LINE, ARG
#LOCALSIZE 1
#LOCALSSIZE 1
WHILE LINECOUNT < ARG
	PRINTL 
WEND

;-------------------------------------------------
;ARGを(ARG:1 / 100)倍する
;A = MULTIPLY(A, 150)　は　TIMES A, 1.50　と等しい。
;A = MULTIPLY(A, B)　でB <= 0ならば計算結果はマイナスでなくA*1/100とする。
;-------------------------------------------------
@MULTIPLY(ARG, ARG:1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
;0以下の数をかけたときには ARG:1 = 1 とする
SIF ARG:1 <= 0
	ARG:1 = 1
LOCAL = ARG * ARG:1 / 100
RETURNF LOCAL

;-------------------------------------------------
;ARGを(100/ARG:1)倍する
;A = DIVIDE(A, 150)　は　TIMES A, 0.66　と大体等しく、A = A * 100 / 150　と等しい
;A = DIVIDE(A, B)　でB <= 0ならば計算結果は無限やマイナスでなくAのままとなる。
;-------------------------------------------------
@DIVIDE(ARG, ARG:1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
;0以下の数では割れない
SIF ARG:1 <= 0
	RETURNF ARG
LOCAL = ARG * 100 / ARG:1
RETURNF LOCAL

;-------------------------------------------------
;確率判定関数@PERCENT
;式中で使用する関数です。引数％の確率判定を行います。通れば1を返します
;常用する関数ですが、なぜ本体にこれ無いんだろ…。
;-------------------------------------------------
@PERCENT(ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF (ARG - RAND:100) > 0

;-------------------------------------------------------
;get percent from GvT
;-------------------------------------------------------
@GETPERCENT, ARG:0, ARG:1, ARG:2
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
SIF ARG:0 == 0 || ARG:1 == 0
	RETURNF 0 
SIF ARG:2
	RETURNF ROUND_NEAREST(ARG:0*100, ARG:1)
LOCAL = ARG:0*100 / ARG:1
RETURNF LOCAL

;-------------------------------------------------
;範囲判定関数@RANGE
;	引数0：対象の数字
;	引数1～2：範囲の上限・下限（逆順可）
;引数0が引数1と引数2の間に収まっていれば1を返します。ともに同値を許容します
;こいつも本体にあっていいと思う。
;-------------------------------------------------
@RANGE(ARG:0, ARG:1, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
RETURNF INRANGE(ARG:0, MIN(ARG:1, ARG:2), MAX(ARG:1, ARG:2))

;-------------------------------------------------
;dynamically set the padding for text
;-------------------------------------------------
@STRPAD(strIn)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIMS REF strIn
#DIM DYNAMIC nTextPad
;determine text padding by checking the pending item to show
FOR LOCAL, 0, VARSIZE("strIn")
	SIF STRLENS(strIn:LOCAL) > nTextPad
		nTextPad = STRLENS(strIn:LOCAL)
NEXT
RETURNF nTextPad

;-------------------------------------------------
;半角でARG:1文字以上ARG文字以下の文字列入力をさせて、RESULTSでかえす
;-------------------------------------------------
@INPUTS_SELECT, ARG, ARG:1
#LOCALSIZE 1
#LOCALSSIZE 1
INPUTS
SELECTCASE STRLENS(RESULTS)
CASE IS < ARG:1
	IF STRLENS(RESULTS) == 0
		PRINTL You have to input something.
	ELSE
		PRINTFORML %RESULTS% is too short. You have to input at least {ARG:1} symbols, up to {ARG}.
	ENDIF
	RESTART
CASE IS > ARG
	PRINTFORML %RESULTS% is too long {STRLENS(RESULTS)}/{ARG}.
	RESTART
CASEELSE

ENDSELECT

;----------------------------------------------------------------------
;Create a list in a string, e.g: "X, Y and Z"
;Pass a string with the list here and it will do it for you
;Stuff in string should be listed with comma like %", "%
;Modded to it uses full-width commas, i.e. "、"
;Added option to adjust the last connector, enabling the use of " or" instead of " and" 
;----------------------------------------------------------------------
@LIST_IN_STRING(ARGS, strSplit = "、", strLast = "和")
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIMS strSplit
#DIMS strLast
;add spaces
ARGS '= REPLACE(ARGS, strSplit, strSplit + " ")
;clear off the last part
ARGS '= REPLACE(ARGS, strSplit + " $", "")
;use negative lookahead to replace comma before the last item with "AND"
ARGS '= REPLACE(ARGS, "(" + strSplit + ")(?!.*" + strSplit + ")", strLast)
;finally place commas in case splitter wasn't a comma
ARGS '= REPLACE(ARGS, strSplit, ",")

RETURNF ARGS

@LIST_IN_STRING_CN(ARGS, strSplit = "、", strLast = "和")
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIMS strSplit
#DIMS strLast
;clear off the last part
ARGS '= REPLACE(ARGS, strSplit + " $", "")
ARGS '= REPLACE(ARGS, strSplit + "$", "")
;use negative lookahead to replace comma before the last item with "AND"
ARGS '= REPLACE(ARGS, "(" + strSplit + ")(?!.*" + strSplit + ")", strLast)
;finally place commas in case splitter wasn't a comma
ARGS '= REPLACE(ARGS, strSplit, "、")

RETURNF ARGS

;--------------------------------------------------------
;choose only unique non-repeating strings per multiple splits
;in case you want to cram things like "hot and soft", "soft and warm" into one split
;how it works -
;specify the string in the first argument, a string to pad them with in the second argument,
;and amount of times it needs to be output in the third
;for example - SPLIT_M("Fuah:Haah:Fah:Kh:Mmm:Nn:Ah", "...: ", 3) would output
;"Kh... Ah... Fah..." without repeating once, which is equivalent to doing SPLIT_R() three times, except this doesn't repeat
;specify empty space as a split to separate strPad character with spaces
;additionally strPad can be specified with noed command as a split to remove the pad string at the end
;example - SPLIT_M("warm:soft:hot:fuzzy", " and :noed", 2) - "soft and warm", removing " and " at the end
;one thing to watch out for - nCount should never be greater than the split amount, or it will not work due to unique string check
;--------------------------------------------------------
@SPLIT_M(strIn, strPad, nCount)
#FUNCTIONS
#DIM nLoop
#DIM nLoop1
#DIMS strIn
#DIMS DYNAMIC strPad
#DIMS DYNAMIC strTemp, 100
#DIMS DYNAMIC strPrevName, 100
#DIMS DYNAMIC strOutput
#DIM DYNAMIC nCount

SIF STRCOUNT(strIn, ":") + 1 < nCount
	RETURNF "@SPLIT_M has less splits than the amount to output, which will result in an infinite loop (String - " + strIn + "; Amount - " + TOSTR(nCount)

FOR nLoop, 0, nCount
	DO
		;prepare do loop in case of duplicate
		LOCAL = 0
		;generate top part of the name
		strTemp:nLoop '= SPLIT_G(strIn)
		;this thing kinda supposed to make all names unique, ie do not repeat twice within one roll
		FOR nLoop1, 0, nCount
			IF strTemp:nLoop == strPrevName:nLoop1
				LOCAL++
				BREAK
			ENDIF
		NEXT
	LOOP LOCAL
	
	strPrevName:nLoop '= strTemp:nLoop
	
	strOutput = %strOutput%%strTemp:nLoop%%SPLIT_SINGLE(strPad)%\@ SPLIT_CHECK(strPad, " ") && nLoop != nCount-1 ? %BL(1)% # \@
NEXT
;remove the pad symbol at the end if specified
SIF SPLIT_CHECK(strPad, "noed")
	strOutput '= REPLACE(strOutput, SPLIT_SINGLE(strPad) + "$", "")

RETURNF strOutput


@SPLIT_G(ARGS, ARGS:1 = ":")
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 300
VARSET LOCALS
SPLIT ARGS, ARGS:1, LOCALS, LOCAL
RETURNF LOCALS:(RAND:LOCAL)

;--------------------------------------------------------
;searches and returns a specified part of a split string if any part matches (greedy)
;bugfixes-避免污染RESULT
;--------------------------------------------------------
@MATCH_SPLIT(ARGS:0, ARGS:1, ARGS:2 = ":")
#FUNCTIONS
#LOCALSIZE 2
#LOCALSSIZE 300
VARSET LOCALS
SPLIT ARGS:0, ARGS:2, LOCALS, LOCAL:1

FOR LOCAL, 0, LOCAL:1
	SIF STRCOUNT(LOCALS:(LOCAL), ARGS:1)
		RETURNF LOCALS:(LOCAL)
NEXT

;In order to avoid having to write stuff like \@(local:1) == 2?%" "%and the others#\@
@COND_STR(ARGS, CONDITION)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIM CONDITION
IF CONDITION
	RETURNF @"%ARGS%"
ENDIF

@TWENTY_FOUR_TIME(ARG,ARGS = "：")
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
RETURNF TOSTR((ARG % 1440) / 60,"D2")+ARGS+TOSTR((ARG % 1440) % 60,"D2")

;禁用
@VAR_OR_DEFAULT(ARGS, ARG:1 = 0)
#LOCALSIZE 3
#LOCALSSIZE 2
#FUNCTION
LOCAL = STRFIND(ARGS, ":")
IF LOCAL >= 0
	LOCALS '= SUBSTRING(ARGS, 0, LOCAL)
	SIF !GETBIT(EXISTVAR(LOCALS), 0)
		RETURNF ARG:1
	LOCAL:1 = LOCAL + 1
	LOCAL:2 = LOCAL:1
	WHILE LOCAL:2 >= 0
		LOCAL:2 = STRFIND(ARGS, ":", LOCAL:1)
		SIF LOCAL:2 >= 0
			LOCAL:1 = LOCAL:2 + 1
	WEND
	LOCALS:1 '= SUBSTRING(ARGS, LOCAL:1) ;last segment
	IF ISNUMERIC(LOCALS:1)
		RETURNF GETVAR(ARGS)
	ELSE
		LOCAL = 0
		SELECTCASE LOCALS
			CASE "ABL"
				LOCAL = GETNUM(ABL, LOCALS:1)
			CASE "BASE"
				LOCAL = GETNUM(BASE, LOCALS:1)
			CASE "CFLAG"
				LOCAL = GETNUM(CFLAG, LOCALS:1)
			CASE "DAY"
				LOCAL = GETNUM(DAY, LOCALS:1)
			CASE "EQUIP"
				LOCAL = GETNUM(EQUIP, LOCALS:1)
			CASE "EX"
				LOCAL = GETNUM(EX, LOCALS:1)
			CASE "EXP"
				LOCAL = GETNUM(EXP, LOCALS:1)
			CASE "FLAG"
				LOCAL = GETNUM(FLAG, LOCALS:1)
			CASE "ITEM"
				LOCAL = GETNUM(ITEM, LOCALS:1)
			CASE "JUEL"
				LOCAL = GETNUM(JUEL, LOCALS:1)
			CASE "MARK"
				LOCAL = GETNUM(MARK, LOCALS:1)
			CASE "PALAM"
				LOCAL = GETNUM(PALAM, LOCALS:1)
			CASE "SOURCE"
				LOCAL = GETNUM(SOURCE, LOCALS:1)
			CASE "STAIN"
				LOCAL = GETNUM(STAIN, LOCALS:1)
			CASE "TALENT"
				LOCAL = GETNUM(TALENT, LOCALS:1)
			CASE "TCVAR"
				LOCAL = GETNUM(TCVAR, LOCALS:1)
			CASE "TEQUIP"
				LOCAL = GETNUM(TEQUIP, LOCALS:1)
			CASE "TFLAG"
				LOCAL = GETNUM(TFLAG, LOCALS:1)
			CASEELSE
				LOCAL = GETBIT(EXISTVAR(LOCALS), 0) ;may crash, but non-standard variables should not be called by name
		ENDSELECT
		SIF LOCAL >= 0
			RETURNF GETVAR(ARGS)
	ENDIF
ELSE
	SIF GETBIT(EXISTVAR(ARGS), 0)
		RETURNF GETVAR(ARGS)
ENDIF
RETURNF ARG:1

;禁用
@VARS_OR_DEFAULT(ARGS, ARGS:1 = "")
#LOCALSIZE 3
#LOCALSSIZE 2
#FUNCTIONS
LOCAL = STRFIND(ARGS, ":")
IF LOCAL >= 0
	LOCALS '= SUBSTRING(ARGS, 0, LOCAL)
	SIF !GETBIT(EXISTVAR(LOCALS), 1)
		RETURNF ARGS:1
	LOCAL:1 = LOCAL + 1
	LOCAL:2 = LOCAL:1
	WHILE LOCAL:2 >= 0
		LOCAL:2 = STRFIND(ARGS, ":", LOCAL:1)
		SIF LOCAL:2 >= 0
			LOCAL:1 = LOCAL:2 + 1
	WEND
	LOCALS:1 '= SUBSTRING(ARGS, LOCAL:1) ;last segment
	IF ISNUMERIC(LOCALS:1)
		RETURNF GETVARS(ARGS)
	ELSE
		LOCAL = 0
		SELECTCASE LOCALS
			CASE "CSTR"
				LOCAL = GETNUM(CSTR, LOCALS:1)
			CASE "STR"
				LOCAL = GETNUM(STR, LOCALS:1)
			CASEELSE
				LOCAL = 1 ;may crash, but non-standard variables should not be called by name
		ENDSELECT
		SIF LOCAL >= 0
			RETURNF GETVARS(ARGS)
	ENDIF
ELSE
	SIF GETBIT(EXISTVAR(ARGS), 1)
		RETURNF GETVARS(ARGS)
ENDIF

;checks if all values exist within an array
@ARRAY_EXISTS_ALL(ARRAY, START, END, ARG:0, ARG:1, ARG:2 = -1, ARG:3 = -1, ARG:4 = -1, ARG:5 = -1, ARG:6 = -1, ARG:7 = -1, ARG:8 = -1, ARG:9 = -1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM REF ARRAY
#DIM START
#DIM END

FOR LOCAL, 0, 10
	SIF ARG:LOCAL < 0
		RETURNF 1
	SIF FINDELEMENT(ARRAY, ARG:LOCAL, START, END) < 0
		RETURNF 0
NEXT

RETURNF 1

;-------------------------------------------------
;末尾に空行ひとつだけを残した状態にする関数
;Function to leave only one blank line at the end
;-------------------------------------------------
@SINGLE_EMPTY_LINE()
#LOCALSIZE 1
#LOCALSSIZE 1
WHILE 1
	IF HTML_GETPRINTEDSTR(0) != "<p align='left'><nobr> </nobr></p>"
		PRINTL
		BREAK
	ELSEIF HTML_GETPRINTEDSTR(1) == "<p align='left'><nobr> </nobr></p>"
		CLEARLINE 1
	ELSE
		BREAK
	ENDIF
WEND

;-------------------------------------------------
;Get Train command num by its name (original japanese name)
;-------------------------------------------------
@TNUM(ARGS)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
IF GETNUM(TRAINNAME, ARGS) < 0
	PRINTFORML Unrecognized TRAINNAME is being specified for TNUM (%ARGS%)
	;THROW @"Unrecognized TRAINNAME is being specified for TNUM (%ARGS%)"
	RETURNF -1
ENDIF
RETURNF GETNUM(TRAINNAME, ARGS)

;-------------------------------------------------
;Confirm if ARGS equals currently selected Train command (multiple can be specified with / split)
;-------------------------------------------------
@IS_SELECTCOM(ARGS, ARG = 0)
#LOCALSIZE 1
#FUNCTION
#DIM nLoop

SPLIT ARGS, "/", LOCALS, LOCAL
FOR nLoop, 0, LOCAL
	SIF SELECTCOM:ARG == TNUM(LOCALS:nLoop)
		RETURNF SELECTCOM:ARG == TNUM(LOCALS:nLoop)
NEXT

;-------------------------------------------------
;Same as above but for previous command
;-------------------------------------------------
@IS_PREVCOM(ARGS, ARG = 0)
#LOCALSIZE 1
#FUNCTION
#DIM nLoop

SPLIT ARGS, "/", LOCALS, LOCAL
FOR nLoop, 0, LOCAL
	SIF PREVCOM:ARG == TNUM(LOCALS:nLoop)
		RETURNF PREVCOM:ARG == TNUM(LOCALS:nLoop)
NEXT

@COM_TAGS_CHECK(COM_ID = 0, TAG = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM COM_ID
#DIM TAG

RETURNF GETBIT(GETMETH(@"COM{COM_ID}_TAGS", 0), TAG)

@GET_COM_TAGS(COM_ID = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM COM_ID
#DIM TAG

RETURNF GETMETH(@"COM{COM_ID}_TAGS", 0)

@ACTIVITY_TAGS_CHECK(ACTIVITY_ID = 0, TAG = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM ACTIVITY_ID
#DIM TAG

RETURNF GETBIT(GETMETH(@"ACTIVITY{ACTIVITY_ID}_TAGS", 0), TAG)

@GET_ACTIVITY_TAGS(ACTIVITY_ID = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTION
#DIM ACTIVITY_ID
#DIM TAG

RETURNF GETMETH(@"ACTIVITY{ACTIVITY_ID}_TAGS", 0)

;-------------------------------------------------
[SKIPSTART]
; 将 TEXTR 和 HPH_PRINT 合并成一个函数，随机概率显示文章内容
; HPH 显示心形标记，BRK 进行换行
; 使用示例：「ちんぽなんかに絶対に負けない！」BRK「負けまひたあ～～HPH」
; 需要与 @RECORD_WORD 配合使用，推荐写法如下：

LOCALS = 候选文本1
CALL RECORD_WORD(LOCALS)
IF ABL:舌 >= 3
	LOCALS = 候选文本2
	CALL RECORD_WORD(LOCALS)
ENDIF
IF ABL:舌 >= 5
	LOCALS = 候选文本3
	CALL RECORD_WORD(LOCALS)
ENDIF
CALL PRINT_HPH_TEXTR,RECORDED_WORD,"W"
[SKIPEND]
;-------------------------------------------------

@PRINT_HPH_TEXTR(ARGS, 输出形式)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM  DYNAMIC L_RESULT               ; 保存 RESULT 用
#DIM  DYNAMIC L_COUNT               ; 保存 RESULT 用
#DIMS DYNAMIC CHOICES        ; 最终结果
#DIMS DYNAMIC STR_ARRAY, 100 ; 临时储存选择肢
#DIMS DYNAMIC LINES_ARRAY, 10; 临时储存分行后的文本
#DIMS DYNAMIC 输出形式
L_RESULT = RESULT

ARGS '= REPLACE(ARGS, "\/+", "/")
SIF CHARATU(ARGS, STRLENSU(ARGS) - 1) == "/"
	ARGS '= SUBSTRINGU(ARGS, 0, STRLENSU(ARGS) - 1)
SPLIT ARGS, "/", STR_ARRAY, L_COUNT
CHOICES '= STR_ARRAY:(RAND:L_COUNT)
SIF CHOICES == "＿"
	CHOICES =

; 用 BRK 分割成多行
SPLIT CHOICES, "BRK", LINES_ARRAY, L_COUNT
FOR LOCAL,0, L_COUNT
	CALL HPH_PRINT, LINES_ARRAY:LOCAL, 输出形式
NEXT
RESULT = L_RESULT
RECORDED_WORD =


;用于保存 PRINT_HPH_TEXTR 字符串的函数
@RECORD_WORD(ARGS)
#LOCALSIZE 1
#LOCALSSIZE 1
IF RECORDED_WORD == ""
	RECORDED_WORD += ARGS
ELSE
	RECORDED_WORD += "/" + ARGS
ENDIF
