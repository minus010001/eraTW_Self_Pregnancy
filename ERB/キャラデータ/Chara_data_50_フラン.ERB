@CHARAMOVE_DATA_50(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 50
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
		;蕾米莉亜と同様の処理
		IF DAY:2 == 2
			CFLAG:LOCAL:生活時間補正 = 60
		ELSEIF DAY:2 == 4
			CFLAG:LOCAL:生活時間補正 = -60
		ELSE
			CFLAG:LOCAL:生活時間補正 = 0
		ENDIF
	;移動傾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;仓库、使用人部屋には入らない、屋敷外には出ない
			CASE P305仓库, P330使用人部屋, P334廃洋館・入口 TO P348霧之湖・湖底
				RETURN -100
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 正門, 大廳, 食堂, 大図書館, 地下室
				RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
ENDSELECT
@EXIST_キャラデータ50
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ50(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "芙兰")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裤子+兜帽）套")

CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, [[頭装備_帽子]])
CASE "便装：靴"
	CALLF MAKE_INT(V_NAME, [[靴_鞋子]])
CASE "便装：襪子"
	CALLF MAKE_INT(V_NAME, [[襪子_袜子]])
CASE "便装：裙子"
	CALLF MAKE_INT(V_NAME, [[裙子_裙子]])
CASE "便装：上身衣服２"
	CALLF MAKE_INT(V_NAME, [[上身衣服_はだけ可能_女式衬衫]])
CASE "便装：上身衣服１"
	CALLF MAKE_INT(V_NAME, [[上身衣服_はだけ不可_无袖上衣]])

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "略显成熟的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "荷叶边丁字裤")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "胸衣")
CASE "固有内衣着用条件"
	SIF !RAND:(3 - (2 * 勝負内衣判定([[芙兰]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 2)

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "肉料理/油炸食品/蛋糕/丰盛/ジャンキー/淡白/鲜艳/豪快/りんご/怪诞/软绵绵/女僕流/洋食/洋食/甜/甜/甜/")
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "和食/質素/辣/辣/野菜/野菜/野菜/謎料理/謎料理/謎料理/")
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "彩り/刺激的/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "赤/混沌/かわいい/ポップ/洋風/パワフル/ロリータ/" + "血/魔法/人形/" * 3 + "蝠の/悪魔の/紅魔の/禁断の/禁忌の/狂気の/布娃娃/魔法少女の/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "秩序/和風/繊細/知的/" + "水/光/書物/护符/护臂/烟袋/女僕的/" * 3 + "日/鎖の/太陽石の/" * 5)
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_50
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 蔓越莓陷阱
RESULTS = 甜点
CALL 材料設定("草莓", "树莓")
DISH_TASTE = 洋食/甜/甜/甜/ドッキリ味/上品/鲜艳/怪诞/血の魔力/
DISH_COMMENT = 乍一看、似乎只是一种浆果……。
RETURN 1

;IMAGE_I50_特殊服装(CHARA)-------------------------------------------------------------------
;キャラクター特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I50_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS
SELECTCASE CFLAG:50:差し替え適用
	CASE 2
		;可选立绘
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立ち絵
			ENDIF
		ELSE
			;服
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立ち絵
			ENDIF
		ENDIF
	CASE 1
		IF IS_BUNNY(CHARA)
			RESULTS = _バニー
		ENDIF
	CASE 0
		;初始立绘
		IF CHECK_NUDE(CHARA)
			;裸
			SIF BATHROBE_SWITCH && BATHROOM() && !CFLAG:诶嘿嘿
				RESULTS = _浴袍
		ELSE
			;服
		ENDIF		
	CASEELSE
		
ENDSELECT 

@IMAGE_EX_I50_PREリソースチェック(CHARA, 表示形式, 服装, 表情, 表情_マウスオーバー, 差分, 乳量)
#DIM CHARA
#DIMS 表示形式
#DIMS 服装
#DIMS 表情
#DIMS 表情_マウスオーバー
#DIMS 差分
#DIMS 乳量

IF CFLAG:[[フラン]]:差し替え適用 == 1
	CALL COMPOSE_SPRITE_PRESET([[フラン]],"別顔_裸_/別顔_服_/別顔_服_バニー_/","通常/笑顔/憤怒/発情/トロ顔/イキ顔/通常２")
	CALL COMPOSE_SPRITE_PRESET([[フラン]],"別立ち_裸_/別立ち_服_/別立ち_服_バニー_/別立ち_妊娠裸_/別立ち_妊娠服_/別立ち_妊娠服_バニー_/","通常/笑顔/憤怒/発情/トロ顔/イキ顔/通常２",,"別立ち")
ENDIF

@IMAGE_I50_体形(CHARA)
#DIM CHARA
VARSET RESULTS
IF CFLAG:[[フラン]]:差し替え適用 == 1
	;差し替え画像
	IF FLAG:立ち絵種類
		;顔絵
	ELSE
		;立ち絵
		IF TALENT:CHARA:妊娠 && CFLAG:CHARA:妊娠経過日数 >= 41 
			IF CURRENT_CLOTHES_SET(CHARA) == 74
			ELSE
				RESULTS = 妊娠
			ENDIF
		ENDIF
	ENDIF
ENDIF