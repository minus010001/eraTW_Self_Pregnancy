@CHARAMOVE_DATA_4(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 4
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向
	CASE 4
		SELECTCASE ARG:2
			;よく居る
			CASE P11庭
				RETURN 5
			;他所
			CASE P27三妖精之家・大廳 TO P35三妖精之家・地下室, P39夢幻遺跡・浴室, P56阿吽の棲家
				RETURN -100
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 桜花道的鳥居, 神社境内, 神社本堂, 神社走廊, 夢幻遺跡
				RETURN 1
			CASE 広場, 商家町, 呑屋小道
				RETURN 1
			CASE 森林的入口, 香霖堂, 古木的大樹, 霧雨魔法店, 人偶的洋館, 再思之道, 無縁塚
				RETURN 1
			CASE 中有之道, 三途之川, 地獄
				RETURN 1
			CASE 魔界都市, 万魔殿
				RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
ENDSELECT

@EXIST_キャラデータ4
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "魅魔")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裤子）套")

CASE "便装：外搭"
	CALLF MAKE_INT(V_NAME, [[外搭_披肩]])
CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, [[頭装備_帽子]])
CASE "便装：裙子"
	CALLF MAKE_INT(V_NAME, [[裙子_长裙]])
CASE "便装：上身衣服２"
	CALLF MAKE_INT(V_NAME, [[上身衣服_はだけ可能_女式衬衫]])
CASE "便装：其他１"
	CALLF MAKE_INT(V_NAME, [[其他衣装_新月之杖]])

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "嫩肤之上夺目的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "薔薇刺绣胖次")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "蔷薇刺绣胸罩")
CASE "固有内衣着用条件"
	SIF !RAND:(3 - (2 * 勝負内衣判定([[魅魔]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 1)

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "香料/蘑菇/洋食/汁物/咸/简易/淡白/豪華/上品/豪快/软绵绵/赏月/辣/辣/辣/")
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "中華/甜/丰盛/濃厚/質素/魔界風/魔界風/魔界風/")
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "魔力/" * 2)
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "黒/混沌/可怕的/懷舊/知的/威厳/" + "幽霊/魔法/闇/" * 3 + "幽霊の/闇の/斗篷/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "秩序/未来/ロリータ/" + "神霊/护符/护臂/" * 3)

ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_4
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 幸運曲奇
RESULTS = 甜点
CALL 材料設定("甜味料")
DISH_TASTE = 洋食/卵/简易/芸術的/上品/サクサク/幸運/
DISH_COMMENT = 这种曲奇饼里面有一張写着星座运势的纸条。也许能带来好运气?
RETURN 1

;IMAGE_I4_特殊服装(CHARA)-------------------------------------------------------------------
;キャラクター特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I4_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS

SELECTCASE CFLAG:4:差し替え適用
	;差し替え画像
	CASE 2
		IF CHECK_NUDE(CHARA)
		ELSE
			;服
			IF CFLAG:CHARA:睡眠 || CFLAG:CHARA:陪睡中 || 睡眠時間(CHARA)
				RESULTS = _下着
			ELSEIF IS_BUNNY(CHARA)
				RESULTS = _兔女郎
			ELSEIF CHECK_CLO("bra_visible", CHARA)
				RESULTS = _下着
			ENDIF
		ENDIF
	CASE 1
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立ち絵
			ENDIF
		ELSE
			;服
			IF FLAG:立絵種類
				;顔絵
				IF GROUPMATCH(CFLAG:4:差し替え適用, 1)
					IF IS_BUNNY(CHARA)
						RESULTS = _バニー
					ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
						RESULTS = _変T
					ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
						RESULTS = _セーラー
					ELSEIF CHECK_CLO("bra_visible", CHARA)
						RESULTS = _下着
					ENDIF
				ENDIF
			ELSE
				;立ち絵
				IF GROUPMATCH(CFLAG:4:差し替え適用, 1)
					IF CHECK_CLO("bra_visible", CHARA) && EQUIP:CHARA:下半身下着２ != 0
						RESULTS = _下着
					ELSEIF CHECK_CLO("bra_visible", CHARA) && EQUIP:CHARA:下半身下着２ == 0
						RESULTS = _ノーパン下着
					{
					ELSEIF (!EQUIP:CHARA:上半身上着１ && !EQUIP:CHARA:上半身上着２ && !EQUIP:CHARA:外搭 &&
							!EQUIP:CHARA:和服 && !EQUIP:CHARA:連衣裙 &&
							!EQUIP:CHARA:レオタード && !EQUIP:CHARA:ボディースーツ) &&
							EQUIP:CHARA:下半身内衣２ != 0
					}
						RESULTS = _ノーブラ下着
					ELSEIF IS_BUNNY(CHARA)
						RESULTS = _バニー
					ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
						RESULTS = _変T
					ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
						RESULTS = _セーラー
					ENDIF
				ENDIF
			ENDIF
		ENDIF
	CASEELSE
		;通常画像
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立ち絵
			ENDIF
		ELSE
			;服
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立ち絵
			ENDIF
		ENDIF
ENDSELECT

;IMAGE_I4_特殊表情(CHARA)--------------------------------------------------------------------
;角色特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"通常","笑顔","憤怒","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I4_特殊表情(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA

IF CFLAG:4:差し替え適用 == 2
	IF TALENT:CHARA:心情 == 1 || BASE:CHARA:情緒 > MAXBASE:CHARA:情緒 / 2 || TALENT:CHARA:恋慕
		IF RESULTS:0 == "通常"
			RESULTS:0 '= "微笑"
		ENDIF
		IF RESULTS:1 == "通常"
			RESULTS:1 '= "微笑"
		ENDIF
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I4_特殊表情COM(CHARA, 指令)-------------------------------------------------------
;直前の指令で変化する角色特有の表情表示条件
;時間停止中は変化しない表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"自慰","愛撫","性交"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I4_特殊表情COM(CHARA, 指令)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM 指令

SELECTCASE CFLAG:4:差し替え適用
	;替换用画像
	CASE 2
		IF CHECK_NUDE(CHARA)
		ELSE
			;服
			IF IS_BUNNY(CHARA)
			ELSEIF CHECK_CLO("bra_visible", CHARA)
			ELSE
				SELECTCASE 指令
					CASE 305
						RESULTS = 膝枕覆盖
					CASE 6,12,313
						RESULTS = 揉胸覆盖
				ENDSELECT
			ENDIF
		ENDIF
	CASE 1
	CASEELSE
		;通常画像
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
ENDSELECT
