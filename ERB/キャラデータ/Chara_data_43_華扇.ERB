@CHARAMOVE_DATA_43(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 43
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;あまり入らない部屋
			CASE P725仙人的宅邸・大鷲的房間, P728仙人的宅邸・空的部屋
				RETURN -5
			;各私室
			CASE P704河童の宣傳站, P706厄神的家, P716秋姉妹的家・土間 TO P719秋姉妹的家・座敷
				RETURN -100
			CASE P733正邪的隠匿処, P744合欢宅邸, P737招财猫的家, P740山童の宣傳站, P742美天居所
				RETURN -100
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 桜花道的鳥居, 神社境内, 神社走廊
				RETURN 1
			CASE 広場, 商家町, 呑屋小道
				RETURN 1
			CASE 山麓樹海, 玄武之澤, 大蛤蟆之池, 仙人的宅邸, 森の深奥
				RETURN 1
			CASE 九天瀑布, 絶景之丘, 山之湖, 守矢神社境内
				RETURN 1
			;addition custom code, Kourindou
			CASE 香霖堂
				RETURN 1
		ENDSELECT
	;引力設定
	CASE 8
		;平日は3分の2で出步行の
		IF YOUBI_MATCH(DAY, "月火水木金") && INRANGE(TIME, 600, 960)
			SELECTCASE CFLAG:LOCAL:今日の運勢 % 3
			CASE 1
				TCVAR:LOCAL:引力点 = P703玄武之澤
			CASE 2
				TCVAR:LOCAL:引力点 = P711休憩所
			CASEELSE
				TCVAR:LOCAL:引力点 = 0
			ENDSELECT
		ENDIF
ENDSELECT

@EXIST_キャラデータ43
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ43(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "華扇")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（襦袢）套")

CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, [[頭装備_束发簪]])
CASE "便装：靴"
	CALLF MAKE_INT(V_NAME, [[靴_鞋子]])
CASE "便装：襪子"
	CALLF MAKE_INT(V_NAME, [[襪子_袜子]])
CASE "便装：連衣裙"
	CALLF MAKE_INT(V_NAME, [[連衣裙_道士服]])
CASE "便装：裙子"
	CALLF MAKE_INT(V_NAME, [[裙子_裙子]])
CASE "便装：其他１"
	CALLF MAKE_INT(V_NAME, [[其他衣装_绷带]])
CASE "便装：其他２"
	CALLF MAKE_INT(V_NAME, [[其他衣装_拘束具]])

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "淡粉色的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "褶边短裤")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "褶边胸罩")
CASE "固有内衣着用条件"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 2)

;;addition custom code
;CASE "固有下着SHIFTABLE"
;	CALLF MAKE_INT(V_NAME, 1)

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "肉料理/米/中華/油炸食品/下酒菜/丰盛/濃厚/上品/鲜艳/豪快/软糯的/仙術/地底風/地獄風/甜/甜/咸/咸/")
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "淡白/酸/酸/冥界風/冥界風/冥界風/" + "謎料理/小人族秘伝の味/" * 5)
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "浓郁/得意料理＊中華＊/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "赤/秩序/美的/大陸風/知的/" + "動物/食器/生体品/鎖の/幻獣の/ドラゴンの/酒/" * 3 + "鬼の/龍の/仙人の/鬼力の/茨の/華の/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "混沌/ポップ/ロリータ/" + "機械/" * 3 + "河童の/天狗の/" * 10)

;特殊衣装設定、おうちデートで稀にみこかせん
CASE "おうちデート衣装"
	SIF MAIN_MAP == 0 && !RAND:15
		CALLF MAKE_INT(V_NAME, 101)
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_43
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 中華饅
RESULTS = 主食
CALL 材料設定("獣肉", "竹笋", "洋葱", "卷心菜", "白菜", "魚")
DISH_TASTE = 中華/獣肉/野菜/咸/丰盛/豪快/温暖/软糯的/鬼の魔力/
DISH_COMMENT = 这种中式馒头除了肉以外,还加入了各种不同的馅料。所有的馅料都塞得满满當當的在面皮里。
;めっちゃ量出来て料理に時間かかるし腹持ちもすごいっていうのを表現したいけど数値どのくらいがいいかわかんないから保留
RETURN 1

;IMAGE_I43_特殊服装(CHARA)-------------------------------------------------------------------
;角色特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I43_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS

IF CFLAG:43:差し替え適用 == 4
	IF CHECK_NUDE(CHARA)
		IF IS_BUNNY(CHARA)
			RESULTS = _逆兔女郎
		ELSEIF GROUPMATCH(1, TEQUIP:CHARA:縄, TEQUIP:CHARA:拘束)
			RESULTS = _绳子
		ELSEIF GROUPMATCH(1, TEQUIP:CHARA:新妻PLAY)
			RESULTS = _围裙
		ENDIF
	ELSE
		;服
		IF CURRENT_CLOTHES_SET(CHARA) == 18
			RESULTS = _鬼套装
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[晚宴礼服套装]]
			RESULTS = _晚礼服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[哥特洛丽塔套装]]
			RESULTS = _哥特洛丽塔
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 60
			RESULTS = _白衬衫
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 55
			RESULTS = _兔女郎
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 66
			RESULTS = _逆兔女郎
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
			RESULTS = _水手服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 33
			RESULTS = _睡裙
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 34
			RESULTS = _睡衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 58
			RESULTS = _网球服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 68
			RESULTS = _浴衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[西式职业女装]]
			RESULTS = _ol制服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[花嫁盛装]]
			RESULTS = _花嫁
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[保健室的女教师套]]
			RESULTS = _保健老师
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[圣诞套]]
			RESULTS = _圣诞服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[修女服]]
			RESULTS = _修女服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[赛车女郎套装]]
			RESULTS = _赛车女郎
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[女牛仔套装]]
			RESULTS = _牛仔服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[啦啦队制服]]
			RESULTS = _啦啦队制服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[白衣天使套]]
			RESULTS = _护士服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[大正风男学生制服]]
			RESULTS = _学兰服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[一套執事服]]
			RESULTS = _管家服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[一套女僕服]]
			RESULTS = _女佣服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[婚紗套]]
			RESULTS = _婚纱
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[生足美鈴套]]
			RESULTS = _旗袍
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[风纪委员长套装]]
			RESULTS = _风纪委员
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[巫女装束]]
			RESULTS = _巫女服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[热带风情休闲套装]]
			RESULTS = _沙滩装
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[童贞杀手毛衣]]
			RESULTS = _高领毛衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 80
			RESULTS = _西装制服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[舞娘套装]]
			RESULTS = _舞女服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[女仆装]]
			RESULTS = _女仆装
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
			RESULTS = _怪T
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 43
			RESULTS = _浴巾
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 29
			RESULTS = _竞技泳衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 28
			RESULTS = _连衣裙泳衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 27
			RESULTS = _Y字泳衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 87
			RESULTS = _暗杀者
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 30
			RESULTS = _死库水
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[日式学校运动服]]
			RESULTS = _布鲁马
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[辣妹系休闲套装]]
			RESULTS = _热裤
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 52 || CURRENT_CLOTHES_SET(CHARA) == 53 || TEQUIP:CHARA:SWIMMING
			RESULTS = _比基尼
		ELSEIF CHECK_CLO("bra_visible", CHARA)
			RESULTS = _内衣
		ENDIF
	ENDIF
ELSEIF CFLAG:43:差し替え適用 == 3
	;替换用画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
	ELSE
		;服
		IF CURRENT_CLOTHES_SET(CHARA) == [[巫女装束]]
			RESULTS = _巫女服
		ELSEIF CHECK_CLO("bra_visible", CHARA) || TEQUIP:CHARA:SWIMMING
			RESULTS = _泳衣
		ELSEIF DAY:2 == 4
			RESULTS = _冬服
		ENDIF
	ENDIF
ELSEIF CFLAG:43:差し替え適用 == 2
	;替换用画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
	ELSE
		;服
		IF IS_BUNNY(CHARA)
			IF FLAG:立絵種類
				;顔絵
				RESULTS = _バニー
			ELSE
				;立絵
				RESULTS = _バニー
			ENDIF
		ENDIF
	ENDIF
ELSEIF CFLAG:43:差し替え適用 == 1
	;替换用画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF CURRENT_CLOTHES_SET(CHARA) == 66
			RESULTS = _兔女郎
		ENDIF
	ELSE
		;服
		IF CFLAG:CHARA:睡眠 || CFLAG:CHARA:陪睡中 || 睡眠時間(CHARA)
			RESULTS = _睡衣
		ELSEIF GROUPMATCH(1, TEQUIP:CHARA:新妻PLAY)
			RESULTS = _白无垢
		ELSEIF IS_BUNNY(CHARA)
			RESULTS = _兔女郎
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[浴衣穿搭]]
			RESULTS = _浴衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 34 || CURRENT_CLOTHES_SET(CHARA) == 31
			RESULTS = _睡衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[危险野兽礼装]]
			RESULTS = _危险野兽
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 27 || TEQUIP:CHARA:SWIMMING
			RESULTS = _Y字泳衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[日式学校运动服]]
			RESULTS = _运动短裤
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[不知火舞忍装]]
			RESULTS = _不知火舞
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[寄叶二号B型套装]]
			RESULTS = _2B
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[花魁盛装]]
			RESULTS = _花魁
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[白无垢]]
			RESULTS = _白无垢
		ELSEIF CHECK_CLO("bra_visible", CHARA)
			RESULTS = _下着
		ENDIF
	ENDIF
ELSE
	;通常画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
	ELSE
		;服
		IF IS_BUNNY(CHARA)
			IF FLAG:立絵種類
				;顔絵
				;RESULTS = _バニー
			ELSE
				;立絵
				RESULTS = _バニー
			ENDIF
		ENDIF
	ENDIF
ENDIF


;IMAGE_I43_体形(CHARA)-----------------------------------------------------------------------
;角色特有の体形表示条件
;エフェクトを共有する体形差分はこちら
;服装を指定しても自動で表示されるので注意
;使用時注意点:
;	RESULTSに体形を格納すること
;	書式は %体形%%服装% 適宜"_"等入れること
;	"巨乳"は本編で実装済み %体形%巨乳%服装% で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I43_体形(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:43:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I43_特殊表情(CHARA)--------------------------------------------------------------------
;角色特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"通常","笑顔","憤怒","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I43_特殊表情(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA

IF CFLAG:43:差し替え適用 == 4
	IF TALENT:CHARA:心情 == 1 || BASE:CHARA:情緒 > MAXBASE:CHARA:情緒 / 2 || TALENT:CHARA:恋慕
		IF RESULTS:0 == "笑顔"
			RESULTS:0 '= "美味"
		ENDIF
		IF RESULTS:1 == "笑顔"
			RESULTS:1 '= "美味"
		ENDIF
	ENDIF
ELSEIF CFLAG:43:差し替え適用 == 3
	;替换用画像
	IF CHECK_NUDE(CHARA)
		IF RESULTS:0 == "通常"
			RESULTS:0 '= "裸通常"
		ENDIF
		IF RESULTS:1 == "通常"
			RESULTS:1 '= "裸通常"
		ENDIF
	ENDIF
ELSEIF CFLAG:43:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I43_特殊表情COM(CHARA, 指令)-------------------------------------------------------
;直前の指令で変化する角色特有の表情表示条件
;時間停止中は変化しない表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"自慰","愛撫","性交"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I43_特殊表情COM(CHARA, 指令)
#DIM CHARA
#DIM 指令

SELECTCASE CFLAG:43:差し替え適用
	;替换用画像
	CASE 2
	CASE 1
		IF CHECK_NUDE(CHARA)
		ELSE
			IF IS_BUNNY(CHARA)
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[浴衣穿搭]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 34 || CURRENT_CLOTHES_SET(CHARA) == 31
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[危险野兽礼装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 27 || TEQUIP:CHARA:SWIMMING
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[日式学校运动服]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[不知火舞忍装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[寄叶二号B型套装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[花魁盛装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[白无垢]]
			ELSEIF CHECK_CLO("bra_visible", CHARA)
			ELSE
				SELECTCASE 指令
					CASE 305
						RESULTS = 膝枕覆盖
					CASE 6,12,313
						RESULTS = 揉胸覆盖
				ENDSELECT
			ENDIF
		ENDIF
	CASEELSE
		;通常画像
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
ENDSELECT

;IMAGE_I43_差分(CHARA)-----------------------------------------------------------------------
;角色特有の差分表示条件
;裸やエフェクトを共有しない画像はこちら
;使用時注意点:
;	RESULTSに差分を格納すること
;	書式は {CHARA}%差分% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I43_差分(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
;VARSET RESULTS

IF CFLAG:43:差し替え適用 == 3
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSEIF CFLAG:43:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF
VARSET RESULTS


;IMAGE_I43_特殊照れ(CHARA)--------------------------------------------------------------------
;角色特有の照れエフェクト表示条件
;使用時注意点:
;	RESULTSに特殊照れを格納すること
;	書式は %特殊照れ%照れ
;	"裸"は本編で実装済み %特殊照れ%裸照れ で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I43_特殊照れ(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:43:差し替え適用 == 3
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSEIF CFLAG:43:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I43_特殊吐息(CHARA)--------------------------------------------------------------------
;角色特有の吐息エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊吐息を格納すること
;	書式は %特殊吐息%吐息
;	"裸"は本編で実装済み %特殊吐息%裸吐息 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I43_特殊吐息(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:43:差し替え適用 == 3
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSEIF CFLAG:43:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I43_特殊汗(CHARA)--------------------------------------------------------------------
;角色特有の汗エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊汗を格納すること
;	書式は %特殊汗%汗
;	"裸"は本編で実装済み %特殊汗%裸汗 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I43_特殊汗(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:43:差し替え適用 == 3
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSEIF CFLAG:43:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I43_特殊エフェクトチェック(CHAR, エフェクト)--------------------------------------------
;角色特有のエフェクト判定  特殊エフェクトを直接指定したい場合に参照
;引数 "エフェクト" に固有のエフェクトが含まれるかを判定する
;例の街道、STRFINDを使用してください
;-------------------------------------------------------------------------------------------
@IMAGE_I43_特殊エフェクトチェック(CHARA, エフェクト)
#DIM CHARA
#DIMS エフェクト

IF CFLAG:43:差し替え適用 == 1
	;替换用画像
	SIF STRFIND(エフェクト, "运动服湿") != -1
		RETURN 1
ELSE
	;通常画像
	SIF STRFIND(エフェクト, "母乳") != -1
		RETURN 1
ENDIF

RETURN 0

;IMAGE_I43_特殊エフェクト(CHARA)--------------------------------------------------------------------
;角色特有のエフェクト表示条件
;使用時注意点:
;	RESULTS配列に特殊エフェクトを格納
;	RETURNで追加するエフェクト数を返却すること
;-------------------------------------------------------------------------------------------
@IMAGE_I43_特殊エフェクト(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:43:差し替え適用 == 3
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSEIF CFLAG:43:差し替え適用 == 1
	;替换用画像
	IF SRPITE_运动服湿_43()
		RESULTS:エフェクト数 = 运动服湿
		エフェクト数 += 1
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
		IF TALENT:CHARA:母乳体質 && NOWEX:CHARA:噴乳 && CHECK_NUDE(CHARA)
			IF FLAG:巨乳差分表示 && TALENT:CHARA:胸围>=2
				RESULTS:エフェクト数 = 爆乳母乳
			ELSE
				RESULTS:エフェクト数 = 母乳
			ENDIF
			エフェクト数 += 1
		ENDIF
	ELSE
		;立絵
	ENDIF
ENDIF

RETURN エフェクト数

;IMAGE_I43_自慰エフェクト(CHARA)--------------------------------------------------------------------
;角色特有の自慰エフェクト表示条件
@IMAGE_I43_自慰エフェクト(CHARA)
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:43:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

RETURN エフェクト数
;********************************************************************************************
