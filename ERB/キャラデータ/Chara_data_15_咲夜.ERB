@CHARAMOVE_DATA_15(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 15
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
		SIF GET_MAPID(CFLAG:[[レミリア]]:遭遇位置) != MAP湖
			RETURN [[レミリア]]
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;仓库には入らない、屋敷外には出ない
			CASE P305仓库, P334廃洋館・入口 TO P348霧之湖・湖底
				RETURN -100
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 桜花道的鳥居, 神社境内, 神社走廊
				RETURN 1
			CASE 広場, 商家町, 呑屋小道, 長屋街
				RETURN 1
			CASE 霧之湖, 正門, 大廳, 食堂, 大図書館, 地下室
				RETURN 1
			CASE 森林的入口, 香霖堂, 霧雨魔法店, 人偶的洋館
				RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
ENDSELECT

@EXIST_キャラデータ15
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ15(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "咲夜")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（吊带睡裙）套")
CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, [[頭装備_女仆发箍]])
CASE "便装：靴"
	CALLF MAKE_INT(V_NAME, [[靴_鞋子]])
CASE "便装：襪子"
	IF FLAG:2 > 0
		CALLF MAKE_INT(V_NAME, [[襪子_吊带袜]])
	ELSE
		CALLF MAKE_INT(V_NAME, [[襪子_长筒袜]])
	ENDIF
CASE "便装：連衣裙"
	CALLF MAKE_INT(V_NAME, [[連衣裙_围裙洋装]])
CASE "便装：其他１"
	CALLF MAKE_INT(V_NAME, [[其他衣装_蝴蝶结]])
CASE "便装：其他２"
	CALLF MAKE_INT(V_NAME, [[其他衣装_小刀]])

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "带吊带的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "高级丝绸胖次")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "高级丝绸胸罩")
CASE "固有内衣着用条件"
	SIF !RAND:(3 - (2 * 勝負内衣判定([[咲夜]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 2)
;特殊衣装
CASE "デート衣装", "人里デート衣装", "おうちデート衣装"
	CALLF MAKE_INT(V_NAME, [[外出女仆套装]])

CASE "固有指令"
	IF 成人再教育插件开关 == 1 
		CALLF MAKE_STR(V_NAME, "与咲夜对话")
	ELSE
		CALLF MAKE_STR(V_NAME, "红魔馆出差服务")
	ENDIF
CASE "固有指令実行可能"
	IF 成人再教育插件开关 == 1
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		IF TFLAG:上门清扫 && (FLAG:澆水代行 || !ITEM:150)
		ELSEIF MAIN_MAP == MAP湖
		ELSE
			CALLF MAKE_INT(V_NAME, 1)
		ENDIF
	ENDIF

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "魚料理/香料/中華/煮物/甜/淡白/豪華/上品/家庭的/サクサク/絢爛/" + "酸/" * 3)
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "質素/野趣/咸/咸/虫肉/虫肉/虫肉/謎料理/謎料理/謎料理/謎料理/謎料理/")
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "隠し味/秘術/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "青/美的/シンプル/洋風/別緻/" + "钟表/食器/" * 3 + "瀟洒な/女僕的/時の/時空の/怀表/小刀/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "パワフル/ロリータ/" + "烟袋/" * 3)

;ここは依頼IDが関連しない情報
;依頼発生率スカ2/一般3/固有5
CASE "没有依頼発生率"
	CALLF MAKE_INT(V_NAME, 2)
CASE "一般依頼発生率"
	CALLF MAKE_INT(V_NAME, 3)
CASE "固有依頼発生率"
	CALLF MAKE_INT(V_NAME, 5)
CASE "固有依頼最終要素"
	CALLF MAKE_INT(V_NAME, 101)
CASEELSE
	;固有依頼関連のデータにバトンタッチ
	SIF STRCOUNT(O_DATA, "依頼")
		CALLF F固有依頼15(ARG, O_DATA, V_NAME)
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_15
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 红魔馆午餐
RESULTS = 主食
CALL 材料設定("高级食材")
DISH_TASTE = 洋食/儿童午餐/面包/牛奶/甜/丰盛/濃厚/温暖/豪華/上品/鲜艳/秘術/女僕流/
DISH_COMMENT = 咲夜为大小姐制作的儿童午餐。
RETURN 1

;IMAGE_I15_特殊服装(CHARA)-------------------------------------------------------------------
;角色特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I15_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS
SELECTCASE CFLAG:15:差し替え適用
	CASE 2
		IF CHECK_NUDE(CHARA)
		;裸
			IF GROUPMATCH(1, TEQUIP:CHARA:新妻PLAY)
				RESULTS = _裸エプロン
			ENDIF
		ELSE
		;服
			IF CURRENT_CLOTHES_SET(CHARA) == 33
				RESULTS = _パジャマ
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[忍者套]]
				RESULTS = _女忍
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[暗杀者套装]]
				RESULTS = _杰克
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[忍者套]]
				RESULTS = _暗杀者套装
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[黑暗系休闲套装]]
				RESULTS = _私服2
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[哥特洛丽塔套装]]
				RESULTS = _ゴシック
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[辣妹系休闲套装]]
				RESULTS = _私服
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[迪奥套装]]
				RESULTS = _Dio
			ELSEIF IS_BUNNY(CHARA)
				RESULTS = _バニーギャル
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
				RESULTS = _暁美ほむら
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 61
				RESULTS = _ウェディングドレス
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 52 || TEQUIP:CHARA:SWIMMING
				RESULTS = _水着
			ELSEIF CHECK_CLO("bra_visible", CHARA) && GROUPMATCH(1, TEQUIP:CHARA:新妻PLAY)
				RESULTS = _裸エプロン
			ELSEIF CHECK_CLO("bra_visible", CHARA)
				RESULTS = _下着
			ELSEIF ABL:CHARA:亲密 > 14
				RESULTS = _高好感
			ENDIF
		ENDIF
	CASE 3
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立ち絵
			ENDIF
		ELSE
			;服
			IF FLAG:立絵種類
				;顔絵
				IF CHECK_CLO("bra_visible", CHARA)
					RESULTS = _下着
				ELSEIF IS_BUNNY(CHARA)
					RESULTS = _バニー
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
					RESULTS = _変T
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
					RESULTS = _セーラー
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[外出女仆套装]]
					RESULTS = _人里
				ENDIF
			ELSE
				;立ち絵
				IF CHECK_CLO("bra_visible", CHARA) && EQUIP:CHARA:下半身下着２ != 0
					RESULTS = _下着
				ELSEIF CHECK_CLO("bra_visible", CHARA) && EQUIP:CHARA:下半身下着２ == 0
					RESULTS = _ノーパン下着
				{
				ELSEIF (!EQUIP:CHARA:上半身上着１ && !EQUIP:CHARA:上半身上着２ && !EQUIP:CHARA:外搭 &&
						!EQUIP:CHARA:和服 && !EQUIP:CHARA:連衣裙 &&
						!EQUIP:CHARA:レオタード && !EQUIP:CHARA:ボディースーツ) &&
						EQUIP:CHARA:下半身内衣２ != 0
				}
					RESULTS = _ノーブラ下着
				ELSEIF IS_BUNNY(CHARA)
					RESULTS = _バニー
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
					RESULTS = _変T
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
					RESULTS = _セーラー
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[外出女仆套装]]
					RESULTS = _人里
				ENDIF
			ENDIF
		ENDIF
	CASE 4
	CASE 5
		IF CHECK_NUDE(CHARA)
		ELSE
		;服
			IF CHECK_CLO("bra_visible", CHARA) || CURRENT_CLOTHES_SET(CHARA) == 33
				IF TALENT:CHARA:3 || TALENT:CHARA:7 == 1 || TALENT:CHARA:184 == 1
					IF DAY:0 % 2 || TCVAR:CHARA:142 == 1
						RESULTS = _黑内衣
					ELSE
						RESULTS = _白内衣
					ENDIF
				ELSE
					RESULTS = _白内衣
				ENDIF
			ELSEIF IS_BUNNY(CHARA)
				RESULTS = _兔女郎
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
				RESULTS = _水手服
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[晚宴礼服套装]]
				RESULTS = _礼服
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[外出女仆套装]]
				RESULTS = _人里
			ELSEIF DAY:0 % 2
				RESULTS = _轮换
			ENDIF
		ENDIF
	CASE 0
	;通常画像
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
				SIF BATHROBE_SWITCH && BATHROOM() && !CFLAG:诶嘿嘿
					RESULTS = _浴袍
			ELSE
				;立ち絵
				SIF BATHROBE_SWITCH && BATHROOM() && !CFLAG:诶嘿嘿
					RESULTS = _浴袍
			ENDIF
		ELSE
			;服
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立ち絵
			ENDIF
		ENDIF
ENDSELECT

;IMAGE_I15_体形(CHARA)-----------------------------------------------------------------------
;キャラクター特有の体形表示条件
;エフェクトを共有する体形差分はこちら
;服装を指定しても自動で表示されるので注意
;使用時注意点:
;	RESULTSに体形を格納すること
;	書式は %体形%%服装% 適宜"_"等入れること
;	"巨乳"は本編で実装済み %体形%巨乳%服装% で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I15_体形(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:15:差し替え適用
	;差し替え画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

;IMAGE_I15_特殊表情(CHARA)--------------------------------------------------------------------
;キャラクター特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"通常","笑顔","憤怒","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I15_特殊表情(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
SELECTCASE CFLAG:15:差し替え適用
	CASE 2
		IF TALENT:CHARA:恋慕 && !MARK:CHARA:反発刻印 && TALENT:CHARA:機嫌 >= 0
			SIF RESULTS:0 == "通常"
				RESULTS:0 = 微笑
			SIF RESULTS:1 == "通常"
				RESULTS:1 = 微笑
		ENDIF
	CASE 3
	CASE 4
	CASE 5
	CASE 0
ENDSELECT

;IMAGE_I15_特殊表情COM(CHARA, コマンド)-------------------------------------------------------
;直前のコマンドで変化するキャラクター特有の表情表示条件
;時間停止中は変化しない表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"オナニー","愛撫","性交"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I15_特殊表情COM(CHARA, コマンド)
#DIM CHARA
#DIM コマンド

SELECTCASE CFLAG:15:差し替え適用
	;替换用画像
	CASE 2
		IF CHECK_NUDE(CHARA)
			SELECTCASE コマンド
				CASE 81, 88, 140
					RESULTS = 口交覆盖{RANDNR(15, "COM_DIFF_AS06MARKIV") + 1}
			ENDSELECT
		ELSE
			IF CURRENT_CLOTHES_SET(CHARA) == 33
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[忍者套]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[暗杀者套装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[忍者套]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[黑暗系休闲套装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[哥特洛丽塔套装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[辣妹系休闲套装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[迪奥套装]]
			ELSEIF IS_BUNNY(CHARA)
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 61
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 52 || TEQUIP:CHARA:SWIMMING
			ELSEIF CHECK_CLO("bra_visible", CHARA) && GROUPMATCH(1, TEQUIP:CHARA:新妻PLAY)
			ELSEIF CHECK_CLO("bra_visible", CHARA)
			ELSE
				SELECTCASE コマンド
					CASE 305
						RESULTS = 膝枕覆盖
					CASE 6,12,313
						IF ABL:CHARA:露出癖 > 5 && ABL:CHARA:亲密 > 9
							RESULTS = 揉胸2覆盖
						ELSE
							RESULTS = 揉胸覆盖
						ENDIF
				ENDSELECT
			ENDIF
		ENDIF
	CASEELSE
		;通常画像
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
ENDSELECT

;IMAGE_I15_差分(CHARA)-----------------------------------------------------------------------
;キャラクター特有の差分表示条件
;裸やエフェクトを共有しない画像はこちら
;使用時注意点:
;	RESULTSに差分を格納すること
;	書式は {CHARA}%差分% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I15_差分(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS

IF CFLAG:15:差し替え適用
	;差し替え画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
	SIF CURRENT_CLOTHES_SET(CHARA) == [[外出女仆套装]] && CFLAG:15:差し替え適用 == 4
		RESULTS = _人里
ELSE
	;通常画像
	SIF CURRENT_CLOTHES_SET(CHARA) == [[外出女仆套装]]
		RESULTS = _人里
ENDIF

;IMAGE_I15_特殊照れ(CHARA)--------------------------------------------------------------------
;キャラクター特有の照れエフェクト表示条件
;使用時注意点:
;	RESULTSに特殊照れを格納すること
;	書式は %特殊照れ%照れ
;	"裸"は本編で実装済み %特殊照れ%裸照れ で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I15_特殊照れ(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:15:差し替え適用
	;差し替え画像
	IF FLAG:立絵種類
		;顔絵
		IF GROUPMATCH(CFLAG:15:差し替え適用, 3)
			IF CURRENT_CLOTHES_SET(CHARA) == [[外出女仆套装]] && !CHECK_NUDE(CHARA)
				RESULTS = 人里
			ENDIF
		ELSE

		ENDIF
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

;IMAGE_I15_特殊吐息(CHARA)--------------------------------------------------------------------
;キャラクター特有の吐息エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊吐息を格納すること
;	書式は %特殊吐息%吐息
;	"裸"は本編で実装済み %特殊吐息%裸吐息 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I15_特殊吐息(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:15:差し替え適用
	;差し替え画像
	IF FLAG:立絵種類
		;顔絵
		IF GROUPMATCH(CFLAG:15:差し替え適用, 3)
			IF CURRENT_CLOTHES_SET(CHARA) == [[外出女仆套装]] && !CHECK_NUDE(CHARA)
				RESULTS = 人里
			ENDIF
		ENDIF
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

;IMAGE_I15_特殊汗(CHARA)--------------------------------------------------------------------
;キャラクター特有の汗エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊汗を格納すること
;	書式は %特殊汗%汗
;	"裸"は本編で実装済み %特殊汗%裸汗 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I15_特殊汗(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:15:差し替え適用
	;差し替え画像
	IF FLAG:立絵種類
		;顔絵
		IF GROUPMATCH(CFLAG:15:差し替え適用, 3)
			IF CURRENT_CLOTHES_SET(CHARA) == [[外出女仆套装]]
				IF CHECK_NUDE(CHARA)
					RESULTS = 人里裸
				ELSE
					RESULTS = 人里
				ENDIF
			ENDIF
		ENDIF
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

;IMAGE_I15_特殊エフェクトチェック(CHAR, エフェクト)--------------------------------------------
;キャラクター特有のエフェクト判定  特殊エフェクトを直接指定したい場合に参照
;引数 "エフェクト" に固有のエフェクトが含まれるかを判定する
;例の通り、STRFINDを使用してください
;-------------------------------------------------------------------------------------------
@IMAGE_I15_特殊エフェクトチェック(CHARA, エフェクト)
#DIM CHARA
#DIMS エフェクト

IF CFLAG:15:差し替え適用
	;差し替え画像
ELSE
	;通常画像
	;SIF STRFIND(エフェクト, "母乳") != -1
	;	RETURN 1
ENDIF

RETURN 0

;IMAGE_I15_特殊エフェクト(CHARA)--------------------------------------------------------------------
;キャラクター特有のエフェクト表示条件
;使用時注意点:
;	RESULTS配列に特殊エフェクトを格納
;	RETURNで追加するエフェクト数を返却すること
;-------------------------------------------------------------------------------------------
@IMAGE_I15_特殊エフェクト(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:15:差し替え適用
	;差し替え画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

RETURN エフェクト数

;IMAGE_I15_自慰エフェクト(CHARA)--------------------------------------------------------------------
;キャラクター特有の自慰エフェクト表示条件
@IMAGE_I15_自慰エフェクト(CHARA)
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:15:差し替え適用
	;差し替え画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

RETURN エフェクト数
