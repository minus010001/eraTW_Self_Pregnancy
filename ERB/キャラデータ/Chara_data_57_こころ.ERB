@CHARAMOVE_DATA_57(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 57
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;よく居る
			CASE P2境内, P10走廊
				RETURN 5
			;私室にはあまり入らない
			CASE P15霊夢私室, P52別屋
				RETURN -5
			;他所
			CASE P27三妖精之家・大廳 TO P35三妖精之家・地下室, P37夢幻遺跡・一階通路 TO P51夢幻遺跡・倉庫, P56阿吽の棲家
				RETURN -100
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 桜花道的鳥居, 神社境内, 神社走廊
				RETURN 1
			CASE 地蔵小路, 倒垂的柳樹下, 命蓮寺境内, 命蓮寺方丈, 神霊廟広場, 神霊廟道場
				RETURN 1
			CASE 広場, 商家町, 呑屋小道
				RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
ENDSELECT

@EXIST_キャラデータ57
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ57(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC O_DATA
#DIMS DYNAMIC V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "心")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裤子+兜帽）套")

CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, [[頭装備_面]])
CASE "便装：靴"
	CALLF MAKE_INT(V_NAME, [[靴_鞋子]])
CASE "便装：襪子"
	CALLF MAKE_INT(V_NAME, [[襪子_袜子]])
CASE "便装：裙子"
	CALLF MAKE_INT(V_NAME, [[裙子_长裙]])
CASE "便装：上身衣服２"
	CALLF MAKE_INT(V_NAME, [[上身衣服_はだけ可能_女式衬衫]])

;けして開放式胖次ではない
CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "各种笑脸面具的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "镂空胖次")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 0)
CASE "固有内衣着用条件"
	SIF !RAND:(1 + (2 * 勝負内衣判定([[心]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "米/果物/和食/煮物/ジャンキー/淡白/豪華/芸術的/鲜艳/软糯的/辣/辣/咸/咸/")
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "濃厚/酸/酸/" + "青蛙/虫肉/謎料理/" * 3)
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "彩り/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "粉色/美的/芸術的/和風/派手/不思議/" + "精神/音楽/面/蜃気楼の/" * 3 + "心の/希望の/舞踊の/感情の/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "別緻/洋風/" + "生体品/护臂/" * 3)
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_57
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 蜜意人形烧
RESULTS = 甜点
CALL 材料設定("甜味料")
DISH_TASTE = 和食/饅頭/卵/甜/芸術的/芸術的/芸術的/软绵绵/
DISH_COMMENT = 在秦心的演出节目里所制作的、以被恋爱的火焰烧毁的48个面具为主题的人形烧。
RETURN 1

;IMAGE_I57_特殊服装(CHARA)-------------------------------------------------------------------
;キャラクター特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I57_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC CHARA
VARSET RESULTS

IF CFLAG:57:差し替え適用 == 3
	;差し替え画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立ち絵種類
			;顔絵
		ELSE
			;立ち絵
		ENDIF
	ELSE
		;服
		IF CHECK_CLO("bra_visible", CHARA) || TEQUIP:CHARA:SWIMMING
			RESULTS = _泳衣
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[舞娘套装]]
			RESULTS =_舞娘
		ENDIF
	ENDIF
ELSEIF CFLAG:57:差し替え適用
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立ち絵種類
			;顔絵
		ELSE
			;立ち絵
		ENDIF
	ELSE
		;服
		IF FLAG:立ち絵種類
			;顔絵
			IF GROUPMATCH(CFLAG:57:差し替え適用, 1)
				IF CHECK_CLO("bra_visible", CHARA)
					RESULTS = _下着
				ELSEIF IS_BUNNY(CHARA)
					RESULTS = _バニー
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
					RESULTS = _変T
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
					RESULTS = _セーラー
				ENDIF
			ELSEIF GROUPMATCH(CFLAG:57:差し替え適用, 2)
				IF CURRENT_CLOTHES_SET(CHARA) == [[舞娘套装]]
					RESULTS =_踊り子
				ENDIF
			ENDIF
		ELSE
			;立ち絵
			IF GROUPMATCH(CFLAG:57:差し替え適用, 1)
				IF CHECK_CLO("bra_visible", CHARA) && EQUIP:CHARA:下半身下着２ != 0
					RESULTS = _下着
				ELSEIF CHECK_CLO("bra_visible", CHARA) && EQUIP:CHARA:下半身下着２ == 0
					RESULTS = _ノーパン下着
				{
				ELSEIF (!EQUIP:CHARA:上半身上着１ && !EQUIP:CHARA:上半身上着２ && !EQUIP:CHARA:外衣 &&
						!EQUIP:CHARA:着物 && !EQUIP:CHARA:ワンピース &&
						!EQUIP:CHARA:レオタード && !EQUIP:CHARA:ボディースーツ) &&
						EQUIP:CHARA:下半身下着２ != 0
				}
					RESULTS = _ノーブラ下着
				ELSEIF IS_BUNNY(CHARA)
					RESULTS = _バニー
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
					RESULTS = _変T
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
					RESULTS = _セーラー
				ENDIF
			ELSEIF GROUPMATCH(CFLAG:57:差し替え適用, 2)
				IF CURRENT_CLOTHES_SET(CHARA) == [[舞娘套装]]
					RESULTS =_踊り子
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ELSE
	;通常画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立ち絵種類
			;顔絵
		ELSE
			;立ち絵
		ENDIF
	ELSE
		;服
		IF FLAG:立ち絵種類
			;顔絵
		ELSE
			;立ち絵
		ENDIF
	ENDIF
ENDIF

@MUSIC_BUFF_57(ARG, SUCCESS)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC SUCCESS
IF SUCCESS == 1
	TIMES SOURCE:ARG:歓楽, 1.2
	TIMES SOURCE:ARG:征服, 1.2
	CALL COLORMESSAGE(@"%CALLNAME:[[こころ]]%的能力，欢乐提升了",C_YELLOW,1)
	CALL COLORMESSAGE(@"%CALLNAME:[[こころ]]%的能力，征服提升了",C_YELLOW,1)
ENDIF