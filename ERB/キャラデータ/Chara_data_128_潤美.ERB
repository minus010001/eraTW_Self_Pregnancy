@CHARAMOVE_DATA_128(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 128
SELECTCASE ARG
	;連動設定（参照する側＞RETURN=参照先, 参照される側＞RESULT:1～3=連動対象）
	CASE 1
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向
	CASE 4
		SELECTCASE ARG:2
			;八雲邸と彼岸から先は行かない（入り口部分は出る時用）
			CASE P604彼岸, P610是非曲直庁大堂 TO P655尤魔宅の風呂, P605地獄
				RETURN -100
			;瓔花的家には行かない
			CASE P609瓔花的家, P606日狭美の家, P607残無の家
				RETURN -100
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 広場, 商家町, 呑屋小道
				RETURN 1
			CASE 中有之道, 三途之川
				RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
ENDSELECT

@EXIST_キャラデータ128
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ128(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "潤美")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裤子）套")

CASE "便装：上身衣服２"
	CALLF MAKE_INT(V_NAME, [[上身衣服_はだけ可能_无袖服]])
CASE "便装：外搭"
	CALLF MAKE_INT(V_NAME, [[外搭_羽织]])
CASE "便装：裤子"
	CALLF MAKE_INT(V_NAME, [[裤子_短裤]])
CASE "便装：其他１"
	CALLF MAKE_INT(V_NAME, [[其他衣装_石像]])

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "田园风的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "牛图案胖次")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "牛图案胸罩")
CASE "固有内衣着用条件"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 2)

CASE "固有指令"
	IF 成人再教育插件开关 == 1
		CALLF MAKE_STR(V_NAME, "与润美对话")
	ELSE
		CALLF MAKE_STR(V_NAME, "鱼的交换")
	ENDIF
CASE "固有指令実行可能"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有指令：鱼的交换比率"
	CALLF MAKE_INT(V_NAME, MAX(5, 10 - ABL:[[潤美]]:親密))

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "魚料理/虫肉/煮物/下酒菜/丰盛/豪華/野趣/家庭的/サクサク/辣/辣/咸/咸/")
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "酸/酸/青蛙/青蛙/青蛙/小人族秘伝の味/小人族秘伝の味/小人族秘伝の味/")
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "冥界風/得意料理＊魚＊/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "茶/かわいい/シンプル/別緻/" + "魚/人形/鬼の/" * 3 + "石の/深海魚の/古代魚の/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "ポップ/派手/ロリータ/マニアック/")
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_128
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 活花枝生魚片
RESULTS = 主食
CALL 材料設定("好吃的魚")
DISH_TASTE = 和食/魚料理/咸/简易/淡白/豪華/野趣/芸術的/
DISH_COMMENT = 将活鱼进行处理拼盘而成的新鲜刺身。
RETURN 1


;IMAGE_I128_特殊服装(CHARA)-------------------------------------------------------------------
;キャラクター特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I128_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS

IF CFLAG:128:差し替え適用 == 2
	IF CHECK_NUDE(CHARA)
	ELSE
		IF CFLAG:CHARA:睡眠 || CFLAG:CHARA:陪睡中 || 睡眠時間(CHARA)
			RESULTS = _下着
		ELSEIF IS_BUNNY(CHARA)
			RESULTS = _バニーギャル
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[白衣天使套]]
			RESULTS = _ナース
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[啦啦队制服]]
			RESULTS = _チアリーダー
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
			RESULTS = _セーラー服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[修女服]]
			RESULTS = _シスター
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[女仆装]]
			RESULTS = _メイド
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[巫女装束]]
			RESULTS = _巫女
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[忍者套]]
			RESULTS = _忍者
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[辣妹系休闲套装]]
			RESULTS = _私服
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[Y字泳衣]]
			RESULTS = _水着
		ELSEIF CHECK_CLO("bra_visible", CHARA)
			RESULTS = _下着
		ENDIF
	ENDIF
ELSEIF CFLAG:128:差し替え適用 == 1
	;差し替え画像
ELSE
	;通常画像
ENDIF

;IMAGE_I128_特殊表情(CHARA)--------------------------------------------------------------------
;キャラクター特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"通常","笑顔","憤怒","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I128_特殊表情(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
IF CFLAG:128:差し替え適用 == 2
ELSEIF CFLAG:128:差し替え適用 == 1
	;差し替え画像
ELSE
	;通常画像
ENDIF

;IMAGE_I128_特殊表情COM(CHARA, 指令)-------------------------------------------------------
;直前の指令で変化する角色特有の表情表示条件
;時間停止中は変化しない表情はこちら
;使用時注意点:
;   RESULTS:0に表情を格納
;   RESULTS:1にマウスオーバー時の表情を格納すること
;   "自慰","愛撫","性交"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I128_特殊表情COM(CHARA, 指令)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM 指令

SELECTCASE CFLAG:128:差し替え適用
	;替换用画像
	CASE 2
		IF CHECK_NUDE(CHARA)
		ELSE
			;服
			IF IS_BUNNY(CHARA)
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[白衣天使套]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[啦啦队制服]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[修女服]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[女仆装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[巫女装束]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[忍者套]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[辣妹系休闲套装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[Y字泳衣]]
			ELSEIF CHECK_CLO("bra_visible", CHARA)
			ELSE
				SELECTCASE 指令
					CASE 305
						RESULTS = 膝枕覆盖
					CASE 6,12,313
						RESULTS = 揉胸覆盖
				ENDSELECT
			ENDIF
		ENDIF
	CASEELSE
		;通常画像
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
ENDSELECT

;IMAGE_I128_特殊エフェクトチェック(CHAR, エフェクト)--------------------------------------------
;角色特有のエフェクト判定  特殊エフェクトを直接指定したい場合に参照
;引数 "エフェクト" に固有のエフェクトが含まれるかを判定する
;例の街道、STRFINDを使用してください
;-------------------------------------------------------------------------------------------
@IMAGE_I128_特殊エフェクトチェック(CHARA, エフェクト)
#DIM CHARA
#DIMS エフェクト

IF CFLAG:128:差し替え適用
	;替换用画像
ELSE
	;通常画像
	SIF STRFIND(エフェクト, "母乳") != -1
		RETURN 1
ENDIF

RETURN 0


;IMAGE_I128_特殊エフェクト(CHARA)--------------------------------------------------------------------
;角色特有のエフェクト表示条件
;使用時注意点:
;	RESULTS配列に特殊エフェクトを格納
;	RETURNで追加するエフェクト数を返却すること
;-------------------------------------------------------------------------------------------
@IMAGE_I128_特殊エフェクト(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:128:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
		IF TALENT:CHARA:母乳体質 && NOWEX:CHARA:噴乳 && CHECK_NUDE(CHARA)
			IF FLAG:巨乳差分表示 && TALENT:CHARA:胸围>=2
				RESULTS:エフェクト数 = 爆乳母乳
			ELSE
				RESULTS:エフェクト数 = 母乳
			ENDIF
			エフェクト数 += 1
		ENDIF
	ELSE
		;立絵
	ENDIF
ENDIF

RETURN エフェクト数