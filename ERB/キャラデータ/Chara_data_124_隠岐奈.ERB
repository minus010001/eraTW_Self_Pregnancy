@CHARAMOVE_DATA_124(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 124
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;未設定
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 広場, 商家町, 呑屋小道
				RETURN 1
			CASE 森林的入口, 香霖堂, 古木的大樹, 霧雨魔法店 , 再思之道, 無縁塚
				RETURN 1
			CASE 中有之道, 三途之川, 迷途之家
				RETURN 1
			CASE 地下室
				RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
ENDSELECT

@EXIST_キャラデータ124
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ124(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "隠岐奈")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裤子）套")

CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, [[頭装備_乌帽子]])
CASE "便装：靴"
	CALLF MAKE_INT(V_NAME, [[靴_长靴]])
CASE "便装：襪子"
	CALLF MAKE_INT(V_NAME, [[襪子_袜子]])
CASE "便装：連衣裙"
	CALLF MAKE_INT(V_NAME, [[連衣裙_道士服]])

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "投影了夜空的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "星辰胖次")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "星辰胸罩")
CASE "固有内衣着用条件"
	SIF !RAND:3
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 2)
;特殊衣装
CASE "デート衣装", "人里デート衣装"
	CALLF MAKE_INT(V_NAME, [[辣妹系休闲套装]])

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "米/煮物/健康/豪華/芸術的/上品/滑溜溜/辣/辣/酸/酸/")
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "質素/咸/咸/" + "青蛙/虫肉/謎料理/" * 3 + "天狗流/" * 5)
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "附魔/加護/絢爛/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "黄/高級/混沌/美的/芸術的/懷舊/派手/知的/威厳/" + "巫術/美術品/四季の/" * 3 + "七星の/後戸の/隐匿の/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "洋風/別緻/ロリータ/" + "安物/" * 3 + "天狗の/沉眠の/" * 10)
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_124
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 秘神火腿
RESULTS = 主食
CALL 材料設定("獣肉")
DISH_TASTE = 洋食/肉料理/野菜/酸/丰盛/豪華/上品/神力/
DISH_COMMENT = 用生火腿制成的时髦料理。
RETURN 1

;IMAGE_I124_特殊服装(CHARA)-------------------------------------------------------------------
;キャラクター特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I124_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS

IF CFLAG:124:差し替え適用
	;替换用画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立絵種類
			;顔絵
			IF GROUPMATCH(CFLAG:124:差し替え適用, 1)
				SIF IS_BUNNY(CHARA)
					RESULTS = _バニー
			ENDIF
		ELSE
			;立絵
		ENDIF
	ELSE
		;服
		IF FLAG:立絵種類
			;顔絵
			IF GROUPMATCH(CFLAG:124:差し替え適用, 1)
				IF IS_BUNNY(CHARA)
					RESULTS = _バニー
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
					RESULTS = _変T
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
					RESULTS = _セーラー
				ELSEIF CHECK_CLO("bra_visible", CHARA)
					IF DAY:0 % 2
						RESULTS = _下着
					ELSE
						RESULTS = _下着2
					ENDIF
				ENDIF
			ENDIF
		ELSE
			;立絵
		ENDIF
	ENDIF
ELSE
	;通常画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
	ELSE
		;服
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
	ENDIF
ENDIF

;IMAGE_I124_体形(CHARA)-----------------------------------------------------------------------
;キャラクター特有の体形表示条件
;エフェクトを共有する体形差分はこちら
;服装を指定しても自動で表示されるので注意
;使用時注意点:
;	RESULTSに体形を格納すること
;	書式は %体形%%服装% 適宜"_"等入れること
;	"巨乳"は本編で実装済み %体形%巨乳%服装% で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I124_体形(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:124:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I124_特殊表情(CHARA)--------------------------------------------------------------------
;キャラクター特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"通常","笑顔","憤怒","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I124_特殊表情(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA

IF CFLAG:124:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
		IF GROUPMATCH(CFLAG:124:差し替え適用, 1)
			IF INRANGE(CFLAG:CHARA:現在位置, 200, 297) && !CHECK_NUDE(CHARA)
				SIF CHECK_BLUSH(CHARA, @"%GET_BASESTYLE(CHARA)%", "_人里")
					RESULTS = 赤面
				SIF BASE:CHARA:情緒 >= MAXBASE:CHARA:情緒*8/10
					RESULTS = LOVE
			ENDIF
		ENDIF
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I124_特殊表情COM(CHARA, コマンド)-------------------------------------------------------
;直前のコマンドで変化するキャラクター特有の表情表示条件
;時間停止中は変化しない表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"自慰","愛撫","性交"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I124_特殊表情COM(CHARA, 指令)
#DIM CHARA
#DIM 指令

IF CFLAG:124:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I124_差分(CHARA)-----------------------------------------------------------------------
;キャラクター特有の差分表示条件
;裸やエフェクトを共有しない画像はこちら
;使用時注意点:
;	RESULTSに差分を格納すること
;	書式は {CHARA}%差分% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I124_差分(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS

IF CFLAG:124:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
		IF GROUPMATCH(CFLAG:124:差し替え適用, 1)
			SIF INRANGE(CFLAG:CHARA:現在位置, 200, 297) && !CHECK_NUDE(CHARA)
				RESULTS = _人里
		ENDIF
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I124_特殊照れ(CHARA)--------------------------------------------------------------------
;キャラクター特有の照れエフェクト表示条件
;使用時注意点:
;	RESULTSに特殊照れを格納すること
;	書式は %特殊照れ%照れ
;	"裸"は本編で実装済み %特殊照れ%裸照れ で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I124_特殊照れ(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:124:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I124_特殊吐息(CHARA)--------------------------------------------------------------------
;キャラクター特有の吐息エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊吐息を格納すること
;	書式は %特殊吐息%吐息
;	"裸"は本編で実装済み %特殊吐息%裸吐息 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I124_特殊吐息(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:124:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I124_特殊汗(CHARA)--------------------------------------------------------------------
;キャラクター特有の汗エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊汗を格納すること
;	書式は %特殊汗%汗
;	"裸"は本編で実装済み %特殊汗%裸汗 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I124_特殊汗(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:124:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I124_特殊エフェクトチェック(CHAR, エフェクト)--------------------------------------------
;キャラクター特有のエフェクト判定  特殊エフェクトを直接指定したい場合に参照
;引数 "エフェクト" に固有のエフェクトが含まれるかを判定する
;例の街道、STRFINDを使用してください
;-------------------------------------------------------------------------------------------
@IMAGE_I124_特殊エフェクトチェック(CHARA, エフェクト)
#DIM CHARA
#DIMS エフェクト

IF CFLAG:124:差し替え適用
	;替换用画像
	IF GROUPMATCH(CFLAG:124:差し替え適用, 1)
		;SIF STRFIND(エフェクト, "母乳") != -1
		;	RETURN 1
		SIF STRFIND(エフェクト, "ぶっかけ") != -1
			RETURN 1
		SIF STRFIND(エフェクト, "お口") != -1
			RETURN 1
	ENDIF
ELSE
	;通常画像
ENDIF

RETURN 0

;IMAGE_I124_特殊エフェクト(CHARA)--------------------------------------------------------------------
;キャラクター特有のエフェクト表示条件
;使用時注意点:
;	RESULTS配列に特殊エフェクトを格納
;	RETURNで追加するエフェクト数を返却すること
;-------------------------------------------------------------------------------------------
@IMAGE_I124_特殊エフェクト(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:124:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
		IF GROUPMATCH(CFLAG:124:差し替え適用, 1)
			IF CFLAG:CHARA:脸上精液
				;胸ぶっかけ
				RESULTS:エフェクト数 = ぶっかけ
				エフェクト数 += 1
			ENDIF
			IF CFLAG:CHARA:口内精液
				;口交
				RESULTS:エフェクト数 = お口
				エフェクト数 += 1
			ENDIF
		ENDIF
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

RETURN エフェクト数

;IMAGE_I124_自慰エフェクト(CHARA)--------------------------------------------------------------------
;キャラクター特有の自慰エフェクト表示条件
@IMAGE_I124_自慰エフェクト(CHARA)
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:124:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

RETURN エフェクト数
