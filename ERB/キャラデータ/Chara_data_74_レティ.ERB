@CHARAMOVE_DATA_74(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 74
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;よく居る
			CASE P343湖畔, P345琪露諾的冰屋, P347霧之湖
				RETURN 5
			;紅魔館内部、廃洋館には行かない
			CASE P303守衛小屋 TO P342莉莉卡私室
				RETURN -100
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 広場, 商家町, 呑屋小道
				;冬の間だけ
				SIF DAY:2 == 4
					RETURN 1
			CASE 霧之湖, 冰造的小雪屋, 正門
				RETURN 1
			CASE 森林的入口, 古木的大樹, 再思之道, 無縁塚
				;冬の間だけ
				SIF DAY:2 == 4
					RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
ENDSELECT

@EXIST_キャラデータ74
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ74(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "蕾蒂")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裤子）套")

CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, [[頭装備_头巾帽]])
CASE "便装：靴"
	CALLF MAKE_INT(V_NAME, [[靴_鞋子]])
CASE "便装：襪子"
	CALLF MAKE_INT(V_NAME, [[襪子_袜子]])
CASE "便装：連衣裙"
	CALLF MAKE_INT(V_NAME, [[連衣裙_連衣裙]])
CASE "便装：外搭"
	CALLF MAKE_INT(V_NAME, [[外搭_背心]])
CASE "便装：其他１"
	CALLF MAKE_INT(V_NAME, [[其他衣装_围巾]])
CASE "便装：其他２"
	CALLF MAKE_INT(V_NAME, [[其他衣装_胸针]])

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "冰霜般的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "雪女胖次")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "雪女胸罩")
CASE "固有内衣着用条件"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 2)

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "魚料理/牛奶/鍋物/冰淇淋/丰盛/濃厚/豪華/芸術的/家庭的/软糯的/甜/甜/咸/咸/" + "冷/" * 3 + "冬物/" * 5)
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "辣/辣/" + "香料/春物/謎料理/" * 3)
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "氷結/得意料理＊冬物＊/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "美的/芸術的/別緻/" + "白/銀/氷/" * 3 + "水晶の/ゆきだるまの/冬の/白雪の/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "日/护臂/" * 3 + "春の/秋の/樱花の/向日葵の/西瓜の/春風の/秋風の/" * 5 + "火/夏の/" * 10)
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_74
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 雪人冰淇淋
RESULTS = 甜点
CALL 材料設定("冰淇淋")
DISH_TASTE = 洋食/冰淇淋/牛奶/甜/冷/冷/冬物/芸術的/氷結/
DISH_COMMENT = 像雪人一样堆在一起的冰淇淋。好想钻进被炉里吃。
RETURN 1

;IMAGE_I74_特殊服装(CHARA)-------------------------------------------------------------------
;キャラクター特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I74_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS
SELECTCASE CFLAG:74:差し替え適用
	;替换用画像
	CASE 1
		IF CHECK_NUDE(CHARA)
			;裸
		ELSE
			;服
			IF CFLAG:CHARA:睡眠 || CFLAG:CHARA:陪睡中 || 睡眠時間(CHARA)
				RESULTS = _下着
			ELSEIF IS_BUNNY(CHARA)
				RESULTS = _兔女郎
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[白衣天使套]]
				RESULTS = _护士服
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[晚宴礼服套装]]
				RESULTS = _晚礼服
			ELSEIF CHECK_CLO("bra_visible", CHARA)
				RESULTS = _下着
			ENDIF
		ENDIF
		
	CASEELSE
		;通常画像
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立絵
			ENDIF
		ELSE
			;服
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立絵
			ENDIF
		ENDIF
		
ENDSELECT

;IMAGE_I74_特殊表情(CHARA)--------------------------------------------------------------------
;角色特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"通常","笑顔","憤怒","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I74_特殊表情(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA

IF CFLAG:74:差し替え適用 == 1
	IF TALENT:CHARA:心情 == 1 || BASE:CHARA:情緒 > MAXBASE:CHARA:情緒 / 2 || TALENT:CHARA:恋慕
		IF RESULTS:0 == "通常"
			RESULTS:0 '= "微笑"
		ENDIF
		IF RESULTS:1 == "通常"
			RESULTS:1 '= "微笑"
		ENDIF
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;-------------------------------------------------------------------------------------------
@IMAGE_I74_特殊表情COM(CHARA, 指令)
#DIM CHARA
#DIM 指令
SELECTCASE CFLAG:74:差し替え適用

CASE 1
	;替换用画像
	IF CHECK_NUDE(CHARA)
	ELSE
		IF IS_BUNNY(CHARA)
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[白衣天使套]]
		ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[晚宴礼服套装]]
		ELSEIF CHECK_CLO("bra_visible", CHARA)
		ELSE
			SELECTCASE 指令
				CASE 305
					RESULTS = 膝枕覆盖
				CASE 6,12,313
					RESULTS = 揉胸覆盖
			ENDSELECT
		ENDIF
	ENDIF
CASEELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDSELECT
