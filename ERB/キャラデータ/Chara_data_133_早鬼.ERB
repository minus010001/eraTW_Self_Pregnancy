@CHARAMOVE_DATA_133(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 133
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
		SIF CFLAG:LOCAL:今日の運勢 % 15 == 0
			RETURN [[神子]]
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;是非曲直庁、冥界、八雲邸には行かない（入り口部分は出る時用）
			CASE P612法廷 TO P639八雲邸居間
				RETURN -100
			;他人的家には入らない
			CASE P608潤美的家, P609瓔花的家, P606日狭美の家, P607残無の家
				RETURN -100
			;霊長園には行かない
			CASE P642霊長園 TO P645磨弓的房間
				RETURN -100
			;他所の風呂には行かない
			CASE P646八千慧宅の風呂, P655尤魔宅の風呂
				RETURN -100
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 中有之道, 三途之川, 彼岸, 地獄, 畜生界
				RETURN 1
			CASE 山麓樹海, 玄武之澤, 大蛤蟆之池
				RETURN 1
			CASE 九天瀑布, 絶景之丘, 山之湖, 守矢神社境内
				RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
ENDSELECT

@EXIST_キャラデータ133
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ133(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "早鬼")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裤子）套")

CASE "便装：帽子"
	CALLF MAKE_INT(V_NAME, [[頭装備_牛仔帽]])
CASE "便装：上身衣服２"
	CALLF MAKE_INT(V_NAME, [[上身衣服_はだけ可能_女式衬衫]])
CASE "便装：裙子"
	CALLF MAKE_INT(V_NAME, [[裙子_裙子]])
CASE "便装：靴"
	CALLF MAKE_INT(V_NAME, [[靴_长靴]])
CASE "便装：襪子"
	CALLF MAKE_INT(V_NAME, [[襪子_袜子]])
CASE "便装：其他１"
	CALLF MAKE_INT(V_NAME, [[其他衣装_结丝巾]])

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "蹄铁印纹的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "黑色紧身裤")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "黑翼胸罩")
CASE "固有内衣着用条件"
	SIF !RAND:(3 - (2 * 勝負内衣判定([[早鬼]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 2)

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "肉料理/香料/中華/鍋物/丰盛/ジャンキー/濃厚/温暖/豪華/野趣/豪快/サクサク/畜生流/" + "辣/胡萝卜/" * 3)
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "質素/" + "甜/酸/简易/淡白/" * 2 + "天界風/" * 3)
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "畜生流/得意料理＊豪快＊/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "黒/赤/混沌/かっこいい/シンプル/パワフル/壮大/" + "狼の/" * 3 + "马の/天馬の/结丝巾/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "秩序/繊細/知的/" + "神霊/書物/機械/" * 3 + "埴轮の/水獭の/鹫の/龟の/龍の/埴轮/" * 10)
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_133
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 鬼形圆松饼
RESULTS = 甜点
CALL 材料設定("甜味料", "ナッツ")
DISH_TASTE = 洋食/卵/牛奶/辣/芸術的/野趣/豪快/サクサク/妖力/畜生流/
DISH_COMMENT = 形状妖艳的粒状点心。不能用于弹幕。
RETURN 1

;IMAGE_I133_特殊服装(CHARA)-------------------------------------------------------------------
;角色特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;   RESULTSに特殊服装を格納すること
;   書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I133_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS
SELECTCASE CFLAG:133:差し替え適用
	CASE 2
		IF CHECK_NUDE(CHARA)
		ELSE
			IF CFLAG:CHARA:睡眠 || CFLAG:CHARA:陪睡中 || 睡眠時間(CHARA)
				RESULTS = _下着
			ELSEIF IS_BUNNY(CHARA)
				RESULTS = _バニーギャル
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[骑手服]]
				RESULTS = _ボディストッキング
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[日式学校运动服]]
				RESULTS = _トレセン体操服
			ELSEIF IS_SCHOOLUNIFORM(CHARA)
				RESULTS = _トレセン制服
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 61
				RESULTS = _ウェディングドレス
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[死库水泳衣]] || TEQUIP:CHARA:SWIMMING
				RESULTS = _トレセン水着
			ELSEIF CHECK_CLO("bra_visible", CHARA)
				RESULTS = _下着
			ENDIF
		ENDIF
	CASE 0,1
	IF CHECK_NUDE(CHARA)
	ELSE
	ENDIF
ENDSELECT

;IMAGE_I133_体形(CHARA)-----------------------------------------------------------------------
;角色特有の体形表示条件
;エフェクトを共有する体形差分はこちら
;服装を指定しても自動で表示されるので注意
;使用時注意点:
;   RESULTSに体形を格納すること
;   書式は %体形%%服装% 適宜"_"等入れること
;   "巨乳"は本編で実装済み %体形%巨乳%服装% で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I133_体形(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:133:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I133_特殊表情(CHARA)--------------------------------------------------------------------
;角色特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;   RESULTS:0に表情を格納
;   RESULTS:1にマウスオーバー時の表情を格納すること
;   "通常","笑顔","憤怒","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I133_特殊表情(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA

IF CFLAG:133:差し替え適用 == 2
	IF TALENT:CHARA:心情 == 1 || BASE:CHARA:情緒 > MAXBASE:CHARA:情緒 / 2 || TALENT:CHARA:恋慕
		IF RESULTS:0 == "通常"
			RESULTS:0 '= "微笑"
		ENDIF
		IF RESULTS:1 == "通常"
			RESULTS:1 '= "微笑"
		ENDIF
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I133_特殊表情COM(CHARA, 指令)-------------------------------------------------------
;直前の指令で変化する角色特有の表情表示条件
;時間停止中は変化しない表情はこちら
;使用時注意点:
;   RESULTS:0に表情を格納
;   RESULTS:1にマウスオーバー時の表情を格納すること
;   "自慰","愛撫","性交"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I133_特殊表情COM(CHARA, 指令)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM 指令

SELECTCASE CFLAG:133:差し替え適用
	;替换用画像
	CASE 2
		IF CHECK_NUDE(CHARA)
		ELSE
			;服
			IF IS_BUNNY(CHARA)
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[骑手服]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[日式学校运动服]]
			ELSEIF IS_SCHOOLUNIFORM(CHARA)
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[死库水泳衣]] || TEQUIP:CHARA:SWIMMING
			ELSEIF CHECK_CLO("bra_visible", CHARA)
			ELSE
				SELECTCASE 指令
					CASE 305
						RESULTS = 膝枕覆盖
					CASE 6,12,313
						RESULTS = 揉胸覆盖
				ENDSELECT
			ENDIF
		ENDIF
	CASEELSE
		;通常画像
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
ENDSELECT
