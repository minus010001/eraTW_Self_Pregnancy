@CHARAMOVE_DATA_64(ARG, ARG:2)
#LOCALSIZE 1
#LOCALSSIZE 1
LOCAL = 64
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;地霊殿には行かない
			CASE P901地霊殿前 TO P930旧血の池地獄, P947ちやりの棲み処
				RETURN -100
			;酒場と鬼的聚集場所に行きやすい
			CASE P933酒場, P938鬼的聚集場所
				RETURN 5
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			;春と秋だけ
			CASE 呑屋小道
				SIF GROUPMATCH(DAY:2, 1, 3)
					RETURN 1
			CASE 山麓樹海, 玄武之澤, 大蛤蟆之池
				SIF GROUPMATCH(DAY:2, 1, 3)
					RETURN 1
			CASE 幻想風穴, 地獄的深道, 旧地獄街道
				RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
		;夜はよく酒場
		SIF CFLAG:LOCAL:今日の運勢 % 4 == 0 && INRANGE(TIME, 1200, 1440)
			TCVAR:LOCAL:引力点 = P933酒場
ENDSELECT

@EXIST_キャラデータ64
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラデータ64(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "勇儀")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（襦袢）套")

CASE "便装：靴"
	CALLF MAKE_INT(V_NAME, [[靴_木屐]])
CASE "便装：裙子"
	CALLF MAKE_INT(V_NAME, [[裙子_裙子]])
CASE "便装：上身衣服２"
	CALLF MAKE_INT(V_NAME, [[上身衣服_はだけ可能_女式衬衫]])
CASE "便装：其他１"
	CALLF MAKE_INT(V_NAME, [[其他衣装_拘束具]])
CASE "便装：飾品"
	CALLF MAKE_INT(V_NAME, [[飾品_酒杯]])

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "超强的")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "虎皮鬼胖次")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "裹胸")
CASE "固有内衣着用条件"
	SIF !RAND:(1 + (2 * 勝負内衣判定([[勇儀]])))
		CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 3)

;特殊衣装
CASE "デート衣装", "人里デート衣装", "おうちデート衣装"
	CALLF MAKE_INT(V_NAME, [[勇仪和服套装]])

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "肉料理/香料/和食/鍋物/下酒菜/辣/丰盛/濃厚/芸術的/野趣/豪快/サクサク/地底風/天狗流/" + "咸/" * 3)
CASE "料理：讨厌的味道"
	CALLF MAKE_STR(V_NAME, "简易/淡白/甜/甜/謎料理/謎料理/謎料理/化学的/化学的/化学的/" + "小人族秘伝の味/" * 5)
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "地底風/得意料理＊豪快＊/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "赤/金/混沌/かっこいい/パワフル/壮大/" + "食器/鎖の/酒/" * 3 + "鬼の/華の/鬼力の/力の/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "精神/ポップ/繊細/知的/ロリータ/" + "刃物/弾/書物/機械/" * 3 + "河童の/天狗の/" * 10)
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_64
#LOCALSIZE 1
#LOCALSSIZE 1
DISH_NAME = 牡丹火鍋
RESULTS = 主食
CALL 材料設定("獣肉")
DISH_TASTE = 肉料理/野菜/和食/鍋物/丰盛/濃厚/芸術的/野趣/咸/
DISH_COMMENT = 使用和妖怪非常接近的野猪的肉的锅料理。排列整齐的肉就像牡丹花一样美丽。
RETURN 1

;IMAGE_I64_特殊服装(CHARA)-------------------------------------------------------------------
;角色特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I64_特殊服装(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS

SELECTCASE CFLAG:64:差し替え適用
	;替换用画像
	CASE 2
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立絵
			ENDIF
		ELSE
			;服
			IF CFLAG:CHARA:睡眠 || CFLAG:CHARA:陪睡中 || 睡眠時間(CHARA)
				RESULTS = _下着
			ELSEIF IS_BUNNY(CHARA)
				RESULTS = _裙甲
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[神秘宝藏礼装]]
				RESULTS = _神秘宝藏
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[不知火舞忍装]]
				RESULTS = _不知火
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[女仆装]]
				RESULTS = _女仆
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[女牛仔套装]]
				RESULTS = _牛仔
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[啦啦队制服]]
				RESULTS = _啦啦队
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[婚紗套]]
				RESULTS = _婚纱
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[花魁盛装]]
				RESULTS = _花魁
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[勇仪和服套装]]
				RESULTS = _和服
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[日式学校运动服]]
				RESULTS = _布鲁马
			ELSEIF EQUIP:CHARA:外搭 == 11
				RESULTS = _连帽衫
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 52 || CURRENT_CLOTHES_SET(CHARA) == 53 || TEQUIP:CHARA:SWIMMING
				RESULTS = _比基尼
			ELSEIF CHECK_CLO("bra_visible", CHARA)
				RESULTS = _下着
			ELSEIF SPIRIT_马尾判定_64()
				RESULTS = _马尾
			ENDIF
		ENDIF
	CASE 1
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立絵
			ENDIF
		ELSE
			IF CURRENT_CLOTHES_SET(CHARA) == 75
				RESULTS = _黄昏
			ENDIF
		ENDIF
	CASE 0
		;通常画像
		IF CHECK_NUDE(CHARA)
			;裸
			SIF BATHROBE_SWITCH && BATHROOM() && !CFLAG:诶嘿嘿
				RESULTS = _浴袍
		ELSE
			;服
			SIF EQUIP:CHARA:上身衣服２ == 3
				RESULTS = _睡衣
		ENDIF
ENDSELECT

;IMAGE_I64_特殊表情(CHARA)--------------------------------------------------------------------
;角色特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"通常","笑顔","憤怒","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I64_特殊表情(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA

SELECTCASE CFLAG:64:差し替え適用
	;替换用画像
	CASE 2
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立絵
			ENDIF
		ELSE
			;服
			IF CFLAG:CHARA:睡眠
				RESULTS = 睡眠覆盖
				RESULTS:1 = 睡眠覆盖
			ENDIF
		ENDIF
	CASE 1
	CASE 0
		;通常画像
		IF CHECK_NUDE(CHARA)
			;裸
		ELSE
			;服
		ENDIF
ENDSELECT

;IMAGE_I64_特殊表情COM(CHARA, 指令)-------------------------------------------------------
;直前の指令で変化する角色特有の表情表示条件
;時間停止中は変化しない表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"自慰","愛撫","性交"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I64_特殊表情COM(CHARA, 指令)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM 指令

SELECTCASE CFLAG:64:差し替え適用
	CASE 2
		IF CHECK_NUDE(CHARA)
			SELECTCASE 指令
				CASE 80
					RESULTS = 手交覆盖
				CASE 81, 88, 140
					RESULTS = 口交覆盖
				CASE 82
					RESULTS = 乳交覆盖
				CASE 85
					RESULTS = 足交覆盖
				CASE 8
					IF EX:CHARA:膣内精液
						RESULTS = 张开阴唇2覆盖
					ELSE
						RESULTS = 张开阴唇覆盖
					ENDIF
				CASE 13
					IF EX:CHARA:肛門内精液
						RESULTS = 张开菊穴2覆盖
					ELSE
						RESULTS = 张开菊穴覆盖
					ENDIF
				CASEELSE
					IF MASTER_POSE(SET_V,SET_P) == CHARA
						SELECTCASE TEQUIP:CHARA:110
							CASE POSE_MISSIONARY
							RESULTS = V_正面覆盖
							CASE POSE_COWGIRL,POSE_LOTUS,POSE_SUSPENDED_CONGRESS
							RESULTS = V_上位覆盖
							CASE POSE_DOGGYSTYLE,POSE_REVLOTUS,POSE_REV_COWGIRL
							RESULTS = V_背后覆盖
						ENDSELECT
					ELSEIF MASTER_POSE(SET_A,SET_P) == CHARA
						SELECTCASE TEQUIP:CHARA:110
							CASE POSE_MISSIONARY
							RESULTS = A_正面覆盖
							CASE POSE_COWGIRL,POSE_LOTUS,POSE_SUSPENDED_CONGRESS
							RESULTS = A_上位覆盖
							CASE POSE_DOGGYSTYLE,POSE_REVLOTUS,POSE_REV_COWGIRL
							RESULTS = A_背后覆盖
						ENDSELECT
					ENDIF
			ENDSELECT



		ELSE
			IF IS_BUNNY(CHARA)
			ELSEIF EQUIP:CHARA:外搭 == [[神秘宝藏礼装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[不知火舞忍装]]
			ELSEIF EQUIP:CHARA:連衣裙 == 23
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[女仆装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[女牛仔套装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[啦啦队制服]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[婚紗套]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[花魁盛装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[勇仪和服套装]]
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == [[日式学校运动服]]
			ELSEIF EQUIP:CHARA:外搭 == 11
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 52 || CURRENT_CLOTHES_SET(CHARA) == 53 || TEQUIP:CHARA:SWIMMING
			ELSEIF CHECK_CLO("bra_visible", CHARA)
			ELSE
				SELECTCASE 指令
					CASE 305
						RESULTS = 膝枕覆盖
					CASE 310
						RESULTS = 揉屁股覆盖
					CASE 6,12,313
						RESULTS = 揉胸覆盖
				ENDSELECT
			ENDIF
		ENDIF
	CASEELSE
		IF CHECK_NUDE(CHARA)
			;裸
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立ち絵
			ENDIF
		ELSE
			;服
			IF FLAG:立絵種類
				;顔絵
			ELSE
				;立ち絵
			ENDIF
		ENDIF
ENDSELECT

;IMAGE_I64_差分(CHARA)-----------------------------------------------------------------------
;角色特有の差分表示条件
;裸やエフェクトを共有しない画像はこちら
;使用時注意点:
;	RESULTSに差分を格納すること
;	書式は {CHARA}%差分% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I64_差分(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
VARSET RESULTS

IF CFLAG:64:差し替え適用 == 1
	;替换用画像
ELSEIF CFLAG:64:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	IF CURRENT_CLOTHES_SET(CHARA) == 75 && !CHECK_NUDE(CHARA)
		RESULTS = _黄昏
	ENDIF
ENDIF

;IMAGE_I64_特殊エフェクト(CHARA)--------------------------------------------------------------------
;角色特有のエフェクト表示条件
;使用時注意点:
;	RESULTS配列に特殊エフェクトを格納
;	RETURNで追加するエフェクト数を返却すること
;-------------------------------------------------------------------------------------------
@IMAGE_I64_特殊エフェクト(CHARA)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:64:差し替え適用 == 2
	;差し替え画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
		IF CHECK_NUDE(CHARA) && CFLAG:CHARA:1998 == 1
			RESULTS:エフェクト数 = 阴毛
			エフェクト数 += 1
		ENDIF
	ENDIF
ELSEIF CFLAG:64:差し替え適用 == 1
	;差し替え画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
		IF CURRENT_CLOTHES_SET(CHARA) == 75
			IF CFLAG:CHARA:脸上精液
				;胸ぶっかけ
				RESULTS:エフェクト数 = 顔精液
				エフェクト数 += 1
			ENDIF
			IF CFLAG:CHARA:口内精液
				;フェラ
				RESULTS:エフェクト数 = 胸精液
				エフェクト数 += 1
			ENDIF
		ENDIF
	ELSE
		;立ち絵
	ENDIF
ENDIF

RETURN エフェクト数