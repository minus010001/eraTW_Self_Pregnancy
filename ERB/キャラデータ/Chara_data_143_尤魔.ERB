@CHARAMOVE_DATA_143(ARG, ARG:2)
LOCAL = 143
SELECTCASE ARG
	;連動設定（RETURN=依存する角色, RESULT:1～3=対等に連動する角色）
	CASE 1
	;特殊生活時間設定（CFLAG:LOCAL:生活時間補正を設定する）
	CASE 3
	;移動傾向（±～10=行きやすい/行きにくい, -100=行かない）
	CASE 4
		SELECTCASE ARG:2
			;是非曲直庁、冥界、八雲邸には行かない（入り口部分は出る時用）
			CASE P612法廷 TO P639八雲邸居間
				RETURN -100
			;他人的家には入らない
			CASE P608潤美的家, P609瓔花的家
				RETURN -100
			;霊長園には行かない
			CASE P642霊長園 TO P645磨弓的房間
				RETURN -100
			;他所の風呂には行かない
			CASE P652早鬼宅の風呂, P646八千慧宅の風呂
				RETURN -100
		ENDSELECT
	;出現場所
	CASE 7
		SELECTCASE ARG:2
			CASE 中有之道, 三途之川, 彼岸, 地獄, 畜生界
				RETURN 1
			CASE 旧地獄街道, 灼熱地獄跡
				RETURN 1
		ENDSELECT
	;引力設定（CFLAG:LOCAL:引力点を設定する）
	CASE 8
ENDSELECT

@EXIST_角色データ143
#LOCALSIZE 1
#LOCALSSIZE 1

@角色データ143(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "稱呼"
	CALLF MAKE_STR(V_NAME, "尤魔")
;こっから衣装設定
CASE "今天的睡衣"
	CALLF MAKE_STR(V_NAME, "睡衣（裙子+兜帽")
CASE "便装：連衣裙"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("連衣裙", "連衣裙"))
CASE "便装：其他１"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("其他１", "勺子"))

CASE "固有内衣形容詞"
	CALLF MAKE_STR(V_NAME, "モコモコの")
CASE "固有下半身内衣名"
	CALLF MAKE_STR(V_NAME, "ひつじさん胖次")
CASE "固有上半身内衣"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有上半身内衣名"
	CALLF MAKE_STR(V_NAME, "ひつじさんブラ")
CASE "固有内衣着用条件"
	CALLF MAKE_INT(V_NAME, 1)
CASE "固有内衣半透明(前)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "固有内衣半透明(後)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "固有内衣布料厚度"
	CALLF MAKE_INT(V_NAME, 3)

;料理関連
CASE "料理：喜欢的味道"
	CALLF MAKE_STR(V_NAME, "中華/垃圾/濃厚/畜生流/" + "辣/丰盛/豪快/" * 3)
CASE "料理：讨厌的味道"
	;例外的に讨厌的味道が極端に少ない
	CALLF MAKE_STR(V_NAME, "質素/简易/淡白/")
CASE "料理：助手効果"
	CALLF MAKE_STR(V_NAME, "畜生流/")
;贈り物関連
CASE "感性：好き"
	CALLF MAKE_STR(V_NAME, "白/混沌/可怕的/知的/" + "強欲な/血/" * 3  + "羊の/鹫の/剛欲な/" * 5)
CASE "感性：嫌い"
	CALLF MAKE_STR(V_NAME, "秩序/誇張/芸術的/" + "神霊/" * 3 + "埴轮の/水獭の/狼の/龟の/龍の/埴轮/" * 10)
ENDSELECT

;-------------------------------------------------
;固有料理設定
;DISH_NAME          料理名
;RESULTS            料理の種類（軽食/主食/甜点）
;CALL 材料設定("")  材料の指定。"汎用"を入れると自動で汎用食材になる
;DISH_TASTE         味の設定
;DISH_COMMENT       （任意）一行コメント
;DISH_TIME          （任意）調理にかかる時間
;DISH_MAXLIMIT      （任意）料理の持続時間
;-------------------------------------------------
@SPECIAL_MENU_143
DISH_NAME = 刚欲兽神的晚餐
RESULTS = 主食
CALL 材料設定("葱")
DISH_TASTE = 中華/鍋物/香料/辣/丰盛/丰盛/濃厚/温暖/温暖/新鲜/豪快/怪诞/妖力/畜生流/
DISH_COMMENT = 刚欲同盟惯例的巨大杂烩中华锅。是常人吃不完的量。
RETURN 1

;********************************************************************************************
;角色別画像表示貂プレ IMAGE_I143_○○.ERB
;2020/11/07 画像表示修正パッチv2.0 版
;制作者		:画像修正パッチの人
;Licence	:ライセンスフリー(CC0)
;
;角色特有の表情やエフェクト画像を個別に表示できるようにしたもの。
;使用方法は ERB\ステータス表示関連\角色別画像表示 フォルダにある他角色のファイルを参照してください。
;使用時は"XX"を角色番号に全置換すること。
;********************************************************************************************
;IMAGE_I143_特殊服装(CHARA)-------------------------------------------------------------------
;角色特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I143_特殊服装(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
	ELSE
		;服
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
	ENDIF
ELSE
	;通常画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
	ELSE
		;服
		IF FLAG:立絵種類
			;顔絵
		ELSE
			;立絵
		ENDIF
	ENDIF
ENDIF

;IMAGE_I143_体形(CHARA)-----------------------------------------------------------------------
;角色特有の体形表示条件
;エフェクトを共有する体形差分はこちら
;服装を指定しても自動で表示されるので注意
;使用時注意点:
;	RESULTSに体形を格納すること
;	書式は %体形%%服装% 適宜"_"等入れること
;	"巨乳"は本編で実装済み %体形%巨乳%服装% で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I143_体形(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I143_特殊表情(CHARA)--------------------------------------------------------------------
;角色特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"通常","笑顔","憤怒","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I143_特殊表情(CHARA)
#DIM CHARA

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I143_特殊表情COM(CHARA, 指令)-------------------------------------------------------
;直前の指令で変化する角色特有の表情表示条件
;時間停止中は変化しない表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"自慰","愛撫","性交"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I143_特殊表情COM(CHARA, 指令)
#DIM CHARA
#DIM 指令

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I143_差分(CHARA)-----------------------------------------------------------------------
;角色特有の差分表示条件
;裸やエフェクトを共有しない画像はこちら
;使用時注意点:
;	RESULTSに差分を格納すること
;	書式は {CHARA}%差分% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I143_差分(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I143_特殊照れ(CHARA)--------------------------------------------------------------------
;角色特有の照れエフェクト表示条件
;使用時注意点:
;	RESULTSに特殊照れを格納すること
;	書式は %特殊照れ%照れ
;	"裸"は本編で実装済み %特殊照れ%裸照れ で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I143_特殊照れ(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I143_特殊吐息(CHARA)--------------------------------------------------------------------
;角色特有の吐息エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊吐息を格納すること
;	書式は %特殊吐息%吐息
;	"裸"は本編で実装済み %特殊吐息%裸吐息 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I143_特殊吐息(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I143_特殊汗(CHARA)--------------------------------------------------------------------
;角色特有の汗エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊汗を格納すること
;	書式は %特殊汗%汗
;	"裸"は本編で実装済み %特殊汗%裸汗 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I143_特殊汗(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

;IMAGE_I143_特殊エフェクトチェック(CHAR, エフェクト)--------------------------------------------
;角色特有のエフェクト判定  特殊エフェクトを直接指定したい場合に参照
;引数 "エフェクト" に固有のエフェクトが含まれるかを判定する
;例の街道、STRFINDを使用してください
;-------------------------------------------------------------------------------------------
@IMAGE_I143_特殊エフェクトチェック(CHARA, エフェクト)
#DIM CHARA
#DIMS エフェクト

IF CFLAG:CHARA:差し替え適用
	;替换用画像
ELSE
	;通常画像
	;SIF STRFIND(エフェクト, "母乳") != -1
	;	RETURN 1
	SIF STRFIND(エフェクト, "顔精液") != -1
		RETURN 1
	SIF STRFIND(エフェクト, "口精液") != -1
		RETURN 1
ENDIF

RETURN 0

;IMAGE_I143_特殊エフェクト(CHARA)--------------------------------------------------------------------
;角色特有のエフェクト表示条件
;使用時注意点:
;	RESULTS配列に特殊エフェクトを格納
;	RETURNで追加するエフェクト数を返却すること
;-------------------------------------------------------------------------------------------
@IMAGE_I143_特殊エフェクト(CHARA)
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
		IF CFLAG:CHARA:脸上精液
			RESULTS:エフェクト数 = 顔精液
			エフェクト数 += 1
		ENDIF
		IF CFLAG:CHARA:口内精液
			RESULTS:エフェクト数 = 口精液
			エフェクト数 += 1
		ENDIF
	ELSE
		;立絵
	ENDIF
ENDIF

RETURN エフェクト数

;IMAGE_I143_自慰エフェクト(CHARA)--------------------------------------------------------------------
;角色特有の自慰エフェクト表示条件
;使用時注意点:
;	RESULTS配列に自慰エフェクトを格納
;	RETURNで追加するエフェクト数を返却すること
;-------------------------------------------------------------------------------------------
@IMAGE_I143_自慰エフェクト(CHARA)
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:CHARA:差し替え適用
	;替换用画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:立絵種類
		;顔絵
	ELSE
		;立絵
	ENDIF
ENDIF

RETURN エフェクト数
