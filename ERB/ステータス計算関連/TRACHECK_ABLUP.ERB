@SOURCE_ABLUP(ARG)
#DIM NEED_EXP, 2
#DIM DYNAMIC TALENT278_ADJUST            ; 实际从等级中减去的数值

; 当启用时，调整值 = TALENT278 本身
; 如果 TALENT278 为正，减去它 → 所需经验等级降低（升级变快）
; 如果 TALENT278 为负，减去它等于加上绝对值 → 所需经验等级提高（升级变慢）
SIF 悟性系统开关 == 1
	TALENT278_ADJUST = TALENT:ARG:悟性

IF ABL:ARG:戦闘能力 < 6 + 2* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(42, EXPLV:(ABL:ARG:戦闘能力 + 3 - TALENT278_ADJUST))
	IF EXP:ARG:戦闘経験 > NEED_EXP || (EXP:ARG:戦闘経験 > 5 + 2* MAGIC_DLC_等级重置 && ABL:ARG:戦闘能力 == 0)
		;CALL ChangeABL(ARG, "戦闘能力", 1)
		ABL:ARG:戦闘能力 ++
		IF !TALENT:ARG:高难开局 || TALENT:ARG:神性
			SIF MP_GRADE:ARG:0 < ABL:ARG:戦闘能力
				CALL CHANGE_MAXBASE(ARG, BASE_法力, MP_FACTOR(ARG) * 5 * (ABL:ARG:戦闘能力-MP_GRADE:ARG:0),1)
			MP_GRADE:ARG:0 = ABL:ARG:戦闘能力	
		ENDIF
		PRINTFORMW %CALLNAME:ARG%的战斗能力变成了LV{ABL:ARG:戦闘能力}
		;技能レベルUP口上,1,戦闘能力
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 1)
	ENDIF
ENDIF
IF ABL:ARG:話術技能 < 6 + 2* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(41, EXPLV:(ABL:ARG:話術技能 + 3 - TALENT278_ADJUST))
	IF EXP:ARG:会話経験 > NEED_EXP
		ABL:ARG:話術技能 ++
		PRINTFORMW %CALLNAME:ARG%的话术技能变成了LV{ABL:ARG:話術技能}
		;技能レベルUP口上,2,话术技能
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 2)
	ENDIF
ENDIF

IF ABL:ARG:清掃技能 < 6 + 4* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(40, EXPLV:(ABL:ARG:清掃技能 + 3 - TALENT278_ADJUST))
	IF EXP:ARG:清掃経験 > NEED_EXP
		ABL:ARG:清掃技能 ++
		PRINTFORMW %CALLNAME:ARG%的清扫技能变成了LV{ABL:ARG:清掃技能}
		;技能レベルUP口上,3,清扫技能
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 3)
	ENDIF
ENDIF
IF ABL:ARG:教養 < 6 + 2* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(43, EXPLV:(ABL:ARG:教養 + 3 - TALENT278_ADJUST))
	IF EXP:ARG:学習経験 > NEED_EXP || (EXP:ARG:学習経験 > 5 + 2* MAGIC_DLC_等级重置 && ABL:ARG:教養 == 0)
		ABL:ARG:教養 ++
		PRINTFORMW %CALLNAME:ARG%的教养变成了LV{ABL:ARG:教養}
		;技能レベルUP口上,4,教养
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 4)
		SIF ABL:ARG:教養 == 1 && !ARG
			PRINTW 可以使用媚药了
	ENDIF
ENDIF
IF ABL:ARG:料理技能 < 6 + 4* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(44, EXPLV:(ABL:ARG:料理技能 + 3 - TALENT278_ADJUST))
	IF EXP:ARG:料理経験 > NEED_EXP || (EXP:ARG:料理経験 > 5 + 4* MAGIC_DLC_等级重置 && ABL:ARG:料理技能 == 0)
		ABL:ARG:料理技能 ++
		PRINTFORMW %CALLNAME:ARG%的料理技能变成了LV{ABL:ARG:料理技能}
		;技能レベルUP口上,5,料理技能
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 5)
		IF ARG == MASTER && ABL:ARG:料理技能 >= 3 && !TALENT:MASTER:大胃王
			PRINTFORMW %CALLNAME:MASTER%获得了【大胃王】
			TALENT:MASTER:大胃王 = 1
		ENDIF
	ENDIF
ENDIF
IF TALENT:ARG:採集Lv < 6 + 4* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(46, EXPLV:(TALENT:ARG:採集Lv + 3 - TALENT278_ADJUST))
	IF EXP:ARG:採集経験 > NEED_EXP || (EXP:ARG:採集経験 > 5 + 4* MAGIC_DLC_等级重置 && TALENT:ARG:採集Lv == 0)
		TALENT:ARG:採集Lv ++
		PRINTFORMW %CALLNAME:ARG%的采集Lv变成了LV{TALENT:ARG:採集Lv}
		;技能レベルUP口上,6,采集Lv
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 6)
	ENDIF
ENDIF
IF ABL:ARG:音楽技能 < 6 + 2* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(45, (EXPLV:(ABL:ARG:音楽技能 + 3 - TALENT278_ADJUST) * 2))
	IF (MAX(EXP:ARG:演奏経験, EXP:ARG:歌唱経験, EXP:ARG:舞踏経験) > NEED_EXP) || (MAX(EXP:ARG:演奏経験, EXP:ARG:歌唱経験, EXP:ARG:舞踏経験) > 20 && ABL:ARG:音楽技能 == 0)
		ABL:ARG:音楽技能 ++
		PRINTFORMW %CALLNAME:ARG%的音乐技能变成了LV{ABL:ARG:音楽技能}
		SIF ARG == MASTER && ABL:ARG:音楽技能 == 2
			PRINTW 可以使用小提琴了
		;技能レベルUP口上,7-0,音楽技能（7-1～はEVENTTURNENDで）
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 7, 0)

		[SKIPSTART]
		IF ABL:ARG:音楽技能 != 2
		ELSEIF ARG == MASTER
			PRINTW 可以使用小提琴了
		ELSEIF TALENT:ARG:音楽知識
		ELSE
			;おそらく、現状で経験を積む手段がない
			SELECTCASE MAX(EXP:ARG:演奏経験, EXP:ARG:歌唱経験, EXP:ARG:舞踏経験)
			CASE EXP:ARG:演奏経験
				PRINTFORMW 在経験的积累下%CALLNAME:ARG%可以在他人面前演奏楽器了
				TALENT:ARG:音楽知識 = 1
			CASE EXP:ARG:歌唱経験
				PRINTFORMW 在経験的积累下%CALLNAME:ARG%可以在他人面前展示歌喉了
				TALENT:ARG:音楽知識 = 2
			CASE EXP:ARG:舞踏経験
				PRINTFORMW 在経験的积累下%CALLNAME:ARG%可以在他人面前表演舞蹈了
				TALENT:ARG:音楽知識 = 3
			CASEELSE
		ENDIF
		[SKIPEND]
	ENDIF
ENDIF
IF ARG == MASTER && EXP:ARG:演奏経験 >= 1000 && ABL:ARG:技巧 >= 3 && !TALENT:ARG:音楽知識 == 1
	PRINTFORMW %CALLNAME:MASTER%乐器演奏的造诣变深了
	PRINTFORMW %CALLNAME:MASTER%获得了[乐器知识]
	TALENT:ARG:音楽知識 = 1
ENDIF
IF ARG == MASTER && EXP:ARG:演奏経験 >= 5000 && !TALENT:ARG:音感 == 1
	PRINTFORMW %CALLNAME:MASTER%的音感得到了相当的锻炼
	PRINTFORMW %CALLNAME:MASTER%获得了[音感]
	TALENT:ARG:音感 = 1
ELSEIF MAGIC_DLC_等级重置 && ARG == MASTER && EXP:ARG:演奏経験 >= 10000 && TALENT:ARG:音感 < 2
	PRINTFORMW %CALLNAME:MASTER%的音感得到了相当的锻炼
	PRINTFORMW %CALLNAME:MASTER%获得了[绝对音感]
	TALENT:ARG:音感 = 2
ENDIF

IF TALENT:ARG:釣りLv < 6 + 2* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(47, EXPLV:(TALENT:ARG:釣りLv + 3 - TALENT278_ADJUST))
	IF EXP:ARG:釣り経験 > NEED_EXP || (EXP:ARG:釣り経験 > 5 + 2* MAGIC_DLC_等级重置 && TALENT:ARG:釣りLv == 0)
		TALENT:ARG:釣りLv ++
		PRINTFORMW %CALLNAME:ARG%的钓鱼技能变成了LV{TALENT:ARG:釣りLv}
		;技能レベルUP口上,8,釣りLv
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 8)
	ENDIF
ENDIF

IF TALENT:ARG:調合Lv < 6 + 2* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(48, EXPLV:(TALENT:ARG:調合Lv + 3 - TALENT278_ADJUST))
	IF EXP:ARG:調合経験 > NEED_EXP || (EXP:ARG:調合経験 > 5 + 2* MAGIC_DLC_等级重置 && TALENT:ARG:調合Lv == 0)
		TALENT:ARG:調合Lv ++
		PRINTFORMW %CALLNAME:ARG%的调合技能变成了LV{TALENT:ARG:調合Lv}
		;技能レベルUP口上,9-0,调合Lv
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 9, 0)
	ENDIF
ENDIF

IF TALENT:ARG:伐採Lv < 6 + 2* MAGIC_DLC_等级重置
	NEED_EXP = CALC_ABL_DEMAND(49, EXPLV:(TALENT:ARG:伐採Lv + 3 - TALENT278_ADJUST))
	IF EXP:ARG:伐採経験 > NEED_EXP || (EXP:ARG:伐採経験 > 5 + 2* MAGIC_DLC_等级重置 && TALENT:ARG:伐採Lv == 0)
		TALENT:ARG:伐採Lv ++
		PRINTFORMW %CALLNAME:ARG%的采伐技能变成了LV{TALENT:ARG:伐採Lv}
		;技能レベルUP口上,10,采伐Lv
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 10)
	ENDIF
ENDIF

IF ABL:ARG:足 < 6
	NEED_EXP = CALC_ABL_DEMAND(50, EXPLV:(ABL:ARG:足 + 3 - TALENT278_ADJUST))
	LOCAL = EXP:ARG:足交经验
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:美脚 * 2 + TALENT:ARG:習得速度
	IF LOCAL * LOCAL:1 / 10 > NEED_EXP / 2
		ABL:ARG:足 ++
		PRINTFORMW %CALLNAME:ARG%的足技变得更高明了
		;技能レベルUP口上,17-0,足技
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 17, 0)
		IF ABL:ARG:足 >= 5 && !TALENT:ARG:美脚
			PRINTFORMW %CALLNAME:ARG%获得了[美脚]
			TALENT:ARG:美脚 = 1
			;技能レベルUP口上,17-1,美脚
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 17, 1)
		ENDIF
	ENDIF
ENDIF
IF ABL:ARG:指 < 6
	NEED_EXP = CALC_ABL_DEMAND(50, EXPLV:(ABL:ARG:指 + 3 - TALENT278_ADJUST))
	LOCAL = EXP:ARG:手淫経験
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:器用な指 * 2 + TALENT:ARG:習得速度
	IF LOCAL * LOCAL:1 / 10 > NEED_EXP / 2; && EXP_UP(24,ARG)
		ABL:ARG:指 ++
		PRINTFORMW %CALLNAME:ARG%的手指变得灵活了
		;技能レベルUP口上,11-0,指
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 11, 0)
		IF ABL:ARG:指 >= 5 && !TALENT:ARG:器用な指
			PRINTFORMW %CALLNAME:ARG%获得了[灵活手指]
			TALENT:ARG:器用な指 = 1
			;技能レベルUP口上,11-1,器用な指
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 11, 1)
		ENDIF
	ENDIF
ENDIF
IF ABL:ARG:舌 < 6
	NEED_EXP = CALC_ABL_DEMAND(51, EXPLV:(ABL:ARG:舌 + 3 - TALENT278_ADJUST))
	LOCAL = EXP:ARG:口淫経験
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:舌使い * 2 + TALENT:ARG:習得速度
	IF  LOCAL * LOCAL:1 / 10 > NEED_EXP; && EXP_UP(25,ARG)
		ABL:ARG:舌 ++
		PRINTFORMW %CALLNAME:ARG%的舌头变得灵活了
		;技能レベルUP口上,12-0,舌
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 12, 0)
		IF ABL:ARG:舌 >= 5 && !TALENT:ARG:舌使い
			PRINTFORMW %CALLNAME:ARG%获得了[灵舌]
			TALENT:ARG:舌使い = 1
			;技能レベルUP口上,12-1,舌使い
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 12, 1)
		ENDIF
	ENDIF
ENDIF
IF ABL:ARG:胸 < 6
	NEED_EXP = CALC_ABL_DEMAND(52, EXPLV:(ABL:ARG:胸 + 3 - TALENT278_ADJUST))
	NEED_EXP:1 = CALC_ABL_DEMAND(52, EXPLV:(ABL:ARG:胸 + 1))
	LOCAL = EXP:ARG:乳交経験 * 2 +  EXP:ARG:Ｂ経験 / 5 + EXP:ARG:噴乳経験  / 4
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:胸围 + TALENT:ARG:習得速度
	SIF FLAG:抱負 == 5
		LOCAL:1 ++
	SIF GETBIT (CFLAG:ARG:继承, 5)
		LOCAL:1 *= 4
	IF LOCAL * LOCAL:1 / 10 > NEED_EXP && EXP:ARG:乳交経験 + EXP:ARG:噴乳経験  / 5 > NEED_EXP:1; && EXP_UP(26,ARG)
		ABL:ARG:胸 ++
		PRINTFORMW %CALLNAME:ARG%在使用胸部的性技上的技术提高了
		;技能レベルUP口上,13-0,胸
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 13, 0)
		IF ABL:ARG:胸 >= 5 && TALENT:ARG:2 != 2 && !TALENT:ARG:淫乳
			PRINTFORMW %CALLNAME:ARG%获得了[淫乳]
			TALENT:ARG:淫乳 = 1
			;技能レベルUP口上,13-1,淫乳
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 13, 1)
		ENDIF
	ENDIF
ENDIF
IF ABL:ARG:腰 < 6
	NEED_EXP = CALC_ABL_DEMAND(53, EXPLV:(ABL:ARG:腰 + 3 - TALENT278_ADJUST))
	LOCAL = EXP:ARG:Ｖ性交経験 + EXP:ARG:Ａ性交経験 + EXP:ARG:挿入経験
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:習得速度
	SIF GETBIT (CFLAG:ARG:继承, 3) || GETBIT (CFLAG:ARG:继承, 4)
		LOCAL:1 *= 4
	IF LOCAL * LOCAL:1 / 10 > NEED_EXP
		ABL:ARG:腰 ++
		PRINTFORMW %CALLNAME:ARG%的腰技变得更高明了
		;技能レベルUP口上,14,腰
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 14)
	ENDIF
ENDIF
IF ABL:ARG:膣 < 6
	NEED_EXP = CALC_ABL_DEMAND(54, EXPLV:(ABL:ARG:膣 + 3 - TALENT278_ADJUST))
	NEED_EXP:1 = CALC_ABL_DEMAND(54, EXPLV:(ABL:ARG:膣 + 1))
	;広げすぎるとガバガバになっちゃいますよ
	LOCAL = EXP:ARG:Ｖ性交経験  * 2 +  EXP:ARG:Ｖ経験 / 5 + (EXP:ARG:Ｖ拡張経験 <= 10 ? EXP:ARG:Ｖ拡張経験 * 5 # 100 - EXP:ARG:Ｖ拡張経験 * 5)
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:習得速度
	SIF FLAG:抱負 == 3
		LOCAL:1 ++
	SIF GETBIT (CFLAG:ARG:继承, 3)
		LOCAL:1 *= 4
	IF LOCAL * LOCAL:1 / 10 > NEED_EXP && EXP:ARG:Ｖ性交経験 >  NEED_EXP:1; && EXP_UP(20,ARG)
		ABL:ARG:膣 ++
		PRINTFORMW %CALLNAME:ARG%的性交技术提高了
		;技能レベルUP口上,15-0,膣
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 15, 0)
		IF ABL:ARG:膣 >= 5 && TALENT:ARG:2 != 2 && !TALENT:ARG:淫壺
			PRINTFORMW %CALLNAME:ARG%获得了[淫壶]
			TALENT:ARG:淫壺 = 1
			;技能レベルUP口上,15-1,淫壺
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 15, 1)
		ENDIF
	ENDIF
ENDIF
IF ABL:ARG:肛門 < 6
	NEED_EXP = CALC_ABL_DEMAND(55, EXPLV:(ABL:ARG:肛門 + 3 - TALENT278_ADJUST))
	NEED_EXP:1 = CALC_ABL_DEMAND(55, EXPLV:(ABL:ARG:肛門 + 1))
	LOCAL = EXP:ARG:Ａ性交経験  * 2 +  EXP:ARG:Ａ経験 / 5 + (EXP:ARG:Ａ拡張経験 <= 25 ? EXP:ARG:Ａ拡張経験 * 2 # 100 - 2 * EXP:ARG:Ａ拡張経験)
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:習得速度
	SIF FLAG:抱負 == 4
		LOCAL:1 ++
	SIF GETBIT (CFLAG:ARG:继承, 4)
		LOCAL:1 *= 4
	IF LOCAL * LOCAL:1 / 10 > NEED_EXP && EXP:ARG:Ａ性交経験 > NEED_EXP:1; && EXP_UP(21,ARG)
		ABL:ARG:肛門 ++
		PRINTFORMW %CALLNAME:ARG%用肛门性交的技术提高了
		;技能レベルUP口上,16,肛门
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 16, 0)
		IF ABL:ARG:肛門 >= 5 && !TALENT:ARG:尻穴狂
			PRINTFORMW %CALLNAME:ARG%获得了[尻穴狂]
			TALENT:ARG:尻穴狂 = 1
			;技能レベルUP口上,16,尻穴狂
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 16, 1)
		ENDIF
	ENDIF
ENDIF

IF !TALENT:ARG:調合知識 && TALENT:ARG:調合Lv >= 3 && ABL:ARG:教養 >= 3
	PRINTFORML %CALLNAME:ARG%得到了【调合知识】
	TALENT:ARG:調合知識 = 1
	;技能レベルUP口上,9-1,調合知識
	CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 9, 1)
ENDIF
IF ABL:ARG:腰 >= 4 && ABL:ARG:戦闘能力 >= 4 && !TALENT:ARG:大力士
	PRINTFORML 将战斗与性技巧融会贯通，%CALLNAME:ARG%调整体位更加得心应手了
	PRINTFORMW %CALLNAME:ARG%获得了【大力士】！
	TALENT:ARG:大力士 = 1
	;技能レベルUP口上,18,大力士
	CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 18)
ENDIF
IF EXP:ARG:伐採経験 >= 1000 && !TALENT:ARG:大力士
	PRINTFORML 经过了无数次的挥斧，%CALLNAME:ARG%的肌肉变得强壮有力。
	PRINTFORMW %CALLNAME:ARG%获得了【大力士】！
	TALENT:ARG:大力士 = 1
	;技能レベルUP口上,18,大力士
	CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 18)
ENDIF
