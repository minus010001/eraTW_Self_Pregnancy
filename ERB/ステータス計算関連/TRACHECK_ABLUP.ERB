@SOURCE_ABLUP(ARG)
IF ABL:ARG:戦闘能力 < 6
	IF EXP:ARG:戦闘経験 > EXPLV:(ABL:ARG:戦闘能力 + 3) || (EXP:ARG:戦闘経験 > 5 && ABL:ARG:戦闘能力 == 0)
		;CALL ChangeABL(ARG, "戦闘能力", 1)
		ABL:ARG:戦闘能力 ++
		PRINTFORMW %CALLNAME:ARG%的戦闘能力变成了LV{ABL:ARG:戦闘能力}
		;技能レベルUP口上,1,戦闘能力
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 1)
	ENDIF
ENDIF
IF ABL:ARG:話術技能 < 6
	IF EXP:ARG:会話経験 > EXPLV:(ABL:ARG:話術技能 + 3)
		ABL:ARG:話術技能 ++
		PRINTFORMW %CALLNAME:ARG%的話術技能变成了LV{ABL:ARG:話術技能}
		;技能レベルUP口上,2,話術技能
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 2)
	ENDIF
ENDIF

IF ABL:ARG:清掃技能 < 6
	IF EXP:ARG:清掃経験 > EXPLV:(ABL:ARG:清掃技能 + 3)
		ABL:ARG:清掃技能 ++
		PRINTFORMW %CALLNAME:ARG%的清掃技能变成了LV{ABL:ARG:清掃技能}
		;技能レベルUP口上,3,清掃技能
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 3)
	ENDIF
ENDIF
IF ABL:ARG:教養 < 6
	IF EXP:ARG:学習経験 > EXPLV:(ABL:ARG:教養 + 3) || (EXP:ARG:学習経験 > 5 && ABL:ARG:教養 == 0)
		ABL:ARG:教養 ++
		PRINTFORMW %CALLNAME:ARG%的教養变成了LV{ABL:ARG:教養}
		;技能レベルUP口上,4,教養
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 4)
		SIF ABL:ARG:教養 == 1 && !ARG
			PRINTW 可以使用媚薬了
	ENDIF
ENDIF
IF ABL:ARG:料理技能 < 6
	IF EXP:ARG:料理経験 > EXPLV:(ABL:ARG:料理技能 + 3) || (EXP:ARG:料理経験 > 5 && ABL:ARG:料理技能 == 0)
		ABL:ARG:料理技能 ++
		PRINTFORMW %CALLNAME:ARG%的料理技能变成了LV{ABL:ARG:料理技能}
		;技能レベルUP口上,5,料理技能
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 5)
		IF ARG == MASTER && ABL:ARG:料理技能 >= 3 && !TALENT:MASTER:大胃王
			PRINTFORMW %CALLNAME:MASTER%获得了【大胃王】
			TALENT:MASTER:大胃王 = 1
		ENDIF
	ENDIF
ENDIF
IF TALENT:ARG:採集Lv < 6
	IF EXP:ARG:採集経験 > EXPLV:(TALENT:ARG:採集Lv + 3) || (EXP:ARG:採集経験 > 5 && TALENT:ARG:採集Lv == 0)
		TALENT:ARG:採集Lv ++
		PRINTFORMW %CALLNAME:ARG%的採集Lv变成了LV{TALENT:ARG:採集Lv}
		;技能レベルUP口上,6,採集Lv
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 6)
	ENDIF
ENDIF
IF ABL:ARG:音楽技能 < 6
	IF (MAX(EXP:ARG:演奏経験, EXP:ARG:歌唱経験, EXP:ARG:舞踏経験) > EXPLV:(ABL:ARG:音楽技能 + 3) * 2) || (MAX(EXP:ARG:演奏経験, EXP:ARG:歌唱経験, EXP:ARG:舞踏経験) > 20 && ABL:ARG:音楽技能 == 0)
		ABL:ARG:音楽技能 ++
		PRINTFORMW %CALLNAME:ARG%的音楽技能变成了LV{ABL:ARG:音楽技能}
		SIF ARG == MASTER && ABL:ARG:音楽技能 == 2
			PRINTW 可以使用小提琴了
		;技能レベルUP口上,7-0,音楽技能（7-1～はEVENTTURNENDで）
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 7, 0)

		[SKIPSTART]
		IF ABL:ARG:音楽技能 != 2
		ELSEIF ARG == MASTER
			PRINTW 可以使用小提琴了
		ELSEIF TALENT:ARG:音楽知識
		ELSE
			;おそらく、現状で経験を積む手段がない
			SELECTCASE MAX(EXP:ARG:演奏経験, EXP:ARG:歌唱経験, EXP:ARG:舞踏経験)
			CASE EXP:ARG:演奏経験
				PRINTFORMW 在経験的积累下%CALLNAME:ARG%可以在他人面前演奏楽器了
				TALENT:ARG:音楽知識 = 1
			CASE EXP:ARG:歌唱経験
				PRINTFORMW 在経験的积累下%CALLNAME:ARG%可以在他人面前展示歌喉了
				TALENT:ARG:音楽知識 = 2
			CASE EXP:ARG:舞踏経験
				PRINTFORMW 在経験的积累下%CALLNAME:ARG%可以在他人面前表演舞蹈了
				TALENT:ARG:音楽知識 = 3
			CASEELSE
		ENDIF
		[SKIPEND]
	ENDIF
ENDIF
IF ARG == MASTER && EXP:ARG:演奏経験 >= 1000 && ABL:ARG:技巧 >= 3 && !TALENT:ARG:音楽知識 == 1
	PRINTFORMW %CALLNAME:MASTER%楽器演奏的造詣变深了
	PRINTFORMW %CALLNAME:MASTER%获得了[楽器知識]
	TALENT:ARG:音楽知識 = 1
ENDIF
IF ARG == MASTER && EXP:ARG:演奏経験 >= 5000 && !TALENT:ARG:音感 == 1
	PRINTFORMW %CALLNAME:MASTER%的音感得到了相当的锻炼
	PRINTFORMW %CALLNAME:MASTER%获得了[相対音感]
	TALENT:ARG:音感 = 1
ENDIF

IF TALENT:ARG:釣魚Lv < 6
	IF EXP:ARG:釣魚経験 > EXPLV:(TALENT:ARG:釣魚Lv + 3) || (EXP:ARG:釣魚経験 > 5 && TALENT:ARG:釣魚Lv == 0)
		TALENT:ARG:釣魚Lv ++
		PRINTFORMW %CALLNAME:ARG%的钓鱼技能变成了LV{TALENT:ARG:釣魚Lv}
		;技能レベルUP口上,8,釣魚Lv
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 8)
	ENDIF
ENDIF

IF TALENT:ARG:調合Lv < 6
	IF EXP:ARG:調合経験 > EXPLV:(TALENT:ARG:調合Lv + 3) || (EXP:ARG:調合経験 > 5 && TALENT:ARG:調合Lv == 0)
		TALENT:ARG:調合Lv ++
		PRINTFORMW %CALLNAME:ARG%的調合技能变成了LV{TALENT:ARG:調合Lv}
		;技能レベルUP口上,9-0,調合Lv
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 9, 0)
	ENDIF
ENDIF

IF TALENT:ARG:伐採Lv < 6
	IF EXP:ARG:伐採経験 > EXPLV:(TALENT:ARG:伐採Lv + 3) || (EXP:ARG:伐採経験 > 5 && TALENT:ARG:伐採Lv == 0)
		TALENT:ARG:伐採Lv ++
		PRINTFORMW %CALLNAME:ARG%的伐採技能变成了LV{TALENT:ARG:伐採Lv}
		;技能レベルUP口上,10,伐採Lv
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 10)
	ENDIF
ENDIF

IF ABL:ARG:指 < 6
	LOCAL = EXP:ARG:手淫経験
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:靈活手指 * 2 + TALENT:ARG:習得速度
	IF LOCAL * LOCAL:1 / 10 > EXPLV:(ABL:ARG:指 + 3) / 2; && EXP_UP(24,ARG)
		ABL:ARG:指 ++
		PRINTFORMW %CALLNAME:ARG%的手指变得灵活了
		;技能レベルUP口上,11-0,指
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 11, 0)
		IF ABL:ARG:指 >= 5 && !TALENT:ARG:靈活手指
			PRINTFORMW %CALLNAME:ARG%获得了[靈活手指]
			TALENT:ARG:靈活手指 = 1
			;技能レベルUP口上,11-1,靈活手指
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 11, 1)
		ENDIF
	ENDIF
ENDIF
IF ABL:ARG:舌 < 6
	LOCAL = EXP:ARG:口淫経験
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:舌頭靈活 * 2 + TALENT:ARG:習得速度
	IF  LOCAL * LOCAL:1 / 10 > EXPLV:(ABL:ARG:舌 + 3); && EXP_UP(25,ARG)
		ABL:ARG:舌 ++
		PRINTFORMW %CALLNAME:ARG%的舌头变得灵活了
		;技能レベルUP口上,12-0,舌
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 12, 0)
		IF ABL:ARG:舌 >= 5 && !TALENT:ARG:舌頭靈活
			PRINTFORMW %CALLNAME:ARG%获得了[舌頭靈活]
			TALENT:ARG:舌頭靈活 = 1
			;技能レベルUP口上,12-1,舌頭靈活
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 12, 1)
		ENDIF
	ENDIF
ENDIF
IF ABL:ARG:胸 < 6
	LOCAL = EXP:ARG:乳交経験 * 2 +  EXP:ARG:Ｂ経験 / 5 + EXP:ARG:噴乳経験  / 4
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:胸圍 + TALENT:ARG:習得速度
	SIF FLAG:抱負 == 5
		LOCAL:1 ++
	SIF GETBIT (CFLAG:ARG:继承, 5)
		LOCAL:1 *= 4
	IF LOCAL * LOCAL:1 / 10 > EXPLV:(ABL:ARG:胸 + 3) && EXP:ARG:乳交経験 + EXP:ARG:噴乳経験  / 5 > EXPLV:(ABL:ARG:胸 + 1); && EXP_UP(26,ARG)
		ABL:ARG:胸 ++
		PRINTFORMW %CALLNAME:ARG%在使用胸部的性技上的技術提高了
		;技能レベルUP口上,13-0,胸
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 13, 0)
		IF ABL:ARG:胸 >= 5 && TALENT:ARG:2 != 2 && !TALENT:ARG:淫乳
			PRINTFORMW %CALLNAME:ARG%获得了[淫乳]
			TALENT:ARG:淫乳 = 1
			;技能レベルUP口上,13-1,淫乳
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 13, 1)
		ENDIF
	ENDIF
ENDIF
IF ABL:ARG:腰 < 6
	LOCAL = EXP:ARG:Ｖ性交経験 + EXP:ARG:Ａ性交経験 + EXP:ARG:挿入経験
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:習得速度
	SIF GETBIT (CFLAG:ARG:继承, 3) || GETBIT (CFLAG:ARG:继承, 4)
		LOCAL:1 *= 4
	IF LOCAL * LOCAL:1 / 10 > EXPLV:(ABL:ARG:腰 + 3)
		ABL:ARG:腰 ++
		PRINTFORMW %CALLNAME:ARG%的腰技变得更高明了
		;技能レベルUP口上,14,腰
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 14)
	ENDIF
ENDIF
IF ABL:ARG:膣 < 6
	;広げすぎるとガバガバになっちゃいますよ
	LOCAL = EXP:ARG:Ｖ性交経験  * 2 +  EXP:ARG:Ｖ経験 / 5 + (EXP:ARG:Ｖ拡張経験 <= 10 ? EXP:ARG:Ｖ拡張経験 * 5 # 100 - EXP:ARG:Ｖ拡張経験 * 5)
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:習得速度
	SIF FLAG:抱負 == 3
		LOCAL:1 ++
	SIF GETBIT (CFLAG:ARG:继承, 3)
		LOCAL:1 *= 4
	IF LOCAL * LOCAL:1 / 10 > EXPLV:(ABL:ARG:膣 + 3) && EXP:ARG:Ｖ性交経験 >  EXPLV:(ABL:ARG:膣 + 1); && EXP_UP(20,ARG)
		ABL:ARG:膣 ++
		PRINTFORMW %CALLNAME:ARG%的性交技術提高了
		;技能レベルUP口上,15-0,膣
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 15, 0)
		IF ABL:ARG:膣 >= 5 && TALENT:ARG:2 != 2 && !TALENT:ARG:淫壺
			PRINTFORMW %CALLNAME:ARG%获得了[淫壺]
			TALENT:ARG:淫壺 = 1
			;技能レベルUP口上,15-1,淫壺
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 15, 1)
		ENDIF
	ENDIF
ENDIF
IF ABL:ARG:肛門 < 6
	LOCAL = EXP:ARG:Ａ性交経験  * 2 +  EXP:ARG:Ａ経験 / 5 + (EXP:ARG:Ａ拡張経験 <= 25 ? EXP:ARG:Ａ拡張経験 * 2 # 100 - 2 * EXP:ARG:Ａ拡張経験)
	LOCAL:1 = 10 + ABL:ARG:技巧 / 2 + TALENT:ARG:習得速度
	SIF FLAG:抱負 == 4
		LOCAL:1 ++
	SIF GETBIT (CFLAG:ARG:继承, 4)
		LOCAL:1 *= 4
	IF LOCAL * LOCAL:1 / 10 > EXPLV:(ABL:ARG:肛門 + 3) && EXP:ARG:Ａ性交経験 > EXPLV:(ABL:ARG:肛門 + 1); && EXP_UP(21,ARG)
		ABL:ARG:肛門 ++
		PRINTFORMW %CALLNAME:ARG%用肛門性交的技術提高了
		;技能レベルUP口上,16,肛門
		CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 16, 0)
		IF ABL:ARG:肛門 >= 5 && !TALENT:ARG:尻穴狂
			PRINTFORMW %CALLNAME:ARG%获得了[尻穴狂]
			TALENT:ARG:尻穴狂 = 1
			;技能レベルUP口上,16,尻穴狂
			CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 16, 1)
		ENDIF
	ENDIF
ENDIF

IF !TALENT:ARG:調合知識 && TALENT:ARG:調合Lv >= 3 && ABL:ARG:教養 >= 3
	PRINTFORML %CALLNAME:ARG%得到了【調合知識】
	TALENT:ARG:調合知識 = 1
	;技能レベルUP口上,9-1,調合知識
	CALL KOJO_MESSAGE_SEND("EVENT", 34, ARG, 9, 1)
ENDIF