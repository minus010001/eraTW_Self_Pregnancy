@FIELDMAP
PRINTL ■■■■■■■■■■■■■
PRINT ■■┌
CALL MAPPOINT("狱",67)
PRINT ─
CALL MAPPOINT("彼",64)
PRINT 　
CALL MAPPOINT("月",102)
PRINT ─
CALL MAPBUTTON("槐",10)
PRINTL 　■■
PRINT ■　
CALL MAPPOINT("畜",68)
PRINT 　
CALL MAPPOINT("幽",66)
PRINT ┴
CALL MAPBUTTON("川",6)
PRINT ─┐└
CALL MAPBUTTON("守",8)
PRINTL ┐■
PRINT ■　　　　　│　├
CALL MAPBUTTON("山",7)
PRINT ┤
CALL MAPPOINT("天",89)
PRINTL ■
PRINT ■　　
CALL MAPPOINT("无",57)
PRINT ┐　│　│：
CALL MAPPOINT("猫",74)
PRINTL 　■
PRINT ■　　　└─
CALL MAPBUTTON("森",5)
PRINTL ┌┘：　　■
PRINT ■　　　　　││
CALL MAPPOINT("邸",33)
PRINTL ：　　■
PRINT ■　　　　　├┴
CALL MAPBUTTON("湖",3)
PRINT ÷
CALL MAPPOINT("红",34)
PRINTL 　■
PRINT ■　
CALL MAPPOINT("永",45)
PRINT ┬
CALL MAPBUTTON("竹",4)
PRINTL ┐│　　：　　■
PRINT ■　　│　├
CALL MAPBUTTON("里",2)
PRINT 　　
CALL MAPBUTTON("地",9)
PRINTL 　　■
PRINT ■　
CALL MAPPOINT("太",47)
PRINT ┘　
CALL MAPBUTTON("寺",1)
PRINT └──┴
CALL MAPBUTTON("博",0)
PRINTL 　■
PRINTL ■■　　　　　　　　　■■
PRINTL ■■■■■■■■■■■■■


@MAPBUTTON(ARGS,ARG)
SETCOLOR MAPBUTTON_COLOR(ARG)
PRINTBUTTON ARGS,ARG
RESETCOLOR

@MAPBUTTON_HTML_F(ARGS,ARG)
#FUNCTIONS
RETURNF @"<font color = '%"#" + TOSTR(MAPBUTTON_COLOR(ARG), "X6")%'>" + @"<button value = '{ARG}'>" + HTML_ESCAPE(ARGS) + "</button>" + "</font>" 

@MAPBUTTON_COLOR(ARG)
#FUNCTION
SETCOLOR C_P_BLUE
SELECTCASE ARG
	CASE MAIN_MAP
		SIF !CHK_DATENOW(CFLAG:MASTER:约会中)
			RETURNF 0x00ff00
	CASE 0
		SIF CFLAG:MASTER:约会中 == ARG
			RETURNF 0x00ff00
	CASE 1
		SIF CFLAG:MASTER:约会中 == ARG
			RETURNF 0x00ff00
	CASE 2
		SIF CFLAG:MASTER:约会中 == ARG
			RETURNF 0x00ff00
	CASE 3
		SIF CFLAG:MASTER:現在位置 == 霧之湖 || CFLAG:MASTER:現在位置 == 冰造的小雪屋
			RETURNF 0x00ff00
	CASE 4
		SIF CFLAG:MASTER:現在位置 >= 竹林入口 && CFLAG:MASTER:現在位置 <= 兎的洞穴
			RETURNF 0x00ff00
	CASE 5
		SIF CFLAG:MASTER:現在位置 >= 森林的入口 && CFLAG:MASTER:現在位置 <= 再思之道
			RETURNF 0x00ff00
	CASE 6
		SIF CFLAG:MASTER:現在位置 >= 中有之道 && CFLAG:MASTER:現在位置 <= 三途之川
			RETURNF 0x00ff00
	CASE 7
		;SIF CFLAG:MASTER:约会中 == ARG && CFLAG:MASTER:現在位置 != 迷途之家
		;	SETCOLOR 0 , 255, 0
	CASE 8
		SIF CFLAG:MASTER:约会中 == ARG && CFLAG:MASTER:現在位置 != 天界
			RETURNF 0x00ff00
	CASE 9
		SIF CFLAG:MASTER:约会中 == ARG
			RETURNF 0x00ff00
	CASE 10
		SIF CFLAG:MASTER:現在位置 == 槐安通路
			RETURNF 0x00ff00
ENDSELECT
RETURNF C_P_BLUE


@MAPPOINT(ARGS,ARG)
SELECTCASE ARG
	CASE 33
		SIF CFLAG:MASTER:現在位置 == 廃洋館
			SETCOLOR 0 , 255, 0
		PRINT 邸
	CASE 34
		SIF CFLAG:MASTER:現在位置 >= 正門 && CFLAG:MASTER:現在位置 <= 地下室
			SETCOLOR 0 , 255, 0
		PRINT 红
	CASE 45
		SIF CFLAG:MASTER:現在位置 == 永遠亭
			SETCOLOR 0 , 255, 0
		PRINT 永
	CASE 47
		SIF CFLAG:MASTER:現在位置 == 無名之丘 || CFLAG:MASTER:現在位置 == 太陽花田
			SETCOLOR 0 , 255, 0
		PRINT 太
	CASE 57
		SIF CFLAG:MASTER:現在位置 == 無縁塚
			SETCOLOR 0 , 255, 0
		PRINT 无
	CASE 64
		SIF CFLAG:MASTER:現在位置 == 彼岸
			SETCOLOR 0 , 255, 0
		PRINT 彼
	CASE 66
		SIF CFLAG:MASTER:現在位置 == 白玉楼庭 || CFLAG:MASTER:現在位置 == 白玉楼
			SETCOLOR 0 , 255, 0
		PRINT 幽
	CASE 67
		SIF CFLAG:MASTER:現在位置 == 地獄
			SETCOLOR 0 , 255, 0
		PRINT 狱
	CASE 68
		SIF CFLAG:MASTER:現在位置 == 畜生界 || CFLAG:MASTER:現在位置 == 霊長園
			SETCOLOR 0 , 255, 0
		PRINT 畜
	CASE 74
		;SIF CFLAG:MASTER:現在位置 == 迷途之家
		;	SETCOLOR 0 , 255, 0
		PRINT 猫
	CASE 89
		SIF CFLAG:MASTER:現在位置 == 天界
			SETCOLOR 0 , 255, 0
		PRINT 天
	CASE 102
		SIF CFLAG:MASTER:現在位置 >= 月之都 && CFLAG:MASTER:現在位置 <= 寧靜海
			SETCOLOR 0 , 255, 0
		PRINT 月
ENDSELECT 
;PRINTBUTTON ARGS,(ARG / 10)
RESETCOLOR