@仕事情景描写(ARG)
;ARGは角色　角色別の情景描写にどうぞ。
IF CFLAG:ARG:職種 == 48
	PRINTFORMW %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 一边牵挂着%CALLNAME:MASTER%一边 # 忙碌的\@做宴会的準備。
ELSEIF CFLAG:ARG:職種 == 50
	PRINTFORMW %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 一边牵挂着%CALLNAME:MASTER%一边 # 默默地\@做宴会的善后工作。
ELSEIF CFLAG:ARG:職種 == 46
	PRINTFORMW %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 一边时不时把视线投向%CALLNAME:MASTER%一边 # \@玩耍着。
ELSEIF CFLAG:ARG:職種 == 52
	SELECTCASE ARG
		;こっから女子会　※後々角色毎に個性持たせたいので分岐だけ作っておく
		CASE 4
			PRINTFORML %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 和%CALLNAME:MASTER%相视而笑然后继续 # \@和她们一起谈笑着。
		CASE 26
			PRINTFORML %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 和%CALLNAME:MASTER%相视而笑然后继续 # \@和她们一起谈笑着。
		CASE 32
			PRINTFORML %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 和%CALLNAME:MASTER%相视而笑然后继续 # \@和她们一起谈笑着。
		CASE 55
			PRINTFORML %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 和%CALLNAME:MASTER%相视而笑然后继续 # \@和她们一起谈笑着。
		CASE 66
			PRINTFORML %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 和%CALLNAME:MASTER%相视而笑然后继续 # \@和她们一起谈笑着。
		CASE 72
			PRINTFORML %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 和%CALLNAME:MASTER%相视而笑然后继续 # \@和她们一起谈笑着。
		CASE 102
			PRINTFORML %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 和%CALLNAME:MASTER%相视而笑然后继续 # \@和她们一起谈笑着。
		CASE 124
			PRINTFORML %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 和%CALLNAME:MASTER%相视而笑然后继续 # \@和她们一起谈笑着。
	ENDSELECT
		;女子会ここまで
ELSE
	SELECTCASE ARG
		CASE 30
			IF INRANGE(TIME, CLOCK_TO_TIME(9, 0), CLOCK_TO_TIME(11, 59))
				PRINTFORML %CALLNAME:ARG%安静地进行着%GET_JOBNAME(ARG)%。
			ELSEIF INRANGE(TIME, CLOCK_TO_TIME(12, 0), CLOCK_TO_TIME(12, 59))
				PRINTFORML %CALLNAME:ARG%正在进行%GET_JOBNAME(ARG)%。
			ELSE
				PRINTFORML %CALLNAME:ARG%冷漠地进行着%GET_JOBNAME(ARG)%。
			ENDIF
		CASE 54
			PRINTFORMW %CALLNAME:ARG%头也不抬的看着书…
		CASE 57
			PRINTFORMW %CALLNAME:ARG%在悠哉地做%GET_JOBNAME(ARG)%。
		CASE 67
			IF TIME >= 720 && TIME <= 780
				PRINTFORMW %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 一边牵挂着%CALLNAME:MASTER%一边 # \@陪孩子們%GET_JOBNAME(ARG)%。
			ELSE
				PRINTFORMW %CALLNAME:ARG%\@ TALENT:ARG:恋慕 ? 一边牵挂着%CALLNAME:MASTER%一边 # \@做著%GET_JOBNAME(ARG)%。
			ENDIF
		CASE 70
			IF DAY % 7 == 3 || DAY % 7 == 4
				PRINTFORML %CALLNAME:ARG%一边读书一边%GET_JOBNAME(ARG)%。
			ELSE
				PRINTFORML %CALLNAME:ARG%一邊仔細的確認著內容一邊%GET_JOBNAME(ARG)%。
			ENDIF
		CASEELSE
			PRINTFORML %CALLNAME:ARG%心情不错的%GET_JOBNAME(ARG)%着。
	ENDSELECT
ENDIF
@宴会描写(ARG)
;FLAG:宴会開催FLAGを使って宴会種別で書き分けてもいいかも

IF TCVAR:ARG:烂醉 == 0
	IF CFLAG:ARG:職種 == 49 && TALENT:ARG:恋慕 == 1
		PRINTFORMW %CALLNAME:ARG%一边享受着宴会一边时不时看向%CALLNAME:MASTER%。
	ELSE
		PRINTFORML %CALLNAME:ARG%享受着宴会
	ENDIF
ELSE
	IF TALENT:ARG:恋慕 == 1
		PRINTFORMW %CALLNAME:ARG%雖然喝得酩酊大醉但也不時不忘記往%CALLNAME:MASTER%所在的方向望去。
	ELSE
		PRINTFORML %CALLNAME:ARG%喝了個爛醉整個人趴了下去。
	ENDIF
ENDIF
IF CFLAG:ARG:職種 == 51 && (BASE:ARG:酒気 > (MAXBASE:ARG:酒気 / 10) * 8)
	PRINTW 　好像还醉醺醺的様子…
ELSEIF CFLAG:ARG:職種 == 51 && (BASE:ARG:酒気 > (MAXBASE:ARG:酒気 / 10) * 6)
	PRINTW 　好像离酒醒还需要一段时间…
ELSEIF CFLAG:ARG:職種 == 51
	PRINTW 　还差一点就能从醉意中清醒过来了…
ELSEIF CFLAG:ARG:職種 == 49 && (BASE:ARG:酒気 > (MAXBASE:ARG:酒気 / 10) * 6) 
	IF TALENT:ARG:感情缺乏 && RAND:4
		ELSEIF !RAND:10
			IF !RAND:2
				PRINTFORMW 　%CALLNAME:ARG%眼神呆滞…
				PRINTFORMW 　好像喝醉了的様子
					TALENT:ARG:15 = -1
			ELSE
				PRINTFORMW 　%CALLNAME:ARG%看上去非常開心
				PRINTFORMW 　状态良好的来回敬酒的様子
					TALENT:ARG:15 = 1
			ENDIF
		ELSE
			IF TALENT:ARG:15 == -1
				PRINTFORMW 　%CALLNAME:ARG%说着醉话…
			ELSEIF TALENT:ARG:15 == 1
				PRINTFORMW 　%CALLNAME:ARG%意义不明的笑着…
			ELSE
				PRINTW 　差不多要醉倒了的様子…
			ENDIF
	 	ENDIF
ELSEIF CFLAG:ARG:職種 == 49 && (BASE:ARG:酒気 > (MAXBASE:ARG:酒気 / 10) * 4)
	PRINTW 　酒過三巡、氣氛開始變好了…
ELSEIF TALENT:ARG:酒耐性 == -2 || CFLAG:ARG:妊娠自覚状態 == 1
	IF BASE:ARG:工作量 >= (MAXBASE:ARG:工作量 / 3) * 2
		PRINTW 　並沒有在喝酒而在跟周圍的人談笑風生…
	ELSEIF BASE:ARG:工作量 >= (MAXBASE:ARG:工作量 / 3)
		IF CFLAG:ARG:妊娠自覚状態 == 1
			PRINTW 　坚决不喝酒并享受着…
		ELSEIF TALENT:ARG:酒耐性 == -2
			PRINTW 　还是老様子滴酒不沾…
		ENDIF
	ELSEIF TALENT:ARG:酒耐性 == -2 || CFLAG:ARG:妊娠自覚状態 == 1
		PRINTW 　堅決不碰一滴酒精的模樣…
	ENDIF
ELSEIF CFLAG:ARG:職種 == 49 
	PRINTW 　还很能喝呢…
ENDIF