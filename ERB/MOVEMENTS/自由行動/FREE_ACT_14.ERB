@EXIST_FreeAct14
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@FFreeAct14(ARG, O_DATA, V_NAME)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
#DIM 経過時間
経過時間 = Activity_Duration:TARGET
SELECTCASE O_DATA
;陪同行動指令の名称
CASE "名前"
	RETURNF "帮忙买东西"
CASE "CHAIN"
;他角色に連鎖しない場合コメントアウトすること
	RETURNF @"{1}"
CASE "経過時間"
;陪同行動指令での経過時間
;0だと30分か残り時間全部の小さい方
	RETURNF @"{0}"
ENDSELECT

@自由行動内容_14(CHARA, ARGS, 時間係数, CHAIN)
#DIM CHARA
#DIM CHAIN
#DIM 現在地
#DIM メッセージ表示
#DIM 時間係数
#DIM 経過時間
現在地 = CFLAG:CHARA:現在位置
メッセージ表示 = 現在地 == CFLAG:MASTER:現在位置 ? 1 # 0
SELECTCASE ARGS
	CASE "発生"
		;お店にいる時
		SIF !KAIDASHI_SHOP(現在地) && !FLOWER_SHOP(現在地) && !SAKE_SHOP(現在地) && !YASAI_SHOP(現在地)
			RETURN 0
		SIF BASE:CHARA:気力 <= 500
			RETURN 0
		SIF !INRANGE(TIME, 540, 1140)
			RETURN 0
		SIF メッセージ表示
			PRINTFORMW %CALLNAME:CHARA%\@ CHAIN ? 决定跟着%CALLNAME:CHAIN%一起购物了。# 拿出了购物袋来了。\@
		RETURN 60 + RAND:30
	CASE "行動中"
		IF メッセージ表示
			IF 充填率(CHARA,1) >= 10 || 充填率(CHARA,2) >= 10
				PRINTFORMW %CALLNAME:CHARA%满脸通红的%TEXTR("寻找着便宜的商品/拿起物品开始估值/在烦恼着该买哪个/在随意地逛街购物中")%。
			ELSE
				PRINTFORMW %CALLNAME:CHARA%%TEXTR("寻找着便宜的商品/拿起物品开始估值/在烦恼着该买哪个/在随意地逛街购物中")%。
			ENDIF	
		ENDIF
	CASE "完了"
		IF 充填率(CHARA,1) >= 10 || 充填率(CHARA,2) >= 10
			SIF メッセージ表示
				PRINTFORMW %CALLNAME:CHARA%吐着甜美的喘息买完东西了。
		ELSE
			SIF メッセージ表示
				PRINTFORMW %CALLNAME:CHARA%似乎买完东西了。
		ENDIF	
	CASE "中断"
	CASE "付き合う"
		;同行时自由行动的触发文本
		PRINTFORML %CALLNAME:PLAYER%陪%CALLNAME:CHARA%一起逛街購物了
		IF 充填率(CHARA,1) >= 150 || 充填率(CHARA,2) >= 150
				PRINTFORML %CALLNAME:CHARA%捂着小腹忍耐着体内\@ TALENT:MASTER:濃厚精液? 浓厚的 # 的\@精子的爱抚…香汗淋漓…
				PRINTFORML 脸涨得通红并且夹杂着甜美喘息看着商品、
				PRINTFORML 停留过的地方留下了迷之水痕…
			ELSEIF 充填率(CHARA,1) >= 100 || 充填率(CHARA,2) >= 100
				PRINTFORML %CALLNAME:CHARA%正享受着体内满满\@ TALENT:MASTER:濃厚精液? 浓厚的 # 的\@精子的刺激轻轻娇喘……
				PRINTFORML %CALLNAME:PLAYER%观察着%CALLNAME:CHARA%的状态、夹杂着甜美的娇喘的购物格外有趣…
			ELSEIF 充填率(CHARA,1) >= 70 || 充填率(CHARA,2) >= 70
				PRINTFORML %CALLNAME:CHARA%正感受着体内大量\@ TALENT:MASTER:濃厚精液? 浓厚的 # 的\@精子的刺激……
				PRINTFORML %CALLNAME:CHARA%断断续续的选购、说话时不时夹杂着甜美的娇喘…
			ELSEIF 充填率(CHARA,1) >= 40 || 充填率(CHARA,2) >= 40
				PRINTFORML %CALLNAME:CHARA%正被体内许多\@ TALENT:MASTER:濃厚精液? 浓厚的 # 的\@精子刺激着……
				PRINTFORML %TEXTR(@"满脸通红的%CALLNAME:CHARA%询问要买的商品的位置…/满脸通红的%CALLNAME:CHARA%告知要寻找类似的商品…/提满脸通红的%CALLNAME:CHARA%的购物袋到处移动中…")%
			ELSEIF 充填率(CHARA,1) >= 10 || 充填率(CHARA,2) >= 10
				PRINTFORML %CALLNAME:CHARA%正被体内残存\@ TALENT:MASTER:濃厚精液? 浓厚的 # 的\@精子刺激着……
				PRINTFORML %TEXTR(@"红着脸的%CALLNAME:CHARA%询问要买的商品的位置…/被红着脸的%CALLNAME:CHARA%告知要寻找类似的商品…/提着红着脸的%CALLNAME:CHARA%的购物袋到处移动中…")%
			ELSE
				PRINTFORML %TEXTR(@"%CALLNAME:CHARA%询问要买的商品的位置…/被%CALLNAME:CHARA%告知要寻找类似的商品…/提着%CALLNAME:CHARA%的购物袋到处移动中…")%
			ENDIF		
	CASE "ソース"
		;交手指令選択時の入手ソース、時間係数は付き合った時間の1/10
		DOWNBASE:MASTER:体力 += 20 * 時間係数
		DOWNBASE:MASTER:気力 += 20 * 時間係数
		DOWNBASE:CHARA:体力 += 10 * 時間係数
		DOWNBASE:CHARA:気力 += 10 * 時間係数
		SOURCE:CHARA:歓楽 = (200 + ((ABL:CHARA:従順 + 好感度係数(CHARA))  * 50)) * 時間係数
		TFLAG:信頼度管理 = 1
ENDSELECT