@QOL_COM400
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC INPUT_LOOP_LINE
#DIM DYNAMIC ANIMATION_SEED
#DIMS DYNAMIC UI_MESSAGE
#DIM DYNAMIC HOVER_PAUSE
#DIM DYNAMIC LOCAL_MAP

;MAIN_MAP/MAXROOM/ENTRANCE不再全局替换，改为局部变量传递给子函数
IF GMF_IS_ODEKAKE()
	CALL GMF_604_TO_400
ENDIF
LOCAL_MAP = GET_CURRENT_MAP()

TOOLTIP_SETDELAY 100
ANIMATION_SEED = 0
HOVER_PAUSE = 0

LINESTART = LINECOUNT
$MAP_LOOP
CLEARLINE LINECOUNT - LINESTART
CALL COM400_DRAW_UI(ANIMATION_SEED, UI_MESSAGE, LOCAL_MAP)

UI_MESSAGE = 
INPUT_LOOP_LINE = LINECOUNT

$INPUT_LOOP
CLEARLINE LINECOUNT - INPUT_LOOP_LINE

; NF后缀 + 悬停暂停：
; 动画模式 → TINPUTSNF 帧间隔超时，超时后检查 MOUSEB()
;   鼠标在按钮上 → 切换到悬停模式（HOVER_PAUSE=1）
;   鼠标不在 → 推进动画
; 悬停模式 → TINPUTSNF 长超时（1500ms），给用户点击按钮的窗口
;   超时 → 推进一帧慢速动画（不卡死），重新检测悬停
;   点击 → 正常处理按钮输入
IF ANIMATERECOLOREDMAPS > 0 && !FLAG:70
	IF HOVER_PAUSE
		TINPUTSNF 2000, "UPDATE", 0, ""
	ELSE
		TINPUTSNF ANIMATERECOLOREDMAPS, "UPDATE", 0, ""
	ENDIF
ELSE
	INPUTS
ENDIF

; 悬停状态检测（仅超时时更新标志，不跳过输入处理）
IF RESULTS == "UPDATE"
	IF MOUSEB() != ""
		; 鼠标在按钮上 → 暂停动画
		IF !HOVER_PAUSE
			HOVER_PAUSE = 1
		ENDIF
	ELSE
		; 鼠标离开按钮 → 恢复动画
		IF HOVER_PAUSE
			HOVER_PAUSE = 0
		ENDIF
	ENDIF
ENDIF

LOCAL:0 = (RESULTS == "UPDATE") ? 0 # 1
LOCALS:0 = %RESULTS%

CALL COM400_PROCESS_INPUT(LOCAL:0, LOCALS:0, UI_MESSAGE, LOCAL_MAP)

SELECTCASE RESULT
	CASE -1
		TOOLTIP_SETDELAY 500
		RETURN -1
	CASE 0
		TOOLTIP_SETDELAY 500
		RETURN 1
	CASE 1
		GOTO INPUT_LOOP
	CASE 2
		GOTO MAP_LOOP
	CASE 3
		ANIMATION_SEED ++
		SIF ANIMATION_SEED >= 20
			ANIMATION_SEED = 0
		GOTO MAP_LOOP
ENDSELECT

;================================================
; COM400 UI 渲染模块
;================================================
@COM400_DRAW_UI(ANIMATION_SEED, UI_MESSAGE, LOCAL_MAP)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC ANIMATION_SEED
#DIMS REF UI_MESSAGE
#DIM DYNAMIC LOCAL_MAP
#DIM DYNAMIC BUTTON返回
#DIM DYNAMIC BUTTON切替左
#DIM DYNAMIC BUTTON切替中
#DIM DYNAMIC BUTTON切替右

BUTTON返回 = MAXROOM(LOCAL_MAP) + 1
BUTTON切替左 = MAXROOM(LOCAL_MAP) + 2
BUTTON切替中 = MAXROOM(LOCAL_MAP) + 4
BUTTON切替右 = MAXROOM(LOCAL_MAP) + 4

; 人数カウントをリセット
VARSET COUNTCHARA
FOR LOCAL, 1, CHARANUM
	COUNTCHARA:(CFLAG:LOCAL:現在位置) ++
NEXT
CALL TURN_RESET

DRAWLINE
IF RECOLOREDMAPS
	CALL DRAW_COLOREDMAP(ANIMATION_SEED)
ELSE
	CALL DRAW_MAP
ENDIF

IF FLAG:移動先表示 < 3
	PRINTL
	TRYCALLFORM MAP_{LOCAL_MAP}_{TFLAG:マップ切り替え}
ENDIF
PRINTL
PRINTFORML 当前位置→[{(CFLAG:MASTER:現在位置)}]%NAME_FROM_PLACE(CFLAG:MASTER:現在位置)%

IF FLAG:察知模式 == 1
	PRINT 颜色的说明：
	CALL COLORMESSAGE("逗留中　", C_H_PINK, 0)
	CALL COLORMESSAGE("工作中　", C_D_ORANGE, 0)
	CALL COLORMESSAGE("睡眠中　", C_N_BLUE, 0)
	CALL COLORMESSAGE("路人　", C_D_PINK, 0)
	CALL COLORMESSAGE("自由行动", C_YELLOW, 0)
	PRINTL
ELSEIF FLAG:察知模式 == 2
	IF OUTROOF(CFLAG:MASTER:現在位置) && DAY:2 == 4
		PRINT 积雪量：
		CALL COLORMESSAGE("干净　", C_GREEN, 0)
		CALL COLORMESSAGE("小　", C_M_GREEN , 0)
		CALL COLORMESSAGE("较小　", C_P_GREEN, 0)
		CALL COLORMESSAGE("中　", 0xFFFFFF, 0)
		CALL COLORMESSAGE("较大　", 0xFFFFFF, 0)
		CALL COLORMESSAGE("大　", 0xFFFFFF , 0)
		CALL COLORMESSAGE("雪山", 0xFFFFFF, 0)
		PRINTL
	ELSE
		PRINT 清洁度：
		CALL COLORMESSAGE("最高　", C_GREEN, 0)
		CALL COLORMESSAGE("高　", C_M_GREEN , 0)
		CALL COLORMESSAGE("较高　", C_P_GREEN, 0)
		CALL COLORMESSAGE("中　", C_YELLOW, 0)
		CALL COLORMESSAGE("较低　", C_V_PINK, 0)
		CALL COLORMESSAGE("低　", C_RED , 0)
		CALL COLORMESSAGE("垃圾山", C_RED, 0)
		PRINTL
	ENDIF
ENDIF

SIF CFLAG:MASTER:現在位置 != CFLAG:MASTER:初期位置 && (!GMF_GETBIT("幻想乡大地图") || GET_MAPID(CFLAG:MASTER:現在位置) == GMF_HOME_MAP)
	PRINTL [  0] - 返回私室

FOR LOCAL, MINROOM(LOCAL_MAP), MAXROOM(LOCAL_MAP)
	SIF !(CAN_MOVE(CFLAG:MASTER:現在位置, LOCAL) & 1) || NAME_FROM_PLACE(LOCAL) == ""
		CONTINUE
	SELECTCASE FLAG:移動先表示
		CASE 1, 3
			PRINTFORML [{LOCAL, 3}] - %NAME_FROM_PLACE(LOCAL)%
		CASE 2, 4
			PRINTFORM [{LOCAL, 3}] - %NAME_FROM_PLACE(LOCAL),5,LEFT%
	ENDSELECT
NEXT

SIF FLAG:移動先表示 == 2 || FLAG:移動先表示 == 4
	PRINTL

IF !CAN_MOVE(CFLAG:MASTER:現在位置, GET_ENTRANCE(LOCAL_MAP)) & 1 && CFLAG:MASTER:現在位置 != GET_ENTRANCE(LOCAL_MAP)
	SIF FLAG:移動先表示 == 1
		PRINTL
	PRINTFORML [{GET_ENTRANCE(LOCAL_MAP), 3}] - %NAME_FROM_PLACE(GET_ENTRANCE(LOCAL_MAP))%
ELSEIF FLAG:移動先表示 == 1
	PRINTL
ENDIF

PRINTFORM [{BUTTON返回, 3}] - 返回　　　
PRINTBUTTON "＜]", TOSTR(BUTTON切替左)
PRINTBUTTON "地图切换", TOSTR(BUTTON切替中)
PRINTFORM ({TFLAG:地圖切換})
PRINTBUTTON "[＞", TOSTR(BUTTON切替右)

SIF TALENT:MASTER:居場所察知 || TALENT:MASTER:汚部屋察知
	PRINTBUTTON "　　[Y] - 察觉模式", "Y"
IF FLAG:察知模式 == 1
	PRINT (居)　
ELSEIF FLAG:察知模式 == 2
	PRINT (污)　
ELSE
	PRINT 　　
ENDIF

SIF TALENT:MASTER:居場所察知 && FLAG:察知模式 == 1
	PRINTBUTTON "[Z] - 所在地察觉　 ", "Z"
SIF TALENT:MASTER:汚部屋察知 && FLAG:察知模式 == 2
	PRINTBUTTON "[X] - 脏房间察觉　 ", "X"
PRINTBUTTON "[C] - 切换目的地显示", "C"

IF TALENT:MASTER:体型 < -4
	PRINTL
	IF CFLAG:MASTER:同行
		PRINT （搭顺风车真开心）
	ELSE
		PRINT （走过去都很辛苦…）
	ENDIF
ENDIF

IF UI_MESSAGE != ""
	PRINTL
	CALL COLORMESSAGE(@" ※ %UI_MESSAGE% ※", C_RED, 1)
ELSE
	PRINTL
ENDIF

;================================================
; COM400 逻辑处理模块 (返回代码告知主控进行何种跳转)
; -1=退出当前指令, 0=完成移动, 1=忽略等待, 2=需重绘(清除屏幕), 3=Tick更新
;================================================
@COM400_PROCESS_INPUT(EVENT_TYPE, EVENT_DATA, UI_MESSAGE, LOCAL_MAP)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC EVENT_TYPE
#DIMS DYNAMIC EVENT_DATA
#DIMS REF UI_MESSAGE
#DIM DYNAMIC LOCAL_MAP
#DIM DYNAMIC BUTTON返回
#DIM DYNAMIC BUTTON切替左
#DIM DYNAMIC BUTTON切替中
#DIM DYNAMIC BUTTON切替右
#DIM DYNAMIC NUM_RES
#DIM DYNAMIC OHEYA_LOOP_LINE
#DIM DYNAMIC PAGE_NUM

BUTTON返回 = MAXROOM(LOCAL_MAP) + 1
BUTTON切替左 = MAXROOM(LOCAL_MAP) + 2
BUTTON切替中 = MAXROOM(LOCAL_MAP) + 4
BUTTON切替右 = MAXROOM(LOCAL_MAP) + 4
;DEBUGPRINTFORML PROCESS_INPUT:{EVENT_TYPE}-%EVENT_DATA%

; --- 1. 动画 Tick 拦截 ---
IF EVENT_TYPE == 0 && EVENT_DATA == "UPDATE"
	RETURN 3
ENDIF

; --- 2. 退出指令拦截 (ESC / 鼠标右键) ---
IF EVENT_TYPE == 3 || EVENT_DATA == "MOUSE_R"
	RETURN -1
ENDIF

; --- 3. 忽略无效空字符串 (如点击非按钮区域) ---
IF EVENT_DATA == ""
	RETURN 1
ENDIF

; --- 4. 热键处理 ---
SELECTCASE EVENT_DATA
	CASE "Z", "z", "X", "x"
		IF TALENT:MASTER:居場所察知 && (EVENT_DATA == "Z" || EVENT_DATA == "z")
			CALL QOL_HITOSAGASI(, 1)
		ELSEIF TALENT:MASTER:汚部屋察知 && (EVENT_DATA == "X" || EVENT_DATA == "x")
			CALL OHEYA_CHECK(LOCAL_MAP)
			OHEYA_LOOP_LINE = LINECOUNT
			DO
				CLEARLINE LINECOUNT - OHEYA_LOOP_LINE
				INPUT
				IF GROUPMATCH(RESULT, 0, CFLAG:MASTER:現在位置) || RESULT < 0
					RETURN 2
				ENDIF
				IF RESULT >= MINROOM(LOCAL_MAP) && RESULT <= MAXROOM(LOCAL_MAP)
					NUM_RES = RESULT
					GOTO PROCESS_MOVE_LOGIC
				ENDIF
			LOOP 1
		ELSE
			RETURN 2
		ENDIF
		IF RESULT > 0
			NUM_RES = RESULT
			GOTO PROCESS_MOVE_LOGIC
		ENDIF
		RETURN 2
	CASE "Y", "y"
		IF TALENT:MASTER:居場所察知 && TALENT:MASTER:汚部屋察知
			FLAG:察知模式 = (FLAG:察知模式 >= 2) ? 0 # FLAG:察知模式 + 1
		ELSEIF TALENT:MASTER:居場所察知
			FLAG:察知模式 = !FLAG:察知模式
		ELSEIF TALENT:MASTER:汚部屋察知
			FLAG:察知模式 = (!FLAG:察知模式) * 2
		ENDIF
		RETURN 2
	CASE "C", "c"
		FLAG:移動先表示 = (FLAG:移動先表示 >= 5) ? 0 # FLAG:移動先表示 + 1
		RETURN 2
ENDSELECT

; --- 5. 数字按键(地点跳转)处理 ---
IF ISNUMERIC(EVENT_DATA)
	NUM_RES = TOINT(EVENT_DATA)
ELSE
	RETURN 1
ENDIF
PAGE_NUM = GMF_GETBIT("幻想乡大地图") ? GMF_MAP_PAGES:LOCAL_MAP # MAP_PAGES
SELECTCASE NUM_RES
	CASE BUTTON返回, CFLAG:MASTER:現在位置
		RETURN -1
	CASE BUTTON切替左
		TFLAG:地圖切換 -= 1
		SIF TFLAG:地圖切換 <= 0
			TFLAG:地圖切換 = PAGE_NUM
		RETURN 2
	CASE BUTTON切替右
		TFLAG:地圖切換 += 1
		SIF TFLAG:地圖切換 > PAGE_NUM
			TFLAG:地圖切換 = 1
		RETURN 2
	CASE 0
		IF GMF_GETBIT("幻想乡大地图") && GET_MAPID(CFLAG:MASTER:現在位置) != GMF_HOME_MAP
			RETURN 2
		ENDIF
		IF IN_HOME(CFLAG:MASTER:初期位置) != 1
			UI_MESSAGE = 初始位置不正确，重设为私室位置
			CFLAG:MASTER:初期位置 = 默认初期位置
			RETURN 2 ; 要求重绘把红字印出来
		ENDIF
		SIF CFLAG:MASTER:現在位置 != CFLAG:MASTER:初期位置
			CALL SKIP_MOVE(CFLAG:MASTER:現在位置, CFLAG:MASTER:初期位置)
		RETURN 0
ENDSELECT

; 常规地点处理
SIF INRANGE(NUM_RES, 1, 99)
	NUM_RES += LOCAL_MAP * 100

$PROCESS_MOVE_LOGIC
; 移动判定
IF NUM_RES == CFLAG:MASTER:現在位置
	RETURN -1
ELSEIF IN_HOME(NUM_RES) != 1
	TFLAG:マップ切り替え = 0
	UI_MESSAGE = 移动范围外
	RETURN 2 ; 越界，重绘报错
ELSEIF MAP_CAN_MOVE(NUM_RES) == 0
	IF TFLAG:移動不能メッセージ == 999 && LOCKPICK_SYSTEM > 0
		; 开锁系统接管
		CALL LOCKPICK_INFO(NUM_RES)
		RETURN 0
	ELSE
		UI_MESSAGE '= QOL_CANNOT_MOVE_MESSAGE(NUM_RES)
		RETURN 2 ; 走不通，要求重绘出红字
	ENDIF
ELSE
	CALL SKIP_MOVE(CFLAG:MASTER:現在位置, NUM_RES)
	RETURN 0
ENDIF

@QOL_COM604
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC INPUT_LOOP_LINE
#DIM DYNAMIC ANIMATION_SEED
#DIMS DYNAMIC UI_MESSAGE
#DIM DYNAMIC HOVER_PAUSE
#DIM DYNAMIC LOCAL_MAP

IF !GMF_IS_ODEKAKE(-1)
	CALL GMF_400_TO_604
ENDIF
LOCAL_MAP = GET_CURRENT_MAP()

FLAG:自室施錠 = 0 ;custom code, unlocks door
ANIMATION_SEED = 0
HOVER_PAUSE = 0

LINESTART = LINECOUNT
$MAP_LOOP
CLEARLINE LINECOUNT - LINESTART
CALL COM604_DRAW_UI(ANIMATION_SEED, UI_MESSAGE, LOCAL_MAP)

UI_MESSAGE = 
INPUT_LOOP_LINE = LINECOUNT

$INPUT_LOOP
CLEARLINE LINECOUNT - INPUT_LOOP_LINE

; NF后缀 + 悬停暂停
IF ANIMATERECOLOREDMAPS > 0 && !FLAG:70
	IF HOVER_PAUSE
		TINPUTSNF 2000, "UPDATE", 0, ""
	ELSE
		TINPUTSNF ANIMATERECOLOREDMAPS, "UPDATE", 0, ""
	ENDIF
ELSE
	INPUTS
ENDIF

; 悬停状态检测（仅超时时更新标志，不跳过输入处理）
IF RESULTS == "UPDATE"
	IF MOUSEB() != ""
		IF !HOVER_PAUSE
			HOVER_PAUSE = 1
		ENDIF
	ELSE
		IF HOVER_PAUSE
			HOVER_PAUSE = 0
		ENDIF
	ENDIF
ENDIF

LOCAL:0 = (RESULTS == "UPDATE") ? 0 # 1
LOCALS:0 = %RESULTS%

CALL COM604_PROCESS_INPUT(LOCAL:0, LOCALS:0, UI_MESSAGE, LOCAL_MAP)
SELECTCASE RESULT
	CASE -1
		RETURN -1
	CASE 0
		RETURN 1
	CASE 1
		GOTO INPUT_LOOP
	CASE 2
		GOTO MAP_LOOP
	CASE 3
		ANIMATION_SEED = (ANIMATION_SEED + 1) % 20
		GOTO MAP_LOOP
ENDSELECT

@COM604_DRAW_UI(ANIMATION_SEED, UI_MESSAGE, LOCAL_MAP)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC ANIMATION_SEED
#DIMS REF UI_MESSAGE
#DIM DYNAMIC LOCAL_MAP
#DIM DYNAMIC LPCOUNT
#DIM DYNAMIC 現在地点
#DIM DYNAMIC P_ID
#DIM DYNAMIC 所要時間
#DIM DYNAMIC 時間補正
#DIM DYNAMIC 時間停止ジャンプ
#DIM DYNAMIC 特殊ワープ

時間停止ジャンプ = FLAG:時間停止 || (GETBIT(FLAG:瞬間移動, AT_HOME(MASTER)) && !FLAG:デート相手)

DRAWLINEFORM ～
IF RECOLOREDMAPS
	CALL DRAW_COLOREDMAP(ANIMATION_SEED, "ODEKAKE")
ELSE
	CALL DRAW_MAP("ODEKAKE")
ENDIF
DRAWLINEFORM ～

PRINTL 要去哪里呢？
現在地点 = CFLAG:MASTER:現在位置 % 100 / 10
LOCAL = 0
FOR LPCOUNT, 1, 10
	P_ID = GET_MAPID(CFLAG:MASTER:現在位置) * 100 + LPCOUNT * 10
	SIF NAME_FROM_PLACE(P_ID) == ""
		CONTINUE
	IF LPCOUNT == 現在地点
		PRINTPLAINFORM [  0] - %NAME_FROM_PLACE(P_ID), 18, LEFT%
		CALL COLORMESSAGE(@"现在地　", C_GREEN, 3)
	ELSE
		時間補正 = 0
		IF 時間停止ジャンプ
			LOCALS = TSP%" "%
		ELSE
			SIF IS_TSUKIAU([[小町]])
				時間補正 += -50
			SIF IS_TSUKIAU([[若鷺姫]]) && !GETBIT (FLAG:泳池, 0)
				時間補正 += 100
			SIF SHORTCUT(GET_MAPID(CFLAG:MASTER:現在位置), -1)
				時間補正 += -50
			LOCALS = 分　
		ENDIF
		所要時間 = 散步所要時間(CFLAG:MASTER:現在位置, P_ID, 時間補正)
		PRINTFORM [{LPCOUNT, 3}] - %NAME_FROM_PLACE(P_ID), 18, LEFT%
		IF 時間補正 < 0
			SETCOLOR C_L_GREEN
		ELSEIF 時間補正 > 0
			SETCOLOR C_ROYAL_BLUE
		ENDIF
		PRINTFORM {所要時間, 3} %LOCALS%
		RESETCOLOR
	ENDIF
	LOCAL ++
	SIF LOCAL % 2 == 0
		PRINTL
NEXT
SIF LOCAL % 2 == 1
	PRINTL

特殊ワープ = ODEKAKEMAP_SETTING(CFLAG:MASTER:現在位置, "特殊ワープ")
IF 特殊ワープ
	所要時間 = 時間停止ジャンプ ? 30 # 10
	PRINTFORML [{特殊ワープ, 3}] - %NAME_FROM_PLACE(特殊ワープ), 18, LEFT%{所要時間, 3} %LOCALS%
ENDIF

PRINTL [100] - 返回　　　　　　　　　　　[ 99] - 移动至其他地域
IF UI_MESSAGE != ""
	PRINTL
	CALL COLORMESSAGE(@" ※ %UI_MESSAGE% ※", C_RED, 1)
ENDIF

@COM604_PROCESS_INPUT(EVENT_TYPE, EVENT_DATA, UI_MESSAGE, LOCAL_MAP)
#DIM DYNAMIC EVENT_TYPE
#DIMS DYNAMIC EVENT_DATA
#DIMS REF UI_MESSAGE
#DIM DYNAMIC LOCAL_MAP
#DIM DYNAMIC NUM_RES
#DIM DYNAMIC P_ID
#DIM DYNAMIC 所要時間
#DIM DYNAMIC 時間補正
#DIM DYNAMIC 時間停止ジャンプ
#DIM DYNAMIC 特殊ワープ

IF EVENT_TYPE == 0 && EVENT_DATA == "UPDATE"
	RETURN 3
ENDIF
IF EVENT_TYPE == 3 || EVENT_DATA == "MOUSE_R"
	RETURN -1
ENDIF
IF EVENT_DATA == "" || !ISNUMERIC(EVENT_DATA)
	RETURN 1
ENDIF

NUM_RES = TOINT(EVENT_DATA)
時間停止ジャンプ = FLAG:時間停止 || (GETBIT(FLAG:瞬間移動, AT_HOME(MASTER)) && !FLAG:デート相手)
特殊ワープ = ODEKAKEMAP_SETTING(CFLAG:MASTER:現在位置, "特殊ワープ")

IF NUM_RES == 100 || NUM_RES == 0
	RETURN -1
ELSEIF NUM_RES == 99
	DRAWLINE
	CALL OTHERREGIONS("散步")
	IF RESULT == 100
		RETURN 2 ; 重绘
	ELSE
		RETURN 0 ; 移动成功
	ENDIF
ELSEIF FLAG:70 && FLAG:约会的对象
	UI_MESSAGE = 不能丢下约会的对象
	RETURN 2
ELSEIF 特殊ワープ && NUM_RES == 特殊ワープ
	IF !FLAG:時間停止 && GETBIT(FLAG:瞬間移動, AT_HOME(MASTER)) && 30 > BASE:MASTER:TSP
		時間停止ジャンプ = 0
	ENDIF
	IF FLAG:時間停止 && BASE:MASTER:TSP <= 30
		UI_MESSAGE = TSP不足
		RETURN 2
	ENDIF
	IF 時間停止ジャンプ
		BASE:MASTER:TSP -= 30
	ELSE
		TIME += 10
	ENDIF
	PRINTFORML %CALLNAME:MASTER%踏入了空间的扭曲后
	PRINTFORMW 不知不觉间便到达了%NAME_FROM_PLACE(特殊ワープ)%…
	P_ID = 特殊ワープ
	CFLAG:MASTER:現在位置 = P_ID
	CFLAG:MASTER:约会中 = GET_MAPID(P_ID)
	IF FLAG:约会的对象
		CFLAG:(FLAG:约会的对象):現在位置 = P_ID
		CFLAG:(FLAG:约会的对象):约会中 = GET_MAPID(P_ID)
		IF MOREDATE
			FOR LOCAL, 0, NumberOfDates
				CFLAG:(Datees:LOCAL):現在位置 = P_ID
				CFLAG:(Datees:LOCAL):デート中 = GET_MAPID(P_ID)
			NEXT
		ENDIF
	ENDIF
	RETURN 0
ELSEIF INRANGE(NUM_RES, 1, 9)
	P_ID = GET_MAPID(CFLAG:MASTER:現在位置) * 100 + NUM_RES * 10
	SIF NAME_FROM_PLACE(P_ID) == ""
		RETURN 1
	CALL SANSAKU_UNIQUEPLACE(P_ID)
	IF RESULT
		P_ID = RESULT
		; 重新计算时间
		時間補正 = 0
		SIF IS_TSUKIAU([[小町]])
			時間補正 += -50
		SIF IS_TSUKIAU([[若鷺姫]]) && !GETBIT (FLAG:泳池, 0)
			時間補正 += 100
		SIF SHORTCUT(GET_MAPID(CFLAG:MASTER:現在位置), -1)
			時間補正 += -50
		所要時間 = 散步所要時間(CFLAG:MASTER:現在位置, P_ID, 時間補正)

		IF !FLAG:時間停止 && GETBIT(FLAG:瞬間移動, AT_HOME(MASTER)) && 所要時間 > BASE:MASTER:TSP
			時間停止ジャンプ = 0
			所要時間 /= 3
		ENDIF
		IF FLAG:约会的对象
			CFLAG:(FLAG:约会的对象):現在位置 = P_ID
			TIME += 所要時間
		ELSEIF FLAG:時間停止 && BASE:MASTER:TSP <= 所要時間
			UI_MESSAGE = TSP不足
			RETURN 2
		ELSEIF 時間停止ジャンプ
			BASE:MASTER:TSP -= 所要時間
		ELSE
			TIME += 所要時間
		ENDIF
		CFLAG:MASTER:現在位置 = P_ID
		RETURN 0
	ELSE
		; 移动失败或被取消
		UI_MESSAGE = 没能去到那里
		RETURN 2
	ENDIF
ENDIF
RETURN 1

@QOL_SET_MAINHOME
#LOCALSIZE 4
#LOCALSSIZE 1
#DIM DYNAMIC INPUT_LOOP_LINE
#DIM DYNAMIC ANIMATION_SEED
#DIMS DYNAMIC UI_MESSAGE
#DIM DYNAMIC HOVER_PAUSE

#DIM DYNAMIC 設定前MAIN_MAP
#DIM DYNAMIC 設定前酒虫
#DIM DYNAMIC 設定前初期位置
#DIM DYNAMIC 設定前大家

ANIMATION_SEED = 0
HOVER_PAUSE = 0

[IF DEBUG]
	IF ANIMATERECOLOREDMAPS
		ANIMATION_SEED = (GETMILLISECOND() / ANIMATERECOLOREDMAPS) % 20
	ENDIF
[ENDIF]

IF !引っ越し
	IF ゲーム開始処理中 || ゲーム继承処理中
		MAIN_MAP = 0
		TFLAG:地圖切換 = 0
		TRYCALLFORM ROOMSETTING_{MAIN_MAP}
	ENDIF
	設定前MAIN_MAP = MAIN_MAP
	設定前酒虫 = 拠点_酒虫
	設定前初期位置 = 默认初期位置
	設定前大家 = 拠点_家主
	引っ越し = 1
	SIF GMF_GETBIT("幻想乡大地图")
		GMF_MAP_MODE = 0
ENDIF

IF !GMF_临时更新地图
	IF FLAG:一時滞在
		PRINTFORMW 因为有来这里过夜的角色、所以无法搬家
		RETURN
	ENDIF
ENDIF

LINESTART = LINECOUNT
$MAP_LOOP
CLEARLINE LINECOUNT - LINESTART
CALL SET_MAINHOME_DRAW_UI(ANIMATION_SEED, UI_MESSAGE)

UI_MESSAGE = 
INPUT_LOOP_LINE = LINECOUNT

$INPUT_LOOP
CLEARLINE LINECOUNT - INPUT_LOOP_LINE

; NF后缀 + 悬停暂停
IF ANIMATERECOLOREDMAPS > 0 && !FLAG:70
	IF HOVER_PAUSE
		TINPUTSNF 2000, "UPDATE", 0, "", 1
	ELSE
		TINPUTSNF ANIMATERECOLOREDMAPS, "UPDATE", 0, "", 1
	ENDIF
ELSE
	INPUTS "UPDATE", 1
ENDIF

; 悬停状态检测（仅超时时更新标志，不跳过输入处理）
IF RESULTS == "UPDATE"
	IF MOUSEB() != ""
		IF !HOVER_PAUSE
			HOVER_PAUSE = 1
		ENDIF
	ELSE
		IF HOVER_PAUSE
			HOVER_PAUSE = 0
		ENDIF
	ENDIF
ENDIF

LOCAL:0 = (RESULTS == "UPDATE") ? 0 # 1
LOCALS:0 = %RESULTS%

CALL SET_MAINHOME_PROCESS_INPUT(LOCAL:0, LOCALS:0, 設定前MAIN_MAP, 設定前酒虫, 設定前初期位置, 設定前大家, ANIMATION_SEED, UI_MESSAGE)
SELECTCASE RESULT
	CASE -1
		RETURN ; 中止
	CASE 0
		GOTO POST_MOVE ; 决定
	CASE 1
		GOTO INPUT_LOOP
	CASE 2
		GOTO MAP_LOOP
	CASE 3
		ANIMATION_SEED = RESULT:1 ; 更新后的SEED
		GOTO MAP_LOOP
ENDSELECT

$POST_MOVE
SIF !GMF_临时更新地图
	FLAG:神簽契約 = 0
TFLAG:地圖切換 = 0
CALL SET_ENKAI_LOCATIONDISPLAY
CALL MAP_DEFAULTRESIDENT(MAIN_MAP)
VARSET LOCK

FOR LOCAL,1,CHARANUM
	SIF QOL_STAY_AWAKE(LOCAL)
		CONTINUE
	CALL CHARA_SLEEP, LOCAL, 0
NEXT

LOCAL:2 = 0 ; CHK_TRAINING
LOCAL:3 = 0 ; CHK_GARDENING
FOR LOCAL,1,100
	SIF ROOMDATA:LOCAL & 場所_訓練
		LOCAL:2 = 1
	SIF ROOMDATA:LOCAL & 場所_菜園
		LOCAL:3 = 1
NEXT
SIF !LOCAL:2
	CALL COLORMESSAGE(@"这个地图无法进行[战斗训练]",C_YELLOW,2)
SIF !LOCAL:3
	CALL COLORMESSAGE(@"这个地图无法进行[家庭菜园]",C_YELLOW,2)
SIF !SUMIKOMI_ROOM
	CALL COLORMESSAGE(@"这个地图不会发生角色搬入事件。",C_YELLOW,2)
SIF FLAG:扮演 && CFLAG:(FLAG:扮演):出禁 && MAIN_MAP == GET_MAPID(CSVCFLAG(FLAG:扮演,311)) && CSVCFLAG(FLAG:扮演,311) != SUKIMA()
	CFLAG:MASTER:初期位置 = CSVCFLAG(FLAG:扮演,311)

CALL WIFE_MOVE_IN() ;marriage custom code
CALL TRAP_MOVING
RETURN

@SET_MAINHOME_DRAW_UI(ANIMATION_SEED, UI_MESSAGE)
#LOCALSIZE 2
#LOCALSSIZE 1
#DIM DYNAMIC ANIMATION_SEED
#DIMS REF UI_MESSAGE

DRAWLINE
IF RECOLOREDMAPS
	CALL DRAW_COLOREDMAP(ANIMATION_SEED)
ELSE
	CALL DRAW_MAP("HIKKOSHI")
ENDIF
DRAWLINE

PRINTFORML ■%GET_MAPNAME(MAIN_MAP)%
CALL 物件データ(MAIN_MAP)
PRINTFORM 采集点：
CALL ShowGatheringSpots(MAIN_MAP)
PRINTFORML
FONTITALIC
CALLFORM Map紹介_{MAIN_MAP}
FONTREGULAR
PRINTL
DRAWLINEFORM =
PRINT 可以设定住处 想住在哪里呢？
PRINTBUTTON @"[预览图切换({TFLAG:地圖切換})]", 1997
PRINTL

LOCAL:1 = 0
FOR LOCAL, 0, MAXHOME
	SIF MAIN_MAP == LOCAL
		SETCOLOR C_AQUA
	IF GET_EXISTMAP(LOCAL)
		PRINTFORM [{LOCAL + 2000}] %GET_MAPNAME(LOCAL), 16, LEFT%
		SIF (++LOCAL:1) % 4 == 0
			PRINTFORML
	ENDIF
	RESETCOLOR
NEXT
SIF LOCAL:1 % 4 > 0
	PRINTFORML

PRINT [1999] 决定　　　　　　
SIF !ゲーム開始処理中 && !ゲーム继承処理中
	PRINT [1998] 中止
PRINTL

IF UI_MESSAGE != ""
	CALL COLORMESSAGE(@" ※ %UI_MESSAGE% ※", C_RED, 1)
ENDIF

@SET_MAINHOME_PROCESS_INPUT(EVENT_TYPE, EVENT_DATA, 設定前MAIN_MAP, 設定前酒虫, 設定前初期位置, 設定前大家, ANIMATION_SEED, UI_MESSAGE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC EVENT_TYPE
#DIMS DYNAMIC EVENT_DATA
#DIM DYNAMIC 設定前MAIN_MAP
#DIM DYNAMIC 設定前酒虫
#DIM DYNAMIC 設定前初期位置
#DIM DYNAMIC 設定前大家
#DIM DYNAMIC ANIMATION_SEED
#DIMS REF UI_MESSAGE
#DIM DYNAMIC NUM_RES

IF EVENT_TYPE == 0 && EVENT_DATA == "UPDATE"
	RESULT:1 = (ANIMATION_SEED + 1) % 20
	RETURN 3
ENDIF
IF EVENT_DATA == "" || !ISNUMERIC(EVENT_DATA)
	RETURN 1
ENDIF

NUM_RES = TOINT(EVENT_DATA)

IF NUM_RES >= 2000 && GET_EXISTMAP(NUM_RES - 2000)
	MAIN_MAP = NUM_RES - 2000
	CALLFORM ROOMSETTING_{MAIN_MAP}
	CALLFORM DEFAULT_OWNER_{MAIN_MAP}
	TFLAG:地圖切換 = 1
	RETURN 2
ELSEIF NUM_RES == 1997
	TFLAG:地圖切換 = (TFLAG:地圖切換 >= MAP_PAGES) ? 1 # TFLAG:地圖切換 + 1
	RETURN 2
ELSEIF NUM_RES == 1998 && !ゲーム開始処理中 && !ゲーム继承処理中
	引っ越し = 0
	MAIN_MAP = 設定前MAIN_MAP
	拠点_酒虫 = 設定前酒虫
	默认初期位置 = 設定前初期位置
	拠点_家主 = 設定前大家
	TFLAG:地圖切換 = 0
	CALL ROOMSETTING
	RETURN -1
ELSEIF NUM_RES == 1999
	SIF 設定前MAIN_MAP == MAIN_MAP
		CALLFORM DEFAULT_OWNER_{MAIN_MAP}
	RETURN 0
[IF DEBUG]
ELSEIF NUM_RES == -1001
	ANIMATION_SEED = (ANIMATION_SEED - 1 < 0) ? 19 # ANIMATION_SEED - 1
	MAP_ANIMATION_PAUSE = 1
	RESULT:1 = ANIMATION_SEED
	RETURN 3
ELSEIF NUM_RES == -1002
	ANIMATION_SEED = (ANIMATION_SEED + 1) % 20
	MAP_ANIMATION_PAUSE = 1
	RESULT:1 = ANIMATION_SEED
	RETURN 3
ELSEIF NUM_RES == -1003
	MAP_ANIMATION_PAUSE = !MAP_ANIMATION_PAUSE
	RESULT:1 = ANIMATION_SEED
	RETURN 3
[ENDIF]
ENDIF
RETURN 1

@QOL_SET_HIKKOSHI_ROOM
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC INPUT_LOOP_LINE
#DIM DYNAMIC ANIMATION_SEED
#DIMS DYNAMIC UI_MESSAGE
#DIM DYNAMIC HOVER_PAUSE

ANIMATION_SEED = 0
HOVER_PAUSE = 0

LINESTART = LINECOUNT
$MAP_LOOP
CLEARLINE LINECOUNT - LINESTART
CALL HIKKOSHI_ROOM_DRAW_UI(ANIMATION_SEED, UI_MESSAGE)

UI_MESSAGE = 
INPUT_LOOP_LINE = LINECOUNT

$INPUT_LOOP
CLEARLINE LINECOUNT - INPUT_LOOP_LINE

; NF后缀 + 悬停暂停
IF ANIMATERECOLOREDMAPS > 0 && !FLAG:70
	IF HOVER_PAUSE
		TINPUTSNF 2000, "UPDATE", 0, "", 1
	ELSE
		TINPUTSNF ANIMATERECOLOREDMAPS, "UPDATE", 0, "", 1
	ENDIF
ELSE
	INPUTS "UPDATE", 1
ENDIF

; 悬停状态检测（仅超时时更新标志，不跳过输入处理）
IF RESULTS == "UPDATE"
	IF MOUSEB() != ""
		IF !HOVER_PAUSE
			HOVER_PAUSE = 1
		ENDIF
	ELSE
		IF HOVER_PAUSE
			HOVER_PAUSE = 0
		ENDIF
	ENDIF
ENDIF

LOCAL:0 = (RESULTS == "UPDATE") ? 0 # 1
LOCALS:0 = %RESULTS%

CALL HIKKOSHI_ROOM_PROCESS_INPUT(LOCAL:0, LOCALS:0, UI_MESSAGE)
SELECTCASE RESULT
	CASE 0
		RETURN
	CASE 1
		GOTO INPUT_LOOP
	CASE 2
		GOTO MAP_LOOP
	CASE 3
		ANIMATION_SEED = (ANIMATION_SEED + 1) % 20
		GOTO MAP_LOOP
ENDSELECT

@HIKKOSHI_ROOM_DRAW_UI(ANIMATION_SEED, UI_MESSAGE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC ANIMATION_SEED
#DIMS REF UI_MESSAGE
#DIM DYNAMIC CHARA

DRAWLINE
IF RECOLOREDMAPS
	CALL DRAW_COLOREDMAP(ANIMATION_SEED)
ELSE
	CALL DRAW_MAP("HIKKOSHI")
ENDIF
DRAWLINE

PRINTFORML %PRINT_COLOR("这个地图有多个", C_WHITE) + PRINT_COLOR("私室候补", C_ORANGE) + PRINT_COLOR("。", C_WHITE)%
SIF DAY <= 1
	PRINTFORML 第一天限定选项：无视必要信赖选择住的地点。
PRINTBUTTON @"[预览图切换({TFLAG:地圖切換})]", 1997
PRINTL

FOR CHARA, 1, CHARANUM
	IF CFLAG:CHARA:神社在住 && CFLAG:CHARA:大家候補 && GET_MAPID(CFLAG:CHARA:大家候補) == MAIN_MAP
		SIF CFLAG:CHARA:居住必要信赖度 > CFLAG:CHARA:信赖度 && DAY > 1
			SETCOLOR C_L_GRAY
		PRINTBUTTON @"[{CFLAG:CHARA:大家候補 % 100, 3}] - %NAME_FROM_PLACE(CFLAG:CHARA:大家候補), 20, LEFT%　家主:%CALLNAME:CHARA, 15, LEFT%　必要信赖度:{CFLAG:CHARA:居住必要信赖度}", CHARA
		PRINTL
		RESETCOLOR
	ENDIF
NEXT

SIF FamilyHotel
	CALL 引っ越し初期位置設定_FamilyHotel_Choice

PRINTFORML [  0] - ({默认初期位置 % 100, 2})%NAME_FROM_PLACE(默认初期位置)%（默认设定）

IF UI_MESSAGE != ""
	CALL COLORMESSAGE(@" ※ %UI_MESSAGE% ※", C_RED, 1)
ENDIF

@HIKKOSHI_ROOM_PROCESS_INPUT(EVENT_TYPE, EVENT_DATA, UI_MESSAGE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC EVENT_TYPE
#DIMS DYNAMIC EVENT_DATA
#DIMS REF UI_MESSAGE
#DIM DYNAMIC NUM_RES
#DIM DYNAMIC CHARA

IF EVENT_TYPE == 0 && EVENT_DATA == "UPDATE"
	RETURN 3
ENDIF
IF EVENT_DATA == "" || !ISNUMERIC(EVENT_DATA)
	RETURN 1
ENDIF

NUM_RES = TOINT(EVENT_DATA)

IF NUM_RES == 0
	CALL 默认引っ越し
	RETURN 0
ELSEIF NUM_RES < CHARANUM && CFLAG:NUM_RES:神社在住 && CFLAG:NUM_RES:大家候補
	CHARA = NUM_RES
	IF CHARA != 0 && CFLAG:CHARA:居住必要信赖度 > CFLAG:CHARA:信赖度 && DAY > 1
		PRINTFORMW %CALLNAME:CHARA%还没有信赖%CALLNAME:MASTER%到这种程度…
		RETURN 2
	ELSE
		拠点_家主 = CHARA
		默认初期位置 = CFLAG:CHARA:大家候補
		ROOMDATA:(默认初期位置 % 100) |= 場所_被窝
		ROOMDATA:(默认初期位置 % 100) |= 場所_プライベート
		PRINTFORMW 将%NAME_FROM_PLACE(CFLAG:CHARA:大家候補)%设定为了%CALLNAME:MASTER%的私室
		RETURN 0
	ENDIF
ELSEIF NUM_RES == 1997
	TFLAG:地圖切換 = (TFLAG:地圖切換 >= MAP_PAGES) ? 1 # TFLAG:地圖切換 + 1
	RETURN 2
ELSEIF FamilyHotel && NUM_RES == P58大主卧
	CALL 引っ越し初期位置設定_FamilyHotel_Sethome
	RETURN 0
ENDIF
RETURN 1

;-------------------------------------------------
; 函数名: QOL_CANNOT_MOVE_MESSAGE
; 概要:   获取「当前无法移动到该地点」的提示文本（非阻塞式）
; 参数:
;   P_ID     - 目标地点ID
; 返回值: 文字列（失败原因的提示文本）
;-------------------------------------------------
@QOL_CANNOT_MOVE_MESSAGE(P_ID)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC P_ID
#FUNCTIONS
IF !P_ID || NAME_FROM_PLACE(P_ID) == "" || TFLAG:移動不能メッセージ == 998
	LOCALS = 错误的输入({P_ID})
ELSEIF TFLAG:移動不能メッセージ == 999
	LOCALS = 锁上了
ELSE
	LOCALS = 无法向[%NAME_FROM_PLACE(P_ID)%]移动
ENDIF
TFLAG:移動不能メッセージ = 0
RETURNF LOCALS

;-------------------------------------------------
; 函数名: QOL_HITOSAGASI
; 概要:   显示寻人界面，让玩家选择一个房间/地点，返回选择的房间编号
; 参数:
;   DEF_ROOM   - 默认选中的房间编号（可选，0表示无默认）
;   SHOW_OTHER_AREA - 是否显示「查看其他区域」按钮（0=否，1=是）
; 返回值:
;   ≥ MINROOM() 的房间编号   → 玩家选择了该房间
;   -1                        → 玩家按了返回/取消
;-------------------------------------------------
@QOL_HITOSAGASI(DEF_ROOM = 0, SHOW_OTHER_AREA = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC DEF_ROOM
#DIM DYNAMIC SHOW_OTHER_AREA
#DIM DYNAMIC MAP_LOOP_LINE
#DIM DYNAMIC INPUT_LOOP_LINE
#DIM DYNAMIC LOCAL_MAP

LOCAL_MAP = GET_CURRENT_MAP()

MAP_LOOP_LINE = LINECOUNT
DO
	CLEARLINE LINECOUNT - MAP_LOOP_LINE
	CALL HITOSAGASI_2(1, LOCAL_MAP)
	CALL HITOSAGASI_2(2, LOCAL_MAP)
	PRINTBUTTON "[返回]", 0
	IF SHOW_OTHER_AREA == 1
		PRINTL
		PRINTBUTTON "[查看其他区域]", -1
		PRINTL
	ENDIF

INPUT_LOOP_LINE = LINECOUNT
	DO
		CLEARLINE LINECOUNT - INPUT_LOOP_LINE
		INPUTS
		IF RESULTS == "0" || RESULTS == "ESC" || RESULTS == "MOUSE_R"
			RETURN -1
		ELSEIF RESULTS == "-1" && SHOW_OTHER_AREA == 1
			CALL BROAD_HITOSAGASI(LOCAL_MAP)
			BREAK
		ENDIF
		IF ISNUMERIC(RESULTS)
			RESULT = TOINT(RESULTS)
			IF RESULT >= MINROOM(LOCAL_MAP) && RESULT <= MAXROOM(LOCAL_MAP)
				RETURN RESULT
			ENDIF
		ENDIF
		CONTINUE
		
	LOOP 1
	
LOOP 1

;-------------------------------------------------
;函数名:@AWAIT_UI_INPUT
;概　要:非阻塞式UI事件监听引擎
;参　数:CONFIG_STR  = 监听配置字符串 (如 "MOUSE_L|KEY_ENTER|HOTKEYS:Z,X,C")
;      :UPDATE_TIME = (可选) 动画Tick的超时时间(毫秒)，0表示无限等待
;返回值:RESULT   = 事件大类分类码
;         0 : Update Tick (超时更新)
;         1 : 常规输入 (左键按钮、文本框Enter、指定快捷键)
;         2 : 特殊鼠标 (右键、中键)
;         3 : 系统按键 (ESC)
;      :RESULTS  = 具体触发的值（如按钮的Value，文本框内容，或者 "MOUSE_R", "ESC"）
;      :RESULT:1 = 修饰键状态位掩码 (0=无, 1=Shift, 2=Ctrl, 4=Alt, 及其组合)
;备　注:全面接管鼠标左右中键、按键、组合键及文本框。使用 AWAIT 挂起释放 CPU。
;-------------------------------------------------
@AWAIT_UI_INPUT(CONFIG_STR = "DEFAULT", UPDATE_TIME = 0, AWAIT_TIME = -1)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC CONFIG_STR
#DIM DYNAMIC UPDATE_TIME
#DIM DYNAMIC AWAIT_TIME
#DIM DYNAMIC START_TIME
#DIMS DYNAMIC TEMP_STR
#DIM DYNAMIC MODIFIER_KEY

#DIM  DYNAMIC LISTEN_MOUSE_L
#DIM  DYNAMIC LISTEN_MOUSE_R
#DIM  DYNAMIC LISTEN_MOUSE_M
#DIM  DYNAMIC LISTEN_ENTER
#DIM  DYNAMIC LISTEN_ESC
#DIMS DYNAMIC HOTKEY_STRS, 50
#DIM  DYNAMIC HOTKEY_CODES, 50
#DIM  DYNAMIC HOTKEY_COUNT
#DIMS DYNAMIC CONF_ARR, 20
#DIM  DYNAMIC CONF_NUM
#DIMS DYNAMIC HOT_ARR, 50
#DIM  DYNAMIC HOT_NUM
#DIM  DYNAMIC L_COUNT

SIF AWAIT_TIME == -1
	AWAIT_TIME = 16
IF CONFIG_STR == "DEFAULT"
	CONFIG_STR = "MOUSE_L|KEY_ENTER"
ELSEIF CONFIG_STR == "MAP_MODE"
	CONFIG_STR = "MOUSE_L|KEY_ENTER|KEY_ESC|HOTKEYS:Z,X,C,Y"
ENDIF

SPLIT CONFIG_STR, "|", CONF_ARR, CONF_NUM
FOR L_COUNT, 0, CONF_NUM
	SELECTCASE CONF_ARR:L_COUNT
		CASE "MOUSE_L"
			LISTEN_MOUSE_L = 1
		CASE "MOUSE_R"
			LISTEN_MOUSE_R = 1
		CASE "MOUSE_M"
			LISTEN_MOUSE_M = 1
		CASE "KEY_ENTER"
			LISTEN_ENTER = 1
		CASE "KEY_ESC"
			LISTEN_ESC = 1
		CASEELSE
			IF STRFIND(CONF_ARR:L_COUNT, "HOTKEYS:") == 0
				TEMP_STR = %SUBSTRING(CONF_ARR:L_COUNT, 8)%
				SPLIT TEMP_STR, ",", HOT_ARR
				HOT_NUM = RESULT
				FOR LOCAL, 0, HOT_NUM
					HOTKEY_STRS:HOTKEY_COUNT = %HOT_ARR:LOCAL%
					HOTKEY_CODES:HOTKEY_COUNT = ENCODETOUNI(HOT_ARR:LOCAL)
					HOTKEY_COUNT ++
				NEXT
			ENDIF
	ENDSELECT
NEXT

START_TIME = GETMILLISECOND()
DO
	AWAIT AWAIT_TIME
	
	; [新增] 悬停侦测，如果鼠标悬停状态发生改变，立即触发UPDATE
	IF MOUSEB() != AWAIT_HOVER_VALUE
		AWAIT_HOVER_VALUE '= MOUSEB()
		RESULTS '= "UPDATE"
		RESULT:1 = 0
		RETURN 0
	ENDIF

	MODIFIER_KEY = 0
	SIF GETKEY(16)
		SETBIT MODIFIER_KEY, 0
	SIF GETKEY(17)
		SETBIT MODIFIER_KEY, 1
	SIF GETKEY(18)
		SETBIT MODIFIER_KEY, 2

	; 【鼠标左键】
	IF LISTEN_MOUSE_L && GETKEYTRIGGERED(1)
		TEMP_STR '= MOUSEB()
		IF TEMP_STR != ""
			DEBUGPRINTFORML [AWAIT_UI] 左键点击! 目标:%TEMP_STR%
			RESULTS '= TEMP_STR
			RESULT:1 = MODIFIER_KEY
			RETURN 1
		ENDIF
	ENDIF
	
	; 【鼠标右键】
	IF LISTEN_MOUSE_R && GETKEYTRIGGERED(2)
		DEBUGPRINTFORML [AWAIT_UI] 右键点击!
		RESULTS '= "MOUSE_R"
		RESULT:1 = MODIFIER_KEY
		RETURN 2
	ENDIF
	
	; 【鼠标中键】
	IF LISTEN_MOUSE_M && GETKEYTRIGGERED(4)
		RESULTS '= "MOUSE_M"
		RESULT:1 = MODIFIER_KEY
		RETURN 2
	ENDIF

	; 【回车键】
	IF LISTEN_ENTER && GETKEYTRIGGERED(13)
		TEMP_STR = %GETTEXTBOX()%
		IF TEMP_STR != ""
			DEBUGPRINTFORML [AWAIT_UI] 回车输入! 内容:%TEMP_STR%
			RESULTS '= TEMP_STR
			RESULT:1 = MODIFIER_KEY
			RETURN 1
		ENDIF
	ENDIF

	; 【ESC键】
	IF LISTEN_ESC && GETKEYTRIGGERED(27)
		DEBUGPRINTFORML [AWAIT_UI] ESC 按下!
		RESULTS '= "ESC"
		RESULT:1 = MODIFIER_KEY
		RETURN 3
	ENDIF

	; 【自定义快捷键】
	IF HOTKEY_COUNT > 0
		FOR L_COUNT, 0, HOTKEY_COUNT
			IF GETKEYTRIGGERED(HOTKEY_CODES:L_COUNT)
				DEBUGPRINTFORML [AWAIT_UI] 快捷键按下! 键:%HOTKEY_STRS:L_COUNT%
				RESULTS '= HOTKEY_STRS:L_COUNT
				RESULT:1 = MODIFIER_KEY
				RETURN 1
			ENDIF
		NEXT
	ENDIF

	; 【Tick 更新计时器】
	IF UPDATE_TIME > 0 && (GETMILLISECOND() - START_TIME) >= UPDATE_TIME
		RESULTS '= "UPDATE"
		RESULT:1 = 0
		RETURN 0
	ENDIF
LOOP 1

;-------------------------------------------------
;函数名:@UI_AWAIT_CONFIG
;概　要:构建 AWAIT_UI_INPUT 的配置字符串
;参　数:BASE_MODE = 基础模式 ("DEFAULT", "MAP_MODE", "ALL_MOUSE" 等)
;      :ADD_KEYS  = 追加的快捷键，逗号分隔 (如 "A,B,C")
;      :USE_R_BTN = 是否启用右键 (1=启用)
;      :USE_M_BTN = 是否启用中键 (1=启用)
;      :USE_ESC   = 是否启用ESC键 (1=启用)
;返回值:组装好的配置字符串
;备　注:利用此函数可以屏蔽底层字符串拼写，保持业务端代码整洁
;-------------------------------------------------
@UI_AWAIT_CONFIG(BASE_MODE = "DEFAULT", ADD_KEYS = "", USE_R_BTN = 0, USE_M_BTN = 0, USE_ESC = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIMS DYNAMIC BASE_MODE
#DIMS DYNAMIC ADD_KEYS
#DIM DYNAMIC USE_R_BTN
#DIM DYNAMIC USE_M_BTN
#DIM DYNAMIC USE_ESC
#DIMS DYNAMIC FINAL_CONF

SELECTCASE BASE_MODE
	CASE "DEFAULT"
		FINAL_CONF = MOUSE_L|KEY_ENTER
	CASE "MAP_MODE"
		FINAL_CONF = MOUSE_L|KEY_ENTER|KEY_ESC|HOTKEYS:Z,X,C,Y
	CASE "ALL_MOUSE"
		FINAL_CONF = MOUSE_L|MOUSE_R|MOUSE_M|KEY_ENTER
	CASEELSE
		FINAL_CONF = MOUSE_L|KEY_ENTER ; 容错默认值
ENDSELECT

SIF USE_R_BTN && STRFIND(FINAL_CONF, "MOUSE_R") < 0
	FINAL_CONF += "|MOUSE_R"
SIF USE_M_BTN && STRFIND(FINAL_CONF, "MOUSE_M") < 0
	FINAL_CONF += "|MOUSE_M"
SIF USE_ESC && STRFIND(FINAL_CONF, "KEY_ESC") < 0
	FINAL_CONF += "|KEY_ESC"

IF ADD_KEYS != ""
	IF STRFIND(FINAL_CONF, "HOTKEYS:") >= 0
		; 如果已经有 HOTKEYS 标签，直接追加
		FINAL_CONF += "," + ADD_KEYS
	ELSE
		; 否则新建标签
		FINAL_CONF += "|HOTKEYS:" + ADD_KEYS
	ENDIF
ENDIF

RETURNF FINAL_CONF

;-------------------------------------------------
;函数名:@UI_EXECUTE_MOVE
;概　要:处理从菜单、对话框等UI界面发起的瞬时移动请求
;参　数:TARGET_ROOM = 目标房间ID
;返回值:1=移动成功，0=移动失败
;-------------------------------------------------
@UI_EXECUTE_MOVE(TARGET_ROOM)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC TARGET_ROOM
#DIM DYNAMIC LOCAL_MAP
;GMF：用GET_MAPID(TARGET_ROOM)替代MAIN_MAP分发
IF GMF_GETBIT("幻想乡大地图")
	LOCAL_MAP = GET_MAPID(TARGET_ROOM)
ELSE
	LOCAL_MAP = MAIN_MAP
ENDIF

; 已经在目的地
SIF TARGET_ROOM == CFLAG:MASTER:現在位置
	RETURN 0
; 间隙内直接移动
IF IS_GAP_GUEST(MASTER)
	CALL SKIP_MOVE(CFLAG:MASTER:現在位置, TARGET_ROOM)
	RETURN 1
ENDIF
; 不在主地图
IF !AT_HOME(MASTER) || IN_HOME(TARGET_ROOM) != 1
	TFLAG:マップ切り替え = 0
	PRINTW 没法移动过去
	RETURN 0
ENDIF
; 移动限制检查
IF MAP_CAN_MOVE(TARGET_ROOM) == 0
	IF TFLAG:移動不能メッセージ == 999
		IF LOCKPICK_SYSTEM > 0
			; 调用开锁系统
			CALL LOCKPICK_INFO(TARGET_ROOM)
			RETURN RESULT
		ELSE
			PRINTFORMW 锁上了 可恶
		ENDIF
	ELSE
		; 打印原本的不可移动文本
		TRYCALLFORM MAP_CANNOT_WORD_{LOCAL_MAP}(TARGET_ROOM)
	ENDIF
	TFLAG:移動不能メッセージ = 0
	RETURN 0
ENDIF
; 正常移动
CALL SKIP_MOVE(CFLAG:MASTER:現在位置, TARGET_ROOM)
RETURN 1
