@GETMETH_INT(ARG, CLASS_NAME, O_ID, O_DATA)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS CLASS_NAME  ;OBJの名前（例：WEAPON）
#DIM  O_ID        ;参照するOBJのID（例：1）
#DIMS O_DATA      ;参照したい情報の名前（例：価格）
;DEBUGPRINTFORML GET_INT ARG：{ARG}, CLASS_NAME：%CLASS_NAME%, O_ID：{O_ID}, O_DATA：%O_DATA%
;IF !EXISTMETH(@"F%CLASS_NAME%{O_ID}")
;	DEBUGPRINTFORML F%CLASS_NAME%{O_ID}不存在
;	RETURNF 0
;ENDIF
LOCALS = %GETMETHS(@"F%CLASS_NAME%{O_ID}", "0", ARG, O_DATA)%
;DEBUGPRINTFORML GET_INT LOCALS：%LOCALS%
SIF !STRLENS(LOCALS)
	RETURNF 0
SIF !ISNUMERIC(LOCALS)
	RETURNF 0
RETURNF TOINT(LOCALS)

@GETMETH_STR(ARG, CLASS_NAME, O_ID, O_DATA)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS CLASS_NAME  ;OBJの名前（例：WEAPON）
#DIM  O_ID        ;参照するOBJのID（例：1）
#DIMS O_DATA      ;参照したい情報の名前（例：名前）
;DEBUGPRINTFORML GET_STR ARG：{ARG}, CLASS_NAME：%CLASS_NAME%, O_ID：{O_ID}, O_DATA：%O_DATA%
;IF !EXISTMETH(@"F%CLASS_NAME%{O_ID}")
;	DEBUGPRINTFORML F%CLASS_NAME%{O_ID}不存在
;	RETURNF 
;ENDIF
RETURNF GETMETHS(@"F%CLASS_NAME%{O_ID}", "", ARG, O_DATA)

@GETMETH_OBJNAME_TO_ID(ARG, CLASS_NAME, KEY)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC CLASS_NAME
#DIMS DYNAMIC KEY
#DIMS DYNAMIC O_MAP
O_MAP '= CLASS_NAME + "_NAME2ID"
IF MAP_EXIST(O_MAP)
	LOCALS '= MAP_GET(O_MAP, KEY)
	IF LOCALS != ""
		RETURNF TOINT(LOCALS)
	ENDIF
ENDIF
;只有动态名需要
FOR LOCAL, 0, OBJ_ID_LAST + 1
	IF GETMETH_STR(ARG, CLASS_NAME, LOCAL, "名前") == KEY
		RETURNF LOCAL
	ENDIF
NEXT
RETURNF -1

@MAKE_ALL_OOP_MAP
#LOCALSIZE 1
#LOCALSSIZE 1
CALL MAKE_OOP_MAP("キャラデータ", 0, 人物数量上限)
CALL MAKE_OOP_MAP("角色列表", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("衣装セット", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("腕部装束", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("頭装備", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("和服", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("上身衣服_はだけ不可", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("上身衣服_はだけ可能", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("上半身内衣_はだけ不可", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("上半身内衣_はだけ可能", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("襪子", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("靴", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("外搭", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("下半身内衣_ずらし不可", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("下半身内衣_ずらし可能", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("連衣裙", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("レオタード", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("ボディースーツ", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("其他衣装", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("裤子", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("裙子", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("飾品", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("一般依頼", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("FreeAct", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("酒データ", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("ADD_BANQUET", 0, OBJ_ID_LAST)
CALL MAKE_OOP_MAP("ADD_GAME_INFO", 0, OBJ_ID_LAST)

@MAKE_OOP_MAP(CLASS_NAME, O_FIRST = OBJ_ID_FIRST, O_LAST = OBJ_ID_LAST)
#DIMS DYNAMIC CLASS_NAME
#DIM DYNAMIC O_FIRST
#DIM DYNAMIC O_LAST
#DIMS DYNAMIC O_MAP
#LOCALSIZE 1
#LOCALSSIZE 1
O_MAP '= CLASS_NAME + "_NAME2ID"
IF !MAP_EXIST(O_MAP)
	MAP_CREATE O_MAP
	;DEBUGPRINTFORML ------------------------------
	FOR LOCAL, O_FIRST, OBJ_ID_LAST + 1
		LOCALS '= GETMETH_STR(0, CLASS_NAME, LOCAL, "名前")
		IF LOCALS != ""
			MAP_SET O_MAP, LOCALS, TOSTR(LOCAL)
			;DEBUGPRINTFORML {LOCAL, 3, LEFT}: %LOCALS%
		ENDIF
	NEXT
	;DEBUGPRINTFORML ------------------------------
	;DEBUGPRINTFORML %O_MAP% map made
ENDIF
