;==============================================================================
;1：打印与交互(Print and Input)
; PRINT系函数的封装，PRINT系与INPUT交互的封装。
;==============================================================================

;==============================================================================
;符号打印
;此类函数均无太大参考意义，均为系统所需的高频调用，需要的字体属性越复杂，就越应该使用HTML标签来实现
;==============================================================================
;-------------------------------------------------
; 打印等级符号
;-------------------------------------------------
@PRINT_RANK(VAR_VALUE, RANK_SCALE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM  DYNAMIC VAR_VALUE  ;評価対象数値
#DIMS DYNAMIC RANK_SCALE ;評価に使用する尺度の種類
HTML_PRINT GET_RANK_HTML(VAR_VALUE, RANK_SCALE), 1

@GET_RANK_HTML(VAR_VALUE, RANK_SCALE)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIM	DYNAMIC VAR_VALUE
#DIMS	DYNAMIC RANK_SCALE
#DIM	DYNAMIC RANK
#DIM	DYNAMIC RANK_NUM
CALLF CALC_RANK(VAR_VALUE, RANK_SCALE, RANK, RANK_NUM)
RETURNF GET_ALPHABET_HTML(RANK, RANK_NUM)

@PRINT_ALPHABET(RANK, RANK_NUM, ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC RANK
#DIM DYNAMIC RANK_NUM
HTML_PRINT GET_ALPHABET_HTML(RANK, RANK_NUM, ARG), 1

@GET_ALPHABET_HTML(RANK, RANK_NUM, ARG = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIM DYNAMIC RANK
#DIM DYNAMIC RANK_NUM
#DIMS CONST STR_RANK10, 10 = "Ex", "SS", " S", " A", " B", " C", " D", " E", " F", " G"
#DIMS CONST STR_RANK7,   7 = "Ex", " S", " A", " B", " C", " D", " E"
#DIMS CONST STR_RANK10_F, 10 = "ＳＳＳ", "ＳＳ", "Ｓ", "Ａ", "Ｂ", "Ｃ", "Ｄ", "Ｅ", "Ｆ", "Ｇ"
#DIMS CONST STR_RANK7_F,   7 = "ＥＸ", "Ｓ", "Ａ", "Ｂ", "Ｃ", "Ｄ", "Ｅ"
#DIM  CONST C_RANK10, 10 = C_P_PURPLE, C_WHITE, C_WHITE, C_PINK, C_RED, C_ORANGE, C_YELLOW, C_GREEN, C_AQUA, C_GRAY
#DIM  CONST C_RANK7,   7 = C_P_PURPLE, C_WHITE, C_PINK, C_RED, C_ORANGE, C_YELLOW, C_GRAY
#DIMS DYNAMIC RANK_TEXT
#DIM  DYNAMIC RANK_COLOR

IF ARG
	SELECTCASE RANK_NUM
		CASE 10
			RANK_TEXT '= STR_RANK10_F:RANK
			RANK_COLOR = C_RANK10:RANK
		CASE 7
			RANK_TEXT '= STR_RANK7_F:RANK
			RANK_COLOR = C_RANK7:RANK
		CASEELSE
			RETURNF "ERR"
	ENDSELECT
ELSE
	SELECTCASE RANK_NUM
		CASE 10
			RANK_TEXT '= STR_RANK10:RANK
			RANK_COLOR = C_RANK10:RANK
		CASE 7
			RANK_TEXT '= STR_RANK7:RANK
			RANK_COLOR = C_RANK7:RANK
		CASEELSE
			RETURNF "ERR"
	ENDSELECT
ENDIF
RETURNF @"<font color='%HEX_TO_HTML_COLOR(RANK_COLOR)%'>%RANK_TEXT%</font>"

;-------------------------------------------------
; 打印颜色印章
;-------------------------------------------------
@COLOR_STAMP(STAMP_NUM, MAX_NUM, STAMP_STR, STAMP_COLOR, ARG)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM  DYNAMIC STAMP_NUM
#DIM  DYNAMIC MAX_NUM
#DIMS DYNAMIC STAMP_STR
#DIM  DYNAMIC STAMP_COLOR
HTML_PRINT GET_STAMP_HTML(STAMP_NUM, MAX_NUM, STAMP_STR, STAMP_COLOR, ARG), 1

@GET_STAMP_HTML(STAMP_NUM, MAX_NUM, STAMP_STR, STAMP_COLOR, ARG = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIM  DYNAMIC STAMP_NUM
#DIM  DYNAMIC MAX_NUM
#DIMS DYNAMIC STAMP_STR
#DIM  DYNAMIC STAMP_COLOR
#DIMS DYNAMIC RES_STR
; ARG=0 (默认) 是右对齐，先补空格
IF !ARG
	; 使用全角空格 "　" 来对齐全角符号
	RES_STR += "　" * MAX(MAX_NUM - STAMP_NUM, 0)
ENDIF
IF STAMP_NUM > 0
	RES_STR += @"<font color='%HEX_TO_HTML_COLOR(STAMP_COLOR)%'>%STAMP_STR * STAMP_NUM%</font>"
ENDIF
RETURNF RES_STR


;;-------------------------------------------------
;; 函数名：COLOR_PRINT（含义过于宽泛，禁止添加新调用）(已删除)
;; 概  述：实际是PRINT COLORED CHARACTERS，打印着色的指定制表长度的字符串，等级符号打印@PRINT_ALPHABET的抽象工具
;;-------------------------------------------------
;@COLOR_PRINT(ARGS, C_NAME, STR_NUM = 2)
;#LOCALSIZE 1
;#LOCALSSIZE 1
;#DIM C_NAME
;#DIM  STR_NUM
;SIF !STRLENS(ARGS)
;	RETURN
;LOCAL = GETCOLOR()
;SETCOLOR C_NAME
;PRINTFORM %ARGS, STR_NUM%
;SETCOLOR LOCAL

;-------------------------------------------------
;函数名:GET_HEARTMARK_HTML
;概　要:生成粉色爱心的HTML字符串
;参　数:L_COUNT   - 爱心数量
;       USE_COLOR - 是否使用粉色（否则继承当前颜色）
;返回值:包含HTML标签和特殊字体样式的字符串
;备　注:使用Times New Roman字体渲染
;-------------------------------------------------
@GET_HEARTMARK_HTML(L_COUNT = 1, USE_COLOR = 1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIM DYNAMIC L_COUNT
#DIM DYNAMIC USE_COLOR
RETURNF HTMLFONT("❤" * L_COUNT, "Times New Roman", USE_COLOR ? C_H_PINK # -1)

;-------------------------------------------------
; 函数名：HEARTMARK
; 概  述：以特定颜色和字体输出心形符号
; 参  数：L_COUNT - 爱心数量
;         L_MODE  - 模式（PLAIN时不用粉色）
; 返回值：无（直接HTML_PRINT输出）
;-------------------------------------------------
@HEARTMARK(L_COUNT = 1, L_MODE = "")
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC L_COUNT
#DIMS DYNAMIC L_MODE
HTML_PRINT GET_HEARTMARK_HTML(L_COUNT, L_MODE != "PLAIN"), 1

; 兼容旧的 HEART
@HEART(L_COUNT = 1, L_MODE = "")
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC L_COUNT
#DIMS DYNAMIC L_MODE
CALL HEARTMARK(L_COUNT, L_MODE)

;-------------------------------------------------
;函数名:GET_SHYMARK_HTML
;概　要:生成害羞斜杠的HTML字符串
;参　数:L_COUNT   - 斜杠组数
;       USE_COLOR - 是否使用粉色（否则继承当前颜色）
;返回值:包含HTML标签和特殊字体样式的字符串
;备　注:使用MS Gothic字体渲染
;-------------------------------------------------
@GET_SHYMARK_HTML(L_COUNT = 1, USE_COLOR = 1)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIM DYNAMIC L_COUNT
#DIM DYNAMIC USE_COLOR
RETURNF HTMLFONT("///" * L_COUNT, "MS Gothic", USE_COLOR ? C_H_PINK # -1)

;-------------------------------------------------
; 函数名：SHYMARK
; 概  述：以特定颜色和字体输出三连斜杠
; 参  数：L_COUNT - 斜杠组数
;         L_MODE  - 模式（PLAIN时不用粉色）
; 返回值：无（直接HTML_PRINT输出）
;-------------------------------------------------
@SHYMARK(L_COUNT = 1, L_MODE = "")
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC L_COUNT
#DIMS DYNAMIC L_MODE
HTML_PRINT GET_SHYMARK_HTML(L_COUNT, L_MODE != "PLAIN"), 1

;这个函数没有使用意义了，最开始他是调用UNICODE(0x2665)的语法封装。但是现在支持明文心形，所以直接复制粘贴需要的符号即可
@GET_HEART()
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
RETURNF "♥"

@CLIP_HEART(L_STR)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIMS DYNAMIC L_STR
RETURNF "♥" + L_STR + "♥"

;-------------------------------------------------
;函数名:GET_MSG_CODE_HTML
;概　要:使用微软哥特字体渲染符号的HTML字符串
;参　数:ARGS - 符号文本
;返回值:包含HTML标签的字符串
;备　注:继承当前控制台颜色
;-------------------------------------------------
@GET_MSG_CODE_HTML(ARGS)
#FUNCTIONS
RETURNF HTMLFONT(ARGS, "MS Gothic", -1)

;-------------------------------------------------
;函数名:MSG_CODE
;概　要:使用微软哥特字体的符号（封装版本）
;主要是为了控制制表符号显示
;-------------------------------------------------
@MSG_CODE(ARGS)
HTML_PRINT GET_MSG_CODE_HTML(ARGS), 1

;==============================================================================
;文本打印
;==============================================================================
;-------------------------------------------------
; 函数名：COLORMESSAGE
; 概  述：带颜色的打印函数。会保存调用前正在使用的颜色和字体，
;         使用结束后自动恢复为原来的颜色和字体设置。
; 参  数：
;     L_STR        - 文本内容
;     L_COLOR      - 颜色值
;     L_TYPE       - 显示形式
;                   (0 = PRINT
;                    1 = PRINTL
;                    2 = PRINTW
;                    3 = PRINTPLAIN)
;     L_STYLE      - 字体样式
;                   (0 = 普通
;                    1 = 粗体
;                    2 = 斜体
;                    4 = 删除线
;                    8 = 下划线)
;         ※ 样式采用位运算管理，例如：
;           3 = 0011 = 1 + 2 = 粗体 + 斜体
; 备  注：
;     此函数默认不换行，需要显性指定换行或等待。
;-------------------------------------------------
@COLORMESSAGE(L_STR, L_COLOR, L_TYPE, L_STYLE)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC L_STR
#DIM DYNAMIC L_COLOR
#DIM DYNAMIC L_TYPE
#DIM DYNAMIC L_STYLE
#DIM DYNAMIC TEMP_STYLE

LOCAL = GETCOLOR()
TEMP_STYLE = GETSTYLE()

SIF L_STYLE
	FONTSTYLE L_STYLE
SETCOLOR L_COLOR

SELECTCASE L_TYPE
	CASE 0
		PRINTFORM %L_STR%
	CASE 1
		PRINTFORML %L_STR%
	CASE 2
		PRINTFORMW %L_STR%
	CASE 3
		PRINTPLAINFORM %L_STR%
ENDSELECT

SETCOLOR LOCAL
FONTSTYLE TEMP_STYLE

;-------------------------------------------------
; 函数名：COND_COLORMESSAGE
; 概  述：满足条件时打印指定颜色的消息
; 参  数：COND     - 判断条件（满足时才着色）
;         MESSAGE  - 消息文本
;         L_COLOR  - 指定颜色（新增第三个参数，默认为明黄色）
;         L_TYPE   - 输出类型（新增第四个参数，默认为1，即PRINTFORML）
; 备  注：
;     此函数因为向后兼容，所以默认换行。
;-------------------------------------------------
@COND_COLORMESSAGE(COND,MESSAGE,L_COLOR = C_YELLOW,L_TYPE = 1)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM COND
#DIMS MESSAGE
#DIM L_COLOR
#DIM L_TYPE
LOCAL = GETCOLOR()
SIF COND
	SETCOLOR L_COLOR
SELECTCASE L_TYPE
	CASE 0
		PRINTFORM %MESSAGE%
	CASE 1
		PRINTFORML %MESSAGE%
	CASE 2
		PRINTFORMW %MESSAGE%
	CASE 3
		PRINTPLAINFORM %MESSAGE%
ENDSELECT
SETCOLOR LOCAL

;只有参数顺序与COLORMESSAGE不同的另一套带颜色的打印函数。重定向到COLORMESSAGE
@PRINTCOLOR(L_COLOR, L_STR, L_TYPE = 0)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC L_STR
#DIM DYNAMIC L_COLOR
#DIM DYNAMIC L_TYPE
CALL COLORMESSAGE(L_STR, L_COLOR, L_TYPE)
RETURN RESULT

@PRINTCOLORL(L_COLOR, L_STR)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC L_STR
#DIM DYNAMIC L_COLOR
#DIM DYNAMIC L_TYPE
CALL COLORMESSAGE(L_STR, L_COLOR, 1)
RETURN RESULT

@PRINTCOLORW(L_COLOR, L_STR)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC L_STR
#DIM DYNAMIC L_COLOR
#DIM DYNAMIC L_TYPE
CALL COLORMESSAGE(L_STR, L_COLOR, 2)
RETURN RESULT

;-------------------------------------------------
; 函数名：HPH_PRINT
; 概  述：解析字符串中的特殊标记（HPH / BPB），
;         将其替换为对应的粉色爱心或斜杠特效，并按模式输出文本。
; 参  数：
;     L_STR   - 待解析并输出的字符串
;     L_MODE  - 输出模式（可选）
;               ""  = 普通 PRINTS（不推送不换行）
;               "l" = PRINTSL   （输出后换行）
;               "w" = PRINTSW   （输出后等待）
;               "dl"= PRINTSDL  （默认色 + 换行）
;               "dw"= PRINTSDW  （默认色 + 等待）
; 功  能：
;     HPH → 调用 HEARTMARK 输出粉色爱心效果
;     BPB → 输出粉色斜杠特效（///）
; 备  注：
;     ·此函数默认不换行。
;     ·穰子剧本作者原创的特殊文本函数，毫无疑问是最常被调用的文本渲染函数。
;     ·半角的三连///符号是FORM语法的关键字，在PRINTFORM中会被解析为CALLNAME:ASSI，而本游戏中ASSI为-1，一旦被调用就会数值越界而跳出，此函数便是为避免这一情况而出现的
;-------------------------------------------------
@HPH_PRINT(L_STR, L_MODE = "")
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC L_STR
#DIMS DYNAMIC L_MODE
#DIM DYNAMIC L_IDX_HPH
#DIM DYNAMIC L_IDX_BPB
#DIM DYNAMIC L_IDX_NEXT
#DIMS DYNAMIC L_PRINT_PART
SELECTCASE TOLOWER(L_MODE)
	CASE "str"
		L_STR '= REPLACE(L_STR, "HPH", "@H@")
		L_STR '= REPLACE(L_STR, "BPB", "@B@")
		;DEBUGPRINTFORML %L_STR%
		JUMP PRINT_STR, L_STR
	CASE "strl"
		L_STR '= REPLACE(L_STR, "HPH", "@H@")
		L_STR '= REPLACE(L_STR, "BPB", "@B@")
		;DEBUGPRINTFORML %L_STR%
		JUMP PRINT_STRL, L_STR
	CASE "strw"
		L_STR '= REPLACE(L_STR, "HPH", "@H@")
		L_STR '= REPLACE(L_STR, "BPB", "@B@")
		;DEBUGPRINTFORML %L_STR%
		JUMP PRINT_STRW, L_STR
ENDSELECT
DO
	L_IDX_HPH = STRFIND(L_STR, "HPH")
	L_IDX_BPB = STRFIND(L_STR, "BPB")
	
	IF L_IDX_HPH >= 0 && L_IDX_BPB < 0
		L_IDX_NEXT = L_IDX_HPH
	ELSEIF L_IDX_HPH < 0 && L_IDX_BPB >= 0
		L_IDX_NEXT = L_IDX_BPB
	ELSE
		L_IDX_NEXT = MIN(L_IDX_HPH, L_IDX_BPB)
	ENDIF

	; 如果前面有普通文字
	IF L_IDX_NEXT >= 1
		L_PRINT_PART = %SUBSTRING(L_STR, 0, L_IDX_NEXT)%
		IF GROUPMATCH(TOLOWER(L_MODE), "dw", "dl")
			PRINTSD @"%L_PRINT_PART%"
		ELSE
			PRINTS @"%L_PRINT_PART%"
		ENDIF
		L_STR = %SUBSTRING(L_STR, L_IDX_NEXT, -1)%
		
	; 如果开头就是 HPH
	ELSEIF L_IDX_HPH == 0
		CALL HEARTMARK
		L_STR = %SUBSTRING(L_STR, 3, -1)%
		
	; 如果开头就是 BPB
	ELSEIF L_IDX_BPB == 0
		CALL SHYMARK
		L_STR = %SUBSTRING(L_STR, 3, -1)%
		
	; 没有特殊标记了，打印剩余部分并根据 L_MODE 换行/等待
	ELSE
		SELECTCASE TOLOWER(L_MODE)
			CASE "dw"
				PRINTSDW @"%L_STR%"
			CASE "dl"
				PRINTSDL @"%L_STR%"
			CASE "w"
				PRINTSW @"%L_STR%"
			CASE "l"
				PRINTSL @"%L_STR%"
			CASEELSE
				PRINTS @"%L_STR%"
		ENDSELECT
		BREAK
	ENDIF
LOOP 1

;-------------------------------------------------
; 函数名：PRINT_STR
; 概  述：解析特殊标记的字符串并输出
;        支持通过 @ 分割，实现动态变色、按钮化、特殊心形符号等。
; 参  数：允许传入最多10个字符串，内部拼接后解析
;-------------------------------------------------
@PRINT_STR(L_S:0, L_S:1="", L_S:2="", L_S:3="", L_S:4="", L_S:5="", L_S:6="", L_S:7="", L_S:8="", L_S:9="")
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC L_S,10
#DIM DYNAMIC L_OLD_C
#DIM DYNAMIC L_OLD_F
#DIMS DYNAMIC L_STR
#DIMS DYNAMIC L_TOKENS, 300
#DIM DYNAMIC L_TOKEN_CNT
#DIMS DYNAMIC L_PARAMS, 5

L_STR = %L_S:0%
FOR LOCAL, 1, 10
	IF L_S:LOCAL != ""
		L_STR += "@" + L_S:LOCAL
	ENDIF
NEXT
L_OLD_C = GETCOLOR()
L_OLD_F = GETSTYLE()

SPLIT L_STR, "@", L_TOKENS, L_TOKEN_CNT

FOR LOCAL, 0, L_TOKEN_CNT
	SELECTCASE L_TOKENS:LOCAL
		CASE "CALLNAME:TARGET"
			PRINTFORM %CALLNAME:TARGET%
			CONTINUE
		CASE "CALLNAME:PLAYER"
			PRINTFORM %CALLNAME:PLAYER%
			CONTINUE
		CASE "CALLNAME:ASSI"
			PRINTFORM %CALLNAME:ASSI%
			CONTINUE
		CASE "CALLNAME:MASTER"
			PRINTFORM %CALLNAME:MASTER%
			CONTINUE
			
		; 按钮相关
		CASE "BUTTON"
			LOCAL++
			PRINTBUTTON L_TOKENS:LOCAL, L_TOKENS:LOCAL
			SETCOLOR L_OLD_C
			FONTREGULAR
			CONTINUE
		CASE "BUTTONS"
			LOCAL++
			SPLIT L_TOKENS:LOCAL, "|", L_PARAMS
			PRINTBUTTON L_PARAMS:0, L_PARAMS:1
			SETCOLOR L_OLD_C
			FONTREGULAR
			CONTINUE
		CASE "BUTTONNUM"
			LOCAL++
			SPLIT L_TOKENS:LOCAL, "|", L_PARAMS
			PRINTBUTTON L_PARAMS:0, TOINT(L_PARAMS:1)
			SETCOLOR L_OLD_C
			FONTREGULAR
			CONTINUE
		CASE "NOBUTTON"
			LOCAL++
			PRINTPLAINFORM %L_TOKENS:LOCAL%
			SETCOLOR L_OLD_C
			FONTREGULAR
			CONTINUE
			
		; 控制指令
		CASE "-"
			FONTSTYLE 4
			CONTINUE
		CASE "L"
			PRINTL 
			CONTINUE
		CASE "W"
			PRINTW 
			CONTINUE
		CASE "WAIT"
			WAIT
			CONTINUE
		CASE "FORCEWAIT"
			FORCEWAIT
			CONTINUE
			
		; 特殊符号
		CASE "H", "HEART", "HPH"
			CALL HEARTMARK
			CONTINUE
		CASE "／／／", "BPB"
			CALL SHYMARK
			CONTINUE
			
		; 颜色控制
		CASE "RGB"
			LOCAL++
			SPLIT L_TOKENS:LOCAL, "//", L_PARAMS
			SETCOLOR RGB_TO_HEX(TOINT(L_PARAMS:0), TOINT(L_PARAMS:1), TOINT(L_PARAMS:2))
			CONTINUE
		CASE "HEX"
			LOCAL++
			SETCOLOR TOINT(L_TOKENS:LOCAL)
			CONTINUE
			
		; 字体样式
		CASE "Ｂ", "BOLD"
			FONTBOLD
			CONTINUE
		CASE "UNDER"
			FONTSTYLE 8
			CONTINUE
		CASE "ITALIC"
			FONTITALIC
			CONTINUE
		CASE "STRIKE"
			FONTSTYLE 4
			CONTINUE
	ENDSELECT

	; 默认打印普通文本
	PRINTFORM %L_TOKENS:LOCAL%
	SETCOLOR L_OLD_C
	FONTREGULAR
NEXT

@PRINT_STRL, ARGS, ARGS:1, ARGS:2, ARGS:3, ARGS:4, ARGS:5, ARGS:6, ARGS:7, ARGS:8, ARGS:9
#LOCALSIZE 1
#LOCALSSIZE 1
CALL PRINT_STR, ARGS, ARGS:1, ARGS:2, ARGS:3, ARGS:4, ARGS:5, ARGS:6, ARGS:7, ARGS:8, ARGS:9
PRINTL

@PRINT_STRW, ARGS, ARGS:1, ARGS:2, ARGS:3, ARGS:4, ARGS:5, ARGS:6, ARGS:7, ARGS:8, ARGS:9
#LOCALSIZE 1
#LOCALSSIZE 1
CALL PRINT_STR, ARGS, ARGS:1, ARGS:2, ARGS:3, ARGS:4, ARGS:5, ARGS:6, ARGS:7, ARGS:8, ARGS:9
WAIT

@CLEARSTRTAGS(L_STR)
#LOCALSIZE 1
#LOCALSSIZE 1
#FUNCTIONS
#DIMS DYNAMIC L_STR
RETURNF REPLACE(L_STR, "HEX@.*?@|@L|@|BOLD", "")

;-------------------------------------------------
; 带换行、括号和缩进的角色对话打印
;-------------------------------------------------
@PRINT_DIALOGUE(L_STR, L_FLAGS)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS DYNAMIC L_STR
#DIMS DYNAMIC L_FLAGS
#DIMS DYNAMIC L_BRACKET_L
#DIMS DYNAMIC L_BRACKET_R

IF SPLIT_CHECK(TOLOWER(L_FLAGS), "p")
	L_BRACKET_L = （
	L_BRACKET_R = ）
ELSEIF SPLIT_CHECK(TOLOWER(L_FLAGS), "t")
	L_BRACKET_L = 『
	L_BRACKET_R = 』
ELSE
	L_BRACKET_L = 「
	L_BRACKET_R = 」
ENDIF

IF SPLIT_CHECK(TOLOWER(L_FLAGS), "d")
	CALL KEYTYPING(BREAKENG(L_BRACKET_L + L_STR + L_BRACKET_R, 0, "\n　", MAXWIDTH() - 19), "l", 1, 2)
ELSEIF SPLIT_CHECK(TOLOWER(L_FLAGS), "dc")
	PRINTSDL BREAKENG(L_BRACKET_L + L_STR + L_BRACKET_R, 0, "\n　", MAXWIDTH() - 19)
ELSE
	PRINTSL BREAKENG(L_BRACKET_L + L_STR + L_BRACKET_R, 0, "\n　", MAXWIDTH() - 19)
ENDIF

SIF SPLIT_CHECK(TOLOWER(L_FLAGS), "w")
	WAIT

@CHARA_TEXT(L_TGT_ID, L_STR, L_MODE = "l")
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM DYNAMIC L_TGT_ID
#DIMS DYNAMIC L_STR
#DIMS DYNAMIC L_MODE
#DIM DYNAMIC L_OLD_C

L_OLD_C = GETCOLOR()
RESETCOLOR

SIF FLAG:口上色
    CALL SET_KOJO_COLOR(L_TGT_ID)

SELECTCASE TOLOWER(L_MODE)
    CASE "l"
        PRINTFORML %L_STR%
    CASE "w"
        PRINTFORMW %L_STR%
	CASE "str"
		CALL PRINT_STR, L_STR
	CASE "strl"
		CALL PRINT_STRL, L_STR
	CASE "strw"
		CALL PRINT_STRW, L_STR
    CASEELSE
        PRINTFORM %L_STR%
ENDSELECT
SETCOLOR L_OLD_C

;==============================================================================
;输出交互
;==============================================================================
