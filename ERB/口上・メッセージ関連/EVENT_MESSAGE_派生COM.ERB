;-------------------------------------------------
;1,六九式
;-------------------------------------------------
@MESSAGE_SCOM1
IF FLAG:70 == 1
	IF SELECTCOM == 3
		PRINTFORM 用%TINKO(PLAYER)%插进时间停止的%CALLNAME:TARGET%的嘴里、同时将%CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼いワレメ # 秘裂 \@
		IF TEQUIP:振動棒
			PRINTFORML 中妖异蠕动着的うねる振動棒を前後动起来…
		ELSE
			PRINTFORML を舐めほぐした…
		ENDIF
	ELSEIF SELECTCOM == 5
		PRINTFORM 用%TINKO(PLAYER)%插进时间停止的%CALLNAME:TARGET%的嘴里、同时
		;肛用振動棒
		IF TEQUIP:肛用振動棒
			PRINTFORML 前後抽插着正微微顫動的肛用震動棒…
		;後庭拉珠
		ELSEIF TEQUIP:後庭拉珠
			PRINTFORML 前後拉動着肛門拉珠的拉環…
		;浣腸器＋肛塞
		ELSEIF TEQUIP:浣腸
			PRINTFORML 轉動肛塞玩弄著因排泄感而顫動的菊穴…
		;拡張氣球
		ELSEIF TEQUIP:拡張氣球
			PRINTFORML 前後搖動著把肛門擴張到極限的氣球…
		;肛門電極
		ELSEIF TEQUIP:肛門電極
			PRINTFORML 用正嘶嘶作響的電極在菊穴內來回抽插…
		ELSE
			PRINTFORM %CALLNAME:TARGET%的肛門被玩弄着
			;Ａ経験100以上＋欲望のPALAMLV3以上
			SIF PALAM:欲情 >= PALAMLV:3 && EXP:Ａ経験 >= EXPLV:3
				PRINT 、指尖深入直肠来回转动
		PRINTL …
		ENDIF
	ENDIF
ELSE
	IF SELECTCOM == 3
		PRINTFORM 和%CALLNAME:TARGET%互相舔舐性器的同时
		IF TEQUIP:振動棒
			PRINTFORML \@ TALENT:体型 < 0 ? 幼いワレメ # 秘裂 \@の中で妖しくうねる振動棒を前後に抜き差しした…
		ELSE
			PRINTFORML 把手指插进了陰道口…
		ENDIF
	ELSEIF SELECTCOM == 5
		PRINTFORM 和%CALLNAME:TARGET%互相舔舐性器的同时
		;肛用振動棒
		IF TEQUIP:肛用振動棒
			PRINTFORML 前後抽插着正微微顫動的肛用震動棒…
		;後庭拉珠
		ELSEIF TEQUIP:後庭拉珠
			PRINTFORML 前後拉動着肛門拉珠的拉環…
		;浣腸器＋肛塞
		ELSEIF TEQUIP:浣腸
			PRINTFORML 轉動肛塞玩弄著因排泄感而顫動的菊穴…
		;拡張氣球
		ELSEIF TEQUIP:拡張氣球
			PRINTFORML 前後搖動著把肛門擴張到極限的氣球…
		;肛門電極
		ELSEIF TEQUIP:肛門電極
			PRINTFORML 用正嘶嘶作響的電極在菊穴內來回抽插…
		ELSE
			PRINTFORM %CALLNAME:TARGET%的肛門被玩弄着
			;Ａ経験100以上＋欲望のPALAMLV3以上
			SIF PALAM:欲情 >= PALAMLV:3 && EXP:Ａ経験 >= EXPLV:3
				PRINT 、指尖深入直肠来回转动
		PRINTL …
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%と
		;助手が調教中
		SIF ASSIPLAY
			PRINTFORM %CALLNAME:ASSI%は
		PRINTL 互相舔舐着性器…
	ENDIF
ENDIF
;-------------------------------------------------
;2,岩清水
;-------------------------------------------------
@MESSAGE_SCOM2
IF FLAG:70 == 1
	PRINTFORML %CALLNAME:PLAYER%让時停中的%CALLNAME:TARGET%跨坐在自己脸上、故意发出声音吮吸起了陰唇…
ELSE
	PRINTFORML %CALLNAME:PLAYER%让%CALLNAME:TARGET%跨坐在自己脸上、故意发出声音吮吸起了陰唇…
ENDIF
;-------------------------------------------------
;3,Gスポット刺激
;-------------------------------------------------
@MESSAGE_SCOM3
IF FLAG:70 == 1
	;前回が刺激Ｇ點
	IF PREVCOM == 503
		PRINTFORML %CALLNAME:PLAYER%对%CALLNAME:TARGET%的陰道里充满颗粒感的顶部、激烈的摩擦刺激起来…
		IF EXP:Ｖ経験 > 80
			PRINTFORML %CALLNAME:TARGET%的陰道颤动着缠住手指、喷出了愛液…
		ENDIF
	;振動棒挿入中
	ELSEIF TEQUIP:振動棒
		PRINTFORML %CALLNAME:PLAYER%使用在%CALLNAME:TARGET%陰道里淫靡振動的按摩棒、激烈的刺激起充满颗粒感的陰道顶部
		;欲情のPALAMLV3以上＋Ｖ感覚LV3以上
		IF  PALAM:欲情 >= PALAMLV:3 && ABL:Ｖ感覚 >= 3
			PRINTFORML 被蜜汁浸湿的陰道、紧紧缠住了被抽动着的按摩棒…
		;欲情のPALAMLV3未満＋Ｖ感覚LV3以上
		ELSEIF PALAM:欲情 < PALAMLV:3 && ABL:Ｖ感覚 >= 3
			PRINTFORML %CALLNAME:TARGET%的陰道、在被抽动着的按摩棒所带来的快乐下颤动不已…
		;欲情のPALAMLV3以上＋Ｖ感覚LV3未満
		ELSEIF PALAM:欲情 >= PALAMLV:3 && ABL:Ｖ感覚 < 3
			PRINTFORML %CALLNAME:TARGET%的陰道、滴着蜜汁接受了按摩棒…
		;欲情のPALAMLV3未満＋Ｖ感覚LV3未満
		ELSEIF PALAM:欲情 < PALAMLV:3 && ABL:Ｖ感覚 < 3
			PRINTFORML 時停中的%CALLNAME:TARGET%被无法抵抗地蹂躏着没什么经验的陰道…
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:PLAYER%在%CALLNAME:TARGET%的陰道里弯曲手指、刺激起了充满颗粒感的顶端…
	ENDIF
ELSE
	;前回が刺激Ｇ點
	IF PREVCOM == 503
		;Ｖ経験が120より多い
		IF EXP:Ｖ経験 > 120
			PRINTFORML %CALLNAME:TARGET%流出口水、自己摇动着腰贪求着快乐…
		;Ｖ経験が80より多い
		ELSEIF EXP:Ｖ経験 > 80
			PRINTFORML %CALLNAME:TARGET%因为肚子里被来回搅动的感觉喘着热气挣扎着…
		;Ｖ経験が50より多い
		ELSEIF EXP:Ｖ経験 > 50
			PRINTFORML %CALLNAME:TARGET%因为手指蠕动的触感而神情陶醉…
		;Ｖ経験が30より多い
		ELSEIF EXP:Ｖ経験 > 30
			PRINTFORML %CALLNAME:TARGET%很享受手指的触感的様子…
		;Ｖ経験が0より多い
		ELSEIF EXP:Ｖ経験 > 0
			PRINTFORML %CALLNAME:TARGET%因为手指的触感喘着气忍耐着…
		ELSE
			PRINTFORML %CALLNAME:TARGET%对陰道里的異物感感到害怕…
		ENDIF
	;振動棒挿入中＋欲情のPALAMLV3以上＋Ｖ感覚LV3以上
	ELSEIF TEQUIP:振動棒 && PALAM:欲情 >= PALAMLV:3 && ABL:Ｖ感覚 >= 3
		PRINTFORML %CALLNAME:TARGET%的膣穴、紧紧缠住了沾满蜜汁的按摩棒…
	;振動棒挿入中＋欲情のPALAMLV3未満＋Ｖ感覚LV3以上
	ELSEIF TEQUIP:振動棒 && PALAM:欲情 < PALAMLV:3 && ABL:Ｖ感覚 >= 3
		PRINTFORML %CALLNAME:TARGET%因为按摩棒的刺激而挣扎着…
	;振動棒挿入中＋欲情のPALAMLV3以上＋Ｖ感覚LV3未満
	ELSEIF TEQUIP:振動棒 && PALAM:欲情 >= PALAMLV:3 && ABL:Ｖ感覚 < 3
		PRINTFORML %CALLNAME:TARGET%红着脸时不时呻吟起来…
	;振動棒挿入中＋欲情のPALAMLV3未満＋Ｖ感覚LV3未満
	ELSEIF TEQUIP:振動棒 && PALAM:欲情 < PALAMLV:3 && ABL:Ｖ感覚 < 3
		PRINTFORML %CALLNAME:TARGET%露出了有点难受的表情…
	;振動棒挿入中
	ELSEIF TEQUIP:振動棒
		PRINTFORML %CALLNAME:PLAYER%灵巧的操纵按摩棒、侵犯着敏感的陰道顶部…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%在%CALLNAME:TARGET%的陰道里弯曲手指、刺激起了充满颗粒感的顶端…
	ENDIF
ENDIF
;-------------------------------------------------
;4,乱牡丹
;-------------------------------------------------
@MESSAGE_SCOM4
;連続
IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
	PRINTFORML 再次把手攀上与%CALLNAME:TARGET%的結合部位和胸部、像卖弄似的动起了腰…
;抜かず
ELSEIF TCVAR:100 == PLAYER
	PRINTFORM 保持从背后插入%CALLNAME:TARGET%身体的状态并将抬起的腿大大地张開…
ELSE
	PRINTFORML 抱起%CALLNAME:TARGET%的身体、慢慢地%SLIT(, 1)%插进去、把腿大大地张開…
ENDIF
;露出癖
IF ABL:露出癖 >= 5
	PRINTFORML %CALLNAME:TARGET%媚眼如丝的看着鏡中映出的痴態发出了歓喜的呻吟声…
ELSEIF ABL:露出癖 >= 4
	PRINTFORML %CALLNAME:TARGET%用沉溺在羞恥与快楽中的表情凝視着鏡中映出的痴態…
ELSEIF ABL:露出癖 >= 2
	PRINTFORML %CALLNAME:TARGET%羞红了脸并时不时发出妩媚的呻吟声…
ELSEIF ABL:露出癖 >= 1
	PRINTFORML %CALLNAME:TARGET%用手捂住脸并因为羞恥而颤动着身体…
ENDIF
;-------------------------------------------------
;5,手淫口交
;-------------------------------------------------
@MESSAGE_SCOM5

IF TFLAG:194 == 0
	PRINTFORML %CALLNAME:TARGET%保持著含住前端的狀態并握住了%CALLNAME:PLAYER%的%TINKO(PLAYER)%、
	PRINTL 就这様激烈地捋動著肉竿部分…
	SIF ABL:技巧 > 3
		PRINTL 并用空出来的手温柔地揉弄起了陰嚢…
ELSE
	PRINTFORML %CALLNAME:TARGET%用手握住%CALLNAME:PLAYER%的%TINKO(PLAYER)%并把前端含进嘴里
	PRINTL 在捋動著肉竿部分的同时用舌头刺激起亀頭…
	SIF ABL:技巧 > 3
		PRINTL 并用空出来的手温柔地揉弄起了陰嚢…
ENDIF
;-------------------------------------------------
;8,６９乳交
;-------------------------------------------------
@MESSAGE_SCOM8
PRINTFORM %CALLNAME:TARGET%用%OPPAI_DESCRIPTION(TARGET)%将%TINKO(PLAYER)%
;貧乳
IF TALENT:胸圍 < 0
	PRINTFORML 包もうとして
;巨乳
ELSEIF TALENT:胸圍 == 1
	PRINTFORML 包み込んで
;爆乳
ELSEIF TALENT:胸圍 == 2
	PRINTFORML 包み隠すようにして
ELSE
	PRINTFORML をはさんで
ENDIF
PRINTFORML 在被%CALLNAME:PLAYER%舌头带来的快感玩弄的同时努力给予摩擦…
IF ABL:TARGET:胸 < 2
	PRINTFORM %CALLNAME:TARGET%的奉仕不太好、虽然这么讲对%CALLNAME:TARGET%不太好
	SIF TALENT:胸圍 >= 1
		PRINT 視覚上姑且不说
	PRINTL 实在不是很舒服…
ELSEIF ABL:TARGET:胸 > 2
	PRINTFORML %CALLNAME:TARGET%用巧妙的奉仕手段来取悦%CALLNAME:PLAYER%…
ENDIF
;-------------------------------------------------
;9,雙重口交
;-------------------------------------------------
@MESSAGE_SCOM9
LOCAL = MASTER_POSE(SET_M,SET_P,1)
IF FLAG:70 == 1
	PRINTFORML %CALLNAME:PLAYER%把%TINKO(PLAYER)%轮流插入并侵犯着時停中的%CALLNAME:TARGET%和%CALLNAME:LOCAL%的口腔…
ELSE
	PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%は
	;奴隷と助手が恋慕か親愛
	SIF TALENT:恋慕 && TALENT:LOCAL:恋慕
		PRINTFORM 用陶醉的眼神瞟着%CALLNAME:PLAYER%、
	PRINTFORML 两人一起用舌头舔舐着%CALLNAME:PLAYER%的%TINKO(PLAYER)%…
ENDIF
;-------------------------------------------------
;10,雙重素股
;-------------------------------------------------
@MESSAGE_SCOM10
LOCAL = MASTER_POSE(SET_C,SET_P,1)
PRINTFORML 用%CALLNAME:TARGET%和%CALLNAME:LOCAL%的性器挟住%TINKO(PLAYER)%、在兩人之間捋動起來…
;-------------------------------------------------
;11,雙重乳交
;-------------------------------------------------
@MESSAGE_SCOM11
LOCAL = MASTER_POSE(SET_B,SET_P,1)
PRINTFORM %CALLNAME:TARGET%和%CALLNAME:LOCAL%
;奴隷と助手が恋慕か親愛
SIF TALENT:恋慕 && TALENT:LOCAL:恋慕
	PRINTFORM 用陶醉的眼神瞟着%CALLNAME:PLAYER%、
PRINT 互相把
;奴隷と助手が巨乳か爆乳
IF TALENT:胸圍 > 0 && TALENT:LOCAL:胸圍 > 0
	PRINT 豊満的
;奴隷と助手が貧乳か絶壁
ELSEIF TALENT:胸圍 < 0 && TALENT:LOCAL:胸圍 < 0
	PRINT 貧瘠的
ENDIF
PRINTFORML 胸部疊在一起捋動起%CALLNAME:PLAYER%的%TINKO(PLAYER)%…

;-------------------------------------------------
;12,乳夾口交
;-------------------------------------------------
@MESSAGE_SCOM12
IF FLAG:70 == 1
	PRINTFORM %CALLNAME:MASTER%让時停中的%CALLNAME:TARGET%用嘴衔住亀頭、
	;巨乳
	IF TALENT:胸圍 == 1
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%挟住%TINKO(PLAYER)%并開始动起了腰…
	;爆乳
	ELSEIF TALENT:胸圍 == 2
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%挟住%TINKO(PLAYER)%并開始动起了腰…
	ELSE
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%挟住%TINKO(PLAYER)%并開始动起了腰…
	ENDIF
ELSE
	PRINTFORM %CALLNAME:TARGET%
	;恋慕か親愛
	SIF TALENT:恋慕
		PRINT 在露出陶醉表情的同时、
	;貧乳
	IF TALENT:胸圍 == -1
		PRINTFORML 不光用%OPPAI_DESCRIPTION(TARGET,1)%还用起舌头努力地刺激起了整个%TINKO(PLAYER)%…
	;巨乳
	ELSEIF TALENT:胸圍 == 1
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%包住%TINKO(PLAYER)%、并舔舐刺激着露出来的亀頭…
	;爆乳
	ELSEIF TALENT:胸圍 == 2
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%包裹住%TINKO(PLAYER)%、时不时把舌头伸进去、努力的舔舐着%TINKO(PLAYER)%…
	ELSE
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%夹住%TINKO(PLAYER)%、用舌头刺激着露出来的部分…
	ENDIF
	IF ABL:TARGET:胸 < 2
		PRINTFORM %CALLNAME:TARGET%的奉仕不太好、虽然这么讲对%CALLNAME:TARGET%不太好
		SIF TALENT:胸圍 >= 1
			PRINT 視覚上姑且不说
		PRINTL 实在不是很舒服…
	ELSEIF ABL:TARGET:胸 > 2
		PRINTFORML %CALLNAME:TARGET%用巧妙的奉仕手段来取悦%CALLNAME:PLAYER%…
	ENDIF
ENDIF
;-------------------------------------------------
;13,交互挿入
;-------------------------------------------------
@MESSAGE_SCOM13
LOCAL = MASTER_POSE(SET_V,SET_P,1)
IF FLAG:70 == 1
	;現在2人以上の処女を同ターンで奪うことは出来ないため、処女関連はここ一つにまとめる
	IF TALENT:処女 == 1
		PRINTFORML 在保持对%CALLNAME:LOCAL%陰道的插入的同时抱住%CALLNAME:TARGET%、把她那没尝过男人滋味的\@ TALENT:体型 < 0 ? 幼小裂缝 # 秘穴 \@也一起贯穿了…
	;連続
	ELSEIF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%
		;正常位＋後背位
		IF ((TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2) || (TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1))
			PRINT 面对面靠到一起、
		;正常位＋正常位
		ELSEIF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 1
			PRINT 仰面躺在一起、
		ELSE
			PRINT 四脚着地趴下、
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 在品尝着二人陰道触感的细微差别的同时
		PRINTL 再次轮流挿入起来…
	ELSE
		IF RAND:2 == 0
			PRINTFORM 在保持对%CALLNAME:LOCAL%陰道的插入的同时抱住%CALLNAME:TARGET%、
			IF RAND:2 == 0
				PRINTFORM その\@ PALAM:潤滑 >= PALAMLV:4? 充分湿润的# \@
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小裂缝 # 秘裂 \@も
			ELSE
				IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
					PRINT 把重合起来的
				ELSE
					PRINT 把靠在一起的
				ENDIF
				PRINT 两个
				SIF PALAM:潤滑 >= PALAMLV:4 && PALAM:LOCAL:潤滑 >= PALAMLV:4
					PRINT 充分湿润的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小裂缝 # 秘裂 \@を
			ENDIF
		ELSE
			IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
				IF RAND:2 == 0
					PRINTFORM 将%CALLNAME:TARGET%和%CALLNAME:LOCAL%面对面靠在一起、将那\@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小裂缝 # 秘穴 \@给
				ELSE
					PRINTFORM 将%CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼小裂缝 # 秘穴 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼小裂缝 # 秘穴 \@紧紧贴在一起、
				ENDIF
			ELSE
				PRINTFORM 将%CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼小裂缝 # 秘穴 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼小裂缝 # 秘穴 \@靠在一起、
			ENDIF
		ENDIF
		PRINTL 轮流贯穿了…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ELSE
	;現在2人以上の処女を同ターンで奪うことは出来ないため、処女関連はここ一つにまとめる
	IF TALENT:処女 == 1
		PRINTFORML 在保持对%CALLNAME:LOCAL%陰道的插入的同时抱住%CALLNAME:TARGET%、把她那没尝过男人滋味的\@ TALENT:体型 < 0 ? 幼小裂缝 # 秘穴 \@也一起贯穿了…
	;連続
	ELSEIF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%
		;正常位＋後背位
		IF ((TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2) || (TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1))
			PRINT 的身体紧紧贴在一起、
		;正常位＋正常位
		ELSEIF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 1
			PRINT 仰面躺在一起、
		ELSE
			PRINT 的屁股翘向这边、
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 在品尝着二人陰道触感的细微差别的同时
		PRINTL 再次轮流挿入起来…
	ELSE
		IF RAND:2 == 0
			PRINTFORM 在保持对%CALLNAME:LOCAL%阴道的插入的同时抱住%CALLNAME:TARGET%、交互貫穿着
			IF RAND:2 == 0
				PRINT 那
				SIF PALAM:潤滑 >= PALAMLV:4
					PRINT 充分湿润的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小裂縫 # 秘裂 \@
			ELSE
				IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
					PRINT 疊在一起
				ELSE
					PRINT 並列著的
				ENDIF
				PRINT 兩個
				SIF PALAM:潤滑 >= PALAMLV:4 && PALAM:LOCAL:潤滑 >= PALAMLV:4
					PRINT 充分湿润的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小裂縫 # 秘裂 \@
			ENDIF
		ELSE
			IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
				IF RAND:2 == 0
					PRINTFORM 讓%CALLNAME:TARGET%和%CALLNAME:LOCAL%抱在一起、交互貫穿着那\@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小裂縫 # 秘裂 \@
				ELSE
					PRINTFORM 讓%CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼小裂縫 # 秘裂 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼小裂縫 # 秘裂 \@緊貼在一起、交互貫穿着
				ENDIF
			ELSE
				PRINTFORM 讓%CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼小裂縫 # 秘裂 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼小裂縫 # 秘裂 \@排成一列、交互貫穿着
			ENDIF
		ENDIF
		PRINTL …
	ENDIF

	IF TALENT:恋慕 && TALENT:LOCAL:恋慕
		WAIT
		PRINTFORML %CALLNAME:TARGET%和%CALLNAME:LOCAL%在被轮流插入的同时、央求着%CALLNAME:MASTER%的%TINKO(PLAYER)%…
	ELSEIF EXP:百合経験 >= 1000 && EXP:LOCAL:百合経験 >= 1000
		WAIT
		PRINTFORML %CALLNAME:TARGET%和%CALLNAME:LOCAL%在互相接吻的同时、承受着%CALLNAME:MASTER%的%TINKO(PLAYER)%的插入…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ENDIF


;-------------------------------------------------
;16,肛門交互挿入
;-------------------------------------------------
@MESSAGE_SCOM16
LOCAL = MASTER_POSE(SET_A,SET_P,1)
IF FLAG:70 == 1	;連続
	IF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%
		;正常位＋後背位
		IF ((TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2) || (TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1))
			PRINT 面对面靠到一起、
		;正常位＋正常位
		ELSEIF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 1
			PRINT 仰面躺在一起、
		ELSE
			PRINT 四脚着地趴下、
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 二人の腸内の感触の違いを味わいながら
		PRINTL 再次轮流挿入起来…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%の腸内に挿入したまま%CALLNAME:TARGET%を抱き寄せ、
			IF RAND:2 == 0
				PRINT その
				SIF PALAM:潤滑 >= PALAMLV:4
					PRINT ヌルついた
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼い窄まり # 肛門 \@も
			ELSE
				IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
					PRINT 竖着靠在一起的
				ELSE
					PRINT 横着靠在一起的
				ENDIF
				PRINT 两个
				SIF PALAM:潤滑 >= PALAMLV:4 && PALAM:LOCAL:潤滑 >= PALAMLV:4
					PRINT ヌルついた
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼い窄まり # 肛門 \@を
			ENDIF
		ELSE
			IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
				IF RAND:2 == 0
					PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%を向かい合わせに重ね、その\@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼い窄まり # 肛門 \@を
				ELSE
					PRINTFORM %CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼い窄まり # 肛門 \@と%CALLNAME:LOCAL%の\@ TALENT:LOCAL:体型 < 0 ? 幼い窄まり # 肛門 \@を縦に並べさせると、
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼い窄まり # 肛門 \@と%CALLNAME:LOCAL%の\@ TALENT:LOCAL:体型 < 0 ? 幼い窄まり # 肛門 \@を横に並べ、
			ENDIF
		ENDIF
		PRINTL 轮流贯穿了…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ELSE	;連続
	IF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%
		;正常位＋後背位
		IF ((TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2) || (TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1))
			PRINT 的身体紧紧贴在一起、
		;正常位＋正常位
		ELSEIF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 1
			PRINT 仰面躺在一起、
		ELSE
			PRINT 的屁股翘向这边、
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 二人の腸内の感触の違いを味わいながら
		PRINTL 再次轮流挿入起来…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%の腸内に挿入したまま%CALLNAME:TARGET%を抱き寄せ、
			IF RAND:2 == 0
				PRINT その
				SIF PALAM:潤滑 >= PALAMLV:4
					PRINT ヌルついた
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼い窄まり # 肛門 \@も
			ELSE
				IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
					PRINT 竖着靠在一起的
				ELSE
					PRINT 横着靠在一起的
				ENDIF
				PRINT 两个
				SIF PALAM:潤滑 >= PALAMLV:4 && PALAM:LOCAL:潤滑 >= PALAMLV:4
					PRINT ヌルついた
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼い窄まり # 肛門 \@を
			ENDIF
		ELSE
			IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
				IF RAND:2 == 0
					PRINTFORM %CALLNAME:TARGET%と%CALLNAME:LOCAL%を抱き合わせて、その\@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼い窄まり # 肛門 \@を
				ELSE
					PRINTFORM %CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼い窄まり # 肛門 \@と%CALLNAME:LOCAL%の\@ TALENT:LOCAL:体型 < 0 ? 幼い窄まり # 肛門 \@を縦に並べさせると、
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼い窄まり # 肛門 \@と%CALLNAME:LOCAL%の\@ TALENT:LOCAL:体型 < 0 ? 幼い窄まり # 肛門 \@を横に並べ、
			ENDIF
		ENDIF
		PRINTL 轮流贯穿了…
	ENDIF

	IF TALENT:恋慕 && TALENT:LOCAL:恋慕
		WAIT
		PRINTFORML %CALLNAME:TARGET%和%CALLNAME:LOCAL%在被轮流插入的同时、央求着%CALLNAME:MASTER%的%TINKO(PLAYER)%…
	ELSEIF EXP:百合経験 >= 1000 && EXP:LOCAL:百合経験 >= 1000
		WAIT
		PRINTFORML %CALLNAME:TARGET%和%CALLNAME:LOCAL%在互相接吻的同时、承受着%CALLNAME:MASTER%的%TINKO(PLAYER)%的插入…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ENDIF

;-------------------------------------------------
;17,同時吸乳头
;-------------------------------------------------
@MESSAGE_SCOM17
SIF FLAG:70 == 1
	PRINTFORM 在停止的時間中的
IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
	PRINTFORM %CALLNAME:TARGET%\@FLAG:70 ?を犯したまま #と身體連接著 \@
ELSE
	PRINTFORM %CALLNAME:TARGET%的
ENDIF
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL,17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:TARGET:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL,EQUIP:TARGET:LOCAL)%的外面将
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:母乳体質 && RAND:3 == 0
	PRINTFORM 滲著%TEXTR("雌乳/母乳/奶汁")%的
ENDIF
IF TALENT:TARGET:胸圍 >= 1
	PRINTFORML %OPPAI_DESCRIPTION(TARGET)%挤在一起、
	PRINTFORML 两个%NIPPLE_DESCRIPTION(TARGET,1)%%TEXTR("を同時に口に含んで甘噛みしつつ弄びながら吸引した/に同時にむしゃぶり付き、厭らしい音を立てながら吸いあげた/を同時に口に含みつつ、乳輪の周りを挑逗ように舐め吸いあげた/を同時に口に含み、口内で擦り合わせて刺激した")%……
ELSE
	PRINTFORML %OPPAI_DESCRIPTION(TARGET)%の%TEXTR("先の、/頂に在る、/")%
	PRINTFORML %NIPPLE_DESCRIPTION(TARGET,1)%%TEXTR("を交互に口に含んで甘噛みしつつ弄びながら吸引した/に交互にむしゃぶり付き、厭らしい音を立てながら吸いあげた/を交互に口に含みつつ、乳輪の周りを挑逗ように舐め吸いあげた")%……
ENDIF
IF ABL:TARGET:親密 >= 10 && !FLAG:70
	SELECTCASE ABL:TARGET:Ｂ感覚 
	CASE 0,1
		PRINTFORM %CALLNAME:TARGET%一边苦笑、一边温柔地抚摸着正吸着%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的頭
	CASE 2 TO 5
		PRINTFORM %CALLNAME:TARGET%一边忍受快感、一边温柔地抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的頭
	CASE 6 TO 9
		PRINTFORM %CALLNAME:TARGET%因为快感而娇喘起来的同时、用力抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的頭
	CASEELSE
		PRINTFORML %CALLNAME:TARGET%露出按捺不住快感的様子紧紧抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%、
		PRINTFORML 比起愛撫更想要对乳头的强烈刺激
	ENDSELECT
ENDIF

;-------------------------------------------------
;18,後背位打屁股
;-------------------------------------------------
@MESSAGE_SCOM18
PRINTFORM %CALLNAME:PLAYER%一边用腰身撞击着%CALLNAME:TARGET%、一边
SIF TEQUIP:0 & 1 && (EQUIP:裙子 || EQUIP:下身衣服 || EQUIP:連衣裙 || EQUIP:和服)
	PRINTFORM 掀起%SHOW_BOTTOM(TARGET,1)%
PRINTFORM 使劲拍打起
SIF EQUIP:TARGET:緊身内衣 || EQUIP:TARGET:連体内衣
	PRINTFORM 被%SHOW_下半身内衣(TARGET,2)%包住的
PRINTFORML %HIP(TARGET)%发出了响亮的声音

;-------------------------------------------------
;19,授乳PLAY
;-------------------------------------------------
@MESSAGE_SCOM19
PRINTFORM %CALLNAME:TARGET%%CALLNAME:MASTER%を胸に抱き寄せ、
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL,17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:TARGET:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL,EQUIP:TARGET:LOCAL)%的外面将
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:母乳体質 && RAND:3 == 0
	PRINTFORM 滲著%TEXTR("雌乳/母乳/奶汁")%的
ENDIF
SIF TALENT:TARGET:胸圍 >= 1
	PRINTFORM %OPPAI_DESCRIPTION(TARGET)%を持ち上げて、
PRINTFORML %NIPPLE_DESCRIPTION(TARGET,1)%を%CALLNAME:MASTER%の口に含ませたまま%TINKO(PLAYER)%を扱き始めた
PRINTFORM %CALLNAME:MASTER%は%CALLNAME:TARGET%に身を任せ、
SELECTCASE RAND:4
CASE 0
	PRINTFORML %NIPPLE_DESCRIPTION(TARGET)%を甘噛みしつつ舌で弄びながら吸引した……
CASE 1
	PRINTFORML %NIPPLE_DESCRIPTION(TARGET)%にむしゃぶり付き、厭らしい音を立てながら吸いあげた……
CASE 2
	PRINTFORML 乳輪の周りを挑逗ように舐めてから、%NIPPLE_DESCRIPTION(TARGET)%を吸いあげた……
CASEELSE
	PRINTFORML %NIPPLE_DESCRIPTION(TARGET)%を舌の先で転がしてから、赤子のように吸いあげた……
ENDSELECT
IF ABL:TARGET:親密 >= 10 && !FLAG:70
	SELECTCASE ABL:TARGET:Ｂ感覚 
	CASE 0,1
		PRINTFORM %CALLNAME:TARGET%一边苦笑、一边温柔地抚摸着正吸着%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的頭
	CASE 2 TO 5
		PRINTFORM %CALLNAME:TARGET%一边忍受快感、一边温柔地抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的頭
	CASE 6 TO 9
		PRINTFORM %CALLNAME:TARGET%因为快感而娇喘起来的同时、用力抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的頭
	CASEELSE
		PRINTFORML %CALLNAME:TARGET%は快感に耐え切れない様子で%OPPAI_DESCRIPTION(TARGET)%に吸い付く%CALLNAME:MASTER%の頭を掻き抱き、
		PRINTFORML より強い乳頭への刺激を求めてきた
	ENDSELECT
ENDIF