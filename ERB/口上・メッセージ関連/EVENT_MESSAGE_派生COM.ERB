;-------------------------------------------------
;1,六九式
;-------------------------------------------------
@MESSAGE_SCOM1
IF (FLAG:70 && CFLAG:TARGET:344 == 0)
	IF SELECTCOM == 3
		PRINTFORM 用%TINKO(PLAYER)%插进时间停止的%CALLNAME:TARGET%的嘴里、同时将%CALLNAME:TARGET%\@ TALENT:体型 < 0 ? 嫩穴 # 蜜穴 \@
		IF TEQUIP:振動棒
			PRINTFORML 中蠕动的振动棒抽插起来…
		ELSE
			PRINTFORML 舔舐起来…
		ENDIF
	ELSEIF SELECTCOM == 5
		PRINTFORM 用%TINKO(PLAYER)%插进时间停止的%CALLNAME:TARGET%的嘴里、同时
		;肛用振动棒
		IF TEQUIP:肛用振動棒
			PRINTFORML 前后抽插着正微微颤动的肛用震动棒…
		;后庭拉珠
		ELSEIF TEQUIP:後庭拉珠
			PRINTFORML 前后拉动着肛门拉珠的拉环…
		;浣肠器＋肛塞
		ELSEIF TEQUIP:浣腸
			PRINTFORML 转动肛塞玩弄着因排泄感而颤动的菊穴…
		;扩张气球
		ELSEIF TEQUIP:拡張氣球
			PRINTFORML 前后摇动着把肛门扩张到极限的气球…
		;肛门电极
		ELSEIF TEQUIP:肛門電極
			PRINTFORML 用正嘶嘶作响的电极在菊穴内来回抽插…
		ELSE
			PRINTFORM %CALLNAME:TARGET%的肛门被玩弄着
			;Ａ经验100以上＋欲望のPALAMLV3以上
			SIF PALAM:欲情 >= PALAMLV:3 && EXP:Ａ経験 >= EXPLV:3
				PRINT 、指尖深入直肠来回转动
		PRINTL …
		ENDIF
	ENDIF
ELSE
	IF SELECTCOM == 3
		PRINTFORM 和%CALLNAME:TARGET%互相舔舐性器的同时
		IF TEQUIP:振動棒
			PRINTFORML 将\@ TALENT:体型 < 0 ? 嫩穴 # 蜜穴 \@中蠕动的振动棒抽插起来…
		ELSE
			PRINTFORML 把手指插进了阴道口…
		ENDIF
	ELSEIF SELECTCOM == 5
		PRINTFORM 和%CALLNAME:TARGET%互相舔舐性器的同时
		;肛用振动棒
		IF TEQUIP:肛用振動棒
			PRINTFORML 前后抽插着正微微颤动的肛用震动棒…
		;后庭拉珠
		ELSEIF TEQUIP:後庭拉珠
			PRINTFORML 前后拉动着肛门拉珠的拉环…
		;浣肠器＋肛塞
		ELSEIF TEQUIP:浣腸
			PRINTFORML 转动肛塞玩弄着因排泄感而颤动的菊穴…
		;扩张气球
		ELSEIF TEQUIP:拡張氣球
			PRINTFORML 前后摇动着把肛门扩张到极限的气球…
		;肛门电极
		ELSEIF TEQUIP:肛門電極
			PRINTFORML 用正嘶嘶作响的电极在菊穴内来回抽插…
		ELSE
			PRINTFORM %CALLNAME:TARGET%的肛门被玩弄着
			;Ａ经验100以上＋欲望のPALAMLV3以上
			SIF PALAM:欲情 >= PALAMLV:3 && EXP:Ａ経験 >= EXPLV:3
				PRINT 、指尖深入直肠来回转动
		PRINTL …
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%和
		;助手が调教中
		SIF ASSIPLAY
			PRINTFORM %CALLNAME:ASSI%
		PRINTL 互相舔舐着性器…
	ENDIF
ENDIF
;-------------------------------------------------
;2,岩清水
;-------------------------------------------------
@MESSAGE_SCOM2
IF (FLAG:70 == 1 && CFLAG:TARGET:344 == 0)
	PRINTFORML %CALLNAME:PLAYER%让时停中的%CALLNAME:TARGET%跨坐在自己脸上、故意发出声音吮吸起了阴唇…
ELSE
	PRINTFORML %CALLNAME:PLAYER%让%CALLNAME:TARGET%跨坐在自己脸上、故意发出声音吮吸起了阴唇…
ENDIF
;-------------------------------------------------
;3,G点刺激
;-------------------------------------------------
@MESSAGE_SCOM3
IF (FLAG:70 && CFLAG:TARGET:344 == 0)
	;前回が刺激Ｇ点
	IF PREVCOM == 72
		PRINTFORML %CALLNAME:PLAYER%对%CALLNAME:TARGET%的阴道里充满颗粒感的顶部、激烈的摩擦刺激起来…
		IF EXP:Ｖ経験 > 80
			PRINTFORML %CALLNAME:TARGET%的阴道颤动着缠住手指、喷出了爱液…
		ENDIF
	;振动棒插入中
	ELSEIF TEQUIP:振動棒
		PRINTFORML %CALLNAME:PLAYER%使用在%CALLNAME:TARGET%阴道里淫靡振动的按摩棒、激烈的刺激起充满颗粒感的阴道顶部
		;情欲のPALAMLV3以上＋Ｖ感觉LV3以上
		IF  PALAM:欲情 >= PALAMLV:3 && ABL:Ｖ感覚 >= 3
			PRINTFORML 被蜜汁浸湿的阴道、紧紧缠住了被抽动着的按摩棒…
		;情欲のPALAMLV3未满＋Ｖ感觉LV3以上
		ELSEIF PALAM:欲情 < PALAMLV:3 && ABL:Ｖ感覚 >= 3
			PRINTFORML %CALLNAME:TARGET%的阴道、在被抽动着的按摩棒所带来的快乐下颤动不已…
		;情欲のPALAMLV3以上＋Ｖ感觉LV3未满
		ELSEIF PALAM:欲情 >= PALAMLV:3 && ABL:Ｖ感覚 < 3
			PRINTFORML %CALLNAME:TARGET%的阴道、滴着蜜汁接受了按摩棒…
		;情欲のPALAMLV3未满＋Ｖ感觉LV3未满
		ELSEIF PALAM:欲情 < PALAMLV:3 && ABL:Ｖ感覚 < 3
			PRINTFORML 时停中的%CALLNAME:TARGET%被无法抵抗地蹂躏着没什么经验的阴道…
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:PLAYER%在%CALLNAME:TARGET%的阴道里弯曲手指、刺激起了充满颗粒感的顶端…
	ENDIF
ELSE
	;前回が刺激Ｇ点
	IF PREVCOM == 72
		;Ｖ经验が120より多い
		IF EXP:Ｖ経験 > 120
			PRINTFORML %CALLNAME:TARGET%流出口水、自己摇动着腰贪求着快乐…
		;Ｖ经验が80より多い
		ELSEIF EXP:Ｖ経験 > 80
			PRINTFORML %CALLNAME:TARGET%因为肚子里被来回搅动的感觉喘着热气挣扎着…
		;Ｖ经验が50より多い
		ELSEIF EXP:Ｖ経験 > 50
			PRINTFORML %CALLNAME:TARGET%因为手指蠕动的触感而神情陶醉…
		;Ｖ经验が30より多い
		ELSEIF EXP:Ｖ経験 > 30
			PRINTFORML %CALLNAME:TARGET%很享受手指的触感的样子…
		;Ｖ经验が0より多い
		ELSEIF EXP:Ｖ経験 > 0
			PRINTFORML %CALLNAME:TARGET%因为手指的触感喘着气忍耐着…
		ELSE
			PRINTFORML %CALLNAME:TARGET%对阴道里的异物感感到害怕…
		ENDIF
	;振动棒插入中＋情欲のPALAMLV3以上＋Ｖ感觉LV3以上
	ELSEIF TEQUIP:振動棒 && PALAM:欲情 >= PALAMLV:3 && ABL:Ｖ感覚 >= 3
		PRINTFORML %CALLNAME:TARGET%的膣穴、紧紧缠住了沾满蜜汁的按摩棒…
	;振动棒插入中＋情欲のPALAMLV3未满＋Ｖ感觉LV3以上
	ELSEIF TEQUIP:振動棒 && PALAM:欲情 < PALAMLV:3 && ABL:Ｖ感覚 >= 3
		PRINTFORML %CALLNAME:TARGET%因为按摩棒的刺激而挣扎着…
	;振动棒插入中＋情欲のPALAMLV3以上＋Ｖ感觉LV3未满
	ELSEIF TEQUIP:振動棒 && PALAM:欲情 >= PALAMLV:3 && ABL:Ｖ感覚 < 3
		PRINTFORML %CALLNAME:TARGET%红着脸时不时呻吟起来…
	;振动棒插入中＋情欲のPALAMLV3未满＋Ｖ感觉LV3未满
	ELSEIF TEQUIP:振動棒 && PALAM:欲情 < PALAMLV:3 && ABL:Ｖ感覚 < 3
		PRINTFORML %CALLNAME:TARGET%露出了有点难受的表情…
	;振动棒插入中
	ELSEIF TEQUIP:振動棒
		PRINTFORML %CALLNAME:PLAYER%灵巧的操纵按摩棒、侵犯着敏感的阴道顶部…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%在%CALLNAME:TARGET%的阴道里弯曲手指、刺激起了充满颗粒感的顶端…
	ENDIF
ENDIF
;-------------------------------------------------
;4,乱牡丹
;-------------------------------------------------
@MESSAGE_SCOM4
;连续
IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
	PRINTFORML 再次把手攀上与%CALLNAME:TARGET%的结合部位和胸部、像卖弄似的动起了腰…
;抜かず
ELSEIF TCVAR:100 == PLAYER
	PRINTFORM 保持从背后插入%CALLNAME:TARGET%身体的状态并将抬起的腿大大地张开…
ELSE
	PRINTFORML 抱起%CALLNAME:TARGET%的身体、把腿大大地张开、慢慢地向%SLIT(, 1)%插进去…
ENDIF
;露出癖
IF ABL:露出癖 >= 5
	PRINTFORML %CALLNAME:TARGET%媚眼如丝的看着镜中映出的痴态发出了欢喜的呻吟声…
ELSEIF ABL:露出癖 >= 4
	PRINTFORML %CALLNAME:TARGET%用沉溺在羞耻与快乐中的表情凝视着镜中映出的痴态…
ELSEIF ABL:露出癖 >= 2
	PRINTFORML %CALLNAME:TARGET%羞红了脸并时不时发出妩媚的呻吟声…
ELSEIF ABL:露出癖 >= 1
	PRINTFORML %CALLNAME:TARGET%用手捂住脸并因为羞耻而颤动着身体…
ENDIF
;-------------------------------------------------
;5,手淫口交
;-------------------------------------------------
@MESSAGE_SCOM5

IF TFLAG:194 == 0
	PRINTFORML %CALLNAME:TARGET%保持着含住前端的状态并握住了%CALLNAME:PLAYER%的%TINKO(PLAYER)%、
	PRINTL 就这样激烈地撸弄着肉竿部分…
	SIF ABL:技巧 > 3
		PRINTL 并用空出来的手温柔地揉弄起了阴嚢…
ELSE
	PRINTFORML %CALLNAME:TARGET%用手握住%CALLNAME:PLAYER%的%TINKO(PLAYER)%并把前端含进嘴里
	PRINTL 在撸弄着肉竿部分的同时用舌头刺激起龟头…
	SIF ABL:技巧 > 3
		PRINTL 并用空出来的手温柔地揉弄起了阴嚢…
ENDIF
;-------------------------------------------------
;8,６９乳交
;-------------------------------------------------
@MESSAGE_SCOM8
PRINTFORM %CALLNAME:TARGET%用%OPPAI_DESCRIPTION(TARGET)%将%TINKO(PLAYER)%
;贫乳
IF TALENT:胸围 < 0
	PRINTFORML 试图包住
;巨乳
ELSEIF TALENT:胸围 == 1
	PRINTFORML 包住
;爆乳
ELSEIF TALENT:胸围 == 2
	PRINTFORML 裹了起来
ELSE
	PRINTFORML 紧紧夹住
ENDIF
PRINTFORML 在被%CALLNAME:PLAYER%舌头带来的快感玩弄的同时努力给予摩擦…
IF ABL:TARGET:胸 < 2
	PRINTFORM %CALLNAME:TARGET%的侍奉不太好、虽然这么讲对%CALLNAME:TARGET%不太好
	SIF TALENT:胸围 >= 1
		PRINT 视觉上姑且不说
	PRINTL 实在不是很舒服…
ELSEIF ABL:TARGET:胸 > 2
	PRINTFORML %CALLNAME:TARGET%用巧妙的侍奉手段来取悦%CALLNAME:PLAYER%…
ENDIF
;-------------------------------------------------
;9,双重口交
;-------------------------------------------------
@MESSAGE_SCOM9
LOCAL = MASTER_POSE(SET_M,SET_P,1)
IF (FLAG:70 && CFLAG:TARGET:344 == 0 && CFLAG:LOCAL:344 == 0)

	;1011追加
	PRINTFORM %CALLNAME:PLAYER%让
	IF RELATION:TARGET:LOCAL >= 200 || RELATION:LOCAL:TARGET >= 200 
		PRINTFORM 感情深厚的
	ELSEIF RELATION:TARGET:LOCAL >= 150 || RELATION:LOCAL:TARGET >= 150 
		PRINTFORM 互为挚友的
	ELSEIF RELATION:TARGET:LOCAL >= 100 || RELATION:LOCAL:TARGET >= 100 
		PRINTFORM 关系友好的
	ELSEIF (RELATION:TARGET:LOCAL >= 50  || RELATION:LOCAL:TARGET >= 50) && (RELATION:TARGET:LOCAL < 70  || RELATION:LOCAL:TARGET < 70) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
		PRINTFORM 旧怨难消的
	ELSEIF (RELATION:TARGET:LOCAL >= 70  || RELATION:LOCAL:TARGET >= 70) && (RELATION:TARGET:LOCAL < 100  || RELATION:LOCAL:TARGET < 100) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
		PRINTFORM 稍有嫌隙的
	ELSEIF RELATION:TARGET:LOCAL == 100 || RELATION:LOCAL:TARGET == 100 
		PRINTFORM 尽释前嫌的
	ENDIF	
	PRINTFORM %CALLNAME:TARGET%和%CALLNAME:LOCAL%并膝跪在一起，
	PRINTFORMW 使%CALLNAME:TARGET%的嘴靠近正在被侵犯的%CALLNAME:LOCAL%的双唇，暂时拔出%TINKO(PLAYER)%置于两嘴之间
	PRINTFORML
	;1011追加

	PRINTFORML %CALLNAME:PLAYER%把%TINKO(PLAYER)%轮流插入并侵犯着时停中的%CALLNAME:TARGET%和%CALLNAME:LOCAL%的口腔…
	
	;1011追加
	PRINTFORMW
	PRINTFORM 同时享受这对
	IF RELATION:TARGET:LOCAL >= 200 || RELATION:LOCAL:TARGET >= 200 
		PRINTFORM 姊妹的
	ELSEIF RELATION:TARGET:LOCAL >= 150 || RELATION:LOCAL:TARGET >= 150 
		PRINTFORM 挚友的
	ELSEIF (RELATION:TARGET:LOCAL >= 50  || RELATION:LOCAL:TARGET >= 50) && (RELATION:TARGET:LOCAL < 70  || RELATION:LOCAL:TARGET < 70) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
		PRINTFORM 冤家的
	ENDIF
	PRINTFORM 嘴唇，
	IF NOWEX:MASTER:射精 && TCVAR:PLAYER:避孕套 != 1
		PRINTFORMW %CALLNAME:PLAYER%控制不住射精的欲望，把\@ NOWEX:MASTER:射精 >1 ? 大量 # \@的%SAMEN_DESCRIPTION(PLAYER)%播撒在%CALLNAME:TARGET%和%CALLNAME:LOCAL%的嘴里
		SETCOLOR 119,136,153
		PRINTFORML 冥冥之中，%CALLNAME:TARGET%和%CALLNAME:LOCAL%的关系稍微变好了
		IF RELATION:TARGET:LOCAL != 0
			RELATION:TARGET:LOCAL += 1
		ELSE
			RELATION:TARGET:LOCAL = 101
		ENDIF
		IF RELATION:LOCAL:TARGET != 0
			RELATION:LOCAL:TARGET += 1
		ELSE
			RELATION:LOCAL:TARGET = 101
		ENDIF
		RESETCOLOR
	ELSEIF NOWEX:MASTER:射精 && TCVAR:PLAYER:避孕套 == 1
		PRINTFORMW %CALLNAME:PLAYER%控制不住射精的欲望，\@ NOWEX:MASTER:射精 >1 ? 大量 # \@的%SAMEN_DESCRIPTION(PLAYER)%把避孕套装满了
		PRINTFORML 只可惜避孕套里的%SAMEN_DESCRIPTION(PLAYER)%不能让%CALLNAME:TARGET%和%CALLNAME:LOCAL%同时品尝
	ELSE
		PRINTFORMW %CALLNAME:PLAYER%得到了不止于两倍的满足感
	ENDIF
	;1011追加
	
ELSE
	PRINTFORM %CALLNAME:TARGET%和%CALLNAME:LOCAL%
	;奴隷と助手が恋慕か亲爱
	SIF TALENT:恋慕 && TALENT:LOCAL:恋慕
		PRINTFORM 用陶醉的眼神瞟着%CALLNAME:PLAYER%、
	PRINTFORML 两人一起用舌头舔舐着%CALLNAME:PLAYER%的%TINKO(PLAYER)%…
ENDIF
;-------------------------------------------------
;10,双重素股
;-------------------------------------------------
@MESSAGE_SCOM10
LOCAL = MASTER_POSE(SET_C,SET_P,1)
PRINTFORML 用%CALLNAME:TARGET%和%CALLNAME:LOCAL%的性器挟住%TINKO(PLAYER)%、在两人之间撸弄起来…
;-------------------------------------------------
;11,双重乳交
;-------------------------------------------------
@MESSAGE_SCOM11
LOCAL = MASTER_POSE(SET_B,SET_P,1)
PRINTFORM %CALLNAME:TARGET%和%CALLNAME:LOCAL%
;奴隷と助手が恋慕か亲爱
SIF TALENT:恋慕 && TALENT:LOCAL:恋慕
	PRINTFORM 用陶醉的眼神瞟着%CALLNAME:PLAYER%、
PRINT 互相把
;奴隷と助手が巨乳か爆乳
IF TALENT:胸围 > 0 && TALENT:LOCAL:胸围 > 0
	PRINT 丰满的
;奴隷と助手が贫乳か绝壁
ELSEIF TALENT:胸围 < 0 && TALENT:LOCAL:胸围 < 0
	PRINT 贫瘠的
ENDIF
PRINTFORML 胸部叠在一起撸弄起%CALLNAME:PLAYER%的%TINKO(PLAYER)%…

;-------------------------------------------------
;12,乳夹口交
;-------------------------------------------------
@MESSAGE_SCOM12
IF (FLAG:70 && CFLAG:TARGET:344 == 0)
	PRINTFORM %CALLNAME:MASTER%让时停中的%CALLNAME:TARGET%用嘴衔住龟头、
	;巨乳
	IF TALENT:胸围 == 1
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%挟住%TINKO(PLAYER)%并开始动起了腰…
	;爆乳
	ELSEIF TALENT:胸围 == 2
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%挟住%TINKO(PLAYER)%并开始动起了腰…
	ELSE
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%挟住%TINKO(PLAYER)%并开始动起了腰…
	ENDIF
ELSE
	PRINTFORM %CALLNAME:TARGET%
	;恋慕か亲爱
	SIF TALENT:恋慕
		PRINT 在露出陶醉表情的同时、
	;贫乳
	IF TALENT:胸围 == -1
		PRINTFORML 不光用%OPPAI_DESCRIPTION(TARGET,1)%还用起舌头努力地刺激起了整个%TINKO(PLAYER)%…
	;巨乳
	ELSEIF TALENT:胸围 == 1
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%包住%TINKO(PLAYER)%、并舔舐刺激着露出来的龟头…
	;爆乳
	ELSEIF TALENT:胸围 == 2
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%包裹住%TINKO(PLAYER)%、时不时把舌头伸进去、努力的舔舐着%TINKO(PLAYER)%…
	ELSE
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%夹住%TINKO(PLAYER)%、用舌头刺激着露出来的部分…
	ENDIF
	IF ABL:TARGET:胸 < 2
		PRINTFORM %CALLNAME:TARGET%的侍奉不太好、虽然这么讲对%CALLNAME:TARGET%不太好
		SIF TALENT:胸围 >= 1
			PRINT 视觉上姑且不说
		PRINTL 实在不是很舒服…
	ELSEIF ABL:TARGET:胸 > 2
		PRINTFORML %CALLNAME:TARGET%用巧妙的侍奉手段来取悦%CALLNAME:PLAYER%…
	ENDIF
ENDIF
;-------------------------------------------------
;13,交互插入
;-------------------------------------------------
@MESSAGE_SCOM13
LOCAL = MASTER_POSE(SET_V,SET_P,1)
IF (FLAG:70 && CFLAG:TARGET:344 == 0 && CFLAG:LOCAL:344 == 0)
	;现在2人以上の处女を同ターンで夺うことは出来ないため、处女关联はここ一つにまとめる
	IF TALENT:処女 == 1
		PRINTFORML 在保持对%CALLNAME:LOCAL%阴道的插入的同时抱住%CALLNAME:TARGET%、把她那没尝过男人滋味的\@ TALENT:体型 < 0 ? 幼穴 # 蜜穴 \@也一起贯穿了…

		;1011追加
		PRINTFORML 
		PRINTFORM 沾染着
		IF RELATION:TARGET:LOCAL >= 200 || RELATION:LOCAL:TARGET >= 200 
			PRINTFORM 姊妹的
		ELSEIF RELATION:TARGET:LOCAL >= 150 || RELATION:LOCAL:TARGET >= 150 
			PRINTFORM 挚友的
		ELSEIF (RELATION:TARGET:LOCAL >= 50  || RELATION:LOCAL:TARGET >= 50) && (RELATION:TARGET:LOCAL < 70  || RELATION:LOCAL:TARGET < 70) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
			PRINTFORM 冤家的
		ELSE
			PRINTFORM %CALLNAME:LOCAL%的
		ENDIF		
		IF EX:LOCAL:処女喪失记号 == 1
			PRINTFORM 处女血的
		ELSE
			PRINTFORM 爱液的
		ENDIF
		PRINTFORMW %TINKO(PLAYER)%夺走了%CALLNAME:TARGET%的纯洁，这一事实令%CALLNAME:PLAYER%无比激动
		PRINTFORML
		PRINTFORM 与%CALLNAME:LOCAL%
		IF RELATION:TARGET:LOCAL >= 200  
			PRINTFORM 感情深厚的%CALLNAME:TARGET%或许也能因此接受这次唐突的破处吧？
		ELSEIF RELATION:TARGET:LOCAL >= 150 
			PRINTFORM 视为挚友的%CALLNAME:TARGET%当然不会在意这超友谊的初体验吧？
		ELSEIF RELATION:TARGET:LOCAL >= 100  
			PRINTFORM 关系友好的%CALLNAME:TARGET%会不会因此而尝试更进一步的关系呢？
		ELSEIF RELATION:TARGET:LOCAL >= 50 && RELATION:TARGET:LOCAL < 70  
			PRINTFORM 旧怨难消的%CALLNAME:TARGET%以此为契机冰释前嫌不是很美妙吗？
		ELSEIF RELATION:TARGET:LOCAL >= 70 && RELATION:TARGET:LOCAL < 100 
			PRINTFORM 稍有嫌隙的%CALLNAME:TARGET%不如从此转变态度吧？
		ELSEIF RELATION:TARGET:LOCAL == 100 
			PRINTFORM 尽释前嫌的%CALLNAME:TARGET%当然也会原谅这次事故咯？
		ELSE 
			PRINTFORM 并无深交的%CALLNAME:TARGET%也算是得到了拓展社交面的契机吧？
		ENDIF
		PRINTFORMW %CALLNAME:PLAYER%不切实际地妄想着
		SETCOLOR 119,136,153
		PRINTFORML 冥冥之中，%CALLNAME:TARGET%对%CALLNAME:LOCAL%的态度更友好了
		IF RELATION:TARGET:LOCAL != 0
			RELATION:TARGET:LOCAL += 10
		ELSE
			RELATION:TARGET:LOCAL = 110
		ENDIF
		RESETCOLOR
		;1011追加

	;连续
	ELSEIF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%和%CALLNAME:LOCAL%
		;正常位＋后背位
		IF ((TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2) || (TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1))
			PRINT 面对面靠到一起、
		;正常位＋正常位
		ELSEIF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 1
			PRINT 仰面躺在一起、
		ELSE
			PRINT 四脚着地趴下、
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 在品尝着二人阴道触感的细微差别的同时
		PRINTL 再次轮流插入起来…

		;1011追加
		PRINTFORML
		PRINTFORM 反复品尝这对
		IF RELATION:TARGET:LOCAL >= 200 || RELATION:LOCAL:TARGET >= 200 
			PRINTFORM 姊妹的
		ELSEIF RELATION:TARGET:LOCAL >= 150 || RELATION:LOCAL:TARGET >= 150 
			PRINTFORM 挚友的
		ELSEIF (RELATION:TARGET:LOCAL >= 50  || RELATION:LOCAL:TARGET >= 50) && (RELATION:TARGET:LOCAL < 70  || RELATION:LOCAL:TARGET < 70) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
			PRINTFORM 冤家的
		ENDIF
		PRINTFORM %SLIT(TARGET)%和%SLIT(LOCAL)%，
		
		;正常位＋后背位
		IF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2
			PRINTFORML 处于下方的%CALLNAME:TARGET%的%SLIT(TARGET)%能被捅到子宫口，而处于上方的%CALLNAME:LOCAL%的%SLIT(LOCAL)%能更轻易地被刺激到G点
		ELSEIF TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1
			PRINTFORML 处于下方的%CALLNAME:LOCAL%的%SLIT(LOCAL)%能被捅到子宫口，而处于上方的%CALLNAME:TARGET%的%SLIT(TARGET)%能更轻易地被刺激到G点
		ELSE
			PRINTFORML %CALLNAME:TARGET%的%SLIT(TARGET)%和%CALLNAME:LOCAL%的%SLIT(LOCAL)%一左一右地榨取着%TINKO(PLAYER)%
		ENDIF
		PRINTFORML

		IF NOWEX:MASTER:射精 && TCVAR:PLAYER:避孕套 != 1
			IF RELATION:TARGET:LOCAL >= 200 || RELATION:LOCAL:TARGET >= 200 
				PRINTFORM 这对好姐妹会是谁先受孕呢？
			ELSEIF RELATION:TARGET:LOCAL >= 150 || RELATION:LOCAL:TARGET >= 150 
				PRINTFORM 就让%SAMEN_DESCRIPTION(PLAYER)%成为友谊的证明吧！
			ELSEIF RELATION:TARGET:LOCAL >= 100 || RELATION:LOCAL:TARGET >= 100 
				PRINTFORM 好友之间学会分享是理所当然的呢。
			ELSEIF (RELATION:TARGET:LOCAL >= 50  || RELATION:LOCAL:TARGET >= 50) && (RELATION:TARGET:LOCAL < 70  || RELATION:LOCAL:TARGET < 70) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
				PRINTFORM 让这一起灌入的%SAMEN_DESCRIPTION(PLAYER)%抵消这份仇怨吧。
			ELSEIF (RELATION:TARGET:LOCAL >= 70  || RELATION:LOCAL:TARGET >= 70) && (RELATION:TARGET:LOCAL < 100  || RELATION:LOCAL:TARGET < 100) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
				PRINTFORM 只要大家都爽到了，就不会有谁讨厌谁的理由了吧。
			ELSEIF RELATION:TARGET:LOCAL == 100 || RELATION:LOCAL:TARGET == 100 
				PRINTFORM 为%CALLNAME:TARGET%和%CALLNAME:LOCAL%献上解怨的礼炮吧！
			ELSE
				PRINTFORM 让大家一起受孕是建立友谊的好办法呢。
			ENDIF
			PRINTFORMW 相当失礼的念头在%CALLNAME:PLAYER%的脑子里一闪而过
			PRINTFORML
			PRINTFORMW 终于控制不住射精的冲动，%CALLNAME:PLAYER%把\@ NOWEX:MASTER:射精 >1 ? 大量的 # \@%SAMEN_DESCRIPTION(PLAYER)%播撒在%CALLNAME:TARGET%的%SLIT(TARGET)%和%CALLNAME:LOCAL%的%SLIT(LOCAL)%里	
			PRINTFORML
			IF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2
				PRINTFORML 从%SLIT(TARGET)%溢出的%SAMEN_DESCRIPTION(PLAYER)%滴落到同样晕染着白浊的%CALLNAME:LOCAL%的%SLIT(LOCAL)%出口处
			ELSEIF TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1
				PRINTFORML 从%SLIT(LOCAL)%溢出的%SAMEN_DESCRIPTION(PLAYER)%滴落到同样晕染着白浊的%CALLNAME:TARGET%的%SLIT(TARGET)%出口处
			ELSE
				PRINTFORML %CALLNAME:TARGET%的%SLIT(TARGET)%和%CALLNAME:LOCAL%的%SLIT(LOCAL)%之间糊满了浊液，已经分不清是谁的爱液或是%CALLNAME:PLAYER%的%SAMEN_DESCRIPTION(PLAYER)%
			ENDIF
			SETCOLOR 119,136,153
			PRINTFORML 冥冥之中，%CALLNAME:TARGET%和%CALLNAME:LOCAL%的关系稍微变好了
			IF RELATION:TARGET:LOCAL != 0
				RELATION:TARGET:LOCAL += 1
			ELSE
				RELATION:TARGET:LOCAL = 101
			ENDIF
			IF RELATION:LOCAL:TARGET != 0
				RELATION:LOCAL:TARGET += 1
			ELSE
				RELATION:LOCAL:TARGET = 101
			ENDIF
			RESETCOLOR
		ELSEIF NOWEX:MASTER:射精 && TCVAR:PLAYER:避孕套 == 1
			PRINTFORMW %CALLNAME:PLAYER%控制不住射精的欲望，\@ NOWEX:MASTER:射精 >1 ? 大量的 # \@%SAMEN_DESCRIPTION(PLAYER)%把避孕套装满了
			PRINTFORML 只可惜避孕套里的%SAMEN_DESCRIPTION(PLAYER)%不能同时浇灌%CALLNAME:TARGET%和%CALLNAME:LOCAL%
		ELSE
			PRINTFORMW %CALLNAME:TARGET%的%SLIT(TARGET)%和%CALLNAME:LOCAL%的%SLIT(LOCAL)%给予了%CALLNAME:PLAYER%不止于两倍的满足感
		ENDIF
		;1011追加

	ELSE
		IF RAND:2 == 0
			PRINTFORM 在保持对%CALLNAME:LOCAL%阴道的插入的同时抱住%CALLNAME:TARGET%、
			IF RAND:2 == 0
				PRINTFORM 这\@ PALAM:潤滑 >= PALAMLV:4? 充分湿润的# \@
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼穴 # 秘裂 \@
			ELSE
				IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
					PRINT 把重合起来的
				ELSE
					PRINT 把靠在一起的
				ENDIF
				PRINT 两个
				SIF PALAM:潤滑 >= PALAMLV:4 && PALAM:LOCAL:潤滑 >= PALAMLV:4
					PRINT 充分湿润的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼穴 # 秘裂 \@
			ENDIF
		ELSE
			IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
				IF RAND:2 == 0
					PRINTFORM 将%CALLNAME:TARGET%和%CALLNAME:LOCAL%面对面靠在一起、将那\@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼穴 # 蜜穴 \@给
				ELSE
					PRINTFORM 将%CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼穴 # 蜜穴 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼穴 # 蜜穴 \@紧紧贴在一起、
				ENDIF
			ELSE
				PRINTFORM 将%CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼穴 # 蜜穴 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼穴 # 蜜穴 \@靠在一起、
			ENDIF
		ENDIF
		PRINTL 轮流贯穿了…
		
		;1011追加
		PRINTL
		PRINTFORM 开始轮流享受这对
		IF RELATION:TARGET:LOCAL >= 200 || RELATION:LOCAL:TARGET >= 200 
			PRINTFORM 姊妹的
		ELSEIF RELATION:TARGET:LOCAL >= 150 || RELATION:LOCAL:TARGET >= 150 
			PRINTFORM 挚友的
		ELSEIF (RELATION:TARGET:LOCAL >= 50  || RELATION:LOCAL:TARGET >= 50) && (RELATION:TARGET:LOCAL < 70  || RELATION:LOCAL:TARGET < 70) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
			PRINTFORM 冤家的
		ENDIF
		PRINTFORMW %SLIT(TARGET)%，插入拔出的姿势虽然感觉比较别扭，但两种不同的体验让%CALLNAME:PLAYER%停不下来
		PRINTFORML
		IF NOWEX:MASTER:射精 && TCVAR:PLAYER:避孕套 != 1
			IF RELATION:TARGET:LOCAL >= 200 || RELATION:LOCAL:TARGET >= 200 
				PRINTFORM 这对好姐妹会是谁先受孕呢？
			ELSEIF RELATION:TARGET:LOCAL >= 150 || RELATION:LOCAL:TARGET >= 150 
				PRINTFORM 就让%SAMEN_DESCRIPTION(PLAYER)%成为友谊的证明吧！
			ELSEIF RELATION:TARGET:LOCAL >= 100 || RELATION:LOCAL:TARGET >= 100 
				PRINTFORM 好友之间学会分享是理所当然的呢。
			ELSEIF (RELATION:TARGET:LOCAL >= 50  || RELATION:LOCAL:TARGET >= 50) && (RELATION:TARGET:LOCAL < 70  || RELATION:LOCAL:TARGET < 70) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
				PRINTFORM 让这一起灌入的%SAMEN_DESCRIPTION(PLAYER)%抵消这份仇怨吧。
			ELSEIF (RELATION:TARGET:LOCAL >= 70  || RELATION:LOCAL:TARGET >= 70) && (RELATION:TARGET:LOCAL < 100  || RELATION:LOCAL:TARGET < 100) && (RELATION:TARGET:LOCAL != 0  || RELATION:LOCAL:TARGET != 0)
				PRINTFORM 只要大家都爽到了，就不会有谁讨厌谁的理由了吧。
			ELSEIF RELATION:TARGET:LOCAL == 100 || RELATION:LOCAL:TARGET == 100 
				PRINTFORM 为%CALLNAME:TARGET%和%CALLNAME:LOCAL%献上解怨的礼炮吧！
			ELSE
				PRINTFORM 让大家一起受孕是建立友谊的好办法呢。
			ENDIF
			PRINTFORMW 相当失礼的念头在%CALLNAME:PLAYER%的脑子里一闪而过
			PRINTFORML
			PRINTFORML %CALLNAME:PLAYER%控制不住射精的欲望，把\@ NOWEX:MASTER:射精 >1 ? 大量的 # \@%SAMEN_DESCRIPTION(PLAYER)%播撒在%CALLNAME:TARGET%和%CALLNAME:LOCAL%的%SLIT(TARGET)%里
			SETCOLOR 119,136,153
			PRINTFORML 冥冥之中，%CALLNAME:TARGET%和%CALLNAME:LOCAL%的关系稍微变好了
			IF RELATION:TARGET:LOCAL != 0
				RELATION:TARGET:LOCAL += 1
			ELSE
				RELATION:TARGET:LOCAL = 101
			ENDIF
			IF RELATION:LOCAL:TARGET != 0
				RELATION:LOCAL:TARGET += 1
			ELSE
				RELATION:LOCAL:TARGET = 101
			ENDIF
			RESETCOLOR
		ELSEIF NOWEX:MASTER:射精 && TCVAR:PLAYER:避孕套 == 1
			PRINTFORMW %CALLNAME:PLAYER%控制不住射精的欲望，\@ NOWEX:MASTER:射精 >1 ? 大量的 # \@%SAMEN_DESCRIPTION(PLAYER)%把避孕套装满了
			PRINTFORML 只可惜避孕套里的%SAMEN_DESCRIPTION(PLAYER)%不能同时浇灌%CALLNAME:TARGET%和%CALLNAME:LOCAL%
		ELSE
			PRINTFORMW %CALLNAME:TARGET%的%SLIT(TARGET)%和%CALLNAME:LOCAL%的%SLIT(LOCAL)%给予了%CALLNAME:PLAYER%不止于两倍的满足感
		ENDIF
		;1011追加

	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ELSE
	;现在2人以上の处女を同ターンで夺うことは出来ないため、处女关联はここ一つにまとめる
	IF TALENT:処女 == 1
		PRINTFORML 在保持对%CALLNAME:LOCAL%阴道的插入的同时抱住%CALLNAME:TARGET%、把她那没尝过男人滋味的\@ TALENT:体型 < 0 ? 幼穴 # 蜜穴 \@也一起贯穿了…
	;连续
	ELSEIF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%和%CALLNAME:LOCAL%
		;正常位＋后背位
		IF ((TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2) || (TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1))
			PRINT 的身体紧紧贴在一起、
		;正常位＋正常位
		ELSEIF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 1
			PRINT 仰面躺在一起、
		ELSE
			PRINT 的屁股翘向这边、
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 在品尝着二人阴道触感的细微差别的同时
		PRINTL 再次轮流插入起来…
	ELSE
		IF RAND:2 == 0
			PRINTFORM 在保持对%CALLNAME:LOCAL%阴道的插入的同时抱住%CALLNAME:TARGET%、交互贯穿着
			IF RAND:2 == 0
				PRINT 那
				SIF PALAM:潤滑 >= PALAMLV:4
					PRINT 充分湿润的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼穴 # 秘裂 \@
			ELSE
				IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
					PRINT 叠在一起
				ELSE
					PRINT 并列着的
				ENDIF
				PRINT 两个
				SIF PALAM:潤滑 >= PALAMLV:4 && PALAM:LOCAL:潤滑 >= PALAMLV:4
					PRINT 充分湿润的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼穴 # 秘裂 \@
			ENDIF
		ELSE
			IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
				IF RAND:2 == 0
					PRINTFORM 让%CALLNAME:TARGET%和%CALLNAME:LOCAL%抱在一起、交互贯穿着那\@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼穴 # 秘裂 \@
				ELSE
					PRINTFORM 让%CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼穴 # 秘裂 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼穴 # 秘裂 \@紧贴在一起、交互贯穿着
				ENDIF
			ELSE
				PRINTFORM 让%CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼穴 # 秘裂 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼穴 # 秘裂 \@排成一列、交互贯穿着
			ENDIF
		ENDIF
		PRINTL …
	ENDIF

	IF TALENT:恋慕 && TALENT:LOCAL:恋慕
		PRINTFORML %CALLNAME:TARGET%和%CALLNAME:LOCAL%在被轮流插入的同时、央求着%CALLNAME:MASTER%的%TINKO(PLAYER)%…
	ELSEIF EXP:百合経験 >= 1000 && EXP:LOCAL:百合経験 >= 1000
		PRINTFORML %CALLNAME:TARGET%和%CALLNAME:LOCAL%承受着%CALLNAME:MASTER%%TINKO(PLAYER)%插入的同时，动情地互相接吻起来…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ENDIF


;-------------------------------------------------
;16,肛门交互插入
;-------------------------------------------------
@MESSAGE_SCOM16
LOCAL = MASTER_POSE(SET_A,SET_P,1)
IF (FLAG:70 && CFLAG:TARGET:344 == 0 && CFLAG:LOCAL:344 == 0)	;连续
	IF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%和%CALLNAME:LOCAL%
		;正常位＋后背位
		IF ((TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2) || (TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1))
			PRINT 面对面靠到一起、
		;正常位＋正常位
		ELSEIF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 1
			PRINT 仰面躺在一起、
		ELSE
			PRINT 四脚着地趴下、
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 一边品味着两人菊穴内触感的不同
		PRINTL 再次轮流抽插起来…
	ELSE
		IF RAND:2 == 0
			PRINTFORM 在插入%CALLNAME:LOCAL%的菊穴的同时，抱紧%CALLNAME:TARGET%
			IF RAND:2 == 0
				PRINT 那
				SIF PALAM:潤滑 >= PALAMLV:4
					PRINT 湿滑的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小窄缝 # 肛门 \@
			ELSE
				IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
					PRINT 并列在一起的
				ELSE
					PRINT 并排在一起的
				ENDIF
				PRINT 两个
				SIF PALAM:潤滑 >= PALAMLV:4 && PALAM:LOCAL:潤滑 >= PALAMLV:4
					PRINT 湿滑的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小窄缝 # 肛门 \@
			ENDIF
		ELSE
			IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
				IF RAND:2 == 0
					PRINTFORM 将%CALLNAME:TARGET%和%CALLNAME:LOCAL%面对面叠在一起，然后将她们的\@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小窄缝 # 肛门 \@
				ELSE
					PRINTFORM %CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼小窄缝 # 肛门 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼小窄缝 # 肛门 \@并列在一起的、
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼小窄缝 # 肛门 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼小窄缝 # 肛门 \@并排在一起、
			ENDIF
		ENDIF
		PRINTL 轮流贯穿了…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ELSE	;连续
	IF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%和%CALLNAME:LOCAL%
		;正常位＋后背位
		IF ((TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 2) || (TEQUIP:体位 == 2 && TEQUIP:LOCAL:体位２ == 1))
			PRINT 的身体紧紧贴在一起、
		;正常位＋正常位
		ELSEIF TEQUIP:体位 == 1 && TEQUIP:LOCAL:体位２ == 1
			PRINT 仰面躺在一起、
		ELSE
			PRINT 的屁股翘向这边、
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 一边品味着两人菊穴内触感的不同
		PRINTL 再次轮流抽插起来…
	ELSE
		IF RAND:2 == 0
			PRINTFORM 在插入%CALLNAME:LOCAL%的菊穴的同时，抱紧%CALLNAME:TARGET%
			IF RAND:2 == 0
				PRINT 那
				SIF PALAM:潤滑 >= PALAMLV:4
					PRINT 湿滑的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小窄缝 # 肛门 \@
			ELSE
				IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
					PRINT 并列在一起的
				ELSE
					PRINT 并排在一起的
				ENDIF
				PRINT 两个
				SIF PALAM:潤滑 >= PALAMLV:4 && PALAM:LOCAL:潤滑 >= PALAMLV:4
					PRINT 湿滑的
				PRINTFORM \@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小窄缝 # 肛门 \@
			ENDIF
		ELSE
			IF TEQUIP:体位 == 2 && (TEQUIP:LOCAL:体位２ == 1 || TEQUIP:LOCAL:体位２ == 4)
				IF RAND:2 == 0
					PRINTFORM 将%CALLNAME:TARGET%和%CALLNAME:LOCAL%面对面叠在一起，然后将她们的\@ ( TALENT:体型 < 0 && TALENT:LOCAL:体型 < 0 ) ? 幼小窄缝 # 肛门 \@
				ELSE
					PRINTFORM %CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼小窄缝 # 肛门 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼小窄缝 # 肛门 \@并列在一起的、
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%的\@ TALENT:体型 < 0 ? 幼小窄缝 # 肛门 \@和%CALLNAME:LOCAL%的\@ TALENT:LOCAL:体型 < 0 ? 幼小窄缝 # 肛门 \@并排在一起、
			ENDIF
		ENDIF
		PRINTL 轮流贯穿了…
	ENDIF

	IF TALENT:恋慕 && TALENT:LOCAL:恋慕
		WAIT
		PRINTFORML %CALLNAME:TARGET%和%CALLNAME:LOCAL%在被轮流插入的同时、央求着%CALLNAME:MASTER%的%TINKO(PLAYER)%…
	ELSEIF EXP:百合経験 >= 1000 && EXP:LOCAL:百合経験 >= 1000
		WAIT
		PRINTFORML %CALLNAME:TARGET%和%CALLNAME:LOCAL%在互相接吻的同时、承受着%CALLNAME:MASTER%的%TINKO(PLAYER)%的插入…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ENDIF

;-------------------------------------------------
;17,同时吸乳头
;-------------------------------------------------
@MESSAGE_SCOM17
SIF FLAG:70 == 1
	PRINTFORM 在停止的时间中，
IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
	PRINTFORM 一边\@(FLAG:70 && CFLAG:TARGET:344 == 0) ?侵犯着%CALLNAME:TARGET% #与%CALLNAME:TARGET%身体交合着 \@一边
ELSE
	;PRINTFORM 将%CALLNAME:TARGET%
ENDIF
IF TEQUIP:1 == 0
	;PRINT 
ELSE
	FOR LOCAL,17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:TARGET:LOCAL 
			PRINTFORM 伸进%CLOTHNAME(LOCAL,EQUIP:TARGET:LOCAL)%
			BREAK
		ENDIF
	NEXT
ENDIF
PRINTFORM 将%CALLNAME:TARGET%
IF TALENT:母乳体質 && RAND:3 == 0
	PRINTFORM 渗着%TEXTR("雌乳/母乳/奶汁")%
ENDIF
IF TALENT:TARGET:胸围 >= 1
	PRINTFORML 的%OPPAI_DESCRIPTION(TARGET)%挤在一起、
	PRINTFORML %TEXTR(@"同时将两个%NIPPLE_DESCRIPTION(TARGET,1)%含入口中，轻轻啃咬玩弄并吸吮着/同时用力吮吸两个%NIPPLE_DESCRIPTION(TARGET,1)%，故意发出下流的声音/同时将两个%NIPPLE_DESCRIPTION(TARGET,1)%含入口中，一边挑逗乳晕周围一边吸吮/同时将两个%NIPPLE_DESCRIPTION(TARGET,1)%含入口中，在口中互相摩擦刺激着")%……
ELSE
	PRINTFORML 的%OPPAI_DESCRIPTION(TARGET)%轻轻捏起，
	PRINTFORML %TEXTR(@"将%NIPPLE_DESCRIPTION(TARGET,1)%交替含入口中，轻轻啃咬玩弄并吸吮着/交替吮吸%NIPPLE_DESCRIPTION(TARGET,1)%，故意发出下流的声音/将%NIPPLE_DESCRIPTION(TARGET,1)%交替含入口中，一边挑逗乳晕周围一边吸吮")%……
ENDIF
IF ABL:TARGET:親密 >= 10 && (!FLAG:70 || CFLAG:TARGET:344 == 1)
	SELECTCASE ABL:TARGET:Ｂ感覚 
	CASE 0,1
		PRINTFORM %CALLNAME:TARGET%一边苦笑、一边温柔地抚摸着正吸着%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的头
	CASE 2 TO 5
		PRINTFORM %CALLNAME:TARGET%一边忍受快感、一边温柔地抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的头
	CASE 6 TO 9
		PRINTFORM %CALLNAME:TARGET%因为快感而娇喘起来的同时、用力抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的头
	CASEELSE
		PRINTFORML %CALLNAME:TARGET%露出按捺不住快感的样子紧紧抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%、
		PRINTFORML 比起爱抚更想要对乳头的强烈刺激
	ENDSELECT
ENDIF

;-------------------------------------------------
;18,后背位打屁股
;-------------------------------------------------
@MESSAGE_SCOM18
PRINTFORM %CALLNAME:PLAYER%一边用腰身撞击着%CALLNAME:TARGET%、一边
SIF TEQUIP:0 & 1 && (EQUIP:裙子 || EQUIP:下身衣服 || EQUIP:連衣裙 || EQUIP:和服)
	PRINTFORM 掀起%SHOW_BOTTOM(TARGET,1)%
PRINTFORM 使劲拍打起
SIF EQUIP:TARGET:レオタード || EQUIP:TARGET:ボディースーツ
	PRINTFORM 被%SHOW_下半身内衣(TARGET,2)%包住的
PRINTFORML %HIP(TARGET)%发出了响亮的声音

;-------------------------------------------------
;19,授乳PLAY
;-------------------------------------------------
@MESSAGE_SCOM19
PRINTFORM %CALLNAME:TARGET%将%CALLNAME:MASTER%抱在胸前，
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL,17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:TARGET:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL,EQUIP:TARGET:LOCAL)%的外面将
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:母乳体質 && RAND:3 == 0
	PRINTFORM 渗着%TEXTR("雌乳/母乳/奶汁")%的
ENDIF
SIF TALENT:TARGET:胸围 >= 1
	PRINTFORM 将%OPPAI_DESCRIPTION(TARGET)%托起、
PRINTFORML 在%NIPPLE_DESCRIPTION(TARGET,1)%被%CALLNAME:MASTER%含在口中的状态下，开始撸动%TINKO(PLAYER)%
PRINTFORM %CALLNAME:MASTER%也将身体交给了%CALLNAME:TARGET%、
SELECTCASE RAND:4
CASE 0
	PRINTFORML 轻轻啃咬着%NIPPLE_DESCRIPTION(TARGET)%，同时用舌头玩弄并吸吮着……
CASE 1
	PRINTFORML 用力吮吸着%NIPPLE_DESCRIPTION(TARGET)%，发出下流的声音并吸吮着……
CASE 2
	PRINTFORML 挑逗般地舔舐着乳晕周围，然后再吸吮%NIPPLE_DESCRIPTION(TARGET)%……
CASEELSE
	PRINTFORML 用舌尖在%NIPPLE_DESCRIPTION(TARGET)%上画着圈，然后像婴儿一样吸吮着……
ENDSELECT
IF ABL:TARGET:親密 >= 10 && (!FLAG:70 || CFLAG:TARGET:344 == 1)
	SELECTCASE ABL:TARGET:Ｂ感覚 
	CASE 0,1
		PRINTFORM %CALLNAME:TARGET%一边苦笑、一边温柔地抚摸着正吸着%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的头
	CASE 2 TO 5
		PRINTFORM %CALLNAME:TARGET%一边忍受快感、一边温柔地抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的头
	CASE 6 TO 9
		PRINTFORM %CALLNAME:TARGET%因为快感而娇喘起来的同时、用力抱住了正吸吮%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的头
	CASEELSE
		PRINTFORML %CALLNAME:TARGET%似乎快感难耐，一把抱住正吮吸着%OPPAI_DESCRIPTION(TARGET)%的%CALLNAME:MASTER%的头，
		PRINTFORML 并寻求更加强烈的乳头刺激
	ENDSELECT
ENDIF