; 立絵追加リソースセット関連
@DISPLAY_COM_IMAGE(リソース名, COMNO)
#DIMS リソース名
#DIM COMNO
IF !FLAG:立絵種類 && CHECK_NUDE(NO:TARGET)
	リソース名 = 立絵_裸_COM_{NO:TARGET}_{COMNO}
	;PRINTFORML 诶嘿嘿画像存在チェック%リソース名%
	IF SPRITECREATED(リソース名)
		CALL 画像表示(リソース名, 0, 0, 0, 100)
		CALL ピクセル自動改行(默认角色画像横幅)
		CALL 画像描画終了
	ELSE
		;PRINTFORML 存在しません
	ENDIF
ELSE
	;PRINTFORML 立絵追加リソース表示しません
ENDIF
