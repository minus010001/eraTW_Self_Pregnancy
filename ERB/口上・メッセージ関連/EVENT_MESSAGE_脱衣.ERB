
@MESSAGE_脱衣外套;だけ
IF SHOW_TOP(TARGET) != ""
	IF TEQUIP:3 == 0; && EQUIP:TARGET:上半身内衣１
		PRINTFORM %CALLNAME:TARGET%\@ FLAG:70 ? の # が \@
		IF EQUIP:TARGET:上身衣服１
			IF EQUIP:TARGET:連衣裙
				PRINTFORM %CLOTHNAME(17, EQUIP:TARGET:上身衣服１)%
				SIF EQUIP:TARGET:上身衣服２
					PRINTFORM と%CLOTHNAME(16, EQUIP:TARGET:上身衣服２)%
				SIF EQUIP:TARGET:和服
					PRINTFORM と%CLOTHNAME(14, EQUIP:TARGET:和服)%
				PRINTFORM と%CLOTHNAME(13, EQUIP:TARGET:連衣裙)%を脱\@ FLAG:70 ? がせる # ぐ \@と、
			ELSE
				PRINTFORM %CLOTHNAME(17, EQUIP:TARGET:上身衣服１)%
				IF EQUIP:TARGET:上身衣服２
					PRINTFORM を脱\@ FLAG:70 ? がせ # ぎ \@%CLOTHNAME(16, EQUIP:TARGET:上身衣服２)%
					SIF EQUIP:TARGET:和服
						PRINTFORM と%CLOTHNAME(14, EQUIP:TARGET:和服)%
					PRINTFORM を肌蹴\@ FLAG:70 ? させる # る \@と、
				ELSEIF EQUIP:TARGET:和服
					PRINTFORM を脱\@ FLAG:70 ? がせ # ぎ \@%CLOTHNAME(14, EQUIP:TARGET:和服)%を肌蹴\@ FLAG:70 ? させる # る \@と、
				ELSE
					PRINTFORM を脱\@ FLAG:70 ? がせる # ぐ \@と、
				ENDIF
			ENDIF
		ELSEIF EQUIP:TARGET:上身衣服２
			PRINTFORM %CLOTHNAME(16, EQUIP:TARGET:上身衣服２)%
			IF EQUIP:TARGET:連衣裙
				SIF EQUIP:TARGET:和服
					PRINTFORM と%CLOTHNAME(14, EQUIP:TARGET:和服)%
				PRINTFORM を肌蹴させ%CLOTHNAME(13, EQUIP:TARGET:連衣裙)%を脱\@ FLAG:70 ? がせる # ぐ \@と、
			ELSE
				SIF EQUIP:TARGET:和服
					PRINTFORM と%CLOTHNAME(14, EQUIP:TARGET:和服)%
				PRINTFORM を肌蹴\@ FLAG:70 ? させる # る \@と
			ENDIF
		ELSEIF EQUIP:TARGET:和服
			PRINTFORM %CLOTHNAME(14, EQUIP:TARGET:和服)%
			IF EQUIP:TARGET:連衣裙
				PRINTFORM を肌蹴させ%CLOTHNAME(13, EQUIP:TARGET:連衣裙)%を脱\@ FLAG:70 ? がせる # ぐ \@と、
			ELSE
				PRINTFORM を肌蹴\@ FLAG:70 ? させる # る \@と、
			ENDIF
		ELSE ;ワンピのみ
			PRINTFORM %CLOTHNAME(13, EQUIP:TARGET:連衣裙)%を脱\@ FLAG:70 ? がせる # ぐ \@と、
		ENDIF

;		IF SHOW_TOP(TARGET,1) != ""
;			PRINTFORM %CALLNAME:TARGET%\@ FLAG:70 ? 的 # 把 \@%SHOW_TOP(TARGET,1)%里的肌肤\@ FLAG:70 ? 被展现出来了 # 展现出来了 \@同時、
;		ELSE
;			PRINTFORM %CALLNAME:TARGET%\@ FLAG:70 ? 的 # 把 \@%SHOW_TOP(TARGET,2)%\@ FLAG:70 ? 被脱下来了 # 脱下来了 \@同時、
;		ENDIF
		
		IF SHOW_上半身内衣(TARGET) != ""
			PRINTFORM %OPPAI_DESCRIPTION(TARGET)%
			IF EQUIP:TARGET:緊身内衣 || EQUIP:TARGET:連体内衣 || EQUIP:TARGET:連衣裙 || EQUIP:TARGET:和服
				PRINTFORM 包着的%SHOW_上半身内衣(TARGET)%
				IF SHOW_BOTTOM(TARGET) == ""
					PRINTFORM 和%SLIT(TARGET)%
				ELSEIF !EQUIP:TARGET:裙子
					PRINTFORM 和%SHOW_BOTTOM(TARGET, 14)%
				ENDIF
				PRINTFORMW 暴露了出来
			ELSEIF EQUIP:TARGET:上半身内衣１
				PRINTFORMW 包着的%SHOW_上半身内衣(TARGET)%露出来了
			ELSEIF EQUIP:TARGET:下半身内衣２
				PRINTFORMW 被从敞開的%SHOW_TOP(TARGET,1)%露出來了
			ENDIF
		ELSE
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%
			IF EQUIP:TARGET:緊身内衣 || EQUIP:TARGET:連体内衣 || EQUIP:TARGET:連衣裙 || EQUIP:TARGET:和服
				IF SHOW_BOTTOM(TARGET) == ""
					PRINTFORM 和%SLIT(TARGET)%
				ELSEIF !EQUIP:TARGET:裙子
					PRINTFORM 和%SHOW_BOTTOM(TARGET, 14)%
				ENDIF
			ENDIF
			PRINTFORMW 暴露了出来
		ENDIF
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET, 1)%暴露了出来
ENDIF

IF PALAM:TARGET:欲情 >= PALAMLV:3
	IF PALAM:TARGET:欲情 >= PALAMLV:6
		PRINTFORM 那%NIPPLE_DESCRIPTION(TARGET)%、
		IF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000 && ABL:Ｂ感覚 <= 5
			IF RAND:2 == 0
				PRINT 好像怀着对乳房凌辱的期待而颤动着的様子
			ELSE
				PRINTFORM 仿佛在央求着%CALLNAME:PLAYER%的乳奸似的
			ENDIF
		ENDIF
		IF RAND:4 == 0
			PRINT 因为快感而開始膨胀て
		ELSEIF RAND:3 == 0
			PRINT 開始变硬
		ELSEIF RAND:2 == 0
			PRINT 乳晕開始膨胀
		ELSE
			PRINT 乳晕開始变大
		ENDIF
		PRINTFORMW \@ EQUIP:TARGET:上半身内衣１ ? 在%SHOW_上半身内衣(TARGET)%的布料上凸顯出来 #  \@了……
	ELSEIF PALAM:TARGET:欲情 >= PALAMLV:3
		PRINTFORM 那%NIPPLE_DESCRIPTION(TARGET)%、
		IF RAND:3 == 0
			PRINT 往上挺起来
		ELSEIF RAND:2 == 0
			PRINT 完全硬起来
		ELSE
			PRINT 勃起
		ENDIF
		PRINTFORMW \@ EQUIP:TARGET:上半身内衣１ ? 、在%SHOW_上半身内衣(TARGET)%的布料上凸顯出来 #  \@了…
	ENDIF
ENDIF

SIF !((EQUIP:TARGET:緊身内衣 || EQUIP:TARGET:連体内衣 || EQUIP:TARGET:連衣裙 || EQUIP:TARGET:和服) && !(EQUIP:TARGET:裙子 || EQUIP:TARGET:裤子))
	RETURN
IF (EQUIP:TARGET:下半身内衣１ || EQUIP:TARGET:下半身内衣２)
	IF PALAM:潤滑 >= PALAMLV:3
		PRINTFORMW %UNDER_SKIRT_DESCRIPTION(TARGET)%は愛液で染みが出来ている…
	ELSEIF PALAM:潤滑 >= PALAMLV:2
		PRINTFORMW %UNDER_SKIRT_DESCRIPTION(TARGET)%が愛液で湿っている…
	ENDIF
ENDIF

	
@MESSAGE_脱衣下内衣
IF EQUIP:TARGET:下半身内衣２
	PRINTFORM %CALLNAME:TARGET%\@ !FLAG:70 ? 把 # 从\@%PANTSNAME(EQUIP:TARGET:下半身内衣２, TARGET)%给
	IF PALAM:TARGET:潤滑 >= PALAMLV:4
		PRINTFORM \@ !FLAG:70 ? 脱掉 # 让脱掉\@
		IF GET_INT(TARGET, "下半身内衣_ずらし可能", EQUIP:TARGET:下半身内衣２, "半透明(前)") && !RAND:2
			PRINTFORMW 结果发现、%CALLNAME:TARGET%的%SLIT(TARGET)%湿漉漉黏糊糊的、%PANTSNAME(EQUIP:TARGET:下半身内衣２, TARGET)%吸收了大量愛液変得沉甸甸的…
		ELSE
			PRINTFORMW 结果发现、%SLIT(TARGET)%里溢出的愛液滴滴答答地流到了大腿上…
		ENDIF
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:3
		PRINTFORM \@ !FLAG:70 ? 脱掉 # 让脱掉\@
		IF GET_INT(TARGET, "下半身内衣_ずらし可能", EQUIP:TARGET:下半身内衣２, "半透明(前)") && !RAND:2
			PRINTFORMW 结果发现、%SLIT(TARGET)%与%PANTSNAME(EQUIP:TARGET:下半身内衣２, TARGET)%之間愛液拉出了一道淫絲…
		ELSE
			PRINTFORMW 结果发现、%CALLNAME:TARGET%的%SLIT(TARGET)%很明顯的湿透了…
		ENDIF
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:2
		PRINTFORMW \@ !FLAG:70 ? 脱掉 # 让脱掉\@之后发现、%CALLNAME:TARGET%的%SLIT(TARGET)%開始湿潤了…
	ELSE
		PRINTFORMW \@ !FLAG:70 ? 脱掉了 # 让脱掉了\@
	ENDIF
ELSEIF EQUIP:TARGET:下半身内衣１
	PRINTFORM %CALLNAME:TARGET%\@ !FLAG:70 ? 把 # 从\@%SHOW_下半身内衣(TARGET, 3)%给
	IF PALAM:TARGET:潤滑 >= PALAMLV:4
		PRINTFORM \@ !FLAG:70 ? 脱掉 # 让脱掉\@
		IF GET_INT(TARGET, "下半身内衣_ずらし不可", EQUIP:TARGET:下半身内衣１, "半透明(前)") && !RAND:2
			PRINTFORMW 结果发现、%CALLNAME:TARGET%的%SLIT(TARGET)%湿漉漉黏糊糊的、%SHOW_下半身内衣(TARGET, 3)%吸收了大量愛液変得沉甸甸的…	
		ELSE
			PRINTFORMW 结果发现、%SLIT(TARGET)%里溢出的愛液滴滴答答地流到了大腿上…
		ENDIF
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:3
		PRINTFORM \@ !FLAG:70 ? 脱掉 # 让脱掉\@
		IF GET_INT(TARGET, "下半身内衣_ずらし不可", EQUIP:TARGET:下半身内衣１, "半透明(前)") && !RAND:2
			PRINTFORMW 结果发现、%SLIT(TARGET)%与%SHOW_下半身内衣(TARGET, 3)%之間愛液拉出了一道淫絲…
		ELSE
			PRINTFORMW 结果发现、%CALLNAME:TARGET%的%SLIT(TARGET)%很明顯的湿透了…
		ENDIF
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:2
		PRINTFORMW \@ !FLAG:70 ? 脱掉 # 让脱掉\@之后发现、%CALLNAME:TARGET%的%SLIT(TARGET)%開始湿潤了…
	ELSE
		PRINTFORMW \@ !FLAG:70 ? 脱掉了 # 让脱掉了\@
	ENDIF
ELSE
	IF PALAM:TARGET:潤滑 >= PALAMLV:4
		PRINTFORMW %CALLNAME:TARGET%的私处湿漉漉黏糊糊的、从%SLIT(TARGET)%溢出的愛液滴滴答答地流到了大腿上…
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:3
		PRINTFORMW %CALLNAME:TARGET%的%SLIT(TARGET)%很明顯的湿透了…
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:2
		PRINTFORMW %CALLNAME:TARGET%的%SLIT(TARGET)%開始湿潤了…
	ELSE
		PRINTFORMW %CALLNAME:TARGET%的%SLIT(TARGET)%有了水迹…
	ENDIF
ENDIF

SELECTCASE CFLAG:TARGET:妊娠経過日数
	CASE IS >= 60
		IF TALENT:TARGET:体型 == -2
			PRINTFORMW 臨盆的腹部以与幼小身躯不相称的程度膨胀起来
		ELSE
			PRINTFORMW 臨盆的腹部大大地膨胀起来
		ENDIF
	CASE IS >= 40
		PRINTFORMW %CALLNAME:TARGET%的体形与以前相比变得圆润、肚子很明显的鼓了起来
	CASE IS >= 30
		PRINTFORMW 一看到開始膨胀的%CALLNAME:TARGET%的腹部、便重新感受到了令%CALLNAME:TARGET%怀孕了的実感
	CASE IS >= 20
		PRINTFORMW %CALLNAME:TARGET%的肚子虽然还没有膨大起来、但一想到她的肚子里怀着自己的孩子便不由感慨万千
ENDSELECT

@MESSAGE_脱衣上
IF SHOW_TOP(TARGET) != ""
	IF SHOW_TOP(TARGET, 1) != ""
		PRINTFORM \@ FLAG:70 ? # %CALLNAME:TARGET%把\@%SHOW_TOP(TARGET,1)%里的肌肤\@ FLAG:70 ? 被展现出来了 # 展现出来了 \@
		IF (SHOW_上半身内衣(TARGET) != SHOW_TOP(TARGET,1)) && (SHOW_上半身内衣(TARGET) != "")
			SIF !EQUIP:TARGET:上半身内衣１ || !EQUIP:TARGET:上半身内衣２
				PRINTFORM %SHOW_上半身内衣(TARGET)%\@ FLAG:70 ? 被脱掉 # 脱掉 \@、
;		ELSE
;			SIF !EQUIP:TARGET:上半身内衣１ || !EQUIP:TARGET:上半身内衣２
;				PRINTFORM %SHOW_上半身内衣(TARGET)%\@ FLAG:70 ? 被脱掉 # 脱掉 \@、
		ENDIF
		PRINTFORML %OPPAI_DESCRIPTION(TARGET, 1)%被暴露了出来
	ELSE
		PRINTFORM \@ FLAG:70 ? %CALLNAME:TARGET%から # %CALLNAME:TARGET%が \@%SHOW_TOP(TARGET,2)%
		IF SHOW_上半身内衣(TARGET) != SHOW_TOP(TARGET,1) && SHOW_上半身内衣(TARGET) != SHOW_TOP(TARGET,2)
			SIF !EQUIP:TARGET:上半身内衣１ || !EQUIP:TARGET:上半身内衣２
				PRINTFORM 与%SHOW_上半身内衣(TARGET)%
		ENDIF
		PRINTFORML \@ FLAG:70 ? 被脱掉 # 脱掉 \@、%OPPAI_DESCRIPTION(TARGET, 1)%被暴露了出来
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET, 1)%暴露了出来
ENDIF

IF PALAM:TARGET:欲情 >= PALAMLV:3
	IF PALAM:TARGET:欲情 >= PALAMLV:6
		PRINTFORM 那%NIPPLE_DESCRIPTION(TARGET)%、
		IF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000 && ABL:Ｂ感覚 <= 5
			IF RAND:2 == 0
				PRINT 好像怀着对乳房凌辱的期待而颤动着的様子
			ELSE
				PRINTFORM 仿佛在央求着%CALLNAME:PLAYER%的乳奸似的
			ENDIF
		ENDIF
		IF RAND:4 == 0
			PRINT 因为快感而開始膨胀
		ELSEIF RAND:3 == 0
			PRINT 開始变硬
		ELSEIF RAND:2 == 0
			PRINT 乳晕開始膨胀
		ELSE
			PRINT 乳晕開始变大
		ENDIF
		PRINTFORMW 隆起……
	ELSEIF PALAM:TARGET:欲情 >= PALAMLV:3
		PRINTFORM 那%NIPPLE_DESCRIPTION(TARGET)%、
		IF RAND:3 == 0
			PRINT 往上挺起来
		ELSEIF RAND:2 == 0
			PRINT 完全硬起来
		ELSE
			PRINT 勃起
		ENDIF
		PRINTFORMW 了、仿佛在彰显自己一様…
	ENDIF
ENDIF

@MESSAGE_脱衣下(ARG)
SIF !(EQUIP:TARGET:裙子 || EQUIP:TARGET:裤子)
	RETURN
SIF (!ARG && EQUIP:TARGET:裙子) || EQUIP:TARGET:裤子
	PRINTFORM \@ EQUIP:TARGET:裤子 ? %SHOW_BOTTOM(TARGET)% # %SHOW_BOTTOM(TARGET, 1)%\@\@ FLAG:70 ? 被脱掉 # 脱掉 \@了之后发现、

PRINTFORM %UNDER_SKIRT_DESCRIPTION(TARGET)%
IF (EQUIP:TARGET:下半身内衣１ || EQUIP:TARGET:下半身内衣２)
	IF PALAM:潤滑 >= PALAMLV:3
		PRINTFORMW 被愛液的染透了…
	ELSEIF PALAM:潤滑 >= PALAMLV:2
		PRINTFORMW 被愛液打湿了…
	ELSE
		PRINTFORMW 被看到了…
	ENDIF
ELSE
	PRINTFORMW 被看到了…
ENDIF
