
@MESSAGE_COM61通常
;背面立位
IF TFLAG:193
	;抜かず
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%\@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 拽住%CALLNAME:TARGET%的双手 # 抓住%CALLNAME:TARGET%的屁股 \@、
			PRINTFORML 为了将精液注入膣内开始激烈地动起了腰…
		ELSEIF TCVAR:102
			PRINTFORML 在%CALLNAME:TARGET%的膣内射入精液之后、
			PRINTFORML 趁着%TINKO(PLAYER)%尚未失去硬度再次开始动起了腰…
		ELSEIF TEQUIP:体位２ == 2
			;调教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着%CALLNAME:TARGET%膣内的触感的同时、再次开始动起了腰…
			;穿戴式假阴茎を使用している场合
			ELSE
				PRINTFORML 没有从%CALLNAME:TARGET%的膣内拔出、再次开始动起了腰…
			ENDIF
		ELSE
			IF TEQUIP:体位２ == 2
				PRINTFORML 没有从%CALLNAME:TARGET%的膣内拔出%TINKO(PLAYER)%并再次开始动起了腰…
			;对面立位から
			ELSE
				PRINTFORML 暂时从%CALLNAME:TARGET%体内拔出%TINKO(PLAYER)%将其扶住墻壁
				PRINTFORML 从后面插入进去并再次开始动起了腰…
			ENDIF
		ENDIF
	ELSE
		IF TALENT:処女 == 1
			PRINTFORML \@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 从后面抓住双手让%CALLNAME:TARGET% # 让扶着墻壁的%CALLNAME:TARGET% \@抬高屁股
			PRINTFORML 没尝过男人滋味的\@ TALENT:体型 < 0 ? 幼穴 # 秘裂 \@里将%TINKO(PLAYER)%贯穿了进去…
		ELSEIF MASTER_POSE(SET_V,SET_P,1) && MASTER_POSE(SET_V,SET_P,1) != TARGET
			PRINTFORML 从%CALLNAME:(MASTER_POSE(SET_V,SET_P,1))%体内拔出%TINKO(PLAYER)%的同时
			PRINTFORML 插入了%CALLNAME:TARGET%…
		ELSE
			IF TEQUIP:0 & 2
				PRINTFORM 拨开%SHOW_下半身内衣(TARGET,2)%
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 掀起%SHOW_BOTTOM(TARGET,1)%
			ENDIF
			PRINTFORM \@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 抓住%CALLNAME:TARGET%伸出的双手 # 让%CALLNAME:TARGET%的两手扶着墻壁 \@、从后面插入到了
			;润滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充分湿润的
			;矮小体型
			IF TALENT:体型 < 0
				PRINT 幼穴
			ELSE
				PRINT 秘裂
			ENDIF
			PRINTFORML 的最深处…
		ENDIF
	ENDIF
ELSE
;抜かず
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抓住四肢着地趴下的%CALLNAME:TARGET%的腰、
			PRINTFORML 为了将精液注入膣内开始激烈地动起了腰…
		ELSEIF TCVAR:102
			PRINTFORML 在%CALLNAME:TARGET%的膣内射入精液之后、
			PRINTFORML 趁着%TINKO(PLAYER)%尚未失去硬度再次开始动起了腰…
		ELSEIF TEQUIP:体位２ == 2
			;调教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着%CALLNAME:TARGET%膣内的触感的同时、再次开始动起了腰…
			;穿戴式假阴茎を使用している场合
			ELSE
				PRINTFORML 没有从%CALLNAME:TARGET%的膣内拔出、再次开始动起了腰…
			ENDIF
		ELSE
			PRINTFORML 保持着插入状态让%CALLNAME:TARGET%四肢着地趴下、再次开始动起了腰…
		ENDIF
	ELSE
		IF TALENT:処女 == 1
			PRINTFORML 将四肢着地趴下的%CALLNAME:TARGET%的屁股高高托起
			PRINTFORML 没尝过男人滋味的\@ TALENT:体型 < 0 ? 幼穴 # 秘裂 \@里将%TINKO(PLAYER)%贯穿了进去…
		ELSEIF MASTER_POSE(SET_V,SET_P,1) && MASTER_POSE(SET_V,SET_P,1) != TARGET
			PRINTFORML 从%CALLNAME:(MASTER_POSE(SET_V,SET_P,1))%体内拔出%TINKO(PLAYER)%的同时
			PRINTFORML 插入了%CALLNAME:TARGET%…
		ELSE
			PRINTFORM 对四肢着地趴下的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨开%SHOW_下半身内衣(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 把%SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%插入
			;润滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充分湿润的
			;矮小体型
			IF TALENT:体型 < 0
				PRINT 幼穴
			ELSE
				PRINT 秘裂
			ENDIF
			PRINTFORML 的最深处…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;322,对方主导后背位
;-------------------------------------------------
@MESSAGE_COM322
IF TALENT:処女 == 1
	PRINTFORML 按照%CALLNAME:TARGET%的期望、把龟头对准了那不识男人滋味的\@ TALENT:体型 < 0 ? 幼穴 # 秘裂 \@、
	PRINTFORML 慢慢地撑开紧闭的秘唇并插了进去、撕裂了纯洁之证…
		SIF TALENT:恋慕
			PRINTFORML %CALLNAME:TARGET%一边忍耐着痛楚一边紧紧抱住了%CALLNAME:PLAYER%…
ELSE
	PRINTFORML %CALLNAME:MASTER%压在%CALLNAME:TARGET%身上、慢慢地把龟头插入了因为欲求而微颤的膣口…
	SELECTCASE ABL:TARGET:膣
		CASE 0
			PRINTFORML %TINKO()%撑开了狭窄的膣肉一点点前进、侵入到了%CALLNAME:TARGET%的最深处…
		CASE 1
			PRINTFORML 撑开还是十分紧窄的膣肉、把%TINKO()%插入到了%CALLNAME:TARGET%的最深处…
		CASE 2,3
			PRINTFORML 慢慢推开由柔肉形成的紧致膣壁、把%TINKO()%插入到了%CALLNAME:TARGET%的最深处…
		CASEELSE
			PRINTFORML 即使不怎么用力、%CALLNAME:TARGET%的已经融化的媚肉也非常高兴的把%CALLNAME:MASTER%带到了最深处…
	ENDSELECT
	PRINTFORML 被温暖紧致的肉襞所包裹、被合适的场所容纳的%CALLNAME:MASTER%的%TINKO()%因为愉悦而微微颤抖
ENDIF