
@MESSAGE_COM61通常
;背面立位
IF TFLAG:193
	;抜かず
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%\@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 拽住%CALLNAME:TARGET%的雙手 # 抓住%CALLNAME:TARGET%的屁股 \@、
			PRINTFORML 为了将精液注入膣内開始激烈地动起了腰…
		ELSEIF TCVAR:102
			PRINTFORML 在%CALLNAME:TARGET%的膣内射入精液之后、
			PRINTFORML 趁着%TINKO(PLAYER)%尚未失去硬度再次開始动起了腰…
		ELSEIF TEQUIP:体位２ == 2
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着%CALLNAME:TARGET%膣内的触感的同时、再次開始动起了腰…
			;穿戴式假陰茎を使用している場合
			ELSE
				PRINTFORML 没有从%CALLNAME:TARGET%的膣内拔出、再次開始动起了腰…
			ENDIF
		ELSE
			IF TEQUIP:体位２ == 2
				PRINTFORML 没有从%CALLNAME:TARGET%的膣内拔出%TINKO(PLAYER)%并再次開始动起了腰…
			;対面立位から
			ELSE
				PRINTFORML 暂时从%CALLNAME:TARGET%体内拔出%TINKO(PLAYER)%将其扶住墻壁
				PRINTFORML 从后面插入进去并再次開始动起了腰…
			ENDIF
		ENDIF
	ELSE
		IF TALENT:処女 == 1
			PRINTFORML \@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 從後面抓住雙手讓%CALLNAME:TARGET% # 讓扶著墻壁的%CALLNAME:TARGET% \@抬高屁股
			PRINTFORML 没尝过男人滋味的\@ TALENT:体型 < 0 ? 幼小裂缝 # 秘裂 \@里将%TINKO(PLAYER)%贯穿了进去…
		ELSEIF MASTER_POSE(SET_V,SET_P,1) && MASTER_POSE(SET_V,SET_P,1) != TARGET
			PRINTFORML 從%CALLNAME:(MASTER_POSE(SET_V,SET_P,1))%体内拔出%TINKO(PLAYER)%的同時
			PRINTFORML 插入了%CALLNAME:TARGET%…
		ELSE
			IF TEQUIP:0 & 2
				PRINTFORM 拨開%SHOW_BOTTOM(TARGET,2)%
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 掀起%SHOW_BOTTOM(TARGET,1)%
			ENDIF
			PRINTFORM \@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 抓住%CALLNAME:TARGET%伸出的雙手 # 讓%CALLNAME:TARGET%的両手扶著墻壁 \@、從後面挿入到了
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充分湿润的
			;矮小体型
			IF TALENT:体型 < 0
				PRINT 幼小裂縫
			ELSE
				PRINT 秘裂
			ENDIF
			PRINTFORML 的最深处…
		ENDIF
	ENDIF
ELSE
;抜かず
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抓住四肢着地趴下的%CALLNAME:TARGET%的腰、
			PRINTFORML 为了将精液注入膣内開始激烈地动起了腰…
		ELSEIF TCVAR:102
			PRINTFORML 在%CALLNAME:TARGET%的膣内射入精液之后、
			PRINTFORML 趁着%TINKO(PLAYER)%尚未失去硬度再次開始动起了腰…
		ELSEIF TEQUIP:体位２ == 2
			;調教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着%CALLNAME:TARGET%膣内的触感的同时、再次開始动起了腰…
			;穿戴式假陰茎を使用している場合
			ELSE
				PRINTFORML 没有从%CALLNAME:TARGET%的膣内拔出、再次開始动起了腰…
			ENDIF
		ELSE
			PRINTFORML 保持着挿入状态让%CALLNAME:TARGET%四肢着地趴下、再次開始动起了腰…
		ENDIF
	ELSE
		IF TALENT:処女 == 1
			PRINTFORML 将四肢着地趴下的%CALLNAME:TARGET%的屁股高高托起
			PRINTFORML 没尝过男人滋味的\@ TALENT:体型 < 0 ? 幼小裂缝 # 秘裂 \@里将%TINKO(PLAYER)%贯穿了进去…
		ELSEIF MASTER_POSE(SET_V,SET_P,1) && MASTER_POSE(SET_V,SET_P,1) != TARGET
			PRINTFORML 從%CALLNAME:(MASTER_POSE(SET_V,SET_P,1))%体内拔出%TINKO(PLAYER)%的同時
			PRINTFORML 插入了%CALLNAME:TARGET%…
		ELSE
			PRINTFORM 対四肢着地趴下的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨開%SHOW_BOTTOM(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%挿入
			;潤滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充分湿润的
			;矮小体型
			IF TALENT:体型 < 0
				PRINT 幼小裂缝
			ELSE
				PRINT 秘裂
			ENDIF
			PRINTFORML 的最深处…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;322,对方主導後背位
;-------------------------------------------------
@MESSAGE_COM322
IF TALENT:処女 == 1
	PRINTFORML 按照%CALLNAME:TARGET%的期望、把亀頭対準了那不识男人滋味的\@ TALENT:体型 < 0 ? 幼小裂缝 # 秘裂 \@、
	PRINTFORML 慢慢地撐開緊閉的秘唇并插了進去、撕裂了純潔之証…
		SIF TALENT:恋慕
			PRINTFORML %CALLNAME:TARGET%一边忍耐着痛楚一边紧紧抱住了%CALLNAME:PLAYER%…
ELSE
	PRINTFORML %CALLNAME:MASTER%壓在%CALLNAME:TARGET%身上、慢慢地把亀頭挿入了因為欲求而微顫的膣口…
	SELECTCASE ABL:TARGET:膣
		CASE 0
			PRINTFORML %TINKO()%撐開了狹窄的膣肉一點點前進、侵入到了%CALLNAME:TARGET%的最深処…
		CASE 1
			PRINTFORML 撐開還是十分緊窄的膣肉、把%TINKO()%挿入到了%CALLNAME:TARGET%的最深処…
		CASE 2,3
			PRINTFORML 慢慢推開由柔肉形成的緊緻膣壁、把%TINKO()%挿入到了%CALLNAME:TARGET%的最深処…
		CASEELSE
			PRINTFORML 即使不怎麼用力、%CALLNAME:TARGET%的已經融化的媚肉也非常高興的把%CALLNAME:MASTER%帯到了最深処…
	ENDSELECT
	PRINTFORML 被溫暖緊緻的肉襞所包裹、被合適的場所容納的%CALLNAME:MASTER%的%TINKO()%因為愉悅而微微顫抖
ENDIF