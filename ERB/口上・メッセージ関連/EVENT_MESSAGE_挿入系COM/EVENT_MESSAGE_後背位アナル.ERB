;-------------------------------------------------
;63,後背位肛交
;-------------------------------------------------
@MESSAGE_COM63
IF (FLAG:70 == 1 && CFLAG:TARGET:344 == 0)
	IF TFLAG:193
		;连续
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;调教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着毫无抵抗的%CALLNAME:TARGET%肛门的触感的同时、再次开始动起了腰…
			;穿戴式假阴茎を使用している场合
			ELSE
				PRINTFORML 没有从毫无抵抗的%CALLNAME:TARGET%的肠内拔出、再次开始动起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:体位２ == 2
				PRINTFORML 保持着对毫无抵抗的%CALLNAME:TARGET%肛门的插入再次开始动起了腰…
			;对面立位から
			ELSE
				PRINTFORML 暂时将%TINKO(PLAYER)%从%CALLNAME:TARGET%体内拔出并绕到后面
				PRINTFORML 将屁股摆成突出来的姿势再次开始侵犯起了%CALLNAME:TARGET%的肛门…
			ENDIF
		ELSE
			PRINTFORM 时停中的被摆成屁股突出来的姿势的%CALLNAME:TARGET%的
			IF TEQUIP:0 & 2
				PRINTFORM 拨开%SHOW_下半身内衣(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 把%SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%插入
			;润滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充满黏液的
			PRINTFORML 肛门的最深处…
		ENDIF
	ELSE
		;连续
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;调教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着毫无抵抗的%CALLNAME:TARGET%肛门的触感的同时、再次开始动起了腰…
			;穿戴式假阴茎を使用している场合
			ELSE
				PRINTFORML 没有从毫无抵抗的%CALLNAME:TARGET%的肠内拔出、再次开始动起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 保持着插入让%CALLNAME:TARGET%四肢着地趴下、再次开始动起了腰…
		ELSE
			PRINTFORM 对时停中被摆成四肢着地趴下姿势的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨开%SHOW_下半身内衣(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 把%SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%插入
			;润滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充满黏液的
			PRINTFORML 肛门的最深处…
		ENDIF
	ENDIF
ELSEIF CFLAG:MASTER:恶作剧 == 2
	IF TFLAG:193
		;连续
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;调教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着睡眠中的%CALLNAME:TARGET%肛门的触感的同时、再次开始动起了腰…
			;穿戴式假阴茎を使用している场合
			ELSE
				PRINTFORML 没有从睡眠中的%CALLNAME:TARGET%的肠内拔出、再次开始动起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:体位２ == 2
				PRINTFORML 保持着对睡眠中的%CALLNAME:TARGET%肛门的插入再次开始动起了腰…
			;对面立位から
			ELSE
				PRINTFORML 暂时将%TINKO(PLAYER)%从%CALLNAME:TARGET%体内拔出并绕到后面
				PRINTFORML 将屁股摆成突出来的姿势再次开始侵犯起了%CALLNAME:TARGET%的肛门…
			ENDIF
		ELSE
			PRINTFORM 对睡眠中被摆成了凸出臀部姿势的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨开%SHOW_下半身内衣(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 把%SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%插入
			;润滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充满黏液的
			PRINTFORML 肛门的最深处…
		ENDIF
	ELSE
		;连续
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;调教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着睡眠中的%CALLNAME:TARGET%肛门的触感的同时、再次开始动起了腰…
			;穿戴式假阴茎を使用している场合
			ELSE
				PRINTFORML 没有从睡眠中的%CALLNAME:TARGET%的肠内拔出、再次开始动起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 保持着插入让%CALLNAME:TARGET%趴着、再次开始动起了腰…
		ELSE
			PRINTFORM 对着趴着睡眠中的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨开%SHOW_下半身内衣(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 把%SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%插入
			;润滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充满黏液的
			PRINTFORML 肛门的最深处…
		ENDIF
	ENDIF
ELSE
	IF TFLAG:193
		;连续
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;调教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着%CALLNAME:TARGET%肛门的触感的同时、再次开始动起了腰…
			;穿戴式假阴茎を使用している场合
			ELSE
				PRINTFORML 没有从%CALLNAME:TARGET%的肠内拔出、再次开始动起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:体位２ == 2
				PRINTFORML 保持着对%CALLNAME:TARGET%肛门的插入再次开始动起了腰…
			;对面立位から
			ELSE
				PRINTFORML 暂时从%CALLNAME:TARGET%体内拔出%TINKO(PLAYER)%并\@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 抓住%CALLNAME:TARGET%伸出的双手 # 让%CALLNAME:TARGET%扶住墻壁 \@
				PRINTFORML 从后面插入进去并再次开始动起了腰…
			ENDIF
		ELSE
			IF TEQUIP:0 & 2
				PRINTFORM 拨开%SHOW_下半身内衣(TARGET,2)%
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 掀起%SHOW_BOTTOM(TARGET,1)%
			ENDIF
			PRINTFORM %CALLNAME:TARGET%
			PRINTFORM \@ OPENPLACE(CFLAG:MASTER:現在位置) == 2 ? 抓住伸出的双手 # 的两手扶着墻壁 \@、从后面插入到了
			;润滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充满黏液的
			PRINTFORML 肛门的最深处…
		ENDIF
	ELSE
		;连续
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;调教者についてる
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 在品味着%CALLNAME:TARGET%肛门的触感的同时、再次开始动起了腰…
			;穿戴式假阴茎を使用している场合
			ELSE
				PRINTFORML 没有从%CALLNAME:TARGET%的肠内拔出、再次开始动起了腰…
			ENDIF
		;抜かず
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 保持着插入状态让%CALLNAME:TARGET%四肢着地趴下、再次开始动起了腰…
		ELSE
			PRINTFORM 对四肢着地趴下的%CALLNAME:TARGET%
			IF TEQUIP:0 & 2
				PRINTFORM 拨开%SHOW_下半身内衣(TARGET,2)%、
			ELSEIF TEQUIP:0 & 1
				PRINTFORM 把%SHOW_BOTTOM(TARGET,1)%掀起来、
			ENDIF
			PRINTFORM 从后面用%TINKO(PLAYER)%插入
			;润滑LV4以上
			SIF PALAM:潤滑 >= PALAMLV:4
				PRINT 充满黏液的
			PRINTFORML 肛门的最深处…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;323,对方主导后背位Ａ
;-------------------------------------------------
@MESSAGE_COM323
PRINTFORML %CALLNAME:MASTER%从后面压在%CALLNAME:TARGET%身上、把龟头对准了\@ ABL:肛門 > 2? 因为欲求而微颤的# \@肛门、慢慢地沉下了腰……
PRINTFORML %CALLNAME:TARGET%仔细品味着插入感并发出了娇喘……