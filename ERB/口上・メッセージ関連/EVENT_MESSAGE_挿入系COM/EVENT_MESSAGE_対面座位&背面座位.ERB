;-------------------------------------------------
;67,対面座位
;-------------------------------------------------
@MESSAGE_COM67
IF FLAG:70 == 1
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抱住時停中的%CALLNAME:TARGET%、
			PRINTFORML 在感受著%CALLNAME:TARGET%的氣味的同时、顺从着上涌的衝動向上耸动起了腰部…
		ELSEIF TEQUIP:体位２ == 4
			PRINTFORML %CALLNAME:PLAYER%吻向了時停中的%CALLNAME:TARGET%、在舌头交织在一起的同时再次開始摇晃起了腰身…
		ELSE
			PRINTFORM 沒有從膣内拔出%TINKO(PLAYER)%并摟住了時停中的%CALLNAME:TARGET%的身体、
			PRINTFORML 一邊品嘗著口內的滋味一邊再次開始搖晃起腰身…
		ENDIF
	ELSE
		PRINTFORM 抱起時停中的%CALLNAME:TARGET%的身体、
		PRINTFORM 用%TINKO(PLAYER)%対準
		SIF TEQUIP:0 & 2
			PRINTFORM 從拨開的%SHOW_BOTTOM(TARGET,2)%的空隙中露出的
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充分湿润的
		;矮小体型
		IF TALENT:体型 < 0
			PRINT 幼小肉穴
		ELSE
			PRINT 膣穴
		ENDIF
		PRINTFORM 挿入進去、
		;口球の有無
		SIF TEQUIP:口球 == 0
			PRINT 一邊接吻一邊
		PRINTL 把%CALLNAME:TARGET%當做飛機杯如同在自慰一般搖動起腰身…
	ENDIF
ELSE
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抱住%CALLNAME:TARGET%、
			PRINTFORML 顺从着上涌的衝動向上耸动起了腰部…
		ELSEIF TEQUIP:体位２ == 4
			PRINTFORML %CALLNAME:TARGET%亲吻着%CALLNAME:PLAYER%、在舌头交织在一起的同时再次開始摇晃起了腰身…
		ELSE
			PRINTFORM 沒有從膣内拔出%TINKO(PLAYER)%并緊緊地抱住了%CALLNAME:TARGET%的身体、
			PRINTFORML 在舌头交织在一起的同时再次開始摇晃起了腰身…
		ENDIF
	ELSE
		PRINTFORM 抱起%CALLNAME:TARGET%的身体、
		PRINTFORM 慢慢放下的同時挿入了
		SIF TEQUIP:0 & 2
			PRINTFORM 從拨開的%SHOW_BOTTOM(TARGET,2)%的空隙中露出的
		PRINTFORM %SLIT(, 1)%挿入进去、
		;口球の有無
		SIF TEQUIP:口球 == 0
			PRINT 一邊接吻一邊
		PRINTL 摇动起了腰身…
	ENDIF
	;SIF TALENT:恋慕
	;	PRINTFORML %CALLNAME:TARGET%用手环抱住了%CALLNAME:PLAYER%的脖子…
ENDIF
;-------------------------------------------------
;68,背面座位
;-------------------------------------------------
@MESSAGE_COM68
IF FLAG:70 == 1
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抱住時停中的%CALLNAME:TARGET%、
			PRINTFORML 在感受著%CALLNAME:TARGET%的氣味的同时、顺从着上涌的衝動向上耸动起了腰部…
		ELSEIF TEQUIP:体位２ == 5
			PRINTFORML 從背後抱住時停中的%CALLNAME:TARGET%、再次開始摇晃起了%CALLNAME:TARGET%的身体…
		ELSE
			PRINTFORML 沒有從膣内拔出%TINKO(PLAYER)%、并從背後抱住了時停中的%CALLNAME:TARGET%的身体、
			PRINTFORML 搖動起身體使用%CALLNAME:TARGET%再次開始了自慰…
		ENDIF
	ELSE
		PRINTFORM 抱起時停中的%CALLNAME:TARGET%的身体、%SLIT(, 1)%
		IF TEQUIP:0 & 2
			PRINTFORML %SHOW_BOTTOM(TARGET,2)%的空隙中挿入了%SLIT()%…
		ELSE
			PRINTFORML 用准备完畢的%TINKO(PLAYER)%貫入了%SLIT()%…
		ENDIF
	ENDIF
ELSE
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抱住%CALLNAME:TARGET%、
			PRINTFORML 顺从着上涌的衝動向上耸动起了腰部…
		ELSEIF TEQUIP:体位２ == 5
			PRINTFORML 從背後抱住%CALLNAME:TARGET%、再次開始摇晃起了%CALLNAME:TARGET%的身体…
		ELSE
			PRINTFORML 沒有從膣内拔出%TINKO(PLAYER)%并從背后抱住了%CALLNAME:TARGET%的身体、開始摇晃起了身体…
		ENDIF
	ELSE
		PRINTFORM 抱起%CALLNAME:TARGET%的身体、%SLIT(, 1)%
		IF TEQUIP:0 & 2
			PRINTFORML %SHOW_BOTTOM(TARGET,2)%的空隙慢慢地插入了%SLIT()%…
		ELSE
			PRINTFORML 将准备完畢的陽具慢慢的贯入了%SLIT()%…
		ENDIF
	ENDIF
	;SIF TALENT:恋慕
	;	PRINTFORML %CALLNAME:TARGET%向后仰着身体靠在%CALLNAME:PLAYER%身上…
ENDIF
;-------------------------------------------------
;69,対面座位肛交
;-------------------------------------------------
@MESSAGE_COM69
IF FLAG:70 == 1
	;連続
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %CALLNAME:PLAYER%吻向了時停中的%CALLNAME:TARGET%、在舌头交织在一起的同时再次開始摇晃起了腰身…
		RETURN 0
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 沒有從腸内拔出
		;調教者についてる
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%を
		ELSE
			PRINTFORM 假陰茎
		ENDIF
			PRINTFORM 并從背後抱住了%CALLNAME:TARGET%的身体、
			PRINTFORML 搖動起身體使用%CALLNAME:TARGET%再次開始了自慰…
	ELSE
		PRINTFORM 抱起時停中的%CALLNAME:TARGET%的身体
		SIF TEQUIP:0 & 2
			PRINTFORM 强行拨開%SHOW_BOTTOM(TARGET,2)%
		PRINTFORM 用%TINKO(PLAYER)%対準
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充满粘液的
		PRINTFORM 菊穴挿入進去、
	ENDIF
	;口球の有無
	SIF TEQUIP:口球 == 0
		PRINT 一邊接吻一邊
	PRINTL 為了使用%CALLNAME:TARGET%的肛門自慰而摇动起了腰身…
ELSE
	;連続
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %CALLNAME:TARGET%亲吻着%CALLNAME:PLAYER%、在舌头交织在一起的同时再次開始摇晃起了腰身…
		RETURN 0
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 沒有從腸内拔出
		;調教者についてる
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%を
		ELSE
			PRINTFORM 假陰茎
		ENDIF
			PRINTFORM 并緊緊抱住了%CALLNAME:TARGET%的身体、
	ELSE
		PRINTFORM 抱起%CALLNAME:TARGET%的身体
		SIF TEQUIP:0 & 2
			PRINTFORM 强行拨開%SHOW_BOTTOM(TARGET,2)%
		PRINTFORM 慢慢放下的同時挿入了
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充满粘液的
		PRINTFORM 肛門、
	ENDIF
	;口球の有無
	SIF TEQUIP:口球 == 0
		PRINT 一邊接吻一邊
	PRINTL 摇动起了腰身…
	;陥落済み
	SIF TALENT:恋慕
		PRINTFORML %CALLNAME:TARGET%用手环抱住了%CALLNAME:PLAYER%的脖子…
ENDIF

;-------------------------------------------------
;70,背面座位肛交
;-------------------------------------------------
@MESSAGE_COM70
IF FLAG:70 == 1
	;連続
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML 從背後抱住時停中的%CALLNAME:TARGET%、再次開始摇晃起了%CALLNAME:TARGET%的身体…
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 沒有從腸内拔出
		;調教者についてる
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%を
		ELSE
			PRINTFORM 假陰茎
		ENDIF
		PRINTFORML 、并從背後抱住了時停中的%CALLNAME:TARGET%的身体、
		PRINTFORML 搖動起身體使用%CALLNAME:TARGET%再次開始了自慰…
	ELSE
		PRINTFORML 抱起時停中的%CALLNAME:TARGET%的身体、
		SIF TEQUIP:0 & 2
			PRINTFORM 從拨開的%SHOW_BOTTOM(TARGET,2)%的空隙中
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充满粘液的
		PRINTFORML 用准备完畢的%TINKO(PLAYER)%慢慢貫入了尻穴…
	ENDIF
ELSE
	;連続
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML 從背後抱住%CALLNAME:TARGET%、再次開始摇晃起了%CALLNAME:TARGET%的身体…
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 沒有從腸内拔出
		;調教者についてる
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%を
		ELSE
			PRINTFORM 假陰茎
		ENDIF
		PRINTFORML 并從背後抱住了%CALLNAME:TARGET%的身体、開始摇晃起了身体…
	ELSE
		PRINTFORML 抱起%CALLNAME:TARGET%的身体、
		SIF TEQUIP:0 & 2
			PRINTFORM 從拨開的%SHOW_BOTTOM(TARGET,2)%的空隙中
		;潤滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充满粘液的
		PRINTFORML 将准备完畢的陽具慢慢的贯入了肛門…
	ENDIF
	;陥落済み
	SIF TALENT:恋慕
		PRINTFORML %CALLNAME:TARGET%向后仰着身体靠在%CALLNAME:PLAYER%身上…
ENDIF