;-------------------------------------------------
;67,对面座位
;-------------------------------------------------
@MESSAGE_COM67
IF (FLAG:70 == 1 && CFLAG:TARGET:344 == 0)
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抱住时停中的%CALLNAME:TARGET%、
			PRINTFORML 在感受着%CALLNAME:TARGET%的气味的同时、顺从着上涌的冲动向上耸动起了腰部…
		ELSEIF TEQUIP:体位２ == 4
			PRINTFORML %CALLNAME:PLAYER%吻向了时停中的%CALLNAME:TARGET%、在舌头交织在一起的同时再次开始摇晃起了腰身…
		ELSE
			PRINTFORM 没有从膣内拔出%TINKO(PLAYER)%并搂住了时停中的%CALLNAME:TARGET%的身体、
			PRINTFORML 一边品尝着口内的滋味一边再次开始摇晃起腰身…
		ENDIF
	ELSE
		PRINTFORM 抱起时停中的%CALLNAME:TARGET%的身体、
		PRINTFORM 用%TINKO(PLAYER)%对准
		SIF TEQUIP:0 & 2
			PRINTFORM 从拨开的%SHOW_下半身内衣(TARGET,2)%的空隙中露出的
		;润滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充分湿润的
		;矮小体型
		IF TALENT:体型 < 0
			PRINT 幼小肉穴
		ELSE
			PRINT 膣穴
		ENDIF
		PRINTFORM 插入进去、
		;口球の有无
		SIF TEQUIP:口球 == 0
			PRINT 一边接吻一边
		PRINTFORML 把%CALLNAME:TARGET%当做飞机杯如同在自慰一般摇动起腰身…
	ENDIF
ELSE
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抱住%CALLNAME:TARGET%、
			PRINTFORML 顺从着上涌的冲动向上耸动起了腰部…
		ELSEIF TEQUIP:体位２ == 4
			PRINTFORML %CALLNAME:TARGET%亲吻着%CALLNAME:PLAYER%、在舌头交织在一起的同时再次开始摇晃起了腰身…
		ELSE
			PRINTFORM 没有从膣内拔出%TINKO(PLAYER)%并紧紧地抱住了%CALLNAME:TARGET%的身体、
			PRINTFORML 在舌头交织在一起的同时再次开始摇晃起了腰身…
		ENDIF
	ELSE
		PRINTFORM 抱起%CALLNAME:TARGET%的身体、
		PRINTFORM 慢慢放下的同时插入了
		SIF TEQUIP:0 & 2
			PRINTFORM 从拨开的%SHOW_下半身内衣(TARGET,2)%的空隙中露出的
		PRINTFORM %SLIT(, 1)%插入进去、
		;口球の有无
		SIF TEQUIP:口球 == 0
			PRINT 一边接吻一边
		PRINTL 摇动起了腰身…
	ENDIF
	;SIF TALENT:恋慕
	;	PRINTFORML %CALLNAME:TARGET%用手环抱住了%CALLNAME:PLAYER%的脖子…
ENDIF
;-------------------------------------------------
;68,背面座位
;-------------------------------------------------
@MESSAGE_COM68
IF (FLAG:70 == 1 && CFLAG:TARGET:344 == 0)
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抱住时停中的%CALLNAME:TARGET%、
			PRINTFORML 在感受着%CALLNAME:TARGET%的气味的同时、顺从着上涌的冲动向上耸动起了腰部…
		ELSEIF TEQUIP:体位２ == 5
			PRINTFORML 从背后抱住时停中的%CALLNAME:TARGET%、再次开始摇晃起了%CALLNAME:TARGET%的身体…
		ELSE
			PRINTFORML 没有从膣内拔出%TINKO(PLAYER)%、并从背后抱住了时停中的%CALLNAME:TARGET%的身体、
			PRINTFORML 摇动起身体使用%CALLNAME:TARGET%再次开始了自慰…
		ENDIF
	ELSE
		PRINTFORM 抱起时停中的%CALLNAME:TARGET%的身体、%SLIT(, 1)%
		IF TEQUIP:0 & 2
			PRINTFORML %SHOW_下半身内衣(TARGET,2)%的空隙中插入了%SLIT()%…
		ELSE
			PRINTFORML 用准备完毕的%TINKO(PLAYER)%贯入了%SLIT()%…
		ENDIF
	ENDIF
ELSE
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:射精
			PRINTFORML %CALLNAME:PLAYER%抱住%CALLNAME:TARGET%、
			PRINTFORML 顺从着上涌的冲动向上耸动起了腰部…
		ELSEIF TEQUIP:体位２ == 5
			PRINTFORML 从背后抱住%CALLNAME:TARGET%、再次开始摇晃起了%CALLNAME:TARGET%的身体…
		ELSE
			PRINTFORML 没有从膣内拔出%TINKO(PLAYER)%并从背后抱住了%CALLNAME:TARGET%的身体、开始摇晃起了身体…
		ENDIF
	ELSE
		PRINTFORM 抱起%CALLNAME:TARGET%的身体、%SLIT(, 1)%
		IF TEQUIP:0 & 2
			PRINTFORML %SHOW_下半身内衣(TARGET,2)%的空隙慢慢地插入了%SLIT()%…
		ELSE
			PRINTFORML 将准备完毕的阳具慢慢的贯入了%SLIT()%…
		ENDIF
	ENDIF
	;SIF TALENT:恋慕
	;	PRINTFORML %CALLNAME:TARGET%向后仰着身体靠在%CALLNAME:PLAYER%身上…
ENDIF
;-------------------------------------------------
;69,对面座位肛交
;-------------------------------------------------
@MESSAGE_COM69
IF (FLAG:70 == 1 && CFLAG:TARGET:344 == 0)
	;连续
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %CALLNAME:PLAYER%吻向了时停中的%CALLNAME:TARGET%、在舌头交织在一起的同时再次开始摇晃起了腰身…
		RETURN 0
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 没有从肠内拔出
		;调教者についてる
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%
		ELSE
			PRINTFORM 假阴茎
		ENDIF
			PRINTFORM 并从背后抱住了%CALLNAME:TARGET%的身体、
			PRINTFORML 摇动起身体使用%CALLNAME:TARGET%再次开始了自慰…
	ELSE
		PRINTFORM 抱起时停中的%CALLNAME:TARGET%的身体
		SIF TEQUIP:0 & 2
			PRINTFORM 强行拨开%SHOW_下半身内衣(TARGET,2)%
		PRINTFORM 用%TINKO(PLAYER)%对准
		;润滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充满黏液的
		PRINTFORM 菊穴插入进去、
	ENDIF
	;口球の有无
	SIF TEQUIP:口球 == 0
		PRINT 一边接吻一边
	PRINTFORML 为了使用%CALLNAME:TARGET%的肛门自慰而摇动起了腰身…
ELSE
	;连续
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %CALLNAME:TARGET%亲吻着%CALLNAME:PLAYER%、在舌头交织在一起的同时再次开始摇晃起了腰身…
		RETURN 0
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 没有从肠内拔出
		;调教者についてる
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%
		ELSE
			PRINTFORM 假阴茎
		ENDIF
			PRINTFORM 并紧紧抱住了%CALLNAME:TARGET%的身体、
	ELSE
		PRINTFORM 抱起%CALLNAME:TARGET%的身体
		SIF TEQUIP:0 & 2
			PRINTFORM 强行拨开%SHOW_下半身内衣(TARGET,2)%
		PRINTFORM 慢慢放下的同时插入了
		;润滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充满黏液的
		PRINTFORM 肛门、
	ENDIF
	;口球の有无
	SIF TEQUIP:口球 == 0
		PRINT 一边接吻一边
	PRINTL 摇动起了腰身…
	;陷落済み
	SIF TALENT:恋慕
		PRINTFORML %CALLNAME:TARGET%用手环抱住了%CALLNAME:PLAYER%的脖子…
ENDIF

;-------------------------------------------------
;70,背面座位肛交
;-------------------------------------------------
@MESSAGE_COM70
IF (FLAG:70 == 1 && CFLAG:TARGET:344 == 0)
	;连续
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML 从背后抱住时停中的%CALLNAME:TARGET%、再次开始摇晃起了%CALLNAME:TARGET%的身体…
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 没有从肠内拔出
		;调教者についてる
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%
		ELSE
			PRINTFORM 假阴茎
		ENDIF
		PRINTFORML 、并从背后抱住了时停中的%CALLNAME:TARGET%的身体、
		PRINTFORML 摇动起身体使用%CALLNAME:TARGET%再次开始了自慰…
	ELSE
		PRINTFORML 抱起时停中的%CALLNAME:TARGET%的身体、
		SIF TEQUIP:0 & 2
			PRINTFORM 从拨开的%SHOW_下半身内衣(TARGET,2)%的空隙中
		;润滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充满黏液的
		PRINTFORML 用准备完毕的%TINKO(PLAYER)%慢慢贯入了尻穴…
	ENDIF
ELSE
	;连续
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML 从背后抱住%CALLNAME:TARGET%、再次开始摇晃起了%CALLNAME:TARGET%的身体…
	;抜かず
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 没有从肠内拔出
		;调教者についてる
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%
		ELSE
			PRINTFORM 假阴茎
		ENDIF
		PRINTFORML 并从背后抱住了%CALLNAME:TARGET%的身体、开始摇晃起了身体…
	ELSE
		PRINTFORML 抱起%CALLNAME:TARGET%的身体、
		SIF TEQUIP:0 & 2
			PRINTFORM 从拨开的%SHOW_下半身内衣(TARGET,2)%的空隙中
		;润滑LV4以上
		SIF PALAM:潤滑 >= PALAMLV:4
			PRINT 充满黏液的
		PRINTFORML 将准备完毕的阳具慢慢的贯入了肛门…
	ENDIF
	;陷落済み
	SIF TALENT:恋慕
		PRINTFORML %CALLNAME:TARGET%向后仰着身体靠在%CALLNAME:PLAYER%身上…
ENDIF