; 看不懂说明不需要使用以下模板，以免造成无法预料的错误
; 在每条指令本体函数@COMXX执行之前触发
;@M_KOJO_EVENT_K77_EVENTCOM(ARGS,ARG,ARG:1)
; 在此编写你的代码
	
; 在指令回合结束之后触发，但比其他的 EVENTCOMEND 触发更早执行（高优先级）
;@M_KOJO_EVENT_K77_EVENTCOMEND_PRI(ARGS,ARG,ARG:1)
; 在此编写你的代码

; 在指令回合结束之后触发
@M_KOJO_EVENT_K77_EVENTCOMEND(ARGS,ARG,ARG:1)
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM FAVOR_CHANGE
#DIM TRUST_CHANGE
SIF CFLAG:77:998 || TFLAG:現在的TARGET != 77
	RETURN 0
FAVOR_CHANGE = CFLAG:77:好感度 - CFLAG:77:PREV_FAVOR
TRUST_CHANGE = CFLAG:77:信賴度 - CFLAG:77:PREV_TRUST

IF FLAG:時間停止

ELSEIF !SHIRAHU(77)

ELSEIF CFLAG:77:诶嘿嘿

ELSEIF IS_ANGRY(77)

ELSEIF BATHCHECK(CFLAG:MASTER:300)

ELSE
	SIF TFLAG:约会道中 && CFLAG:77:好感度 > 100 && FIRSTTIME("K77_GETCAMERA",,77)
		CALL K77_GETCAMERA
	IF GROUPMATCH(SELECTCOM, 301, 331) && INROOM(CFLAG:MASTER:300) && !GETBIT(TCVAR:77:MAKING_WEIRDTEA, 0)
		SIF TALENT:77:恋人 && K77_PROB(3, 7)
			CALL K77_MAKING_WEIRDTEA_SHIZUHA(1)
		SIF TALENT:77:恋慕 && K77_PROB(2, 7)
			CALL K77_MAKING_WEIRDTEA_SHIZUHA(1)
	ENDIF
ENDIF

; 功能与事件31(@EVENTTRAIN内触发）重叠，但不要求玩家已遇见该角色，必然在@EVENTTRAIN内触发
; 在@AFTERTRAIN之前触发，用于清除后续的标志位等
;@M_KOJO_EVENT_K77_AFTERTRAIN_BEGIN(ARGS,ARG,ARG:1)
; 在此编写你的代码

; 在@AFTERTRAIN相关处理结束时触发
;@M_KOJO_EVENT_K77_AFTERTRAIN_END(ARGS,ARG,ARG:1)
; 在此编写你的代码

; 功能与@K77_BEFORETRAIN(@BEFORETRAIN内触发）重叠
; 在@BEFORETRAIN相关处理结束时触发，用于清除@K77_BEFORETRAIN无法影响的标志等
;@M_KOJO_EVENT_K77_BEFORETRAIN_END(ARGS,ARG,ARG:1)
; 在此编写你的代码

; 当月份变更（新月份开始）时触发
;@M_KOJO_EVENT_K77_MONTH_CHANGE(ARGS,ARG,ARG:1)
; 在此编写你的代码