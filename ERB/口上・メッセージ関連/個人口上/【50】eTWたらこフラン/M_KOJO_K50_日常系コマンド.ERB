;※※※※※※※※※※※※※※※※※※※※※※※※※
;日常系指令
;※※※※※※※※※※※※※※※※※※※※※※※※※
;==================================================
;300,会話
;==================================================
@M_KOJO_MESSAGE_COM_K50_300
;指令共通メッセージ(TRAIN_MESSAGE) 
;削除すると「～说着话。」とかが消えます
CALL TRAIN_MESSAGE
;口上メイン呼出
CALL M_KOJO_MESSAGE_COM_K50_300_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_300_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;减少になってもいいさと考えるんだ
	;IF !CFLAG:1502 && CFLAG:信賴度 >= 500 && ABL:教養 > 2 && MONEY >= 300000
	;IF !CFLAG:1502 && CFLAG:信賴度 >= 500 && ABL:教養 > 2
	;	TRYCALLFORM M_KOJO_MESSAGE_COM_K50_進学
	;	RETURN 1
	;ENDIF
	
	IF 家庭菜園可(CFLAG:MASTER:現在位置) && !GETBIT (TCVAR:350,1)
		SETBIT TCVAR:350,1
		;SELECTCASE PLANT_TYPE_0
		SELECTCASE PLANT_TYPE
			CASE 151
				PRINTFORMW 「啊，卷心菜田。」
				PRINTFORML 「竟然把家庭菜园弄成这样。%UNICODE(0x2661)%」
				PRINTFORMW 「哥哥，是想要孩子嗎？」
				IF CFLAG:允许无套 <= 2 && TALENT:恋人
					IF CFLAG:16:信賴度 < 500
						PRINTFORML 「但是姐姐大人說不行哦」
					ELSE
						PRINTFORML 
						PRINTFORML 「好啊」
						PRINTFORMW 「因為%CALLNAME:TARGET%也想要哥哥的寶寶」
						PRINTFORMW 「诶嘿嘿！来啊哥哥大人！在卷心菜田里抓住我！」
						CFLAG:允许无套 = 2
					ENDIF
				ELSE
					PRINTDATAL
						DATALIST
							DATAFORM 「%CALLNAME:TARGET%覺得为时过早」
							;ばっちゃに語呂を合わせぱっちぇ呼称
							DATAFORM 「如果不來紅豆飯的話，那就不可能了。帕秋莉是这么说的」
						ENDLIST
						DATALIST
							DATAFORM 「哥哥大人、拜託慢慢来吧」
							DATAFORM 「不要勉强啊，诶嘿嘿♪」
						ENDLIST
						DATALIST
							DATAFORM 「想・要・吧・？兄・長・大・人？」
							DATAFORM 「被%CALLNAME:TARGET%看穿了」
							DATAFORM 「誒嘿嘿。現在還不行%UNICODE(0x2661)%」
						ENDLIST
					ENDDATA
				ENDIF
			CASE 163
				PRINTFORML 「姐姐大人以前也是蓝色的吗」
			CASE 164
				PRINTFORML 「西瓜先生～西瓜先生～好大啊ー西瓜先生～」
				PRINTFORML 「大家一起吃西瓜先生吧！」
				PRINTFORMDW %CALLNAME:TARGET%突然笑了
			CASE 177
				PRINTFORML 「把这葡萄摘下来就做红酒吧」
				PRINTFORML 「%CALLNAME:TARGET%呢。想要去踩葡萄」
				PRINTFORML 「誒嘿嘿。好期待啊♪」
				PRINTFORMD %CALLNAME:TARGET%把裙子卷了起來
				
				IF TALENT:MASTER:受虐狂
					PRINTFORMDL 于是、%CALLNAME:MASTER%把目光移到踩踏的大腿之間，股間隆起了
				ELSE
					PRINTFORMDL 一副欢快的样子
				ENDIF
			CASE 187
				PRINTFORML 「百合～♪小百合～♪」
				PRINTFORML 「呐呐兄長大人。百合的根部可以吃喲！」
				SIF TALENT:MASTER:性別 == 3
					PRINTFORMDL %CALLNAME:TARGET%看著%CALLNAME:MASTER%的胯下的眼神很奇怪…
			CASE 188
				IF TCVAR:媚薬
					PRINTFORML 「呐呐哥哥大人」
					PRINTFORML 「给胡萝卜浇上柠檬就会成为媚药！！但是媚藥是什麼啊？」
				ELSE
					PRINTFORML 「胡蘿蔔先生♪胡蘿蔔先生♪香菇先生♪牛蒡先生♪啊啊～空空的蓮藕先生♪」
					PRINTFORML 「木瓜已經經過了♪」
				ENDIF
		ENDSELECT
	ENDIF
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	
	IF TFLAG:193 == 1
		PRINTFORMDW %CALLNAME:TARGET%對%CALLNAME:MASTER%的話贊同似的用力地點點頭
		RETURN 1
	ELSEIF TFLAG:193 == -1
		PRINTFORMDW %CALLNAME:TARGET%對%CALLNAME:MASTER%的話感到了小小的疑惑，微微歪著脑袋
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;ここに口上書いて下さい

ENDIF
RETURN 1
;==================================================
;301,泡茶
;==================================================
@M_KOJO_MESSAGE_COM_K50_301
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_301_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_301_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	
	IF TFLAG:193 == -1
		PRINTFORMW 「好燙！」
		PRINTFORMDW %CALLNAME:TARGET%的舌頭被燙到了
		PRINTFORMW 「舌頭火辣辣的」
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;302,身体接觸
;==================================================
@M_KOJO_MESSAGE_COM_K50_302
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_302_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_302_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	SELECTCASE CFLAG:300
		CASE 13,24,39
			PRINTFORML 「洗澡澡♪」
			PRINTFORML 「ぶんぶんしーんぶん♪」
	ENDSELECT
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:10
			PRINTFORMDW %CALLNAME:TARGET%拉起%CALLNAME:MASTER%的手转了一圈
			PRINTFORML 「哥哥大人」
			PRINTFORMDW %CALLNAME:TARGET%认真目光贯穿了%CALLNAME:MASTER%！
			PRINTFORMDW 回过神来，%CALLNAME:TARGET%的手臂抱住了%CALLNAME:MASTER%的腰…
			PRINTFORML 「嘻嘻」
			PRINTFORMDW 意想不到的看到高貴的%CALLNAME:TARGET%，在侧面的%CALLNAME:MASTER%心跳加剧了…
			PRINTFORML 
			PRINTFORML 「跳舞时姐姐大人扮演淑女角色而%CALLNAME:TARGET%总是扮演绅士」
			PRINTFORMW 「姐姐大人真狡猾」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;303,道歉
;==================================================
@M_KOJO_MESSAGE_COM_K50_303
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_303_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_303_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	IF TALENT:TARGET:2 & 2
		PRINTFORMW 「哎？你刚才说什么都可以做？」
		PRINTFORML 「哥哥大人」
		PRINTFORML 「你不是说现在什么都可以做吗？誒嘿嘿%UNICODE(0x2661)%」
		CFLAG:MASTER:诶嘿嘿 = 2
		CFLAG:诶嘿嘿 = 2
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;304,幫忙干活
;==================================================
@M_KOJO_MESSAGE_COM_K50_304
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_304_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_304_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;ちんこついてる
	PRINTFORML 「猫先生，好可愛啊！」
	IF TALENT:TARGET:2 & 2
		PRINTFORMW 「但是%CALLNAME:TARGET%也想给哥哥大人『猫先生』喂牛奶♪」
		PRINTFORMW %CALLNAME:MASTER%被拉进房间深处推倒了
		CFLAG:MASTER:诶嘿嘿 = 2
		CFLAG:诶嘿嘿 = 2
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;305,索求膝枕
;==================================================
@M_KOJO_MESSAGE_COM_K50_305
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_305_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_305_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;306,撫摸肚子
;==================================================
@M_KOJO_MESSAGE_COM_K50_306
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_306_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_306_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;307,捏臉蛋
;==================================================
@M_KOJO_MESSAGE_COM_K50_307
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_307_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_307_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;308,捅臉頰
;==================================================
@M_KOJO_MESSAGE_COM_K50_308
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_308_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_308_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	;IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	PRINTFORML 「哇！」
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	;ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;309,摸頭
;==================================================
@M_KOJO_MESSAGE_COM_K50_309
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_309_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_309_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
		PRINTFORML 「哥哥大人！芙兰已经是一位淑女了，所以别把我當成小孩！」
		PRINTFORMDW 被芙蘭罵了……
	;恋慕 後期
	ELSEIF TALENT:恋慕
		IF !RAND:3
			PRINTFORML 「哈呼……」
			PRINTFORMDW %CALLNAME:TARGET%的翅膀微微動著……
		ELSEIF !RAND:2
			PRINTFORML 「哥哥大人……」
		ELSE
			PRINTFORMW 「被哥哥大人抚摸了一顿」
			PRINTFORMW 「這麼强行的」
			PRINTFORMW 「太過分了……%UNICODE(0x2661)%」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			PRINTFORML 「强行抚摸是不行的！」
		ELSEIF !RAND:2
			PRINTFORML 「哥哥大人……」
			PRINTFORML 「不要啊……不要啊……」
			PRINTFORMDW %CALLNAME:TARGET%眼淚汪汪的樣子……
		ELSE
			PRINTFORML 「哥哥大人這樣撫摸是不行的…」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			PRINTFORML 「呀！」
			PRINTFORMDW %CALLNAME:TARGET%擺出防禦的姿勢%CALLNAME:MASTER%的手被擋住了
		ELSEIF !RAND:2
			PRINTFORML 「不要……鱈魚子要來了……」
		ELSE
			PRINTFORML 「不行啊…不行啊…」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;310,摸屁股
;==================================================
@M_KOJO_MESSAGE_COM_K50_310
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_310_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_310_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;311,擁抱
;==================================================
@M_KOJO_MESSAGE_COM_K50_311
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_311_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_311_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	PRINTFORML 「誒嘿嘿ー♪抱抱ー♪」
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;312,接吻
;==================================================
@M_KOJO_MESSAGE_COM_K50_312
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_312_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_312_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	IF FIRSTTIME()
		PRINTFORML 「哥哥大人。」
		PRINTFORMDW %CALLNAME:TARGET%一脸认真地抓住了%CALLNAME:MASTER%
		PRINTFORMW 「誒誒ー親一下是只有小孩子才會做的事哦」
		PRINTFORMW 「不能这么简单地做哟？」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			PRINTFORML 「%CALLNAME:TARGET%也要親下哥哥大人的大雞雞%UNICODE(0x2661)%」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;妊娠中
	IF TALENT:妊娠
	;	PRINTFORML 「」
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			PRINTFORML 「嘻嘻…%UNICODE(0x2661)%」
			PRINTFORMW 「哥哥大人啊…%CALLNAME:TARGET%、又要有好多小寶寶了呢…%UNICODE(0x2661)%」
		;ELSEIF !RAND:2
		ELSE
			PRINTFORMW 「%CALLNAME:TARGET%。%CALLNAME:TARGET%要成為媽媽了呢…%UNICODE(0x2661)%」
		ENDIF
		RETURN 1
	;思慕 中期
	;ELSEIF TALENT:思慕
	;	IF !RAND:3
	;		;PRINTFORML 「」
	;	ELSEIF !RAND:2
	;		;PRINTFORML 「」
	;	ELSE
	;		;PRINTFORML 「」
	;	ENDIF
	;	RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			PRINTFORML 「哥哥…不能親嘴啊…」
		ELSEIF !RAND:2
			PRINTFORMW 「%CALLNAME:TARGET%。%CALLNAME:TARGET%要成為媽媽了呢…」
		ELSE
			PRINTFORMW 「親嘴…會有小寶寶的……」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;313,胸愛撫
;==================================================
@M_KOJO_MESSAGE_COM_K50_313
;CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_313_1
CALL TRAIN_MESSAGE
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_313_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	IF TIME > 360 && TIME > 1020
		PRINTFORML 「哥哥。会被其他人看到喲？不可以啊？」
		PRINTFORMDW 和%CALLNAME:TARGET%做H的事情要等到晚上。。
		PRINTFORML 
		PRINTFORML 「嘻嘻嘻不行喲」
		PRINTFORML 「不行啦」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;314,肛門愛撫
;==================================================
@M_KOJO_MESSAGE_COM_K50_314
CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_314_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_314_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;315,陰蒂愛撫
;==================================================
@M_KOJO_MESSAGE_COM_K50_315
CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_315_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_315_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;316,指挿入れ
;==================================================
@M_KOJO_MESSAGE_COM_K50_316
CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_316_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_316_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;317,Ｖ腰を振る
;==================================================
@M_KOJO_MESSAGE_COM_K50_317
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_317_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_317_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;318,Ａ腰を振る
;==================================================
;@M_KOJO_MESSAGE_COM_K50_318
;CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_318_1
;RETURN RESULT

;@M_KOJO_MESSAGE_COM_K50_318_1
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
;LOCAL = 0
;-------------------------------------------------
;IF LOCAL

;ENDIF
;RETURN 1
;==================================================
;330,掀裙子
;==================================================
@M_KOJO_MESSAGE_COM_K50_330
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_330_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_330_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
	;幼女のマジ泣き。可愛げを出さないように
		PRINTFORMW 「誒……」
		PRINTFORMDW %CALLNAME:TARGET%目瞪口呆的表情眼看着就崩溃了……
		PRINTFORMW 「呜誒……」
		PRINTFORML 「唔”啊”啊”ーー！」
		PRINTFORMW 「哥”哥”大”人”唔”欺負人”ーー！！」
		PRINTFORMW 「啊”啊”ーー！！啊”ーーー！！！」
	;犯される生娘のように真剣に
	ELSE
		PRINTFORMW 「不要啊」
		PRINTFORMW 「哥哥大人停下來！」
		PRINTFORMW 「不要哥哥快住手！」
		PRINTFORMW 「不要啊……」
		PRINTFORMDW %CALLNAME:TARGET%的臉頰落下大粒的淚水…
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;331,泡茶（下薬）
;==================================================
@M_KOJO_MESSAGE_COM_K50_331
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_331_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_331_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
		PRINTFORML 「這種味道、%CALLNAME:15%的味道」
		PRINTFORML 「你做得很好，哥哥大人」
		PRINTFORML 「不錯不錯」
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;350,推倒（時姦時猥褻
;TFLAG:194 (0=成功 1=人目がある　2=野外は嫌でござりまする　3=多人数プレイは勘弁　4=合意があっても情緒が足りぬ）
;TCVAR:初次拥抱　1=普通に合意ゲット　2=先に接吻ぐらいしろよ　3=媚薬or発情には勝てなかったよお…　4=悔しい…刻印さえなければﾋﾞｸﾋﾞｸﾝ
;5=強引に迫られると断れぬ(素質膽怯持ち専用) 6=まんざらでもない　7=駄目　8=論外
;==================================================
@M_KOJO_MESSAGE_COM_K50_350
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_350_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_350_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;351,帯出去
;==================================================
@M_KOJO_MESSAGE_COM_K50_351
;連れ出しにはTRAIN_MESSAGEが無い
CALL M_KOJO_MESSAGE_COM_K50_351_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_351_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	IF FIRSTTIME()
		PRINTFORML 「怎麼了，哥哥？」
		PRINTFORML 「……難道說」
		PRINTFORML 「不能一個人去廁所嗎？」
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;352,告白
;==================================================
@M_KOJO_MESSAGE_COM_K50_352
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_352_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_352_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;353,猥褻
;==================================================
@M_KOJO_MESSAGE_COM_K50_353
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_353_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_353_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;354,陪睡（相手は起きて被窝に招く感じ）開始側
;==================================================
@M_KOJO_MESSAGE_COM_K50_354
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_354_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_354_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;355,時止め（地の文の改変程度に留めるべき
;==================================================
@M_KOJO_MESSAGE_COM_K50_355
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_355_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_355_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;360,任憑擺佈
;==================================================
@M_KOJO_MESSAGE_COM_K50_360
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_360_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_360_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;363,朗読（[幼稚]持ち）
;==================================================
@M_KOJO_MESSAGE_COM_K50_363
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_363_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_363_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;364,撒豆子(萃香、勇儀、斯喀雷特姉妹用
;==================================================
@M_KOJO_MESSAGE_COM_K50_364
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_364_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_364_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;365,向守矢神社移動(早苗、神奈子、諏訪子用
;==================================================
@M_KOJO_MESSAGE_COM_K50_365
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_365_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_365_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;402,就寝（添い寝時）
;==================================================
@M_KOJO_MESSAGE_COM_K50_402
;就寝にTRAIN_MESSAGEは無い
CALL M_KOJO_MESSAGE_COM_K50_402_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_402_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;403,休憩
;==================================================
@M_KOJO_MESSAGE_COM_K50_403
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_403_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_403_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;405,外出(同行時约会に外出
;==================================================
@M_KOJO_MESSAGE_COM_K50_405
;外出にTRAIN_MESSAGEは無い
CALL M_KOJO_MESSAGE_COM_K50_405_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_405_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;410,掃除
;==================================================
@M_KOJO_MESSAGE_COM_K50_410
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_410_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_410_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;411,戦闘訓練
;==================================================
@M_KOJO_MESSAGE_COM_K50_411
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_411_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_411_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;412,学习
;==================================================
@M_KOJO_MESSAGE_COM_K50_412
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_412_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_412_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;413,料理
;==================================================
@M_KOJO_MESSAGE_COM_K50_413
;料理にTRAIN_MESSAGEは無い
CALL M_KOJO_MESSAGE_COM_K50_413_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_413_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 1
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		;吸血鬼の性知識
		PRINTFORML 「好痛！」
		PRINTFORMDW 料理中、%CALLNAME:TARGET%手指被割到了
		PRINTFORML 
		PRINTFORMDW %CALLNAME:MASTER%用嘴吸住了傷口%CALLNAME:TARGET%變的慌張起來
		PRINTFORML 「誒、哥哥大人？」
		PRINTFORMW 「%CALLNAME:TARGET%是吸血鬼，而吸血是繁殖行為的吧？」
		PRINTFORMW 「討厭啦怎麼辦。%CALLNAME:TARGET%明明還沒出嫁、就被哥哥大人繁殖了」
		PRINTFORML 「即使是姐姐大人也沒对我做過這樣的事呢」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;414,吃飯
;==================================================
@M_KOJO_MESSAGE_COM_K50_414
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_414_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_414_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	IF TASTE_REACTION("辣")
		PRINTFORMDL 面對鲜红色的料理%CALLNAME:TARGET%流下了冷汗…
		PRINTFORMW 「呐、呐、哥哥大人」
		PRINTFORMW 「很辣嗎？？」
		PRINTFORMW 「不是鱈魚子吧？」
		PRINTFORMDW 从你这里得到了不是鱈魚子的保证之后，%CALLNAME:TARGET%有点畏缩地把菜送到嘴边……
		PRINTFORML 唔！
		PRINTFORML 「唔啊啊啊！！」
		;時間停止深喉插入のススメ
		PRINTFORMDL %CALLNAME:TARGET%咳，突然伸出舌頭來…
		RETURN 1
	ENDIF
	
	IF TASTE_REACTION("芥末")
;		PRINTFORMDW %CALLNAME:MASTER%决定给怀孕的%CALLNAME:TARGET%一點酸辣的東西…
		PRINTFORML 「嗯～～哈！」
		PRINTFORML  「芥末先生讓%CALLNAME:TARGET%的鼻子深处感覺很難受！」
		RETURN 1
	ENDIF
	;妊娠中
	IF TALENT:妊娠
		IF TASTE_REACTION("酸")
			PRINTFORMDW %CALLNAME:MASTER%决定给怀孕的%CALLNAME:TARGET%一點酸辣的東西…
			PRINTFORML 「%CALLNAME:TARGET%喜歡的味道？」
			PRINTFORML  「啊！又酸又好吃♪」
			SIF TALENT:処女 == -1
				PRINTFORMD 不知自己身在何处的她的身体自然地寻求柑橘，
			PRINTFORML %CALLNAME:TARGET%的臉上洋溢著幸福的表情吃著%CALLNAME:MASTER%親手做的料理…
		ENDIF
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;415,招待吃飯
;==================================================
@M_KOJO_MESSAGE_COM_K50_415
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_415_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_415_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	IF TASTE_REACTION("辣")
		PRINTFORMDL 面對鲜红色的料理%CALLNAME:TARGET%流下了冷汗…
		PRINTFORMW 「呐、呐、哥哥大人」
		PRINTFORMW 「很辣嗎？？」
		PRINTFORMW 「不是鱈魚子吧？」
		PRINTFORMDW 从你这里得到了不是鱈魚子的保证之后，%CALLNAME:TARGET%有点畏缩地把菜送到嘴边……
		PRINTFORML 唔！
		PRINTFORML 「唔啊啊啊！！」
		;時間停止深喉插入のススメ
		PRINTFORMDL %CALLNAME:TARGET%咳，突然伸出舌頭來…
		RETURN 1
	ENDIF
	
	IF TASTE_REACTION("甜")
		IF TALENT:恋人
			PRINTFORML （這個味道…）
			PRINTFORMDW %CALLNAME:TARGET%收到了味道的信息在%CALLNAME:MASTER%的耳邊輕輕地低聲細語…
			PRINTFORML 「可以哦♪兩個人獨處就喝一杯%UNICODE(0x2661)%吧！」
		ELSE
			PRINTFORMDW 「飯好甜啊！」
		ENDIF
		RETURN 1
	ENDIF
	IF TASTE_REACTION("芥末")
;		PRINTFORMDW %CALLNAME:MASTER%决定给怀孕的%CALLNAME:TARGET%一點酸辣的東西…
		PRINTFORML 「嗯～～哈！」
		PRINTFORML  「芥末先生讓%CALLNAME:TARGET%的的鼻子感覺很難受！」
		RETURN 1
	ENDIF
	;妊娠中
	IF TALENT:妊娠
		IF TASTE_REACTION("酸")
			PRINTFORMDW %CALLNAME:MASTER%决定给怀孕的%CALLNAME:TARGET%一點酸辣的東西…
			PRINTFORML 「%CALLNAME:TARGET%喜歡的味道？」
			PRINTFORML  「啊！又酸又好吃♪」
			SIF TALENT:処女 == -1
				PRINTFORMD 不知自己身在何处的她的身体自然地寻求柑橘，
			PRINTFORML %CALLNAME:TARGET%的臉上洋溢著幸福的表情吃著%CALLNAME:MASTER%親手做的料理…
		ENDIF
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;416,演奏(凍結中？
;==================================================
@M_KOJO_MESSAGE_COM_K50_416
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_416_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_416_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;417,午睡をする
;==================================================
@M_KOJO_MESSAGE_COM_K50_417
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_417_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_417_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;421,祈願する
;==================================================
@M_KOJO_MESSAGE_COM_K50_421
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_421_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_421_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;431,在浴室里放鬆
;==================================================
@M_KOJO_MESSAGE_COM_K50_431
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_431_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_431_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	IF TALENT:心情 == -1
		;IF !RAND:3
			PRINTFORMDL %CALLNAME:TARGET%把臉沉到水裏吐出氣泡…
		;ELSEIF TALENT:心情 == -1 && !RAND:2
			;躾がなっていない事と幼い事は別物故に破棄
		;	;PRINTFORMDL %CALLNAME:TARGET%は湯船を掴み足をばたつかせている…
		;什麼也不做分岐
		;ELSE
		;ENDIF
		RETURN 1
	ENDIF
	
	IF !RAND:10
		PRINTFORML 「哥哥大人！哥哥大人！小鴨子哦！」
		PRINTFORML 「呱呱～嘻！！」
		PRINTFORMDW %CALLNAME:TARGET%推著鴨子看起來超可愛的樣子…
	ELSEIF !RAND:9
		PRINTFORMDW %CALLNAME:TARGET%的手作出碗的樣子、按入水下……
		PRINTFORML 「哗啦哗啦～！」
	ELSEIF !RAND:8
		PRINTFORMW 「哥哥大人哥哥大人！%CALLNAME:TARGET%呢。要泡在熱水裏」
		PRINTFORML 「再多點！」
		PRINTFORMW 『再多點！』
		PRINTFORML 「洗头的时候你看着我后面！」
		PRINTFORMW 『洗头的时候你看着我后面！』
		PRINTFORML 「呐~？」
		PRINTFORMW 『呐~？』
	ELSEIF !RAND:7
		
	ELSE
	ENDIF
	
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;440,等待
;==================================================
@M_KOJO_MESSAGE_COM_K50_440
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_440_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_440_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	IF !RAND:3
		PRINTFORML 「あーぶくたったーにえたったー♪にえたかどうだかたべてみよー」
		PRINTFORML 「むしゃむしゃ」
		PRINTFORML 「まだ煮えない」
		PRINTFORMDW %CALLNAME:TARGET%在%CALLNAME:MASTER%旁邊消磨時間
	ELSEIF !RAND:2
		PRINTFORML 「呐ー哥哥大人？」
		PRINTFORMW 「………」
		PRINTFORML 
		PRINTFORMW 「不要讓女士感到無聊」
		PRINTFORML 「誒！」
	ELSE
		PRINTFORML 「啊ー啊ー哥哥啦。」
		PRINTFORMW 「一起玩吧？」
		PRINTFORMW %CALLNAME:TARGET%拉着你的手臂
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;441,聽課(慧音専用？
;==================================================
@M_KOJO_MESSAGE_COM_K50_441
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_441_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_441_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	;IF CFLAG:诶嘿嘿 == 2
	;	;ちんこついてる
	;	IF TALENT:TARGET:2 & 2
	;		PRINTFORML 「せんせーい！バナナが入りました～！」
	;		PRINTFORMDW %CALLNAME:TARGET%のバナナに貫かれながら%CALLNAME:MASTER%は獣のような声を上げている…
	;	;ちんこついてない
	;	ELSE
	;		PRINTFORML 「せんせーい！バナナはおやつに入りますか？」
	;		PRINTFORMDW %CALLNAME:TARGET%はここぞとばかりに質問した
	;	ENDIF
	;	RETURN 1
	;ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;442,伐採
;==================================================
@M_KOJO_MESSAGE_COM_K50_442
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_442_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_442_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;※※※※※※※※※※※※※※※※※※※※※※※※※
;约会系指令
;※※※※※※※※※※※※※※※※※※※※※※※※※
[SKIPSTART]
;現状未実装
;==================================================
;600,约会会話
;==================================================
@M_KOJO_MESSAGE_COM_K50_600
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_600_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_600_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;601,约会身体接觸
;==================================================
@M_KOJO_MESSAGE_COM_K50_601
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_601_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_601_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;602,约会接吻
;==================================================
@M_KOJO_MESSAGE_COM_K50_602
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_602_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_602_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
[SKIPEND]
;==================================================
;603,牽手
;==================================================
@M_KOJO_MESSAGE_COM_K50_603
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_603_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_603_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;604,散步
;==================================================
@M_KOJO_MESSAGE_COM_K50_604
;散步にはTRAIN_MESSAGEが無い
CALL M_KOJO_MESSAGE_COM_K50_604_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_604_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;605,繞道
;==================================================
@M_KOJO_MESSAGE_COM_K50_605
;繞道にはTRAIN_MESSAGEが無い
CALL M_KOJO_MESSAGE_COM_K50_605_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_605_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	IF !RAND:5
		PRINTFORMDW %CALLNAME:TARGET%扭扭捏捏的一邊抓住%CALLNAME:MASTER%的衣服的下擺一邊用眼神訴說著
		PRINTFORMDW %CALLNAME:TARGET%的眼睛濕潤了…
		PRINTFORMW 「……想尿尿」
	ELSEIF !RAND:4
	ELSEIF !RAND:3
		PRINTFORMDW 失去理性的%CALLNAME:MASTER%把%CALLNAME:TARGET%拖入没有人气的粗点心店后，往%CALLNAME:TARGET%的口中放入了黑色的粗壮的糕点！
		MONEY:0 -= 20
	ELSEIF !RAND:2
		IF !RAND:2
			PRINTFORMDW %CALLNAME:MASTER%無可奈可的給%CALLNAME:TARGET%買了一個棉花糖
			MONEY:0 -= 500
		ELSE
			PRINTFORMDW %CALLNAME:MASTER%無可奈可的給%CALLNAME:TARGET%買了一個沾著糖漿的可麗餅
			MONEY:0 -= 500
		ENDIF
	ELSE
		PRINTFORMDW 做完的%CALLNAME:MASTER%用充滿愛意的眼眺望著%CALLNAME:TARGET%
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;610,請吃飯
;==================================================
@M_KOJO_MESSAGE_COM_K50_610
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_610_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_610_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;613,賺錢
;==================================================
@M_KOJO_MESSAGE_COM_K50_613
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_613_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_613_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;614,採購食材
;==================================================
@M_KOJO_MESSAGE_COM_K50_614
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_614_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_614_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;615,吃便當
;==================================================
@M_KOJO_MESSAGE_COM_K50_615
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_615_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_615_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	IF !TALENT:処女 && !RAND:(ABL:料理技能 + 1)
		PRINTFORMW 「呐呐哥哥大人。%CALLNAME:TARGET%能成為好的新娘嗎？？」
		PRINTFORMW 「已经不能嫁出去了吗？即使說別的也沒用哦哥哥大人」
		PRINTFORMW 「這是姐姐的决定哦」
	ELSE
	PRINTDATAL
		DATALIST
			DATAFORM 「吃便当吃便当♪」
			DATAFORM 「たっすけったかっめにーつーれらーれてー♪」
		ENDLIST
		DATALIST
			DATAFORM 「哥哥，哥哥！%CALLNAME:TARGET%能成為好的妻子嗎？能成為嗎？」
			DATAFORM 「能成为吧！嗯。芙蘭要加油！」
		ENDLIST
		DATALIST
			DATAFORM 「哥哥，只烤單面的煎雞蛋是桑妮教我的哦」
			DATAFORM 「芙蘭不擅長這個啊。所以啊ー我還有很大的提升空間呢」
			DATAFORM 芙蘭摇着紮起來的頭髮
		ENDLIST
	ENDDATA
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;617,甘味処に誘う
;==================================================
@M_KOJO_MESSAGE_COM_K50_617
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_617_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_617_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	PRINTFORML 「哥哥大人！好好吃！」
	PRINTFORML 「超凡魅力的鳗鱼也下来了！！」
	PRINTFORML 「%UNICODE(0x2661)%」
	PRINTFORMDW %CALLNAME:TARGET%一边拍着翅膀一边大口吃着甜食
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;618,花屋
;==================================================
@M_KOJO_MESSAGE_COM_K50_618
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_618_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_618_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	PRINTFORML 「真漂亮」
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;620,鈴奈庵
;==================================================
@M_KOJO_MESSAGE_COM_K50_620
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_620_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_620_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	IF TALENT:妊娠
		PRINTFORML 「哥哥大人。推薦圖書有點暈頭轉向。」
	ELSE
		PRINTFORMDL %CALLNAME:TARGET%非常期待…
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;660,読書
;==================================================
@M_KOJO_MESSAGE_COM_K50_660
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_660_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_660_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	IF EQUIP:飾品 == 1
		PRINTFORMDL %CALLNAME:TARGET%戴着眼镜沉浸在看书中的样子，
		PRINTFORMDW 在天真无邪的举动中充分发挥了平时隐藏着的气质。
		PRINTFORML 「…怎么了哥哥大人」
		IF TALENT:TARGET:2 & 2
			PRINTFORMDW %CALLNAME:TARGET%悄悄的合上了難為情的書…
		ELSEIF ABL:教養 < 3
			PRINTFORMDW %CALLNAME:TARGET%悄悄的合上了書…
		ENDIF
	ELSE
		PRINTFORMDW 不知不覺%CALLNAME:TARGET%習慣戴眼鏡了
		EQUIP:飾品 = 1
	ENDIF
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;698,進入房間
;==================================================
@M_KOJO_MESSAGE_COM_K50_698
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_698_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_698_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;699,外出
;==================================================
@M_KOJO_MESSAGE_COM_K50_699
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_699_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_699_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;※※※※※※※※※※※※※※※※※※※※※※※※※
;派生指令
;※※※※※※※※※※※※※※※※※※※※※※※※※
;==================================================
;60,掏耳朵
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_60
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_60_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_60_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;61,抱き枕
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_61
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_61_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_61_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		;IF !RAND:3
			;PRINTFORML 「」
		;ELSEIF !RAND:2
			;PRINTFORML 「」
		;ELSE
			;PRINTFORML 「」
		;ENDIF
		PRINTFORML 「Q…」
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;62,亲額頭
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_62
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_62_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_62_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;63,更多的接吻
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_63
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_63_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_63_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	IF TALENT:妊娠
	;	PRINTFORML 「」
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	PRINTFORML （哥哥大人，真的想讓%CALLNAME:TARGET%懷孕呢…）
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;80,拉手
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_80
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_80_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_80_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;对方主導被逆推中
	IF CFLAG:诶嘿嘿 == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;你主導推倒中
	IF CFLAG:诶嘿嘿 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえず随机三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:16:恋慕
		IF !RAND:3
			PRINTFORML 「如果讓姐姐伤心的話%CALLNAME:TARGET%是不会做的」
			PRINTFORMDW %CALLNAME:TARGET%眼泪汪汪地诉说起来
		ELSEIF !RAND:2
			PRINTFORML 「不行的哥哥大人！」
		ELSE
			PRINTFORML 「哥哥大人不行…」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:16:思慕
		IF !RAND:3
			PRINTFORML 「如果被姐姐大人看到的话会被人嫉妒的啊。」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			PRINTFORML 「等一下哥哥大人！%CALLNAME:TARGET%這樣是違法的！」
		ELSEIF !RAND:2
			PRINTFORML 「哥哥大人！等一下！等一下！」
		ELSE
			PRINTFORML 「不要拉著啦…哥哥大人」
			PRINTFORMW 「哥哥会被抓起来被起诉的哦。」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1