@厌弃_38
IF TARGET==38 && 关系_514()==0
	SELECTCASE RAND:3
		CASE 0
			PRINTFORMDW   %CALLNAME:MASTER%被恋无视了。
		CASEELSE
			PRINTFORMDW   恋瞥了%CALLNAME:MASTER%一眼，什么也没说。
	ENDSELECT
	TFLAG:好感度管理=MAX(TFLAG:好感度管理-RAND:4,0)
	TFLAG:信赖度管理=MAX(TFLAG:信赖度管理-RAND:2,0)
	IF !RAND:8
		TALENT:心情=MAX(TALENT:心情-RAND:2,-1)
	ENDIF
ENDIF

