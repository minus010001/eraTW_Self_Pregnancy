;※※※※※※※※※※※※※※※※※※※※※※※※※
;奉仕系コマンド
;※※※※※※※※※※※※※※※※※※※※※※※※※
;==================================================
;80,手淫
;==================================================
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================
@別人変身版_MESSAGE_COM_K69_80
#DIM TRANSAFTER
#DIMS VALUE
TRANSAFTER = CFLAG:TARGET:変化先 == 0 ? K69_CNUM # CFLAG:TARGET:変化先
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;小分岐
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;基本セット
	;時姦中
	IF FLAG:70
		RETURN 1
	ELSEIF TALENT:恋慕 || TALENT:愛欲 || TALENT:セフレ
		IF NOWEX:MASTER:射精
			LOCAL:99 = RAND:2
			SELECTCASE LOCAL:99
			CASE 0
				CALL HPH_PRINT,@"「唔姆，射得爽快HPH　\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的\@手给咱接好精液HPH」","W"
			CASE 1
				CALL HPH_PRINT,@"「鸡巴都抽动了呐HPH　来，用\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的\@手撸射吧HPH」","W"
			ENDSELECT
		ELSE
			IF BASE:MASTER:勃起 < 1000
				LOCAL:99 = RAND:2
				SELECTCASE LOCAL:99
				CASE 0
					CALL HPH_PRINT,@"「怎地蔫头耷脑的…… 且让咱用\@ TRANSAFTER == K69_CNUM ?  # 这姑娘的 \@手给你提振提振HPH」","W"
				CASE 1
					CALL HPH_PRINT,@"「瞧好咯，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@\@ TALENT:K69_CNUM:体型 < 0 ? 纤纤小手 # 手掌 \@抚弄阳物，还不快些硬挺起来HPH」","W"
				ENDSELECT
			ELSE
				LOCAL:99 = RAND:3
				SELECTCASE LOCAL:99
				CASE 0
					CALL HPH_PRINT,@"「如何呀？被\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@\@ TALENT:K69_CNUM:体型 < 0 ? 纤纤小手 # 手掌 \@抚弄的滋味……HPH可还受用？」","W"
				CASE 1
					CALL HPH_PRINT,@"「这般上下捋动……嗯啊……HPH」","W"
				CASE 2
					CALL HPH_PRINT,@"「\@ TALENT:K69_CNUM:体型 < 0 ? 被孩童般的身子侍奉阳物，%MASTERNAME:K69_CNUM%当真趣味独特…… # 喏喏，这般摆弄，舒坦得紧吧？\@HPH」","W"
				ENDSELECT
			ENDIF
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF NOWEX:MASTER:射精
			CALL HPH_PRINT,@"「唔姆，射得爽快HPH　\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的\@手给咱接好精液HPH」","W"
		ELSE
			CALL HPH_PRINT,@"「这般上下捋动……嗯啊……」","W"
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1

;==================================================
;81,フェラチオ
;==================================================
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================
@別人変身版_MESSAGE_COM_K69_81
#DIM TRANSAFTER
#DIMS VALUE
TRANSAFTER = CFLAG:TARGET:変化先 == 0 ? K69_CNUM # CFLAG:TARGET:変化先
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;小分岐
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;基本セット
	IF FLAG:70
		RETURN 1
	ELSEIF TALENT:恋慕 || TALENT:愛欲 || TALENT:セフレ
		IF NOWEX:MASTER:射精
			LOCAL:99 = RAND:2
			SELECTCASE LOCAL:99
			CASE 0
				CALL HPH_PRINT,@"「舔舔……HPH　呼呼，不错嘛HPH　\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的\@嘴里尽管射个痛快HPH」","W"
			CASE 1
				CALL HPH_PRINT,@"「在嘴里一跳一跳的呐……HPH　吸溜HPH　来，最后一击啦HPH　\@ TRANSAFTER == K69_CNUM ? 用咱的 # 用%CALLNAME:TRANSAFTER%的\@嘴高潮吧HPH」","W"
			ENDSELECT
		ELSE
			IF BASE:MASTER:勃起 < 1000
				LOCAL:99 = RAND:2
				SELECTCASE LOCAL:99
				CASE 0
					CALL HPH_PRINT,@"「咋了，软趴趴的……　来，让咱用\@ TRANSAFTER == K69_CNUM ? # 这姑娘的 \@嘴给你弄大吧HPH」","W"
				CASE 1
					CALL HPH_PRINT,@"「喂，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@\@ TALENT:K69_CNUM:体型 < 0 ? 小嘴巴 # 嘴 \@都给你含鸡鸡了还不快点硬起来HPH」","W"
				ENDSELECT
			ELSE
				LOCAL:99 = RAND:4
				SELECTCASE LOCAL:99
				CASE 0
					CALL HPH_PRINT,@"「舔舔……HPH　如何？\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@\@ TALENT:K69_CNUM:体型 < 0 ? 小嘴巴 # 嘴 \@舒服吗？HPH」","W"
				CASE 1
					CALL HPH_PRINT,@"「嗯唔……，含着%MASTERNAME:K69_CNUM%的鸡鸡，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@身体都兴奋起来了……HPH」","W"
				CASE 2
					CALL HPH_PRINT,@"「嗯噗……，\@ TALENT:K69_CNUM:体型 < 0 ? 被这种幼女身体含着鸡鸡就兴奋，%MASTERNAME:K69_CNUM%你个萝莉控变态…… # 嗯噗……，舔舔……，这样弄很舒服吧……？HPH\@」","W"
				CASE 3
					CALL HPH_PRINT,@"「舔舔……%MASTERNAME:K69_CNUM%的浓烈气味和味道，咱喜欢……HPH \@ TRANSAFTER == K69_CNUM ? # 　%CALLNAME:TRANSAFTER%的身体也很享受呢……HPH \@」","W"
				ENDSELECT
			ENDIF
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF NOWEX:MASTER:射精
			CALL HPH_PRINT,@"「舔舔……HPH　呼呼，不错嘛HPH　\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的\@嘴里尽管射个痛快HPH」","W"
		ELSE
			CALL HPH_PRINT,@"「像这样，舔舔，啵噜啵噜……」","W"
		ENDIF
		RETURN 1
	ENDIF
ENDIF


;==================================================
;82,パイズリ
;==================================================
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================
@別人変身版_MESSAGE_COM_K69_82
#DIM TRANSAFTER
#DIMS VALUE
TRANSAFTER = CFLAG:TARGET:変化先 == 0 ? K69_CNUM # CFLAG:TARGET:変化先
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;小分岐
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;基本セット
	;時姦中
	IF FLAG:70
		RETURN 1
	ELSEIF TALENT:恋慕 || TALENT:愛欲 || TALENT:セフレ
		IF NOWEX:MASTER:射精 && NOWEX:TARGET:Ｂ絶頂
			LOCAL:99 = RAND:1
			SELECTCASE LOCAL:99
			CASE 0
				CALL HPH_PRINT,@"「嗯呜HPH　……咋了？要高潮了么……？HPH　咱也……HPH嗯啊啊HPHHPH」","W"
			ENDSELECT
		ELSEIF NOWEX:TARGET:Ｂ絶頂
			LOCAL:99 = RAND:3
			SELECTCASE LOCAL:99
			CASE 0
				CALL HPH_PRINT,@"「咕……HPH　不行呐，奶子，好烫……HPHHPH」","W"
			CASE 1
				CALL HPH_PRINT,@"「为啥啊，明明是咱在进攻，HPH　乳头却……HPHHPH」","W"
			CASE 2
				CALL HPH_PRINT,@"「不行了，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@乳头，太敏感了，要…去了……HPH」","W"
			ENDSELECT
		ELSEIF NOWEX:MASTER:射精
			LOCAL:99 = RAND:3
			SELECTCASE LOCAL:99
			CASE 0
				CALL HPH_PRINT,@"「呼呼，变得滑溜溜了呐……HPH　不错嘛，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@这对\@ TALENT:K69_CNUM:胸围 < 0 ? 小奶子 # 奶子 \@上全射出来吧HPH」","W"
			CASE 1
				CALL HPH_PRINT,@"「咋了？要射了么？HPH　呼呼，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@\@ TALENT:K69_CNUM:胸围 < 0 ? 小奶子 # 奶子 \@\@ TALENT:K69_CNUM:胸围 > 0 ? ，想让人怀孕吗？ # 上想播种吗？ \@HPH　可以哦，来吧HPH」","W"
			CASE 2
				CALL HPH_PRINT,@"「%MASTERNAME:K69_CNUM%果然喜欢\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@\@ TALENT:K69_CNUM:胸围 < 0 ? 小奶子 # 奶子 \@啊……HPH　尽管射个痛快吧HPH」","W"
			ENDSELECT
		ELSE
			IF BASE:MASTER:勃起 < 1000
				LOCAL:99 = RAND:3
				SELECTCASE LOCAL:99
				CASE 0
					CALL HPH_PRINT,@"「呼呼，变得滑溜溜了呐……HPH　不错嘛，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@这对\@ TALENT:K69_CNUM:胸围 < 0 ? 小奶子 # 奶子 \@上全射出来吧HPH」","W"
				CASE 1
					CALL HPH_PRINT,@"「咋了，软趴趴的……　来，让咱用\@ TRANSAFTER == K69_CNUM ? # 这姑娘的 \@胸给你弄大吧HPH」","W"
				CASE 2
					CALL HPH_PRINT,@"「喂，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@这对\@ TALENT:K69_CNUM:胸围 < 0 ? 小奶子 # 奶子 \@都给你蹭鸡鸡了还不快点硬起来HPH」","W"
				ENDSELECT
			ELSE
				LOCAL:99 = RAND:4
				SELECTCASE LOCAL:99
				CASE 0
					CALL HPH_PRINT,@"「嗯，嗯HPH　如何？\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@\@ TALENT:K69_CNUM:胸围 < 0 ? 小奶子 # 奶子 \@舒服吧HPH」","W"
				CASE 1
					CALL HPH_PRINT,@"「嗯，嗯，%MASTERNAME:K69_CNUM%的鸡鸡，好烫呐……HPH\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@身体也兴奋起来了……HPH」","W"
				CASE 2
					CALL HPH_PRINT,@"「嗯，嗯HPH　\@ TALENT:K69_CNUM:体型 < 0 ? 被这种幼女胸脯蹭鸡鸡就兴奋，%MASTERNAME:K69_CNUM%你个萝莉控变态…… # 来，这样弄很舒服吧……？HPH\@」","W"
				CASE 3
					CALL HPH_PRINT,@"「嗯，嗯HPH　这样蹭着，%MASTERNAME:K69_CNUM%的浓烈气味直冲鼻腔……HPH　\@ TRANSAFTER == K69_CNUM ? 咱的 # 这个 \@身体也兴奋起来了……HPH」","W"
				ENDSELECT
			ENDIF
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF NOWEX:MASTER:射精
			CALL HPH_PRINT,@"「呼呼，变得滑溜溜了呐……HPH　不错嘛，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@这对\@ TALENT:K69_CNUM:胸围 < 0 ? 小奶子 # 奶子 \@上全射出来吧HPH」","W"
		ELSE
			CALL HPH_PRINT,@"「像这样，蹭蹭，滑溜溜地……」","W"
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1

;==================================================
;83,素股
;==================================================
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================
@別人変身版_MESSAGE_COM_K69_83
#DIM TRANSAFTER
#DIMS VALUE
TRANSAFTER = CFLAG:TARGET:変化先 == 0 ? K69_CNUM # CFLAG:TARGET:変化先
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;小分岐
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;基本セット
	;時姦中
	IF FLAG:70
		PRINTFORMW 
		RETURN 1
	;恋慕
	ELSEIF TALENT:恋慕
		PRINTFORMW 
		RETURN 1
	;屈服3
	ELSEIF MARK:不埒刻印 == 3
		PRINTFORMW 
		RETURN 1
	;それ以外
	ELSE
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
RETURN 1

;==================================================
;84,泡踊り
;==================================================
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================
@別人変身版_MESSAGE_COM_K69_84
#DIM TRANSAFTER
#DIMS VALUE
TRANSAFTER = CFLAG:TARGET:変化先 == 0 ? K69_CNUM # CFLAG:TARGET:変化先
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;小分岐
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;基本セット
	;恋慕
	IF TALENT:恋慕
		PRINTFORMW 
		RETURN 1
	;屈服3
	ELSEIF MARK:不埒刻印 == 3
		PRINTFORMW 
		RETURN 1
	;それ以外
	ELSE
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF
RETURN 1

;==================================================
;85,足扱き
;==================================================
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
@別人変身版_MESSAGE_COM_K69_85
#DIM TRANSAFTER
#DIMS VALUE
TRANSAFTER = CFLAG:TARGET:変化先 == 0 ? K69_CNUM # CFLAG:TARGET:変化先
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;小分岐
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;基本セット
	;時姦中
	IF FLAG:70
		RETURN 1
	ELSEIF TALENT:恋慕 || TALENT:愛欲 || TALENT:セフレ
		IF NOWEX:MASTER:射精
			LOCAL:99 = RAND:2
			SELECTCASE LOCAL:99
			CASE 0
				CALL HPH_PRINT,@"「咯咯，射得痛快HPH　\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的\@脚丫子就接好精液罢HPH」","W"
			CASE 1
				CALL HPH_PRINT,@"「咯咯，抽抽得欢实嘞HPH　来呀，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的\@脚丫子给汝登仙去HPH」","W"
			ENDSELECT
		ELSE
			IF BASE:MASTER:勃起 < 1000
				LOCAL:99 = RAND:2
				SELECTCASE LOCAL:99
				CASE 0
					CALL HPH_PRINT,@"「怎的，蔫茄子似的……　来，咱用\@ TRANSAFTER == K69_CNUM ? # 这妮子的 \@脚丫子给汝整治整治HPH」","W"
				CASE 1
					CALL HPH_PRINT,@"「瞧好，\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@\@ TALENT:K69_CNUM:体型 < 0 ? 玲珑小脚 # 玉足 \@这般伺候，还不快快硬起来HPH」","W"
				ENDSELECT
			ELSE
				LOCAL:99 = RAND:3
				SELECTCASE LOCAL:99
				CASE 0
					CALL HPH_PRINT,@"「如何呀？\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的 \@\@ TALENT:K69_CNUM:体型 < 0 ? 玲珑小脚 # 玉足 \@这般作践，可舒坦？HPH」","W"
				CASE 1
					CALL HPH_PRINT,@"「这般蹭着，撸着……HPH」","W"
				CASE 2
					CALL HPH_PRINT,@"「\@ TALENT:K69_CNUM:体型 < 0 ? 被孩童般的脚丫子踩着那话儿就兴奋，%MASTERNAME:K69_CNUM%可真是恋童的变态…… # 来来，这般摆弄，可美得很？\@HPH」","W"
				ENDSELECT
			ENDIF
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF NOWEX:MASTER:射精
			CALL HPH_PRINT,@"「唔姆，射得爽快HPH　\@ TRANSAFTER == K69_CNUM ? 咱的 # %CALLNAME:TRANSAFTER%的\@手给咱接好精液HPH」","W"
		ELSE
			CALL HPH_PRINT,@"「这般上下捋动……嗯啊……」","W"
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;88,バキュームフェラ
;==================================================
;==================================================
;Optional: Determines if the standard description should be skipped.
;Return 0 if description and KOJO should be shown, 1 if description should be hidden and KOJO shown, 2 if description should be shown and KOJO hidden, and 3 if both description and KOJO should be hidden.
;==================================================
@別人変身版_MESSAGE_COM_K69_88
#DIM TRANSAFTER
#DIMS VALUE
TRANSAFTER = CFLAG:TARGET:変化先 == 0 ? K69_CNUM # CFLAG:TARGET:変化先
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;-------------------------------------------------
	;小分岐
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 0
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		PRINTFORMW 
		RETURN 1
	ENDIF
	;基本セット
	;時姦中
	IF FLAG:70
		RETURN 1
	ELSEIF TALENT:恋慕 || TALENT:愛欲 || TALENT:セフレ
		IF NOWEX:MASTER:射精
			LOCAL:99 = RAND:2
			SELECTCASE LOCAL:99
			CASE 0
				CALL HPH_PRINT,@"「嗯唔……HPH　吸溜、吸溜溜溜呜HPH」","L"
			CASE 1
				CALL HPH_PRINT,@"「嗯嗯！HPH　……吸溜、吸溜溜溜溜呜HPH」","L"
			ENDSELECT
			PRINTFORMW 察觉到射精征兆的猯藏，仿佛要发起最后冲刺般用\@ TRANSAFTER == K69_CNUM ? # %CALLNAME:TRANSAFTER%的\@喉咙吮吸起阴茎...
		ELSE
			LOCAL:99 = RAND:2
			SELECTCASE LOCAL:99
			CASE 0
				CALL HPH_PRINT,@"「嗯嗯……、啾噜、啾嗯……HPH」","L"
			CASE 1
				CALL HPH_PRINT,@"「啾噗、啾噗、啾噗呜……HPH」","L"
			ENDSELECT
			SELECTCASE RAND:2
				CASE 0
					PRINTFORMW 藏正用\@ TRANSAFTER == K69_CNUM ? 那 # %CALLNAME:TRANSAFTER%的\@\@ TALENT:K69_CNUM:体型 < 0 ? 小巧的 # \@整个口腔和喉咙，侍奉着%CALLNAME:MASTER%的阴茎...
				CASE 1
					PRINTFORMW 猯藏以\@ TRANSAFTER == K69_CNUM ? # %CALLNAME:TRANSAFTER%的模样\@将\@ TALENT:K69_CNUM:体型 < 0 ? 小嘴根本容纳不下的 # \@%CALLNAME:MASTER%的阴茎深深含住，用口腔和喉咙殷勤侍奉着...
			ENDSELECT
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF NOWEX:MASTER:射精
			CALL HPH_PRINT,@"「嗯唔……　吸溜溜呜HPH」","W"
		ELSE
			CALL HPH_PRINT,@"「嗯嗯……、啾噜、啾嗯……」","W"
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1