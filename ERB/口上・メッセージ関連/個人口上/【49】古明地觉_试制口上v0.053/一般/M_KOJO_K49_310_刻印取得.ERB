;-------------------------------------------
;刻印取得
;-------------------------------------------
@M_KOJO_NK_MESSAGE_MARKCNG_K49
#DIM 窒息
;このままだと地の文が表示されません
;地の文を表示したい方は、下記のコメントアウトを削除して下さい
;CALL MARK_MESSAGE
;苦痛刻印
IF !CFLAG:MASTER:恶作剧 && !FLAG:時間停止
	SELECTCASE TFLAG:苦痛刻印取得
		CASE 1
			PRINTFORML 「呜…」
			PRINTFORMDW   觉默默忍受着。
		CASE 2
			PRINTFORML 「咕…住手…」
			PRINTFORMDW   觉似乎有些受不了了。
		CASE 3
			PRINTFORML 「呜呀！快停下来啊！」
			PRINTFORMDW   觉哭叫着，让%CALLNAME:MASTER%停止暴行。
	ENDSELECT
ENDIF

;時姦刻印
IF TFLAG:時姦刻印取得 == 1
	PRINTFORML 
ELSEIF TFLAG:時姦刻印取得 == 2
	PRINTFORML 
ELSEIF TFLAG:時姦刻印取得 == 3
	PRINTFORML 
ENDIF

;快楽刻印
IF !CFLAG:MASTER:恶作剧 && !FLAG:時間停止
	SELECTCASE TFLAG:快楽刻印取得
		CASE 1
			PRINTFORML 「哈啊…哈啊…」
			PRINTFORMDW   觉大口吐息着，灼热而甜蜜的气息让%CALLNAME:MASTER%沉溺其中。
		CASE 2
			PRINTFORML 「呜…！」
			PRINTFORMDW   觉的表情透露着极度的悦乐。
		CASE 3
			PRINTFORML 「%CALLNAME:MASTER%，%CALLNAME:MASTER%！」
			PRINTFORMDW   觉一遍遍叫着%CALLNAME:MASTER%的名字，被快感冲刷的脑海一片空白。
	ENDSELECT
ENDIF

;不埒刻印
IF !CFLAG:MASTER:恶作剧 && !FLAG:時間停止
	SELECTCASE TFLAG:屈服刻印取得
	;快楽による刻印
;		CASE 11
;			PRINTFORML 「
;		CASE 12
;			PRINTFORML 「
;		CASE 13
;			PRINTFORML 「
	;屈服、恭順による刻印	
		CASE 31
			PRINTFORML 「啊啊，只是对你有些在意」
			PRINTFORMDW   觉的眼神和平时明显不一样，是幻觉吗？
		CASE 32
			PRINTFORML 「做一些亲密些的事情吧～」
			PRINTFORMDW   觉亲热地凑了过来。
		CASE 33
			PRINTFORML 「是你的话…不管做什么，我也是，没关系的哦？」
			PRINTFORMDW   觉的目光中溢出几分妩媚。	
	ENDSELECT
ENDIF

;反発刻印
IF !CFLAG:MASTER:恶作剧 && !FLAG:時間停止
	SELECTCASE TFLAG:反発刻印取得
		CASE 1
			PRINTFORML 「啧」
			PRINTFORMDW   觉露出明显不愉快的表情，有些生气。
		CASE 2
			PRINTFORML 「就在这里把你处决，如何？」
			PRINTFORMDW   杀意满盈而露骨的威胁，看来是被惹恼了。
		CASE 3
			PRINTFORMDW   眼前飞来好几条触手，死死缠住了%CALLNAME:MASTER%。
			PRINTFORMDL   尝试时间停止也丝毫不能脱身，
			PRINTFORMDL   要被绞杀了，不能呼吸了；
			PRINTFORMDW   胸腔被触手挤出最后一丝空气，完全没有扩张的空间。
			PRINTFORMW 「…这可是你咎由自取」
			PRINTFORMDL   话音未落，身上的触手加了几分力道；
			PRINTFORMDW   气息被强行掐断，意识逐渐模糊，%CALLNAME:MASTER%最终晕厥了。
			BASE:MASTER:TSP -= 50
			BASE:MASTER:体力=0
			BASE:MASTER:気力=0
			窒息 = MAX((MAXBASE:MASTER:気力 + MAXBASE:MASTER:体力)/ 5, 2000)
			CALL BUFF_BASE(MASTER,BASE_体力,-窒息)
			CALL BUFF_BASE(MASTER,BASE_気力,-窒息)
	ENDSELECT
ENDIF