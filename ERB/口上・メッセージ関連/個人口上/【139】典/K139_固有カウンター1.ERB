;[[典]]→[[典]]

;固有Counter可否
@UNIQUE_COUNTER1_ABLE_K139
SIF TFLAG:62
	RETURN 0
SIF TEQUIP:[[典]]:50 >= 0 || TEQUIP:[[典]]:51 >= 0
	RETURN 0
SIF ABL:[[典]]:欲望 < 5
	RETURN 0
RETURN 1

;固有Counterの種別と頻度
@UNIQUE_COUNTER1_FREQUENCY_K139
;ソフト/ベリーソフト/コミュ/着衣/脱衣愛撫/脱衣強要/抱き着き/性交/責め/おねだりの中なら選ぶべし
;タイプによって発生しない状況があるので注意
RESULTS = 脱衣愛撫
;発生率、基本値(10)にこの数値を足す
;通常のCounterはプラ​舞​5以下なので、テストプレイ以外であんまり極端な数字を入れない方がいい
RETURN 10

;文本
@UNIQUE_COUNTER1_MESSAGE_K139
SELECTCASE RAND:3
CASE 0
	PRINTFORML %CALLNAME:TARGET%微笑着用松松软软的尾巴像围巾一样悄悄缠绕%CALLNAME:MASTER%的脖子…
CASE 1
	PRINTFORML %CALLNAME:TARGET%用蓬松的尾巴温柔的撫摸%CALLNAME:MASTER%的身体…
CASE 2
	PRINTFORML %CALLNAME:TARGET%用蓬松的尾巴温柔的愛撫%CALLNAME:MASTER%的股間…
ENDSELECT

;固有Counterのソース等
@UNIQUE_COUNTER1_SOURCE_K139

	;体勢の変更
	;=============================
	;CALL TOUCH_SET(SET_FROM_YUBI,1,[[典]])
	;TFLAG:MASTERのＣ使用中 ++

	;ソース計算および内部処理系統
	;=============================
	;IF TEQUIP:PLAYER:0
	;	IF TEQUIP:PLAYER:0 & 1
	;		CALL DATUI_BOTTOM([[典]],1)
	;	ELSEIF TEQUIP:PLAYER:0 & 8
	;		CALL DATUI_BOTTOM([[典]],2)
	;	ENDIF
	;ENDIF

	SOURCE:[[典]]:情愛 += 150
	SOURCE:[[典]]:与快Ｃ += 100
	SOURCE:[[典]]:誘惑 += 200
	SOURCE:[[典]]:挑発 += 100

	;CALL EVENT_COUNTER_POSE_69([[典]],2)