;1001-1500用于不是非常重要的函数调用
;CFLAG:54:1001这是一个用于检测表白次数用于多种表白拒绝与事后处理
;CFLAG:54:1001 == 6虚伪的恋人，你不会真的以为拒绝了5次还能正常看待你吧（未施工）
;CFLAG:54:1002无陷落多次触摸身体发怒了
;CFLAG:54:1003恋人发情，不要过多调戏帕琪哦
;CFLAG:54:1004我超！帕琪彻底愤怒了
;CFLAG:54:1005戳戳
;1501-1999会触发大事件在很多地方会被反复调取
;CFLAG:54:1501催眠事件1简单理解为催眠深度1
;CFLAG:54:1502催眠事件2
;CFLAG:54:1503催眠事件3
;CFLAG:54:1504催眠事件4
;CFLAG:54:1505帕琪痴女化计数满10触发特殊事件
;CFLAG:54:1506七夕佳节
;TCVAR:54:350-351用于触发情况下被逆推时锁掉350推倒和挣脱351锁定摆脱
;TCVAR:54:353七夕看星星

;帕秋莉的魔法实验（预定）
;帕秋莉的的炼金素材回收（预定）
;帕秋莉的小市场(预定)

@K54_TALK_THREE_DETECT;三人成行检测
;纯粹抄袭nyou的产物因为我完全没看懂（自豪）
#FUNCTION
#DIM CONST RELATED_PEOPLE = 59,15,16,50,58,1,11,13,14,29,49
#DIM CONST 多人对话 = 352
FOR LOCAL,0,10
	IF CFLAG:(RELATED_PEOPLE:LOCAL):300 == CFLAG:54:300 && !GETBIT(TCVAR:54:多人对话,LOCAL)
		SETBIT TCVAR:54:多人对话,LOCAL
		RETURNF RELATED_PEOPLE:LOCAL
	ENDIF
NEXT
RETURNF 0
@LOVE_COUNT()
#FUNCTION
#DIM DYNAMIC LOVECOUNT
FOR LOCAL,1,CHARANUM
	IF TALENT:LOCAL:3
		LOVECOUNT ++
	ENDIF
NEXT
RETURNF LOVECOUNT



IF 充填率(54,3) >= 250 && !RAND:4 && TALENT:3
	IF TALENT:7;恋人
	ELSE
	ENDIF
ELSEIF 充填率(54,3) >= 125 && !RAND:4 && TALENT:3
	IF TALENT:7;恋人
	ELSE
	ENDIF
ELSEIF 充填率(54,3) >= 75 && !RAND:4 && TALENT:3
	IF TALENT:7;恋人
	ELSE
	ENDIF
ELSEIF 充填率(54,3) >= 50 && !RAND:4 && TALENT:3
	IF TALENT:7;恋人
	ELSE
	ENDIF
ELSEIF 充填率(54,3) >= 35 && !RAND:4 && TALENT:3
	IF TALENT:7;恋人
	ELSE
	ENDIF
ELSEIF 充填率(54,3) >= 15 && !RAND:4 && TALENT:3
	IF TALENT:7;恋人
	ELSE
	ENDIF	
ELSEIF 充填率(54,3) >= 5 && !RAND:4 && TALENT:3
	IF TALENT:7;恋人
	ELSE
	ENDIF
ENDIF