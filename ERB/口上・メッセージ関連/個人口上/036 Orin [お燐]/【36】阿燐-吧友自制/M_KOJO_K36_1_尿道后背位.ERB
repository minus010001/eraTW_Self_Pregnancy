@尿道后背位

;尿道后背位

;TALENT:MASTER:形状 6巨根，10马屌
;CFLAG:36:1016;尿道扩张程度
CALL 尿道插入判断
IF RESULT
	PRINTFORM %CALLNAME:PLAYER%将
		SIF TEQUIP:拘束
			PRINTFORM 被拘束具牢牢禁錮著的
		PRINTFORM %CALLNAME:TARGET%的
	;尿道经验10次以上、かつ欲情LV4以上で縛られてないと誘ってきます
	IF (CFLAG:36:1016 > TALENT:MASTER:形状) && CFLAG:36:1015 > 10 && PALAM:欲情 >= PALAMLV:4 && TEQUIP:拘束 == 0 && RAND:2 == 0
		IF 前回指令 == "掰开小穴"
			PRINT 用手指展开陰部的同時、將
		ELSEIF 前回指令 == "掰开菊穴"
			PRINT 用手指推开臀瓣的同時、將
		;淫乱
		ELSEIF (TALENT:淫乱 || CFLAG:36:1018 >= 3) && RAND:2 == 0
			IF RAND:2
				PRINT 尻部推开展示出來的同時、將
			ELSE
				PRINT 將重要的地方展示出來的同時、將
			ENDIF
		ELSE
			PRINTFORM %TEXTR("將羞恥的地方暴露到一覽無遺的同時、將/將顯示出雌性氣息的股間清楚地暴露著的同時、將")%
		ENDIF
		;小悪魔
		IF TALENT:小悪魔 && RAND:3
			PRINT 無聲的轻笑著的
		;(叛逆でなくて淫乱) or(叛逆かつ恋慕)
		ELSEIF  ( (TALENT:淫乱 && TALENT:態度 == -1) || ((TALENT:態度 == 1) && TALENT:恋慕) ) && RAND:3
			SIF TALENT:小悪魔 && RAND:2 == 0
				PRINT 、大膽地
			PRINT 轻聲發起邀請的
		;叛逆かつ淫乱
		ELSEIF (TALENT:態度 == 1) && TALENT:淫乱
			PRINT 偏過頭、臉頰被羞恥染紅的
		;害羞
		ELSEIF (TALENT:羞恥心 == 1)
			PRINT 臉頰微紅、不自觉紧张的微笑的
		ELSE
			PRINTFORM %TEXTR("微笑着的/横卧着的")%
		ENDIF
		PRINTFORM %CALLNAME:TARGET%的
	ELSE
		;縛ってますか？
		IF TEQUIP:拘束
			PRINTFORM 被拘束具牢牢禁錮著的%CALLNAME:TARGET%那
		;拉米亚ですか？
		ELSEIF TALENT:妖怪 == 6
			SIF (前回指令 == "尿道后背位" || 前回指令 == "尿道正常位" || 前回指令 == "尿道后背位立位" || 前回指令 == "尿道正常位立位" || 前回指令 == "尿道屈居位") && RAND:2 == 0
				PRINT 用尾巴摩擦並纏繞著的
			PRINTFORM %CALLNAME:TARGET%的
		;尿道性交に慣れているなら誘ってきます
		ELSEIF (前回指令 == "尿道后背位" || 前回指令 == "尿道正常位") && RAND:2 == 0
			SIF (TALENT:羞恥心 == 1)
				PRINT 脸颊因羞涩而染红的
			IF 前回指令 == "尿道后背位立位" || 前回指令 == "尿道正常位立位" || 前回指令 == "正常位立位" || 前回指令 == "后背位立位"
				PRINTFORM 尻部扭动着
			ELSE
				PRINTFORM 四肢着地趴跪着
			ENDIF
			;淫乱かつ欲情4以上
			IF TALENT:淫乱 && PALAM:欲情 >= PALAMLV:4 && RAND:2 == 0
				PRINT 、并且轻轻摇晃着尻部诱惑着的
			ELSE
				PRINT 的
			ENDIF
			PRINTFORM %CALLNAME:TARGET%的
		ELSE
			IF (TALENT:態度 == 1) && CFLAG:36:1018 <= 2 && TALENT:恋慕 == 0
				PRINT 讓滿臉嫌惡的
			ELSEIF (TALENT:羞恥心 == 1)
				PRINT 臉頰因害羞而染上紅暈的
			ENDIF
			PRINTFORM %CALLNAME:TARGET%四肢著地趴跪下來、將
			SIF (TALENT:羞恥心 == 1) || ((TALENT:態度 == 1) && CFLAG:36:1018 <= 2 && TALENT:恋慕 == 0)
				PRINT 、
		ENDIF
		IF CFLAG:36:1018 >= 7
			PRINTFORM 已经完全沉迷快感的、敏感的
		ELSEIF CFLAG:36:1018 >= 5
			PRINTFORM 已经开发完毕的
		ELSEIF CFLAG:36:1018 >= 3
			PRINTFORM 已经初步开发的
		ELSE
			PRINTFORM 尚待开发的
		ENDIF
	ENDIF
	PRINTFORM %TEXTR("排尿的孔穴/尿道")%，
	PRINTFORM 用%TINKO(MASTER)%
	;PLAYERが施虐狂かつ尿道経験20以下
	IF ABL:PLAYER:施虐属性 >= 3 && CFLAG:36:1015 < 20
		PRINT 用力地
	;PLAYERが施虐狂or尿道性交に慣れている
	ELSEIF ((前回指令 == "尿道正常位" || 前回指令 == "尿道后背位" || 前回指令 == "尿道正常位立位" || 前回指令 == "尿道后背位立位") || ABL:PLAYER:施虐属性 >= 3) && RAND:2 == 0
		PRINT 一口気
	ELSEIF RAND:2 == 0 || CFLAG:36:1015 <= EXPLV:3
		PRINT 慎重地
	ENDIF
	;PLAYERが施虐狂or尿道経験20以上or恋慕かつ尿道感覚Lv3以上
	SIF ABL:PLAYER:施虐属性 >= 3 || CFLAG:36:1015 >= 20 || (TALENT:恋慕 && CFLAG:36:1018 >= 3)
		PRINT 直達根部地
	;PLAYERが施虐狂
	IF ABL:PLAYER:施虐属性 >= 3
		PRINT 壓入、
	ELSE
		PRINT 刺入、
	ENDIF
	IF TALENT:妊娠 && RAND:2 == 0
		PRINT 在轻轻撫摸膨脹的腹部的同時、
	ELSEIF TALENT:妊娠
		PRINT 細緻地關注並小心的不对腹腔中的小生命造成負擔的同時、
	ELSEIF RAND:2
		PRINTFORM %TEXTR("抚摸着/揉捏着/抓捏着")%屁股的同时
	;PLAYERが施虐狂orA経験20以上or恋慕かつA感覚Lv3以上
	ELSEIF (TALENT:体型 < 0) && (ABL:PLAYER:施虐属性 >= 3 || CFLAG:36:1015 >= 20 || (TALENT:恋慕 && CFLAG:36:1018 >= 3) )
		PRINT 將整个嬌小的身體緊緊抓住、
	ELSEIF ABL:PLAYER:施虐属性 >= 3 || CFLAG:36:1015 >= 20 || (TALENT:恋慕 && CFLAG:36:1018 >= 3)
		PRINT 將纖細的腰肢緊緊抓住、
	ENDIF
	;PLAYERが施虐狂or妊娠していない、かつ尿道経験3以上or恋慕かつ尿道感覚Lv4以上
	IF (ABL:PLAYER:施虐属性 >= 3 || TALENT:妊娠 == 0) && (CFLAG:36:1015 >= 20 || (TALENT:恋慕 && CFLAG:36:1018 >= 3) ) && RAND:3 == 0
		PRINT 像是在享受痛苦的反應一樣粗暴地
	ELSEIF (ABL:PLAYER:施虐属性 >= 3 || CFLAG:36:1015 >= 20 || CFLAG:36:1018 >= 3 || (ABL:PLAYER:技巧 > 2)) && RAND:2 == 0
		SIF (ABL:PLAYER:技巧 > 2) && RAND:2
			PRINT 從入口一直到最深處都一併玩弄的
		PRINT 给予沉重一击似地
	ELSEIF CFLAG:36:1015 < 50
		PRINTFORM %TEXTR("浅浅地/慢慢地")%
	ELSE
		PRINTFORM %TEXTR("將敏感黏膜咯哧咯哧的摩擦著地/激烈地")%
	ENDIF
	;PLAYERが施虐狂or尿道経験20以上or尿道感覚Lv3以上
	IF (ABL:PLAYER:施虐属性 >= 3 || CFLAG:36:1015 >= 20 || CFLAG:36:1018 >= 3) && RAND:3 == 0
		PRINTFORMW 用腰肢擊打著…
	ELSE
		PRINTFORMW %TEXTR("反復進出著/腰肢前後衝擊著/晃动着腰肢")%…
	ENDIF
	CALL 尿道射精
	前回指令 = "尿道后背位"

ELSE
	PRINTFORMW %CALLNAME:MASTER%尝试插入阿燐的尿道，却因为尿道开发不够，完全没办法进入。
	前回指令 = "尿道玩法失败"
ENDIF
CALL 尿道玩法经验结算
RETURN 1