@尿道屈居位
;尿道屈曲位
;TALENT:MASTER:形状 6巨根，10马屌
;CFLAG:36:1016;尿道扩张程度
CALL 尿道插入判断
IF RESULT
	PRINTFORM %CALLNAME:PLAYER%将
	IF 前回指令 == "掰开小穴"
		PRINTFORM %CALLNAME:TARGET%的身体
		PRINTFORM 做出將羞澀的淫穴向上展示一樣的姿勢、
	ELSE
		PRINTFORM 將%CALLNAME:TARGET%的身體翻轉過來、
		IF TEQUIP:継続接吻
			PRINTFORM 在熱情地親吻著的同時、
		ENDIF
	ENDIF
	IF CFLAG:36:1018 >= 7
		PRINTFORM 已经完全沉迷快感的、敏感的
	ELSEIF CFLAG:36:1018 >= 5
		PRINTFORM 已经开发完毕的
	ELSEIF CFLAG:36:1018 >= 3
		PRINTFORM 已经初步开发的
	ELSE
		PRINTFORM 尚待开发的
	ENDIF
	PRINTFORM 的尿道
	IF TALENT:妊娠 && ABL:PLAYER:施虐属性 >= 3
		PRINTFORMW 像是平日里一樣的激烈侵犯著……
	ELSE
		PRINTFORMW 以最深處为目標猛烈的衝擊著……
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:MASTER%尝试插入阿燐的尿道，却因为尿道开发不够，完全没办法进入。
	前回指令 = "尿道玩法失败"
ENDIF
CALL 尿道玩法经验结算
RETURN 1
