@厌弃_49
IF TARGET==49 && 关系_310()==0
	SELECTCASE RAND:3
		CASE 0
			PRINTFORMDW   似乎现在觉还不想搭理%CALLNAME:MASTER%。
		CASEELSE
			PRINTFORMDW   觉看也不打算看%CALLNAME:MASTER%一眼。
	ENDSELECT
	TFLAG:好感度管理=MAX(TFLAG:好感度管理-RAND:5,0)
	TFLAG:信赖度管理=MAX(TFLAG:信赖度管理-RAND:3,0)
	IF !RAND:5
		TALENT:心情=MAX(TALENT:心情-RAND:2,-1)
	ENDIF
ENDIF

