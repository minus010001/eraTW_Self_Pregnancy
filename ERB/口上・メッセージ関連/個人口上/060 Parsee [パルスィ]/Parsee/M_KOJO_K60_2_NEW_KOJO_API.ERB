;================================
;custom com
;y is the id of the button, range from 0 to 29
;カスタムコマンド、0～29まで設定可能
;使用する際はSKIPSTART～SKIPENDを外してください。
;================================

;コマンド名
@KOJO_EGG_COM_NAME_K60_0
RESULTS = 桥姬服务

;judge
;judge the button can be shown, if return 1, it will show
;コマンド使用条件、通常のCOM_ABLE関数と同じくRETURN 1すれば実行可能としてコマンドが表示される
@KOJO_EGG_COM_ABLE_K60_0
SIF WORKING(TARGET)
	RETURN 0
SIF !SHIRAHU(TARGET)
	RETURN 0

RETURN 1

;================================
;実行可能判定に係る部分、通常は書かなくてよい
@KOJO_EGG_COM_K60_0
CALL K60_HASHIHIME_SERVICES()

;refer to @CAN_COM9(ARG)
;or you can don't write it
; @KOJO_EGG_CAN_COM_K60_0(skipFlag)
; 	#DIM skipFlag
; 	SKIPDISP skipFlag
; 	SKIPDISP 0
;================================
