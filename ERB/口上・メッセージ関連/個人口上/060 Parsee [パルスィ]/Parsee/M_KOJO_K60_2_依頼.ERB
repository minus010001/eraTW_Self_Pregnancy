;更新日期：2025年2月2日
;==================================================
;Common Request Dialogue
;依頼口上・各依頼で共通の口上
;==================================================
@M_KOJO_EGG_IRAI_K60(ROLE, SCENE, IRAI_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
		;The character making the request
#DIMS SCENE    ;セリフの表示されるタイミング
		;When the dialogue appears
#DIM  IRAI_ID  ;依頼自体のID
		;The request ID
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
		;The requestee's ID
#DIM  CHK_ID   ;依頼IDだけ抜き出したもの
		;The request type ID (cut some wood, water my flowers, etc.)
#DIMS IRAI_NAME;依頼名
		;The name of the request
CHK_ID    = IRAI_ID % 1000
CLIENT_ID = IRAI_ID_TO_CLIENT(IRAI_ID)
IRAI_NAME = %STR_DATA_IRAI(IRAI_ID, OPR_委托名, CLIENT_ID)%
RESULT = 0
IF CHK_ID > 100
	TRYCALLFORM M_KOJO_EGG_IRAI_UNIQUE_{CHK_ID}_K60(ROLE, SCENE, CLIENT_ID)
ELSE
	TRYCALLFORM M_KOJO_EGG_IRAI_{CHK_ID}_K60(ROLE, SCENE, CLIENT_ID)
ENDIF
SIF RESULT
	RETURN 1
;--------------------------------------------------
;Here is the common opening statement for each request.
;If you display the common comments for each request, the common comments will not be displayed.
;The items circled by [ ] are the background sentences before and after the opening remarks and the processing that takes place.
;Please use this as a guideline for whether to WAIT or not (L and W stand for PRINTFORML and PRINTFORMW, respectively).
;ここにあるのは各依頼で共通の口上です
;依頼ごとの口上を表示した場合は共通の口上は表示されません
;【】で囲ってあるのは口上前後の地の文や発生する処理です
;WAITをするか等の目安にしてください（L,WはそれぞれPRINTFORML,PRINTFORMWの略）
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时
; ●When a request is made to you.
; [w It seems that [she] has a favor to ask of [you].
;-◆Dialogue
; [Request Data Display].
; [INPUT (Do you accept the request? Y/N)].
;●依頼を持ちかけられる時
;【W どうやら［彼女］は［あなた］に頼み事があるようだ】
;【◆口上】
;【依頼データ表示】
;【INPUT（受託するかY/N）】
		SELECTCASE K60_ATTITUDE()
			CASE 0
				SELECTCASE RAND:3
					CASE 0
						PRINTFORML 「希望没有打扰到你……能请你帮我个忙吗？」
					CASE 1
						CALL PRINT_STRL, @"「达令，能帮我个忙吗？」"
					CASE 2
						PRINTFORML 「现在有空吗，%K60_MASTER()%？想请你帮我点忙……」
				ENDSELECT
			CASE 1
				SELECTCASE RAND:3
					CASE 0
						PRINTFORML 「有空帮一下你的爱人吗？」
					CASE 1
						CALL PRINT_STRL, @"「你看起来总是“精”力充沛呢……能帮我个忙吗？」"    

					CASE 2
						PRINTFORML 「有件事需要你帮忙。会给相应的报酬」
				ENDSELECT
			CASE 2
				SELECTCASE RAND:3
					CASE 0
						PRINTFORML 「我不太想老是依赖你……」
						PRINTFORML 「但我也不想因为这事就麻烦大家，而且他们大概也不会帮我吧」
					CASE 1
						PRINTFORML 「哈，看来我遇到了你少有的不去骚扰哪个女孩子的时候」
						PRINTFORML 「既然你很闲，不如帮我个忙吧」
					CASE 2
						PRINTFORML 「为什么不让自己显得更有用点，过来帮我做点事呢？」
				ENDSELECT
			CASE 3
				SELECTCASE RAND:3
					CASE 0
						PRINTFORML 「听说你是个很有能力的%PRINT_MALE_CN("男人", MASTER)%……能帮帮我吗？」
					CASE 1
						PRINTFORML 「有空帮我做件事吗？」
					CASE 2
						CALL M_KOJO_EGG_COLOR_K60_JEALOUS
						PRINTFORML 「你的能力比我强许多……有件事希望你能帮我」
						CALL M_KOJO_EGG_COLOR_K60
				ENDSELECT
		ENDSELECT
	CASE OPR_委托非受託时
;●When you saw only the contents and did not accept the request.
;- [L [you] politely declined
;-◆Dialogue
;W She looks disappointed.
;●内容だけ見て受託しなかった時
;【L ［你］は丁重にお断りした】
;【◆口上】
;【W ［彼女］は残念そうな顔をしている】
		SELECTCASE K60_ATTITUDE()
			CASE 0
				SELECTCASE RAND:3
					CASE 0
						PRINTFORML 「……会太麻烦你吗？」
					CASE 1
						PRINTFORML 「……明白了」
					CASE 2
						PRINTFORML 「剩下的我自己应该能做完……」
				ENDSELECT
			CASE 1
				SELECTCASE RAND:3
					CASE 0
						PRINTFORML 「希望你不仅仅是因为我的肉体才对我感兴趣」
					CASE 1
						PRINTFORMDL 帕露西皱着眉，嘴里轻声说着什么
					CASE 2
						PRINTFORML 「哼，今晚别想和我用一个枕头了」
				ENDSELECT
			CASE 2
				SELECTCASE RAND:3
					CASE 0
						PRINTFORML 「我不会惊讶的」
						PRINTFORML 「帮我的忙意味着要和一个女人做些和交配无关的事」
					CASE 1
						CALL M_KOJO_EGG_COLOR_K60_VERY_JEALOUS
						CALL PRINT_STRL, @"「我理解，你可能有更重要的事情。或者，是除我以外的特别的人……」"
						CALL M_KOJO_EGG_COLOR_K60
					CASE 2
						PRINTFORML 「你拒绝得还真温和。我原本还怀疑你会往我脸上啐一口唾沫」
				ENDSELECT
			CASE 3
				SELECTCASE RAND:3
					CASE 0
						CALL M_KOJO_EGG_COLOR_K60_JEALOUS
						PRINTFORML 「因为你在陪另一个特别的人，是吗？」
						CALL M_KOJO_EGG_COLOR_K60
					CASE 1
						CALL M_KOJO_EGG_COLOR_K60_JEALOUS
						PRINTFORML 「如果我对你来说更重要，大概就会愿意抽时间陪我了吧……」
						CALL M_KOJO_EGG_COLOR_K60
					CASE 2
						PRINTFORML 「嗯？那好吧……」
				ENDSELECT
		ENDSELECT

	CASE OPR_委托受託时
;●When [you] accepted [her] request
;W [you] accepted [her] request
;- [◆Dialogue
;●依頼を受託した時
;【W ［你］は［彼女］の依頼を受けた】
;【◆口上】
		SELECTCASE K60_ATTITUDE()
			CASE 0
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「嗯，就知道你值得我相信」
						PRINTFORMDW 帕露西轻轻鞠了一躬，并露出了微笑
					CASE 1
						PRINTFORMW 「帅气、聪明、迷人，还勤奋？还真是个完美的 %PRINT_MALE_CN("男", MASTER)%朋友呢」
						PRINTFORMDW 帕露西的耳朵渐渐地染上了红晕……
					CASE 2
						PRINTFORML 「谢谢, %K60_MASTER()%」
						PRINTFORMW 「要是觉得太麻烦了说一声就行，好吗？」
				ENDSELECT
			CASE 1
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「你值得我的依靠」
						PRINTFORMDW 帕露西对你露出了微笑。
					CASE 1
						PRINTFORML 「真好！」
						PRINTFORMW 「我不想等太久，好吗？你不在的时候我都会想着你哦..」
					CASE 2
						PRINTFORMW 「好好期待我准备给你的奖励吧」
				ENDSELECT
			CASE 2
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「我想你大概会忘掉这件事吧」
					CASE 1
						PRINTFORMW 「这么做只是为了奖励，对吧？」
					CASE 2
						PRINTFORMW 「我想应该对你说声“谢谢”……等你完成这件事后再说」
				ENDSELECT
			CASE 3
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「十分感谢，%K60_MASTER()%」
					CASE 1
						PRINTFORMW 「我不会忘记你的诺言……」
					CASE 2
						PRINTFORMW 「如果你做不了的话，请告诉我，好吗？我不会生气的」
				ENDSELECT
		ENDSELECT

	CASE OPR_委托确认时
;●[366] When doing "Listen to Request" after accepting the request
;◆Dialogue
;-[Request Data Display].
;[INPUT (Discard or Y/N)].
;●[366]で依頼内容を依頼人に確認した時
;【◆口上】
;【依頼データ表示】
;【INPUT（破棄するかY/N）】
		SELECTCASE K60_ATTITUDE()
			CASE 0, 3
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「难道你这就忘了吗……」
					CASE 1
						PRINTFORMW 「需要我再说一次吗？」
					CASE 2
						PRINTFORMW 「好吧，那这次可要记好了……」
				ENDSELECT
			CASE 1
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「希望你不会因为满脑子都是和别人交配才忘了正事」    

					CASE 1
						PRINTFORMW 「请你这次不要再被那些和别人上床的事情分散注意力了」
					CASE 2
						PRINTFORMW 「我知道这事和做爱没关系，但它真的很重要」
				ENDSELECT
			CASE 2
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「希望这次你能更认真些」
					CASE 1
						PRINTFORMW 「当然。反正我说的话也没人在乎」
					CASE 2
						PRINTFORMW 「我知道你这家伙对骗女孩子和你上床以外的事都不在乎，但这回你真的得认真点了」
				ENDSELECT
		ENDSELECT

	CASE OPR_委托破棄时
;When you cancel a request you have been entrusted with.
; [◆Dialogue].
; W I have discarded the request.
;●受託した依頼を破棄する時
;【◆口上】
;【W 依頼を破棄しました。】
		SELECTCASE K60_ATTITUDE()
			CASE 0
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「……好吧」
					CASE 1
						PRINTFORMW 「真可惜」
					CASE 2
						PRINTFORMW 「希望你不会觉得这件事完全不重要……」
				ENDSELECT
			CASE 1
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「你最好用那段时间来满足我」
					CASE 1
						CALL M_KOJO_EGG_COLOR_K60_JEALOUS
						PRINTFORMW 「又打算去找其他女人了吗？」
						CALL M_KOJO_EGG_COLOR_K60
					CASE 2
						PRINTFORMW 「我希望你之所以放弃是因为这对你来说太难，而不是你除了我的肉体以外都不关心」
				ENDSELECT
			CASE 2
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「起码你直接告诉了我你不想做，而不是无视我」
					CASE 1
						CALL M_KOJO_EGG_COLOR_K60_VERY_JEALOUS
						PRINTFORMW 「我明白，这件事对你的本事来说不算什么难题。你只是不在乎，对吧？」
						CALL M_KOJO_EGG_COLOR_K60
					CASE 2
						PRINTFORMW 「我早该想到你懒得搭理这种事」
				ENDSELECT
			CASE 3
				SELECTCASE RAND:3
					CASE 0
						PRINTFORMW 「还是太难了吗？」
					CASE 1
						CALL M_KOJO_EGG_COLOR_K60_JEALOUS
						PRINTFORMW 「哦？还有更重要的事情吗？」    

						CALL M_KOJO_EGG_COLOR_K60
					CASE 2
						PRINTFORMW 「本以为你能做到的……」
				ENDSELECT
		ENDSELECT
	CASE OPR_成功报告时
;Success
;When the request is completed and reported: Continued from the above dialogue.
;[◆Dialogue]
; [Obtaining a source, obtaining a reward such as an item, etc.].
;●報告時（成功）
;【依頼実行と報告が同時の時：▲上の口上から続き】
;【◆口上】
;【ソースの入手・アイテム等の報酬獲得】
		SELECTCASE K60_ATTITUDE()
			CASE 0
				;todo expand
				; SELECTCASE RAND:3
				; 	CASE 0
				PRINTFORMW 「谢谢你，%K60_MASTER()%！」
				; 	CASE 1
				; 		PRINTFORML 
				; 	CASE 2
				; 		PRINTFORML 
				; ENDSELECT
			CASE 1
				; SELECTCASE RAND:3
				; 	CASE 0
				PRINTFORMW 「多谢了，%K60_MASTER()%」
				PRINTFORMDW 帕露西对你笑了笑
				PRINTFORMW 「给你一点特殊奖励吧……」
				CALL MUTUAL_KISS
				; 	CASE 1
				; 		PRINTFORML 
				; 	CASE 2
				; 		PRINTFORML 
				; ENDSELECT
			CASE 2
				SELECTCASE RAND:3
					CASE 0
						CALL M_KOJO_EGG_COLOR_K60_VERY_JEALOUS
						PRINTFORML 「我丝毫不意外。你做事一直都很完美呢」
						PRINTFORMW 「看来贤者们和神社的巫女都该下岗了，让你来做事才更好」
						CALL M_KOJO_EGG_COLOR_K60
						PRINTFORMDW ……她是不是更希望你没有帮她？
					CASE 1
						PRINTFORMW 「……」
						PRINTFORMW 「……谢谢你」
					CASE 2
						PRINTFORMW 「如此看来，你还是能做到和“骚扰别人”无关的事情的」
				ENDSELECT
			CASE 3
				; SELECTCASE RAND:3
				; 	CASE 0
				PRINTFORMW 「万分感谢, %K60_MASTER()%！」
				CALL M_KOJO_EGG_COLOR_K60_JEALOUS
				PRINTFORMW 「你是天才吗……」
				CALL M_KOJO_EGG_COLOR_K60
				; 	CASE 1
				; 		PRINTFORML 
				; 	CASE 2
				; 		PRINTFORML 
				; ENDSELECT
		ENDSELECT

	CASE OPR_失败报告时
;When a report is forgotten or expired, it is called up at the end of the encounter.
;(During the encounter) [Greetings, etc.] → [Good or Bad Mood] → [Detection of Inferiority] → [Holiday Event] → [◆Coco! (In addition, the date invitation will not occur if the request is judged to have failed.)
;◆Dialogue
;(If the request is judged as a failure, a date invitation will not occur)
; ○ Change of mood, additional penalties, etc. should be handled by the dialogue.
;○直接失敗報告は現在「06_誰か教えて！」のみ
;●報告忘れ・期限切れになった際、遭遇時の最後に呼び出される
;（遭遇時）【挨拶等】→【心情の良し悪し】→【劣情察知】→【祭日イベント】→【◆ココ！】（尚、依頼失敗判定があると約會のお誘いは発生しません）
;【◆口上】
;○心情の変化や追加のペナルティ等は口上側で処理してくださ
		SELECTCASE K60_ATTITUDE()
			;todo expand
			CASE 0
				; SELECTCASE RAND:3
				; 	CASE 0
				PRINTFORMW 「%K60_MASTER()%……别和我说你忘了？我说过这件事特别重要的！」
				; 	CASE 1
				; 		PRINTFORML 
				; 	CASE 2
				; 		PRINTFORML 
				; ENDSELECT
			CASE 1
				; SELECTCASE RAND:3
				; 	CASE 0
				PRINTFORMW 「不是吧，%K60_MASTER()%？你难道压根就没放在心上？」
				; 	CASE 1
				; 		PRINTFORML 
				; 	CASE 2
				; 		PRINTFORML 
				; ENDSELECT
			CASE 2
				; SELECTCASE RAND:3
				; 	CASE 0
				PRINTFORMW 「我早该想到你会这样」
				; 	CASE 1
				; 		PRINTFORML 
				; 	CASE 2
				; 		PRINTFORML 
				; ENDSELECT
			CASE 3
				; SELECTCASE RAND:3
				; 	CASE 0
				PRINTFORMW 「你好像有更紧要的事。真可惜……」
				; 	CASE 1
				; 		PRINTFORML 
				; 	CASE 2
				; 		PRINTFORML 
				; ENDSELECT
		ENDSELECT

;I don't think this is used anymore
	CASE OPR_委托报告不要
;○一般依頼では現在「02_お花に水をあげて」「09_誰かの落とし物」のみ
;●起床時、報告不要依頼を達成していた場合に呼ばれる口上
;（起床時）【起床メッセージ】→【X時間寝た等】→【◆ココ！】→【おみくじ・補充等】→【一日開始】
;○地の文はありません
;○口上主がその場にいなくても表示されます（MASTERが寝る時のイベント口上と同じタイプ）
		PRINTFORMDW CASE OPR_委托报告不要 was called! Report this!

ENDSELECT
RETURN 1

;==================================================
;依頼口上・一般依頼01：掃除を手伝って
;==================================================
@M_KOJO_EGG_IRAI_1_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	;Request made
	CASE OPR_委托提示时

		RETURN 0

	;Request declined
	CASE OPR_委托非受託时

		RETURN 0
	;Request accepted
	CASE OPR_委托受託时

		RETURN 0

	;Listen to Request (after accepting it)
	CASE OPR_委托确认时

		RETURN 0

	;Request canceled
	CASE OPR_委托破棄时

		RETURN 0

	;Request completed (after handing it in but before the completion text displays)
	CASE OPR_委托实行直前
;The requestee is the TARGET here
;○この依頼は依頼主がTARGETです
		IF YOGORE:(GET_CHARAHOME(TARGET)) < 1000
	;If you cleaned the requestee's room before accepting the request.
	;[◆Dialogue]
	;W %CALLNAME:TARGET%'s room is already clean enough.
	;[▼ go straight to the bottom of the dialogue]
	;先に掃除してしまっていた場合
	;【◆口上】
	;【W ［彼女］の部屋は十分にきれいだ】
	;【▼そのまま下の口上へ】

			RETURN 0
		ELSE
	;While cleaning with the requestee.
	;[◆Dialogue]
	;W You clean the room with the help of %CALLNAME:TARGET%.
	;[▼ go straight to the bottom of the dialogue]
	;一緒に掃除する場合
	;【◆口上】
	;【W ［你］は［彼女］と協力して部屋の掃除を行なった】
	;【▼そのまま下の口上へ】

			RETURN 0
		ENDIF
	;Request completed (after the completion text displays)
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】

		RETURN 0
;Success
	CASE OPR_成功报告时
;報酬：低級素材
;【▲上の口上から続き】
;【◆口上】
;【報酬獲得】

		RETURN 0
	;Request Failed
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼02：お花に水をあげて
;==================================================
@M_KOJO_EGG_IRAI_2_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;【◆口上】
;【W ［你］は［依頼主］の家の鉢植えや植木に水をあげた】
;【▼そのまま下の口上へ】
		IF ROLE == OPR_委托人
	;水やりした時、依頼主である口上主がTARGETの場合

			RETURN 0
		ELSEIF ROLE == OPR_目标
	;水やりした時、依頼主ではない口上主がTARGETの場合

			RETURN 0
		ENDIF
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】
		IF ROLE == OPR_委托人
	;水やりした時、依頼主である口上主がTARGETの場合
	;この場合はそのまま下の口上へ続きます

			RETURN 0
		ELSEIF ROLE == OPR_目标
	;水やりした時、依頼主ではない口上主がTARGETの場合

			RETURN 0
		ENDIF
	CASE OPR_成功报告时
;報酬：\500

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
	CASE OPR_委托报告不要

		RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼03：いっしょにあそぼ
;※この依頼は[幼稚]を持っているキャラのみ発生します
;==================================================
@M_KOJO_EGG_IRAI_3_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;○この依頼は依頼主がTARGETです
;【◆口上】
;【W ［你］は［彼女］と遊んだ】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_成功报告时
;報酬：好感度と信頼度のみ（心情が良くなる）
;【▲上の口上から続き】
;【◆口上】

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼04：料理上手になりたい
;==================================================
@M_KOJO_EGG_IRAI_4_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;○この依頼は依頼主がTARGETです
;【◆口上】
;【L ［你］は［彼女］と一緒に料理をした】
;【W ［料理名］を作ります】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_成功报告时
;報酬：\500
;【▲上の口上から続き】
;【◆口上】
;【報酬獲得】
;【L できた料理は二人でいっしょに頂いた】
;【食事効果（料理採点）】

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼05：特訓につきあって
;==================================================
@M_KOJO_EGG_IRAI_5_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;○この依頼は依頼主がTARGETです
;【◆口上】
;【W ［你］は［彼女］と弾幕ごっこの特訓をした】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_成功报告时
;報酬：\500
;【▲上の口上から続き】
;【◆口上】
;【報酬獲得】

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼06：誰か教えて！
;==================================================
@M_KOJO_EGG_IRAI_6_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
		IF IS_LIBRARY(CFLAG:MASTER:現在位置) && TARGET == CLIENT_ID
	;［しらべもの］と［教えてあげる］が重なっている場合
	;【◆口上】
	;【L ［你］は［彼女］の疑問に答えるために［現在地］を訪れた】
	;【L ここなら役に立つ資料が見つかりそうだ】
	;【INPUT（しらべもの/教えてあげる）】
	;【しらべもの＞［你］と［彼女］は［現在地］で調べ物をした】
	;【▼そのまま下の口上へ】
	;【教えてあげる＞［教えてあげる］へジャンプ】

			RETURN 0
		ELSEIF IS_LIBRARY(CFLAG:MASTER:現在位置)
	;［しらべもの］をした時、依頼主ではない口上主がTARGETの場合
	;【◆口上】
	;【［你］は［現在地］で調べ物をした】
	;【▼そのまま下の口上へ】

			RETURN 0
		ELSE
	;［教えてあげる］時（依頼主がTARGET）
	;【◆口上】
	;【L ［你］は［彼女］の疑問に答えた】
	;【成功：［彼女］の疑問は氷解した！】
	;【失敗：［彼女］は納得できなかったようだ……】
	;【▼そのまま下の口上へ】

			RETURN 0
		ENDIF
	CASE OPR_委托实行直后
	;【▲上の口上から続き】
	;【◆口上】
		SELECTCASE CFLAG:CLIENT_ID:依頼状況
			CASE 2
	;［しらべもの］をした時
				IF ROLE == OPR_委托人
		;一緒に［しらべもの］をした時

					RETURN 0
				ELSEIF ROLE == OPR_目标
		;依頼主ではない口上主の前で［しらべもの］をした時

					RETURN 0
				ENDIF
			CASE 3
	;成功した時
	;【▼そのまま下の口上へ】

				RETURN 0
			CASE 6
	;失敗した時
	;【▼そのまま失敗報告時の口上へ】

				RETURN 0
		ENDSELECT
	CASE OPR_成功报告时
;報酬：\500

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼07：大掃除が終わらない
;==================================================
@M_KOJO_EGG_IRAI_7_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;○この依頼は依頼主がTARGETです
		IF YOGORE:(GET_CHARAHOME(TARGET)) < 1000
	;先に掃除してしまっていた場合
	;【◆口上】
	;【W ［彼女］の部屋は十分にきれいだ】
	;【▼そのまま下の口上へ】

			RETURN 0
		ELSE
	;一緒に掃除する場合
	;【◆口上】
	;【W ［你］は［彼女］と協力して徹底的に部屋を掃除した】
	;【▼そのまま下の口上へ】

			RETURN 0
		ENDIF
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_成功报告时
;報酬：\1500
;【▲上の口上から続き】
;【◆口上】
;【報酬獲得】

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼08：手紙を届けて
;※この依頼はキャラによっては発生しません
;==================================================
@M_KOJO_EGG_IRAI_8_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;○この依頼では手紙を渡される側の口上です
;【◆口上】
;【W ［你］は［彼女］に預かっていた手紙を渡した】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】

		RETURN 0
	CASE OPR_成功报告时
;報酬：素材4つ

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼09：誰かの落とし物
;==================================================
@M_KOJO_EGG_IRAI_9_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;○この依頼では落とし物を渡される側の口上です
;【◆口上】
;【W ［你］は［彼女］に預かっていた落とし物を返した】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】

		RETURN 0
	CASE OPR_成功报告时
;報酬：上級素材

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
	CASE OPR_委托报告不要

		RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼10：どうしてもみつからない（場所探しパターン）
;==================================================
@M_KOJO_EGG_IRAI_10_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;【◆口上】
;【W あたりを探していた［你］はついに探していた落とし物を発見した】
;【▼そのまま下の口上へ】
		IF ROLE == OPR_委托人
	;依頼主である口上主と一緒に発見した場合

			RETURN 0
		ELSEIF ROLE == OPR_目标
	;発見した時、依頼主ではない口上主がTARGETの場合

			RETURN 0
		ENDIF
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】
		IF ROLE == OPR_委托人
	;依頼主である口上主と一緒に発見した場合

			RETURN 0
		ELSEIF ROLE == OPR_目标
	;発見した時、依頼主ではない口上主がTARGETの場合

			RETURN 0
		ENDIF
	CASE OPR_成功报告时
;報酬：上級素材2つ

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼11：どうしてもみつからない（人物探しパターン）
;==================================================
@M_KOJO_EGG_IRAI_11_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;○この依頼では失くしもの持っていた側の口上です
;【◆口上】
;【W 聞き込みを続けていた［你］はついに探していた落とし物を発見した】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】

		RETURN 0
	CASE OPR_成功报告时
;報酬：\3200

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼12：懲らしめて！
;==================================================
@M_KOJO_EGG_IRAI_12_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;○この依頼では討伐される側の口上です
		IF CANNOT_DANMAKU() == 1
	;屋内のとき（勝負できない）
	;【◆口上】
	;【W 屋外で挑もう…】

			RETURN 0
		ELSEIF CANNOT_DANMAKU() == 2
	;工作中（勝負できない）
	;【◆口上】
	;【W お取り込み中だ…】

			RETURN 0
		ELSEIF CANNOT_DANMAKU() == 3
	;你が疲労中（勝負できない）
	;【◆口上】
	;【W 今は疲れている…】

			RETURN 0
		ELSEIF CANNOT_DANMAKU() == 4
	;口上主が疲労竜（勝負できない）
	;【◆口上】
	;【W 今挑むのはフェアじゃない…】

			RETURN 0
		ELSEIF CANNOT_DANMAKU() == 5
	;既に今日一度弾幕勝負をしている（勝負できない）
	;【◆口上】
	;【W 今日はもう挑めない…】

			RETURN 0
		ELSE
	;【◆口上】
	;【W 勝負開始！！】
	;【以降はそのまま弾幕口上が呼ばれます】

			RETURN 0
		ENDIF
	;【▼そのまま下の口上へ】
	CASE OPR_委托实行直后
;○この依頼では討伐される側の口上です
		IF CFLAG:CLIENT_ID:依頼状況 == 2
			IF CANNOT_DANMAKU() == 0
			;败犬
			;【▲上の口上から続き】
			;【L ［彼女］の討伐に失敗した……】
			;【W ……こうなったら後日出直すしかない】
			;【◆口上】
				RETURN 0
			ELSE
			;挑めなかった場合
			;【▲上の口上から続き】
			;【◆口上】
				RETURN 0
			ENDIF
		ELSEIF CFLAG:CLIENT_ID:依頼状況 == 3
	;勝利
	;【▲上の口上から続き】
	;【W ［彼女］を懲らしめた！】
	;【◆口上】

			RETURN 0
		ENDIF
	CASE OPR_成功报告时
;報酬：賞金（時価）

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼13：うまい魚が食べたいなぁ
;==================================================
@M_KOJO_EGG_IRAI_13_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前
;○この依頼は依頼主がTARGETです
;【◆口上】
;【L ［你］は［彼女］にうまい魚を渡した】
;【L 何だか生きる希望がわいてきたよ。】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_委托实行直后
;○この依頼は必ず実行後達成します
;【▲上の口上から続き】
;【◆口上】
;【▼そのまま下の口上へ】

		RETURN 0
	CASE OPR_成功报告时
;報酬：白米３個

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT

;==================================================
;依頼口上・一般依頼16：お悩み相談
;==================================================
@M_KOJO_EGG_IRAI_16_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前

		RETURN 0
	CASE OPR_委托实行直后
		IF TFLAG:193 == 1
		;正解の選択肢
		ELSEIF TFLAG:193 == 2
		;ハズレの選択肢だったが逆に正解
		ELSEIF TFLAG:193 == -1
		;ハズレの選択肢
		ELSEIF TFLAG:193 == -2
		;正解の選択肢だったが逆にハズレ
		ENDIF
		RETURN 0
	CASE OPR_成功报告时
;報酬：野菜か果物

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
	CASE "口上委託率"
;＜この依頼特有の分岐です。(RESULT:100の値)％の確率で"口上専用"が発生します＞
		RESULT:100 = 0

;＜RESULT:YYに0以外の数値を入れると、該当のお悩みを発生させなくできます（詳しくは一般依頼16_お悩み相談参照）＞
;1,2:弾幕ごっこの悩み 3,4:晩酌の悩み 5,6:本の悩み 7,8:香料の悩み
;9,10:音楽の悩み 11,12:服の悩み 13:髪型の悩み 14:参拝の悩み 15:ダイエットの悩み
;16,17:菜園の悩み 18:休日の悩み 19:人気の悩み 20:集中力の悩み 21:御三家の悩み 22:進化の悩み
		RESULT:1 = 0
		RESULT:2 = 0
		RETURN 1
	CASE "口上専用"
;＜この依頼特有の分岐です。(口上委託率)％の確率で発生します＞
;16,お悩み相談 の場合
;RESULT        発生判定,0だと発生しません
;RESULT:1      正解の選択肢（ただしRESULT:2が0だと、一定確率で逆にハズレる）
;RESULT:2      逆張り正解／ハズレ判定の消去,1だと逆張りが無くなります
;RESULTS:0     お悩み文章（自动で『』で囲まれます）
;RESULTS:1～3  選択肢１～３
;何も発生しない状態になると無限ループになるので注意してください
;●追記メモ
;RESULT:1を4～6にするとそれぞれ1～3がハズレ（残り2つが正解）になります
;RESULT:1を7にするとどれも正解になります
;RESULT:1を8にするとどれもハズレになります
;1～3以外の場合は逆張り判定が自動で無くなります
		RESULT = 0

;▼サンプル
[SKIPSTART]
	SELECTCASE RAND:5
	CASE 0
		;このお悩みは、恋慕で発生し、親密によって正解が変わります
		SIF TALENT:恋慕
			RESULT = 1
		RESULTS:0 = 赤ちゃん作るなら何人がいいかな？
		RESULTS:1 = ５人でバスケチームを作ろう
		RESULTS:2 = ９人で野球チームを作ろう
		RESULTS:3 = １１人でサッカーチームを作ろう
		IF ABL:親密 >= 100
			RESULT:1 = 3
		ELSEIF ABL:親密 >= 20
			RESULT:1 = 2
		ELSE
			RESULT:1 = 1
		ENDIF
	CASE 1
		;このお悩みは、無条件で発生し、正解は絶対に2番で固定化されます
		SIF 1
			RESULT = 1
		RESULTS:0 = どのジョブがいいかな？
		RESULTS:1 = かっ恋ナイト
		RESULTS:2 = シーフが便利だよ
		RESULTS:3 = 青魔道士最強！
		RESULT:1 = 2
		RESULT:2 = 1
	CASE 2
		;このお悩みは、夏の月に発生し、正解がランダムで変わります
		SIF DAY:2 == 2
			RESULT = 1
		RESULTS:0 = どの水着を買おうかな？
		RESULTS:1 = かわいい連衣裙
		RESULTS:2 = クールなセパレート
		RESULTS:3 = 色っぽいビキニ
		RESULT:1 = RAND:3 + 1
	CASE 3
		;このお悩みは、無条件で発生し、1番と3番が正解になります（5 - 3 = 2番がハズレになります）
		SIF 1
			RESULT = 1
		RESULTS:0 = ワンちゃんの名前は何にしよう？
		RESULTS:1 = ボロンゴ
		RESULTS:2 = ゲレゲレ
		RESULTS:3 = プックル
		RESULT:1 = 5
	CASE 4
		;このお悩みは、無条件で発生し、どれを選んでも正解になります
		SIF 1
			RESULT = 1
		RESULTS:0 = 夕飯は何がいい？
		RESULTS:1 = 肉じゃが
		RESULTS:2 = コロッケ
		RESULTS:3 = 焼き魚
		RESULT:1 = 7
	ENDSELECT
[SKIPEND]

		SIF !RESULT
			RESTART
		RETURN 1
ENDSELECT

;==================================================
;依頼口上・固有依頼101：
;固有依頼の仕様はキャラによって異なりますが依頼番号は101～で共通です
;OPR_委托名部分に依頼名を入れて下さい
;==================================================
@M_KOJO_EGG_IRAI_依頼名_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
	CASE OPR_委托提示时

		RETURN 0
	CASE OPR_委托非受託时

		RETURN 0
	CASE OPR_委托受託时

		RETURN 0
	CASE OPR_委托确认时

		RETURN 0
	CASE OPR_委托破棄时

		RETURN 0
	CASE OPR_委托实行直前

		RETURN 0
	CASE OPR_委托实行直后

		RETURN 0
	CASE OPR_成功报告时
;報酬：

		RETURN 0
	CASE OPR_失败报告时

		RETURN 0
	CASE OPR_委托报告不要

		RETURN 0
ENDSELECT

[SKIPSTART]
;＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
;依頼口上テンプレのテンプレ
;==================================================
;依頼口上・一般依頼Y：
;==================================================
@M_KOJO_EGG_IRAI_Y_K60(ROLE, SCENE, CLIENT_ID)
#DIMS ROLE     ;キャラの立場、依頼者かそうでないか
#DIMS SCENE    ;セリフの表示されるタイミング
#DIM  CLIENT_ID;依頼者IDだけ抜き出したもの
;--------------------------------------------------
;RETURN 0 の部分は共通の口上が表示されます
;依頼ごとの口上を表示する部分は RETURN 0 を RETURN 1 にしてください
;--------------------------------------------------
SELECTCASE SCENE
CASE OPR_委托提示时
	
	RETURN 0
CASE OPR_委托非受託时
	
	RETURN 0
CASE OPR_委托受託时
	
	RETURN 0
CASE OPR_委托确认时
	
	RETURN 0
CASE OPR_委托破棄时
	
	RETURN 0
CASE OPR_委托实行直前
	
	RETURN 0
CASE OPR_委托实行直后
	
	RETURN 0
CASE OPR_成功报告时
;報酬：
	
	RETURN 0
CASE OPR_失败报告时
	
	RETURN 0
;CASE OPR_委托报告不要
;＜この依頼にはありません＞
;	RETURN 0
ENDSELECT
[SKIPEND]
