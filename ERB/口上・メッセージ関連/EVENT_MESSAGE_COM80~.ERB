;-------------------------------------------------
;80,手淫
;-------------------------------------------------
@MESSAGE_COM80
VARSET LOCAL
IF MASTER_POSE(SET_V,SET_P,1)
	LOCAL = MASTER_POSE(SET_V,SET_P,1)
ELSEIF MASTER_POSE(SET_A,SET_P,1)
	LOCAL = MASTER_POSE(SET_A,SET_P,1)
ENDIF
IF FLAG:70 == 1
	IF NOWEX:MASTER:射精
		IF LOCAL == TARGET
			PRINTFORML 從時停中被侵犯的%CALLNAME:TARGET%的\@ MASTER_POSE(SET_V,SET_P,1) ? 小穴 # 肛門 \@中拔出%TINKO(PLAYER)%、讓%CALLNAME:TARGET%的手将其握住
		ELSEIF LOCAL
			PRINTFORML 從時停中被侵犯的%CALLNAME:LOCAL%体内拔出%TINKO(PLAYER)%、讓%CALLNAME:TARGET%的手将其握住
		ELSE
			PRINTFORML 让時停中的%CALLNAME:TARGET%的手握住%TINKO(PLAYER)%
		ENDIF
			PRINTFORML 使用%CALLNAME:TARGET%的手、前后撸动着即将射精的%TINKO(PLAYER)%…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%用時停中的%CALLNAME:TARGET%的手握住%TINKO(PLAYER)%摩擦起来…
	ENDIF
ELSEIF CFLAG:MASTER:恶作剧 == 2
	IF NOWEX:MASTER:射精
		IF LOCAL == TARGET
			PRINTFORML 從睡眠中被侵犯的%CALLNAME:TARGET%的\@ MASTER_POSE(SET_V,SET_P,1) ? 小穴 # 肛門 \@中拔出%TINKO(PLAYER)%、讓%CALLNAME:TARGET%的手将其握住
		ELSEIF LOCAL
			PRINTFORML 從睡眠中被侵犯的%CALLNAME:LOCAL%体内拔出%TINKO(PLAYER)%、讓%CALLNAME:TARGET%的手将其握住
		ELSE
			PRINTFORML 讓睡眠中被侵犯的%CALLNAME:TARGET%的手握住%TINKO(PLAYER)%
		ENDIF
			PRINTFORML 前后撸动着即将射精的%TINKO(PLAYER)%…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%用睡眠中的%CALLNAME:TARGET%的手握住%TINKO(PLAYER)%摩擦起来…
	ENDIF
ELSE
	IF NOWEX:MASTER:射精
		IF LOCAL == TARGET
			PRINTFORML 在即将射精之前從\@ MASTER_POSE(SET_V,SET_P,1) ? 小穴 # 肛門 \@内拔出%TINKO(PLAYER)%讓%CALLNAME:TARGET%握住…
		ELSEIF LOCAL
			PRINTFORML 在即将射精之前從%CALLNAME:LOCAL%体内拔出%TINKO(PLAYER)%讓%CALLNAME:TARGET%握住…
		ELSE
			PRINTFORML 让%CALLNAME:TARGET%握住即将射精的%TINKO(PLAYER)%…
		ENDIF
		IF ABL:技巧 > 3
			IF ABL:精液中毒 >= 5
				PRINTFORML %CALLNAME:TARGET%激烈撸动起了%TINKO(PLAYER)%并张開嘴凑到尿道口前等待着精液…
			ELSE
				PRINTFORML %CALLNAME:TARGET%用熟练的手法刺激着%TINKO(PLAYER)%的敏感部位促使%CALLNAME:PLAYER%射精…
			ENDIF
		ELSE
			PRINTFORML %CALLNAME:TARGET%帯著困惑緊盯着尿道口的部位、前後撸动着%TINKO(PLAYER)%…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%
		;恋慕か親愛
		SIF TALENT:恋慕
			PRINT 露出充满愛意的表情、
		;淫乱か娼婦
		SIF TALENT:淫乱
			PRINT 主动
		SIF CFLAG:败犬 && !TALENT:恋慕 && !TALENT:淫乱
			PRINTFORM 转过脸来
		PRINTFORML 用手指纏住%CALLNAME:PLAYER%的%TINKO(PLAYER)%開始摩擦…
	ENDIF
ENDIF
;-------------------------------------------------
;81,口交
;-------------------------------------------------
@MESSAGE_COM81
VARSET LOCAL
;お掃除用
IF TCVAR:MASTER:112 == 1
	LOCAL = MASTER_POSE(SET_V,SET_P,1)
ELSEIF TCVAR:MASTER:112 == 2
	LOCAL = MASTER_POSE(SET_A,SET_P,1)
ENDIF
IF MASTER_POSE(SET_V,SET_P,1)
	LOCAL:1 = MASTER_POSE(SET_V,SET_P,1)
ELSEIF MASTER_POSE(SET_A,SET_P,1)
	LOCAL:1 = MASTER_POSE(SET_A,SET_P,1)
ENDIF
IF FLAG:70 == 1
	;お掃除
	IF LOCAL
		PRINTFORML 拔出在時停中的%CALLNAME:LOCAL%体内盡情射精的%TINKO(PLAYER)%、
		PRINTFORML 把沾着精液的%TINKO(PLAYER)%插入%CALLNAME:TARGET%口中、清洁着%TINKO(PLAYER)%…
	ELSEIF NOWEX:MASTER:射精
		IF LOCAL:1 == TARGET
			PRINTFORML 從時停中被侵犯的%CALLNAME:TARGET%的\@ MASTER_POSE(SET_V,SET_P,1) ? 小穴 # 肛門 \@内拔出%TINKO(PLAYER)%、插進%CALLNAME:TARGET%口中
		ELSEIF LOCAL:1
			PRINTFORML 從時停中被侵犯的%CALLNAME:(LOCAL:1)%体内拔出%TINKO(PLAYER)%、插进%CALLNAME:TARGET%口中
		ELSE
			PRINTFORML 把%TINKO(PLAYER)%插进時停中的%CALLNAME:TARGET%口中
		ENDIF
			PRINTFORML 把即将射精的%TINKO(PLAYER)%插进喉咙深处…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%把%TINKO(PLAYER)%插进時停中的%CALLNAME:TARGET%口中、动起了腰摩擦着舌头…
	ENDIF
	SIF TALENT:無接吻経験
		PRINTFORML 玷污了連初吻経験都沒有的%CALLNAME:TARGET%的可憐的嘴唇、讓%CALLNAME:MASTER%非常的興奮
ELSEIF CFLAG:MASTER:恶作剧 == 2
	;お掃除
	IF LOCAL
		PRINTFORML 拔出在睡眠中被侵犯的%CALLNAME:LOCAL%体内盡情射精的%TINKO(PLAYER)%、
		PRINTFORML 把沾着精液的%TINKO(PLAYER)%插入%CALLNAME:TARGET%口中、清洁着%TINKO(PLAYER)%…
	ELSEIF NOWEX:MASTER:射精
		IF LOCAL:1 == TARGET
			PRINTFORML 從睡眠中被侵犯的%CALLNAME:TARGET%的\@ MASTER_POSE(SET_V,SET_P,1) ? 小穴 # 肛門 \@内拔出%TINKO(PLAYER)%、插進%CALLNAME:TARGET%口中
		ELSEIF  LOCAL:1
			PRINTFORML 從睡眠中被侵犯的%CALLNAME:(LOCAL:1)%体内拔出%TINKO(PLAYER)%、插進%CALLNAME:TARGET%口中
		ELSE
			PRINTFORML 把%TINKO(PLAYER)%插入睡眠中被侵犯的%CALLNAME:TARGET%口中
		ENDIF
			PRINTFORML 把即将射精的%TINKO(PLAYER)%插进喉咙深处…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%把%TINKO(PLAYER)%插入睡眠中被侵犯的%CALLNAME:TARGET%口中、动起了腰摩擦着舌头…
	ENDIF
ELSE
	;お掃除
	IF LOCAL
		PRINTFORML 拔出在%CALLNAME:LOCAL%体内盡情射精的%TINKO(PLAYER)%、
		PRINTFORML 讓%CALLNAME:TARGET%舔舐着沾着精液的%TINKO(PLAYER)%…
		SIF TFLAG:8 && !TCVAR:TARGET:烂醉
			PRINTFORML %CALLNAME:TARGET%把残留在尿道里的精液都清理干净后才\@ ABL:精液中毒 > 4 ? 恋恋不舍的 # \@从%TINKO(PLAYER)%上移開了嘴唇…
	ELSEIF NOWEX:MASTER:射精
		IF LOCAL:1 == TARGET
			PRINTFORML 在即将射精之前從\@ MASTER_POSE(SET_V,SET_P,1) ? 小穴 # 肛門 \@中拔出%TINKO(PLAYER)%讓%CALLNAME:TARGET%含住…
		ELSEIF LOCAL:1
			PRINTFORML 在即将射精之前從%CALLNAME:(LOCAL:1)%体内拔出%TINKO(PLAYER)%讓%CALLNAME:TARGET%含住…
		ELSE
			PRINTFORML 讓%CALLNAME:TARGET%含住即将射精的%TINKO(PLAYER)%…
		ENDIF
		IF ABL:舌 > 2 && !TCVAR:TARGET:烂醉
			IF ABL:精液中毒 > 4
				PRINTFORML %CALLNAME:TARGET%用嘴唇包住亀頭并把舌头缠上去拨动起来以促使%CALLNAME:PLAYER%射精…
			ELSE
				PRINTFORML %CALLNAME:TARGET%用舌头舔着%TINKO(PLAYER)%的敏感部位以促使%CALLNAME:PLAYER%射精…
			ENDIF
		ELSE
			PRINTFORML %CALLNAME:TARGET%在观察%CALLNAME:PLAYER%反応的同时努力地动着舌头…
		ENDIF
	ELSEIF TCVAR:TARGET:烂醉
		PRINTFORML 让迷迷糊糊的%CALLNAME:TARGET%用嘴含住%TINKO(PLAYER)%
		PRINTFORML 虽然一做就被发觉了但没有被咬下去的様子……
	ELSE
		PRINTFORM %CALLNAME:TARGET%%CALLNAME:PLAYER%の%TINKO(PLAYER)%に
		;恋慕
		SIF TALENT:恋慕
			PRINT 露出慈爱的表情、
		;淫乱
		SIF TALENT:淫乱
			PRINT 主動
		SIF CFLAG:败犬 && !TALENT:恋慕 && !TALENT:淫乱
			PRINTFORM 流着泪
		PRINTFORML 含着用舌头舔起来…
	ENDIF
ENDIF
;-------------------------------------------------
;82,乳交
;-------------------------------------------------
@MESSAGE_COM82
IF SELECTCOM == PREVCOM
	IF FLAG:70 == 1 || CFLAG:MASTER:恶作剧 == 2
		PRINTFORM %CALLNAME:PLAYER%对\@ FLAG:70?時間被停止的 #睡眠中的 \@%CALLNAME:TARGET%の%OPPAI_DESCRIPTION(TARGET,1)%
		IF RAND:2
			IF RAND:3 == 0
				PRINTFORM 的乳輪用大拇指挠着并
			ELSEIF RAND:2
				PRINTFORM 的%NIPPLE_DESCRIPTION(TARGET,1)%用手指捊动着并
			ELSE
				PRINTFORM \@ TALENT:胸圍 == -2 ? 用整个手掌来回抚摸着并# 揉捏起来并\@
			ENDIF
		ELSE
			PRINTFORM %TEXTR("を内側に寄せながら/を密着させながら/紧缠住并/按住并")%
		ENDIF
		SIF EXP:PLAYER:Ｖ性交経験 + EXP:PLAYER:Ａ性交経験 && RAND:2
			PRINTFORM %TEXTR("性交でもするかのように/乳を孕ませようと/まるで交尾をしているかの如く")%
		SIF RAND:2
			PRINTFORM 凭仅剩的体力
		IF ABL:PLAYER:施虐属性 && RAND:3 == 0
			PRINTFORM 剧烈的动起了腰
		ELSE
			PRINTFORM %TEXTR("激烈的动起了腰/前後动起了腰")%
		ENDIF
		IF RAND:2 == 0
			PRINTFORML 持续侵犯起来…
		ELSE
			PRINTFORML 持续侵犯着…
		ENDIF
	ELSE
		;超乳
		IF TALENT:胸圍 == 3 && RAND:3
			PRINTFORML 超出常规的%OPPAI_DESCRIPTION(TARGET,1)%吞下了%CALLNAME:PLAYER%的手指、一没托住就会因自重肉乎乎的垂下来…
			PRINTFORML 就这様%CALLNAME:TARGET%不断地托起%OPPAI_DESCRIPTION(TARGET)%、利用这个動作持续撸起了%CALLNAME:PLAYER%的%TINKO(PLAYER)%…
		;貧乳
		ELSEIF TALENT:胸圍 == -1
			PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%努力的持续摩擦着%TINKO(PLAYER)%…
		;絶壁
		ELSEIF TALENT:胸圍 == -2
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%と%NIPPLE_DESCRIPTION(TARGET,1)%で必死に%TINKO(PLAYER)%を擦り続けている…
		;巨乳
		ELSEIF TALENT:胸圍 == 1
			PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%把%TINKO(PLAYER)%包进去持续摩擦着…
		;爆乳、超乳
		ELSEIF TALENT:胸圍 >= 2
			PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%把%TINKO(PLAYER)%包裹起来持续摩擦着…
		ELSE
			PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%夹住%TINKO(PLAYER)%持续摩擦着…
		ENDIF
	ENDIF
	RETURN 0
ENDIF

IF FLAG:70 == 1
	PRINTFORM %CALLNAME:PLAYER%用時停中的%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
	;貧乳
	IF TALENT:胸圍 == -1
		PRINTFORM 把%TINKO(PLAYER)%轻轻的摩擦
	;絶壁
	ELSEIF TALENT:胸圍 == -2
		PRINTFORM で、そそり立つ%NIPPLE_DESCRIPTION(TARGET,1)%に%TINKO(PLAYER)%を擦りつけ
	;普乳以上
	ELSE
		PRINTFORM 把%TINKO(PLAYER)%夹在里面摩擦
	ENDIF
	PRINTL …
ELSEIF CFLAG:MASTER:恶作剧 == 2
	PRINTFORM %CALLNAME:PLAYER%用睡眠中的%CALLNAME:TARGET%的
	;貧乳
	IF TALENT:胸圍 == -1
		PRINTFORM %TINKO(PLAYER)%を擦りつけ
	;絶壁
	ELSEIF TALENT:胸圍 == -2
		PRINTFORM そそり立つ%NIPPLE_DESCRIPTION(TARGET,1)%に、%TINKO(PLAYER)%を擦りつけ
	;巨乳
	ELSEIF TALENT:胸圍 == 1
		PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%把%TINKO(PLAYER)%夹在里面摩擦
	;爆乳
	ELSEIF TALENT:胸圍 == 2
		PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%把%TINKO(PLAYER)%夹在里面摩擦
	ELSE
		PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%把%TINKO(PLAYER)%夹在里面摩擦
	ENDIF
	PRINTL …
ELSE
	PRINTFORM %CALLNAME:TARGET%
	;恋慕か親愛（愛欲の有無で稍微分岐）
	SIF TALENT:愛欲
		PRINTFORM 帯着\@ TALENT:恋慕 ? 陶醉的 # 心蕩神馳 \@的表情、
		SIF CFLAG:败犬 && !TALENT:恋慕 && !TALENT:淫乱
			PRINTFORM 表情透露出打心底里的讨厌
	IF ABL:TARGET:胸 > 2
	;胸3以上
		;胸で分岐させて尺寸はSELECTCASEで分けてみる。大きさは違ってもその先に突起はあるんだよ
		SELECTCASE TALENT:胸圍
		;きょにゅー以上
		CASE IS > 0
			IF RAND:2 == 0
				PRINTFORML 把丰满的双峰凑上来、用%OPPAI_DESCRIPTION(TARGET,1)%间的峡谷夹住%TINKO(PLAYER)%、撸动起来。
				PRINTL 坚硬的肉棒被湿润地包裹住、陷入在柔软的部位里
				PRINTL 那令人沉陷的舒适感觉、就好像是在肉海中漂荡一般…
			ELSE
				PRINTFORML 把那个%OPPAI_DESCRIPTION(TARGET,1)%压上来、充分地发挥%OPPAI_DESCRIPTION(TARGET)%的重圧、
				PRINTFORML 引导着%CALLNAME:PLAYER%的%TINKO(PLAYER)%。
				PRINTFORML 在%CALLNAME:TARGET%的柔肉圧倒般的质量及接触的舒适感下、%CALLNAME:PLAYER%沉陷进去了…
			ENDIF
			PRINTL 滑溜溜的双峰不断蹭动收容了硬邦邦的陰茎、有着令人沉迷程度的柔软…
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 其中桃色的%OPPAI_DESCRIPTION(TARGET,1)%的前端、%NIPPLE_DESCRIPTION(TARGET,1)%时不时、咯哩咯哩地摩擦着肉棒和亀頭…
		;ひんぬー以下
		CASE IS < 0
			PRINTFORML 用経験和技术弥补物理上的缺陷使用%OPPAI_DESCRIPTION(TARGET,1)%奉仕起%TINKO(PLAYER)%。
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 用充血的胸板前端的%NIPPLE_DESCRIPTION(TARGET,1)%仔細的摩擦奉仕起肉棒和亀頭。
		;ふにゅぅ
		CASEELSE
			PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%挟住%TINKO(PLAYER)%、撸了起来。
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 用时不时冒汗的%OPPAI_DESCRIPTION(TARGET,1)%的顶端、%NIPPLE_DESCRIPTION(TARGET,1)%来摩擦着肉棒和龟头并发出娇喘声。
		ENDSELECT
	ELSEIF ABL:TARGET:胸 <= 2
	;胸2以下
		;貧乳
		IF TALENT:胸圍 == -1
			PRINTFORM 用%OPPAI_DESCRIPTION(TARGET,1)%努力的摩擦起%TINKO(PLAYER)%
		;絶壁
		ELSEIF TALENT:胸圍 == -2
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%と%NIPPLE_DESCRIPTION(TARGET,1)%で必死に%TINKO(PLAYER)%を
		;巨乳
		ELSEIF TALENT:胸圍 == 1
			PRINTFORM 用%OPPAI_DESCRIPTION(TARGET,1)%包住%TINKO(PLAYER)%并摩擦起来
		;爆乳、超乳
		ELSEIF TALENT:胸圍 >= 2
			PRINTFORM 用%OPPAI_DESCRIPTION(TARGET,1)%完全包住%TINKO(PLAYER)%并摩擦起来
		ELSE
			PRINTFORM 用%OPPAI_DESCRIPTION(TARGET,1)%夹住%TINKO(PLAYER)%并摩擦起来
		ENDIF
		PRINTL …
	ELSE

	ENDIF
	
	;恋慕又は愛欲
	IF IS_LOVER(TARGET) && EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 30000 && RAND:3 == 0
		PRINTFORML 是因为心情很好吗、%CALLNAME:TARGET%激烈的摇晃着身体、
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET)%全力地撸起了%CALLNAME:PLAYER%的%TINKO(PLAYER)%、仿佛都要冒出热气了
		PRINTFORML 我的乳交感觉如何？　舒服吗？
		PRINTFORML %SAMEN_DESCRIPTION(PLAYER)%还没出来吗？　有感觉了吗？　不射吗？
		PRINTFORML 提出了诸如此类的流露出内心喜悦之情的香艳的问题…
	ENDIF
	SELECTCASE ABL:TARGET:胸
	CASE IS < 2
		PRINTFORM 虽然这么讲对%CALLNAME:TARGET%不太好、半吊子的奉仕
		SIF TALENT:胸圍 >= 1
			PRINT 視覚上姑且不说
		PRINTL 实在不是很舒服…
	CASE IS  > 2
		PRINTFORML %CALLNAME:TARGET%熟练地奉仕并取悦%CALLNAME:PLAYER%…
	CASEELSE
		PRINTFORML %CALLNAME:TARGET%的奉仕令%CALLNAME:PLAYER%感到很舒服…
	ENDSELECT
ENDIF
;-------------------------------------------------
;83,素股
;-------------------------------------------------
@MESSAGE_COM83
IF FLAG:70 == 1
	PRINTFORML %CALLNAME:PLAYER%把%TINKO(PLAYER)%夹在時間停止的%CALLNAME:TARGET%的大腿之间、用
	;欲情のPALAMLV4以上
	SIF PALAM:潤滑 >= PALAMLV:3
		PRINT 湿的一塌糊涂的
	;矮小体型
	IF TALENT:体型 < 0
		PRINT 幼小裂缝
	ELSE
		PRINT 陰唇
	ENDIF
	PRINTFORML 摩擦起来…
ELSE
	PRINTFORM %CALLNAME:TARGET%用大腿夹住%CALLNAME:PLAYER%的%TINKO(PLAYER)%、
	;淫乱か娼婦＋欲情のPALAMLV5以上
	IF TALENT:淫乱 && PALAM:欲情 >= PALAMLV:5
		PRINTFORM 露出沉溺于快楽的表情看着%CALLNAME:PLAYER%同时
	;恋慕か親愛＋欲情のPALAMLV5以上
	ELSEIF TALENT:恋慕 && PALAM:欲情 >= PALAMLV:5
		PRINT 露出陶醉的表情同时
	;欲情のPALAMLV4以上
	ELSEIF PALAM:欲情 >= PALAMLV:4
		PRINT 嘴角流出口水同时
	;恋慕か親愛
	ELSEIF TALENT:恋慕
		PRINTFORM 用陶酔的眼神看向%CALLNAME:PLAYER%同时
	;淫乱か娼婦
	ELSEIF TALENT:淫乱
		PRINTFORM 用淫猥的视线发出挑衅同时
	ELSEIF CFLAG:败犬
		PRINTFORM 表情透露出打心底里的讨厌
	;不知羞恥
	ELSEIF TALENT:羞恥心 < 0
		PRINTFORM 大胆的动起腰身同时
	;感情缺乏
	ELSEIF TALENT:感情缺乏
		PRINTFORM 脸颊微微泛红同时
	ELSE
		PRINT 有点害臊的
	ENDIF
	;矮小体型
	IF TALENT:体型 < 0
		PRINT 用幼小裂缝
	ELSE
		PRINT 用陰唇
	ENDIF
	PRINTFORML 摩擦起了%TINKO(PLAYER)%…
ENDIF
;-------------------------------------------------
;84,泡泡浴
;-------------------------------------------------
@MESSAGE_COM84
PRINTFORM %CALLNAME:TARGET%用肥皂弄出泡泡涂在自己身上、
;恋慕か親愛
SIF TALENT:恋慕
	PRINT 带着陶醉的表情同时
;矮小体型
IF TALENT:体型 < 0
	PRINTFORM 用小小的身体尽可能地擦洗并愛撫着%CALLNAME:PLAYER%
;巨乳か爆乳＋矮小体型でない
ELSEIF TALENT:胸圍 > 0
	PRINTFORM 用%OPPAI_DESCRIPTION(TARGET)%和馥郁的肌肤擦洗并愛撫着%CALLNAME:PLAYER%
ELSE
	PRINTFORM 擦洗并愛撫着%CALLNAME:PLAYER%全身
ENDIF
PRINTL …
;-------------------------------------------------
;85,足交
;-------------------------------------------------
@MESSAGE_COM85
IF FLAG:70 == 1
	PRINTFORML %CALLNAME:PLAYER%用時停中的%CALLNAME:TARGET%的脚底摩擦起了%TINKO(PLAYER)%…
ELSEIF CFLAG:MASTER:恶作剧 == 2
	PRINTFORML %CALLNAME:PLAYER%用睡眠中的%CALLNAME:TARGET%的脚底摩擦起了%TINKO(PLAYER)%…
ELSE
	PRINTFORM %CALLNAME:TARGET%用腳底
	;施虐狂
	SIF TALENT:施虐狂
		PRINT 一邊蹂躪一邊
	;恋慕か親愛
	SIF TALENT:恋慕
		PRINT 細心
	PRINTFORML 摩擦起%CALLNAME:PLAYER%の%TINKO(PLAYER)%…
ENDIF
;-------------------------------------------------
;86,足舐め強制
;-------------------------------------------------
@MESSAGE_COM86
;野外PLAY中
IF TEQUIP:野外PLAY
	;昼
	IF TIME == 0
		PRINT 光天化日之下、
	;夜
	ELSE
		PRINT 在月光下、
	ENDIF
	;膽怯、自尊心高、害羞のいずれか
	IF TALENT:胆量 < 0 || TALENT:自尊心 > 0 || TALENT:羞恥心 > 0
		PRINTL 因为这幅痴態可能会被誰看到而颤抖的
	ELSE
		PRINTL 赤裸的肌肤被風抚摸而微微颤抖的
	ENDIF
;媚薬有効
ELSEIF TCVAR:媚薬
	PRINT 眼神迷离飘忽的
ENDIF
PRINTFORML %CALLNAME:TARGET%用舌头舔起了%CALLNAME:PLAYER%伸出的脚…
;-------------------------------------------------
;88,真空口交
;-------------------------------------------------
@MESSAGE_COM88
	PRINTFORML %CALLNAME:TARGET%将%CALLNAME:PLAYER%的%TINKO(PLAYER)%连根都含了进去、
	PRINTL 以這種激烈的勢頭吸著陰茎
;-------------------------------------------------
;89,喝套内精液
;-------------------------------------------------
@MESSAGE_COM89
IF FLAG:70

	PRINTFORML 将使用过的避孕套翻转过来対著%CALLNAME:TARGET%的口中、款待她喝下\@ TFLAG:194 > 150 ? 大量的 # \@%SAMEN_DESCRIPTION(PLAYER)%…
	PRINTFORML 為了讓其好好記住%CALLNAME:PLAYER%的味道、%CALLNAME:PLAYER%将翻转过来的避孕套在她口腔里蹭来蹭去、一滴不剩地讓她品嘗了%SAMEN_DESCRIPTION(PLAYER)%…
ELSEIF CFLAG:MASTER:恶作剧 == 2
	PRINTFORML 将使用过的避孕套翻转过来対著%CALLNAME:TARGET%的口中、款待她喝下\@ TFLAG:194 > 150 ? 大量的 # \@%SAMEN_DESCRIPTION(PLAYER)%…
	PRINTFORML 為了讓其好好記住%CALLNAME:PLAYER%的味道、%CALLNAME:PLAYER%将翻转过来的避孕套在她口腔里蹭来蹭去、一滴不剩地讓她品嘗了%SAMEN_DESCRIPTION(PLAYER)%…
ELSE
	PRINTFORM 往%CALLNAME:TARGET%的脸上、将
	SELECTCASE TFLAG:193
		CASE IS > 300
			PRINT 装着满满的
		CASE IS > 150
			PRINT 前端装满了
		CASE IS > 0
			PRINT 装着
	ENDSELECT
		PRINTFORML 精液的安全套摁了過去……
	;精液中毒5未満
	IF ABL:精液中毒 < 5
		PRINTFORML 将精液全部倒在怯生生的张開嘴的%CALLNAME:TARGET%的舌头上、让%CALLNAME:TARGET%就这様咽下去……
	ELSE
		PRINTFORML %CALLNAME:TARGET%露出恍惚的表情、津津有味的品尝起了流进嘴里的散发着橡胶臭味的精液……
		SIF ABL:精液中毒 >= 7
			PRINTFORML 把精液喝干后、%CALLNAME:TARGET%还依依不舍地舔起了翻转过来的避孕套……
	ENDIF
ENDIF
;-------------------------------------------------
;90,被愛撫肛門
;-------------------------------------------------
@MESSAGE_COM90
IF TEQUIP:PLAYER:0 > 1
	PRINTFORM %CALLNAME:TARGET%隔着衣服对%CALLNAME:PLAYER%的肛門
ELSE
	PRINTFORM %CALLNAME:TARGET%对%CALLNAME:PLAYER%的肛門
ENDIF

IF ABL:PLAYER:Ａ感覚 <= 1
	PRINT 用指腹摩挲
ELSEIF ABL:PLAYER:Ａ感覚 <= 3
	PRINT 為了讓其放鬆而
ELSE
	PRINT 為了把手指插入而
ENDIF

PRINTFORML 愛撫起来…

IF ABL:PLAYER:Ａ感覚 <= 1
	PRINTFORML %CALLNAME:PLAYER%对这种触感还很不适应…
ELSEIF ABL:PLAYER:Ａ感覚 <= 3
	PRINTFORML %CALLNAME:PLAYER%因为異物感而发抖起来…
ELSE
	PRINTFORML %CALLNAME:PLAYER%露出恍惚的表情就这様任其摆布…
ENDIF

;-------------------------------------------------
;91,被舐肛門
;-------------------------------------------------
@MESSAGE_COM91
PRINTFORML %CALLNAME:TARGET%把嘴凑到了%CALLNAME:PLAYER%的肛門上
IF ABL:奉仕精神 >= 3
	PRINTFORML 舔舐着張開的褶皺、肛腔之中被侵犯的感覺讓%CALLNAME:PLAYER%不由得顫抖起来…
ELSE
	PRINTFORML 敏感的粘膜被舔舐着、讓%CALLNAME:PLAYER%身体颤抖了起来…
ENDIF
;-------------------------------------------------
;96,给对方乳交
;-------------------------------------------------
@MESSAGE_COM96
IF FLAG:70 == 1
	PRINTFORM %CALLNAME:PLAYER%将時間停止中的%CALLNAME:TARGET%的%TINKO(TARGET)%、
ELSEIF CFLAG:MASTER:恶作剧 == 2
	PRINTFORM %CALLNAME:PLAYER%将睡眠中的%CALLNAME:TARGET%的%TINKO(TARGET)%、
ELSE
	PRINTFORM %CALLNAME:PLAYER%将%CALLNAME:TARGET%的%TINKO(TARGET)%、
ENDIF
PRINTFORML 用自己的%OPPAI_DESCRIPTION(PLAYER,1)%\@ TALENT:PLAYER:胸圍 <= -1? 拼命地摩擦起来 # 夹住后摩擦起来\@…
;-------------------------------------------------
;120,強制舐陰
;-------------------------------------------------
@MESSAGE_COM120
PRINTFORML 讓%CALLNAME:TARGET%舔舐着%CALLNAME:PLAYER%的陰唇…
;-------------------------------------------------
;121,貝あわせ
;-------------------------------------------------
@MESSAGE_COM121
IF FLAG:70 == 1
	PRINTFORM 用性器摩擦起時停中的%CALLNAME:TARGET%的性器…
ELSEIF CFLAG:MASTER:恶作剧 == 2
	PRINTFORM 用性器摩擦起睡眠中的%CALLNAME:TARGET%的性器…
ELSE
	PRINTFORML %CALLNAME:PLAYER%和%CALLNAME:TARGET%用性器互相摩擦起来…
ENDIF
;-------------------------------------------------
;122,讓助手侵犯
;-------------------------------------------------
@MESSAGE_COM122
PRINTFORML 讓%CALLNAME:TARGET%抱了%CALLNAME:PLAYER%…
;-------------------------------------------------
;123,给对方足交
;-------------------------------------------------
@MESSAGE_COM123
PRINTFORM %CALLNAME:PLAYER%
;調教者が施虐狂
SIF TALENT:PLAYER:施虐狂
	PRINT 像踩踏一般
PRINTL 用脚撸动着
SIF FLAG:70 == 1
	PRINTFORM 时停中的
SIF CFLAG:MASTER:恶作剧 == 2
	PRINTFORM 睡眠中的
PRINTFORML %CALLNAME:TARGET%的%TINKO(TARGET)%…

;-------------------------------------------------
;124,给对方手淫
;-------------------------------------------------
@MESSAGE_COM124
PRINTFORM %CALLNAME:PLAYER%は
SIF TALENT:恋人
	PRINT 露出充满愛意的表情、
SIF FLAG:70 == 1
	PRINT 时停中的
SIF CFLAG:MASTER:恶作剧 == 2
	PRINT 睡眠中的
PRINTFORM 对%CALLNAME:TARGET%的%TINKO(TARGET)%
IF TEQUIP:33 || TEQUIP:34
	PRINT 用围裙包住撸了起来…
ELSEIF TEQUIP:0 > 1
	PRINT 隔着内裤摩擦了起来…
ELSE
	PRINT 伸手握住手指合拢撸了起来…
ENDIF
PRINTL 動作著…
;-------------------------------------------------
;125,被愛撫
;-------------------------------------------------
@MESSAGE_COM125
PRINTFORM %CALLNAME:PLAYER%拉住
SIF FLAG:70 == 1
	PRINTFORM 時停中的
PRINTFORML %CALLNAME:TARGET%的手并帯向了自己的身体…
;-------------------------------------------------
;126,互揉胸部
;-------------------------------------------------
@MESSAGE_COM126
PRINTFORML %CALLNAME:PLAYER%和%CALLNAME:TARGET%互相揉起対方的胸部…
;-------------------------------------------------
;140,深喉插入
;-------------------------------------------------
@MESSAGE_COM140
IF CFLAG:MASTER:恶作剧 == 2
	PRINTFORML %CALLNAME:PLAYER%為了不驚醒%CALLNAME:TARGET%而小心翼翼的把%TINKO(PLAYER)%插进了%CALLNAME:TARGET%的喉咙深処…
ELSE
	PRINTFORML %CALLNAME:PLAYER%粗暴地插进了%CALLNAME:TARGET%的喉咙…
ENDIF
;-------------------------------------------------
;145,肛門奉仕強制
;-------------------------------------------------
@MESSAGE_COM145
PRINTFORML %CALLNAME:PLAYER%、讓%CALLNAME:TARGET%舔起了肛門…

;-------------------------------------------------
;150,雙乳間抽插
;-------------------------------------------------
@MESSAGE_COM150
IF FLAG:70 == 1
	LOCALS:0 = 時停中的
ELSE
	LOCALS:0 = 
ENDIF
IF CFLAG:MASTER:恶作剧 == 2
	LOCALS:1 = 睡眠中的
ELSE
	LOCALS:1 = 
ENDIF
IF SELECTCOM == PREVCOM
	PRINTFORM %CALLNAME:PLAYER%
	IF RAND:2
		IF RAND:3 == 0
			PRINTFORM 一邊用大拇指轉弄着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%的乳輪
		ELSEIF RAND:2
			PRINTFORM 一邊用手指捋動着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%的%NIPPLE_DESCRIPTION(TARGET,1)%
		ELSE
			PRINTFORM 一邊揉捏着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
		ENDIF
	ELSE
		IF RAND:4 == 0
			PRINTFORM 一邊擠壓着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
		ELSEIF RAND:3 == 0
			PRINTFORM 一邊貼緊%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
		ELSEIF RAND:2
			PRINTFORM 一邊緊抓着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
		ELSE
			PRINTFORM 一邊圧迫着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
		ENDIF
	ENDIF

	IF EXP:PLAYER:Ｖ性交経験 + EXP:PLAYER:Ａ性交経験 && RAND:2
		IF RAND:3 == 0
			PRINT 一邊如同在性交一樣
		ELSEIF RAND:2
			PRINTFORM 一邊像要讓乳房懷孕一樣
		ELSE
			PRINT 一邊簡直像是在交尾一樣
		ENDIF
	ENDIF
	SIF RAND:2
		PRINTFORM 靠僅剩的体力
	IF ABL:PLAYER:施虐属性 && RAND:3 == 0
		PRINTFORML 粗暴地動着腰
	ELSEIF RAND:2
		PRINTFORML 激烈地動着腰
	ELSE
		PRINTFORML 前後動起腰
	ENDIF
	
	IF RAND:2 == 0
		PRINTFORM 用%TINKO(PLAYER)%持續侵犯起
	ELSE
		PRINTFORM 用%TINKO(PLAYER)%持續侵犯着
	ENDIF
	
	IF RAND:3
		PRINTFORML %OPPAI_DESCRIPTION(TARGET)%
	ELSE
		PRINTFORML %OPPAI_DESCRIPTION(TARGET)%的谷間
	ENDIF
	
	RETURN 0
ENDIF

PRINTFORM %CALLNAME:PLAYER%用
IF FLAG:70 == 1
	LOCALS:0 = 時停中的
ELSE
	LOCALS:0 = 
ENDIF
IF CFLAG:MASTER:恶作剧 == 2
	LOCALS:1 = 睡眠中的
ELSE
	LOCALS:1 = 
ENDIF
PRINTFORM %LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的
;貧乳
IF TALENT:胸圍 == -1
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%摩擦着%TINKO(PLAYER)%
;巨乳
ELSEIF TALENT:胸圍 == 1
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%夹住%TINKO(PLAYER)%開始摩擦
;爆乳
ELSEIF TALENT:胸圍 == 2
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%夹住%TINKO(PLAYER)%開始摩擦
;超乳
ELSEIF TALENT:胸圍 == 3
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%包住%TINKO(PLAYER)%開始摩擦
ELSE
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%夹住%TINKO(PLAYER)%開始摩擦
ENDIF
PRINTL …

PRINTFORM %CALLNAME:PLAYER%
IF RAND:2
	IF RAND:3 == 0
		PRINTFORM 一邊用大拇指轉弄着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%的乳輪
	ELSEIF RAND:2
		PRINTFORM 一邊用手指捋動着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%的%NIPPLE_DESCRIPTION(TARGET,1)%
	ELSE
		PRINTFORM 一邊揉捏着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
	ENDIF
ELSE
	IF RAND:4 == 0
		PRINTFORM 一邊擠壓着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
	ELSEIF RAND:3 == 0
		PRINTFORM 一邊貼緊%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
	ELSEIF RAND:2
		PRINTFORM 一邊緊抓着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
	ELSE
		PRINTFORM 一邊圧迫着%LOCALS:0%%LOCALS:1%%CALLNAME:TARGET%的%OPPAI_DESCRIPTION(TARGET,1)%
	ENDIF
ENDIF

IF EXP:PLAYER:Ｖ性交経験 + EXP:PLAYER:Ａ性交経験 && RAND:2
	IF RAND:3 == 0
		PRINT 一邊如同在性交一樣
	ELSEIF RAND:2
		PRINTFORM 一邊像要讓乳房懷孕一樣
	ELSE
		PRINT 一邊簡直像是在交尾一樣
	ENDIF
ENDIF

IF ABL:PLAYER:施虐属性 && RAND:3 == 0
	PRINTFORMW 粗暴地動起了腰…
ELSEIF RAND:2
	PRINTFORMW 激烈地動起了腰…
ELSE
	PRINTFORMW 前後動起了腰…
ENDIF

IF !FLAG:70 && CFLAG:MASTER:恶作剧 != 2
	;恋慕か親愛（愛欲の有無で稍微分岐）
	IF TALENT:愛欲
		PRINTFORML 正被侵犯着乳沟的%CALLNAME:TARGET%露出了\@ TALENT:恋慕 ? 陶醉的 # 心蕩神馳 \@的表情…
	ELSE
		PRINTFORML %CALLNAME:TARGET%正被侵犯着乳沟…
	ENDIF
	IF ABL:PLAYER:技巧 > 2
	;プレイヤーの技巧3以上
		;胸で分岐させて尺寸はSELECTCASEで分けてみる。大きさは違ってもその先に突起はあるんだよ
		SELECTCASE TALENT:胸圍
		;きょにゅー以上
		CASE IS > 0
			IF RAND:2 == 0
				PRINTFORML 把丰满的双峰凑上来、用%OPPAI_DESCRIPTION(TARGET,1)%间的峡谷夹住%TINKO(PLAYER)%、撸动起来。
				PRINTL 坚硬的肉棒被湿润地包裹住、陷入在柔软的部位里
				PRINTL 那令人沉陷的舒适感觉、就好像是在肉海中漂荡一般…
			ELSE
				PRINTFORML 把那个%OPPAI_DESCRIPTION(TARGET,1)%压在%TINKO(PLAYER)%上、充分地发挥%OPPAI_DESCRIPTION(TARGET)%的重圧、
				PRINTFORML 撞向%CALLNAME:PLAYER%的%TINKO(PLAYER)%。
				PRINTFORML 在%CALLNAME:TARGET%的柔肉圧倒般的质量及接触的舒适感下、%CALLNAME:PLAYER%沉陷进去了…
			ENDIF
			PRINTL 滑溜溜的双峰不断蹭动收容了硬邦邦的陰茎、有着令人沉迷程度的柔软…
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 其中桃色的%OPPAI_DESCRIPTION(TARGET,1)%的前端、%NIPPLE_DESCRIPTION(TARGET,1)%时不时、咯哩咯哩地摩擦着肉棒和亀頭…
		;ひんぬー以下
		CASE IS < 0
			PRINTFORML 用経験和技术弥补物理上的缺陷使用%OPPAI_DESCRIPTION(TARGET,1)%奉仕起%TINKO(PLAYER)%。
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 用充血的胸板前端的%NIPPLE_DESCRIPTION(TARGET,1)%仔細的摩擦奉仕起肉棒和亀頭。
		;ふにゅぅ
		CASEELSE
			PRINTFORML 用%OPPAI_DESCRIPTION(TARGET,1)%挟住%TINKO(PLAYER)%、撸了起来。
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 用时不时冒汗的%OPPAI_DESCRIPTION(TARGET,1)%的顶端、%NIPPLE_DESCRIPTION(TARGET,1)%来摩擦着肉棒和龟头并发出娇喘声。
		ENDSELECT
	ELSEIF ABL:PLAYER:技巧 <= 2
	;胸2以下
		;貧乳
		IF TALENT:胸圍 == -1
			PRINTFORM 用%OPPAI_DESCRIPTION(TARGET,1)%努力的摩擦起%TINKO(PLAYER)%
		;巨乳
		ELSEIF TALENT:胸圍 == 1
			PRINTFORM 用%OPPAI_DESCRIPTION(TARGET,1)%包住%TINKO(PLAYER)%并摩擦起来
		;爆乳、超乳
		ELSEIF TALENT:胸圍 >= 2
			PRINTFORM 用%OPPAI_DESCRIPTION(TARGET,1)%完全包住%TINKO(PLAYER)%并摩擦起来
		ELSE
			PRINTFORM 用%OPPAI_DESCRIPTION(TARGET,1)%夹住%TINKO(PLAYER)%并摩擦起来
		ENDIF
		PRINTL …
	ELSE

	ENDIF
	;恋慕又は愛欲
	IF IS_LOVER(TARGET) && EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 30000 && RAND:3 == 0
		PRINTFORML 是因为心情很好吗、%CALLNAME:TARGET%激烈的摇晃着身体、
		PRINTFORML 用%OPPAI_DESCRIPTION(TARGET)%全力地撸起了%CALLNAME:PLAYER%的%TINKO(PLAYER)%、仿佛都要冒出热气了
		PRINTFORML 我的乳交感觉如何？　舒服吗？
		PRINTFORML %SAMEN_DESCRIPTION(PLAYER)%还没出来吗？　有感觉了吗？　不射吗？
		PRINTFORML 提出了诸如此类的流露出内心喜悦之情的香艳的问题…
	ENDIF
ENDIF

;-------------------------------------------------
;180,潤滑液,
;-------------------------------------------------
@MESSAGE_COM180
GETBIT STAIN:1, 7
IF RESULT
	PRINTFORML 把巧克力涂在了%CALLNAME:TARGET%身上…
ELSEIF TALENT:TARGET:体型 == -5
	PRINTFORML 把%CALLNAME:TARGET%泡在了润滑液里
	PRINTFORML 对%CALLNAME:TARGET%来说即使是一次的用量都能令她溺水了
ELSE
	PRINTFORML 把润滑液涂在了%CALLNAME:TARGET%身上…
ENDIF
;-------------------------------------------------
;181,媚薬,
;-------------------------------------------------
@MESSAGE_COM181
IF CFLAG:MASTER:恶作剧 == 2
	SIF ABL:PLAYER:技巧 >= 3
		PRINT 嘴対嘴
	PRINT 慢慢地
	PRINTFORML 讓睡眠中的%CALLNAME:TARGET%喝下了媚薬
	PRINTFORML 沉睡着的%CALLNAME:TARGET%肌膚泛起一片紅潮夾緊大腿不時摩擦…
ELSE
;調教者の技巧LV3以上
SIF ABL:PLAYER:技巧 >= 3
	PRINT 嘴対嘴
;従順LV2以下
SIF ABL:従順 <= 2
	PRINT 強行
	PRINTFORML 讓%CALLNAME:TARGET%喝下了媚薬
	PRINTFORML 大口喘息的%CALLNAME:TARGET%的肌膚泛起一片紅潮…
ENDIF
;-------------------------------------------------
;182,利尿剤,
;-------------------------------------------------
@MESSAGE_COM182
IF CFLAG:MASTER:恶作剧 == 2
	SIF ABL:PLAYER:技巧 >= 3
		PRINT 嘴対嘴
	PRINT 慢慢地
	PRINTFORML 讓睡眠中的%CALLNAME:TARGET%喝下了利尿剤
	PRINTFORML 感覺到尿意的%CALLNAME:TARGET%在睡眠中夾緊大腿不時摩擦…
ELSE
;調教者の技巧LV3以上
SIF ABL:PLAYER:技巧 >= 3
	PRINT 嘴対嘴
;従順LV2以下
SIF ABL:従順 <= 2
	PRINT 強行
PRINTFORML 讓%CALLNAME:TARGET%喝下了利尿剤
PRINTFORML %CALLNAME:TARGET%大口喘息、含著淚忍耐着尿意…
ENDIF
;-------------------------------------------------
;183,攝像機,
;-------------------------------------------------
@MESSAGE_COM183
;解除時
IF !TEQUIP:视频拍摄
	PRINTFORML 中止了对%CALLNAME:TARGET%的摄影…
ELSE
	PRINTFORML 用摄像机開始对%CALLNAME:TARGET%進行摄影…
ENDIF
;-------------------------------------------------
;184,野外PLAY,
;-------------------------------------------------
@MESSAGE_COM184
;解除時
IF !TEQUIP:野外PLAY
	PRINTFORML 回到了房间…
ELSE
	PRINTFORML 给%CALLNAME:TARGET%戴上项圈并牽到了外面…
ENDIF
;-------------------------------------------------
;185,侵犯助手,
;-------------------------------------------------
@MESSAGE_COM185
;前回の調教が侵犯助手であり、抜かないフラグがある場合
IF TFLAG:60 && PREVCOM == 56
	PRINTFORML 在%CALLNAME:TARGET%的注視下、持續侵犯着%CALLNAME:ASSI%…
ELSE
	PRINTFORM 在%CALLNAME:TARGET%的注視下、貫穿了%CALLNAME:ASSI%的
	IF EXP:ASSI:Ｖ経験 == 0
		PRINT 処女穴
	ELSE
		PRINT 小穴
	ENDIF
	PRINTL …
ENDIF
;-------------------------------------------------
;186,羞恥PLAY,
;-------------------------------------------------
@MESSAGE_COM186
;解除時
IF !TEQUIP:羞恥PLAY
	PRINTFORML 带着%CALLNAME:TARGET%从镜子前面离開了…
ELSE
	PRINTFORML 把%CALLNAME:TARGET%带到了镜子前面…
ENDIF
;-------------------------------------------------
;187,浴室PLAY,
;-------------------------------------------------
@MESSAGE_COM187
IF CFLAG:MASTER:現在位置 == OMANEKIBEYA()
	;解除時
	IF !TEQUIP:浴室PLAY
		PRINTFORML 从浴场回来了…
		STR:98 = %CALLNAME:TARGET%的房間
	ELSE
		PRINTFORML 带着%CALLNAME:TARGET%去了浴场…
		STR:98 = %CALLNAME:TARGET%的浴室
	ENDIF
ELSE
	;解除時
	IF !TEQUIP:浴室PLAY
		PRINTFORML 把泳池收起来了…
	ELSE
		PRINTFORML 给泳池放入了温水…
	ENDIF
ENDIF
;-------------------------------------------------
;188,淋浴,
;-------------------------------------------------
@MESSAGE_COM188
IF !TEQUIP:淋浴
	PRINTFORML 中止了对%CALLNAME:TARGET%的淋浴…
ELSE
	PRINTFORML 帮%CALLNAME:TARGET%淋浴了…
ENDIF
;-------------------------------------------------
;189,新妻PLAY,
;-------------------------------------------------
@MESSAGE_COM189
IF !TEQUIP:新妻PLAY
	PRINTFORML 脱下了%CALLNAME:TARGET%的围裙…
ELSE
	PRINTFORML 给%CALLNAME:TARGET%穿上了围裙…
ENDIF
;-------------------------------------------------
;190,圍裙をする,
;-------------------------------------------------
@MESSAGE_COM190
IF !TEQUIP:圍裙
	PRINTFORML 脱下了围裙
ELSE
	PRINTFORML 穿上了围裙
ENDIF
;-------------------------------------------------
;191,睡眠薬,
;-------------------------------------------------
@MESSAGE_COM191
IF CFLAG:MASTER:恶作剧 == 2
	PRINTFORML 往睡眠中的%CALLNAME:TARGET%的肛門里慢慢地把睡眠薬塞了進去
	PRINTFORML %CALLNAME:TARGET%因為直腸吸収了睡眠薬而陥入了昏睡状態
ELSE
	PRINTFORML 往時停中的%CALLNAME:TARGET%的肛門里慢慢地把睡眠薬塞了進去
ENDIF
;-------------------------------------------------
;192,排卵誘発剤,
;-------------------------------------------------
@MESSAGE_COM192
IF CFLAG:MASTER:恶作剧 == 2
	PRINTFORML 往睡眠中的%CALLNAME:TARGET%的嘴里放入排卵誘発剤
ELSE
	PRINTFORML  给%CALLNAME:TARGET%喂下了排卵誘発剤
ENDIF
