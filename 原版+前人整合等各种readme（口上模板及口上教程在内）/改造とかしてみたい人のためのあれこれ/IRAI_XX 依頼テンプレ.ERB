;XXは置換で空いている番号（30まで）に置き換えてください。

;--------------------------------------------
;基本条件いろいろ
;--------------------------------------------
@IRAI_SETTING_XX

;期限、当日中なら0
SETTING_IRAI_DEADLINE:XX =
;依頼主
SETTING_IRAI_CLIENT:XX = 
;対象 依頼主と同じでもいい
SETTING_IRAI_TARGET:XX = 
;依頼実行時のコマンド名
SETTING_IRAI_COMMANDNAME:XX = 


;--------------------------------------------
;依頼発生時のメッセージ
;--------------------------------------------
@IRAI_INITIALMESSAGE_XX

PRINTFORML 「依頼を持ちかけた時のセリフ」
PRINTFORML 依頼内容：
PRINTFORML 報酬　　：

;--------------------------------------------
;依頼実行時のメッセージやソースなどの処理
;--------------------------------------------
@IRAI_CARRYOUT_XX
;ここでRETURN -1した場合、実行されず戻る

;PRINTFORML %CALLNAME:MASTER%は言われたことをやった

;--------------------------------------------
;依頼失敗時のメッセージやソースなどの処理
;--------------------------------------------
@IRAI_FAILED_XX
;依頼を期限切れさせてから会ったときに呼び出される

;システムメッセージつき好感度変化
;最後の引数だけ好感度が変化
;CALL CHANGE_CFLAG(2,TARGET,-5)

;システムメッセージつき信頼度変化
;最後の引数だけ信頼度が変化
;CALL CHANGE_CFLAG(4,TARGET,-3)

;機嫌を悪化させる処理を入れてもいい
;TALENT:機嫌 = LIMIT(TALENT:機嫌 -1, -1,1)

;--------------------------------------------
;依頼達成時のメッセージやソースなどの処理
;--------------------------------------------
@IRAI_SUCCESS_XX
;PRINTFORML 「釣りはいらん、取っておけ」
;MONEY += 10

;システムメッセージつき好感度変化
;最後の引数だけ好感度が変化
;CALL CHANGE_CFLAG(2,TARGET,30)

;システムメッセージつき信頼度変化
;最後の引数だけ信頼度が変化
;CALL CHANGE_CFLAG(4,TARGET,10)

;機嫌をよくする処理を入れてもいい
;TALENT:機嫌 = LIMIT(TALENT:機嫌 + 1, -1, 1)
;--------------------------------------------
;依頼実行条件
;--------------------------------------------
@IRAI_ACTABLE_XX
;これはTARGETに意識があり、ウフフ中でないという条件
;TARGETがSETTING_IRAI_TARGET:XXと一致しない場合ハネられる処理が外部にあるので、TARGETを指定する必要はない

SIF SHIRAHU(TARGET) == 1
	RETURN 1

;--------------------------------------------
;依頼発生条件
;--------------------------------------------
@IRAI_HAPPEN_XX
;1/15になってるけど適当に調整してください
;一行目コメントアウトして無条件でRETURN 1するようにすれば確実に発生するのでテストプレイ時にどうぞ
SIF !RAND:15
	RETURN 1


;--------------------------------------------
;依頼消滅条件
;--------------------------------------------
@IRAI_VANISH_XX
;1/3になってるけど適当に調整してください
;一行目コメントアウトして無条件でRETURN 1するようにすれば（ｒｙ
SIF !RAND:3
	RETURN 1
