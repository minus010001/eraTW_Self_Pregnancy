;-------------------------------------------------
;指令系总览
;-------------------------------------------------
;*********************************************
;施虐系指令说明
;*********************************************
;施虐系指令涉及对对象施加疼痛或拘束的SM行为。根据指令类型，分为打屁股/鞭/蜡烛/针/打胸部等直接施虐指令，
;以及眼罩/绳/口球/拘束PLAY等装备型拘束指令。
;对于装备型指令（104~107号），应分为装备中（TEQUIP:XX为真）和解除时（TEQUIP:XX为假）两个主要分支设计台词。
;典型分支顺序为：时奸判定(FLAG:70) > 初次判定(FIRSTTIME) > 装备/解除分支（仅装备型指令）> 陷落状态嵌套(恋慕/屈服3/思慕/其他)
;101鞭、102蜡烛、103针在Train.csv中标记为未使用，仅供参考模板结构。
;
;关于指令控制系统的完整说明（指令成否判定/数值计算控制/文本控制函数），请参看爱抚系指令模板中的总览。
;这里仅提供文本控制函数模板，按需复制使用。
;
;-------------------------------------------------
;关于剧本标识符
;-------------------------------------------------
;在X2版模板中，各类接口函数均使用 _XXX 作为剧本标识符占位符。
;例如 @M_KOJO_XXX_MESSAGE_COM_KX_100
;其中：
;  _XXX 在创作时替换为角色对应的剧本标识符（如 _K63）
;  KX   为角色编号（如 K49、K63）
;  XX   为指令编号（如 100表示打屁股）
;
;因此完整函数名示例：@M_KOJO_K63_MESSAGE_COM_K49_100
;
[SKIPSTART]
;关于剧本文本控制函数的说明，请参看爱抚系指令中的总览。这里仅提供模板，按需复制使用。
;==================================================
;指令成否判定
;剧本侧覆写式接管指令的结果的规则，以TFLAG:192返回成否鉴定值。0表示剧本侧不干涉COM本体设定，其余值的定义具体参照COMXX。
;==================================================
@M_KOJO_XXX_SUCCESS_COM_KX_XX
#LOCALSIZE 1
#LOCALSSIZE 1
TFLAG:192 = 0

@M_KOJO_XXX_EXTRASOURCE_COM_KX_XX
#LOCALSIZE 1
#LOCALSSIZE 1
;做任何事

@M_KOJO_XXX_MESSAGE_MESSAGECHECK_COM_KX_XX
#LOCALSIZE 1
#LOCALSSIZE 1
RETURN 0

@M_KOJO_XXX_MESSAGE_COM_KX_XX
;#LOCALSIZE 1
;#LOCALSSIZE 1
;可以使用LOCAL与LOCALS。如需使用LOCAL:1，则需要将#LOCALSIZE 1改为#LOCALSIZE 2.依此类推，如需使用LOCAL:X，则需要将#LOCALSIZE 1改为#LOCALSIZE X+1
IF 0
	PRINTFORML
	RETURN 1
ENDIF
RETURN 0

[SKIPEND]

;※※※※※※※※※※※※※※※※※※※※※※※※※
;施虐系指令
;※※※※※※※※※※※※※※※※※※※※※※※※※
;-------------------------------------------------
;总览
;-------------------------------------------------
;维护人：狂气刘俊海
;维护日期：2026年4月29日

;编号100~108号的施虐系指令（SM系）
;*均为诶嘿嘿中指令

;如果不知道指令的含义，或不知道如何设计差分，金标准是参照该指令的情景描写文本。
;本系列的情景描写在EVENT_MESSAGE_COM_SM系.ERB

;-------------------------------------------------
;指令列表
;-------------------------------------------------
;100,打屁股（スパンキング/Spanking）— 用手掌拍打对象的臀部
;101,鞭（鞭/Whip）— [未使用]用鞭子抽打对象的身体
;102,蜡烛（蝋燭/Candle）— [未使用]将灼热的蜡油滴在对象身上
;103,针（針/Needle）— [未使用]用针戳刺对象的敏感部位
;104,眼罩（アイマスク/Blindfold）— 装备型指令，给对象戴上或解除眼罩
;105,绳（縄/Rope）— 装备型指令，用绳子缚住或解开对象
;106,口球（ボールギャグ/Ball Gag）— 装备型指令，给对象塞上或取下口球
;107,拘束PLAY（拘束プレイ/Restraint Play）— 装备型指令，束缚或解除对象的行动
;108,打胸部（乳スパンキング/Breast Spanking）— 用手掌或手指拍打对象的胸部

;==================================================
;基本陷落嵌套模板
;==================================================
[SKIPSTART]
	;恋慕
	IF TALENT:恋慕
		PRINTFORML 
		RETURN 1
	;屈服3
	ELSEIF MARK:不埒刻印 == 3
		PRINTFORML 
		RETURN 1
	;思慕
	ELSEIF TALENT:思慕
		PRINTFORML 
		RETURN 1
	;其他
	ELSE
		PRINTFORML 
		RETURN 1
	ENDIF
[SKIPEND]

;==================================================
;100,打屁股
;==================================================
;说明：用手掌拍打对象的臀部。关键差分：时奸中、初次、通常陷落嵌套。
;受虐属性高时可增加角色兴奋表现的差分。
;情景描写参考@MESSAGE_COM100

@M_KOJO_XXX_MESSAGE_COM_KX_100
#LOCALSIZE 1
#LOCALSSIZE 1

;时奸中
IF FLAG:70
	PRINTFORML 
	RETURN 1
;初次
ELSEIF FIRSTTIME(SELECTCOM)
	PRINTFORML 
	RETURN 1
;通常
ELSE
	;基本陷落嵌套
	;恋慕
	IF TALENT:恋慕
		PRINTFORML 
		RETURN 1
	;屈服3
	ELSEIF MARK:不埒刻印 == 3
		PRINTFORML 
		RETURN 1
	;思慕
	ELSEIF TALENT:思慕
		PRINTFORML 
		RETURN 1
	;其他
	ELSE
		PRINTFORML 
		RETURN 1
	ENDIF
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;101,鞭
;==================================================
;说明：[未使用]用鞭子抽打对象的身体。关键差分：时奸中、初次、通常陷落嵌套。
;此指令在Train.csv中标记为未使用，模板仅供参考结构。
;情景描写参考@MESSAGE_COM101

@M_KOJO_XXX_MESSAGE_COM_KX_101
#LOCALSIZE 1
#LOCALSSIZE 1

;时奸中
IF FLAG:70
	PRINTFORML 
	RETURN 1
;初次
ELSEIF FIRSTTIME(SELECTCOM)
	PRINTFORML 
	RETURN 1
;通常
ELSE
	;基本陷落嵌套
	;恋慕
	IF TALENT:恋慕
		PRINTFORML 
		RETURN 1
	;屈服3
	ELSEIF MARK:不埒刻印 == 3
		PRINTFORML 
		RETURN 1
	;思慕
	ELSEIF TALENT:思慕
		PRINTFORML 
		RETURN 1
	;其他
	ELSE
		PRINTFORML 
		RETURN 1
	ENDIF
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;102,蜡烛
;==================================================
;说明：[未使用]将灼热的蜡油滴在对象身上。关键差分：时奸中、初次、通常陷落嵌套。
;此指令在Train.csv中标记为未使用，模板仅供参考结构。
;可考虑差分：是否被绳缚中(TEQUIP:縄)、对象痛觉(TALENT:痛覚)。
;情景描写参考@MESSAGE_COM102

@M_KOJO_XXX_MESSAGE_COM_KX_102
#LOCALSIZE 1
#LOCALSSIZE 1

;时奸中
IF FLAG:70
	PRINTFORML 
	RETURN 1
;初次
ELSEIF FIRSTTIME(SELECTCOM)
	PRINTFORML 
	RETURN 1
;通常
ELSE
	;基本陷落嵌套
	;恋慕
	IF TALENT:恋慕
		PRINTFORML 
		RETURN 1
	;屈服3
	ELSEIF MARK:不埒刻印 == 3
		PRINTFORML 
		RETURN 1
	;思慕
	ELSEIF TALENT:思慕
		PRINTFORML 
		RETURN 1
	;其他
	ELSE
		PRINTFORML 
		RETURN 1
	ENDIF
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;103,针
;==================================================
;说明：[未使用]用针戳刺对象的敏感部位。关键差分：时奸中、初次、通常陷落嵌套。
;此指令在Train.csv中标记为未使用，模板仅供参考结构。
;可考虑差分：玩家是否擅长用针(TALENT:擅長用針)或施虐狂。
;情景描写参考@MESSAGE_COM103

@M_KOJO_XXX_MESSAGE_COM_KX_103
#LOCALSIZE 1
#LOCALSSIZE 1

;时奸中
IF FLAG:70
	PRINTFORML 
	RETURN 1
;初次
ELSEIF FIRSTTIME(SELECTCOM)
	PRINTFORML 
	RETURN 1
;通常
ELSE
	;基本陷落嵌套
	;恋慕
	IF TALENT:恋慕
		PRINTFORML 
		RETURN 1
	;屈服3
	ELSEIF MARK:不埒刻印 == 3
		PRINTFORML 
		RETURN 1
	;思慕
	ELSEIF TALENT:思慕
		PRINTFORML 
		RETURN 1
	;其他
	ELSE
		PRINTFORML 
		RETURN 1
	ENDIF
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;104,眼罩
;==================================================
;说明：给对象戴上或解除眼罩。装备型指令，通过TEQUIP:眼罩管理装备状态。
;关键差分：时奸中（装着时）、初次、装着/解除分支、基本陷落嵌套（装着时）。
;装着时可考虑差分：时奸中的视觉剥夺反应。
;情景描写参考@MESSAGE_COM104

@M_KOJO_XXX_MESSAGE_COM_KX_104
#LOCALSIZE 1
#LOCALSSIZE 1

;初次
IF FIRSTTIME(SELECTCOM)
	PRINTFORML 
	RETURN 1
;着脱切换
ELSEIF TEQUIP:眼罩
	;装着时
	;时奸中
	IF FLAG:70
		PRINTFORML 
		RETURN 1
	;通常
	ELSE
		;基本陷落嵌套
		;恋慕
		IF TALENT:恋慕
			PRINTFORML 
			RETURN 1
		;屈服3
		ELSEIF MARK:不埒刻印 == 3
			PRINTFORML 
			RETURN 1
		;思慕
		ELSEIF TALENT:思慕
			PRINTFORML 
			RETURN 1
		;其他
		ELSE
			PRINTFORML 
			RETURN 1
		ENDIF
	ENDIF
ELSE
	;解除时
	PRINTFORML 
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;105,绳
;==================================================
;说明：用绳子缚住或解开对象。装备型指令，通过TEQUIP:縄管理装备状态。
;关键差分：时奸中（装着时）、初次、装着/解除分支、基本陷落嵌套（装着时）。
;装着时可考虑差分：时奸中的被缚状态反应、绳缚技巧差分。
;情景描写参考@MESSAGE_COM105

@M_KOJO_XXX_MESSAGE_COM_KX_105
#LOCALSIZE 1
#LOCALSSIZE 1

;初次
IF FIRSTTIME(SELECTCOM)
	PRINTFORML 
	RETURN 1
;着脱切换
ELSEIF TEQUIP:縄
	;装着时
	;时奸中
	IF FLAG:70
		PRINTFORML 
		RETURN 1
	;通常
	ELSE
		;基本陷落嵌套
		;恋慕
		IF TALENT:恋慕
			PRINTFORML 
			RETURN 1
		;屈服3
		ELSEIF MARK:不埒刻印 == 3
			PRINTFORML 
			RETURN 1
		;思慕
		ELSEIF TALENT:思慕
			PRINTFORML 
			RETURN 1
		;其他
		ELSE
			PRINTFORML 
			RETURN 1
		ENDIF
	ENDIF
ELSE
	;解除时
	PRINTFORML 
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;106,口球
;==================================================
;说明：给对象塞上或取下口球。装备型指令，通过TEQUIP:口球管理装备状态。
;关键差分：时奸中（装着时）、初次、装着/解除分支、基本陷落嵌套（装着时）。
;口球装着时角色无法正常说话，可在台词中体现含混不清的发音。
;情景描写参考@MESSAGE_COM106

@M_KOJO_XXX_MESSAGE_COM_KX_106
#LOCALSIZE 1
#LOCALSSIZE 1

;初次
IF FIRSTTIME(SELECTCOM)
	PRINTFORML 
	RETURN 1
;着脱切换
ELSEIF TEQUIP:口球
	;装着时
	;时奸中
	IF FLAG:70
		PRINTFORML 
		RETURN 1
	;通常
	ELSE
		;基本陷落嵌套
		;恋慕
		IF TALENT:恋慕
			PRINTFORML 
			RETURN 1
		;屈服3
		ELSEIF MARK:不埒刻印 == 3
			PRINTFORML 
			RETURN 1
		;思慕
		ELSEIF TALENT:思慕
			PRINTFORML 
			RETURN 1
		;其他
		ELSE
			PRINTFORML 
			RETURN 1
		ENDIF
	ENDIF
ELSE
	;解除时
	PRINTFORML 
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;107,拘束PLAY
;==================================================
;说明：束缚或解除对象的行动。装备型指令，通过TEQUIP:拘束管理装备状态。
;关键差分：时奸中（装着时）、初次、装着/解除分支、基本陷落嵌套（装着时）。
;拘束状态下对象无法自由行动，可在台词中体现无力感或抵抗反应。
;情景描写参考@MESSAGE_COM107

@M_KOJO_XXX_MESSAGE_COM_KX_107
#LOCALSIZE 1
#LOCALSSIZE 1

;初次
IF FIRSTTIME(SELECTCOM)
	PRINTFORML 
	RETURN 1
;着脱切换
ELSEIF TEQUIP:拘束
	;装着时
	;时奸中
	IF FLAG:70
		PRINTFORML 
		RETURN 1
	;通常
	ELSE
		;基本陷落嵌套
		;恋慕
		IF TALENT:恋慕
			PRINTFORML 
			RETURN 1
		;屈服3
		ELSEIF MARK:不埒刻印 == 3
			PRINTFORML 
			RETURN 1
		;思慕
		ELSEIF TALENT:思慕
			PRINTFORML 
			RETURN 1
		;其他
		ELSE
			PRINTFORML 
			RETURN 1
		ENDIF
	ENDIF
ELSE
	;解除时
	PRINTFORML 
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;108,打胸部
;==================================================
;说明：用手掌或手指拍打对象的胸部。关键差分：时奸中、初次、通常陷落嵌套。
;可考虑差分：胸部尺寸(TALENT:胸围)决定拍打方式（贫乳用手指弹、巨乳用手掌拍）。
;受虐属性高时可增加角色兴奋表现的差分。
;情景描写参考@MESSAGE_COM108

@M_KOJO_XXX_MESSAGE_COM_KX_108
#LOCALSIZE 1
#LOCALSSIZE 1

;时奸中
IF FLAG:70
	PRINTFORML 
	RETURN 1
;初次
ELSEIF FIRSTTIME(SELECTCOM)
	PRINTFORML 
	RETURN 1
;通常
ELSE
	;基本陷落嵌套
	;恋慕
	IF TALENT:恋慕
		PRINTFORML 
		RETURN 1
	;屈服3
	ELSEIF MARK:不埒刻印 == 3
		PRINTFORML 
		RETURN 1
	;思慕
	ELSEIF TALENT:思慕
		PRINTFORML 
		RETURN 1
	;其他
	ELSE
		PRINTFORML 
		RETURN 1
	ENDIF
ENDIF
;未成功调用文本，需要返回0
RETURN 0
