;※※※※※※※※※※※※※※※※※※※※※※※※※
;派生指令（SCOM）模板
;更新日期：2026年4月29日
;版本：X2精修版
;※※※※※※※※※※※※※※※※※※※※※※※※※

;======================================================================
;  使用说明
;======================================================================
; 本模板为派生指令（SCOM/Derived Commands）的口上文本模板。
; 派生指令是在特定组合条件下触发的复合指令，编号范围为1~19。
;
; 【剧本标识符说明】
; 模板中使用 _XXX 作为角色剧本标识符（KOJO_NAME）的占位符，
; 脚本创作者在复制本模板到自己的角色文件夹时，
; 需要将 _XXX 替换为自己的剧本标识符（如 K30、K17 等）。
;
; 模板中使用 KX 作为角色编号（NO:TARGET）的占位符，
; 需要将 KX 替换为实际的角色编号（如 K30、K17 等）。
;
; 【函数命名规则】
; 主文本函数：
;   @M_KOJO_XXX_MESSAGE_SCOM_KX_{编号}
;   实际示例：@M_KOJO_K30_MESSAGE_SCOM_K30_1
;
; 文本控制函数（MESSAGECHECK，按需取消注释）：
;   @M_KOJO_XXX_MESSAGE_MESSAGECHECK_SCOM_KX_{编号}
;
; 搭档文本函数（仅多人指令需要）：
;   @M_KOJO_XXX_MESSAGE_SCOM_KX_{编号}_2
;
;======================================================================
;  调用流程（SCOM 分发机制）
;======================================================================
; 在剧本文本的统一调用接口 @KOJO_MESSAGE_SEND 中：
;
;   IF TFLAG:50                                    ; 存在派生指令编号
;     LOCAL = TFLAG:50                             ; 取出SCOM编号（1~19）
;     LOCALS = "SCOM"                              ; 设定分发标识
;   ELSE
;     LOCAL = SELECTCOM                            ; 普通指令则使用SELECTCOM
;     LOCALS = "COM"
;   ENDIF
;
;   ; 最终调用形式（由系统自动拼接）：
;   TRYCALLFORM M_KOJO%KOJO_NAME%_MESSAGE_SCOM_K{NO:TARGET}_{LOCAL}
;   ; 实际展开示例（以角色编号K17、剧本标识符K17、SCOM编号5为例）：
;   ; TRYCALLFORM M_KOJO_K17_MESSAGE_SCOM_K17_5
;
; 【重要提示】
; • TFLAG:50 存放的是当前执行的派生指令编号
; • SELECTCOM 存放的是触发派生指令的原始指令编号
; • 派生指令的初次体验判定使用 FIRSTTIME(TFLAG:50 + 500)
;   因为CSV中派生指令的编号范围是501~519
;
;======================================================================
;  文本控制函数 MESSAGECHECK
;======================================================================
; MESSAGECHECK 用于控制何时需要略过通用的情景描写以及剧本台词。
; 返回值组合一览：
; ────────────────────────────────────
; RESULT 值 | 略过通用描写 | 略过台词  | 典型使用场景
; ────────────────────────────────────
;     0     |  ×（显示）   | ×（显示） | 正常显示所有文本
;     1     |  ○（略过）   | ×（显示） | 剧本侧希望重写行为描述
;     2     |  ×（显示）   | ○（略过） | 控制台词是否略过
;     3     |  ○（略过）   | ○（略过） | 取消所有文本信息
; ────────────────────────────────────
;
; 模板（按需取消注释并添加到对应指令前）：
;@M_KOJO_XXX_MESSAGE_MESSAGECHECK_SCOM_KX_XX
;#LOCALSIZE 1
;#LOCALSSIZE 1
;RETURN 0


;==================================================
;1,六九式
;日文：シックスナイン
;双方互相口交的体位
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_1
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;2,岩清水
;日文：岩清水
;刺激阴道壁使其大量分泌爱液的指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_2
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;3,G点刺激
;日文：Gスポット刺激
;用指交或道具刺激G点的指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_3
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;4,乱牡丹
;日文：乱れ牡丹
;69姿势中同时刺激女方的阴部和肛门的指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_4
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;5,手淫口交
;日文：手淫フェラ
;一边手淫一边口交的指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_5
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;6,插入G点玩弄
;日文：挿入Ｇスポ責め
;插入状态下针对G点的刺激
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_6
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;7,插入子宫口玩弄
;日文：挿入子宮口責め
;插入状态下针对子宫口的刺激
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_7
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;8,69乳交
;日文：６９パイズリ
;69姿势中使用乳交刺激的指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_8
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;9,双重口交
;日文：ダブルフェラ
;两名角色同时为主人口交的多人指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_9
#LOCALSIZE 1
#LOCALSSIZE 1
;当前TARGET身份判定
;此处使用MASTER_POSE确定角色在多人动作中的位置
;可根据需求添加具体条件分支
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;搭档用文本函数
;TARGET被重定向为剧本角色，此时剧本角色并不是指令的执行对象
;搭档文本会在真正的执行对象调用文本后再调用
@M_KOJO_XXX_MESSAGE_SCOM_KX_9_2
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;10,双重素股
;日文：ダブル素股
;两名角色同时用大腿夹住主人肉棒的多人指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_10
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;搭档用文本函数
@M_KOJO_XXX_MESSAGE_SCOM_KX_10_2
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;11,双重乳交
;日文：ダブルパイズリ
;两名角色同时用胸部为主人服务的多人指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_11
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;搭档用文本函数
@M_KOJO_XXX_MESSAGE_SCOM_KX_11_2
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;12,乳夹口交
;日文：パイズリフェラ
;用乳交夹住肉棒的同时进行口交的指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_12
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(SELECTCOM)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;13,交互插入
;日文：交互挿入
;两名角色轮流被插入的多人指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_13
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;搭档用文本函数
@M_KOJO_XXX_MESSAGE_SCOM_KX_13_2
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;14,玩弄S状结肠
;日文：Ｓ状結腸責め
;通过肛门深部刺激S状结肠的指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_14
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;15,隔着刺激阴道
;日文：膣裏責め
;从肛门侧隔着阴道壁进行刺激的指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_15
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;17,同时吸乳头
;日文：乳首同時吸い
;（此编号在CSV中对应517，跳过16号）
;两名角色同时吸吮乳头的多人指令
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_17
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;时奸中
IF FLAG:70
	PRINTFORML
	RETURN 1
;恋慕
ELSEIF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0

;==================================================
;19,授乳play
;日文：授乳プレイ
;（此编号在CSV中对应519，跳过18号）
;模仿哺乳行为的指令，需要角色处于乳汁分泌状态
;==================================================
@M_KOJO_XXX_MESSAGE_SCOM_KX_19
#LOCALSIZE 1
#LOCALSSIZE 1
;初次体验
IF FIRSTTIME(TFLAG:50 + 500)
	PRINTFORML
	RETURN 1
ENDIF
;恋慕
IF TALENT:恋慕
	PRINTFORML
	RETURN 1
;屈服刻印3
ELSEIF MARK:不埒刻印 == 3
	PRINTFORML
	RETURN 1
;其他
ELSE
	PRINTFORML
	RETURN 1
ENDIF
;未成功调用文本，需要返回0
RETURN 0
