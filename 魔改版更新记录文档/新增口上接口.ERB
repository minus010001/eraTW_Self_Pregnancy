;通用的SUCCESS口上接口
@M_KOJO_XXX_SUCCESS_KXX_GENERAL

;自定义shop按钮
;Y为按钮编号,范围为0-9共10个
custom_button_name:y
;判断是否可以显示自定义按钮,返回1时会显示对应的按钮
@KOJO_XXX_CUSTOM_BUTTON_CONDITION_KXX_Y
;点击对应按钮时的函数
@KOJO_XXX_CUSTOM_BUTTON_KXX_Y

;自定义素质(抄袭由ckr制作的ym口上模板)
;xx范围为0-29,共30个
;这个变量决定是否会显示
custom_talent:xx
;决定显示的名称
custom_talent_name:xx
;决定显示的颜色(0为GETDEFCOLOR())
custom_talent_color:xx

;自定义com按钮
;y为按钮编号,范围0-29共30个
@KOJO_XXX_COM_NAME_KXX_Y
	RESULTS = com名称
;按钮出现条件
;返回1时出现,返回0时不出现
@KOJO_XXX_COM_ABLE_KXX_Y
	RETURN 1
;指令具体内容,写法参照已有的com函数
@KOJO_XXX_COM_KXX_Y
;写法参照@CAN_COM9(ARG)
@KOJO_XXX_CAN_COM_KXX_Y(skipFlag)
	#DIM skipFlag
	SKIPDISP skipFlag
	SKIPDISP 0

;读档口上自动更新
;返回当前口上版本
@KOJO_XXX_VERSION_KXX
	RETURN 当前口上版本
;存档里的版本小于上一个函数的返回值时调用
@KOJO_XXX_VERSION_UPDATE_KXX
	;更新时的数据处理
	RETURN 当前口上版本