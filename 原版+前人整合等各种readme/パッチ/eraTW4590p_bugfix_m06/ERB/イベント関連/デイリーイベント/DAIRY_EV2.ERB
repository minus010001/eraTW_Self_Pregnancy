@DAIRY_CONDITION_EV2

IF !RAND:10 && TALENT:MASTER:120				;←ここに発生条件を入れよう　例として30分の1にしてるけど適当に変えてね
	CALL DAIRY_EV2
	RETURN 1
ELSE
	RETURN -1
ENDIF

@DAIRY_EV2
#DIMS MAS_HOME
PRINTFORML 夜中、%CALLNAME:MASTER%は幸せな感覚がして目を覚ました
PRINTFORMW なんだか股間が温かい……
PRINTFORML ！！！！　夢精してしまっている！
PRINTFORMW もっと有意義な射し方をすればよかった…　%CALLNAME:MASTER%は深く後悔した
FLAG:71 = 0
TALENT:MASTER:120 = 0
BASE:MASTER:精力 -= 500
EXP:MASTER:射精経験 ++
CALL FISHER_YATES_SHAFFLE(CHARANUM)
LOCAL:1 = -1
MAS_HOME '= AREANAME_CHARA_HOME(MASTER)
FOR LOCAL, 0, CHARANUM
	;対象があなた
	SIF SHAFFLE_ARRAY:LOCAL == MASTER
		CONTINUE
	;でてこない
	SIF CFLAG:(SHAFFLE_ARRAY:LOCAL):出禁
		CONTINUE
	;同居してないよ
	;判定が多少緩いけど前よりはマシ
	SIF AREANAME_CHARA_HOME(SHAFFLE_ARRAY:LOCAL) != MAS_HOME
		CONTINUE
	LOCAL:1 = SHAFFLE_ARRAY:LOCAL
	BREAK
NEXT
SIF LOCAL:1 <= 0
	RETURN

PRINTFORML %CALLNAME:MASTER%が夢精で汚した下着を洗っていると%CALLNAME:(LOCAL:1)%が飛んできた！
PRINTFORMW %CALLNAME:(LOCAL:1)%はそんなもの他の洗濯物と一緒に洗わないでと抗議すると、%CALLNAME:MASTER%の手に収まった下着をふんだくり戻っていった…
JUEL:(LOCAL:1):11 += 1000
PRINTFORMW %CALLNAME:(LOCAL:1)%　欲情の珠＋1000
RETURN 1

