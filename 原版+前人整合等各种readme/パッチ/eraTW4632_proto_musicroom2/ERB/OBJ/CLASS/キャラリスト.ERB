;キャラリスト
;要素名→ID変換用関数の存否確認用関数
;変換可能（IDと要素名の関係が常に変化しない）なら設置
;状況によって名前が変わるようなのは不可能
@EXIST_キャラリスト_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;要素名→ID変換用関数
;必要なら各オブジェクトで同様に作成のこと
@キャラリスト_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;作成する変数名
#DIMS C_NAME, OBJ_ID_LAST + 1 ;データ保持
#DIM  O_ID                    ;参照用
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "キャラリスト", O_ID, "名前")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



@EXIST_キャラリスト1
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@キャラリスト1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "存在するキャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "存在")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_キャラリスト2
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "主人公以外のキャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER || !CHECK_CHARA(ARG, "存在")
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT


@EXIST_キャラリスト3
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "陥落ずみキャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "陥落")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト4
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "清い交際取得キャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "清い交際")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT





@EXIST_キャラリスト6
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "主人公の弱みを握るキャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "弱み握り")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_キャラリスト7
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "主人公に弱みがあるキャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "弱み握られ")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_キャラリスト8
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト8(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "反発刻印もちキャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "反発刻印")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_キャラリスト9
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト9(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "時姦刻印もちキャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "時姦刻印")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_キャラリスト10
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "同室にいるキャラリスト")
CASE "判定"
	SIF CHECK_CHARA(ARG, "同室")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT


@EXIST_キャラリスト11
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト11(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "口上もちキャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "口上実装状況")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_キャラリスト12
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト12(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "妊娠中キャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "妊娠")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_キャラリスト13
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト13(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "育児中キャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "育児中")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_キャラリスト14
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト14(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "子持ちキャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	SIF CHECK_CHARA(ARG, "子持ち")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

;複合条件系


@EXIST_キャラリスト31
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@キャラリスト31(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "着せ替え可能キャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
;着せ替えることができる
	IF ARG == MASTER
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF !CHECK_CHARA(ARG, "存在") || !CHECK_CHARA(ARG, "同室")
	ELSEIF FLAG:時間停止 == 1
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF 睡眠時間(ARG)
	ELSEIF CHECK_CHARA(ARG, "陥落")
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF CHECK_CHARA(ARG, "弱み握り")
	ELSEIF CHECK_CHARA(ARG, "弱み握られ")
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
	ENDIF
ENDSELECT



@EXIST_キャラリスト32
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト32(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "普段着模倣可能キャラリスト")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF CHECK_CHARA(ARG, "弱み握り")
	ELSEIF CHECK_CHARA(ARG, "陥落") || CHECK_CHARA(ARG, "弱み握られ")
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
	ENDIF
ENDSELECT



@EXIST_キャラリスト33
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト33(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "TARGETリスト")
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在") || !CHECK_CHARA(ARG, "同室")
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT





;51～整数ナンバータイトルの登場キャラリストに使用することに
@EXIST_キャラリスト51
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト51(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方靈異伝登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魅魔]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト52
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト52(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方封魔録登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[里香]], [[明羅]], [[魔理沙]], [[魅魔]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト53
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト53(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方夢時空登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魅魔]], 	[[魔理沙]], [[エレン]], [[カナ]], [[理香子]], [[ちゆり]], [[夢美]], [[る～こと]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
	[SKIPSTART]
	ひとりだけみじっそうであった
	[[小兎姫]]
	[SKIPEND]
ENDSELECT



@EXIST_キャラリスト54
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト54(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方幻想郷登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[くるみ]], [[エリー]], [[幽香]], [[夢月]], [[幻月]])
		CALLF MAKE_INT(V_NAME, 1)
		[SKIPSTART]
		[[オレンジ]]
		[SKIPEND]
	ENDIF
ENDSELECT



@EXIST_キャラリスト55
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト55(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方怪綺談登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[魅魔]], [[幽香]], [[ルイズ]], [[アリス]], [[ユキ]], [[マイ]], [[夢子]], [[神綺]])
		CALLF MAKE_INT(V_NAME, 1)
		[SKIPSTART]
		[[サラ]]
		[SKIPEND]
	ENDIF
ENDSELECT



@EXIST_キャラリスト56
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト56(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方紅魔郷登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[ルーミア]], [[大妖精]], [[チルノ]], [[美鈴]], [[小悪魔]], [[パチュリー]], [[咲夜]], [[レミリア]], [[フラン]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト57
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト57(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方妖々夢登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[咲夜]], [[チルノ]], [[レティ]], [[橙]], [[アリス]], [[リリーＷ]], [[ルナサ]], [[リリカ]], [[メルラン]], [[妖夢]], [[幽々子]], [[藍]], [[紫]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト58
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト58(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方永夜抄登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[紫]], [[魔理沙]], [[アリス]], [[咲夜]], [[レミリア]], [[妖夢]], [[幽々子]], [[リグル]], [[ミスティア]], [[慧音]], [[てゐ]], [[鈴仙]], [[永琳]], [[輝夜]], [[妹紅]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト59
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト59(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方花映塚登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[咲夜]], [[妖夢]], [[鈴仙]], [[チルノ]], [[ルナサ]], [[リリカ]], [[メルラン]], [[ミスティア]], [[てゐ]], [[文]], [[メディスン]], [[幽香]], [[小町]], [[映姫]], [[リリーＷ]], [[リリーＢ]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト60
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト60(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方風神録登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[静葉]], [[穣子]], [[雛]], [[にとり]], [[椛]], [[文]], [[早苗]], [[神奈子]], [[諏訪子]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト61
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト61(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方地霊殿登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[紫]], [[萃香]], [[文]], [[魔理沙]], [[アリス]], [[パチュリー]], [[にとり]], [[キスメ]], [[ヤマメ]], [[パルスィ]], [[勇儀]], [[お燐]], [[さとり]], [[お空]], [[早苗]], [[こいし]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト62
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト62(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方星蓮船登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[早苗]], [[ナズーリン]], [[小傘]], [[一輪]], [[ムラサ]], [[星]], [[白蓮]], [[ぬえ]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト63
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト63(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方神霊廟登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[早苗]], [[妖夢]], [[幽々子]], [[響子]], [[小傘]], [[芳香]], [[青娥]], [[屠自古]], [[布都]], [[神子]], [[ぬえ]], [[マミゾウ]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト64
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト64(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方輝針城登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[咲夜]], [[チルノ]], [[わかさぎ姫]], [[蛮奇]], [[影狼]], [[弁々]], [[八橋]], [[正邪]], [[針妙丸]], [[雷鼓]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト65
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト65(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方紺珠伝登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[早苗]], [[鈴仙]], [[清蘭]], [[鈴瑚]], [[ドレミー]], [[サグメ]], [[クラウンピース]], [[純狐]], [[ﾍｶｰﾃｨｱ]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_キャラリスト66
#LOCALSIZE 1
#LOCALSSIZE 1

@キャラリスト66(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名前"
	CALLF MAKE_STR(V_NAME, "東方天空璋登場キャラ")
CASE "SHOP表示"
	CALLF MAKE_INT(V_NAME, 1)
CASE "判定"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "存在")
	ELSEIF GROUPMATCH(ARG, [[霊夢]], [[魔理沙]], [[文]], [[チルノ]], [[ラルバ]], [[ネムノ]], [[リリーＷ]], [[あうん]], [[成美]], [[里乃]], [[舞]], [[隠岐奈]])
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT
